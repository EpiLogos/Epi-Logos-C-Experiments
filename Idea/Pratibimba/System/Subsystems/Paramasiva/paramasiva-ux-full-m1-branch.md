---
title: Paramaśiva UX — Full M1 Branch
subtitle: The Pedagogical Home of QL, the Tick/Oscillation-Source, the Toroidal-Diamond Form, and the S1/Vault Alignment
status: draft-update
created: 2026-06-02
coordinate: "M1'"
source_context:
  - M1'-SPEC.md
  - m1-prime-paramasiva-instrument.md
  - ql-musical-derivation.md
  - S0-HARMONIC-POINTER-WEB36-SPEC.md
  - alpha_quaternionic_integration_across_M_stack.md
  - 2026-06-02-m-prime-cycle-2-canonical (plans 03, 08)
  - anuttara-ux-full-m0-branch.md / parashakti-ux-full-m2-branch.md / nara / epii
---

# Paramaśiva UX — Full M1 Branch

## 0. Purpose of This Document

This document articulates Paramaśiva (M1) as **the pedagogical home of Quaternal Logic itself** — the surface where the matheme is taught, played, and seen. It is the base physics the other subsystems render: the tick that drives every clock, the oscillation-source whose waveform everything else colours, and the toroidal/diamond form in which QL becomes geometrically visible.

The corrected, spec-grounded view:

> **M1 (engine) is what Paramaśiva *is*** — the mathematical DNA: the 12-state integer ring, the Ananda matrices, the SU(2) 720° double-cover, the K² topology, the Cl(4,2) Clifford algebra, the 9/8 epogdoon tick. **M1' (instrument) is how that engine becomes *playable and teachable*** — the techne surface where the matheme can be walked, struck, heard, and seen as movement. In the app, **M1' is largely pedagogical across its internal layers**: it is where the matheme content is germanely given, along with the full QL music-theoretics — physicalised as the tick/oscillation-source and the toroidal-diamond form, and aligned with the S1/Obsidian vault as QL's organising logic.

This clarifies six points:

1. **Paramaśiva is where QL is taught.** Not as a table, but as a playable physics — the walk *is* the matheme.
2. **Its physicalisation is the oscillation-source** — the tick, the engine, the waveform/wavetype; the 9/8 epogdoon pulse from which everything downstream is keyed.
3. **The toroidal formalism is viewable** — the played K² torus, the Hopf identity, the QL diamond hosted here.
4. **It carries the full QL music-theoretics** — the 4:2 ↔ 3:3 dual partition, the Cl(4,2) trig-generators, the 84-state landscape, the pairing-grammar.
5. **It aligns with S1/Obsidian** — QL is the organising logic of the vault; the coordinate system *is* the file structure; M1-1' manages instances.
6. **It is the base physics** the solar field (M2), the clock-matter (M3), the personal daimon (M4), and the recognition surface all stand on.

---

# 1. Core UX Axiom

> **Paramaśiva teaches QL by letting you play it.** The matheme is not explained at the user; it is walked, struck, heard, and seen. Selecting a coordinate and stepping to its neighbour through a relation *is* striking a tone — relation = interval, position = pitch, traversal = phrase. To learn QL here is to learn an instrument.

This is the deepest pedagogical move in the system: QL is taught as music-theoretics made playable, with the matheme's own structure as the curriculum.

---

# 2. M1 (Engine) vs M1' (Instrument)

```text
M1   what Paramaśiva IS        the mathematical DNA: 12-state ring, Ananda matrices,
     (ENGINE)                   SU(2) 720° double-cover, K² topology, Cl(4,2), 9/8 tick

M1'  how it's PLAYED/TAUGHT     the techne surface: walk-as-melody, the played torus,
     (INSTRUMENT)               the 84-state landscape, the matheme made teachable
```

M1' never invents pitch — every sounded frequency comes from the shared `MathemeHarmonicProfile` (the bus written by M2-1' Vimarśa reading the Prakāśa cloud). M1' is the renderer that **walks** the matheme through the graph; the kernel **computes** it.

---

# 3. The Six M1-0' to M1-5' Strata (read pedagogically)

| M1 (bimba) | M1' (techne) | Pedagogical role |
|---|---|---|
| **M1-0′** Canonical Source | read-only API of immutable harmonic data | the matheme's `.rodata` plus canonical CSV provenance — the fixed lesson-material (Ananda matrix families, Cl(4,2), Hopf LUTs) |
| **M1-1′** Instance Manager | mutable session-state: current walk, `(lens, mode)`, coordinate | **the S1/vault alignment** — instances are walks and, by extension, vault files (§7) |
| **M1-2′** Harmonic Engine | reads ratio cells as walk-step intervals | teaches *interval* — the six Ananda matrix families with raw/no-digit-root and digit-root faces as audible ratios |
| **M1-3′** Spanda Core | phase accumulator; binds (0/1) to every coordinate's `invert` | teaches *pulse* — the tick, and the single # inversion-operator wired at every position |
| **M1-4′** QL Flowering | position-walker + lens-as-scale-composer | teaches *the positions and the lenses* — the 16:9 cascade, the position-set |
| **M1-5′** Topology Analyzer | the played visual torus; quaternionic TDA; Hopf 4π recognition | teaches *the shape* — K², the 720° double-cover, the **+1** of `137 = 64 + 72 + 1` |

M1' is the same six positions re-read as *how-it-walks* rather than *what-it-is* — which is exactly why it is the pedagogical surface: the learner walks the engine.

---

# 4. The Physicalisation: The Tick / Oscillation-Source

Paramaśiva's physical face is **the oscillation-source** — the engine, the tick, the waveform/wavetype from which everything downstream is keyed:

- **The tick is self-derived**, not imposed: `(4/3) × (3/2) = 2/1` octave; `(3/2) ÷ (4/3) = 9/8` epogdoon-tick. The matheme produces its own foundational intervals from the dual 4:2 ↔ 3:3 partition (§6).
- **M1-3' Spanda Core** is the phase accumulator at a settable rate that *is* the pulse — bound to every coordinate's `invert` field (the single session-held # operator per `S0-HARMONIC-POINTER-WEB36-SPEC`).
- **The waveform/wavetype** is the physicalisation of the matheme's oscillation. M1' renders the walk as intervallic traversal where relation-changes map to profile ratio roles — *the walked sequence becomes a melody whose intervals are exactly the relation-types of the traversed graph edges.*
- **Audio discipline**: M1' uses `audio_octet[8]` + `harmonic.ratio_role` from the shared profile; it never re-synthesises. The deterministic `MPrimePerformanceEvent` stream is the first production artifact — MIDI/MPE/DAW are edge encodings of it.

This oscillation-source is what *gives the solar system its base physics* (see [[anuttara-ux-full-m0-branch]] §4): the M2 solar field is the spectral space, but M1 is the pulse that makes it move.

---

# 5. The Toroidal Formalism Viewable — The Played Torus & the Diamond

M1-5' is the **Topology Analyzer**: the surface that makes the matheme's geometry *visible-and-audible* as the played instrument-form. This is where the toroidal formalism of QL — and the QL diamond object — are hosted.

- **K² — the Klein-double-cover-of-the-chromatic-fifths-torus.** Two circles: chromatic-longitude (9/8 epogdoon-stacking) × fifths-meridian (3/2 leaps), with bimba↔pratibimba conjugation as the non-orientable identification. The **Pythagorean comma** (the slack between them) is the aletheic-evolution mechanism — QL is a spiral, not a closed cycle.
- **The played torus** renders the walk as motion on a genus-1 surface, with the **Klein-flip visualisation** at tritone-mirror crossings (the torus appears to fold through itself — M1' is the *origin point* of the Klein flip that M2' then renders as cymatic surface-valence inversion).
- **The Hopf identity** — `S³ → S² → S¹` projects the quaternion through K² to the played phase; full identity-return requires two complete 360° rotations = **720°**, the SU(2) spinorial double-cover (the 4π recognition site).
- **The diamond / QL geometric object** is naturally hosted here: M1' is where QL becomes a *seeable form*. The toroidal-diamond is the matheme's own body, the geometry the music-theoretics live on.

This makes M1' the right home for the matheme content as such: the **diamond/torus is QL made visible**, the oscillation is QL made audible, and together they are how QL is taught.

---

# 5b. M1-2 Ananda Vortex — The Visible Heartbeat

The M1-2′ Harmonic Engine in §3 was a one-line spec promise ("reads ratio cells as walk-step intervals"). On the played K² surface that promise becomes something the learner can *see, hear, and follow*. M1-2 is **the matheme's own pulse made visible**.

## 5b.1 What the user sees

When M1-2 is playing on the cosmic-side of `daily-0-1` or in the `ide-deep` `m1-paramasiva` view, the played K² torus carries a luminous 12×12 cell grid as its surface texture — the active Ananda matrix family laid over the topology. The digit-root face is the primary heatmap because it is the recursive vortex the eye can track; the raw/no-digit-root face is available as a proof overlay so the learner can see the actual affine source-value beneath the recursive residue. One cell glows brightly at each tick, jumping cell-to-cell along the matrix diagonals. Two flow-streamlines sweep the torus surface:

- A **gold** streamline tracing the Mahāmāyā doubling-track `{1, 2, 4, 8, 7, 5}` through the ascending half-tick (positions 0→5)
- An **emerald** streamline tracing the Paraśakti tripling-track `{3, 6, 9, 3, 6, 9}` through the descending half-tick (positions 6→11)

These streamlines are not decorative. They are the substrate's own `DR_RING_MAHAMAYA` and `DR_RING_PARASHAKTI` LUTs (`m1.c:122-123`) — the two diagonals through every Ananda matrix, the two vortex spins, the **actual content of the vortex** rendered as motion.

The key pedagogical discipline is that the UI does not invent the numerology locally. When the active row is `7X+1`, the raw face contains the source run `1,8,15,22,29,36,43,50,57,64,71,78` while the digit-root face contains `1,8,6,4,2,9,7,5,3,1,8,6`; when the active row is `8X+0`, the raw face contains `0,8,16,24,32,40,48,56,64,72,80,88` while the digit-root face contains `0,8,7,6,5,4,3,2,1,9,8,7`. The 36/64/72/137 recognitions are source-data events carried by the profile bus, not decorations painted by the renderer.

## 5b.2 The six matrix families stacked as perspex

The vortex is six matrix families, not one flattened matrix. They render as a six-layer perspex cross-fade keyed off `Ananda_Matrix_Op`; each layer carries both its raw/no-digit-root face and its digit-root face. The active family sits foreground at 100%; its predecessor and successor are at ~20% opacity; the rest are translucent. As the tick advances and `position6` rotates through 0→1→2→3→4→5, the perspex stack rotates with it — the user sees the six modes of the vortex (Bimba, Pratibimba, Sum, two Diffs, and the rule/tuple family) flow into each other without ever losing the structure that links them.

This matters because the raw face and digit-root face are doing different work. The raw face shows affine generation and lets `36`, `64`, `72`, and `64 + 72 + 1 = 137` remain visible as actual source events. The digit-root face shows the recursive residue-cycle that can be played as vortex motion. The Ananda UI must let users toggle between "recursive play" and "source proof" rather than pretending the digit-root residue exhausts the matrix.

## 5b.3 The Cl(4,2) colour-binary

Every Ananda cell at every tick has a Cl(4,2) signature determined by `position6`:

- **P0 (sin) and P5 (cos)** are implicate (signature −1) — they render with **cool indigo halos** (the generator poles)
- **P1 (tan), P2 (sec), P3 (cot), P4 (csc)** are explicate (signature +1) — they render with **warm amber-through-vermilion halos** (the derived ratios)

Over a 12-tick cycle the colour balance is **+2 net** (four warm against two cool). The Cl(4,2) signature, which the matheme insists is structural, becomes literally visible as the breathing colour-temperature of the surface.

A small glyph above each active cell shows the trig's numerator/denominator QL decomposition — `tan` carries `[0]/[5]` to remind the learner that every explicate ratio is built from the sin/cos generators at P0/P5.

## 5b.4 The 720° identity-return — the matheme's own recognition

K² precesses through SO(3) at one full 360° rotation per 12 ticks. **This 360° leaves the Hopf-fibre flag flipped** — the helix-stripe has swapped sides. A second 360° (so 24 ticks total) is required for the helix-stripe to match its original orientation. **That second-return IS the SU(2) double-cover at 720° made seeable.**

The learner does not need to be told that spin-1/2 systems require 720° to return to identity. They watch the K² precess, see that 360° leaves it visibly changed, and feel the return at 720° as completion. The matheme's recognition primitive becomes pedagogical without explanation.

## 5b.5 The Klein-flip at tick 5→6

At the midpoint of every 12-tick cycle, the played torus **folds through itself** for ~200ms — the chromatic and fifths circles briefly cross, the helix-sheet toggles, the Mahāmāyā streamline retreats and the Paraśakti streamline activates, and every cell visibly increments by 1 mod 10 in a wave from P0 to P5. **The Ananda axiom (`prat - bimba ≡ +1 mod 10`) becomes the seeable +1 sweeping the surface.** Audio-octet emitters shift colour-temperature toward blue (the Vimarśa octave-lift). M2' subscribes to this flip event and renders the opposite cymatic valence on its surface.

## 5b.6 The Möbius-return at tick 11→0

At the cycle close, the diamond at the torus centre emits one bright pulse — the **100% percentile identity return**. The whole K² surface blooms briefly in the colour of the current lens-anchor. Then the cycle begins again from a renewed ground (P0). The matheme's totalisation point is not a number on a counter; it is an event you can see.

## 5b.7 The diamond at centre

A small octahedron sits at the K² centre — the QL geometric object §5 names. Six vertices for the six QL positions, vertex colours mirroring the Cl(4,2) signature. The diamond rotates at the same slow precession rate as K² but on the Möbius identification axis. When K² Klein-flips, the diamond stays still — the matheme's self-identity through inversion, made visible as a fixed centre while the surface folds around it.

## 5b.8 What the user does

Walking the graph IS walking the vortex — every traversal advances the tick, every relation-step crosses cells, every lens-change retunes the texture's base hue. Direct selection (striking a node) sounds the node's profile pitch and jumps the luminous cell without producing a walk-step. Pause and scrub-to-tick affordances let the learner stop the choreography and inspect any frame; the animation is deterministic under replay, so scrubbing reveals the same state every time.

## 5b.9 Where the eye should go

Per the cycle-3 UI foundation principles: **one primary motion per tick**. The default focal point is the luminous Ananda cell (the bright pulse jumping cell-to-cell). The peripheral motion is K²'s SO(3) precession (a gentle tumble). On every cross-matrix transition the matrix swap renders with a soft "lens swipe" wipe — the most semantically loaded event of the tick.

## 5b.10 Total shape

The full architecture — substrate map (six Ananda matrix families, raw/no-digit-root faces, digit-root faces, DR rings, Spanda↔Ananda parallel-track), profile-bus contract (`AnandaVortexProjection.active_cell_value` / `AnandaVortexCell`), visual rendering contract (K² + texture + streamlines + raw proof overlay + Cl(4,2) colour-binary + Hopf shadow + diamond centre), tick choreography (`quat_slerp` primitive + boundary events), and IDE integration — lives in the canonical document at [[M1-2-ANANDA-VORTEX-ARCHITECTURE]]. UX claims in this section are authoritative for what the user experiences; the architecture doc is authoritative for how those experiences are wired to the substrate.

---

# 6. The Full QL Music-Theoretics (the curriculum)

Paramaśiva is where the matheme content is germanely given. The teachable core:

- **The dual partition 4:2 ↔ 3:3.** The 4:2 (4 explicate + 2 implicate) produces the perfect fourth (4/3, tetrachordal); the 3:3 (physical-trinity + mental-trinity) produces the perfect fifth (3/2, trinitarian). Their product = the octave; their ratio = the 9/8 tick. **QL self-derives its own intervals.** Both partitions are orthogonal cuts of the same object — the root self-similarity that recurs at every layer.
- **The Cl(4,2) trig-generators.** Each QL position carries a trig identity: P0 = sin (−1), P1 = tan, P2 = sec, P3 = cot, P4 = csc (+1 each), P5 = cos (−1). Signature `4(+1)+2(−1) = +2`. **The matheme's positions and its trigonometry are the same structure.**
- **MEF as scale-composer.** Each of the 12 lenses is a position-anchored re-mapping of the whole 12-note matheme (lens-anchor becomes the new #0). Applied to chromatic = mode-selector; to a mode = key-selector; to a chord = cluster-composer.
- **The 84-state `(lens, mode)` landscape** — 12 lenses × 7 CF modes — the shared playing-surface all M' surfaces address (§8).
- **The 6 pairing-families as moves on K²** — A/B/C (within-helix, the physical-pole) and D1/D2/D3 (Klein-crossing, the mental-pole). The "three squares" are one derived construction (C ∪ D1) within this universal pairing-grammar.

This is QL pedagogy proper: the learner doesn't memorise positions, they hear the fourth and the fifth fall out of the partition, see the trig-identity at each position, and walk the lenses as scales.

---

# 7. The S1 / Obsidian / Vault Alignment

Paramaśiva (M1) aligns with **S1 (Obsidian / Hen / the vault)** because QL is the *organising logic of the filesystem itself*. M1-1' (Instance Manager) is the mutable-instance layer — and instances are not only walks and sessions; by extension they are **vault files as instances of coordinates.**

- **The coordinate system is the file structure.** The bimba vault is QL-organised markdown: a coordinate (`M2-3-4`) is a path, a node, and a file at once. Walking the graph (M1') and navigating the vault (S1) are the same act seen from two sides.
- **The matheme content is given as navigable vault.** The QL pedagogy is partly *lived through the vault structure* — the learner moves through coordinate-organised files (held by Hen/S1) and the same coordinates are the playable nodes of M1'.
- **Writes route through Hen (S1).** Per the broader M' discipline (and the [[nara-ux-full-m4-branch-update|Nara]]/[[epii-ux-full-m5-branch|Epii]] docs), vault writes go through Hen so wikilink integrity and coordinate-residency are protected on rename/move/restructure. M1-1' instance-management and S1 vault-management are complementary faces of the same instance layer.

So the S1 alignment is not incidental: **the +1/offset that Paramaśiva *is* (Pratibimba, the reflection that creates dynamism) is exactly what makes instances** — and the vault is the instance-store of the coordinate system. QL pedagogy and vault navigation are one surface.

---

# 8. The 84-State Landscape & the Walk

```text
12 MEF lenses (Lens 0..5 bimba-anchored, Lens 0'..5' pratibimba-anchored)
× 7 context-frame modes (CF1=Nous .. CF7=Sophia)
= 84 distinct playing-states
```

Changing lens transposes the chromatic substrate (which note is the matheme-tonic); changing mode rotates the CF-progression (which relational-grammar colours the walk). Each cell is one matheme-perspective — and one specific Vimarśa-reading of the Prakāśa cloud. The 84-state is the shared coordinate that M2-1' reads, M1' walks-as-melody, M2' renders as cymatic, M3' classifies as codon-rotation, M0' addresses as graph-walk, M4' binds as journal-context.

The Möbius-return indicator fires at the P5 → P0' rollover — the visible enharmonic-flip when the walk crosses the matheme's totalisation point.

---

# 9. Place in the 1-2-3 Engine — The Base Physics

```text
M1 (+1 parent / tick)  →  M2 (72 frequency-bridge)  →  M3 (64 binary-genetic Matter)
   the pulse                the spectral field           the matter that takes form
```

Paramaśiva is the **+1** — the toroidal parent, the pulse. M2 is the frequency it is played in; M3 is the matter it becomes. The Cl(4,2) algebra M1 establishes runs at four scales (M1 SU(2) ring · M3 codon · M4 personal · Kerykeion natal — see [[anuttara-ux-full-m0-branch]] §8), which is what binds the whole engine into one continuous object. In the integrated 1-2-3 cosmic-engine plugin, M1's tick drives the M2 solar field and the M3 clock together.

---

# 10. Governance & Privacy

- M1' exposes route and relation metadata, never private traversal motives, raw bioquaternion, or protected journal/Graphiti bodies. Traversal deposition carries coordinate, tick address, relation law, and session handle only.
- **No renderer-local source-of-truth**: M1' consumes the kernel-bridge profile; it never forks `m1.h` constants into frontend tables, creates a private clock, or infers graph relations locally.
- **Graph-law generation only**: a walker may choose among valid S2 neighbours, but every step is explainable by the candidate relation set, selected edge, and profile before/after — never random note production.
- Epii corpus work (CPT/RAG over Paramaśiva's QL theory) may *articulate* M1' law but never *mutate* it; this spec is held-out authority for register-fidelity.

---

# 11. Cycle-2 m-dev Anchoring

Relative to `Idea/Bimba/Seeds/M/Legacy/plans/2026-06-02-m-prime-cycle-2-canonical/`:

```text
03-m1-paramasiva-extension          this surface — QL pedagogy + tick/oscillator + toroidal-diamond
08-integrated-1-2-3-cosmic-engine   M1 as the pulse driving the composed cosmic engine
```

---

# 12. Closing Formula

```text
M1-0′ serves the matheme's fixed lesson-material.
M1-1′ manages instances — the S1/vault alignment.
M1-2′ teaches interval; M1-3′ teaches pulse (the tick).
M1-4′ teaches the positions and lenses; M1-5′ teaches the shape (K², Hopf, the diamond).
```

The shorter operational mantra:

> **Paramaśiva teaches QL by letting you play it: relation = interval, position = pitch, traversal = phrase.**
> **Its body is the toroidal-diamond; its pulse is the 9/8 tick; its theory is the 4:2 ↔ 3:3 partition in Cl(4,2); its instances are the vault.**
> **It is the base physics — the pulse the solar field moves to and the clock turns by.**

---

*Counterparts: [[parashakti-ux-full-m2-branch]] (M2, the frequency-field), [[M3'-SPEC]] (M3, the Matter/clock). Ground + integrated view: [[anuttara-ux-full-m0-branch]] (M0). Consuming poles: [[nara-ux-full-m4-branch-update]] (M4), [[epii-ux-full-m5-branch]] (M5). Canonical spec: [[M1'-SPEC]].*
