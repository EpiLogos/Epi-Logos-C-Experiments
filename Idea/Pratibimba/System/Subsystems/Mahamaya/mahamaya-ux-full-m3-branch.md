---
title: Mahāmāyā UX — Full M3 Branch
subtitle: The Symbolic-Genetics Parser, the 360° Clock-Field as Live Data-Structure, and What "Cymatics" Means at M3 (Matter)
status: draft-update
created: 2026-06-02
coordinate: "M3'"
source_context:
  - M3'-SPEC.md
  - m3-prime-symbolic-transcription-research.md
  - alpha_quaternionic_integration_across_M_stack.md
  - alpha_rasa_bridge_ql.md
  - 2026-06-02-m-prime-cycle-2-canonical (plans 05, 08, 06)
  - parashakti-ux-full-m2-branch.md / paramasiva-ux-full-m1-branch.md / anuttara / nara / epii
---

# Mahāmāyā UX — Full M3 Branch

## 0. Purpose of This Document

This document articulates Mahāmāyā (M3) as **the symbolic-genetics parsing system** — the surface where any identity or entity can be read into its codon / tarot / I-Ching signature — and as **the Matter pole** of the 1-2-3 cosmic engine. Its visualisation is the full 360° clock and its lenses; as a live data structure, it is a whole field of nodes pinned by clock geometry with cross-clock relations mapped angularly. **That clock-field is what "cymatics" actually means at M3.**

The corrected, spec-grounded view:

> **M3 (engine) is the binary symbolic-transcription engine** — 64 DNA codons, 64 I-Ching hexagrams, 384 line-change operators, 472 rotational states, 56+8 Tarot compression, the 72→64 epogdoon reception. **M3' (renderer) is that engine made playable** — the clock-pulse surface, the codon-tarot wheel, and the codon-rotation projection of the 84-state `(lens, mode)` landscape into the 472-state `(codon, rotation)` modal-inversion surface. In app UX, M3' is **the integrated parser of the symbolic genetics of any given thing**, visualised as the 360° clock-field.

This clarifies six points:

1. **M3 parses symbolic genetics.** Give it any entity/identity and it reads out the codon / hexagram / tarot signature — the elemental-temperament-quaternion of the thing.
2. **M3 is Matter.** It is the 64-fold binary-genetic substrate; every codon is a quaternion in Cl(4,2). Frequency (M2) becomes form (M3) through Venus's 9:8 epogdoon.
3. **The visualisation is the 360° clock and its lenses** — the wheel, the 24-spoke lattice, the 720° double-cover, the four render-views.
4. **"Cymatics" at M3 is the clock-field-as-live-data-structure** — nodes pinned by clock geometry, cross-clock relations via hops / traversal / angular relations. The matter-crystallisation of M2's continuous standing-wave.
5. **Specifics matter** — the 2-bit nucleotide logic, the dinucleotide matrix, the four-charge quaternion, the 56+8 exact cover, the suit-integrals summing to 360.
6. **It serves Nara** as oracle (tarot/I-Ching casts), as identity (the Gene-Keys/64-code bridge), and as the Janus transformation-doorway.

---

# 1. Core UX Axiom

> **Mahāmāyā reads the symbolic genetics of any given thing, and shows the whole as a turning clock.** Every entity — a person, a name, a concept, a date, a node — can be parsed into its codon/hexagram/tarot signature and pinned on the 360° field; and the relations between things are angular relations on that field. Turning a tarot card *is* changing the mode.

The default surface feels alive and tarot-like; the full multi-matrix engine is summonable when a user or agent needs to *show their work*.

---

# 2. M3 (Engine) vs M3' (Renderer)

```text
M3   the transcription engine     64 codons, 64 hexagrams, 384 line-changes, 472 rotational
     (ENGINE)                      states, 56+8 Tarot, the 72→64 epogdoon reception, codon-as-Cl(4,2)

M3'  the clock/wheel renderer      clock-pulse surface, codon-tarot wheel, (lens,mode)→(codon,rotation)
     (RENDERER)                    projection, the 360° clock-field as live data-structure
```

M3' consumes the kernel clock and the M2-1' audio bus; it does **not** own the audio bus or invent codon-classification math locally. Mahāmāyā has **two 0-side renderings of one Neo4j substrate**: the M3' clock-wheel (temporal-cosmological) and the [[anuttara-ux-full-m0-branch|M0']] graph view (structural). Neither may fork the graph.

---

# 3. The Integrated Symbolic-Genetics Parser

This is the system-UX heart of M3': **parse any identity or entity into its symbolic-genetic signature.** Specifics matter — the parse is exact, not poetic:

- **2-bit nucleotide logic** (polarity ⊕ mobility): `A=6 (Old Yin, Cups, Water)`, `T=9 (Old Yang, Wands, Fire)`, `C=7 (Young Yin, Pentacles, Earth)`, `G=8 (Young Yang, Swords, Air)`. Base-pairing is XOR with `0x01`. The 6/9/7/8 I-Ching values are the **operative arithmetic** of the whole system.
- **Codon = quaternion in Cl(4,2).** For codon `(X,Y,Z)`: `pp = X+Y+Z`, `mm = X−Y−Z`, `mp = X−Y+Z`, `pm = X+Y−Z`, with the invariant `pp+mm+mp+pm = 4X`. The quaternion `{w=pp(Earth), x=mm(Fire), y=mp(Water), z=pm(Air)}` makes **every codon an elemental-temperament-quaternion** — the same Cl(4,2) as M1's SU(2) ring and M4's personal-quaternion.
- **The 16-pair dinucleotide matrix** (complementary / moving-resting / same-quality, plus homogeneous baselines) is the evaluation engine that drives the rotational composition algebra. The three matrix-families are the i/j/k gauge-axes of the quaternionic substrate.
- **The 72→64 reception (M3-0).** M2's 72-fold vibration compresses through the DET `floor(index72 × 8/9) = 64` into the codon address — the frequency→matter transduction. M3' renders the provenance chain (72-source → DET → 64-address → nucleotide triplet → codon-quaternion); it does not reverse-engineer private meaning.

So "the symbolic genetics of any given thing" is well-defined: an entity → an I-Ching-value triplet → a codon → a Cl(4,2) elemental-quaternion → a tarot card + hexagram + line-change signature. The parse is the same operation whether the "thing" is a user's identity (the Gene-Keys/64-code bridge at M4-0) or a bimba node or a cast.

---

# 4. The 360° Clock-Field — and What "Cymatics" Means at M3

**This is the clarification.** At M2, cymatics is the *continuous* standing-wave (frequency made pattern — Chladni). At M3, "cymatics" is the **matter-crystallisation of that wave into a discrete field of nodes pinned by clock geometry.** The frequency field organises into form; the form is the clock-field; its reconfiguration as the clock turns is the *perturbation*.

Concretely, the M3 cymatic surface is a **live data structure**:

```text
- every entity/node carries a clock geometry: degree (0-359°), decan (0-35), sign (0-11),
  codon address (0-63), rotational index, hexagram, trigrams, tarot identity
- nodes are PINNED on the 360° field by that geometry (the wheel is their coordinate)
- cross-clock relations are mapped as ANGULAR relations: aspect, opposition (180°),
  trine, square; via HOPS along the 384 line-change graph; via TRAVERSAL of the 24-spoke lattice
- the field PERTURBS as the kernel tick advances — nodes pulse, the active cell moves,
  the standing-wave redistributes (the matter taking and re-taking form)
```

So the M3 "cymatics" is not a pretty visual on top of the data — it **is** the data, geometrically: the whole node-field arranged by clock position, with its relational dynamics drawn as angular/hop/traversal edges. The continuous M2 wave (frequency) and the discrete M3 node-field (matter) are the two faces of the same becoming, joined by the 9:8 epogdoon.

## 4.1 The Visualisation Lenses

The same clock-field renders through several depth-modes (per M3'-SPEC §8.10):

```text
Flat Clock Debug   degree, tick, codon, line-change, readiness — plainly inspectable
Lens Annulus       the 16+1 Mahāmāyā lens-stack as cosmic wheel / frequency-filter backdrop
Toroidal / World   the K² × T²_Mahāmāya double-torus world-clock (M3-5); Pauli-Jung warrant
Hopf Identity      identity trajectory projected through the SU(2)/Hopf surface into visible state
```

Structural anchors: the **24-spoke 15° lattice** (the discrete-rotation lattice on K², 24 backbone-spokes = `degree % 15 == 0`), the **360/720° SU(2) double-cover** (full identity-return = two rotations), and the **12-deep ring-buffer** (short-term rotational memory). The per-suit integrals sum exactly to the wheel: `Cups 84 + Wands 96 + Pentacles 88 + Swords 92 = 360` — the codon-space's elemental partition *is* the degree-wheel's elemental partition.

---

# 5. The 472 Modal-Inversion Landscape

The 472 `(codon, rotation)` states are the modal-inversion surface of the shared 84-state `(lens, mode)` landscape:

```text
40 non-dual codons × 7 rotational states = 280   (have a symmetry axis → 7 modal inversions, like diatonic-7)
24 dual codons     × 8 rotational states = 192   (no symmetry → 8-fold octatonic freedom)
Total = 472
```

**Each tarot card IS one codon (one tonal signature); its rotational positions ARE its modal inversions; turning the card is changing the mode.** The dual codons unlock modal positions beyond the diatonic-7 that the normal 84-state landscape can't reach — so the wheel is a **modal-extension navigator**, showing which extended-rotation states are currently reachable from the active `(lens, mode)`. (The forward/reverse `(lens, mode) ↔ (codon, rotation)` map lives in the kernel profile's `codonRotationProjection`, not in renderer code.)

---

# 6. Place in the 1-2-3 Engine — Matter Singing in the Frequency-Space

```text
M1 (+1 parent / tick)  →  M2 (72 frequency-bridge)  →  M3 (64 binary-genetic Matter)
   the pulse                the spectral field           the matter that takes form
```

M2 creates the frequency space; **M3 is the Matter that sings within it** — the clock turning, the codon-field crystallising and perturbing. M1's tick drives the turn; M2's audio bus colours it; M3's wheel is where it becomes discrete symbolic form. The **Janus temporal doorway** is the live affordance: every codon-rotation reads backward as inherited state and forward as possible transformation — bidirectional orientation, never fortune-telling certainty.

---

# 7. Mahāmāyā Serves Nara

Mahāmāyā is the symbolic-genetic engine behind several of Nara's layers (see [[nara-ux-full-m4-branch-update]]):

```text
M4-2 Oracle      ←  the Tarot / I-Ching casts ARE M3 codon-rotation reads; a spread is a set of
                    clock-field nodes with their angular relations
M4-0 Identity    ←  the Gene-Keys / Mahāmāyā 64-code bridge parses the user's identity-genetics
M4-3 Transform   ←  the 384 line-change graph + Janus doorway: what line is moving, what's
                    inherited vs possible (the transformation arc as hops on the clock-field)
#4.4.4.4 Personal ← the user's elemental-temperament-quaternion aligns with codon-quaternions
                    in the same Cl(4,2) — resonance is computable
```

In the integrated 4/5/0 / 1-2-3 surfaces, the user's identity-genetics is one node pinned on the clock-field, and its angular relations to the active transit/cast are the reading. The oracle is not a separate module — it is the clock-field queried for a moment, returned as the symbolic genetics of *now* for *this person*. (Protected-local: M3' resolves scalar references from Nara artifacts without loading protected artifact bodies.)

---

# 8. Governance & Privacy

- M3' shows **public-current** clock and symbolic-address data. It must **not** expose raw bioquaternion, private cast interpretation, journal content, or unreconciled Graphiti text.
- **No renderer-local mapping tables**: codon/hexagram/tarot/planetary mappings come from the M3 codec backend and S2 graph law; `pending-dataset-lut` / `provisional-gap` are honest UI states, not licences to compute arbitrary mappings.
- **M3' classifies nothing locally**: M2-5' DET-projects 72→64; the M3 codec classifies; M3' renders. The codon-class / rotational-state / projection fields are kernel-profile fields.
- M5/Epii process-reward RL over Mahāmāyā's calculation pathways lives at M5, not M3'; M3' exposes read-only kernel-trace overlays only.

---

# 9. Cycle-2 m-dev Anchoring

Relative to `Idea/Bimba/Seeds/M/Legacy/plans/2026-06-02-m-prime-cycle-2-canonical/`:

```text
05-m3-mahamaya-extension            this surface — the symbolic-genetics parser + clock-field
08-integrated-1-2-3-cosmic-engine   M3 as the Matter-pole of the composed cosmic engine
06-m4-nara-extension                the consuming surface — oracle / identity / transformation
```

Held open-questions tracked from M3'-SPEC §10.1: the TCT/Nine-of-Wands dataset mismatch, the "17th lens" / "16+1" wording, the 72→64 uniqueness scope, and the `codonRotationProjection` kernel-home.

---

# 10. Closing Formula

```text
M3 parses the symbolic genetics of any given thing into codon / hexagram / tarot / Cl(4,2).
The visualisation is the 360° clock and its lenses.
The "cymatics" is the clock-field as live data-structure — nodes pinned by clock geometry,
  cross-clock relations as angular / hop / traversal edges, perturbing as the tick turns.
M3 is Matter: frequency (M2) crystallised into form through Venus's 9:8 epogdoon.
```

The shorter operational mantra:

> **Mahāmāyā reads the symbolic genetics of anything and pins it on the turning clock.**
> **The cymatics is not a visual on the data — it is the data, arranged by clock geometry and moving as the tick turns.**
> **Each tarot card is a codon is a tonal signature; turning the card is changing the mode; the field is Matter taking form in the frequency-space M2 holds open.**

---

*Counterparts: [[parashakti-ux-full-m2-branch]] (M2, the frequency-field this Matter sings within), [[paramasiva-ux-full-m1-branch]] (M1, the tick that turns the clock). Ground + integrated view: [[anuttara-ux-full-m0-branch]] (M0). Consuming poles: [[nara-ux-full-m4-branch-update]] (M4), [[epii-ux-full-m5-branch]] (M5). Canonical spec: [[M3'-SPEC]].*
