---
coordinate: "M'"
status: "recapture-register"
created: "2026-07-03"
depends_on:
  - "[[2026-07-02-pratibimba-app-phase-1]]"
  - "[[13-decision-register]]"
  - "[[SEED-HARMONISATION-PROTOCOL]]"
---

# Cycle-3 Recapture Register — the Mature Image, Walked Backwards

**VAK Topology:** CPF=(0/1) | CT=CT1,CT5 | CP=4.1 | CF=(0/1) -> Anima | CFP=CFP1 | CS=Day

Produced 2026-07-03 by a full backwards walk (49→00) through `Idea/Bimba/Seeds/M/Legacy/plans/2026-06-02-m-prime-cycle-3-design-reconciliation/` — every plan file read in full, plus the complete 13-decision-register (1,959 lines), each claim annotated against the quarantine ledger (551 tasks: 246 done-CLAIMED · 237 audit_required · 60 quarantine · 8 pending).

**Law of this register:** the cycle-3 plans are the richest statement of the system's designed shape; their *build claims* are unverified. Everything below marked LAW stands as design authority (with its owning spec); everything marked VERIFY is a claim requiring re-verification before anything builds on it; CARRIER names what [[2026-07-02-pratibimba-app-phase-1|pratibimba-app]] has not landed. Per the Build law: capabilities and law carry from the frozen Theia carrier; code does not.

---

## §1. Standing Invariants (bind any carrier)

**Matheme spine.** `137 = 64 + 72 + 1`; the +1 parent is **M1-5, never M0** ([[13-decision-register|DR-M1-1]]/DR-M5-2). Canonical forms per DR-M3-6 (+ CU-FORM-1 sixth form `137 = 64 + 73`); execution trace `64+72=136 → −9 → 127=M₇ → +1 → 128 → +9 → 137 → +δ`; translation rule `9_M2 = 8_M3 + 1_M1` (DR-IG-7). Physics is labelled measurement-face/symbolic-skeleton only — renderers never compute constants; "QL derives alpha" is a forbidden string.

**7-8-9 spine.** Anuttara archetypes (7 Divine Action `#0-3-10` · 8 Structural Reflection `#0-3-11` · 9 Paramesvara `9=(00+00)`) ≡ the biological descent `72 →(−1/9, 9 fold-points)→ 64 →(−1/8, 8 gap sentinels)→ 56 = 7×8` — one series, two ends. Spanda: `N5 = 8n ± n`. (Track 37 §1.)

**72-fold.** One invariant, six addressing axes (MEF·QL / tattva-phase / decan-face / Shem / maqam / DET) + two sonic overlays (Mantra 100 = 50 Matrika + 50 Malini, 144→432 Hz; Asma 99+1 with 36/64 masks) — overlays are tabs, never a seventh axis (DR-M2-2). `M2_PLANET_LUT[10]`: Earth IS the 10th planet as observer-centre, never orbits (DR-M2-1); 9:8 epogdoon = 9 non-Earth planets : 8 chakras. `m2Address72 = tick12·6 + position6` — never `lens·7+mode`.

**Codon algebra.** 64 codons: 40 non-dual ×7 + 24 dual ×8 = 472 rotational states. Charges `pp/nn/np/pn` are the canonical name-set (DR-37-2; `mm/mp/pm` is struct aliasing, never exposed); `pp+nn+np+pn = 4X`; charges = the four X# permutations = the four elements — one fact, three registers. Suit integrals Cups 84 + Wands 96 + Pentacles 88 + Swords 92 = 360. Three matrices (COMP `i^0x3F` → x · MOVE trigram-swap → y · RES 56+8-gaps → z) are the quaternion gauge axes. Nucleotides: A=6/T=9/C=7/G=8 (I-Ching), A=Water/T=Fire/C=Earth/G=Air (code-canonical per DR-37-5). Element IDs: ≥5 schemes live; **canonical bridge scheme = L2' (Aether0/Earth1/Water2/Air3/Fire4/Salt5)**; no raw element integer crosses M2↔M3 (DR-37-3).

**Clock.** 385 nodes = 360 degree + 24 amino backbone + 1 Axis Mundi; `360+24 = 64×6 = 384` line-change graph; 24 palindromic anchors at d%15==0. Fibonacci Ground = Level 0 (60-fold, LCM(6,5,12), Pisano-60; DR-FIB-1..5): 4-level stack 60 → tick12 → 16 derived lenses → 9 walks; the "+1" of 16+1 IS Level 0. **User correction 2026-07-15 supersedes DR-M3-LENS-18:** Ground is functionally primary lens id 16 and grounds the sixteen static division ids 0..15; it is not a homogeneous extra row in `lensCarrier[16]`, and there is no Operator/Frame lens. Namespaces never merge: 12 MEF (M2-1' chromatic) ≠ the M3 primary-plus-derived hierarchy (DR-M3-3). 9 walks are sequential, distinct from simultaneous apertures (WALK_SPANDA 12 · WALK_HEXAGRAM 64 · WALK_LINE_CHANGE 2⁶ · decan/amino/arcana/chakra/degree/axis-mundi). `KairosFrame` = NATAL | REALTIME | KAIROTIC discriminated union (64 bytes, kairotic 4h decay) — layers never swap.

**Klein/inversion.** ONE three-variant `KleinFlipEvent` (`M1TritoneCrossing` / `M2CymaticValenceInvert` / `M3CodonRotationCross`, DR-IG-2) from one profile-tick event drives M1 K² fold + M2 valence invert + M3 200ms rotation-axis flip (DR-IG-3) — no local timer. Flip at tick 5→6; Möbius return at 11→0; 720° SU(2) identity. DR-FLIP-1 global law: **address conserved, phase flips** — `C3'` never silently resolves as `C3`; `q_5_X` and `q_5_i_X` are distinct properties.

**Quaternion.** Cl(4,2) is ONE algebra at four scales (M1 ring · M3 codon · M4 personal · Kerykeion natal), single source `portal-core/src/quaternion.rs`. Quintessence axes `[w=Earth, x=Fire, y=Water, z=Air]` (DR-M4-2, kernel-code-settled). `Q_composed = Q_identity · Q_transit · Q_activity` (left-assoc, normalized); `(q_b, q_p)` is a decomposition, never independent input.

**Mental pole.** 4'-5'-0' = LLM(Nara) / EBM(Epii) / Verifier(Anuttara) (DR-MP-1); EBM atom = 72-dim resonance vector with three tritone squares (0,5)/(1,4)/(2,3) (DR-MP-2); `E_total = (4·E₄ + 5·E₅ + 6·E₆)/15`, weight-6 refusal authority (DR-MP-4); Möbius descent `q_p − log(9/8)·∇_M E_total`, step size epogdoon and non-tunable, 8 steps per 12-tick cycle. Verifier raises typed coordinate-string questions, never pass/fail (DR-MP-3); `anuttara_witness` is emit-only — nothing may gate on it except the human gate and M5 review (10.AW).

**The unified VAK act (Track 08 §8.9 — doctrinal keystone).** The 4/5/0 mental pole performs ONE act of VAK-coordinate-designation per tick with six faces (coordinate-designation · MEF lens · QL position-check · harmonics-read · musical-transcription · physical-pole entailment) — the carrier's modulation graph must be derivable as faces of one act, never six sub-operations. Kernel-tick triplet ≡ tunability-evaluation triplet: one operation, two scales.

**VAK.** Field order `CPF, CT, CP, CF, CFP, CS` (DR-VAK-1; `reading_frame.positions[]`/`cp[]` is cardinality authority). Diatonic CF progression audible: C=(00/00) Nous · D=(0/1) Logos · E=(0/1/2) Eros · F=(0/1/2/3) Mythos · G=(4.0/1-4.4/5) Anima · A=(4.5/0) Psyche · B=(5/0) Sophia (DR-VAK-3). Alphabet 128 = 109 + 7 psychoids + 12 coordinate-types-with-inverses (DR-VAK-7). **Coordinate-tagging IS compression; kernel forward-derivation IS decompression** — no `compress_through_VAK()` module ever; overflow emits dereferenceable `<vak: …>` tokens (DR-COMP-1). Glyph preservation: never abbreviate `##`, `R#`, `#R`, `R#/##`, `(@#)`, `O#/X#/N#/M#`.

**Data spine.** Renderers never compute kernel law; the bridge reads struct fields, never synthesizes. Every gateway route has a CLI twin; no gnostic/graph op bypasses the gateway (DR-S5-ONE-1). "CLI is source of truth; MCP is wire format" — byte-identity across `epi canon coord` / bimba-mcp / `s2'.coordinate.resolve` (17.28). Vimarśa (M2-1') writes `audio_octet[8]` + `nodal_quartet[4]`; M1' and everything else read — "to change a value, route through M2."

**Privacy.** Three q_ namespaces: `q_{n}_{i?}_{semantic}` public · `qm_{n}_{i?}_{semantic}` meta · `q_personal/q_identity/q_activity/q_composed` private, never on the bus (DR-M4-4, four-point enforcement). `PersonalPoleProjection` carries `OpaqueProtectedHandle` only; the single scalar is `q_personal_resonance` (DR-M4-3). Privacy lives at the data class and the model slot (DR-MODEL-1), not at extra gates.

**Design law.** Nine Foundation Principles (Track 15) — coordinate as navigation · profile-tick as the only clock · provenance always inline (no errors panel) · 0/1 as face-dial with state survival · `/` as operator membrane · composition over juxtaposition (CI-rejected) · activity-bar modes not stacks · platform conventions · day-now as ambient thread. Exactly TWO motion primitives: slerp over `RING_QUATERNION_LUT[12]` (15.9) and the Bernoulli lemniscate shader with three ratified configs — 0/1 toggle 400ms cubic-out · Klein 240ms linear · Möbius 320ms smoothstep (DR-UI-4; supersedes the 600ms/420ms drift in 30.3/32.4). 7-member `ProvenanceState` with one `<ProvenanceBadge>` (DR-UI-3). Tick pause/scrub always permitted (DR-TS-6). Six-entry status strip; no modals; **decoration without derivation is rejected at lint time** (Track 30).

**Blocks (Track 44, carrier-independent).** Every M' surface renders schema-validated Blocks: `id, type, ctx{cf,ct,cp,cpf?,cs?} (mandatory, DR-PSS-1), coordinate?, privacyClass, provenance?, data, affordances?`; live wire on the day/now runtime + persisted block-doc (md or MDX, DR-PSS-3) in `Idea/Empty/Present/{day_id}/`; Hen owns CTX templates, surfaces write instances only (DR-PSS-4); verdict/annotate ride `sessions.patch` under the Human Gate (DR-PSS-5); A2A via agent-card over existing methods (DR-PSS-6).

**Canon governance.** Hen is the entity-candidate compiler, Empty→Types→World (DR-S1-4/5); graph canon mutates only via governed M5 promotion receipts — no raw `s2.graph.{create,update,delete}` affordance ever (DR-M0-1). Additive canon changes route through the Track-40 CU ledger (the only legal source of canon-update markers). Canon identities: `128=101+27`, `137=101+36`, `36−27=9`, `73=36+37=72+1`.

**Agent roster (corrected).** Pi single harness; Anima sole dispatcher + synthesis authority (DR-M5-1); Aletheia is a MODE, not an agent (DR-B-3); **six** techne-guardians by CF position (Anansi CF0 · Janus CF1 · Moirai CF2 · Mercurius CF3 · Agora CF4 · Zeithoven CF5); **Techne is NOT an agent** — Pleroma's atomic-skills substrate (DR-S4-TECHNE). The "14-agent roster" framing in CLAUDE.md/memory is a mis-count → 13 (Pi + 7 constitutional + 6 guardians) with Aletheia as mode. Constitutional CT map: Nous CT0 · Logos CT1 · Eros CT2 (relational operator, DR-EROS-1) · Mythos CT3 · Psyche CT4a+4b · Sophia CT5; CF1–CF5 are team-composition gates on the same scale as guardian invocation.

---

## §2. Per-Track Capture (49→00)

Dense form: **LAW** (stands) · **SEAMS** (exact methods/fields) · **VERIFY** (claims) · **CARRIER** (pratibimba-app gaps). Full detail lives in the numbered plan files; this register is the index of what matters.

### 49 — Modal resonator bell kernel *(pending=8)*
LAW: bell/modal contract is interpretive over the existing 8+4 bus, never replaces it; 7+5 partition orthogonal to 8+4; chime carries handles/digests only. SEAMS: `ModalResonatorProfile`, `M123ChimeFrame`, `kernelBridge.m2.epogdoonProjection` in all four capability lists. VERIFY: carrier landed the kernel/gateway/Zod core (S6.3, independently verified) — the Theia-side tranches are dead. CARRIER: 49.6 integrated coherence gate (block chime on M1/M2/M3 tick/degree/address disagreement); 49.7 privacy/drift serialization guards in the carrier's Zod layer; epogdoon capability parity (standing open).

### 48 — Bases / CS reflection layer *(done=7, all CLAIM)*
LAW: C-ladder — CT = C1' write-form → MOC = C4 type-authority → **Base = C5/CS reflection** (structurally read-only) → Möbius to C0; `coordinate` = universal join key; canonical artifact = md note with embedded ```base``` block (`c_4_artifact_role: "base-view"`, `c_5_reflects`, `c_3_projected_from/at`). SEAMS: `s1'.base.ensure`, `s2.graph.list_by_filter(coordinateScope, propertyFilters, limit)`, `BaseViewConfig/BasesRecord/basesModelFromRecords`. VERIFY: 48.5–48.7 almost certainly unbuilt. CARRIER: a coordinate-keyed query-view pane (table/cards/list) over the existing GatewayClient — strong candidate; also the natural home for CU-ledger review.

### 47 — Autonomous loop scheduling *(done=4, CLAIM)*
LAW: three triggers (cron/event/consent); autonomous fire REQUIRES granted consent posture; scheduled fire = normal VAK dispatch through Anima; one due-check in the gateway heartbeat, no new scheduler. SEAMS: `cron::check_due_and_fire`, `epi gate cron add/run`, `chronos_cron_fire`, `khora.result.wake`. CARRIER: no cron/scheduling surface (OmniPanel cron tab family open).

### 46 — Aeon loop unit *(done=6, CLAIM)*
LAW: Aeon = graduated, named, parameterized Z-thread loop (args = VAK address); the loop's doubling is the **QL P↔P′ syzygy return**, not the harness axis; graduation consent-gated CPF (00/00); Sophia emits `MÖBIUS_RETURN`. SEAMS: Z state machine `queued…recomposing`; eval ledger transcript → Redis `…:eval:*` + Graphiti `source:"aeon-eval"`. VERIFY: cited foundations (42.x, 12.26, 12.35) are themselves unverified. CARRIER: none yet (future agent-panel visibility).

### 45 — Bimba Map indexing / DOX-OKF *(self-claimed done)*
LAW: crystallisation upward (Hen) vs reflection downward (Map projection, never re-promotes); coordinate algebra normalisation (`#`→`M`, context-frames parenthesised, `/`→`∕`); DOX ≡ OKF. SEAMS: five cross-language normaliser impls must stay in sync (`wrap_context_frames` Rust/TS/JS/py + projector). VERIFY: Map freshness; Hen `is_valid_coordinate()` still rejects depth≥2 (real gap). CARRIER: coordinate rendering must use gateway-side normalisation — never a 6th local impl.

### 44 — Pratibimba surface standard *(AR=6 done=1 q=3)*
LAW: §1 Blocks — carrier-independent. VERIFY: all Theia block-kit landings. CARRIER: **the largest structural gap** — no block contract, registry, catalog, verdict/annotate loop, `s3'.temporal.*` live transport, or block-doc persistence. Re-derive natively (React/Tauri), spec-first.

### 43 — End-of-cycle-3 audit protocol *(done=6; recent commits real)*
LAW: coordinate headers (`convention:coordinate-header:v1`), `.h` < `.c`, forbidden-imports machine-enforced, DR-HYGIENE-1..3. CARRIER: pratibimba-app itself needs the header convention + its own forbidden-imports row (not covered by the Theia preflight).

### 42 — Harness dynamics *(AR=6 done=2 q=1)*
LAW: harness = access/cost lever, model-family = diversity lever — two independent axes; parent harness always Pi; harness-neutral transcript-of-record; ONE central skill store symlink-projected; vault = async inbox (Khora watch wakes parent). SEAMS: `HarnessTurnEvent`, `HarnessDispatch{purpose∈{implement,review,explore,search,converse}}`, `~/.epi/gate/transcripts/<slug>.jsonl`. VERIFY: whole track suspect; 46/47 cite it as foundation. CARRIER: future session UI shows harness/lineage.

### 41 — Vama Shakti factory + arena *(done=12 — highest fraud-risk file)*
LAW: any /World entity summonable under four closed classifiers (Egregore/Sprite/Daemon/Mantra, classifier byte in BLAKE3 identity hash, DR-VAMA-6/2); PASU IS a Vama Shakti (one algebra); dialogue-only capability frozen (DR-VAMA-5); coordinate-addressable-only (DR-VAMA-3). SEAMS: `m4.arena.*` gateway family + `epi nara arena *` CLI parity + SpacetimeDB tables + `portal-core/src/vama_shakti.rs`. VERIFY: everything — check `vama_shakti.rs`, arena tables, gateway routes, CLI exist at all. CARRIER: arena pane is a large future candidate; 41.7's widget spec is portable design law.

### 40 — Bimba canon-update ledger *(not in ledger)*
LAW: §1 canon governance. SEAMS: `s5'.canon_update.*`, `epi bimba propose|land|refuse`, lint test. VERIFY: five "landed" CU rows (markers actually in target files?); 40.1/40.2 code existence. CARRIER: CU review surface (combine with 48 bases view).

### 39 — S5' one-substrate *(done=1, CLAIM)*
LAW: five access patterns over ONE substrate (Indras Net); three arenas Siva/Shakti/Prana orthogonal to HOT/WARM/COLD (DR-ARENA-1); DR-PRANA-1 session arena. SEAMS: `s5'.gnostic.{query,ingest,notebook,status,candidates,etymology,resolve,list_notebooks,episode_search,evidence_trace,query_with_layers}` + `s0'.anuttara.trace` + `s1'.entity.*` + full `epi gnostic *` CLI parity. VERIFY: the e2e acceptance test. CARRIER: no gnostic panes/client methods; Library pane over `list_notebooks` is the entry.

### 38 — Tunability surface *(AR=3 done=2 q=1; DR-TUNE-1..4 PROPOSED)*
LAW: every knob typed/classed/audited behind one schema; **~15 structural invariants never tunable** (EPOGDOON 9/8, RESONANCE_DIM 72, 360/12, planet mod-10, `M3_TAROT_CODON_MAP`, `M2_PLANET_LUT`, QUATERNION_AXIS_ORDER, `VIRTUE_LUT[9]`, `RING_QUATERNION_LUT[12]`, …); risk classes A/B/C. SEAMS: `s5'.tune.*`, `epi tune get/set/list`, `~/.epi-logos/config.toml`. CARRIER: Tuning pane (landing surface, no modals) — after DR-TUNE ratification.

### 37 — Biological-quaternionic integration *(AR=9 done=1 q=1)*
LAW: §1 codon algebra + 7-8-9 spine + element canonicalisation. SEAMS (the projection backlog): `m2.epogdoonProjection` (37.1) · `m2.planetaryElementalWeights` (37.2) · `m2.cymaticMonoPolyState` (37.3, from `M2_TO_M3_CYMATIC_PROJECTION[72]` + `M2_CAUSAL_RESONANCE_MASKS[36]`) · `m3.lensCodonBinary` (37.8, ratified per commit a9558b81 — re-verify) · `m2m3.lensOrbiterRelations` (37.9, landed per commit 7807551a — re-verify) · `m3.bioquaternionTranscription` (37.11). MonoPoly states: mono / actually-many / actualising-one (forced-lock WARNING) / monopoly (telos) — shared enum with M0. VERIFY: DR-37-2/3/4/5 + DR-PRESAGE-1 resolutions actually in code. CARRIER: epogdoon gap-marking on the wheel; MonoPoly classifier driving the cymatic shader's behaviour state; elemental-weights feed; aperture-aspect edges; the one-object transcription surface; 24-amino backbone ring; RNA/chromosome pending badges.

### 36 — Anuttara pentadic runtime trace *(done=8, CLAIM)*
LAW: `0/1 tick → 5° Shem quantum → 72×5=360 → 24×15=360 → 384 → 64/bioquaternion → EBM return`; 4/5/6 hinge never flattened; Vāk level assignment (parā/paśyantī/madhyamā/vaikharī by CF + recognition; (5/0) without recognition flushes to madhyamā). SEAMS: `AnuttaraPentadicRuntimeTrace` + `VakLanguificationTrace` on ONE subscription; derivations `m2ToM3Symbol == floor(res72·8/9)`, `mahamayaAddress64 == floor(degree360·64/360)`. VERIFY: property tests + Rust/TS mirrors. CARRIER: neither projection consumed; no pentadic inspector; no vakLevel/m0Address chips. **This is the direct answer to "are the deep VAK kernel updates captured" — currently: designed, claimed, unconsumed.** Gated on DR-VAK-4/5/6 (still PROPOSED).

### 35 — Fibonacci Ground *(T0 + T35.1 done; T35.2 coherence-audited)*
LAW: §1 clock. SEAMS: NOW frontmatter `c_3_fibonacci_position/digit/tick12/backbone_index` (Khora stamps, Hen validates); `CLOCK_BACKBONE[24]`; `KairosFrame` accessor `m4_planet_degrees_live()`. VERIFY: DR-FIB VALIDATED + T35.2 kernel↔carrier coherence audit (`plan.runs/35-fibonacci-ground-coherence-audit.md`) — DR-FIB-1/4/5 LANDED, DR-FIB-2 LANDED (position/digit derived-not-stored), **DR-FIB-3 PARTIAL** (the 4h kairotic-decay deadline is never populated on the live path — open finding for the Khora/Kairos temporal lane). CARRIER (T35.1 landed): engine renders the 60-ring; BOTH natal-Sun gold ring and live-Sun silver dot fold through the shared `fibonacciGroundPoint` (both at fibonacci-positions, coincide in-wedge — T35.1); the carrier's 360→60-ring 6° wedge fold is proven coherent with the kernel `phase_space.rs` Level-0 definition (T35.2 vitest reproduces the kernel's pinned 144→24 / 66→11). Remaining: KairosFrame decay semantics (DR-FIB-3), NOW stamping from the vault service.

### 34 — S0 settings + Gemini embedding *(done=2, CLAIM)*
LAW: cloud-opt-in refusal contract; keys from env, never over gateway; cache key = model-version + BLAKE3 + matryoshka-dim. SEAMS: `s0'.settings.api_key_status/opt_in`, `epi settings status|opt-in`. CARRIER: settings pane (presence + opt-in state only).

### 33 — Harmonic energy channel handoff *(not in ledger)*
LAW: E₄ personal / E₅ harmonic (user data NEVER enters E₅) / E₆ verifier; §1 mental pole. Amend-in-place law (no "supersedes" spec files); `c_5_crystallisation_state: "discarded-branchable"` stepping-stone archive. SEAMS: `kernel_energy_evaluate` restructure (kernel.rs plain-sum bug, E₄ missing — VERIFY whether fixed), `kernel_mobius_descent_step`, three-channel Elo `(R_verifier, R_lens, R_user)` never collapsed, evolver as typed VAK choreography over existing primitives. **Data-spine violation of record: `oracle.rs:1236-1665` reimplements codon charges in Rust; `m3_compute_charges` (C) has zero Rust callers** — fix = FFI + boot assert `sum(pp)==360` + 64-codon cross-check. CARRIER: no energy-decomposition surface; the 4-5-0 engine is its consumption point.

### 32 / 31 / 30 — Onboarding · Chrome catalog · Design language *(AR-heavy; 30: done=1 q=5)*
LAW (30, the token source): ~126-token namespace tree `epilogos.{colour,typography,motion,spacing,elevation}.*` — 11 colour sub-namespaces with exact hex (signature cool `#5a73a8`/warm `#d4a14a`; flows gold `#d4a574`/emerald `#4a8b6f`; agent highlights recognition `#d4a574` · prospective `#e8a3a3` · retrospective `#8090a3` · kairos `#b8c0cc` · somatic `#8a7355` · live-spread `#5b3a7e`; elements aether/salt `#7d4f9e` · earth `#8a7355` · water `#5fa9b8` · air `#6ec1c8` · fire `#c5564b`; psyche-facets with deliberate cross-namespace alignments; privacy earth/slate/gold; readiness `#59c75a`/`#d4a14a`/`#f48771` + 9 per-id tints); typography scale (h1 24/600 … caption 11/400; mono.coordinate family-tinted, mono.codon amino-badged, mono.hexagram 16px, matheme KaTeX at h2); motion tokens with `profile-tick.duration` READ from the bridge never free; W3C token format with `$description` derivation citations; consumption lint (no raw hex/font-size/durations/timers outside tokens+primitives); seven-theme polarity-preservation invariant (cool stays cool across inversion); 15-primitive shelf. LAW (31): six-entry strip ids · chord grammar (cmd-period face toggle, cmd-shift-{0..5} Mn, cmd-1..8 membrane tabs, cmd-h+letter highlights) · slot policy (left activity-switched, right exclusive, no three-pane main) · breadcrumbs family→archetype→position · no-modal · preference namespace · one product menu. LAW (32): six-stage cold-start (non-modal splash, "Profile-tick 1 — system alive"), 9-id + 5-flavour readiness grammar via `flavourOf()` reducer, per-Mn empty-state copy (M3 "Cosmic clock at noon — 64 codons stand waiting"), settings taxonomy, typed-confirmation reset law, per-artifact privacy opt-in with consent record. VERIFY: no token file/primitive/lint ever verifiably existed. CONFLICTS: family hues — 30.8 derivation-backed tiers (P neutral / S slate / T parchment / M saturated-per-subsystem / L mist / C violet) vs UI-PATTERNS §1.3 flat hues (P cyan/S violet/T teal/M amber/L rose/C mint) → DR-WC-DL-1 open, re-decide for the carrier; lemniscate durations → **resolved by ratified DR-UI-4** (400/240/320). Family-36, mode-chip, elevation, user-side highlight hex were never minted. CARRIER: the entire token bundle + primitives + lint, re-homed to CSS vars/TS for React/Tauri.

### 29 / 28 / 27 — Composition · IDE chrome · OmniPanel *(AR-heavy)*
LAW (29): geometric-slot registry with handle-class allow/forbid lists (forbidden: raw-quaternion, raw-audio-octet, plaintext-journal, graphiti-episode-body, raw-natal-chart); `compositionLoad` hard-fails juxtaposition, per-slot graceful degrade; 14 composition events; ONE profile subscription (`useCompositionProfile`); persisted composition state survives both toggles. LAW (28): chrome partitions (M0' reads / M5' governance / shared); governed-write flow — canonical writes ONLY via Atelier Möbius write-back → `aletheia_crystallise` → Canon Studio; `CrossLayoutIntent` envelope fields (coordinate/artifactUri/reviewId/dayNow/sessionKey/profileGeneration/privacyClass/…); nine-id readiness rendered inline at binding sites. LAW (27): the `/` membrane is EIGHT tabs = one substrate folded eight ways (Pi Chat · Sessions · Dispatch Trace · Tool Stream · Evidence · Review · Gateway · Diagnostics), identical across layouts; click-through is the verb; slash verbs (`/dispatch`, `/cast`, `/translate`, `/aletheia crystallise`, `/session`, `/skills`, `/cron`); Khora session-start before first message; evidence schema imported never redefined. CARRIER: membrane has 3 of 8 folds; no slash grammar, no dispatch-trace/evidence/review/gateway/diagnostics, no intent router, no coordinate-tree left rail, no governed-write path.

### 26 / 25 / 24 / 23 — M5' · M4' · M3' · M2' frontend deep *(AR-heavy; 25 least-real: AR=14)*
LAW (26): M5' = EBM observatory — "M5' does not talk. It scores." 72-grid + three Klein-V₄ tritone squares (A indigo [0+5] · B amber [1+4] · C emerald [2+3]); wisdom-delta 8-byte XOR into quintessence hash; `MediatedRunEvidencePacket`; psyche-facet badges never actor rows; Logos Atelier scent stages root→cognate→drift→psychoid→pros-hen→möbius-write-back. LAW (25): 23 widget contracts — PASU wizard (derived-only quintessence), oracle aliveness (generating/muting/mute + 4h decay), medicine "evidence not authority", transform containers + alchemical ops, R-factor fretboard (7 strings × 6 frets, R1+R4=5, R2+R3=5, R0 confined, R5 fretless, `(@#)` band-turn), session-close ceremony (XOR trail + 4 contemplation seeds + 9 virtue lamps), time-axis switcher natal|real-time|kairotic, privacy border-tints (earth/slate/gold, never status-bar); **atlas-sync ConsentRecords write to PASU `c_4_atlas_sync_consents` via the dedicated `nara.pasu.consents.append` gateway RPC — a typed/validated array-append, NOT a `nara.pasu.set` scalar-setter overload; ratified DR-WC-M4-4, landed 25.T25.14** (`epi-cli/src/vault/pasu.rs::pasu_append_consent` + `gateway/src/dispatch.rs`). LAW (24): §1 clock + codon; TCT 0x35 = 7 states (dataset 8 is the error, DR-M3-1); tarot 22+56 with `m3_major_arcana_from_codon` (STOP→0xFF honest-pending); ONE-LUT decan chain card→suit→codon→decan→sign→planet→element→chakra→body resolved bridge-side; hexagram browser hops the 384 graph (**king_wen ordinal 1..64 now buses alongside the Fu-Xi `address64`/`hexagram_id` 0..63 via the kernel-owned `KING_WEN_FROM_ADDRESS64` LUT + inverse `ADDRESS64_FROM_KING_WEN` on `MahamayaCodecProjection`, `portal-core/src/luts/mahamaya.rs`; landed 24.T24.5**); NO `Math.random` — casts dispatch to the gateway; four depth modes as overlays. LAW (23): Chladni χ = Σ aᵢ·sin·sin + bᵢ·cos·cos with octet pairs driving and quartet constraining (the carrier's E4 shader matches); six-axis decoder `m2.decodeAxisAt`; F_routing trace `{planetary_hour_ruler, active_decan, shem_pair, maqam_family/mode, mantra_index, asma_name, index72, det64}`; 72-fold breadcrumb with 9:8 indicator; descent engine 23.18 (72→64→56 with fold-point/gap marking); elemental feed 23.19 (9 planets : 8 sites, aspect-modulated); MonoPoly classifier 23.20; Klein flip inverts every panel's valence and reverses cleanly. CARRIER: the M5 observatory, M4 lived-surface depth, M3 inspectors (16-division switcher plus separate [[Fibonacci Ground]], 9-walk navigator, tarot wheel, hexagram browser, transcription engine, pentadic inspector), M2 six-axis/sonic/breadcrumb/descent/MonoPoly surfaces — none landed; carrier has the composed engine base they plug into.

### 22 / 21 / 20 — M1' · M0' · Wave-C overview
LAW (22): M1' instrument strata — Spanda walk navigator (scrub, `#` invert button), Cl(4,2) signature inspector, Klein-flip chronograph, six vortex-matrix browser (raw + digit-root dual faces — the C generator carries digit-root only; raw 12×12 face must come from the Vortex Modulae CSV before Ananda is complete), audio-bus inspector with Vimarśa provenance, Kaprekar 6174 pedagogy, Mersenne/137 proof overlay (dev-mode). LAW (21): M0' one-widget six-panels three-modes; implicate↔explicate one substrate two presentations; virtue witness 9-bit LSB-first; symbolic-coordinate question console; 16-fold void ring; DR-M0-1 banner verbatim. LAW (20): the standing invariants list + six-status taxonomy + "the matheme is the architecture; the widgets are how the matheme shows up to a user." CARRIER: M1'/M0' strata absent (engine base exists); Wave-C's cross-cutting design system is the Track-D source.

### 19 / 18 / 17 / 16 / 15 / 14 — Contemplation · JSON edge · S-stack · CCTs · UI foundations · Gates
LAW (19): `contemplate_session_close(ContemplationObject) → wisdom_delta`; `M5_ContemplationObject` fields; tarot psyche-anchor at open; write-leave-return orbits (`chronos_response_orbit`). LAW (18): typed-at-the-edge — the full projection catalog (KleinFlipEvent, AnandaVortexProjection dual-register, M1TopologyProjection torus_knot_phase SSOT, M3 Oracle/Transcription/Protein packets, M4 PersonalPole handles, anuttara_witness emit-only, pasu_being_pattern with MonoPoly/Perspective/NaraFamily unions + ActualisingOne `reviewRisk: 'forced-unification'`); acceptance = no `Record<string, unknown>` outside documented boundaries. LAW (17): façade-split discipline; `METHOD_DISPATCH_PLAN` canonical (DR-S3-3, co-PR rule); Hen owns rename-reconciliation (integrity authority IS mutation authority); 17.28 canon byte-identity. LAW (16): CCT-14 entity lifecycle CLI parity; CCT-14b birth-codon `c_5_birth_codon`; CCT-16 substrate-integrity five-fix bundle (frontmatter whitelist ERROR, real `khora_sync_queue_flush`, `fire_provenance` retry+dead-letter, `MostRecent` picker, `graph_revision` bump); CCT-17 VAK compression canon; CCT-20 phase preservation; CCT-21 BeingPattern producer (bridge forwards handles, never reconstructs). LAW (15): the nine principles + surface contracts (§1). LAW (14): gates G1–G14 — never truly passed; **the gate list is the recapture checklist**; open-orphan table (F_routing carrier, psychoid renderer, daily-layer widgets, `K2SurfaceHandle`, `cron_evening`, …) presumptively still orphaned. VERIFY: 16 has done=1 — essentially the whole cross-cutting layer is open; 17 is the worst-audited infra track. CARRIER: contemplation pane; typed intent-envelope bus (18's law + DR-VAK-1 fields); CCT-16 items gate the vault service's growth; a phase-2 no-orphan registry.

### 13 — Decision register *(read in full; see §3)*

### 12 / 11 / 10 / 09 / 08 — Agentic · Shell · Profile contract · Graph · 4-5-0
LAW (12): §1 roster + one-substrate; VAK evaluator semantics; `epi know <coord>` composed packet; Verify phase (Compose→Perform→VERIFY→Rehear→Recompose; judge is a VAK-resolved role; judge model ≠ builder; max 3 cycles → human); Sophia `q_proposals[]` with `assertOpens()`; terminal-lease law; NO entitlement side-door; `M3TranscriptionPacket` four faces. VERIFY: **36 of 45 audit_required — the worst track; everything is claim.** LAW (11): face partition (`/` membrane crosses both faces — "it is `#` itself"); 10 highlight categories, agent inscriptions never replace user content; ambient strip + tuning bar → NOW frontmatter via `khora_write`. LAW (10): the profile-field ledger — 19 Phase-B fields all anti-greenfield surfacings; forbidden fields (no `earth_observer_handle`, no `DetFoldState`, no raw M4 bodies); `m1_ananda_get` 10×10 %10 core UNFAITHFUL to the 12×12 CSV — fix before bussing `ananda_vortex`. LAW (09): single `:Bimba` label; four namespaces `:Bimba`/`:World`+`:Archetypal`/`:Gnostic`/`:Pratibimba`; governed-route writes only; `epi canon coord --depth pithy|qv-detail|relational` as THE node-essence reader (~80-token pithy from q registers). VERIFY (09): zero done — nothing landed. LAW (08): §1 unified VAK act; DR-IG-6 dipyramid 6+6=12 (P5/P5' apices, interleaved P1-4/P1'-4', P0/P0' axis-point); q_ privacy partition regexes; Möbius write-back = read-only contemplative offering. CARRIER: mediation routing (`s4'.mediation.route` EXISTS in dispatch.rs per Sprint-4 verifier — wire it); `epi know`/`epi canon` as knowledge surfaces; privacy scrubber at the bridge edge; the 4-5-0 recognition engine (S6.5) built as the E₄/E₅/E₆ consumption point with corrected dipyramid geometry.

### 07 / 06 / 05 / 04 / 03 / 02 / 01 / 00 — Integrated 1-2-3 + M-wave reconciliations
LAW (07): composition contract (read directly this session): 137 assertion with `parentAttribution === 'M1-5'`, five Spanda forms + 7-8-9 triadic overlay, coupling-flow as labelled overlay never fourth pole, `torus_knot_phase` SSOT, Cl(4,2) one-algebra note. LAW (06): Library/Atelier = projections not extensions; EBM at `epii-autoresearch-core/src/resonance_ebm/`; q-review queue + 4 detector skills; energy restructure (see 33). LAW (05): M4 lifecycle — `M4_Symbolic_Protein` session chain (START ATG 0x07 at open, policy-derived STOP at close); `KairosFrame`; q_ vocabulary canon + `q_4_{i?}_locality_signature`; Klein sense law (Day/Night = prospective/retrospective senses; `#` = sense-switch; `cs.direction`→`cs.sense`); daily-briefing five sections; element-ID harmonisation 5.16 (CRITICAL PATH, unverified); `m4_snapshot_now()` zeroes planet_degrees — populator gap. VERIFY (05): 22 of 27 audit_required — least-real M surface. LAW (04): transcription kernel constants (ATG=0x07; STOPs {0x10,0x13,0x1C}; 27 SHARED + 37 TRANSCRIBABLE + 1 START + 3 STOP boot check); `m3_compute_charges` canonical vs oracle.rs deviation; DR-M3-4 reading chain; 4.15 backbone + pisano LUTs. LAW (03): §1 72-fold; `vimarsha_reading.rs` canonical writer; F_routing; Asma mirror overlay (address conserved, phase flips; `mirror_idx=0xFF` = no mirror not error). LAW (02): K² must be 3D (DR-M1-2); klein-flip fires ONLY at tritone crossings; `MPrimePerformanceEvent` deterministic replay; single session-held `#` handle (DR-M1-3); the raw-face CSV gap; 2.10 CPT trainer (EKSFT, blindfolded teacher, >10% perplexity halt-rule) was cycle-3's one sanctioned greenfield. LAW (01): verifier + R-system full theory (§1 glyphs; `R_FACTOR_DISTRIBUTION[7][6]` laws; DR-R0 route words `0x5FC1`); M0 calculus VM `m0_calc_reduce` + seven laws + DR-CALC-1..7; kinship LUTs (`NARA_MSHARP_LUT`, `NARA_TO_TRIGRAM`, **Tao (`5-/5`) ≡ `m3_compute_charges`** — M0 calculus and M3 codon engine are one operation); OWL/SHACL projection-only, failures ARE ?-objects. LAW (00): kernel as bioquaternionic JEPA-EBM operator; four-corpora method; the standing-invariant list; the kickoff's repudiated "Trust the ledger" (replaced repo-wide 2026-07-02).

---

## §3. Architect Decision Queue (consolidated)

1. **S5.5b** (carried from phase-1, blocks playable-84): (a) read-only `profileAtLensMode(lens, mode)` over the pure `vimarsha_read_profile` seam — RECOMMENDED — vs (b) kernel lens-mode setter (DR-M1-3 direction).
2. ~~**Aperture 17 / 18-stack reconciliation.**~~ **CLOSED 2026-07-15 by user correction:** the E1/E2/E3 16+1 pins are the law. Ground is primary functional lens 16 over derived ids 0..15; no Frame/Operator lens or 18-shape update proceeds.
3. **Family palette (DR-WC-DL-1, open).** 30.8 derivation-backed tiers vs UI-PATTERNS §1.3 flat hues, now to be reconciled with the carrier's landed engraved-ephemeris/element-chroma-only direction. Recommendation: 30.8 derivation as token law, element palette as the M2-canon chroma, engraved-atlas as the rendering register — a design-brief pass makes this concrete before ratification.
4. **planet:{Name} selection address** (E5 OPEN, carried).
5. **Batch-validated DRs of 2026-06-09** (DR-VAK-1, DR-M3-6, DR-IG-7, DR-VAK-3, DR-MP-1..4, DR-MOE-1, DR-ELO-1, DR-UC-1, DR-MODEL-1, DR-ML-1, DR-S1-6, DR-M4-4, DR-FIB-1..5, …) were delegated batch adjudications, not per-row validations. Recommendation: accept the substance as standing law, re-verify only implementations.
6. **PROPOSED DRs gating next-phase work:** DR-VAK-4/5/6 (gate the pentadic/languification traces, Track I), DR-M3-TRANSCRIPT-1 (session-as-transcription-chain), DR-ENTITY-CODON-1 (birth codons), DR-TUNE-1..4 (tunability).
7. **Day-id format:** the register's DD-MM-YYYY example vs your 2026-07-02 month-first ratification — carrier practice stands (already written back to [[M4'-SPEC]]); the register row should gain a supersession note.
8. **Roster correction to canon:** CLAUDE.md + S4 canon still carry the "14-agent roster / 7 Aletheia subagents" mis-count vs DR-S4-TECHNE (6 guardians; Techne = Pleroma substrate). Flag for [[S4-SPEC]]/CLAUDE.md correction.
9. **Carried gates:** E5 eyes-gate; S6.4/S5.4 audible/manual gates; the two committed-drift test failures (cache:hot redis prefix; claw-operator doc) for their owners.
10. **Repo safety:** `Body/M/pratibimba-app/` is entirely untracked; ~30 modified files across the frozen epi-theia + epi-cli/epi-lib await a disposition pass.

## §4. Re-Verification Backlog (highest-suspicion first)

1. Track 12 (36 AR) — every agentic seam: `s4'.mediation.*` (partially confirmed live by the Sprint-4 verifier), gnostic routes, terminal-lease spine, Sophia/Moirai/Verify-phase machinery.
2. Track 05 (22 AR) — element-ID harmonisation 5.16 (critical path), symbolic-protein lifecycle, kairos populator (`m4_snapshot_now` zeroing), q_ canon enforcement.
3. Track 09 (12 AR, zero done) — graph exposure methods, `epi canon` family, `:World`/`:Gnostic` labels.
4. Track 17 (13 AR) — the façade splits (some confirmed by 2026-06-28 commits), 17.28 byte-identity, 17.16 dispatch plan.
5. Track 41 (done=12, zero verification) — `vama_shakti.rs`, arena tables, `m4.arena.*`, CLI.
6. Tracks 39/46/47/48 (done-claimed) — one-substrate e2e test, Aeon graduation, cron heartbeat, bases machinery.
7a. *(added 2026-07-03, Track 00 closeout)* epi-cli full-suite runs retain a residual timing flake under load (~1 in 3 full runs, deep in the alphabetical tail; five distinct flake roots already fixed: orphaned preflight daemons, TIME_WAIT rebinding, env-lock poisoning, ambient EPILOGOS_VAULT, gate_chat settled-transcript race). verify-all now prints failing-suite tails even in --quiet so the next occurrence self-identifies; chase-to-zero is standing hygiene work for the rerun.
7. Kernel spot-checks: `m3_compute_charges` FFI callers (33/4.13); element canonical converters (37.10); DR-R0 `0x5FC1` route words; transcription constants 27/37/1/3; `m1_ananda_get` CSV fidelity; `kernel_energy_evaluate` E₄ presence. (E1 already re-verified the clock LUT + decan table kernel-wide.)
   *Automated 2026-07-03 (Track 00.T4, kernel-truth stage — `node .codex/scripts/kernel-truth.mjs`):* FFI reachability + 4X invariant → `portal-core tests/kernel_truth.rs::m3_compute_charges_ffi_reachable_with_4x_invariant_for_all_64_codons` (green pin); canonical converters → epi-lib `test_m_canonical` (60 checks, verify-all epi-lib suite) + `kernel_truth.rs::canonical_element_identity_crosses_m2_m3_as_canonical_b`; route words → `kernel_truth.rs::route_words_dr_r0_family_pinned` (green pin); transcription constants → `kernel_truth.rs::transcription_constants_27_shared_37_transcribable_1_start_3_stop` (green pin); ananda CSV fidelity → `kernel_truth.rs::m1_ananda_12x12_raw_fidelity_vs_vortex_modulae_csv` (**expected-red**, 10.10); energy law → `kernel_truth.rs::kernel_energy_carries_e5_e6_with_456_weighting` (**expected-red**, Track 33). Manifest: `plan.runs/kernel-truth-expected-failures.json`.
   *Extended 2026-07-06 (computational-core truth session — full audit at [[00-computational-core-truth-ledger]]):* Track-02 T2.11 Spanda-oscillator laws → six manifested expected-reds in `portal-core tests/kernel_truth.rs` (`spanda_hkb_antiphase_bistability` · `spanda_standing_identity_superposition` · `tick12_flowers_from_oscillation` · `codon_advances_on_rotational_state_not_tick12` · `spanda_bistability_threshold_asymmetry` · `spanda_two_involutions_distinct`); psychoid `cymatic_signature[64]` → expected-red (Track 08). New greens: involution Klein-four-group (`spanda_involutions.rs`), codon-annulus 2π/64 + Rust↔C Hopf mirror (`k2_geometry_reference.rs`), 72→64 epogdoon Rust↔C↔wire mirror (`epogdoon_bridge_mirror.rs` + live-wire `mahamayaBridgeLaws`), χ-solver kernel reference (`cymatic_chi_reference.rs`), 72-address law (`resonance72_address_law.rs`), ratio-set law (`matheme_harmonic_ratio_laws.rs`), natal→archetype integration (`entity_computation_end_to_end.rs`); `kleinFlip` typed on the wire (schemas + live-wire).

## §5. Data-Spine Violations of Record

1. `oracle.rs:1236-1665` reimplements codon charges; `m3_compute_charges` has zero Rust callers (33/4.13) — the standing kernel-side violation. *(Update 2026-07-03: the oracle has since split into `nara/oracle_engine.rs` + `oracle_frame.rs`, which still carry the independent ±32-per-line algebra; portal-core's `m3_transcription_bridge` now DOES call `m3_compute_charges_ffi`. Automated red: `epi-cli tests/kernel_truth_oracle.rs::oracle_eval4_charges_match_kernel_codon_charge_authority` — kernel-truth stage.)* **CLOSED 2026-07-12 (T33.2):** the codon-charge reimplementation is retired — `oracle_frame.rs::oracle_eval4` derives the six yin/yang lines into a 6-bit codon and routes pp/nn/np/pn through the single kernel authority via `crate::ffi::kernel::compute_codon_charges` → `m3_compute_charges_ffi` (m3.h:681; the C `m3_compute_charges` is at m3.h:675, **not** the stale `:755-767` citation in the stub/index). The named red **`oracle_eval4_charges_match_kernel_codon_charge_authority` is now GREEN** (64-codon cross-check vs `bioquaternion_transcription`, zero tolerance); the boot assert lives in C (`m3_verify_integral_invariant` via `m3_verify`, sum(pp)=1440=360×4) with a Rust mirror (`ffi::kernel::tests::ffi_pp_integral_invariant_matches_c_kernel`). `nara/oracle_engine.rs` is **not** a codon-charge reimplementation — it is the tarot ELEMENT-charge modality (card → element → charge-face); with no 6-bit codon in a spread it cannot call `m3_compute_charges`, so it instead shares the spine at its two real seams (the pp/nn/np/pn charge faces; canonical §5.16 element IDs `element::{AGNI,APAS,PRITHVI,VAYU}` in the Ace branch) and single-sources its per-card magnitude as `TAROT_ELEMENT_CHARGE_UNIT`, now drift-guarded by `nara::oracle_engine::tests` (5 behavioral tests).*
2. Three divergent canon readers (bimba-mcp / `epi core knowing` / `s2'.coordinate.resolve`) pending 17.28 byte-identity.
3. `kernel.rs` energy plain-sum (E₄ missing from `kernel_energy_evaluate`) vs the 4:5:6 law (33). *(Update 2026-07-03: E₄ + the 4:5:6 `canonical_total_energy` have landed; E₅/E₆ are hardcoded zeros. Automated reds: `portal-core tests/kernel_truth.rs::kernel_energy_carries_e5_e6_with_456_weighting` + `epi-cli tests/portal_clock_state.rs::cli_portal_clock_uses_real_kernel_projection_after_cast` — kernel-truth stage.)*
4. `m1_ananda_get` 10×10 %10 core unfaithful to the canonical 12×12 raw+DR CSV (10.10) — blocks `ananda_vortex` bussing. *(Automated red 2026-07-03: `portal-core tests/kernel_truth.rs::m1_ananda_12x12_raw_fidelity_vs_vortex_modulae_csv` — kernel-truth stage.)*
5. `khora_sync_queue_flush` stub (vault→Neo4j broken) + `fire_provenance` no-retry (CCT-16).
6. `graph_api.rs:323` writes `'kernel-resonance'` vs canonical `kernel_core` (CCT-13).
7. `compositor.ts:32-43` silent truncation vs DR-COMP-1 VAK-compression law.
8. m2 plugin decan table duplicates the kernel authority (E1 finding, dedup pending).
9. **`day.rs` nests Present against flat-Present law (DR-PRANA-1 rider). (Architect-flagged 2026-07-27; CLOSED same day.)** Two drifts, one disease — a path law spelled in more than one place. **(a) SHAPE:** `vault/day.rs` carried a SECOND day-path constructor applying the archive's `{YYYY}/{MM}/W{week}/{DD}` shape to the Present root, so `epi vault day-init` and `archive-day` used nested paths while session init, `epii-agent-core` and the autoresearch spine used flat — day-init wrote day folders nothing else read (this is why assess kept reporting a missing daily note straight after running it), and its own unit test asserted the nested path, so a green test protected the bug. **(b) FORMAT:** epi-cli spelled `%d-%m-%Y` at TEN sites while the pratibimba-app carrier used CHARTER:28's month-first `%m-%d-%Y` (Architect-ratified 2026-07-02), so the CLI and the app built DIFFERENT day folders on every day whose day-of-month ≠ month. **Corrections landed:** `vault/paths.rs` is now the sole authority — one constructor (`day_folder`/`day_folder_for_date`) and one format (`DAY_ID_FORMAT` + `format_day_id`/`parse_day_id`); `day.rs` re-exports rather than redefines; all ten format sites route through the constant; `parse_day_id` still accepts legacy day-first and ISO so existing folders resolve. New standing guard `epi-cli/tests/vault_present_day_path_authority.rs` (7 tests) fails the build on a second Present constructor (source scan, test modules excluded, two documented non-day-folder exemptions), a year/week-shaped Present segment, a de-nested or Present-rooted archive, or a CLI↔carrier format disagreement (it READS `pratibimba-app/src-tauri/src/vault.rs`). Guard proven RED on reintroduction, then GREEN. Archive nesting confirmed correct and untouched. **Vault migrated:** 25 historical day folders renamed day-first → month-first with references rewritten inside `Empty/Present/**` only (plan/ledger prose elsewhere is a historical record, deliberately untouched); `07-07-2026` was a natural no-op; the live day was skipped while parallel m-dev agents held it. Receipt: `cargo test` in epi-cli 363 passed / 2 failed, both pre-existing ta-onta port-fidelity failures in Track 50's lane (`agent_extensions_ta_onta.rs:338,392`), unrelated to vault paths.
10. **Static-dataset-as-graph-truth (Architect-flagged 2026-07-12; CLOSED same day, corrective sweep).** The live gateway method `s2.parashaktiCorrespondences` (`epi-cli gate/graph.rs`) and `epi core knowing` (`core/quintessential_view.rs`) served graph data by reading `Idea/Bimba/Map/datasets/**` JSON off disk, and `kernel_bridge_runtime_contract.rs` LOCKED the violation in ("should resolve without Neo4j"). Architect law restated: the live Neo4j Bimba graph is the baseline ontology (M' app = surface projection of the M map); the epi CLI + gateway is THE integrated surface; legitimate offline behavior is C/Rust kernel-LUT law ONLY; datasets are one-way Neo4j seeds, never serving-truth; bimba-mcp is outward tooling, not the integration point. **Corrections landed:** (a) `s2.parashaktiCorrespondences` re-backed — decan chain bridge-side from kernel LUTs (`ZODIAC_DECAN_TABLE`/`PIP_DECAN_MAP`, LAW-24 ONE-LUT), asma/maqam/planetary prose from live Neo4j (`DivineName`/`Maqam`/`PlanetaryHarmonic`/`ChakralCenter`) over the existing `Neo4jClient` seam, `graphUnavailable`+per-section `provenance` honest-absence when the graph is down, `read_parashakti_deep_nodes` and every dataset read DELETED, contract test INVERTED (kernel-decan assertions + dataset-reader-gone grep + a live cross-check that fails loudly without Neo4j); kernel↔live alignment proven — all 36 kernel decans map 1:1 onto live `Decan` nodes, `PIP_DECAN_MAP` tarot matches `m_2_3_tarot_card` per node. (b) `epi core knowing` re-oriented onto live `:Bimba` reads via the same seam; dataset reads deleted; honest-absent offline; live test pinned to a real M2-3 property. (c) NEW harness class **G (live-graph)** + verify-all stage `graph-live`: bolt probe + a real `epi` binary end-to-end read of live M2-3 asserting a real live property — CLI → gateway/graph-services → Neo4j in one pass, FAILS (never skips) when Neo4j is down; fixtures/static datasets can no longer close graph-truth tranches. (d) bimba-mcp `Neo4jMapIndexSource` now exercised by a real live-Neo4j integration test (was fixture-only); the fixture's "real in-memory live-graph source" mislabel corrected. Two value-level surface decisions awaiting Architect ratification (shape unchanged, flagged in the close): address72→decan now `address72/2` (2:1 Shem-quinance fold, zodiacally indexed — the old `%36` file-order mapping was not astronomically indexed and is retired) and `planetaryMode` honest-null (the live graph carries no diurnal/nocturnal sect; if sect is wanted it belongs in a kernel LUT). Related: item 2's `epi core knowing` divergent-reader leg is materially advanced by (b); byte-identity across the three readers remains 17.28's lane.
11. **CLI-as-integrated-surface sweep (Architect-ordered 2026-07-13; CLOSED same day).** Follow-through on item 10: audited every cycle-3 test surface for fixtures-where-the-CLI-should-be-driven ("test everything everytime" through the epi CLI, which routes everything). Audit verdict: the house pattern is sound (28 `gate_*` suites spawn the real gateway via `TestGatewayClient`; `run_epi` drives the real binary across most CLI families; walk + m0-rail e2e are genuine live-graph gates), with six violations found and corrected: (a) `contemplation_rpc_dispatches.rs` was the one fully-faked RPC seam (`deterministic_mock:true`) — new `gate_contemplation_live.rs` drives `nara.contemplate_session_close` over a real spawned gateway and PINS THE HONEST TRUTH that the method has NO live adapter (catch-all "not a known nara method"; the mock's synthetic wisdom_delta tokens can never travel the wire; both files flip when a real S0/S4/S5 adapter lands — documented inline); (b) `epi bimba` had no binary-seam test — new `cli_bimba_binary.rs` drives propose|list|show|land|refuse across 7 real process invocations with cross-process ledger persistence proof; (c) the oracle surface had no live driver — new `gate_oracle_live.rs` exercises the real oracle route over a spawned gateway; (d) `m2-correspondence.spec.ts` silently passed graph-absent (kernel-LUT + em-dash-tolerant assertions) — upgraded to a REAL live-graph gate (asserts live provenance + live-only values, fails when Neo4j is down) and `global-setup.ts` now passes explicit `EPILOGOS_NEO4J_*` env to the spawned gateway; the `M2CorrespondencePane.test.tsx` fixture that asserted the impossible `planetaryMode:"diurnal"` is corrected; (e) `planetaryChakral.planetaryMode` re-pointed from hardcoded null to the LIVE graph's `c_0_modal_signature` (the Bimba map's actual planet-mode concept — octaval, not sect; per-planet honest-null for thin outer-planet stubs M2-5-8/9; the 36 decans are ruled by the seven classical planets per LAW(24), so the adapter path is always non-null in normal operation); (f) NEW standing `lut_graph_parity_live.rs` makes the map the PERMANENT authority over the kernel LUTs (36 Decan nodes keyed 1:1 vs ZODIAC_DECAN_TABLE incl. tarot, 72 DivineName count+keyed matches, planet/chakra keyed matches; fail-loud-offline). Posture corrections: `Idea/Bimba/Map/datasets/` now carries its deprecation notice (seed archive, seeding complete, nothing reads it at runtime/test; Vortex Modulae CSV separately sanctioned as spec-law per the 00-harness register); the misnamed `live_gateway_smoke.rs` and `coordinate_phase_preservation_e2e.rs` renamed so live/e2e labels never provide fixture cover. Remaining sanctioned dataset touchpoints: `dataset_import` (historical seed tooling) and the Vortex CSV (spec-law). All five workers adversarially reviewed solid with stillFixtureGrade=false.
