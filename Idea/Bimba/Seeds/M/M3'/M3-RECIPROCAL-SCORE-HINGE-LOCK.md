# M3 Reciprocal Score-Hinge Lock

Status: focused companion to the M3 deep capability matrix  
Coordinate: `M3 Mahāmāyā` / `M3′ epi.deep.m3`  
Wayfinder: `EpiLogos/Epi-Logos-C-Experiments#32`

## Position

Mahāmāyā is not only the terminal inscription of a forward cosmic calculation. It is the reciprocal score hinge between played world-condition and situated return.

```text
M1 + M2 performed world-event
        ↓
M3 score / transcription
        ↓
M4 lived episode

and

M4 entity / event / narrative
        ↓
M3 re-inscription / score
        ↓
M2 vibrational relation plan
        ↓
M1 harmonic realization / performance
```

The two directions use the same M3 distinctions. A score is therefore a provenance-bearing determinate form capable both of recording what became actual and of organising subsequent replay/transduction.

## Forward score

A forward M3 score should preserve enough source fibre to remain inspectable and replayable:

```text
M3Score {
  address64
  nucleotide_codon_state
  iching_transform
  charge_quaternion
  rotation
  tarot_form
  clock_position

  source_m1_ref
  source_m2_relation_plan_ref
  source_world_event_ref
  provenance[]
}
```

Exact ABI remains body work. The important constraint is that M1/M2 source information is not erased when the 64-fold determination is written.

## Return score

The reciprocal direction begins from a situated whole:

```text
M4 entity / event / activity / narrative
  ↓
identity + elemental + temporal + contextual evidence
  ↓
M3 primary 64-fold selection
  ↓
I-Ching / codon / Tarot / clock articulation
  ↓
compile replay evidence
  ↓
M2RelationPlan
  ↓
M1 harmonic realization
```

The current central kernel gap remains exact and first-class:

```text
Q_entity / Q_composed
→ canonical primary nucleotide / codon / address64
```

The existing system can operate richly once a primary address is available; it does not yet justify a universal inverse-selection law. That law must be derived from the existing elemental, L2′, M2, charge and operator relations rather than supplied by convenience or nearest-neighbour matching.

## 72↔64 relation

M3 receives Paraśakti through the already-developed proportional seam:

```text
M2 72-fold differentiated field
        ↓ 8/9
M3 64-fold determinate field

72 / 64 = 9/8
```

The same epogdoon is generated earlier in the M1 musical theorem as:

```text
2 / (16/9) = 9/8
(3/2) / (4/3) = 9/8
```

M3 does not retroactively generate that ratio. It consumes the later M2/M3 articulation of a relation already present in the theorem spine.

## Clock relation

M3-5 supplies temporal/world placement to both directions:

```text
forward:
current M1/M2 world-condition
→ M3 score
→ clock placement
→ M4 lived episode

return:
M4 episode/narrative
→ M3 re-inscription
→ clock/archetypal placement
→ M2 current/replayed world relation
```

M2-5 planetary/chakral state and M3 clock therefore compose directly. The user/entity is an embodied elemental/chakral field related to the same planetary world-condition the clock is calculating.

## Capability delta

| Capability | Source | Operation | Target | State |
|---|---|---|---|---|
| Played-world score | M1+M2 event | 64-fold determination + transformation + archetypal/temporal articulation | M3′ / M4 | established architecture, partial executable body |
| Nara/entity re-inscription | M4 | situated evidence → primary M3 form | M3 | authored relation; canonical primary-address law still open |
| Replay compilation | M3 score | preserve/resolve score fibres into a bounded M2 relation request | M2 | explicit return contract; compiler body open |
| Clocked planetary/chakral return | M3-5 + M2-5 + M4 | temporal/celestial placement ↔ embodied elemental/chakral identity | M2/M4 | established cross-coordinate relation; provider/body work continues |

## Integrated `1-2-3` and M3′ distinction

The integrated Cosmic surface uses Mahāmāyā as the determinate third moment of one current M1/M2/M3 event.

`M3′` separately exposes Mahāmāyā as a professional symbolic/genetic/archetypal/clock instrument: a human or Agent can inspect addresses, transformations, Tarot forms, rotations and temporal placement directly, and can use the score as a replayable artefact.

These are two compositions of the same M3 field, not separate ontologies.

## Acceptance

A full reciprocal trace is accepted when the system can show:

```text
M1 state
→ M2 relation plan
→ M3 score
→ M4 episode

same M4 episode
→ M3 re-inscription / score
→ M2 vibrational plan
→ M1 harmonic realization
→ performed/observed return
```

and the returned trace preserves:

- M3 primary address and exact derivation provenance;
- I-Ching/codon/Tarot/rotation/clock state;
- M1 source phase/ratio/relation references;
- M2 native decoded views plus M2-5 planetary/chakral state;
- M4 entity/event/narrative identity;
- observations from the replayed/performed return.
