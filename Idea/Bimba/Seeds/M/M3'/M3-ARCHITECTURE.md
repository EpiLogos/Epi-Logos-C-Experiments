---
title: "M3' Mahāmāyā Architecture — Total Shape, Substrate Map, Profile-Bus Contract, Visual Rendering & Tick Choreography"
coordinate: "M3 / M3'"
status: "canonical-architecture-spec"
created: 2026-06-02
authority_relation: "Domain authority for the total shape of the M3' surface. M3'-SPEC §§7-9 are pre-existing law that this document organises into a buildable whole. Where they disagree, M3'-SPEC remains authoritative for substrate contracts; this document is authoritative for the surface-build, sub-coordinate layout, and tick choreography. Companion doc to M3'-SPEC, not a replacement."
depends_on:
  - "[[M3'-SPEC]]"
  - "[[m3-prime-symbolic-transcription-research]]"
  - "[[full_theoretical_alignments_ql_physics]]"
  - "[[alpha_rasa_bridge_ql]]"
  - "[[m3-prime-clifford-consolidation]]"
  - "[[alpha_quaternionic_integration_across_M_stack]]"
  - "[[mahamaya-ux-full-m3-branch]]"
  - "[[M1-2-ANANDA-VORTEX-ARCHITECTURE]]"
related_architecture:
  - "Idea/Bimba/Seeds/M/M0'/M0-ARCHITECTURE.md"
  - "Idea/Bimba/Seeds/M/M1'/M1-ARCHITECTURE.md"
  - "Idea/Bimba/Seeds/M/M2'/M2-ARCHITECTURE.md"
  - "Idea/Bimba/Seeds/M/M4'/M4-ARCHITECTURE.md"
  - "Idea/Bimba/Seeds/M/M5'/M5-ARCHITECTURE.md"
  - "Idea/Bimba/Seeds/M/INTEGRATED-1-2-3-COSMIC-ENGINE-ARCHITECTURE.md"
  - "Idea/Bimba/Seeds/M/INTEGRATED-4-5-0-RECOGNITION-ARCHITECTURE.md"
decisions:
  - "DR-M3-1 — TCT / Nine of Wands cardinality (runtime 7 wins; dataset reconciles)"
  - "DR-M3-2 — 72→64 epogdoon uniqueness/injectivity scope + gap-marker contract"
  - "DR-M3-3 — `M1_LENS` (12) vs `M3_LENS_STACK` (16+1) namespace split"
  - "DR-M3-4 — Transcriptional clock chain landing contract (kernel → packet)"
related_tranches:
  - "04.1 — Codon-rotation projection field contract closure"
  - "04.2 — Summonable inspectors + four depth-view modes execution"
  - "04.3 — Clock-field-as-live-data-structure angular/hop edge overlay"
  - "04.8 — Integrated transcription clock chain"
  - "07 / 08 — Integrated 1-2-3 cosmic-engine composition (M3 pole)"
  - "10.x — Profile-bus extensions (DR-M3-4 `transcription_packet` field)"
  - "15.6 — Inline provenance rendering"
  - "15.7 — Bimba/Pratibimba state persistence"
---

# M3' Mahāmāyā Architecture

## §0. Frame

**M3 is the symbolic-genetics transcription engine; M3' is that engine made playable as the 360° clock-field.** Where M1-2 puts a *vortex* on a torus, M3' puts a *clock* on a torus — but the torus is no longer a single K². It is the **double-torus K² × T²_Mahāmāyā** (M3'-SPEC §8.13), with M3 (the binary-genetic Matter substrate) carrying the 64-codon law and the 472-state rotational landscape that the M1' instrument's 84 `(lens, mode)` cells *can play through but cannot exhaust*.

The total shape: **a clock-field of nodes pinned by clock geometry, with cross-clock relations rendered as angular / hop / traversal edges, perturbing as the kernel tick advances** (UX §4). The "cymatics" of M3 is not a visual on top of data — it is the data, geometrically arranged.

This document is the **total shape** for the M3' surface across its six sub-coordinates. Substrate is overwhelmingly landed (cycle-2 closed the 472-state contract); the work is rendering over law, plus four contradiction-decisions (DR-M3-1..4) that the kernel/profile contract must close before deep inspectors fire. The six commitments of M3'-SPEC §0/1 ground every section below.

The bimba ↔ pratibimba dial at this scale: **M3 IS the binary-genetic substrate** (Matter, A=6, T=9, C=7, G=8 — `m3.h:480-486`) and **M3' IS the playable clock-field rendering of that substrate** through the same Cl(4,2) algebra that M1-5 and M4-4-4-4 use. **Frequency (M2) crystallises into Matter (M3) through Venus's 9:8 epogdoon** (`m3.h:351-353`). The wheel turning is Matter taking form.

Per [[full_theoretical_alignments_ql_physics]], M3' also owns the **measurement-face inspector** for the system: the surface that keeps exact QL symbolic skeletons (`64+72+1`, `64+2(36)+1`, `128+8+1`, `0,4,2,2,9`) visibly distinct from the physics descent (`G_SM -> D_mu -> running couplings -> electroweak mixing -> QED/QCD -> hydrogen spectrum`) and from Nara's recognition/apparatus register. This is a rendering/provenance contract, not a local physics calculator.

---

## §1. The Six Sub-Coordinates

| Sub-coord | Position | Owns | Provides (M3') |
|---|---|---|---|
| **M3-0' Reception** | #0 / Ground | The 72→64 epogdoon transduction; M3-0 reception law (SPEC §8.12) | Provenance strip rendering: `M2-5 72-source → DET floor(idx×8/9) → 64-address → nucleotide triplet → codon quaternion`. Honest gap-rendering at fold/collision. |
| **M3-1' Rotation** | #1 / Definition | The 472-state pair-composition algebra (SPEC §7, §8.5) | Modal-inversion landscape rendering: `(lens, mode) → (codon, rotation)` projection, `40×7 + 24×8 = 472`. Per-codon 7/8 state strip; rotational degrees as 45° steps. |
| **M3-2' Lens-mode** | #2 / Operation | The 84-state shared playing landscape (M1' upstream) projected into M3 | The wheel as **modal-extension navigator** — what extended-rotation cells the active `(lens, mode)` can currently reach. |
| **M3-3' Clock-field** | #3 / Pattern | `CLOCK_DEGREE_LUT[360]` + 384 line-change graph + 24-spoke 15° lattice + 12-deep ring-buffer (SPEC §8.6) | The default user-facing surface: 360° wheel with 24 backbone-spokes, hexagram-cell-per-degree, angular/hop/traversal edge overlay (UX §4). |
| **M3-4' Tarot/I-Ching/symbolic** | #4 / Context, Lemniscate | The 56+8 Tarot exact-cover, Major Arcana → 22 autosomes, DNA/RNA phase, transcription clock chain (SPEC §8.7, §8.15) | Tarot-card surface (the user-loved face); summonable Major Arcana / chromosome panel; the `TranscriptionalClockPacket` chain (DR-M3-4). |
| **M3-5' K² × T²_Mahāmāyā double-torus** | #5 / Integration, Möbius return | The co-foliated world-clock topology — K² (audio-genesis from M1-5) × T²_Mahāmāyā (inscription × lens) (SPEC §8.13); Pauli-Jung warrant (§8.9.1); measurement-face / coupling-flow alignment from [[full_theoretical_alignments_ql_physics]] | The four depth-view modes: Flat Clock Debug · Lens Annulus · Toroidal / World Clock · Hopf Identity, plus the summonable Coupling-Flow / Measurement-Face inspector. The boundary against which M1-5 played-torus must not encroach. |

**The bimba ↔ pratibimba dial across the six:** M3-0 receives, M3-1 rotates, M3-2 projects, M3-3 clocks, M3-4 reflects, M3-5 totalises. The first three are *inputs from outside* (M2 frequency, internal algebra, M1 lens-mode); the last three are *outputs to the world* (the wheel the user sees, the cards they turn, the double-torus they navigate). M3-0/M3-5 are the implicate generator-poles (sin/cos signature −1 in Cl(4,2)); M3-1..M3-4 are the explicate derived ratios (tan/sec/cot/csc signature +1). Cycle-3 §8 audit will verify this Cl(4,2) signature distribution against M1-5's `CL42_BASIS[6]` at `m1.h:629-636`.

---

## §2. Substrate Map

All citations are file:line in `Body/S/S0/`. M3' renders against this substrate; it does not duplicate, fork, or re-derive it.

### §2.1 M3-0' Reception substrate — 72 → 64 transduction

C law in `Body/S/S0/epi-lib/include/m3.h`:

| Symbol | Decl | Role |
|---|---|---|
| `apply_epogdoon_compression(m2_idx_0_to_71)` | `m3.h:351-353` | `(m2_idx * 8) / 9` — the integer DET reduction |
| `is_evolutionary_gap(m2_vibration_index)` | `m3.h:356-360` | Returns true when round-trip `(idx×8/9)×9/8` does not recover input — the **9 fold-points** that don't close |
| `get_parashakti_frequency(rot_state, is_shadow)` | `m3.h:344-348` | Ascending M3→M2 mirror; +36 for shadow phase |

Rust mirror in `Body/S/S0/portal-core/src/kernel.rs:366` — `pub resonance72: MathemeResonance72Projection` carries the M2-side index that DET reduces. The reduction is integer-deterministic but **many-to-one**: 9 of the 72 fold (DR-M3-2 pending).

### §2.2 M3-1' Rotation substrate — 472-state pair-composition algebra

| Symbol | Decl | Role |
|---|---|---|
| `M3_PAIR_MATRIX[16]` | `m3.h:155` | The 16 dinucleotide pairs `(sum_value, difference_value)` — the evaluation engine |
| `M3_MATRIX_QUATERNION_AXIS[3]` | `m3.h:166-170` | i/j/k axes for COMPLEMENTARY/MOVING_RESTING/SAME_QUALITY matrix families |
| `compute_rotational_state(p1, p2)` | `m3.h:190-195` | `(sum1+sum2, diff1+diff2)` |
| `compute_rotational_state_safe(...)` | `m3.h:199-209` | Variant honoring `M3_RESONANCE_GAP=0xFF` → `STATUS_PROVISIONAL_BIT` |
| `M3_Rotational_Generation` struct | `m3.h:533-542` | 8-fold raw pair-composition output: `pair1_idx, pair2_idx, resulting_codon, polarity, rotation_slot, rotation_degrees, rotational_value, is_non_dual` |
| `M3_Rotational_Profile` struct + `[64]` LUT | `m3.h:544-550, 578` | Per-codon canonical: `state_count` (7 or 8), `state_type`, `anchor_pair_a/b`, `paired_codon` |
| `m3_classify_codon(codon)` | `m3.h:721-731` | The algorithmic 3-tier classifier — PerfectPalindromic / ImperfectPalindromic / NonPalindromicNonDual / Dual |
| `m3_codon_rotation_count(codon)` | `m3.h:738-740` | 7 if non-dual, 8 if dual |
| `_Static_assert` 40+24=64 + 40×7+24×8=472 | `m3.h:707-711` | Compile-time invariants binding the spec law |
| `M3_CODON_CLASS[64]` master LUT | `m3.h:742-743` | Initialised once by `m3_init_codon_class_lut()` |

Rust mirror at `Body/S/S0/portal-core/src/rotational.rs`:
- `PM: [(i8, i8); 16]` at `rotational.rs:3-20` — mirrors `M3_PAIR_MATRIX[16]`
- `generate_rotational_states(codon6bit)` at `rotational.rs:61-114` — mirrors `m3_generate_rotational_states()` per `m3.h:904-906`
- `Polarity::{Negative, Positive}` at `rotational.rs:24-27` — mirrors `M3_Rotational_Polarity`
- 4 negative-valence rotations at `rotational.rs:70-83` (vary outer-of-second, fix first_pair)
- 4 positive-valence rotations at `rotational.rs:85-98` (vary inner-of-first, fix last_pair)
- Sort by `rotational_value` at `rotational.rs:100-111` — produces canonical `rotation_slot` ordering

### §2.3 M3-2' Lens-mode substrate — the projection map

Materialised at `Body/S/S0/portal-core/src/codon_rotation_projection.rs`:

| Symbol | Line | Role |
|---|---|---|
| `LENS_COUNT = 12`, `MODE_COUNT = 7`, `LENS_MODE_COUNT = 84` | `codon_rotation_projection.rs:5-7` | The **M1'/MEF** lens-mode landscape (12 chromatic anchors × 7 CF modes — **NOT** the M3 16+1 lens stack; DR-M3-3) |
| `CODON_COUNT = 64`, `CODON_ROTATION_SURFACE_COUNT = 472` | `codon_rotation_projection.rs:8-9` | The M3 codon-rotation surface cardinality |
| `MathemeLensMode { lens, mode }` + `index()` + `lens_label()` + `mode_name()` | `codon_rotation_projection.rs:11-42` | The 84-cell type; `lens<6` → `"L0..L5"`, `lens>=6` → `"L0'..L5'"` |
| `CodonRotationSurfaceCell` | `codon_rotation_projection.rs:44-54` | One cell on the 472-state surface |
| `CodonRotationProjection` | `codon_rotation_projection.rs:56-74` | Forward + reverse map with `dataset_lut_state` provenance string |
| `codon_rotation_surface()` | `codon_rotation_projection.rs:76-99` | Enumerates the 472 cells deterministically |
| `codon_rotation_from_lens_mode(lens, mode)` | `codon_rotation_projection.rs:101-107` | Forward map |
| `lens_mode_from_codon_rotation(codon_id, rotation)` | `codon_rotation_projection.rs:109-115` | Reverse map |
| `codon_charge_quaternion(codon_id)` | `codon_rotation_projection.rs:117-126` | Returns normalised `[pp, mm, mp, pm]` from the 6/9/7/8 I-Ching values |
| `forward_surface_index()` / `reverse_lens_mode_index()` | `codon_rotation_projection.rs:153-159` | Current implementation is a **proportional** projection; DR/cycle-3 4.1 audits whether this is the canonical semantic map or a placeholder |
| `dataset_lut_state: "materialized-kernel-lut"` | `codon_rotation_projection.rs:148` | Provenance string the renderer surfaces verbatim |

### §2.4 M3-3' Clock-field substrate — the 360° topology

| Symbol | Decl | Role |
|---|---|---|
| `CLOCK_DEGREE_LUT[360]` | `m3.h:980` + `Body/S/S0/epi-lib/src/m3_clock_lut.c` (auto-generated, 749 LOC) | One entry per degree node, 27 fields per `Clock_Degree_Entry` (`m3.h:936-978`): `degree_node_360`, `exact_degree_720`, `zodiac_sign`, `zodiac_degree`, `decan_idx`, `decan_position`, `is_backbone_node`, `hexagram_id`, `hexagram_line_active`, `is_non_dual_codon`, `codon_class`, `codon_upper_pair`, `codon_lower_pair`, `tarot_card_id`, `decan_planet`, `decan_element`, `decan_chakra`, `tick12`, `strand`, `dr_ring`, `m1_ananda_value`, `m0_archetype`, `shadow_degree`, `polar_opposite`, `enneadic_chamber`, `chamber_day_night` |
| `_Static_assert(360 + 24 == 64 * 6)` | `m3.h:933-934` | The 384 line-change topology law — compile-time enforced |
| `M3_RES_MATRIX[64]` + `m3_resonance_lookup(...)` | `m3.h:386-396` | The 56+8 resonance structure; 8 gaps at `0xFF` → `STATUS_PROVISIONAL_BIT` |
| `M3_COMP_MATRIX[64]` | `m3.h:403` | `comp[i] = i ^ 0x3F` — 180° I-Ching rotation |
| `M3_MOVE_MATRIX[64]` | `m3.h:404` | Trigram swap |
| `M3_Wheel_State` struct | `m3.h:442-448` | `current_degree (0-719)`, `layer (primary/shadow)`, `active_sectors` bitboard, `season`, `heaven_mode` |
| `M3_Ring_Buffer` struct | `m3.h:451-455` | 12-deep SU(2) double-covering history (6 QL × 2) |
| `polar_opposite_su2(degree_720)` | `m3.h:370-374` | SU(2)-preserving polar opposite — stays in active layer |
| `identity_returned(M3_Wheel_State*)` | `m3.h:458-460` | True only at `current_degree >= 720` — the 4π full return |

### §2.5 M3-4' Tarot/I-Ching/symbolic substrate

| Symbol | Decl | Role |
|---|---|---|
| `Tarot_Suit` enum | `m3.h:300-305` | `SUIT_CUPS=0/A, SUIT_WANDS=1/T, SUIT_PENTACLES=2/C, SUIT_SWORDS=3/G` — suit index = nucleotide 2-bit value |
| `M3_TAROT_CODON_MAP[4][16]` | `m3.h:625` | The 56+8 exact cover; 4 suits × 16 slots; `codon_b = 0xFF` for single-codon cards, otherwise dual-codon court |
| `M3_MINOR_ARCANA_COUNT=56u`, `M3_MAJOR_ARCANA_COUNT=22u`, `M3_TRANSCENDENT_TAROT_COUNT=2u`, `M3_TAROT_QUATERNION_COUNT=80u` | `m3.h:632-635` | The 56 + 22 + 2 = 80 Tarot quaternion points |
| `M3_Major_Arcana_Entry { card_id, name, chromosome_pair (1-22), amino_acid_index (0-21) }` + `[22]` LUT | `m3.h:637-644` | The 22 autosome bridge |
| `M3_Trigram` + `[8]` LUT | `m3.h:263-271, 281` | Per-trigram: binary, earlier/later heaven position, element, family role, degree anchor |
| `M3_Hexagram` + `[64]` LUT | `m3.h:273-279, 282` | Per-hexagram: `id = upper<<3 \| lower`, `line_pattern`, `complement_id = id ^ 0x3F`, nuclear trigrams |
| `M3_IChing_State` | `m3.h:285-291` | Active bitboard, current hexagram, changing lines, result, RNA-phase flag |
| `M3_RNA_FUNCTIONAL_MASK`, `M3_RNA_DARK_MASK` | `m3.h:659-660` | Partition the 64-bit codon-word; `MASK & DARK == 0`, `MASK \| DARK == 0xFFFFFFFFFFFFFFFF` (M3'-SPEC §8.1) |
| `M3_TAROT_PIP_*` (ACE, PRINCESS=10, PRINCE=11, QUEEN=12, KING=13) | `m3.h:616-621` | Pip indices |
| `m3_tarot_rotation(card_id)` | `m3.h:798-826` | Returns the per-card quaternion: Minor Arcana via codon slerp; Major Arcana as angle around y-axis; Transcendent as `±k` (Fool/World) |
| `m3_tarot_translate(card_id, source_pos, codon_to_hexagram)` | `m3.h:828-844` | Bidirectional codon ↔ hexagram quaternion rotation via the Tarot card |
| `CODON_TO_AA[64]` + `AA_STOP=10` | `Body/S/S0/portal-core/src/codon.rs:3-9` | Amino-acid translation; Stop codons at `0x10`, `0x13`, `0x1C` (per `codon.rs:84`) |
| `wc_anticodon(codon)` | `codon.rs:37-42` | Watson-Crick anticodon (reverse-complement); XOR each nucleotide with `0x01` |
| `codon_sequence(codon)` | `codon.rs:45-52` | Decodes to `[A/T/C/G; 3]` |

### §2.6 M3-5' Double-torus substrate

The K² side is **M1-5's substrate** (do not fork). The T²_Mahāmāyā side has no separate C/Rust type — it is the (inscription-circle × lens-circle) parameterisation that `CLOCK_DEGREE_LUT` already provides indirectly:

- **Inscription circle** = the 360° degree-axis × the 720° SU(2) double-cover (`m3.h:370-374` polar opposite; `m3.h:443` `current_degree (0-719)`)
- **Lens circle** = the 16+1 `M3_LENS_STACK` (NOT in code — DR-M3-3 names the canonical home; until then UX-side only)
- K² mesh and `RING_QUATERNION_LUT[12]` come from `Body/S/S0/epi-lib/include/m1.h:551-564` — **M3-5 borrows; does not duplicate**
- Pauli-Jung Várlaki/Rudas `1 + 2×32 + 2×36 = 137` (SPEC §8.9.1) is a **rendering grammar warrant**, not a substrate addition

### §2.7 Cl(4,2) — one algebra, four scales

The codon-as-quaternion identity (`m3.h:478-488` for `evaluate_codon`, `m3.h:490-497` for `m3_eval_to_quat`, `m3.h:499-506` for `m3_quat_to_eval`) lives in the **same `Quaternion` type** as M1's ring (M1-2 architecture §2.6) and M4-4-4-4's personal quaternion. The shared type is at `Body/S/S0/portal-core/src/quaternion.rs`; cycle-3 tranche 4.7 audits "one type, four call-sites".

The 4X invariant compile-asserted at `m3.h:770-771`: `(9+9+9) + (9-9-9) + (9-9+9) + (9+9-9) == 4*9` for TTT. Every codon partitions its vibrational content into four orthogonal axes.

### §2.8 Spanda↔Mahāmāyā parallel-track (M1-2 echo at M3 scale)

The 12-tick `tick12` of M1-3 Spanda IS what advances the `M3_Ring_Buffer` (6 QL × 2 helices = 12-deep). The `dr_ring` field of `Clock_Degree_Entry` (`m3.h:965`) records `0 = Mahāmāyā {1,2,4,8,7,5}` vs `1 = Paraśakti {3,6,9}` — the same DR rings M1-2 owns (M1-2 architecture §2.3). M3' renders which DR ring is currently active per degree; M1-2 owns the source.

---

## §3. Dataset Map

### §3.1 Mahāmāyā-deep narrative dataset

`Idea/Bimba/Map/datasets/mahamaya-deep/`:

- `nodes-full-detail.json` — bimba-map nodes for 64 codons / 56+8 tarot / 22 Major Arcana / 64 hexagrams with fully-attributed metadata. **DR-M3-1 contradiction lives here:** TCT (Nine of Wands) is marked `8/full-rotational` here; runtime says `7/non-dual`. Spec §10.1 and Wave-A row 11 already routed this to decision-register — dataset reconciles, runtime is authority.
- `relations.json` — `YIELDS_CODON`, `REFLECTS_DNA_FORM`, `GOVERNS_TAROT_EXPRESSION`, `TRANSLATES_TO`, `HAS_RNA_POTENTIAL`, `TRANSCRIBES_TO` edges (the §8.15 chain grammar)
- `rotational_state_protocol.txt` — textual protocol naming the pair-composition algebra that `rotational.rs:61-114` implements
- `fibonacci-60-pisano-integration.md` — companion for the +1 growth aperture (the Level-0 meta-position of DR-M3-3; the "17th lens" question)

### §3.2 Low-detail graph form

- `Idea/Bimba/Map/datasets/low-detail/nodes_mahamaya.json` — the bimba-map nodes M0' renders as graph and M3' addresses via S2 handles
- `Idea/Bimba/Map/datasets/low-detail/relations_mahamaya.json` — relational form; cycle-2 Track 02 owns the S2 population

### §3.3 The dataset/runtime authority dial

Per UX §8 and SPEC §4: **M3' renders only kernel-profile / S2-graph / S0-codec sources**. Dataset metadata flows into S2 via Track 02 (cycle-2); M3' surfaces it through the `M3LibrarySummary` provenance handle in the extension (`codon-wheel.ts:22-29`). When `wheelSummary.source === 'pending-S2-M3-library-summary'`, the wheel renders honest-pending — not a hand-filled table.

### §3.4 Clock-field auto-generated dataset

`Body/S/S0/epi-lib/src/m3_clock_lut.c` is **machine-generated** by `tools/build_clock_degree_lut.py` from the bimba-map; the header comment at `m3_clock_lut.c:1-22` declares the field order and the Neo4j-sourced vs first-principles fields. M3' renders against the compiled `.rodata` — never the python script directly.

---

## §4. Profile-Bus Contract

### §4.1 What `MathemeHarmonicProfile` already exposes for M3'

`Body/S/S0/portal-core/src/kernel.rs:344-387`:

| Field | Line | Role |
|---|---|---|
| `tick: u64`, `tick12: u8`, `cycle: u64` | `kernel.rs:353-355` | The kernel tick — M3'-SPEC §2 commitment 1: M3' has no private clock |
| `degree720: u16`, `degree360: u16` | `kernel.rs:356-357` | M3'-SPEC §1 commitment 1: 720° SU(2) never collapses |
| `su2_layer: String` (`"primary"` / `"shadow"`) | `kernel.rs:358, 428-432` | Layer indicator |
| `position6: u8` | `kernel.rs:360` | `tick12 % 6` |
| `lens_mode: MathemeLensMode` | `kernel.rs:363` | The shared 84-cell from M1' |
| `resonance72: MathemeResonance72Projection` | `kernel.rs:366` | M3-0' reception input (the M2 72-source) |
| `audio_octet: [f32; 8]` | `kernel.rs:367` | Vimarśa-window (M2-1' writes; M3' reads for pulse display) |
| `nodal_quartet: [MathemeNodalConstraint; 4]` | `kernel.rs:368` | Vimarśa-window (boundary/standing-wave display) |
| `mahamaya: MathemeBinaryProjection` | `kernel.rs:372` | The 64-address codec result (mahamayaAddress64, codon, hexagram_id, line_change_operator, etc., per `kernel.rs:741-805`) |
| `codon_rotation_projection: CodonRotationProjection` | `kernel.rs:373` | The forward `(lens, mode) → (codon, rotation)` projection — **the load-bearing M3' contract** |
| `q_cosmic: [f32; 4]` | `kernel.rs:374` | Codon charge quaternion (the `[pp, mm, mp, pm]` from `codon_charge_quaternion`) |
| `planetary_chakral: MathemePlanetaryChakralProjection` | `kernel.rs:370` | Read-only for M3-0' cosmic-panel display |

Constructor at `kernel.rs:389-465`: `from_tick(tick)` derives all M3 fields from the kernel tick alone — `lens_mode` from `(tick12, diatonic_degree)`, then `codon_rotation_projection = codon_rotation_from_lens_mode(lens_mode.lens, lens_mode.mode)`, then `q_cosmic = codon_charge_quaternion(codon_rotation_projection.codon_id)`.

### §4.2 What the profile-bus is missing — the load-bearing gaps

Three gaps, four DR decisions:

**Gap 1: The transcriptional clock chain packet (DR-M3-4).** SPEC §8.15 describes the `TranscriptionalClockPacket` YAML, but it has **no Rust counterpart** on the profile bus. The renderer would have to reconstruct it from `mahamaya + codon_rotation_projection + resonance72 + Clock_Degree_Entry`, violating the "windows onto writes" pattern. Cycle-3 tranche 4.8 lands the closure.

**Gap 2: The 16+1 Mahāmāyā lens stack (DR-M3-3).** The 12-count `MathemeLensMode.lens` field is the M1' chromatic-anchor, NOT the §8.10 M3-aperture stack. No `M3_LENS_STACK` field exists on the profile. The Lens Annulus depth view (§5.4 below) cannot render until DR-M3-3 closes the namespace and a field lands.

**Gap 3: The DET fold / gap-marker (DR-M3-2).** The `evolutionaryGap` field is a single bool; SPEC §10.1 names the need for a typed fold-state. The 9-of-72 indices that don't round-trip (`is_evolutionary_gap` at `m3.h:356-360`) need a profile-surface beyond a boolean.

### §4.3 The `transcription_packet` projection (per Tranche 4.8 / DR-M3-4)

Add `pub transcription_packet: TranscriptionalClockPacket` to `MathemeHarmonicProfile`, following the M1-2 `AnandaVortexProjection` pattern:

```rust
pub struct TranscriptionalClockPacket {
    /// The kernel clock state for this transcription.
    pub clock: TranscriptionClockSlot,

    /// The M3 16+1 lens-stack aperture (DR-M3-3 namespace).
    pub aperture: TranscriptionAperture,

    /// The base codon at this clock slot (CLOCK_DEGREE_LUT-indexed).
    pub codon: TranscriptionCodon,

    /// The matrix-path × polarity generation of this codon state.
    pub generation: TranscriptionGeneration,

    /// The DNA/RNA transcription state + amino/operator join.
    pub transcription: TranscriptionState,

    /// The Tarot reflection + hexagram + graph provenance.
    pub expression: TranscriptionExpression,

    /// Honest readiness — pending/materialised/provisional.
    pub readiness: TranscriptionReadiness,
}

pub struct TranscriptionClockSlot {
    pub degree720: u16,
    pub degree360: u16,
    pub walk_type: WalkType, // degree360 | amino24 | zodiac12 | spanda12 | decan36 | hexagram64 | enneadic9 | seasonal4 | lineChange384
    pub line_change_slot: Option<u16>,    // 0..383
    pub hexagram_id: u8,                  // floor(slot/6) when walk=lineChange384
    pub active_line: Option<u8>,          // slot % 6
    pub target_hexagram_id: Option<u8>,   // hexagram_id ^ (1 << active_line)
    pub line_change_operator: Option<u16>,// hexagram_id * 6 + active_line
}

pub struct TranscriptionAperture {
    pub m2_mef_lens: Option<u8>,          // 0..11 (the M1'/MEF chromatic-anchor lens)
    pub m3_lens_segments: Vec<u8>,        // 0..15 (the M3 16-aperture stack indices currently active)
    pub m3_growth_aperture: Option<String>,// the +1 Fibonacci-Pisano meta-position label
    pub namespace_state: ApertureNamespaceState, // resolved | provisional (DR-M3-3 closure flag)
}

pub struct TranscriptionCodon {
    pub codon_id: u8,                     // 0..63
    pub dna_codon: String,                // "ATG" form
    pub nucleotide_bits: [u8; 3],         // [n1, n2, n3], each 0..3
    pub i_ching_values: [u8; 3],          // [6/9/7/8 per nucleotide]
    pub quaternion_pp_mm_mp_pm: [i8; 4],  // raw before normalisation
}

pub struct TranscriptionGeneration {
    pub matrix_path: MatrixPath, // complementarity | movement | resonance
    pub polarity: GenerationPolarity, // negative | positive
    pub generated_codon: String,
    pub rotational_index: u8,
    pub rotational_state_count: u8, // 7 or 8
    pub rotation_degrees: u16, // idx * 45
}

pub struct TranscriptionState {
    pub rna_codon: Option<String>,
    pub transcription_state: TranscriptionStateKind, // dna_only | rna_potential | start | stop | shared_dark
    pub amino_or_operator: Option<String>,
    pub translation_provenance: TranslationProvenance, // graph | runtime_reconciled | pending_reconciliation
}

pub struct TranscriptionExpression {
    pub tarot_reflection_node: Option<String>, // #3-4.0-* graph node id
    pub tarot_card: Option<String>,            // e.g. "Nine of Wands"
    pub hexagram_label: Option<String>,
    pub graph_provenance: Vec<String>,
}

pub struct TranscriptionReadiness {
    pub dataset_lut_state: DatasetLutState, // materialized | pending_dataset_lut | provisional_gap
    pub warnings: Vec<String>,
}
```

**Anti-greenfield:** every field above derives from already-landed substrate — `CLOCK_DEGREE_LUT[360]`, `MathemeBinaryProjection`, `CodonRotationProjection`, `M3_TAROT_CODON_MAP`, `M3_MAJOR_ARCANA`, `CODON_TO_AA`. The Rust struct ports the SPEC §8.15 YAML to the profile bus. The pseudo-code chain at SPEC §8.15.lines 478-496 IS the constructor body. The renderer reads windows onto writes; never composes the packet itself.

### §4.4 The `m3_lens_stack` projection (per DR-M3-3)

Add `pub m3_lens_stack: MahamayaLensStack` to `MathemeHarmonicProfile`:

```rust
pub struct MahamayaLensStack {
    /// The 16 static Mahāmāyā clock apertures (M3-aperture surface).
    /// NOT the M1' 12-chromatic-anchor lens (that lives at MathemeLensMode.lens).
    pub static_segments: [MahamayaLensSegment; 16],

    /// The +1 Fibonacci-Pisano meta-position (Level-0 / growth aperture).
    /// Until DR-M3-3 closes the wording, this is provisional.
    pub growth_meta: Option<MahamayaLensSegment>,

    /// True only after DR-M3-3 is closed and companion specs converge.
    pub namespace_resolved: bool,
}

pub struct MahamayaLensSegment {
    pub segment_index: u8,                  // 0..15 (static) or 16 (growth-meta)
    pub degree_range: (u16, u16),           // inclusive start, exclusive end (degrees on 360° wheel)
    pub aperture_kind: ApertureKind,        // static | growth_meta
    pub active_at_current_tick: bool,
}
```

**Anti-greenfield:** the 16 segments are degree-range partitions of `CLOCK_DEGREE_LUT[360]`; no new LUT. The `+1` meta-aperture's degree range is the **whole wheel** at a different scale — the Fibonacci-Pisano growth term per `mahamaya-deep/fibonacci-60-pisano-integration.md`. The renderer surface (§5.4) reads but never invents.

### §4.5 The `det_fold_state` typed marker (per DR-M3-2)

Replace `evolutionaryGap: bool` with `pub det_fold_state: DetFoldState`:

```rust
pub enum DetFoldState {
    Closed,                         // m2_idx round-trips through (×8/9)×(9/8)
    GapFolded { upper_index: u8 },  // m2_idx is in the 9 fold-points; upper_index is the M2 upper bound
    ProvisionalDataset,             // S2 graph hasn't supplied the canonical disambiguation yet
}
```

**Anti-greenfield:** `is_evolutionary_gap(m2_idx)` at `m3.h:356-360` already computes this; the enum types the existing boolean.

### §4.6 The `coupling_flow_alignment` inspector projection (per full theoretical alignments)

Add an optional inspector-only projection once the typed JSON edge can carry it:

```rust
pub struct CouplingFlowAlignment {
    /// Exact symbolic skeletons currently relevant to the active packet / composition.
    pub symbolic_skeletons: Vec<SymbolicSkeleton>,

    /// Physics reference stack labels. These are citations / register facts, not calculations.
    pub physics_descent: PhysicsDescent,

    /// Measurement-face markers such as alpha_EM(0)^-1 and alpha_EM(M_Z)^-1.
    pub measurement_faces: Vec<MeasurementFace>,

    /// Where recognition happens: Nara/apparatus/oracle/journal/experiment analogue.
    pub recognition_context: RecognitionContext,

    /// Mandatory caveat separating QL symbolic mathematics from physical derivation.
    pub register_discipline: RegisterDiscipline,
}

pub enum SymbolicSkeleton {
    Spine137_64_72_1,
    Vortex137_64_2x36_1,
    EwQcd137_128_8_1,
    Spanda04229,
    VortexRatio64Over36,
    QcdOctetSinglet8Plus1,
}

pub struct PhysicsDescent {
    pub stack: Vec<String>, // G_SM, D_mu, g3/g2/gY, RG, EW breaking, e(mu), alpha_EM(mu), spectrum
    pub qcd_underbody: Vec<String>, // SU(3)c, alpha_s, 8+1, Lambda_QCD, proton, hydrogen
}

pub struct MeasurementFace {
    pub label: String,          // alpha_EM(0)^-1, alpha_EM(M_Z)^-1, alpha_s(M_Z)
    pub register: String,       // measured_physics | symbolic_skeleton | psychoid_bridge
    pub display_value: String,  // text from source/citation, not computed in renderer
}
```

**Anti-greenfield:** this projection is mostly static/citation-backed and packet-linked. It reads active packet provenance (`transcription_packet`), M1/M2/M3 composition state (`64+72+1`), Ananda skeleton events (`64/72`, `36/64`, `Additive137`), and source-warrant constants from specs. It does not compute Standard Model couplings, RG flow, QCD corrections, or experimental constants. If this field is absent, the inspector may render a spec-backed static explainer with `pending-coupling-flow-alignment` provenance, but must not invent dynamic values.

---

## §5. Visual Rendering Contract

The M3' surface is the **360° clock-field as live data-structure** — nodes pinned by clock geometry, edges as angular/hop/traversal relations, perturbed by tick (UX §4). Six render layers across the six sub-coordinates.

### §5.1 M3-3' default surface — the 360° wheel with 24 backbone spokes

The **default user-facing rendering**. Tarot-like, alive, summonable depth.

- **Wheel mesh:** 360 cells (one per degree), drawn as radial sectors on a unit circle. The degree-to-cell map IS `CLOCK_DEGREE_LUT[degree]` — colour, hexagram label, codon-class shading.
- **24 backbone spokes:** highlighted at `degree % 15 == 0` (per `Clock_Degree_Entry.is_backbone_node`). Drawn as a 24-spoke 15° lattice over the wheel — these are the discrete SU(2) steps (the M3-5 reading of M1-5's K² topology; see §2.6).
- **Active degree pulse:** the cell at `profile.degree360` pulses; its `Clock_Degree_Entry` data drives a tooltip / inspector pop.
- **Active hexagram band:** highlight all 6 cells where `hexagram_id == profile.mahamaya.hexagramId` — a 6-cell band rotates around the wheel per the line-change choreography.
- **720° SU(2) primary/shadow split:** the wheel exists at two depths; `su2_layer == "shadow"` renders the back-side wheel (mirror; concentric or behind-the-camera depending on depth mode). **Never collapsed to 360-only** (SPEC §1 commitment 1).
- **12-deep ring-buffer trail:** the last 12 `degree720` positions trail behind the active pulse — the M3 Spanda short-term memory (per `M3_Ring_Buffer` at `m3.h:451-455`).

### §5.2 M3-3' angular/hop/traversal edge overlay (UX §4 "cymatics")

The "cymatics-as-data" rendering:

- **Aspect edges:** classical 60° trine, 90° square, 120° trine, 180° opposition between active nodes (the user can ask "what's in trine to my current degree?"). Computed from `polar_opposite_su2(degree)` and degree-arithmetic.
- **Line-change hop edges:** along the 384 line-change graph (`hexagram_id` differences of one bit). The active hexagram's 6 neighbours (one per line-flip) are reachable as a hop; the edge draws a chord across the wheel.
- **Spoke-lattice traversal:** moving along the 24-spoke lattice (15° steps) is the SU(2) discrete advance; rendered as a step-counter overlay.
- **DR-ring streamlines:** at each degree, `dr_ring ∈ {0=Mahāmāyā, 1=Paraśakti}` per `Clock_Degree_Entry`. Render two concentric streamlines — gold (Mahāmāyā doubling) and emerald (Paraśakti tripling) — that touch the cells where `dr_ring` matches. **The same gold/emerald scheme as M1-2** (M1-2 architecture §5.3) — one DR-spin convention, two render scales.

### §5.3 M3-4' Tarot-card surface — the loved face

Default mode is tarot-like (SPEC §7.5). The wheel optionally re-skins as a **56-card minor-arcana ring with 24 backbone spokes as elemental-quarter dividers**:

- **Suit quadrants:** four 90° arcs: Cups (A/Water) at 0–89°, Wands (T/Fire) at 90–179°, Pentacles (C/Earth) at 180–269°, Swords (G/Air) at 270–359°. Per-suit integral renders as a chord-thickness: Cups=84 (thinnest), Wands=96 (thickest), Pentacles=88, Swords=92. **The codon-space's elemental partition IS the wheel's elemental partition** (SPEC §8.8; `_Static_assert` at `m3.h:594-598`).
- **Active card pulse:** computed from `mahamaya.codonId → suit, rank` via `M3_TAROT_CODON_MAP[suit][rank]`. The current card glows; **turning the card** by gesture cycles through its 7/8 rotational positions.
- **Dual-codon courts:** the 8 court cards (Yin Knight/King; Yang Page/Queen — `m3.h:606-613`) render with **two codon-glyphs** instead of one, indicating their dual-codon nature.
- **Major Arcana ribbon:** above the minor-arcana ring, the 22 Major Arcana run as a chromosome-ribbon (1-22 autosomes per `M3_Major_Arcana_Entry.chromosome_pair`). Summonable detail when active.

### §5.4 M3-2' Lens Annulus depth view (per DR-M3-3)

The **annulus inside the wheel** renders the M3 16+1 lens-stack:

- 16 concentric arcs in the annulus, one per `M3_LENS_STACK` segment. Inner radius for the Level-0 / Fibonacci-Pisano growth meta-aperture.
- **Each segment colours by frequency-filter:** the M2-side spectral character it filters. (M2' owns the spectrum; M3' borrows the colour palette.)
- **Active segment glow:** the segment whose `degree_range` contains `profile.degree360` glows.
- Until DR-M3-3 closes, render with `namespace_resolved = false` and a **`pending-m3-lens-stack-namespace` provenance overlay** (SPEC §10.1; cycle-3 tranche 13).

### §5.5 M3-1' Modal-inversion ring — the 472-state navigator

A second annulus, **outside** the wheel:

- 64 outer-ring sectors (one per codon-address), each subdivided into 7 or 8 slots per `m3_codon_rotation_count(codon)` (40 codons get 7; 24 codons get 8).
- **Active cell:** drawn from `profile.codon_rotation_projection.surface_index ∈ 0..471`.
- **Hover/select** any cell to read its full `RotationalState` (sum/diff value, paired pair index, generated codon) — `m3.h:533-542`.
- **Modal-extension highlights:** when the current `(lens, mode)` projects to a non-dual codon, the dual-codon-only rotation positions (rotation > 6 on the 7-or-8 axis where unreachable) render at 30% opacity — **showing the player what the current state cannot reach**. (SPEC §7 implication.)

### §5.6 M3-0' Reception provenance strip

A horizontal strip below the wheel renders the transduction chain (SPEC §8.12):

```
[ M2-5 source: 72-idx=N ] → [ DET ÷9×8: idx=M ] → [ 64-address: addr ] → [ codon: ATG ] → [ q_cosmic: pp,mm,mp,pm ]
```

- **Fold/gap visibility:** when `det_fold_state == GapFolded { upper_index }`, the DET arrow flashes red with the upper-bound annotation. When `Closed`, blue.
- **Public-current only:** never the user's private bioquaternion. Per SPEC §4 and UX §8.
- Read from `profile.resonance72`, `profile.mahamaya`, `profile.codon_rotation_projection`, `profile.q_cosmic`, `profile.det_fold_state` (post-DR-M3-2).

### §5.7 M3-5' Four depth-view modes — the double-torus

The **summonable** four-mode toggle (SPEC §8.10):

#### 5.7.1 Flat Clock Debug
The default. Renders §5.1 + §5.6 + a degree/tick/codon/line-change/readiness data table. Maximum legibility, minimum aesthetic.

#### 5.7.2 Lens Annulus
Renders §5.1 + §5.4 + §5.6. The 16+1 M3-aperture stack made visually central. Used pedagogically — "this is the Mahāmāyā lens stack."

#### 5.7.3 Toroidal / World Clock
The **K² × T²_Mahāmāyā double-torus** (SPEC §8.13):
- **K²** is the M1-5 chromatic-fifths torus — **borrowed mesh**, do not fork the geometry. Renders at minor radius from §2.6.
- **T²_Mahāmāyā** = inscription-circle × lens-circle, rendered as a **second torus concentric with K²**, slightly offset along the SU(2) axis. Inscription-circle parameter = `degree720 / 720`; lens-circle parameter = active `M3_LENS_STACK` segment index / 16.
- **Co-foliation animation:** the two tori share the kernel tick. When K² Klein-flips at M1-5 tick 5→6 (M1-2 architecture §6.5), T²_Mahāmāyā **rotates 30° around the inscription-axis** — the matter-side acknowledgment of the M1 flip without joining it.
- **Pauli-Jung Várlaki/Rudas grammar (SPEC §8.9.1):** when in pedagogical mode, overlay the `1 + 2×32 + 2×36 = 137` skeleton (1 = parent centre = K² centre; 2×32 = doubled temporal rotation = K² turning twice; 2×36 = doubled spatial quaternary = T²_Mahāmāyā in two layers). This is *grammar*, not substrate — drawn as labels.

#### 5.7.4 Hopf Identity
- Renders the SU(2) `hopf_fiber(degree_720)` (per M1-2 architecture §5.5) as the *identity trajectory* — a path traced through SU(2) base × fibre.
- T²_Mahāmāyā collapses to a phase-shadow ring around the Hopf trajectory.
- **720° return** highlighted — the moment the trajectory closes is the identity-recognition pulse.

### §5.8 M3-4' Tarot transcription clock chain panel (per DR-M3-4)

A summonable side-panel renders the chain of `TranscriptionalClockPacket`s as a horizontal scrolling lane:

| Slot | Aperture | Codon | Generation | Tarot | Amino | Provenance |
|---|---|---|---|---|---|---|
| `slot=N` | `M3_LENS_STACK[k]` | `ATG` | comp+pos | Magician | Met | graph |
| ... | | | | | | |

- Each row = one `TranscriptionalClockPacket`. Scrolls horizontally as the tick advances.
- **No protein-claim language**: per UX §3.1.lines 90-95, this is *symbolic-genetic motif sequence*, not biology.
- Start/Stop governance (ATG→AUG, TAA/TAG/TGA→UAA/UAG/UGA) renders as ribbon-edge markers when active.
- Read entirely from `profile.transcription_packet` (DR-M3-4). When pending, render `pending-transcription-packet` provenance overlay.

### §5.9 The colour-space convention (cross-cutting)

| Channel | Driver | Palette |
|---|---|---|
| Wheel cell hue | `Clock_Degree_Entry.zodiac_sign` (0-11) | 12-step chromatic — synchronised with M1' chromatic LUT (do not fork) |
| Cell saturation | `Clock_Degree_Entry.codon_class` | Perfect=highest, Imperfect=high, NonPalindromicNonDual=mid, Dual=low (highlights the dual codons as "extended" — they're the modal-extension navigators) |
| Spoke colour | DR ring | Gold (Mahāmāyā) / emerald (Paraśakti) — **same as M1-2** |
| Halo colour | Cl(4,2) signature at active codon's matrix-path | indigo for COMPLEMENTARY (i-axis), warm for MOVING_RESTING (j-axis), green for SAME_QUALITY (k-axis) — **using `M3_MATRIX_QUATERNION_AXIS` at `m3.h:166-170`** |
| Provenance state | `dataset_lut_state` | Materialised = solid; pending = dashed; provisional-gap = dotted |
| Fold-state marker | `det_fold_state` | Closed = blue; GapFolded = amber; ProvisionalDataset = grey |

### §5.10 Coupling-flow / measurement-face inspector

This summonable technical panel sits beside the Transcription Clock Chain panel. It has four stacked lanes:

1. **QL symbolic skeleton lane** — displays `137 = 64 + 72 + 1`, `137 = 64 + 2(36) + 1`, `137 = 128 + 8 + 1`, `64/36 = (4/3)^2`, and `0,4,2,2,9`. Active packet/cell provenance highlights which skeletons are actually implicated by the current state. For example, a packet linked to `7X+1` p=5/p=9 may highlight `36/64`; a packet linked to `8X+0` p=8/p=9 may highlight `64/72` and `64+72+1`.
2. **Physics descent lane** — displays the reference stack `G_SM -> D_mu -> (g3,g2,gY) -> RG -> EW breaking -> e(mu) -> alpha_EM(mu) -> alpha_EM(0)` plus the QCD underbody `SU(3)c -> 8+1 -> Lambda_QCD -> proton -> hydrogen spectrum`. This lane is labelled `physics_reference`, never `computed_by_ql`.
3. **Measurement-face lane** — displays measured/reference faces such as `alpha_EM(0)^-1 ~= 137.036`, `alpha_EM(M_Z)^-1 ~= 128`, and `alpha_s(M_Z)` only as cited facts or backend-provided constants. It must show the caveat: `137 is the integer skeleton; 137.035999... is the dressed low-energy measurement-face`.
4. **Recognition/Nara lane** — shows whether the active packet-chain has been handed to Nara as oracle, identity, journal/body, or experiment-analogue evidence. This lane reads protected handles only; it never loads private artifact bodies.

The panel's default state is compact: one line per register with the caveat visible. It expands only in developer/pedagogical mode. Its value is not spectacle; it is preventing the system from sliding between symbolic mathematics, physics, and psychoid bridge without saying which register is speaking.

---

## §6. Tick Choreography

### §6.1 The carry-the-tick primitive: degree-advance + quat_slerp

**Single load-bearing animation primitive:** the active `degree720` increments per profile-tick; the wheel rotates so the active degree always sits at the 12-o'clock position (or — by user preference — stays fixed while the pulse moves). All other animations are derivatives.

```rust
on tick_advance(t, t+1, dt):
    let deg_from = profile.degree720;
    let deg_to = next_profile.degree720;
    wheel.rotation_angle = lerp(deg_from, deg_to, dt / TICK_PERIOD);
    // Or — if pulse-moves-not-wheel mode:
    pulse.position = lerp(deg_from, deg_to, dt / TICK_PERIOD);

    // T²_Mahāmāyā companion (Toroidal mode only):
    t2.inscription_angle = profile.degree720 / 720.0 * TAU;

    // K² (Toroidal mode only — borrowed from M1-5):
    k2.orientation = quat_slerp(
        ring_quat(profile.tick12),
        ring_quat((profile.tick12 + 1) % 12),
        dt / TICK_PERIOD
    );  // do NOT fork RING_QUATERNION_LUT; consume via FFI/profile
```

### §6.2 What changes simultaneously on tick advance

| Surface element | Change | Source field | Cadence |
|---|---|---|---|
| Wheel active-degree pulse | Advances 1° (since `degree720 = tick12 * 60`, this is 60° per tick12 step) | `profile.degree720` | Tick-quantised |
| 12-deep ring-buffer trail | Pushes new tail; fades oldest | `profile.degree720` history | Every frame (fade) |
| Active hexagram band | Re-highlights 6 cells of new hexagram | `profile.mahamaya.hexagramId` | Tick-quantised |
| Active line-change-operator chord | Re-draws if `line_change_operator` changed | `profile.mahamaya.lineChangeOperator` | Tick-quantised |
| 472-state outer ring active cell | Jumps to new `surface_index` | `profile.codon_rotation_projection.surface_index` | Tick-quantised |
| Tarot card glow | Re-targets if codon-suit-rank changed | `profile.mahamaya.codonId` → suit, rank | Tick-quantised |
| Cl(4,2) halo colour | Recolours per matrix-path axis | `profile.transcription_packet.generation.matrix_path` (post-DR-M3-4) | Tick-quantised |
| M3-0 provenance strip | Re-renders entire chain | `profile.{resonance72, mahamaya, codon_rotation_projection, q_cosmic}` | Tick-quantised |
| DET arrow flash | Red flash if `det_fold_state == GapFolded` | `profile.det_fold_state` (post-DR-M3-2) | Tick-quantised |
| Audio pulse (M2-1' window) | 8 emitters at `audio_octet` frequencies | `profile.audio_octet[8]` | Every frame (envelope) |
| Nodal-quartet glyphs | 4 boundary markers at `nodal_quartet` | `profile.nodal_quartet[4]` | Tick-quantised |
| Transcription chain lane | Scrolls horizontally one slot left | `profile.transcription_packet` history | Tick-quantised |

### §6.3 What stays still on tick advance

- **The wheel mesh itself** — only its orientation rotates; the 360 cell positions and the 24 backbone spokes do not. Matheme's still-frame.
- **The suit-quadrant boundaries** (when in Tarot mode) — they anchor the 4-elemental partition at 0/90/180/270°.
- **The Lens Annulus segments** — the 16+1 segments stand still; only "which is active" rotates around them.
- **The Major Arcana / chromosome ribbon** — static; only highlights move.
- **The Cl(4,2) basis legend** — static glyph-strip showing `COMPLEMENTARY=i / MOVING_RESTING=j / SAME_QUALITY=k` with current matrix-path highlighted.

### §6.4 Where the eye goes per tick

- **Default focus:** the wheel pulse at active `degree720`
- **Peripheral motion:** the line-change-operator chord redraw (semantically loaded — it's the hexagram transformation)
- **Foveated highlight at boundaries:**
  - Hexagram changes (every ~6 degree-steps): brief radial-burst at the band
  - Tarot-card changes (every ~6 degree-steps inside a suit): card glow lifts, then settles
  - Suit-quadrant crossings (every 90°): a quadrant-edge ripple
  - Backbone-spoke crossings (every 15°): brief spoke-luminance pulse — these are the SU(2) discrete steps

### §6.5 What happens at the 360→361 boundary (primary→shadow)

`su2_layer` toggles `"primary"` → `"shadow"`:

- The wheel **flips through itself** (rotates 180° around the inscription-axis in ~200ms in Toroidal mode; or — in Flat mode — the wheel dims, briefly inverts, brightens with mirrored colour palette).
- The 12-deep ring-buffer is the only memory across the flip; trail dimsbut persists.
- `audio_octet` octave-lifts (M2 substrate writes; M3' reads).
- The Hopf shadow torus (when in Hopf Identity mode) becomes primary; what was primary becomes shadow.
- This is the same **Klein-flip** signature event the M1-5 single-torus owns; M3' acknowledges by inscription-axis rotation only.

### §6.6 What happens at the 720→0 boundary (full identity return)

`identity_returned(M3_Wheel_State)` fires:

- **The wheel blooms whole-surface** for ~500ms in the colour of the current `M3_LENS_STACK` aperture.
- **Diamond-equivalent pulse at centre:** the centre of the wheel (the empty hub) emits one bright pulse — this is **M3-5's still-point**, matching the M1-2 diamond-at-centre behaviour. The matheme's self-identity through inversion.
- **Transcription chain lane fades and refreshes** — symbolic-peptide boundary.
- The 720° SU(2) recognition is *visibly recognised*: the trail closes, the cycle restarts, no explanation needed.

### §6.7 Simultaneous vs sequential rendering layers

| Layer | Render cadence |
|---|---|
| Wheel mesh + suit boundaries + lens annulus + spokes + ribbon | Every frame (orientation only) |
| Pulse + ring-buffer trail + audio emitters + nodal glyphs | Every frame (positions) |
| Active hexagram band + line-change chord + active Tarot card + 472-state pulse + Cl(4,2) halo + provenance strip + transcription chain lane | Tick-quantised |
| Klein-flip / primary↔shadow inversion + 720° identity bloom | Boundary-quantised (degree360 0→1 crossings; 720°→0 return) |
| Lens Annulus active segment | Lens-anchored (only on `M3_LENS_STACK` segment crossings) |
| Major Arcana / chromosome highlight | Codon-anchored (only on `mahamaya.codonId` changes intersecting a `M3_MAJOR_ARCANA` mapping) |

---

## §7. Boundary Contracts

### §7.1 M3 ↔ M1 (consumes K² geometry; produces clock-field)

- **Consumes:** M1-5's `RING_QUATERNION_LUT[12]` (`m1.h:551-564`) and K² mesh — borrowed for Toroidal / Hopf depth modes; **never forked**. K² geometry is M1-5's authority (M1-2 architecture §7.2).
- **Consumes:** M1-3 Spanda `tick12` — drives the `M3_Ring_Buffer` 12-deep history.
- **Produces:** the `dr_ring` annotation per degree (M1-2 owns the DR rings; M3' is the second renderer of them at clock-scale).
- **Anti-overlap:** M3' renders the **double-torus K² × T²_Mahāmāyā**; M1-2 renders the **single K² played-torus**. The boundary is sharp — M1-2 ARCHITECTURE §7.2 forbids M1-2 from rendering double-torus. This document explicitly confirms M3' as the double-torus authority.

### §7.2 M3 ↔ M2 (consumes 72-source; produces 64-address)

- **Consumes:** `profile.resonance72` (M2 vibration source), `profile.audio_octet`, `profile.nodal_quartet` — the Vimarśa window contract (M1-2 architecture §7.1). M3' reads what M2-1' wrote; **never re-derives**.
- **Consumes:** `profile.planetary_chakral` — for cosmos/solar panel display only (SPEC §1, §2).
- **Produces:** the 64-address codec result via `profile.mahamaya` and `profile.codon_rotation_projection`. M2 reads back via `get_parashakti_frequency` (`m3.h:344-348`) for the ascending M3→M2 bridge.
- **Anti-overlap:** M2 owns the continuous standing-wave (Chladni); M3' owns the discrete matter-crystallisation of that wave into the clock-field. **"Cymatics" at M3 is not the wave — it's the data-structure** (UX §4).

### §7.3 M3 ↔ M0 (one substrate, two renderings)

- **Consumes:** the canonical Neo4j bimba-graph substrate (same nodes M0' graph-renders).
- **Produces:** the temporal-cosmological wheel rendering as the second 0-side face of Mahāmāyā (SPEC §1 commitment 6, §8.13).
- **Anti-overlap:** **M0' graph view + M3' clock-wheel = two views of one Neo4j substrate; neither may fork** (SPEC §1 commitment 6). M3' does not become a private clock; M0' does not become a private graph. Cross-reference: `Idea/Bimba/Seeds/M/M0'/M0-ARCHITECTURE.md` owns the graph rendering contract; this document owns the clock-wheel rendering contract; both must consume the same `s3_world_clock_handle`.

### §7.4 M3 ↔ M4 (Nara serves)

- **Produces:** scalar Tarot/I-Ching/codon references that Nara M4-2 Oracle and M4-0 Identity consume.
- **Produces:** `TranscriptionalClockPacket` chain that Nara M4-3 integrates as `mahamaya_transcription` inside `PatternPacket` (SPEC §8.15).
- **Privacy:** M3' resolves scalar refs from Nara artifacts via `resolveM3ScalarOracleRef` (`codon-wheel.ts`), **without loading protected artifact bodies** — `protectedArtifactBody` is rejected by the API.
- **Anti-overlap:** M3' renders the codon-quaternion; M4-4-4-4 renders the personal-quaternion resonance with codon-quaternions. M3' does NOT compute personal resonance — that lives at M4 (SPEC §8.4, last paragraph).

### §7.5 M3 ↔ M5 (Epii observes)

- **Produces:** kernel-trace evidence (read-only) for M5 process-reward RL and pathway archaeology (SPEC §8.14, §10).
- **Forbidden:** any reward/training/pathway-promotion logic inside the renderer (`rewardTrainingAuthority: 'outside-renderer'` at `codon-wheel.ts:100`).
- **Anti-overlap:** M5 trains over Mahāmāyā calculation pathways; M3' renders the trace of that training as a read-only overlay.

### §7.6 Integrated 1-2-3 cosmic engine composition (Tranches 07, 08; SPEC §8.14)

Per cycle-3 Tranche 15.4 editor-area composition (UI Foundation 6 — composition over juxtaposition):

- The **integrated 1-2-3 plugin** composes M1 (played K² torus) + M2 (cymatic engine) + M3' (codon-rotation projection on the lens-ring) into ONE surface.
- **M3's role in the composition:** the codon-rotation projection lights up cells **on the lens-ring of the played K²** in response to the active profile tick. M3' does not draw a separate wheel inside the composition; instead, **the K² surface receives M3 lights** (the same way M2 cymatic frequencies render on the K² surface).
- **Authority of geometry:** M1 owns the K² mesh; M3 owns what lights up where on it.
- Cross-reference: `Idea/Bimba/Seeds/M/INTEGRATED-1-2-3-COSMIC-ENGINE-ARCHITECTURE.md` for the composition law.
- The **standalone `m3-mahamaya` extension** (`ide-deep` mode) retains the full wheel + summonable inspectors + four depth modes.

### §7.7 Vimarśa-window pattern (load-bearing across M-stack)

The `audio_octet[8]` and `nodal_quartet[4]` are written by M2-1' Vimarśa (`Body/S/S0/portal-core/src/parashakti/vimarsha_reading.rs`) and read by all consuming surfaces — M1', M3', M4', M5'. **M3' reads via the profile bus; never invokes a synthesiser; never indexes M2 LUTs locally for pulse display.** This is the same central contract M1-2 architecture §7.1 names.

---

## §8. IDE Integration (Theia / `m3-mahamaya`)

### §8.1 Extension placement

- **Existing extension:** `Body/M/epi-theia/extensions/m3-mahamaya/` — scaffold landed at contract version `2026-06-01.07-T6` (`codon-wheel.ts:10`).
- **Current surface:** `M3MahamayaWidget` (browser-side React widget) at `src/browser/m3-mahamaya-widget.tsx`; `buildM3ProjectionSurface` contract at `src/common/codon-wheel.ts:77-130`.
- **First-slice status (per `package.json` description):** "64/472 wheel with backend-provided codon projection, provenance, and trace overlay only" — landed.
- **Cycle-3 work:** Tranche 4.2 adds the six summonable inspectors + four depth-view modes **as sub-widgets of the existing extension** — no new extension, no new contract version unless schema changes.
- **Companion extension to `m3-mahamaya`:** none — `m3-mahamaya` is the single M3' authority. The integrated 1-2-3 plugin (`Body/M/epi-theia/extensions/plugin-integrated-1-2-3/`) consumes via `TRACK_08_EXPORTS = ["M3CodonChip", "M3WheelMiniView"]` (`index.ts:19`).

### §8.2 Surface placement in the IDE (per Tranche 15)

- **`ide-deep` `m3-mahamaya` view:** the full wheel + Lens Annulus + 472-state outer ring + M3-0 provenance strip + four summonable inspectors + four depth-view modes. The pedagogical/deep-inspection surface (SPEC §8.14).
- **Cosmic-side of `daily-0-1` (integrated 1-2-3 composition):** M3 projects codon lights onto the K² played-torus (per §7.6); the wheel does NOT render separately; the `M3WheelMiniView` chip provides a compact "current codon + degree + tarot" status indicator only.
- **Personal-side of `daily-0-1` (integrated 4-5-0 composition):** the codon-quaternion at right composition slot — Mahāmāyā recognition layer for the user's personal Q (Tranche 15 personal-side surface contract). Renders via `M3CodonChip` only.
- **OmniPanel (right sidebar):** NO M3 visualisation. The wheel IS the editor-area surface; OmniPanel hosts Pi/sessions/dispatch/evidence only.

### §8.3 Profile-tick clock as global UI clock (per UI Foundation 2)

The widget subscribes to the kernel-bridge profile-tick event via `bridge.onProfile(...)` at `m3-mahamaya-widget.tsx:54-58`. Every tick advance re-builds the projection surface via `buildM3ProjectionSurface(input)` at `codon-wheel.ts:77`. **No local clock; no animation-frame counter; no `setInterval`.** This is the SPEC §1 commitment 1 honored at the IDE layer.

### §8.4 Provenance inline rendering (per UI Foundation 3, Tranche 15.6)

The widget renders provenance per the M3 contract:

- `pendingFields` array surfaced inline as badges per field (codon-wheel.ts:88, 114)
- `M3_EXPECTED_ROTATIONAL_STATES === 472` check in `validateM3LibrarySummary` — when S2 library is `pending-S2-M3-library-summary`, the wheel renders with the cells lit but the Tarot scalar dict pending
- `readiness.state` propagates from `MExtensionReadinessSnapshot`; `authority_payload_missing` blocks the wheel if `codonRotationProjection` is absent (per `codon-wheel.ts:82` `throw` clause)
- **`pending-transcription-packet` overlay** (post-DR-M3-4): the transcription chain lane renders with header but empty rows; honest pending
- **`pending-m3-lens-stack-namespace` overlay** (post-DR-M3-3): the Lens Annulus renders 16 grey segments with the namespace-pending banner
- **`gap-folded-m2-source` mark** (post-DR-M3-2): provenance strip DET arrow flashes red

### §8.5 Bimba/Pratibimba state persistence (per UI Foundation 4, Tranche 15.7)

When the user toggles `daily-0-1` ↔ `ide-deep`, or cosmic ↔ personal:

- The wheel's current `(degree720, codon_id, rotation, lens_mode, active_depth_view_mode)` survives
- Selected Tarot card + selected Major Arcana + active summonable inspector survive
- The 12-deep ring-buffer trail survives
- Per UI Foundation 4 — the bimba/pratibimba dial is a *face switch*, not an app switch
- Implementation via the kernel-bridge DI singleton + extension-side state cache

### §8.6 Accessibility — pause/scrub

The widget accepts a `pause` and `scrub_to_tick(t)` affordance. Scrubbing replays the deterministic state at `(degree720, codon_id, surface_index)`. The animation primitive (`degree720` lerp + tick-quantised events) is deterministic under replay. Audio pulse is throttled during scrub.

### §8.7 Cross-extension route contracts

`m3.scalar-oracle-to-m4.artifact-inspector` route (per `index.ts:67`) — Nara M4-2 Oracle clicks a tarot/codon scalar ref → M3' renders the canonical library detail in `m3-mahamaya` widget. M3' never loads the protected artifact body.

### §8.8 Forbidden direct imports (per `index.ts:95`)

`Body/S/S0`, `Body/S/S2`, `Body/S/S3`, `portal-core`, `@clockworklabs/spacetimedb-sdk` — the extension consumes only via `SHARED_BRIDGE_ADAPTER`. **Anti-greenfield discipline at the import-graph level.**

---

## §9. Anti-Greenfield Audit

### §9.1 Landed in code (consume, do not re-invent)

| Asset | Location |
|---|---|
| `M3_PAIR_MATRIX[16]` + axis assignments | `Body/S/S0/epi-lib/include/m3.h:155, 166-170` |
| `M3_PAIR_MATRIX` Rust mirror (PM table) | `Body/S/S0/portal-core/src/rotational.rs:3-20` |
| `M3_COMP_MATRIX[64]`, `M3_MOVE_MATRIX[64]`, `M3_RES_MATRIX[64]` | `m3.h:386, 403-404` |
| `compute_rotational_state` + safe variant | `m3.h:190-209` |
| `generate_rotational_states` (Rust) | `rotational.rs:61-114` |
| `m3_classify_codon` + 3-tier algorithm | `m3.h:721-731`; Rust `codon.rs:19-34` |
| `classify_codon` 4-class enum | `Body/S/S0/portal-core/src/types.rs::CodonClass` |
| 40+24=64 + 40×7+24×8=472 invariants | `m3.h:707-711` (compile-asserted) |
| `M3_ROTATIONAL_PROFILE[64]` LUT | `m3.h:578` |
| `M3_CODON_CLASS[64]` master LUT + init | `m3.h:742-744` |
| `evaluate_codon` (4-charge closed-form) | `m3.h:478-488` |
| `m3_eval_to_quat` / `m3_quat_to_eval` | `m3.h:490-506` |
| TTT 4X invariant compile-assert | `m3.h:770-771` |
| `m3_quat_from_codon`, `m3_quat_codon_state`, `m3_quat_active_state` | `m3.h:217-256` |
| `m3_compute_charges` | `m3.h:755-767` |
| `M3_TAROT_CODON_MAP[4][16]` | `m3.h:625` |
| `M3_MAJOR_ARCANA[22]` | `m3.h:644` |
| `M3_TAROT_QUATERNION_COUNT = 80u` | `m3.h:635` |
| `m3_tarot_rotation`, `m3_tarot_translate` | `m3.h:798-844` |
| Per-suit integrals (84+96+88+92=360 compile-assert) | `m3.h:589-598` |
| `M3_HEXAGRAM_LUT[64]`, `M3_TRIGRAM_LUT[8]` | `m3.h:281-282` |
| `M3_IChing_State` + `M3_RNA_FUNCTIONAL_MASK` + `M3_RNA_DARK_MASK` | `m3.h:285-291, 659-660` |
| 384=360+24 line-change topology compile-assert | `m3.h:933-934` |
| `CLOCK_DEGREE_LUT[360]` (27 fields × 360) | `m3.h:980` + `m3_clock_lut.c:25-749` (auto-generated) |
| `Clock_Degree_Entry` struct (27 fields) | `m3.h:936-978` |
| `M3_Wheel_State`, `M3_Ring_Buffer`, `M3_DegreePosition` | `m3.h:431-455` |
| `polar_opposite_su2` (SU(2)-preserving) | `m3.h:370-374` |
| `identity_returned` (720° return check) | `m3.h:458-460` |
| `read_cosmic_clock` (Unified_Clock_State alias) | `m3.h:331-333` |
| `apply_epogdoon_compression` / `is_evolutionary_gap` / `get_parashakti_frequency` | `m3.h:351-360, 344-348` |
| Algorithmic codon classifier + sequence/anticodon | `Body/S/S0/portal-core/src/codon.rs:19-52` |
| `CODON_TO_AA[64]` + `AA_STOP=10` + `codon_to_amino_acid` | `codon.rs:3-16` |
| `LENS_COUNT=12`, `MODE_COUNT=7`, `LENS_MODE_COUNT=84` | `Body/S/S0/portal-core/src/codon_rotation_projection.rs:5-7` |
| `CODON_COUNT=64`, `CODON_ROTATION_SURFACE_COUNT=472` | `codon_rotation_projection.rs:8-9` |
| `MathemeLensMode` type + index + lens_label + mode_name | `codon_rotation_projection.rs:11-42` |
| `CodonRotationSurfaceCell`, `CodonRotationProjection` types | `codon_rotation_projection.rs:44-74` |
| `codon_rotation_surface`, `codon_rotation_from_lens_mode`, `lens_mode_from_codon_rotation` | `codon_rotation_projection.rs:76-115` |
| `codon_charge_quaternion` (returns normalised `[pp, mm, mp, pm]`) | `codon_rotation_projection.rs:117-126` |
| `MathemeHarmonicProfile` struct (with `mahamaya`, `codon_rotation_projection`, `q_cosmic`) | `Body/S/S0/portal-core/src/kernel.rs:344-387` |
| `MathemeHarmonicProfile::from_tick` constructor | `kernel.rs:389-465` |
| `MahamayaCodecProjection` (binary codec result) | `Body/S/S0/portal-core/src/mahamaya.rs` |
| Existing m3-mahamaya extension scaffold | `Body/M/epi-theia/extensions/m3-mahamaya/` |
| `M3_CODON_WHEEL_CONTRACT_VERSION = '2026-06-01.07-T6'` | `codon-wheel.ts:10` |
| `buildM3ProjectionSurface` + provenance + depth-view stubs | `codon-wheel.ts:77-375` |
| `M3CodonChip` + `M3WheelMiniView` Track 08 exports | `index.ts:19` |
| `resolveM3ScalarOracleRef` (rejects protected bodies) | `codon-wheel.ts` |
| `mahamaya-deep/` narrative dataset | `Idea/Bimba/Map/datasets/mahamaya-deep/` |
| Low-detail graph form (`nodes_mahamaya.json`, `relations_mahamaya.json`) | `Idea/Bimba/Map/datasets/low-detail/` |

### §9.2 Pending (cycle-3 deliverables)

| Item | Tranche | Type |
|---|---|---|
| `transcription_packet: TranscriptionalClockPacket` on profile | 4.8 / 10.x / DR-M3-4 | profile-bus addition |
| `m3_lens_stack: MahamayaLensStack` on profile | 10.x post-DR-M3-3 | profile-bus addition |
| `det_fold_state: DetFoldState` typed (replaces `evolutionaryGap: bool`) | 10.x post-DR-M3-2 | profile-bus type swap |
| `coupling_flow_alignment: CouplingFlowAlignment` inspector projection | 4.10 / 10.M3 optional | profile-bus or spec-backed inspector addition |
| Six summonable inspectors implementation | 4.2 | extension build |
| Four depth-view-mode rendering implementation | 4.2 / 4.3 | extension build |
| Angular/hop/traversal edge overlay | 4.3 | extension build |
| S2 M3-library scalar-detail dict population | cycle-2 Track 02 (carries) | graph population |
| Round-trip projection canonical-LUT audit | 4.1 | substrate audit |
| One-Cl(4,2)-type audit (Wave-B handoff) | 4.7 | cross-cutting audit |
| TCT/Nine-of-Wands dataset reconciliation | 4.4 / DR-M3-1 | dataset edit + import validator |

### §9.3 Net-new (M' product surface, anti-greenfield exception)

- **The wheel-as-live-data-structure rendering** (UX §4) — the angular/hop/traversal edge overlay is renderer-side composition over landed substrate. Not new substrate; new rendering.
- **The four depth-view modes** as toggleable surfaces — substrate provides every field; the renderer chooses which to project where.
- **The Tarot-suit-quadrant chord-thickness encoding** of per-suit integrals (84/96/88/92) — substrate provides the integers; the visual encoding is renderer-side.
- **The 12-deep ring-buffer trail rendering** — substrate provides the `M3_Ring_Buffer` data; the visual trail is renderer-side.
- **The `K² × T²_Mahāmāyā` concentric-torus animation** in Toroidal depth mode — K² mesh is borrowed; T²_Mahāmāyā is rendered as a second torus parameterised from `CLOCK_DEGREE_LUT`; the co-foliation animation is renderer-side.
- **The Pauli-Jung Várlaki/Rudas `137` skeleton overlay** in Toroidal pedagogical mode — labels and arrows over the geometry; pedagogy, not substrate.
- **The coupling-flow / measurement-face inspector** — source-warrant labels over active M3 packet and integrated 1-2-3 state. It is a register-discipline surface, not a physics engine.

### §9.4 Forbidden (do not invent)

- Local codon-rotation map tables — `codonRotationProjection` is the only projection input (SPEC §7 "lives in the kernel/profile contract … NOT in M3' renderer code")
- Local pitch synthesis — consume `audio_octet` only (SPEC §2; Vimarśa window)
- Local clock — consume `tick`, `tick12`, `degree720`, `degree360` only (SPEC §1 commitment 1)
- Local Tarot/I-Ching/codon mapping tables — `M3_TAROT_CODON_MAP`, `M3_TAROT_QUATERNION_*`, `M3_MAJOR_ARCANA`, `CODON_TO_AA` are kernel-side (SPEC §4)
- Local fork of `m3.h` constants into TS — all constants live in the codec (SPEC §6 readiness; cycle-2 enforces forbidden-imports in `index.ts:95`)
- Local DET fold computation — consume `det_fold_state` (or `is_evolutionary_gap` via FFI when typed) (DR-M3-2)
- Local Cl(4,2) algebra — the one quaternion type lives at `portal-core/src/quaternion.rs` (cycle-3 4.7 audit)
- Local M0' graph fork — render the codon-wheel; address bimba nodes via S2 (SPEC §1 commitment 6, §8.13)
- Local M5 reward/training logic — read-only kernel-trace overlays only (`rewardTrainingAuthority: 'outside-renderer'`)
- Double-torus K² mesh fork — borrow K² geometry from M1-5 (this document §2.6, §7.1)
- Local physics/RG/constants calculation — coupling-flow inspector renders cited/backend-provided facts and register labels only
- Any claim that QL derives `137.035999...` numerically — specs permit `137` as symbolic skeleton and `137.035999...` as measured low-energy face, not their collapse

---

## §10. Test Criteria

The M3' surface is acceptance-ready when:

1. `test -d Body/M/epi-theia/extensions/m3-mahamaya/src/{browser,common}` — scaffold present (already true).
2. `grep "M3_CODON_WHEEL_CONTRACT_VERSION" Body/M/epi-theia/extensions/m3-mahamaya/src/common/codon-wheel.ts` returns a single version constant ≥ `2026-06-01.07-T6`.
3. `grep -r "dinucleotide\|chargeQuaternion\|twentyFourSpoke\|majorArcana\|dnaRnaPhase\|aspectEdge\|hopGraph\|lineChangeEdge\|spokeLattice" Body/M/epi-theia/extensions/m3-mahamaya/src/` returns hits in 6+ files after Tranches 4.2/4.3 land.
4. `cargo check -p portal-core` + `cargo test -p portal-core codon_rotation` — substrate compiles and passes (already true; cycle-3 4.1 audits round-trip).
5. `cargo test -p portal-core classify_codon` for `0x35` returns `CodonClass::ImperfectPalindromic` — DR-M3-1 confirmation; 7-rotational-states for TCT.
6. `cargo test -p portal-core 472_invariant` — `40*7 + 24*8 == 472` runtime check (covered by `m3.h:707-711` compile-assert in C; mirror needed in Rust).
7. `grep -rn "Quaternion\|cl_4_2\|cl42\|Cl(4,2)" Body/S/S0/portal-core/src/ | grep "struct\|pub struct\|type " | wc -l` returns 1 — single canonical Quaternion type (cycle-3 4.7).
8. Widget render test: 360 wheel cells drawn (one per degree); cell at `profile.degree360` is the active pulse; 24 cells at `degree % 15 == 0` are visually marked as backbone spokes.
9. Widget render test: 472 outer-ring cells drawn (40 codons × 7 + 24 codons × 8); active cell drawn from `profile.codon_rotation_projection.surface_index`.
10. Widget render test: 56-card minor arcana ring drawn when Tarot mode active; current card from `profile.mahamaya.codonId → M3_TAROT_CODON_MAP[suit][rank]`.
11. Animation test: 12-tick capture shows monotonic `degree720` advance (60° per tick12 step); shadow layer engages on `degree720 >= 360`.
12. Klein-flip test: tick crossing primary↔shadow produces inscription-axis flip in Toroidal mode; `su2_layer` matches `degree720 >= 360`.
13. Identity-return test: 12-tick × 60° cycle (720° total) ends with whole-surface bloom + centre pulse.
14. Provenance test: when `transcription_packet` missing, transcription chain lane renders header + empty rows + `pending-transcription-packet` badge (post-DR-M3-4 / Tranche 4.8).
15. Provenance test: when `m3_lens_stack.namespace_resolved == false`, Lens Annulus renders 16 grey segments + `pending-m3-lens-stack-namespace` banner (post-DR-M3-3).
16. Provenance test: when `det_fold_state == GapFolded`, M3-0 strip DET arrow renders red with `upper_index` annotation (post-DR-M3-2).
17. Round-trip test: `lens_mode_from_codon_rotation(codon_rotation_from_lens_mode(lens, mode))` is single-valued for representative cells from SPEC §7 (Tranche 4.1).
18. Vimarśa-window audit: `audio_octet` / `nodal_quartet` consumed via `payload.audioOctet` / `payload.nodalQuartet`; no local indexing of `m2.h` LUTs in extension `src/`.
19. Boundary test: no K² mesh primitive or `RING_QUATERNION_LUT` fork in `m3-mahamaya/src/` — Toroidal mode borrows via FFI / shared geometry handle from M1-5 ARCHITECTURE.md scaffold.
20. Privacy test: `resolveM3ScalarOracleRef` rejects calls carrying `protectedArtifactBody` — runtime check.
21. Composition test: `M3CodonChip` and `M3WheelMiniView` render in integrated 1-2-3 plugin without firing the full wheel (codon lights only on K² per §7.6).
22. Replay test: deterministic `MPrimePerformanceEvent` stream produces same `(degree720, codon_id, surface_index, rotation, su2_layer)` trajectory across two runs.
23. State-persistence test: toggle `daily-0-1` ↔ `ide-deep` preserves `(degree720, codon_id, rotation, lens_mode, active_depth_view_mode, ring_buffer_trail)`.
24. Forbidden-import test: `grep -E "from '\.\./(\.\./)*(Body/S/S0|portal-core)" Body/M/epi-theia/extensions/m3-mahamaya/src/` returns zero matches.
25. Source-warrant test: when in Toroidal pedagogical mode, `137 = 1 + 2×32 + 2×36` overlay renders the Várlaki/Rudas integer skeleton labels.
26. Coupling-flow inspector test: renders all four lanes (symbolic skeleton, physics descent, measurement face, recognition/Nara) with the caveat `symbolic skeleton != measured constant`.
27. Register-discipline test: no UI string says or implies `QL derives alpha_EM(0)`; allowed strings distinguish `137` integer skeleton from `137.035999...` low-energy measurement-face.
28. Physics-calculation audit: `m3-mahamaya/src/` contains no RG beta-function, electroweak-mixing, QCD-correction, or measured-constant calculation code; those values are backend/spec/citation facts only.

---

## §11. Closing — Why M3' Is the Clock-Field

The UX docs say "M3 is Matter; M3' is the playable clock-field." The substrate agrees: the **content** of Matter (the 64 codons, the 56+8 Tarot, the 472-state landscape, the 384 line-change graph) is binary-genetic — `m3.h`, `m3.c`, `m3_clock_lut.c` carry it as immutable `.rodata`. The **form** of Matter at the temporal scale is the **360° clock-field** — `CLOCK_DEGREE_LUT[360]` pins every node by clock geometry, and the wheel turning IS Matter taking and re-taking form.

The bimba ↔ pratibimba dial at every scale, restated for M3':

> **Substrate (M3) is the binary-genetic transcription engine; rendering (M3') is the clock-field rendering of that substrate. Frequency (M2) crystallises into Matter (M3) through Venus's 9:8 epogdoon (`apply_epogdoon_compression` at `m3.h:351-353`). Each tarot card is a codon is a Cl(4,2) quaternion is a tonal signature; turning the card is changing the mode; the wheel turning is the matheme's tick made temporal-cosmological.**

M3' carries the 64 in the matheme spine `137 = 64 + 72 + 1` (SPEC §8.10). The `+1` parent stays at M1-5; the 72 stays at M2; M3 holds the binary-genetic 64. The wheel is not a private clock — it is the temporal-cosmological face of the kernel tick, the same tick M1's K² turns through and M2's cymatic field oscillates within.

The double-torus K² × T²_Mahāmāyā (M3-5) is where M1's single-torus recognition and M2's 72-fold bridge unfold into the visible cosmic wheel. The wheel **is** the cosmic. Turning the card **is** changing the mode. The cymatics **is** the data, arranged by clock geometry, moving as the tick turns.

The matheme's own clock, finally seeable, finally turnable.

---

*Companion architecture documents (all in `Idea/Bimba/Seeds/M/`):*
- *`M0'/M0-ARCHITECTURE.md` — graph rendering, the other 0-side face of Mahāmāyā*
- *`M1'/M1-ARCHITECTURE.md` — Paramaśiva instrument; K² geometry M3-5 borrows*
- *`M1'/M1-2-ANANDA-VORTEX-ARCHITECTURE.md` — the DR rings M3' renders at clock-scale*
- *`M2'/M2-ARCHITECTURE.md` — the frequency-bridge whose 9:8 compression generates M3*
- *`M4'/M4-ARCHITECTURE.md` — Nara as consumer (Oracle / Identity / Transform)*
- *`M5'/M5-ARCHITECTURE.md` — Epii as observer / process-reward RL*
- *`INTEGRATED-1-2-3-COSMIC-ENGINE-ARCHITECTURE.md` — the composition law*
- *`INTEGRATED-4-5-0-RECOGNITION-ARCHITECTURE.md` — the personal recognition surface*

*Cross-references in Body: `Body/M/epi-theia/extensions/m3-mahamaya/ARCHITECTURE.md` (this document's IDE-mirror).*
