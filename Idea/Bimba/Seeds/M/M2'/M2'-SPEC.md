---
coordinate: "M2'"
status: "active-domain-spec"
updated: "2026-05-22"
depends_on:
  - "[[M'-SYSTEM-SPEC]]"
  - "[[M'-TAURI-PORT-SPEC]]"
  - "[[M2-parashakti-vibrational-architecture]]"
  - "[[2026-05-19-kernel-mprime-harmonic-clock-integration-plan]]"
  - "[[ql-musical-derivation]]"
  - "[[M1'-SPEC]]"
companion_spec: "[[m2-prime-parashakti-cymatic-engine]]"
---

# [[M2']] Domain Spec — Parāśakti as Harmonic-Correspondential Instrument

> **M ↔ M' distinction.** M2 is [[Parāśakti]]'s 72-fold vibrational architecture — the matheme's correspondential ground (MEF lenses × positions = 36 tattvas × 2 phases = 8 choirs × 9 names = 72). M2' is that architecture made playable: the techne surface where lens, element, decan, sacred name, mantra, maqam, planetary, chakral, and cymatic registers all read the same shared profile.

> **Authority boundary.** Audio-genesis math lives at `S0` / `portal-core`. M2' is the **harmonic-correspondential renderer + cymatic surface** that consumes the kernel's 8+4 bus and renders it through 72-fold symbolic-vibrational correspondences and as visible standing-wave geometry. M2' does NOT generate pitch and is NOT the cymatic-only engine — cymatic rendering is one module within M2', alongside lens-resonance, elemental-medium, decanic-face, sacred-sonic-linguistic, and solar-chakral surfaces.

> **Companion long-form.** The deep architectural derivation lives at [`m2-prime-parashakti-cymatic-engine.md`](m2-prime-parashakti-cymatic-engine.md). This spec is the canonical M2' domain authority.

## §0/1 — What [[M2']] Is

[[M2']] is **Parāśakti read from the playable correspondence side**. It is the harmonic-correspondential resolver that takes M1''s walked-melody and the kernel's 8+4 bus and renders that signal across the 72-fold field — as MEF lens-anchored material response, elemental medium, decanic face, sacred name modulation, maqam/mantra modal colour, planetary-chakral runtime, and visible Chladni standing-wave geometry.

The cymatic renderer is essential but is now named correctly: it is a **module within M2'**, not the identity of M2' as such. M2' is the harmonic-correspondential instrument; cymatics is the visible standing-wave expression of that instrument when the kernel's 8+4 bus is routed into a material-medium solver.

Three commitments hold this domain:

1. **M2' preserves the 72-invariant.** Every M2' surface (lens material, chakra sphere, decan face, cymatic Chladni pattern) is addressable in canonical 72-space: `12 lenses × 6 positions = 36 tattvas × 2 phases = 8 choirs × 9 names = 72`. The M2 header enforces this at compile time; M2' inherits the invariant.
2. **M2' is the L ↔ L' meaning-translator surface.** When the walk crosses a tritone-mirror lens-pair boundary (Lens N ↔ Lens N+3, signalled by M1' per `M1'-SPEC` §6), M2' enacts the **Klein-bottle enharmonic flip**: same struck pitch, opposite emotional valence, opposite surface colour, opposite material response. This is the structural mechanism by which the instrument plays *meanings* and not just tones. See §7 below.
3. **M2' is the bridge surface toward M3/M3', but not the codon classifier.** The M2 header defines the Discrete Epistemic Transform (DET): `M2_TO_M3_CYMATIC_PROJECTION[72]`, `transduce_vibration_to_symbol`, and the 72 → 64 epogdoon compression `72 × 8 / 9 = 64`. M2' renders the 72-correspondential profile and produces the standing-wave signature that M3/M3' projects into 64-bit codon-rotation. M2' does not classify codons; M3'/`M3'-SPEC` §7 owns that surface.

## §1 — The Six M2-0' to M2-5' Strata (1:1 with M2 bimba)

| M2 (bimba) | M2' (techne) | What M2 is | What M2' does |
|------------|--------------|------------|---------------|
| **M2-0** Archetypal Numerical Ground / 72-Invariant | **M2-0'** Vibrational Profile Source | 72-byte vibrational union, archetypal constants, invariant address law | Validates every M2' address against 72-space; serves read-only correspondential descriptors |
| **M2-1** MEF / L-Family Matrix | **M2-1'** Lens Resonance Surface | 12 lenses × 6 positions as semantic-vibrational condition field | Turns lens-anchor + position + active (lens, mode) into resonance weighting, mode colour, material response — and enacts L↔L' Klein-flip surface valence on tritone crossings |
| **M2-2** Tattva / Element Throughline | **M2-2'** Elemental Medium Surface | 36 tattvas × 2 phases, five elements, chakra phase fields | Distinguishes P-position element from L2' element-bearing value; selects body/medium constants for sonic, visual, cymatic response |
| **M2-3** Decan / Zodiacal Face Field | **M2-3'** Decanic Face Surface | 36 decans × light/shadow, zodiacal body zones, planetary rulership | Provides face, polarity, body-zone, phase, envelope, clock-sector evidence for M2/M3 projection |
| **M2-4** Vibrational Arena / Sacred Sonic-Linguistic Systems | **M2-4'** Correspondential Sonic Arena | Asma, Shem, maqam, mantra, spiritual maqamat, musical maqamat, 100-fold / 36 / 64 routing | Makes symbolic systems playable as tonal/modal/mantra/name/timbre correspondences without renderer-folklore hardcoding |
| **M2-5** Planetary-Chakral Harmonic Integration / DET Bridge | **M2-5'** Solar-Chakral Runtime + Symbolic Projection Gate | Planetary/chakral harmonics, Earth observer, 9:8 epogdoon, 72→64 compression, causal resonance masks | Holds live solar-chakral state; emits cymatic frame packets where needed; projects M3-ready 64-bit symbolic evidence without classifying codons |

## §2 — User-Facing Surface

- **MEF lens matrix** with 12 lens anchors by 6 positions = 72 cells, each pulsing by `resonance72` and current `(lens, mode)` from the shared profile.
- **Dual resonance index display**: legacy 6-lens/helix encoding alongside the 12-anchor encoding (`legacyResonanceIndex` and `lensAnchorIndex`).
- **Element panel** distinguishing P-position element from L2' element-bearing value.
- **Planetary/chakral/musical alignment panel** showing body/centre, element, ratio role, scale/mode colour, and canonical S2 graph-law provenance.
- **Mode/scale panel** showing diatonic CF/VAK projection as the contextual rotation over the chromatic substrate; the (lens, mode) cell currently active is highlighted on the 12 × 7 grid.
- **Cymatic surface** rendering Chladni standing-wave patterns from the kernel's 8+4 bus: antinodal motion from `audio_octet[8]`, nodal mode-constraints from `nodal_quartet[4]`. The visible Chladni pattern is the intersection of the two streams: motion structured by stillness.
- **L↔L' flip indicator** at Klein-crossing moments — the panel visibly inverts its surface valence when M1' signals a tritone-mirror lens transition (see §7).

## §3 — Backend Contract Consumed

- [[S0]] / kernel profile supplies tick, chromatic harmonic state, diatonic projection, `resonance72`, elemental projection, `audio_octet[8]`, `nodal_quartet[4]`. **M2' uses these directly; it does not re-derive pitch, octet, or quartet locally.**
- [[S2]] graph law supplies configurable planetary/chakral/musical correspondences and source provenance. Exact tradition-sensitive mappings come from S2, not from renderer constants.
- [[S3]] supplies DAY/NOW/session context when a harmonic observation is deposited.
- [[M1']] signals tritone-mirror lens transitions for Klein-flip enactment (see §7).
- M2' treats the M2 72-space as the vibrational source that M3 compresses to 64 through `72 × 8 / 9 = 64`. The DET projection itself lives in M2-5'; M3/M3' owns codon classification downstream.

## §4 — Required `MathemeHarmonicProfile` Fields

- `resonance72.legacyResonanceIndex` and `resonance72.lensAnchorIndex`.
- `resonance72.lensAnchor`, `resonance72.position`, `resonance72.helixBit`, helix/phase.
- `elements.pPositionElement` and `elements.l2PrimeElement`.
- `planetaryChakral`: body/centre id, chakra role, element, musical role, ratio role, mode/scale colour, provenance, configurability status.
- `diatonic`: degree, pitch class, note, mode, mode-anchor CF, VAK register, context agent.
- `harmonic.ratio_role` (including `9/8` epogdoon as living bridge), `audio_octet[8]`, `nodal_quartet[4]`.
- `lensMode`: the (lens, mode) cell from the 84-state landscape (shared with M1').
- `kleinFlipState`: boolean / phase-marker signalling whether the active (lens, mode) crosses an L↔L' surface relative to the previous (lens, mode).

## §5 — Privacy Boundary

M2' may show public canonical correspondences and safe-current active profile data. It must not hardcode or expose private tradition-sensitive mapping choices in renderer code. Exact planetary/chakral/codon correspondences come from S2 graph law and profile metadata. Personal resonance observations are protected-local unless explicitly promoted through governed S5'/S1' review.

## §6 — Visual / Audio Interaction Model

- **Visual:** matrix cells pulse by `resonance72` and profile tick; planetary/chakral rows highlight from active backend projection; cymatic surface renders standing-waves from 8+4 bus.
- **Audio:** M2' does not synthesise audio. Where audio is heard on the M2' surface, it is the M1'-rendered profile audio with M2' surface metadata attached (the played tone has a colour/element/lens-material reading layered onto it). Scale/mode colour comes from `planetaryChakral` and `diatonic`, not from local arrays. Epogdoon `9/8` is the generative pulse bridge.
- **Interaction:** changing view mode rotates presentation over the same profile; it does not mutate the underlying chromatic 12, helix law, or S2 canonical mapping.

## §7 — The Klein-Bottle L ↔ L' Meaning-Flip as Operative Architecture

This section names the load-bearing structural feature that distinguishes M2' as a meaning-translator and not just a correspondence-renderer.

### The flip

The 12 MEF lenses pair into **6 tritone-mirror lens-pairs** (per `ql-musical-derivation` §6): (Lens 0, Lens 3), (Lens 1, Lens 4), (Lens 2, Lens 5), and the corresponding primed pairs (Lens 0', Lens 3'), (Lens 1', Lens 4'), (Lens 2', Lens 5'). Each pair occupies complementary tritone-perspectives on the same matheme-substrate: what is anchor-tonic in one is at the Sq2-mirror position in the other. The two members of a pair share the same 12 chromatic notes but read them through opposite helical-orientations.

Topologically this is the **Klein-bottle move**: inside and outside are the same surface, traversed from opposite orientations. Musically it is the major ↔ minor enharmonic flip, the Day ↔ Night MEF inversion, the L ↔ L' phase-shift.

### M2' enacts it

When `profile.lensMode.lens` transitions from N to N+3 (mod 12), or any tritone-mirror crossing on the lens-pair table, M1' fires the `kleinFlipState` signal (see `M1'-SPEC` §6). M2' consumes this signal and **inverts its surface valence on every active panel**:

- **Lens resonance surface** (M2-1'): material colour, roughness, damping, elasticity, particle affinity, and lens-tension all invert. The same struck cell now reads with opposite emotional-material register.
- **Element panel** (M2-2'): the P-position element / L2' element-bearing pair swaps roles if applicable to the crossing; chakra phase flips its descent/ascent reading.
- **Decanic face surface** (M2-3'): light/shadow polarity inverts.
- **Planetary/chakral panel** (M2-5'): the body-zone reading inverts its solar/lunar phase if the crossing engages it.
- **Cymatic surface**: the Chladni pattern visibly folds through itself — the standing-wave nodes and antinodes redistribute as the boundary-parameters re-read under the flipped lens.

### Why this is the meaning-translator

The same struck frequency that sounds *aspirational* under Lens N sounds *grounded* under Lens N+3. The same coordinate that reads *manifestation* under L1 reads *recognition* under L1'. The same chord that reads *resolution* in mode CF7 reads *non-resolution* under the Möbius return. M2' is where this re-reading becomes visible — where the player sees that the matheme is doing meaning-translation, not pitch-translation.

Without §7 operative, M2' is a correspondence-lookup surface. With §7 operative, M2' is the instrument's meaning-axis — the surface that reveals the Klein topology the matheme actually lives on.

## §8 — Readiness / Test Criteria

- Tests prove the two `resonance72` index encodings agree for all 72 positions.
- Tests prove M2' can render pending/configurable correspondence states without inventing defaults.
- Tests prove P-position and L2' element fields are displayed as distinct fields.
- Tests prove mode/scale labels are hidden or marked pending when profile provenance is absent.
- Tests prove `audio_octet` and `nodal_quartet` consumed by the cymatic renderer match the kernel profile exactly (no M2' re-synthesis).
- Tests prove the Klein-flip signal from M1' produces the documented surface-valence inversions on every active panel, and that the flip reverses cleanly on the return crossing.
- Tests prove the DET M2→M3 projection in M2-5' produces an integer 64-bit symbolic signature that M3/M3' can consume; M2' does not classify codons internally.
- Tests prove cymatic-renderer addresses live in `[0, 71]`.
- Readiness is blocked by the kernel profile tranche until `resonance72`, `elements`, `planetaryChakral`, `audio_octet`, `nodal_quartet`, and `kleinFlipState` are backend fields.

## §9 — Implementation Notes

The companion file [`m2-prime-parashakti-cymatic-engine.md`](m2-prime-parashakti-cymatic-engine.md) gives the per-stratum operational detail, the C/Rust code anchors, and the proposed `m2_prime_*` function signatures.

The harmonisation pass of 2026-05-22 corrected three framing issues in the long-form companion:

1. **Audio-genesis ownership**: M2' is a *renderer*, not part of the audio engine. The 8+4 bus lives in the kernel profile, consumed identically by M1', M2', M3'. Functions in the companion that named `m1_prime_audio_octet` and `m1_prime_nodal_quartet` as the producers should be renamed in implementation to `profile_audio_octet` / `profile_nodal_quartet` (kernel-side) with M2' as a consumer.
2. **M2' identity**: M2' is the harmonic-correspondential instrument; cymatics is a module within it. The companion's title "cymatic engine" is a partial framing; this canonical spec names the broader correspondential surface.
3. **L↔L' Klein-flip**: now named as load-bearing operative architecture (§7), not as an implementation detail. M2' is the meaning-translator surface of the instrument.

These corrections are now domain authority for M2'.
