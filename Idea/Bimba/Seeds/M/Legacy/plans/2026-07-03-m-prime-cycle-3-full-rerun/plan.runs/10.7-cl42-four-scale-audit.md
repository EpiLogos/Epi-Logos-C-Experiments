# 10.T10.7 — Cl(4,2) One-Algebra Identity Audit (kernel-side memo)

**Tranche:** 10.T10.7 *(spec-ahead-integration; cross-links [[02-m1-paramasiva-reconciliation]] 02.T2.7 M-side memo at `plan.runs/02-T2.7-cl42-four-scale-audit.md` and 04.7)*
**Audited:** 2026-07-10 · owner `opus-a-m1` · fresh enumeration
**Verdict:** the four scales share ONE Cl(4,2)/quaternion primitive through the kernel; the one drift-capable mirror named by the M-side audit is now **mechanically pinned** (test landed this tranche).

## 1. Kernel touch points (brief's four, at current post-split locations)

| Touch point | Current location | Note |
|---|---|---|
| `q_cosmic: [f32; 4]` | [kernel/profile.rs:634](Body/S/S0/portal-core/src/kernel/profile.rs) (brief cited kernel.rs:374 — moved by the kernel-module split) | the profile's public cosmic quaternion |
| `codon_charge_quaternion` | [codon_rotation_projection.rs:117](Body/S/S0/portal-core/src/codon_rotation_projection.rs), re-exported into `kernel/profile.rs:4` | M3 codon scale entry |
| `BioQuaternionState` | portal-core public surface (`lib.rs` re-export; events layer) | M4 personal scale carrier |
| `slash_flip_bimba_prime` | [kernel.rs:925](Body/S/S0/portal-core/src/kernel.rs) over `&BioQuaternionState` | the # inversion on the personal bioquaternion |

All four operate on the same `[f32; 4]` quaternion representation with ops from the ONE algebra module `portal-core/src/quaternion.rs` (+ `hopf.rs`); no second algebra or basis table exists in `Body/S/S0/portal-core/src/` (fresh `grep -rn 'Cl(4,2)|cl42|clifford'` — hits are the bus field `cl42_signature_at_position`, its projection fn, doc comments, and `Cl42QuaternionAxis` in `personal_identity.rs` which *consumes* the algebra for the natal scale).

## 2. Drift-point closure (the M-side audit's recommendation, executed here)

`ananda_vortex.rs::cl42_signature` (the Rust ±1 rule) is now pinned to the C `.rodata` law by
[`kernel_truth.rs::cl42_signature_on_bus_matches_c_basis_for_all_six_positions`](Body/S/S0/portal-core/tests/kernel_truth.rs):
FFI-binds `CL42_BASIS[6]` (8-byte `Cl42_Basis_Entry` — `Trig_Function` is int-sized) and asserts, for all six positions, bus signature == C signature, position fields intact, and the net **+2** Cl(4,2) signature. Same pattern as the T4.13 charge-authority pin. Green.

## 3. Cross-reference

M-side four-scale enumeration (M1 ring / M3 codon / M4 personal / Kerykeion natal = `personal_identity.rs`): `plan.runs/02-T2.7-cl42-four-scale-audit.md`. This memo + that one together close the 02.7/10.7 pair; 04.7 (M3-side note) consumes both.
