# 12.T12.9 — Gateway handler audit: `dispatch_moirai_night_pass` + Aletheia Möbius routing

- auditedAt: 2026-07-07 (cycle-3 full rerun; owner fable-b-agentic)
- carryover status: `audit_required` — original done claim re-verified against the live tree, per charter law.

## Findings (binding facts, file:line)

1. **`s4'.mediation.route` is a registered gateway method** — [`gateway-contract/src/dispatch_plan.rs:557-562`](../../../../../../../Body/S/S3/gateway-contract/src/dispatch_plan.rs): `MethodDispatchPlanEntry { method: "s4'.mediation.route", kind: S4OrchestrationAdapter, authority_path: "Body/S/S4/ta-onta/S4-4p-anima" }`; also named in the protocol method registry at `gateway-contract/src/protocol.rs:124`.
2. **The gateway does NOT name `dispatch_moirai_night_pass` literally — by design.** The original verification expected `grep dispatch_moirai_night_pass Body/S/S3/gateway/src/` to hit. It does not, and should not: S3 routes `s4'.mediation.route` generically to the S4 orchestration authority; the tool name travels in the dispatch payload. Coordinate law (S3 = transport/control-plane, S4 = dispatch semantics) forbids the gateway hard-coding S4 tool names. The literal-grep clause is superseded by this recorded shape.
3. **Anima owns the night-pass dispatch** — `Body/S/S4/ta-onta/S4-4p-anima/modules/moirai-dispatch.ts` (`planMoiraiNightPass`, `buildMoiraiVak`, `classifyMoiraiOutput`, `defaultM5CoordinateClusters`); the registered tool lives in `Body/S/S4/ta-onta/S4-4p-anima/extension/dispatch-tools.ts` (`dispatch_moirai_night_pass`, CFP3 F-Thread Night′, per-Moirai VAK via `MOIRAI_HOST_CF`, fusion pre-flight via `validateFusionDispatch`, consent-gated Aeon graduation branch per 46.4).
4. **The Möbius chain into Aletheia exists and is tested** — `Body/S/S4/ta-onta/S4-5p-aletheia/modules/moirai-rehear.ts` is the rehear seam; `Body/S/S4/ta-onta/S4-5p-aletheia/tests/moirai_rehear_handoff.test.ts` passes 6/6 fresh (2026-07-07). The Janus envelope contract exists at `Body/S/S4/ta-onta/S4-5p-aletheia/S5'/janus-envelope.schema.json`.
5. **Moirai roster fact** — Moirai is one of the six Aletheia techne-guardians (Anansi/Janus/Moirai/Mercurius/Agora/Zeithoven); Anima dispatches it for Night′ routing (matches [[feedback_agent_roster_canonical]]: 7 Anima constitutional + 6 Aletheia guardians).
6. **Night-pass trace surface (retargeted per CHARTER)** — the tool return carries the full dispatch trace in `details` (`plan`, per-Moirai `night_position` sections, `ok_count`), persisted through the gateway transcript. The original "Pi-monitor view" clause is a Theia-era surface; under the rerun charter it retargets to carrier panes and is NOT yet a dedicated monitor view — recorded here as open UI work, owned by Track 27/omnipanel, not by this audit.

## Verdict

The handler chain `s4'.mediation.route → Anima dispatch_moirai_night_pass → Aletheia moirai-rehear (Janus envelope)` is landed and behaviorally tested end-to-seam. The one superseded expectation (literal tool name in gateway source) is resolved by finding 2. Dep note: this task's `12.T12.8` dep is a DEPRECATED retarget stub and was removed as a prerequisite (recorded 2026-07-07); `46.T46.4` depends on this audit and unblocks with it.
