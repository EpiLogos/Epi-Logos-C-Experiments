# 02.T2.7 — Four-Scale Cl(4,2) Identity Audit (M-side memo)

**Tranche:** 02.T2.7 *(spec-ahead-integration; cross-links [[10-kernel-bridge-profile-contract]] Tranche 10.7 — kernel-side)*
**Audited:** 2026-07-10 · owner `opus-a-m1` · fresh enumeration of `Body/S/S0/`
**Verdict:** ONE Cl(4,2) basis source of truth and ONE quaternion algebra per language layer confirmed across all four scales. Two named Rust mirrors exist (below) — one is documented-intentional, one is a small reimplementation flagged for the 10.7 kernel-side parity pin.

## 1. Source of truth

| Surface | Where | Shape |
|---|---|---|
| `Cl42_Basis_Entry` + `CL42_BASIS[6]` | [m1.h:572-577](Body/S/S0/epi-lib/include/m1.h) (decl + `_Static_assert` 6 entries), values [m1.c:56](Body/S/S0/epi-lib/src/m1.c) | THE basis — position axis; P0/P5 implicate (−1), P1–P4 explicate (+1), net +2 |
| C `Quaternion {w,x,y,z}` | epi-lib (families/kernel; `Kernel_Bioquaternion {q_b,q_p}` [kernel.h:38-40](Body/S/S0/epi-lib/include/kernel.h)) | the C ground algebra (`quat_from_ring_pos`, `quat_slerp`, `quat_normalize`, `m3_quat_from_codon`) |
| Rust quaternion ops | [portal-core/src/quaternion.rs](Body/S/S0/portal-core/src/quaternion.rs) (`quat_mul`/`quat_normalize` over `[f32;4]`) + [hopf.rs](Body/S/S0/portal-core/src/hopf.rs) | the ONE Rust algebra module — portal-core declares **no** second `struct Quaternion` (`QuaternionCharges` at m3_transcription_bridge.rs:178 is a charge tuple, not an algebra) |
| epi-cli `ffi/kernel.rs::Quaternion` | [ffi/kernel.rs:5](Body/S/S0/epi-cli/src/ffi/kernel.rs) `#[repr(C)]` | ABI **mirror** of the C type for FFI — declared as such, not a duplicate algebra |

## 2. The four scales — call-sites of the one algebra

1. **M1 ring** — `quat_from_ring_pos` / `RING_QUATERNION_LUT[12]` ([m1.c](Body/S/S0/epi-lib/src/m1.c), [m1.h](Body/S/S0/epi-lib/include/m1.h)); Rust consumers [spanda.rs](Body/S/S0/portal-core/src/spanda.rs) + [ananda_vortex.rs](Body/S/S0/portal-core/src/kernel/projections/ananda_vortex.rs) (`ring_quaternion` on the bus; carrier reads it verbatim — T2.6).
2. **M3 codon** — `m3_quat_from_codon` / `codon_charge_quaternion` / `M3_MATRIX_QUATERNION_AXIS` ([m3.h:166-170](Body/S/S0/epi-lib/include/m3.h), [m3.c](Body/S/S0/epi-lib/src/m3.c)); Rust consumers [codon_rotation_projection.rs](Body/S/S0/portal-core/src/codon_rotation_projection.rs) + [m3_transcription_bridge.rs](Body/S/S0/portal-core/src/m3_transcription_bridge.rs).
3. **M4 personal** — `Kernel_Bioquaternion {q_b, q_p}` + `kernel_bioquaternion_init` / `kernel_slash_flip_bimba_prime` ([kernel.h:38-81](Body/S/S0/epi-lib/include/kernel.h)); Rust FFI [epi-cli ffi/kernel.rs](Body/S/S0/epi-cli/src/ffi/kernel.rs) `KernelBioquaternion`, projected via [portal-core kernel.rs](Body/S/S0/portal-core/src/kernel.rs).
4. **Kerykeion natal** — **owner: [personal_identity.rs](Body/S/S0/portal-core/src/personal_identity.rs)** (↔ [[M4']]-SPEC, as the tranche predicted), with [birthdate_identity.rs](Body/S/S0/portal-core/src/birthdate_identity.rs); both import `crate::quaternion::{quat_mul, quat_normalize}` (the one algebra) and carry `Cl42QuaternionAxis {W,X,Y,Z}` (personal_identity.rs:60) binding natal data onto the same basis axes.

Same `+2` signature semantics at every scale: the ±1 per-position values flow from `CL42_BASIS` (C) / its bus projection — no scale defines its own basis table.

## 3. Mirrors found (the "duplicated definition" question)

| Mirror | Status |
|---|---|
| `ANANDA_FAMILY_SIGNATURE[6]` ([m1.h:122](Body/S/S0/epi-lib/include/m1.h)) | **Intentional, documented** — same signature *shape* on the FAMILY axis (`{-1,+1,+1,+1,+1,-1}`), explicitly cross-referenced to `CL42_BASIS` (position axis). Not a duplicate. |
| `fn cl42_signature(position6)` ([ananda_vortex.rs:190](Body/S/S0/portal-core/src/kernel/projections/ananda_vortex.rs)) | **Small Rust reimplementation** of the ±1 rule (`0\|5 → −1, else +1`) feeding `cl42_signature_at_position` on the bus. Currently value-identical to `CL42_BASIS`; it is the one drift-capable point. **Recommendation → Tranche 10.7 (kernel-side owner):** pin it with an FFI parity test against `CL42_BASIS[6]` (all six positions), the same pattern as the T4.13 charge-authority pin. Round-trip test today asserts P0 = −1 only ([ananda_vortex_projection_round_trip.rs:25](Body/S/S0/portal-core/tests/ananda_vortex_projection_round_trip.rs)). |

## 4. Verification (fresh command)

`grep -rn "CL42_BASIS\|Cl42_Basis_Entry\|cl42_signature" Body/S/S0/` (excluding build artifacts) → 13 hits: 1 C definition pair (m1.h decl + m1.c values), 1 static-assert, 2 documentation cross-refs (m1.h:120-121, oracle_engine.rs:28 comment), 1 bus field + its projection fn + 1 test assertion, 1 `m3.h` cross-ref copy field (`cl42_signature` documented "Copied from CL42_BASIS for cross-reference", m1.h:587). Exactly one source of truth; no second basis table anywhere in `Body/S/S0/`.
