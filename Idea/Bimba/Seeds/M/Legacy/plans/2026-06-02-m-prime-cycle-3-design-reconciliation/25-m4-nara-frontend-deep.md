# Track 25 — M4' Nara Frontend Deep Design (Delta)

This track is the M4' widget-level frontend DELTA over the four tranches that already authored the canvas/highlights/ambient stack and the contemplation-temporal axis: **11.10** (Tiptap canvas-as-input port + Inversify HighlightService + HighlightMark port), **11.11** (six agent-side highlight categories + visual register + `NaraActivityKind::FileReentry|TrancheComplete`), **11.12** (ambient state strip + tuning bar widgets above the canvas), **19.4** (`m4_session_open` drawing tarot psyche-anchor), **19.6** (`contemplate_session_close` RPC = 4'-5'-0' LLM/EBM/Verifier triplet), **19.11** (Khora flow-watcher + `chronos_response_orbit` + `chronos_reentry` + `khora_write_highlighted_inscription`), and **19.12** (`mercurius_kairos_now` populator of `M4_Temporal_Now.planet_degrees[10]`). Those tranches own the canvas, its highlights (user + agent), the strips above it, the tuning bar, the session-open tarot draw, the contemplation gateway, the temporal control plane, and the kairos populator. They are inheritance, not greenfield.

This track closes the rest. Wave-A M4 confirmed the M4 substrate is one of the strongest in the M' stack: `personal_identity.rs` (`q_personal`, `compose_personal_quaternion(Q_identity·Q_transit·Q_activity)`, `PersonalResonance::from_quaternions`), `medicine.rs` (`CHAKRA_BODY_ZONES[8]`, `DECAN_BODY_PARTS[36]`, `DECAN_HERBS[36]`, `PLANET_CHAKRA[8]`, `ELEMENT_CHAKRA`, `SIGN_ELEMENT[12]`, `balance()`, `chakra()`, `materia()`, `prescribe()`, `body_zones_for_elem_sig()`), `oracle.rs` (`PIP_DECAN_MAP[9][4]`, `COURT_SIGN_MAP[16]`, `ACE_ELEMENT_MAP[4]`, `HEXAGRAM_BODY_DYNAMICS[64]`), `transform.rs` / `lens.rs` / `logos.rs` / `pratibimba.rs` / `pasu.rs` / `kairos.rs` are all CLI-real, `nara_journal.rs` carries the artifact ledger, `vak_address.rs` exposes the `Cl(4,2)` carrier types (`cpf, ct, cp, cf, cfp, cs`), and the gateway dispatches `nara.*` through `S4S5DomainAdapter`. The `m4-nara` Theia extension scaffold (contract `2026-06-01.07-T7`, `PRIVACY_CLASS = 'protected_local_handle_only'`) declares `m4.nara.dayContainer`, `m4.nara.graphitiBrowser`, `m4.nara.personalField` view ids and exposes `NaraDayContainer`, `NaraArtifactEnvelope`, `ProtectedPersonalFieldInput`, `ConsentRecord`, `VoiceCorpusAdmissionInput` types — but the only landed widget today is `M4NaraWidget` rendering a 4-row dl over the DayContainer.

The delta is the **per-widget surface that lets the user see and shape what the substrate has been computing**: day-calendar navigation, DayContainer detail with per-session breakdown and canvas deep-links, journal-entries activity-bar timeline, PASU identity-setup wizard, quintessence display, personal cymatic field renderer body (Hopf-linked tori at personal scale), Personal Coordinate activity-bar resonance + conjugate-form indicator, composite oracle cast widget (I-Ching + Tarot psyche-anchor consuming 19.4), oracle history viewer (consuming 5.17 OracleSpreadPosition aliveness), medicine view (chakra-body-zones + active decan + herbalism), transform containers (Bohm / Talking Circle / Diamond with alchemical operator overlay), lens application (jungian / trika / phenomenal + synthesize), logos cycle stage UI, pratibimba personal coordinate with consent gating, kairos display wheel (mod-10 planets, consumes 19.12), Mercurius relay indicator (consumes 19.12), three-mode time-axis switcher (natal / real-time / kairotic — distinct from 11.12 sense-override), per-widget privacy-class chrome (border-tint), session-close wisdom-delta + quintessence-XOR ceremony pane (consumes 19.6 + 19.7), and tarot psyche-anchor coherence panel (consumes 19.4 + 19.5). Each widget is composable as a `TRACK_08_CONTRIBUTION` mini-mode for the `plugin-integrated-4-5-0` editor-area composition per 15.4. **Two roles, one substrate**: every widget ships standalone in `ide-deep` and composes into the `4-5-0` personal plugin per 15.4 — single React component, two compose sites.

The track is the deepest M-band delta in cycle 3 because M4 is the user-facing pole — the surface where lived event becomes legible. Substrate completeness does not become legibility until the widgets land.

## Source Specs and Matrix

- Canonical: `Idea/Bimba/Seeds/M/M4'/M4'-SPEC.md` (§§6.1–6.7, 7.1–7.13), `Idea/Bimba/Seeds/M/M4'/M4-ARCHITECTURE.md` (§§2, 4.3, 5.3, 7, 8.7), `Idea/Pratibimba/System/Subsystems/Nara/nara-ux-full-m4-branch-update.md` (full sweep), `Idea/Bimba/Seeds/M/M4'/2026-06-04-prospective-retrospective-canvas-spec.md` (canvas spec), `Idea/Bimba/Seeds/M/M'-SYSTEM-SPEC.md` (shell-layer)
- Companions: `Idea/Bimba/Seeds/M/M4'/m4-prime-psychoid-cymatic-field-engine.md`, `Idea/Bimba/Seeds/M/M4'/m4-prime-nara-day-episodes-and-oracle-artifacts.md`, `Idea/Bimba/Seeds/M/M4'/nara-m4-0-0-birthdate-encoding-spec.md`, `Idea/Bimba/Seeds/M/M4'/m4-prime-nara-integration-research.md`, `Idea/Bimba/Seeds/M/4-5-0-CONTEMPLATION-INTEGRATION-PLAN.md`, `Idea/Bimba/Seeds/M/INTEGRATED-4-5-0-RECOGNITION-ARCHITECTURE.md`, `Idea/Bimba/Seeds/M/alpha_quaternionic_integration_across_M_stack.md`
- Substrate: `Body/M/epi-theia/extensions/m4-nara/{package.json, src/common/{index.ts, nara-surface.ts}, src/browser/{frontend-module.ts, m4-nara-widget.tsx}, style/index.css}` (contract `2026-06-01.07-T7`); `Body/M/epi-theia/extensions/contracts/{07-t0-extension-contract-preflight.{md,json}, 08-t0-composition-contract-preflight.{md,json}}`; `Body/M/epi-theia/extensions/m-extension-runtime/src/` (`SharedBridgeAdapter`, `MathemeHarmonicProfileBoundary`, `CoordinateContext`, `MExtensionReadinessSnapshot`, `ReadinessBanner`); `Body/M/epi-theia/extensions/pratibimba-layouts/src/common/layout-types.ts` (activity-bar contracts); `Body/M/epi-theia/extensions/plugin-integrated-4-5-0/package.json`; `Body/S/S0/epi-lib/include/m4.h` (772 LOC) + `Body/S/S0/epi-lib/src/m4.c` (673 LOC); `Body/S/S0/portal-core/src/{personal_identity.rs, nara_journal.rs, kernel.rs, vak_address.rs, harmonic_profile.rs, events.rs}`; `Body/S/S0/epi-cli/src/nara/{medicine.rs, oracle.rs, transform.rs, lens.rs, logos.rs, pratibimba.rs, identity.rs, weights.rs, wind.rs}`; `Body/S/S0/epi-cli/src/vault/{pasu.rs, kairos.rs, day.rs}`; `Body/S/S3/gateway/src/{spacetime.rs, dispatch.rs}`; `Body/S/S3/graphiti-runtime/src/lib.rs`; `Body/S/S4/ta-onta/S4-3p-chronos/S3'/kairos-python-adapter.ts`.
- Full row-level reconciliation: `plan.runs/wave-c-m4-nara-frontend-matrix.md`
- Cross-tranche dependencies (consumed verbatim, NOT re-authored): **05** Tranches 5.1, 5.2, 5.4, 5.5, 5.9, 5.11, 5.15, 5.16, 5.17, 5.18, 5.19; **11** Tranches 11.3, 11.10, 11.11, 11.12; **15** Tranches 15.1, 15.2, 15.3, 15.4, 15.5, 15.6, 15.7, 15.10, 15.12; **19** Tranches 19.2, 19.4, 19.5, 19.6, 19.7, 19.11, 19.12; **08** integrated 4-5-0; **06** M5 review-core; **07** integrated-composition.

## Cycle 2 Substrate Inheritance

Consume as-is — `Body/M/epi-theia/extensions/m4-nara/` (the entire scaffold + contract `2026-06-01.07-T7`); `nara-surface.ts` (`NaraDayContainer`, `NaraArtifactEnvelope`, `NaraGraphitiEpisode`, `QActivityUpdatePolicy`, `ProtectedPersonalFieldInput`, `ConsentRecord`, `VoiceCorpusAdmissionInput`, `createNaraArtifact`, `createGraphitiEpisode`, `readNaraDayContainer`, `buildM4NaraSurface`, `buildS2CanonicalProjection`, `buildPublicProfilePayload`, `buildSpaceTimeRows`, `renderProtectedPersonalField`, `inspectOracleArtifact`, `evaluateQActivityUpdate`, `buildTemporalReading`, `evaluateVoiceCorpusAdmission`); `M4NaraWidget` (current dl scaffold); `m-extension-runtime` (`SharedBridgeAdapter`, `ReadinessBanner`, `MathemeHarmonicProfileBoundary`, `CoordinateContext`, `MExtensionReadinessSnapshot`, `Disposable`, `Emitter`/`Event`); `plugin-integrated-4-5-0` extension (consumes m4-nara via `TRACK_08_CONTRIBUTION`); `pratibimba-layouts` activity-bar contribution slots; Theia conventions (`@theia/core/lib/browser/widgets/react-widget`, `@theia/core/shared/inversify`, status-bar, command palette, keybinding chords). Cycle 2 Track 06 owned the `m4-nara` extension scaffold + Nara DayContainer + artifact spine + Graphiti browser; cycle 3 closes the per-widget product surface that consumes them. Every widget below extends `m4-nara/src/browser/` — no new extension package, no new substrate module, no new gateway adapter class.

## Surface Contracts (what's left after 11.10–11.12 contract)

The canvas, highlights, ambient strip, and tuning bar (11.10/11.11/11.12) own the **center editing surface and its immediate scaffolding**. Wave-C lands the SURROUNDING surface contracts:

- **Day-Calendar widget contract** — owns day selection across `${VAULT_ROOT}/Idea/Empty/Present/{YYYY}/{MM}/W{WW}/{DD}/` (DR-M4-1 ratified). Writes day selection to global `CoordinateContext.dayNow` via SharedBridgeAdapter. Read-only of vault.
- **DayContainer detail contract** — extends the current `M4NaraWidget` dl into per-session breakdown bound to `NaraDayContainer.nowLineage`, with `c_3_klein_weighting` / `c_4_artifact_role` / `c_3_briefing_emitted` / `t_4_kairos_context` chips; canvas deep-link per artifact routes to 11.10's `canvas-editor.tsx`.
- **Journal Entries activity-bar contract** — owns left-sidebar activity-bar mode `Journal Entries` for `daily-0-1` (15.3); 30-day timeline of NOW.md inscriptions; closes 15.3 ORPHAN.
- **PASU wizard contract** — 6-step stepper writing via new `nara.pasu.set` RPC (DR-WC-M4-3). Reads via `nara.pasu.show`. Residency at `Idea/Pratibimba/Self/PASU.md` per MEMORY.md.
- **Quintessence display contract** — read-only PASU consumer (`c_5_quintessence_hash`, `c_5_quintessence_clock`); subscribes to `m5.session.contemplation.complete` for wisdom_delta byte trail update.
- **Personal cymatic field contract** (`m4.nara.personalField`) — owns the M4 psychoid renderer surface; consumes Tranche 5.5 opaque renderer handle; DR-IG-6 dipyramid geometry (apex P5/P5' + interleaved P1-P4/P1'-P4' + axis P0/P0'); composes as 15.4 personal-side editor center slot AND standalone view (DR-WC-M4-2 single component).
- **Personal Coordinate activity-bar contract** — owns left-sidebar activity-bar mode `Personal Coordinate` for `daily-0-1` (15.3); lean resonance + ConjugateFormCharacter + L2' element glyphs + dominant chakra; consumes 5.1 (lean form). Closes 15.3 ORPHAN.
- **Oracle cast contract** — owns I-Ching (3-coin A=6/T=9/C=7/G=8) + Tarot psyche-anchor draw (Thoth/Golden Dawn); writes `kind: 'oracle'` artifact via existing `createNaraArtifact`; carries 5.11 OracleFrame + deck-context + symbolic protein refs; consumes 5.17 OracleSpreadPosition aliveness for live-state UI.
- **Oracle history contract** — read-only viewer over `dayContainer.artifactTree.filter(a => a.kind === 'oracle')` with 5.17 live-state and `QActivityUpdatePolicy.decayWindowOpen`.
- **Medicine view contract** — read-only over `medicine.rs` (`CHAKRA_BODY_ZONES[8]`, `DECAN_BODY_PARTS[36]`, `DECAN_HERBS[36]`, `PLANET_CHAKRA[8]`); consumes 5.16 L2' canonical element IDs + 19.12 live planet_degrees for active decan; canvas spec §6.4 consumer law (reads, does not reinvent).
- **Transform container contract** — three modes (Bohm / Talking Circle / Diamond) with cycle stages + alchemical operator overlay (canvas spec §6.5); writes `kind: 'contemplative'` artifact.
- **Lens application contract** — list + apply + synthesize (`nara.lens.list/apply/synthesize` RPCs); writes `kind: 'contemplative'` artifact carrying lens position + active-square + `vak_address`.
- **Logos cycle contract** — 6-stage A-Logos → An-a-Logos progression; advance/regress writes contemplative artifact; per-stage file is read-only.
- **Pratibimba personal coordinate contract** — `#4.4.4.4` PersonalNexus surface; consent-gated atlas-sync (DR-WC-M4-4 residency at PASU `c_4_atlas_sync_consents`); reads identity-augment proposals from 5.9 adapter (read-only on `proposed | reviewed`).
- **Kairos display contract** — mod-10 wheel (Sun=0..Pluto=9 per DR-M2-1; Earth NOT in wheel); three rings (natal / transit from 19.12 / oracle 4h decay); consumes via `nara.kairos.snapshot`.
- **Mercurius relay indicator contract** — small chrome (24px) above 25.15; subscribes to `mercurius.kairos.delta` observability event; FR-3 graceful stub.
- **Three-mode time-axis switcher contract** — distinct from 11.12 tuning bar `sense override` per DR-WC-M4-1; segmented control over `natal | real-time | kairotic`; switches which handle (`qIdentityHandle | qTransitHandle | qActivityHandle`) is foregrounded in 25.6.
- **Privacy-class chrome contract** — per-widget border-tint CSS (DR-WC-M4-5); NOT a status-bar entry (15.10 discipline preserved); every Wave-C widget carries it.
- **Session-close ceremony contract** — invoked on `m5.session.contemplation.complete`; renders wisdom_delta byte trail + XOR animation + 4 contemplation seeds + 9-bit virtue witness vector from 19.6/19.7.
- **Tarot psyche-anchor coherence contract** — reads session-frame psyche-anchor and M3 codon trace; computes coherence via 19.5 `m3_major_arcana_from_codon` reverse-lookup.
- **Track-08 mini-mode contributions** — every Wave-C widget extends the existing `TRACK_08_CONTRIBUTION` pattern in `m4-nara/src/common/index.ts:20`, declaring `miniModes: ['badge','compact-card','inspector']` and matching `currentStateSelectors`/`selectionHandlers` so the `4-5-0` plugin composes them per 15.4.

## Tranches

### 25.1 — Day-Calendar navigation widget *(spec-ahead-integration; first-build under `m4-nara`)*

Build `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/day-calendar.tsx` as a `@theia/core/lib/browser/widgets/react-widget` `ReactWidget` (matching the existing `M4NaraWidget` pattern), Inversify-decorated `@injectable()`, mounting a 7×N week-grid pivot across `${VAULT_ROOT}/Idea/Empty/Present/{YYYY}/{MM}/W{WW}/{DD}/` (DR-M4-1 canonical path). ISO-week roll-over computed locally (no gateway round-trip for navigation). On day-select dispatches `SharedBridgeAdapter.invokeGatewayRpc('nara.day.open', {dayId})` — a thin gateway shim that emits an observability event consumed by `M4NaraWidget` (25.2) and writes the day into the global `CoordinateContext.dayNow` per 15-foundation principle 1. Subscribes via `SharedBridgeAdapter.onCoordinateContext` so external coordinate changes update the visible week. Privacy chrome: `mext-privacy-protected-local` border-tint (25.18). Composes as `TRACK_08_EXPORTS` mini-mode `M4DayCalendarChip` (badge mode: month-day chip; compact-card: week-strip; inspector: full month). View id: `m4.nara.dayCalendar` (new — extend `ALL_VIEW_IDS` in `src/common/index.ts:11`). Register frontend module contribution in `frontend-module.ts`. Bind keybinding `cmd-shift-D` to focus the widget. Subscribes to `m5.session.contemplation.complete` event from gateway for past-session badge accent on days with completed reflection.

Verification: `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; `test -f Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/day-calendar.tsx`; `grep -nE 'm4.nara.dayCalendar|M4DayCalendarChip|nara.day.open' Body/M/epi-theia/extensions/m4-nara/src/common/index.ts` returns the new view id + export + RPC method name; ISO-week boundary test asserts week-grid matches `epi vault day-init` path canon for boundary cases (year roll-over, week-53); widget test renders 7×4 week-grid with `dayId` props and emits selection event; cross-link test asserts `M4NaraWidget` (25.2) updates its DayContainer when the calendar dispatches `nara.day.open`.

### 25.2 — DayContainer detail with per-session breakdown + canvas deep-link *(spec-ahead-integration; extends existing `M4NaraWidget`)*

Extend `Body/M/epi-theia/extensions/m4-nara/src/browser/m4-nara-widget.tsx` (the existing scaffold) so the `Nara DayContainer` section renders per-session blocks instead of a 4-row dl. Each session block iterates over `naraSurface.daySummary.nowLineage` and groups `artifactTree` rows by `sessionKey`. Per-session header chips: `c_3_klein_weighting` (rendered as small horizontal split bar — DOES NOT duplicate 11.12 ambient strip which shows the active session only — this is the historical at-rest read for the lineage), `c_3_briefing_emitted` (ISO timestamp pill if present, consuming the key Tranche 5.18 writes), `c_3_tranche_mode` + `c_3_response_orbit` (small chips). Per-artifact rows show `c_4_artifact_role`, `t_4_kairos_context: "[[Kairos]]"` chip (consumed from artifact frontmatter via gateway artifact-meta RPC), kind icon (`journal | dream | oracle | reminder | contemplative | agent-chat`), `bodySha256` short prefix as provenance handle (NOT body). Click an artifact row dispatches `commands.executeCommand('m4.openArtifact', artifactHandle)` (existing `OPEN_COMMAND_ID`) which routes to the canvas-editor (11.10) via `epi-logos://ide/m4-nara/canvas?artifactHandle=...`. Privacy chrome: `mext-privacy-protected-local-handle-only`. Composes as `M4SessionBreakdownCard` TRACK_08 export.

Per UX §6.5 the surface stays "lean identity sidebar only — no quaternion-dump." Therefore the per-session block does NOT render `q_composed_at_now`, raw resonance vectors, or any quaternion field beyond a single resonance score from Tranche 5.1's per-artifact render (the score is enough; the quaternion is not).

Verification: `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; render test against synthetic `NaraDayContainer` with two sessions × four artifacts; chip presence test asserts each declared frontmatter chip renders only when its key is present; deep-link test routes click through `commands.executeCommand('m4.openArtifact', ...)` and asserts canvas-editor (11.10) mounts; `grep -nE 'M4SessionBreakdownCard|c_3_klein_weighting|c_3_briefing_emitted|t_4_kairos_context' Body/M/epi-theia/extensions/m4-nara/src/` returns the chip-binding code; privacy invariant test asserts `protectedBodiesRendered: false` in the emitted observability event.

### 25.3 — Journal Entries activity-bar widget *(no-orphan-fill; closes 15.3 ORPHAN)*

Build `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/journal-entries-sidebar.tsx` as a `ReactWidget` registered against Theia's activity-bar contribution slot `widget.application-shell-left` per 15.3 + 15-foundation principle 7 ("Activity-bar discipline"). Gates to `daily-0-1` layout only (consumes `epi-logos.layout.active` preference); hidden in `ide-deep` (where its space is reclaimed by Backend Studio + Smart Connections). Renders a vertical timeline (last 30 days, latest at top) of NOW.md inscriptions across days — backed by `SharedBridgeAdapter.invokeGatewayRpc('nara.journal.timeline', {dayRange: 30})`. Each row carries day chip, NOW.md timestamp, session key short prefix, and a kind-icon ribbon for the artifacts the session inscribed. Click opens canvas-editor (11.10) at the relevant artifact via the same `m4.openArtifact` command. Privacy chrome: `mext-privacy-protected-local`. Composes as `M4JournalTimelineCard` TRACK_08 export. View id: `m4.nara.journalEntries` (new — extend `ALL_VIEW_IDS`). Register activity-bar contribution in `frontend-module.ts` per Theia `WidgetContribution` pattern.

Closes 15.3 ORPHAN for the `Journal Entries` activity-bar mode (declared in `layout-types.ts` for `daily-0-1` with no owning extension).

Verification: `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; activity-bar registration test asserts `Journal Entries` mode contributes to `daily-0-1` slot only; gate-test confirms the mode is hidden in `ide-deep` per 15.3; timeline render test consumes 7-day fixture; `grep -nE 'm4.nara.journalEntries|M4JournalTimelineCard|nara.journal.timeline' Body/M/epi-theia/extensions/m4-nara/src/common/index.ts` returns wiring; integration with 11.3 layout claim resolution asserts no widget-id orphan remains for this slot.

### 25.4 — PASU identity setup wizard *(spec-ahead-integration; DR-WC-M4-3)*

Build `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/pasu-wizard.tsx` as a 6-step stepper `ReactWidget`. Steps (matching `Body/S/S0/epi-cli/src/vault/pasu.rs::PasuRecord` shape): (1) **Birth date** (date picker; writes `c_0_birth_date` ISO); (2) **Birth location** (lat/lon picker + place name; writes `c_0_birth_location`); (3) **Natal chart path** — two paths: (a) upload existing chart JSON (writes `c_0_natal_chart_path`); (b) fetch from Kerykeion via Tranche 19.12's `mercurius_kairos_now` (`KAIROS_ENABLED=true` gates this option per FR-3); (4) **Jungian** typology editor (writes `c_2_jungian` per `epi nara identity set --jungian` CLI shape); (5) **Gene Keys** profile (writes `c_3_gene_keys`); (6) **Human Design** chart (writes `c_4_human_design`). Each step writes via `SharedBridgeAdapter.invokeGatewayRpc('nara.pasu.set', {key, value})` — **new gateway RPC routed through `S4S5DomainAdapter`** per DR-WC-M4-3 (the canonical write path; widget MUST NOT shell out to `epi vault pasu set`). Read path: `nara.pasu.show` returns the current PASU as a typed record (handle-only — sensitive fields like natal-chart raw stay local, only path string returns). View id: `m4.nara.pasuWizard` (new). Privacy chrome: `mext-privacy-protected-local`. Composes as `M4PasuWizardBadge` TRACK_08 export (badge mode shows completion %; compact-card shows current-step summary; inspector shows full stepper).

Note: `c_5_quintessence_hash` and `c_5_quintessence_clock` are NOT user-editable in the wizard — they are derived (per MEMORY.md "oracle charges → unit quaternion → clock position → BLAKE3") and surfaced read-only via 25.5.

Verification: `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; `cargo check -p epi-s3-gateway`; `grep -nE 'nara.pasu.set|nara.pasu.show' Body/S/S3/gateway/src/dispatch.rs` returns the new routes; round-trip test sets each PASU key via wizard and reads back via show; protected-local invariant test confirms natal-chart body never enters `buildPublicProfilePayload` (path string only); Kerykeion-fetch path test asserts FR-3 graceful stub when `KAIROS_ENABLED=false`; stepper UX test asserts back/next navigation persists draft state on session-scope.

### 25.5 — Quintessence display widget *(spec-ahead-integration; consumes 19.7 wisdom_delta + PASU)*

Build `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/quintessence-display.tsx` as a small read-only `ReactWidget`. Three rows: (1) **Quintessence hash** — first 8 bytes of `c_5_quintessence_hash` rendered as monospace hex with hover-tooltip showing full 32-byte BLAKE3 short-form; the raw 32-byte value never reaches the renderer (gateway returns a short-form handle per privacy law). (2) **Clock position** — `c_5_quintessence_clock` rendered as degree on cosmic clock 0–360 with cosmic-clock spec §"M4 Quintessence Identity Hash" attribution (Sun-anchored). (3) **Last wisdom_delta** — byte trail from the most recent T19.7 close consumed via `SharedBridgeAdapter.subscribeObservability({kind: 'm5.session.contemplation.complete'})`; renders as small hex strip pulsing once on arrival (purely visual — the actual XOR ceremony lives at 25.19). Subscribes to profile-tick per 15-foundation principle 2 — re-renders on tick advance to surface clock-position drift if `wisdom_delta` shifted it. View id: `m4.nara.quintessence` (new). Privacy chrome: `mext-privacy-protected-local`. Composes as `M4QuintessenceChip` TRACK_08 export.

Verification: `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; render test against fixture PASU; profile-tick subscriber test asserts re-render on synthetic tick advance; wisdom_delta arrival test asserts strip pulses once and persists last delta until next event; privacy test asserts raw 32-byte hash never enters rendered DOM (short-form only); tooltip test confirms full-hash rendering happens via hover-fetched gateway call, not as static prop.

### 25.6 — Personal cymatic field widget body *(spec-ahead-integration; closes `m4.nara.personalField` ORPHAN; DR-WC-M4-2; first-build under M4-ARCHITECTURE §5.3.1 + DR-IG-6)*

Build `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/personal-cymatic-field.tsx` as a `ReactWidget` that mounts the renderer handle returned by `SharedBridgeAdapter.invokeGatewayRpc('nara.field.handle', {sessionKey})` — the gateway wraps Tranche 5.5's `portal-core::psychoid_cymatic::renderer_handle()`. The widget body is a `<canvas>` element (or WebGL surface when available) into which the renderer-handle drives. **DR-IG-6 dipyramid geometry** is the structural law: 2 apex poles (P5 / P5'), 4 top-base vertices (P1 / P2 / P3 / P4), 4 inverted-base vertices (P1' / P2' / P3' / P4') interleaved by mirror law, central axis-point (P0 / P0'). Hopf-linked tori at personal scale wind around the apex axis. The widget never reads into renderer-state; the handle is opaque (DR-M4-3 strict-invariant: no raw bodies cross the bus).

Per DR-WC-M4-2: single React component implementing the `m4.nara.personalField` view id (already declared in `ALL_VIEW_IDS`) AND exported as `M4PersonalCymaticField` TRACK_08 mini-mode contribution, picked up by `plugin-integrated-4-5-0` for the 15.4 personal-side editor center composition slot. The widget consumes the three-mode time-axis switcher (25.17) to know which handle (`qIdentityHandle | qTransitHandle | qActivityHandle`) is foregrounded; renderer-handle accepts a `foregroundedHandle` parameter per call. View id is the existing `m4.nara.personalField`. Privacy chrome: `mext-privacy-protected-local-handle-only`.

Closes the m4.nara.personalField ORPHAN identified in the m4-nara contract (`ALL_VIEW_IDS` declared the id but no widget body existed).

Verification: `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; widget mount test consumes synthetic renderer handle; geometry test asserts all six P/P' positions render (P0 + P0' axis-point + P1..P4 + P1'..P4' base + P5 + P5' apex) per DR-IG-6 — `expect(scene.nodes.length).toBe(12)` over the dipyramid vertex set; Hopf-toric-link test asserts at least two interlocking tori wind the apex axis; composition test mounts widget under `plugin-integrated-4-5-0` editor center slot and asserts no layout breakage per 15.4; time-axis foregrounding test confirms 25.17 mode change propagates to renderer-handle call; privacy invariant test asserts the widget never reads a `q_*` body field — only handle-form passes the bus.

### 25.7 — Personal Coordinate activity-bar widget *(no-orphan-fill; spec-ahead-integration; closes 15.3 ORPHAN; complements Tranche 5.1)*

Build `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/personal-coordinate-sidebar.tsx` as a `ReactWidget` contributing the `Personal Coordinate` activity-bar mode for `daily-0-1` per 15.3. Closes the 15.3 ORPHAN. Tranche 5.1 renders resonance + ConjugateFormCharacter on EACH artifact in the day summary; this widget renders the LIVE Q_personal current-state sidebar — different consumption pattern, same substrate. Lean panel per UX §6.5 "no quaternion-dump":

(a) **Resonance score** (numeric 0.000–1.000) from `MathemeHarmonicProfileBoundary.resonance` (`personal_identity.rs::PersonalResonance::from_quaternions(q_personal, q_cosmic)` via gateway); (b) **ConjugateFormCharacter** (Major / Minor / Shadow) from kernel re-export; (c) **Four element-glyphs** from `ElementalBalance` rendered in L2' canonical order post-5.16 (Earth, Water, Air, Fire — the operative quartet excluding Aether and Salt frames); each glyph sized by intensity from `balance()`; (d) **Dominant chakra** from `ELEMENT_CHAKRA[dominant_element]` (post-5.16 indexed) + sun-decan ruling planet from `PLANET_CHAKRA[sun_planet_id]` resolved via `M4_Temporal_Now.planet_degrees[0]` (post-19.12).

Subscribes to profile-tick per 15-foundation principle 2 — re-renders on every advance. View id: `m4.nara.personalCoordinate` (new). Privacy chrome: `mext-privacy-protected-local`. Composes as `M4PersonalCoordinateBadge` TRACK_08 export.

Verification: `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; activity-bar registration test asserts `Personal Coordinate` mode contributes to `daily-0-1`; lean render test against fixture profile with `ConjugateFormCharacter.Major`; L2' element ordering test asserts (Earth, Water, Air, Fire) post-5.16 canonical IDs; no-quaternion-dump assertion test scans rendered DOM for `[w=, x=, y=, z=]` value strings and asserts absent; profile-tick subscriber test asserts re-render on synthetic tick advance; cross-link 5.1 test asserts the activity-bar surface and the per-artifact surface bind the SAME profile field (single source of truth).

### 25.8 — Composite Oracle cast widget *(spec-ahead-integration; consumes 19.4 + 5.11 + 5.17)*

Build `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/oracle-cast.tsx` as a `ReactWidget` with two sub-modes selected via a small tab strip: **I-Ching** (3-coin method with A=6 / T=9 / C=7 / G=8 nucleotide encoding per MEMORY.md M4 canonical) and **Tarot** (Thoth deck + Golden Dawn correspondences per MEMORY.md M3 canonical; pip names Princess/Prince per M3 code). Each mode is its own React component sub-folder under `src/browser/widgets/oracle/`.

**I-Ching sub-component:** 6-line hexagram builder; each line drawn by 3-coin equivalent (nucleotide draw); line type rendered as old-yin (6) / young-yang (8) / young-yin (7) / old-yang (9); moving lines tracked; output hexagram + line-change resolves through `oracle.rs::HEXAGRAM_BODY_DYNAMICS[64]` for body-chakra map; per-line codon ref written to artifact `scalarRefs` as `refKind: 'i-ching'`. Cast write: `SharedBridgeAdapter.invokeGatewayRpc('nara.oracle.cast_iching', {sessionKey, draw})` returns `{artifactHandle, vakAddress, deckContext, m3RefMap}` per Tranche 5.11 OracleFrame contract.

**Tarot sub-component:** spread-position picker (3-card, 5-card, Quaternal 4-card spread); draws consume `oracle.rs::PIP_DECAN_MAP[9][4]`, `COURT_SIGN_MAP[16]`, `ACE_ELEMENT_MAP[4]` via gateway. Psyche-anchor mode: the Quaternal 4-card spread at session-open uses 19.4 `m4_session_open` → `m4_draw_tarot` and writes the cards as session-context inheritance (NOT a new oracle artifact — it's a session-frame inscription). Cast write: `nara.oracle.cast_tarot` returns same shape. Tarot→body chain (per MEMORY.md): `card → suit(ACTG) → codon → decan → planet → elem_sig → chakra → CHAKRA_BODY_ZONES[8]` — single chain surfaced as a small overlay strip on each drawn card.

**OracleSpreadPosition aliveness (Tranche 5.17):** each drawn position renders its `live_state` (0=generating amber pulse / 1=muting fade / 2=mute grey); target-aspect indicator on positions with `target_aspect` from Tranche 5.17; click-to-mute control writes `nara.oracle.update_position_state` per Tranche 5.17 gateway addition.

View id: `m4.nara.oracleCast` (new). Privacy chrome: `mext-privacy-protected-local`. Composes as `M4OracleCastBadge` TRACK_08 export.

Verification: `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; I-Ching cast test produces hexagram + line-change + codon refs deterministically from fixture rng; Tarot draw test produces `M4_Tarot_Draw` with psyche-anchor + decan + planet + chakra chain; psyche-anchor cast on session-open writes session-frame inscription (NOT oracle artifact) via 19.4 path; OracleSpreadPosition aliveness state UI transitions through generating → muting → mute on synthetic state-update; protected-local assertion confirms interpretation body stays local (only handle + scalar refs cross bus); cross-link test asserts artifact carries `vak_address` + `deck_context` + `oracle_frame_ref` + `m3_ref_map` per Tranche 5.11.

### 25.9 — Oracle history viewer *(spec-ahead-integration; depends on 25.8, 5.17, 5.11)*

Build `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/oracle-history.tsx` as a `ReactWidget` listing past oracle casts in `dayContainer.artifactTree.filter(a => a.kind === 'oracle')` reverse-chronological order. Per-cast row: timestamp short, modality icon (I-Ching glyph / Tarot suit pip), short title, decay window state (open / 4h-decay-active / closed per `QActivityUpdatePolicy.decayWindowOpen`), hygiene state (consumes `epi nara oracle hygiene` output via gateway `nara.oracle.hygiene` RPC), live-state badge from Tranche 5.17 OracleSpreadPosition (`generating | muting | mute`). Click opens the cast in 25.8 Oracle Cast widget (read-only mode). Backed by `SharedBridgeAdapter.invokeGatewayRpc('nara.oracle.history', {dayRange})` — date-bounded fetch over `dayContainer.artifactTree` with OracleSpreadPosition aliveness join.

Decay-window expiry: row state transitions to `closed` after the 4h window per MEMORY.md M4 canonical ("oracle 4h decay"). Subscribes to profile-tick to refresh decay state on advance.

View id: `m4.nara.oracleHistory` (new). Privacy chrome: `mext-privacy-protected-local-handle-only`. Composes as `M4OracleHistoryCard` TRACK_08 export.

Verification: `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; widget test against fixture artifact tree with 3 oracle casts (1 I-Ching, 2 Tarot) mixed state; live-state mapping test asserts Tranche 5.17 enum → UI badge; decay-window expiry test consumes synthetic time advance and confirms closed transition; click-through test opens 25.8 in read-only mode; privacy assertion confirms no interpretation body rendered in row preview (only kind + title + state).

### 25.10 — Medicine view *(spec-ahead-integration; depends on 5.16 + 19.12)*

Build `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/medicine-view.tsx` as a `ReactWidget` with three panels organized vertically. The widget is a CONSUMER per canvas spec §6.4 ("the briefing reads, it does not reinvent") — it never calls `prescribe()` or `balance()` locally; every value is fetched from `SharedBridgeAdapter.invokeGatewayRpc('nara.medicine.snapshot')` which wraps `medicine.rs` LUTs and computations.

**Panel A — Chakra ladder:** 8-row chakra ladder (Muladhara → Sahasrara) from `CHAKRA_BODY_ZONES[8]`. Each row shows dominant element from `ELEMENT_CHAKRA` post-5.16 (canonical Earth=1, Water=2, Air=3, Fire=4 indices); body-zone strings render as click-to-expand list (75 zone strings total across 8 chakras per MEMORY.md). Active chakra (from current `M4_Temporal_Now.planet_degrees[Sun]` via 19.12 → decan → planet → `PLANET_CHAKRA`) highlighted with gold border.

**Panel B — Active decan:** Live decan from sun degree (0..359 mod 12 → sign; degree % 10 → decan index → `decan_idx_in_36 = sign_idx*3 + decan_in_sign`). Renders `DECAN_BODY_PARTS[36][decan_idx]` as body-part string + ruling planet glyph from decan's planetary rulership (via Tranche 19.10 `M0_M2_ZODIACAL_BRIDGE[12].decan_planets[3]`) + cross-link to 25.15 Kairos display for full mod-10 planet wheel.

**Panel C — Herbalism:** `DECAN_HERBS[36][active_decan_idx]` rendered as herb list with vernacular + botanical names; user can pin a herb to today's NOW.md frontmatter via `c_4_pinned_materia` array (writes through `nara.medicine.pin` RPC). Display is **evidence, not authority** per UX §10.4 "body has veto power" — widget never claims medical authority, never writes a prescription.

Active-decan rotation re-fires on each Tranche 19.12 kairos delta. Bit-fix from 5.16 (`body_zones_for_elem_sig` reading `(elem_sig >> 3) & 0b111`) consumed via gateway response. View id: `m4.nara.medicine` (new). Privacy chrome: `mext-privacy-protected-local`. Composes as `M4MedicineCard` TRACK_08 export.

Verification: `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; L2' canonical IDs (post-5.16) verified in widget output — Earth=1, Water=2, Air=3, Fire=4 indices honoured; active-decan rotation test consumes synthetic `M4_Temporal_Now.planet_degrees[Sun]` and asserts correct decan / body-part / ruling-planet; body-zone string assertion against `CHAKRA_BODY_ZONES[8]` fixture for 8 chakras; no-write assertion confirms widget never invokes `prescribe()` or mutates Q_identity / Q_activity / M4-0 evidence (only pin-materia frontmatter write allowed); cross-link test asserts 19.12 delta event triggers active-decan refresh.

### 25.11 — Transform container widget *(spec-ahead-integration)*

Build `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/transform-containers.tsx` as a `ReactWidget` with three container modes selectable via mode-strip: **Bohm Dialogue** (suspension / proprioception of thought / observer-observed collapse stages), **Talking Circle** (passage / speaker-listener / silence-as-speech stages), **Diamond** (essence / personality / contraction / opening stages). Each container exposes a 4-or-5-stage progression. Alchemical operator overlay tags each stage with the appropriate L2' alchemical-tonal register per canvas spec §6.5: the six core ops (nigredo / solutio / sublimatio / calcinatio / coagulatio / fixatio) and the two relational operations (separatio / conjunctio). The user advances/regresses through stages; each transition writes a `kind: 'contemplative'` artifact via `createNaraArtifact` with payload `{container, fromStage, toStage, alchemical_op}` and writes the alchemical-op tag into NOW.md frontmatter as `c_4_active_alchemical_op`. Stage advance via `nara.transform.advance` RPC; container start via `nara.transform.start`.

View id: `m4.nara.transformContainers` (new). Privacy chrome: `mext-privacy-protected-local`. Composes as `M4TransformBadge` TRACK_08 export.

Verification: `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; widget test renders all three containers + all six core alchemical ops + two relational ops as selectable; stage-transition test asserts only forward-stage advance unless explicit user back-step (with confirmation); artifact-write test confirms `kind: 'contemplative'` envelope carries `{container, fromStage, toStage, alchemical_op}` payload; NOW.md frontmatter write test asserts `c_4_active_alchemical_op` set correctly; cross-link 5.18 daily-briefing voice law test confirms the alchemical vocabulary surfaces in briefing somatic-register section without "today is X phase" detached claims.

### 25.12 — Lens application widget *(spec-ahead-integration; consumes 5.11)*

Build `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/lens-application.tsx` as a `ReactWidget` with three panels: **(a) Lens list** — three sub-tabs (Jungian / Trika / Phenomenal) backed by `nara.lens.list` RPC; each tab shows that lens's positions and active-square slots per UX §3.2 (M4-3 native MEF basin). **(b) Apply form** — subject text input + position picker + active-square selector; submits via `nara.lens.apply({lens, subject, position, active_square})`. Output renders inline as a small reading card with `c_3_lens_route`, `c_3_active_square`, and resolved `vak_address` (per Tranche 5.11). **(c) Synthesize** — multi-lens composition: select 2+ prior apply outputs and submit `nara.lens.synthesize({applications: [handles]})` which returns a synthesized reading.

Each apply or synthesize writes a `kind: 'contemplative'` artifact carrying lens position references, active-square, and `vak_address`. Per UX §10.3 "Symbolic systems are dialects, not authorities" the widget always shows lens-position as a READING NAME, never as an authoritative claim about the subject — subject body remains protected-local.

View id: `m4.nara.lensApplication` (new). Privacy chrome: `mext-privacy-protected-local-handle-only` (subject body stays local; only position + active-square + vak_address cross). Composes as `M4LensCard` TRACK_08 export.

Verification: `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; lens-list RPC test against synthetic backend; apply-form test against fixture subject produces deterministic position+active-square; synthesize composition test produces multi-lens artifact with active-square assertion; privacy test confirms only lens position + active-square + vak_address fields enter `buildS2CanonicalProjection`, NOT subject body; `grep -nE 'nara.lens.list|nara.lens.apply|nara.lens.synthesize' Body/S/S3/gateway/src/` returns the three new RPCs.

### 25.13 — Logos cycle stage widget *(spec-ahead-integration)*

Build `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/logos-cycle.tsx` as a `ReactWidget` showing the user's current logos stage in a 6-position progression ring (A-Logos → An-a-Logos, per MEMORY.md m4 logos). Each position is a small card with the stage file content rendered read-only (the file lives under `Idea/Empty/Present/{day_id}/logos/{stage}.md` — per `epi-cli/src/nara/logos.rs` CLI shape). Two controls: **Advance** (writes via `nara.logos.advance` RPC; creates contemplative artifact); **Regress** (writes via `nara.logos.regress`; creates contemplative artifact with explicit `c_4_regression: true` flag). Per-stage card shows the stage's archetype-ratio per `MEMORY.md` ("6-stage cycle"); ring position rotates as user advances. View id: `m4.nara.logosCycle` (new). Privacy chrome: `mext-privacy-protected-local`. Composes as `M4LogosChip` TRACK_08 export.

Verification: `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; 6-stage render test asserts all positions visible; advance/regress test cycles state machine through 6 positions deterministically; artifact-write test on each transition emits contemplative artifact with stage-from/stage-to fields; regression flag test asserts `c_4_regression: true` appears only on regress writes; cross-link 5.18 briefing test asserts logos stage surfaces in briefing QL Walk section.

### 25.14 — Pratibimba personal-coordinate widget with consent gating *(spec-ahead-integration; DR-WC-M4-4; consumes 5.9 + 5.5)*

Build `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/pratibimba-coordinate.tsx` as a `ReactWidget` for the `#4.4.4.4` PersonalNexus. Three panels: **(a) Current state** — opaque handle render of `ProtectedPersonalFieldInput` via `renderProtectedPersonalField` (nara-surface.ts:322); shows `surfaceId`, `privacyClass`, `readiness`, `readinessLabel`, `fieldHash`, and the 6 handle keys (`qIdentityHandle`, `qTransitHandle`, `qActivityHandle`, `qComposedHandle`, `audioBusHandle`, `planetaryChakralStateHandle`) — handle strings only, never body. **(b) Atlas-sync consent control** — typed `ConsentRecord` editor (action: `nara.voice-corpus.include | nara.graphiti.body.inspect | nara.shared-archetype.publish`; scope: `single-artifact | single-day | adapter-corpus`; pressureFree + inspectable booleans). Per DR-WC-M4-4 ConsentRecord residency is `Idea/Pratibimba/Self/PASU.md` `c_4_atlas_sync_consents` array — widget writes via 25.4's `nara.pasu.set` with array-append semantics. Existing `evaluateVoiceCorpusAdmission` (nara-surface.ts:389) validates before write. **(c) Pending identity proposals** — read-only list of `proposed | reviewed` identity-augment proposals from Tranche 5.9 adapter (`nara.identity.proposals.list`). Only `applied` proposals affect `Q_identity` — widget is consumer, not driver. Per UX §10.1 "Never mutate M4-0 from daily activity" the widget enforces M5'-review-gate as the only mutation path; rejecting a proposal writes `rejected` state via 5.9 adapter.

View id: `m4.nara.pratibimbaCoordinate` (new). Privacy chrome: `mext-privacy-protected-local-handle-only`. Composes as `M4PratibimbaCoordinateBadge` TRACK_08 export.

Verification: `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; widget test renders handle panel + consent editor + proposals list; consent-record write test routes through `nara.pasu.set` and lands as array-append in PASU `c_4_atlas_sync_consents`; proposal-state test asserts read-only mode for `proposed | reviewed` and write path only for accept/reject; privacy test asserts no raw `q_b, q_p, q_personal, q_composed` body enters the surface; cross-link 5.9 adapter test confirms only `applied` mutates `Q_identity`.

### 25.15 — Kairos display wheel *(spec-ahead-integration; consumes 19.12)*

Build `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/kairos-display.tsx` as a `ReactWidget` rendering a 10-planet mod-10 wheel (Sun=0, Moon=1, Mercury=2, Venus=3, Mars=4, Jupiter=5, Saturn=6, Uranus=7, Neptune=8, Pluto=9 — Earth as observer-centre NOT in wheel per DR-M2-1 + Track 03.5). Three layered rings on the wheel: **(a) Natal** positions from PASU.md `c_0_natal_chart_path` (resolved via `nara.kairos.natal_positions` RPC); **(b) Transit** positions from `M4_Temporal_Now.planet_degrees[10]` populated by Tranche 19.12 `mercurius_kairos_now`; **(c) Oracle moment** — the cast-time snapshot if an active oracle cast is within the 4h decay window (consumed from 25.9 oracle history). Each ring renders planet glyphs at their degree positions; angular aspects (conjunction, opposition, square, trine, sextile) drawn as connecting arcs between planets. Center of wheel shows the active sign-decan (sun-anchored). The widget is **read-only**; transit refresh fires on the 19.12 mercurius delta event.

Subscribes to `mercurius.kairos.delta` observability event via `SharedBridgeAdapter.subscribeObservability({kind: 'mercurius.kairos.delta'})` for live refresh. View id: `m4.nara.kairosDisplay` (new). Privacy chrome: `mext-privacy-protected-local`. Composes as `M4KairosWheel` TRACK_08 export.

Verification: `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; mod-10 ordering test asserts Sun=0, Moon=1, ... Pluto=9 with no Earth in wheel (length 10); three-ring render test against fixture PASU + fixture `M4_Temporal_Now` + fixture oracle moment; refresh-chip subscriber test re-renders on synthetic 19.12 kairos delta event; aspect-arc render test asserts arcs draw for fixture conjunction/opposition pairs; protected-local assertion confirms natal data stays local (positions cross, raw chart body does not).

### 25.16 — Mercurius signal-relay indicator *(spec-ahead-integration; chrome consumer of 19.12)*

Build `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/mercurius-relay-indicator.tsx` as a small chrome ReactWidget (~24px tall) that mounts above 25.15 in `ide-deep` and as a status-bar-adjacent chip in `daily-0-1`. Shows: last refresh ISO timestamp, delta count since last UI mount, `KAIROS_ENABLED` flag state (per FR-3 stub gate from 19.12). Subscribes via `SharedBridgeAdapter.subscribeObservability({kind: 'mercurius.kairos.delta'})`. When `KAIROS_ENABLED=false` the indicator renders a grey "Kairos disabled" stub; when true and a delta arrives the indicator pulses once and updates the timestamp.

This is the M4-side surface for Mercurius's CF(0/1/2/3) carrier per MEMORY.md ("Mercurius carries kairos signal: PASU.md → Kerykeion → M4 degrees → agents"). It does NOT re-author Mercurius — that's 19.12's deliverable.

View id: `m4.nara.mercuriusRelay` (new). Privacy chrome: none (indicator only — no body). Composes as `M4MercuriusRelayChip` TRACK_08 export.

Verification: `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; subscribe test asserts indicator updates on synthetic `mercurius.kairos.delta` event; FR-3 stub-mode test asserts grey state when `KAIROS_ENABLED=false`; mod-10 ordering propagation test asserts every delta payload carries `planet_degrees[10]` (length 10 enforcement); pulse-animation test asserts single pulse on each delta.

### 25.17 — Three-mode time-axis switcher *(spec-ahead-integration; DR-WC-M4-1 distinct-from-tuning-bar)*

Build `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/time-axis-switcher.tsx` as a small `ReactWidget` (segmented control, ~28px tall) with three modes: `natal | real-time | kairotic`. Per DR-WC-M4-1 this is DISTINCT from the 11.12 tuning bar `sense-override` control — that controls Klein prospective/retrospective face (per Tranche 5.15); this controls WHICH quaternion composition reading is foregrounded:

- `natal` → foregrounds `qIdentityHandle` (the Kerykeion natal baseline, stable);
- `real-time` → foregrounds `qTransitHandle` (current Kerykeion transit positions, refreshed daily);
- `kairotic` → foregrounds `qActivityHandle` (the 4h-decay-windowed activity perturbation reading).

Switching writes session-scope state via `SharedBridgeAdapter.invokeGatewayRpc('nara.session.set_time_axis', {sessionKey, mode})` and propagates to the personal cymatic field renderer (25.6) via subscription. Keystroke: `cmd-shift-T` cycles modes. Default `real-time`. Persisted at session scope; survives `daily-0-1` ↔ `ide-deep` layout switch per 15.7. View id: `m4.nara.timeAxisSwitcher` (new). Privacy chrome: `mext-privacy-protected-local`. Composes as `M4TimeAxisSwitcherChip` TRACK_08 export.

Verification: `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; mode-cycle test asserts `cmd-shift-T` progresses through three modes deterministically; renderer-foregrounding test asserts 25.6 receives the correct handle as foregrounded on mode change; persistence test asserts session-scope mode survives layout switch per 15.7 cross-layout state contract; distinctness test asserts changing 11.12 tuning bar `sense override` does NOT affect this widget's mode (different axes per DR-WC-M4-1).

### 25.18 — Privacy-class chrome CSS contribution *(doc-ahead-landing + spec-ahead-integration; DR-WC-M4-5)*

Build `Body/M/epi-theia/extensions/m4-nara/style/privacy-chrome.css` with three border-tint classes binding to the three M4 privacy classes:

```css
.mext-privacy-protected-local { border-left: 3px solid #8a7355; /* warm earth */ }
.mext-privacy-protected-local-handle-only { border-left: 3px solid #6b7588; /* slate */ }
.mext-privacy-shared-archetype-opt-in { border-left: 3px solid #d4a574; /* gold */ }
```

Every Wave-C widget (25.1-25.20) MUST apply the matching tint class on its outer container at mount. Per DR-WC-M4-5 the indicator does NOT enter the status bar (preserves 15.10 status-bar discipline — six entries only: profile-tick, day-now, session id, gateway readiness, profile generation, active coordinate). Title-bar tooltip on every widget carries the full `PRIVACY_CLASS` string for accessibility. Import the stylesheet in `frontend-module.ts`.

Cross-link the m4-nara contract `PRIVACY_CLASS = 'protected_local_handle_only'` (existing default) — the contract continues to declare the surface-level class; the per-widget tint class is one of three based on what the widget actually renders.

Verification: every Wave-C widget test asserts the outer container carries one of the three tint classes from this contribution; CSS contract test asserts each tint has a defined rule with the specified hex color; privacy-class assertion test enumerates Wave-C widgets and confirms each declares its tint at mount time via the React `className` prop; status-bar discipline test confirms no privacy-class entry leaks into 15.10's status bar.

### 25.19 — Session-close wisdom-delta + quintessence-XOR ceremony pane *(spec-ahead-integration; consumes 19.6 + 19.7)*

Build `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/session-close-ceremony.tsx` as a `ReactWidget` that mounts on the `m5.session.contemplation.complete` observability event (subscribed via `SharedBridgeAdapter.subscribeObservability`). Four sections rendered sequentially: **(a) Wisdom-delta byte trail** — the bioquaternionic resonance-correction bytes returned by 19.6 `contemplate_session_close` RPC, rendered as hex strip; **(b) XOR animation** — visual fold of delta into `quintessence_hash` per T19.7's `m4_mobius_return` XOR; uses a small 8-byte register animation per `quintessence_hash`'s first 8 bytes; **(c) Four contemplation seeds** — the Arch 3/5/7/9 prompts from T19.3 `CONTEMPLATION_PROMPT_LUT[12]` (positions 3, 5, 7, 9) rendered as four card panels (speech / relationship / action / completion per 19.9); **(d) Virtue witness vector** — 9-bit witness from 19.6's Position 0' (Verifier-Anuttara) over `VIRTUE_LUT[9]`, rendered as nine small witness lamps (lit/unlit per bit).

Reads via `SharedBridgeAdapter.invokeGatewayRpc('nara.session.close_summary', {sessionKey})` after the contemplation event fires. Widget is purely ceremonial — NEVER mutates state. Per MEMORY.md "M4 Quintessence Identity Hash: oracle charges → unit quaternion → clock position → BLAKE3" the BLAKE3 carrier is hash-only (raw bytes never enter widget DOM — short-form handles only). Re-mount fires on each new session close.

View id: `m4.nara.sessionCloseCeremony` (new — modal-class, mounts over current view). Privacy chrome: `mext-privacy-protected-local-handle-only`. Composes as `M4SessionCloseCeremonyCard` TRACK_08 export.

Verification: `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; widget mounts on synthetic `m5.session.contemplation.complete` event; wisdom_delta render test asserts byte trail visible as hex strip; XOR animation test fires on tick boundary and renders 8-byte register update; four-contemplation-seed render test asserts each Arch 3/5/7/9 prompt visible with correct register label (speech / relationship / action / completion); virtue-witness 9-bit vector test asserts all nine witness lamps render with state from 19.6 Verifier-Anuttara output; privacy test confirms only handle-form of `quintessence_hash` enters rendered DOM (NOT raw 32-byte BLAKE3); cross-link 5.20 LLM-Nara test asserts the ceremony surfaces 19.6 4'-5'-0' triplet outputs.

### 25.20 — Tarot psyche-anchor coherence panel *(spec-ahead-integration; consumes 19.4 + 19.5)*

Build `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/psyche-anchor-coherence.tsx` as a `ReactWidget` rendering the session's tarot psyche-anchor coherence reading. Two sections side-by-side: **(a) Psyche-anchor cards** — the four cards from `M4_Session_Frame.tarot_psyche_anchor` (drawn at session-open per 19.4), rendered with name + suit + Golden Dawn decan correspondence per MEMORY.md M3 canonical; **(b) Session codon trace** — the codons that rotated through M3 during the session (sourced from gateway's session-trace); each codon shown with its corresponding Major Arcana card resolved via T19.5 `m3_major_arcana_from_codon` reverse-lookup. **Coherence indicator** at top: count of codon-arcana matches with psyche-anchor cards / total codons in trace, rendered as percentage + per-card hit list. Per T19.6 the question is "Did the tarot psyche-anchor's cards correspond to codons that appeared in the trajectory?" — this widget surfaces the answer.

Consumes via `SharedBridgeAdapter.invokeGatewayRpc('nara.session.psyche_anchor', {sessionKey})` returning `{anchor_cards, codon_trace, coherence_score, match_list}`. Read-only — never mutates session state. View id: `m4.nara.psycheAnchorCoherence` (new). Privacy chrome: `mext-privacy-protected-local-handle-only`. Composes as `M4PsycheAnchorCoherenceCard` TRACK_08 export.

Verification: `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; widget test renders 4 anchor cards + N codons against fixture session; coherence-score test asserts deterministic count from fixture (e.g. 2 of 4 cards match); reverse-lookup test consumes T19.5 utility output via gateway and asserts correct arcana name for each codon; STOP codon test asserts the widget surfaces "no card" badge for STOP codons (T19.5 returns 0xFF); privacy test asserts session-frame body never crosses bus (handle-only).

### 25.21 — Integrated `4-5-0` composition mini-mode contributions registry *(spec-ahead-integration; consolidates Wave-C composition surface)*

Extend `Body/M/epi-theia/extensions/m4-nara/src/common/index.ts` to enroll every Wave-C widget into the existing `TRACK_08_CONTRIBUTION` registry. Replace the current `TRACK_08_EXPORTS = ["M4ArtifactHandleChip","M4RecognitionMiniView"]` with the full Wave-C set:

```ts
export const TRACK_08_EXPORTS = [
    "M4ArtifactHandleChip",          // pre-existing
    "M4RecognitionMiniView",         // pre-existing
    "M4DayCalendarChip",             // 25.1
    "M4SessionBreakdownCard",        // 25.2
    "M4JournalTimelineCard",         // 25.3
    "M4PasuWizardBadge",             // 25.4
    "M4QuintessenceChip",            // 25.5
    "M4PersonalCymaticField",        // 25.6 — center composition slot per 15.4
    "M4PersonalCoordinateBadge",     // 25.7
    "M4OracleCastBadge",             // 25.8
    "M4OracleHistoryCard",           // 25.9
    "M4MedicineCard",                // 25.10
    "M4TransformBadge",              // 25.11
    "M4LensCard",                    // 25.12
    "M4LogosChip",                   // 25.13
    "M4PratibimbaCoordinateBadge",   // 25.14
    "M4KairosWheel",                 // 25.15
    "M4MercuriusRelayChip",          // 25.16
    "M4TimeAxisSwitcherChip",        // 25.17
    "M4SessionCloseCeremonyCard",    // 25.19
    "M4PsycheAnchorCoherenceCard"    // 25.20
] as const;
```

Each export declares `miniModes: ['badge','compact-card','inspector']` per the existing pattern at `m4-nara/src/common/index.ts:23-36`. Each declares matching `currentStateSelectors` (`{id: '<viewId>.currentProfile', source: 'shared-bridge', reads: ['profile','readiness','coordinateContext']}`) and `selectionHandlers` (`{exportName: '<viewId>SelectionHandler', inputKind: '<viewId>.selection', outputRoute: ROUTE_PATH}`). `evidenceSerializers` extended for each Wave-C view id with `privacyClass` matching that widget's chrome.

Cross-link `plugin-integrated-4-5-0` plugin consumer (per 08-integrated-4-5-0 ratification, plugin already deps on `m4-nara`). The plugin's `integrated-composition` consumer reads `TRACK_08_CONTRIBUTION` and lays out the personal-side editor area per 15.4 composition pattern: `M4PersonalCymaticField` (25.6) at center; `M4SessionBreakdownCard` (25.2) at right; `M4JournalTimelineCard` (25.3) at left; ambient strip + tuning bar (11.12) above the canvas (11.10) which composes alongside the cymatic field per 15.4 personal-side editor area. Each of the remaining Wave-C widgets surfaces as a badge or compact-card slot in the integrated plugin's contextual panels per `08-t0-composition-contract-preflight.json`.

Verification: `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; contract-preflight validator (`scripts/validate-extension-contract-preflight.mjs`) accepts the extended `TRACK_08_EXPORTS`; composition-contract validator (`08-t0`) asserts each export has matching mini-mode contribution; integration test mounts each Wave-C widget under `plugin-integrated-4-5-0` editor area without breaking the composition contract per 15.4; cross-link 11.9 surface-extension-contract-ledger test asserts every Wave-C view id maps to its owner (the `m4-nara` extension) with status `aligned`.

---

**End of Track 25.** The delta over 11.10/11.11/11.12 + 19.4/19.6/19.11/19.12 is closed: day calendar (25.1), DayContainer detail with deep-link (25.2), journal entries timeline (25.3), PASU wizard (25.4), quintessence display (25.5), personal cymatic field renderer body (25.6), Personal Coordinate sidebar (25.7), oracle cast widget (25.8), oracle history viewer (25.9), medicine view (25.10), transform containers (25.11), lens application (25.12), logos cycle (25.13), pratibimba personal coordinate (25.14), kairos display wheel (25.15), Mercurius relay indicator (25.16), three-mode time-axis switcher (25.17), privacy-class chrome (25.18), session-close ceremony (25.19), psyche-anchor coherence (25.20), and the integrated-4-5-0 composition registry (25.21). Three 15.3 ORPHANs close (25.3 Journal Entries, 25.6 personal-field view body, 25.7 Personal Coordinate). Five decisions go to register (DR-WC-M4-1 through DR-WC-M4-5). Every widget is a CONSUMER of landed substrate or tranche-closed upstream primitive — no widget re-authors `portal-core`, `medicine.rs`, `oracle.rs`, the psychoid renderer, the kairos populator, the canvas, the highlights, the ambient strip, the tuning bar, the flow-watcher, the response-orbit, the re-entry hook, or the contemplation RPC. Two roles, one substrate: every Wave-C widget ships standalone in `ide-deep` and composes into the `4-5-0` personal plugin per 15.4. M4 PROTECTED-LOCAL privacy class enforced on every artifact write via `nara-surface.ts` projection law + per-widget chrome (25.18). The surface that makes M4's substrate legible is the surface that closes the cycle.
