# Wave C — M3' Mahāmāyā Frontend Deep-Design Reconciliation Matrix

**Task id:** `wave-c-m3-mahamaya-frontend`
**Domain:** the `m3-mahamaya` Theia extension as a **standalone deep widget** in `ide-deep` AND as the matter-pole input to the `cosmic-1-2-3` composition (M3 codon-rotation projecting onto the K² lens-ring carried by `m1-paramasiva-played-torus`, per Tranche 15.4).
**Anti-greenfield posture:** `Body/M/epi-theia/extensions/m3-mahamaya/` is landed first-slice substrate (contract `2026-06-01.07-T6`, `buildM3ProjectionSurface`, `m3-mahamaya-widget.tsx`). Every WC row below is `consume as-is`, `audit/verify`, `extend`, or a named integration closure over the landed `M3ProjectionSurface`. No first-build of a parallel rendering pipeline. Wave-C is exclusively the **per-Mn widget DEPTH pass** for M3' — it does not re-decide M3 substrate (Wave A / Tranche 04) and does not re-decide shell hosting / cross-layout intent (Track 11) or foundation principles / composition contract (Track 15).

## Source list (corpora actually consulted)

- **Corpus 1 (UX intent)** — `Idea/Pratibimba/System/Subsystems/Mahamaya/mahamaya-ux-full-m3-branch.md` §0 (M3 = symbolic-genetics parser + matter pole), §1 ("turning a tarot card *is* changing the mode"), §3 (integrated symbolic-genetics parser; 2-bit nucleotide logic A=6/T=9/C=7/G=8; codon = quaternion in Cl(4,2); 16-pair dinucleotide; 72→64 reception), §4 (clock-field as live data structure; angular relations), §4.1 (Lens Annulus / 16+1), §6 (M0' graph view + M3' clock-wheel over one Neo4j substrate), §7.5 (alive-and-tarot-like default; multi-matrix summonable), §8 (kernel-trace overlay only), §9 (inspectors), §10.1 (TCT / 17th-lens open).
- **Corpus 2 (M' Seed authority)** —
  - `Idea/Bimba/Seeds/M/M'-SYSTEM-SPEC.md` §"Shell layer" L91–149 (M3 as one of the six full-depth subsystem pages; `(0/1)` toggle as `#`-enacted at UI scale).
  - `Idea/Bimba/Seeds/M/M3'/M3'-SPEC.md` §0/1, §7 ((lens,mode) ↔ (codon,rotation) bidirectional), §7.5 (surface-philosophy), §8.4 (Cl(4,2) at four scales), §8.6 (360 degree-nodes + 24 backbone + 384 line-change), §8.7 (56+8 exact cover), §8.9 (SU(2) double-cover, 720°), §8.10 (16+1 lens-stack), §8.12 (M3-0 reception law), §8.13 (M3-5 double-torus four-depth-view law), §8.14 (integrated 1-2-3 plugin), §9 (multi-matrix inspectors), §10.1 (open questions TCT / 17th-lens / 72→64 uniqueness).
  - `Idea/Bimba/Seeds/M/M3'/M3-ARCHITECTURE.md` (Third Spanda Equation canonical five forms; 7-8-9 spine; coupling-flow / measurement-face inspector).
  - `Idea/Bimba/Seeds/M/M-SYMBOLIC-LANGUAGE-ARCHITECTURE.md` (TranscriptionalClockPacket / OracleFrame / SymbolicProtein chain).
  - `Idea/Bimba/Seeds/M/M1'/M1-2-ANANDA-VORTEX-ARCHITECTURE.md` (cross-cutting carrier surface — referenced for Klein-flip choreography handoff at tick 5→6).
- **Corpus 3 (substrate the renderer reads)** —
  - `Body/S/S0/portal-core/src/codon_rotation_projection.rs` (`CODON_ROTATION_SURFACE_COUNT = 472`, `codon_rotation_from_lens_mode`, `lens_mode_from_codon_rotation`, `codon_charge_quaternion`, `dataset_lut_state = "materialized-kernel-lut"`, `LENS_COUNT = 12` — M1' chromatic lens namespace).
  - `Body/S/S0/portal-core/src/codon.rs::classify_codon` (3-tier Perfect/Imperfect/NonPalindromicNonDual/Dual).
  - `Body/S/S0/epi-lib/src/m3.c` — `M3_TAROT_CODON_MAP[4][16]` (per-suit + court entries totalling 64); `M3_CODON_TO_AA[64]` ([m3.c:221-245](Body/S/S0/epi-lib/src/m3.c:221)); `M3_MAJOR_ARCANA[22]` ([m3.c:266-289](Body/S/S0/epi-lib/src/m3.c:266)); per-suit integral `_Static_assert(84+96+88+92==360)`. TCT entry `[Nine, TCT, NONE]` (rank=8/Nine of Wands single-codon).
  - `Body/S/S0/epi-lib/src/m3_clock_lut.c::CLOCK_DEGREE_LUT[360]` (385-node cosmic clock substrate per CLAUDE.md COSMIC CLOCK SPEC: 360 degree + 24 amino backbone + 1 Axis Mundi/Quintessence).
  - `Body/S/S0/epi-lib/test/m3/test_m3.c:525-527` (`TCT state count = 7` runtime authority).
  - `Body/S/S0/epi-cli/src/nara/oracle.rs::PIP_DECAN_MAP[4][9]`, `COURT_SIGN_MAP[4][4]`, `ACE_ELEMENT_MAP[4]`, `HEXAGRAM_BODY_DYNAMICS[64]`, `hexagram_body_lookup` — already-landed Golden Dawn / Thoth Tarot decan addressing + per-hexagram body dynamics ledger (canonical reference, NOT direct import — m3-mahamaya is in `forbiddenDirectImports` for `Body/S/S0` per contract 07-T0).
  - `Body/S/S0/epi-cli/src/nara/medicine.rs::CHAKRA_BODY_ZONES[8]`, `DECAN_BODY_PARTS[36]`, `PLANET_CHAKRA[8]`, `ELEMENT_CHAKRA[5]`, `SIGN_ELEMENT[12]` (downstream consumers of the M3 decan addressing chain; cited as the existing tarot→decan→planet→element→chakra→body zone chain — M3' renderer surfaces the same chain through the bridge, never re-imports).
  - `Body/S/S0/epi-cli/schemas/src/kernel-bridge.ts` — `MathemeHarmonicProfile` Zod schema; `codonRotationProjection: z.record(z.unknown())` already typed.
- **Corpus 4 (Theia surface — landed substrate)** —
  - `Body/M/epi-theia/extensions/m3-mahamaya/package.json` (first-slice priority: "64/472 wheel with backend-provided codon projection, provenance, and trace overlay only").
  - `Body/M/epi-theia/extensions/m3-mahamaya/src/common/index.ts` (`EXTENSION_ID = 'm3-mahamaya'`; `PRIMARY_VIEW_ID = 'm3.mahamaya.cosmicWheel'`; `ALL_VIEW_IDS = ["m3.mahamaya.cosmicWheel","m3.mahamaya.codonNavigator","m3.mahamaya.traceOverlay"]`; `PRIVACY_CLASS = 'public_current_with_scalar_oracle_refs_only'`; `OBSERVABILITY_EVENT_TYPES = ["m3.codon_projection","m3.kernel_trace_view"]`; `TRACK_08_EXPORTS = ["M3CodonChip","M3WheelMiniView"]`; `forbiddenImports: ["Body/S/S0","Body/S/S2","Body/S/S3","portal-core","@clockworklabs/spacetimedb-sdk"]`; `bridgeAdapterSymbol: 'SHARED_BRIDGE_ADAPTER'`).
  - `Body/M/epi-theia/extensions/m3-mahamaya/src/common/codon-wheel.ts` (`M3_CODON_WHEEL_CONTRACT_VERSION = '2026-06-01.07-T6'`; `M3_EXPECTED_NON_DUAL_CODONS = 40`; `M3_EXPECTED_DUAL_CODONS = 24`; `M3_EXPECTED_ROTATIONAL_STATES = 472`; `M3ProjectionSurface { activeProjection, wheelSummary, m30ProvenanceStrip, depthViews, provenance, pendingFields, readiness, observabilityEvents }`; `M3ScalarOracleRef`; `validateM3LibrarySummary`; `resolveM3ScalarOracleRef`; depth-view records currently `flatClock` / `doubleTorusWorldClock` / `janusOverlay`).
  - `Body/M/epi-theia/extensions/m3-mahamaya/src/browser/m3-mahamaya-widget.tsx` (DI singleton `SharedBridgeAdapter`; `onReadiness` / `onProfile` / `onCoordinateContext` subscriptions; current render = `<dl>` table over `activeProjection`).
  - `Body/M/epi-theia/extensions/contracts/07-t0-extension-contract-preflight.{md,json}` — m3-mahamaya entry L321-383 (capability set, subscription set, method families `s2.codon.scalar_ref.read` / `s2.m3.library.read` / `s3.world_clock.read` / `s3.subscription.readiness.read` / `s5.evidence.deposit`).
  - `Body/M/epi-theia/extensions/m-extension-runtime/src/common/profile.ts` (`MathemeHarmonicProfileBoundary` — `generation: number`, `pointerAnchor: string | null`, `capabilities: readonly string[]`, `payload: Readonly<Record<string, unknown>>`).
- **Wave-A cross-reference** — `plan.runs/wave-a-m3-reconciliation-matrix.md` (DR-M3-1 TCT cardinality; DR-M3-2 72→64 epogdoon identity; DR-M3-3 17th-lens / 16+1 namespace split; DR-M3-6 Third Spanda canonical 137 = 64+2(36)+1; tranche 4.x lineage).
- **Wave-B cross-reference** — `plan.runs/wave-b-theia-shell-matrix.md` (TS-04, TS-05, TS-11, TS-25 — shell hosts the m3-mahamaya extension; cross-layout intent envelope carries codon state; acceptance-harness must assert M3 codon survives the daily-0-1 ↔ ide-deep toggle).
- **Tranche 11 / 15 / 19 boundaries (avoided)** — 11 owns shell hosting + cross-layout intent; 15 owns composition contract + the m1-paramasiva-played-torus K² renderer (15.8/15.9); 19.5 owns the new `m3_major_arcana_from_codon` utility (substrate). Wave-C consumes them, does not re-author.

## Master 4-way matrix

| # | Claim (UX doc) | Spec authority (M3'-SPEC / M3-ARCHITECTURE) | Code/substrate evidence | Theia surface (m3-mahamaya widget design) | Status |
|---|---|---|---|---|---|
| WC-M3-01 | M3' default surface is "alive & tarot-like" (UX §0, §7.5, §10) — the standalone Mahamaya wheel in `ide-deep` IS the deep widget that closes the per-extension UX pass | §7.5 surface-philosophy invariant; first-slice = 64/472 wheel + provenance + trace overlay; multi-matrix is summonable | `m3-mahamaya-widget.tsx::render()` currently renders a flat `<dl>` over `activeProjection.codon / rotation / DET / wheel-states / pending`; landed first-slice but is NOT the wheel | The Wave-C extension target: replace the flat `<dl>` with a real "alive & tarot-like" K² wheel component (`<M3CosmicWheel>`) consuming the existing `M3ProjectionSurface.activeProjection` — no new contract, no new profile field, no new C/Rust table. Wave-C names the widget IDs and named services that close this. | **DOC-AHEAD-LANDING** — Wave-C tranche `24.1` owns; substrate ready, default surface render not yet built |
| WC-M3-02 | The 360° clock IS a live data structure — nodes pinned by clock geometry; cross-clock relations as angular/hop edges; perturbs as the tick turns (UX §4) | §8.6 `384 = 64 × 6` line-change graph + 24-spoke 15° lattice + 12-deep ring-buffer; cosmic-clock spec 385 nodes = 360 degree + 24 amino backbone + 1 Axis Mundi (per CLAUDE.md COSMIC CLOCK SPEC 2026-03-12; supersedes FR 2.3.5 stub) | `epi-lib/src/m3_clock_lut.c::CLOCK_DEGREE_LUT[360]` materialised; per CLAUDE.md MEMORY: "385 nodes total: 360 degree nodes + 24 amino acid backbone + 1 Axis Mundi/Quintessence"; structural law `_Static_assert(360+24 == 64*6)` (clock topology = I-Ching LINE_CHANGE count) | `codon-wheel.ts::buildDepthViews` exposes `flatClock` / `doubleTorusWorldClock` / `janusOverlay` as **labelled depth-view records** with no rendering payload beyond the common active facts — no angular/hop graph layer; CosmicClockPlugin replacement of M0Dashboard+M1Walk+M2Vibrational stubs (per MEMORY) is NOT here | **DOC-AHEAD-LANDING** — Wave-C tranche `24.2` owns the 360-degree + 24-amino + 1-Axis Mundi clock renderer as a per-Mn widget; substrate ready, rendering not |
| WC-M3-03 | 16 sacred-circle divisions (the 16 lenses) are simultaneous static apertures over the clock — NOT a pair matrix; mirror Anuttara 16-fold void (CLAUDE.md MEMORY COSMIC CLOCK + UX §4.1 Lens Annulus) | M3'-SPEC §8.10 (16+1 Mahāmāyā lens-stack); CLAUDE.md "16 lenses = 16 sacred circle divisions (NOT pair matrix) mirroring Anuttara 16-fold void"; `M3_LENS_STACK` namespace (per DR-M3-3) | No `M3_LENS_STACK[17]` table in substrate. The current `codon_rotation_projection.rs::LENS_COUNT = 12` IS the M1' chromatic lens namespace per DR-M3-3 ratified; `M3_LENS_STACK` is a future profile-field over `MathemeHarmonicProfile`, not a renderer-local table | No Lens Annulus aperture-switcher widget. Renderer must consume `payload.mahamayaLensStack.activeLensId` (or equivalent — final field name landed by Tranche 10.M3 per Wave-B kernel-bridge) and never invent a local 17-table | **SPEC-AHEAD** (kernel-bridge profile-field landing is upstream of Wave-C; Wave-C tranche `24.3` defines the renderer consumption contract and the aperture-switcher widget; renders honest-pending until the field lands) |
| WC-M3-04 | 9 walks = distinct sequential traversal paths; WALK_SPANDA (12-step/30°), WALK_HEXAGRAM / WALK_LINE_CHANGE (2^6 binary), etc.; 9 walks ≠ 16 lenses (CLAUDE.md MEMORY: "16 lenses = simultaneous static apertures; 9 walks = sequential traversal paths (DISTINCT)") | CLAUDE.md COSMIC CLOCK SPEC reference: 9 walk types; UX §4 angular/hop/traversal edges | Per MEMORY.md: `WALK_TORUS` renamed `WALK_SPANDA` (M1 Paramasiva, 12-step/30°); WALK_HEXAGRAM/WALK_LINE_CHANGE = 2^6 binary; substrate clock-walk-protocol expected in `m3_clock_lut.c` or a sibling `m3_walks.c` (Wave-C does not assume which) | No walk-navigator widget. Renderer must surface walks as **named, selectable traversal paths** on the clock-field, distinct from the 16 simultaneous apertures | **DOC-AHEAD-LANDING** — Wave-C tranche `24.4` owns the walk navigator widget; renderer consumes `payload.cosmicClock.walks[]` enumeration (named entries, kernel-trace-only) |
| WC-M3-05 | 64-codon hexagram browser with line-change projection (UX §3, §4; SPEC §8.6 384 = 64 × 6 line-change graph) | §3 codon = quaternion in Cl(4,2); §8.6 `lineChangeOperator(Address)`; `mahamaya.hexagramId / upperTrigram / lowerTrigram / lineChangeOperator` already in profile payload | `codon-wheel.ts::activeProjectionFacts` reads `hexagramId / upperTrigram / lowerTrigram / lineChangeOperator` from `payload.mahamaya` — landed | No hexagram browser widget. Renderer must surface the 64 hexagrams as selectable cells with per-cell line-change projection over the 384-graph | **DOC-AHEAD-LANDING** — Wave-C tranche `24.5` owns the hexagram browser + line-change projection panel |
| WC-M3-06 | Tarot wheel: 22 major arcana ↔ codon via `m3_major_arcana_from_codon` (consumed from 19.5); minor arcana via PIP_DECAN_MAP / COURT_SIGN_MAP / ACE_ELEMENT_MAP (UX §1, §3, §5; SPEC §8.7) | §8.7 56+8 exact cover; 19.5 `m3_major_arcana_from_codon(uint8_t codon) → uint8_t card_id` (composition of `M3_CODON_TO_AA[codon]` + reverse on `M3_MAJOR_ARCANA[].amino_acid_index`); CLAUDE.md MEMORY: `TAROT_DECAN_MAP[56]` GD decan assignments | `M3_MAJOR_ARCANA[22]` ([m3.c:266-289](Body/S/S0/epi-lib/src/m3.c:266)); `M3_CODON_TO_AA[64]` ([m3.c:221-245](Body/S/S0/epi-lib/src/m3.c:221)); 19.5 utility is pending substrate but composition is trivially derivable kernel-side; `oracle.rs::PIP_DECAN_MAP / COURT_SIGN_MAP / ACE_ELEMENT_MAP` already landed (Golden Dawn / Thoth) | No tarot wheel widget. Renderer must surface 22 major + 56 minor as a wheel keyed off `activeProjection.codonId` (major arm) and `activeProjection.tarotMinorId` (minor arm) — never re-import the maps; consume through the bridge profile / scalar-ref resolution | **DOC-AHEAD-LANDING** — Wave-C tranche `24.6` owns the tarot wheel widget; depends on 19.5 substrate utility landing AND on the `mahamaya.tarotMajorArcanaCardId` profile-field landing through Wave-B kernel-bridge (named here, not authored here) |
| WC-M3-07 | Decan-tarot addressing UI: card → suit (ACTG) → codon → decan → planet → element → chakra → body zone (CLAUDE.md MEMORY master-clarity-plan 2026-03-11: "ONE LUT: CHAKRA_BODY_ZONES[8] indexed by Chakra_Id. All routes (planet/decan/tarot) arrive here.") | M3'-SPEC §8.7 tarot decan addressing; cross-link to M4 medicine canonical body chain (downstream consumer) | `oracle.rs::pip_decan_for_card / court_for_card / ace_element_for_card`; `medicine.rs::CHAKRA_BODY_ZONES[8]`, `DECAN_BODY_PARTS[36]`, `PLANET_CHAKRA[8]` — landed; M3 substrate exposes the chain through nara modules (used by M4 today) | M3' must render a **breadcrumb / chain panel** that displays the full chain for the current `activeProjection.tarotMinorId` / `tarotShadowCodon`. Renderer consumes the chain through `bridge.invokeGatewayRpc('s2.codon.scalar_ref.read', { scalarRef })` — never imports `oracle.rs` / `medicine.rs` directly (`forbiddenImports` in 07-T0) | **DOC-AHEAD-LANDING** — Wave-C tranche `24.7` owns the addressing-chain panel widget; depends on `s2.codon.scalar_ref.read` returning the chain elements |
| WC-M3-08 | Hexagram body dynamics viewer: `HEXAGRAM_BODY_DYNAMICS[64]` per hexagram with chakra IDs + body_zones array (CLAUDE.md MEMORY: "HEXAGRAM_BODY_DYNAMICS[64]: HexagramBodyEntry per hexagram with chakra IDs + body_zones array") | M3'-SPEC §4 (M3 serves Nara via body-aware oracle); Wave-A row 18 (M3 → M4) | `oracle.rs::HEXAGRAM_BODY_DYNAMICS[64]` + `hexagram_body_lookup(u8)` landed | Renderer must surface per-hexagram body dynamics (chakra IDs + body_zone strings) as a sub-panel of the hexagram browser (24.5). Consumed through the bridge `s2.codon.scalar_ref.read` with `refKind: 'i-ching'`; never local | **DOC-AHEAD-LANDING** — Wave-C tranche `24.8` owns the hexagram body-dynamics sub-panel; honest-pending if the bridge method does not yet return body-dynamics |
| WC-M3-09 | TCT / Nine-of-Wands cardinality (DR-M3-1) — dataset 8 vs runtime 7; renderer must surface the resolved value, not invent | DR-M3-1 ratified: dataset reconciliation, NOT code change; runtime authority = 7 non-dual | `test_m3.c:525-527` asserts state_count = 7; `classify_codon(0x35) → ImperfectPalindromic` (non-dual); `M3_TAROT_CODON_MAP[1][8] = { Nine, TCT, NONE }` (single-codon Nine of Wands) | Renderer surfaces `activeProjection.rotationalStateCount` directly from the profile; for TCT specifically (`codonId === 0x35`), the surfaced count MUST equal 7 — assertion belongs to the validate-extension-contract-preflight test extended in Wave-C | **CONTRADICTION-DOWNSTREAM** — Wave-C tranche `24.9` owns the renderer-side surfacing rule (NEVER renders 8 for TCT) + extends `Body/M/epi-theia/extensions/test/validate-extension-contract-preflight.test.mjs` with the TCT-7 fixture |
| WC-M3-10 | 16+1 / 17th-lens namespace split (DR-M3-3) — `M2-1' Vimarśa MEF surface` (12 chromatic) vs `M3_LENS_STACK` (16+1 Mahamaya) MUST not silently merge | DR-M3-3 ratified per Tranche 4.6 | `codon_rotation_projection.rs::LENS_COUNT = 12` is the M1' chromatic namespace; no `M3_LENS_STACK_COUNT = 17` exists (forbidden) | Renderer's Lens Annulus aperture-switcher (WC-M3-03) MUST label its data as `M3_LENS_STACK` distinctly from `M1_LENS`; the `activeProjectionFacts.lens` field (currently sourced from `projection.lens`) routes to the M1' chromatic lane; the `mahamayaLensStack.activeLensId` profile-field routes to the M3 aperture lane | **CONTRADICTION-DOWNSTREAM** — Wave-C tranche `24.10` owns the renderer-side namespace discipline: two named widget IDs (`pratibimba.m3-mahamaya:m1-chromatic-lens` consumer AND `pratibimba.m3-mahamaya:m3-lens-stack-aperture`) must coexist without conflation |
| WC-M3-11 | Quintessence / Akasha emerges from balance of 4 elements (low variance in nucleotide balance) (CLAUDE.md MEMORY master-clarity-plan) | §8.4 codon-quaternion `pp+mm+mp+pm = 4X` invariant; §9 charge-quaternion display with `4X` audit | `portal-core::codon_charge_quaternion` returns the normalized [pp,mm,mp,pm]; the `4X` invariant is the auditable kernel law | Renderer must show the four-element balance + a Quintessence/Akasha indicator computed from low variance over `(pp, mm, mp, pm)`. The indicator IS the runtime audit of the `pp+mm+mp+pm = 4X` invariant rendered visually — renderer never recomputes; consumes via `activeProjection.chargeQuaternion` (NEW field needed; named for Wave-B kernel-bridge handoff) | **SPEC-AHEAD** — Wave-C tranche `24.11` owns the Quintessence/Akasha indicator widget; depends on `mahamaya.chargeQuaternion = [pp, mm, mp, pm]` profile-field landing (Wave-B handoff, named here) |
| WC-M3-12 | Third Spanda matheme proof panel (DR-M3-6) — `137 = 64 + 2(36) + 1`; cross-link 19.9 7-8-9 spine + Tranche 4.10 coupling-flow inspector | DR-M3-6 canonical (Third Spanda Equation); `M3-ARCHITECTURE.md` five canonical forms + execution-order trace; Tranche 4.10 coupling-flow / measurement-face inspector mandates source-warrant rendering only | Substrate side is doc-and-skeleton-event: `Additive137`, `MersenneM7Ground`, `SpandaCrownBifurcation` are profile-bus skeleton events per Tranche 4.10 | Renderer must surface the five canonical forms (Mersenne / Binary / Octave-field / Spanda-bridge / M-stack) + the execution-order trace + the translation rule `9_{M_2} = 8_{M_3} + 1_{M_1}` as a labelled inspector. Read-only / source-warrant UI — must NOT compute alpha or RG flow. String audit rejects `QL derives alpha`; accepts disciplined caveat language | **DOC-AHEAD-LANDING** — Wave-C tranche `24.12` owns the Third-Spanda matheme proof panel widget; consumes `payload.couplingFlowAlignment.thirdSpandaForms[]` + `payload.thirdSpandaEvent` (Wave-B kernel-bridge handoff, named here) |
| WC-M3-13 | M3 codon-rotation projects onto the K² lens-ring cells of the played K² torus rendered by `m1-paramasiva-played-torus` per Tranche 15.4 (cosmic-1-2-3 composition contract) | Tranche 15.4 composition-pattern contract: integrated plugins compose three M-extensions into one editor surface; M3 codon-rotation projects onto lens-ring cells on K² torus | `plugin-integrated-1-2-3` package depends on `m1-paramasiva` + `m2-parashakti` + `m3-mahamaya` + `integrated-composition`; `m1-paramasiva-played-torus` extension is the K² renderer (DR-M1-2 ratified) — currently pending build (per TS-23 in wave-b) | The standalone m3-mahamaya widget MUST expose a **reads-only contract** that the cosmic-1-2-3 composition consumes: `m3-mahamaya:codonRotationProjectionForLensRing` (named export) returns the active `(codon, rotation)` × `(lens, mode)` projection state on a per-clock-tick basis. Composition pulls this; never the other way. M3-side never mutates K² geometry | **SPEC-AHEAD** — Wave-C tranche `24.13` owns the named contract export + the no-write discipline; verified via 15.4 composition-contract test + Wave-C extension test |
| WC-M3-14 | 3-coin I-Ching cast UI (A=6 old yin / T=9 old yang / C=7 young yin / G=8 young yang) per CLAUDE.md master-clarity-plan | UX §3 "the 6/9/7/8 I-Ching values are the operative arithmetic of the whole system"; SPEC §8.7 56+8 cover; oracle cast flow | `Body/S/S0/epi-cli/src/nara/oracle.rs` already implements 3-coin / yarrow cast flow with these values; substrate landed | Renderer must surface a 3-coin cast UI ribbon inside the wheel: cast → six lines → primary hexagram → changing lines → derived hexagram. Cast invocation = `bridge.invokeGatewayRpc('s5.oracle.iching.cast', …)`; renderer never randomizes locally | **DOC-AHEAD-LANDING** — Wave-C tranche `24.14` owns the 3-coin cast ribbon widget; the cast RPC method-family entry is named for Wave-B kernel-bridge confirmation (current 07-T0 m3-mahamaya entry has `s2.codon.scalar_ref.read` but not a cast invocation method — Wave-C names the addition without authoring the kernel-bridge contract update; that belongs to Wave-B) |
| WC-M3-15 | Profile-tick is the global UI clock (Foundation Principle 2 / 15.6) — widgets re-render on `MathemeHarmonicProfile` tick advance | Tranche 15.6 spec-ahead-integration; FR-2 binding | `m3-mahamaya-widget.tsx::init()` already subscribes to `bridge.onProfile(profile => …)`; landed | The wheel + all sub-inspectors MUST drive their re-render off the profile-tick subscription, NOT internal RAF / timer; the angular/hop edge overlay (24.2) MUST advance one position per tick; the cast ribbon (24.14) freezes mid-cast for human input but un-freezes on tick advance once result is received | **ALIGNED** at subscription level; **DOC-AHEAD-LANDING** at the discipline level — Wave-C tranche `24.15` owns the per-widget tick-discipline audit |
| WC-M3-16 | Provenance always visible (Foundation Principle 3) — readiness state inline on every datum | Tranche 15.6 + 10 (kernel-bridge readiness ledger) | `codon-wheel.ts` already returns `provenance: M3ProvenanceHandle[]` + `pendingFields[]` + `readiness.blockers[]`; `m3-mahamaya-widget.tsx` renders `ReadinessBanner` from runtime | Every widget added in 24.x MUST surface inline readiness (border-colour, pending badge, blocked overlay) per the kernel-bridge readiness ledger. NO separate "errors" panel inside the M3 widget; the existing `ReadinessBanner` is the top-level surface; sub-widgets inherit via React context | **ALIGNED** at substrate; Wave-C tranche `24.15` extends the discipline to the new sub-widgets |
| WC-M3-17 | Privacy class = `public_current_with_scalar_oracle_refs_only`; no raw bioquaternion, no journal, no unreconciled Graphiti — resolve scalar refs only | M3'-SPEC §4 privacy boundary; `index.ts::PRIVACY_CLASS` constant; `resolveM3ScalarOracleRef` rejects `protectedArtifactBody` | Landed: `resolveM3ScalarOracleRef` throws if `input.ref.protectedArtifactBody !== undefined` | Every new Wave-C widget MUST inherit this discipline. Tarot / I-Ching / decan-chain surfaces must consume `M3ScalarOracleRef` shape and never receive bodies. Renderer NEVER fetches Graphiti, Nara journal, or M4-0 identity-system sources | **ALIGNED** at boundary; Wave-C tranche `24.7`, `24.8`, `24.14` honour the boundary by routing every reach through `bridge.invokeGatewayRpc('s2.codon.scalar_ref.read' / 's2.m3.library.read')` with scalar-ref-only payload |
| WC-M3-18 | SharedBridgeAdapter is the only conduit; `forbiddenImports` includes `Body/S/S0`, `Body/S/S2`, `Body/S/S3`, `portal-core`, `@clockworklabs/spacetimedb-sdk` (07-T0 + index.ts) | 07-T0 m3-mahamaya entry L359-365 + `TRACK_08_CONTRIBUTION.compositionBoundary.forbiddenImports` | `index.ts::TRACK_08_CONTRIBUTION.compositionBoundary.forbiddenImports = ['Body/S/S0', 'Body/S/S2', 'Body/S/S3', 'portal-core', '@clockworklabs/spacetimedb-sdk']`; `bridgeAdapterSymbol = 'SHARED_BRIDGE_ADAPTER'` | Every Wave-C widget MUST consume named services injected via `SHARED_BRIDGE_ADAPTER` Inversify symbol; NO direct import of `oracle.rs`, `medicine.rs`, `m3.c`, `codon_rotation_projection.rs`. The named-service layer (e.g. `CosmicClockRenderService`, `TarotDecanService`, `HexagramBodyDynamicsService`) wraps `bridge.invokeGatewayRpc` calls — these services live INSIDE `m3-mahamaya/src/browser/services/` | **ALIGNED** at policy; Wave-C tranche `24.16` owns the renderer-service architecture |
| WC-M3-19 | Composition over juxtaposition (Foundation Principle 6) — integrated 1-2-3 plugin composes three extensions into one surface, not three side-by-side panes | Tranche 15.4 composition-pattern contract | `plugin-integrated-1-2-3` extension deps on three Mn-extensions + `integrated-composition`; the composition contract at `08-t0-composition-contract-preflight.md` is authoritative | The standalone m3-mahamaya wheel widget (24.1) IS the "active Mn extension widget" in `ide-deep` editor area (per 15-foundation surface contracts). The 24.13 named export `m3-mahamaya:codonRotationProjectionForLensRing` IS the composition handle. NO direct rendering of `m1-paramasiva` or `m2-parashakti` widgets inside the m3-mahamaya widget — composition belongs to `plugin-integrated-1-2-3` | **ALIGNED** at boundary; Wave-C tranche `24.13` honours both sides |
| WC-M3-20 | M3' is consumed by both the `ide-deep` editor area (standalone deep widget — 15-foundation surface contracts) AND the cosmic-side of `daily-0-1` (via `M3CodonChip` / `M3WheelMiniView` mini-views per 07-T0 `track08Exports`) | TRACK_08_EXPORTS + TRACK_08_CONTRIBUTION.compactViews ['badge', 'mini-view']; UX §10 default surface; 15-foundation §"Surface Contracts" | `index.ts::TRACK_08_CONTRIBUTION.compactViews` already declares `M3CodonChip` (`m3.mahamaya.cosmicWheel`) and `M3WheelMiniView` (`m3.mahamaya.codonNavigator`) with mini-modes `['badge', 'mini-view']` | Wave-C tranche `24.1` builds the full wheel for `ide-deep`; the mini-modes (`M3CodonChip` / `M3WheelMiniView`) MUST share the SAME `M3CosmicWheelRenderService` component, parametrized by `miniMode` ('badge' | 'mini-view' | 'full'). NO duplicate render pipeline | **DOC-AHEAD-LANDING** — Wave-C tranche `24.17` owns the parametrized renderer architecture (one component, three modes) |

## Anomalies

### CONTRADICTIONS (renderer-side surfacing rules — Wave-C tranches own)

- **DR-WC-M3-1** — TCT renderer-side rule (downstream of DR-M3-1): the m3-mahamaya widget MUST NEVER render `rotationalStateCount: 8` when `codonId === 0x35` (TCT / Nine of Wands). Substrate authority IS 7; if a future malformed profile-field arrives with 8, the widget MUST badge it as `authority_payload_invalid` (the existing `surfaceBlockers` predicate is extended). Renderer-side, no decision register needed — just an assertion test. **Owner:** Wave-C tranche `24.9`.
- **DR-WC-M3-2** — Lens namespace renderer-side rule (downstream of DR-M3-3): the m3-mahamaya widget MUST surface two named lanes — `pratibimba.m3-mahamaya:m1-chromatic-lens-consumer` (the existing `activeFacts.lens` from `projection.lens`, M1' chromatic, count 12) AND `pratibimba.m3-mahamaya:m3-lens-stack-aperture` (the new `payload.mahamayaLensStack.activeLensId`, M3 aperture, count 16+1) — visually labelled distinctly. **Owner:** Wave-C tranche `24.10`.

### SPEC-AHEAD (Wave-B kernel-bridge handoffs named, NOT authored here)

- **WC-M3-SA-1** — `mahamayaLensStack.activeLensId` (and the 17-cell `mahamayaLensStack.lenses[]` aperture descriptor) profile-field is not yet present on `MathemeHarmonicProfile.payload`; required by 24.3, 24.10. Named for Wave-B kernel-bridge (Tranche 10.M3) — Wave-C consumes when landed; renders honest-pending until.
- **WC-M3-SA-2** — `mahamaya.tarotMajorArcanaCardId` profile-field (`uint8_t card_id` from `m3_major_arcana_from_codon(codonId)`) not yet present; required by 24.6. Named for Wave-B kernel-bridge + depends on 19.5 substrate utility landing.
- **WC-M3-SA-3** — `mahamaya.chargeQuaternion = [pp, mm, mp, pm]` (and the audit invariant `chargeQuaternionInvariant: pp+mm+mp+pm === 4X`) profile-field not yet present; required by 24.11. Named for Wave-B kernel-bridge.
- **WC-M3-SA-4** — `cosmicClock.walks[]` enumeration (the 9 named walk types, including `WALK_SPANDA` / `WALK_HEXAGRAM` / `WALK_LINE_CHANGE`) profile-field not yet present; required by 24.4. Named for Wave-B kernel-bridge.
- **WC-M3-SA-5** — `couplingFlowAlignment.thirdSpandaForms[]` profile-field (the five canonical forms + execution-order trace + translation rule) not yet present; required by 24.12. Named for Wave-B kernel-bridge + cross-link Tranche 4.10.
- **WC-M3-SA-6** — `s5.oracle.iching.cast` (and `s5.oracle.tarot.cast`) RPC method-family entries NOT in the 07-T0 m3-mahamaya method-families list (current list: `s2.codon.scalar_ref.read`, `s2.m3.library.read`, `s3.world_clock.read`, `s3.subscription.readiness.read`, `s5.evidence.deposit`). Required by 24.14. Named here for Wave-B kernel-bridge / contract-preflight extension.

### CODE-PENDING (renderer-side; Wave-C builds)

- **WC-M3-CP-1** — `M3CosmicWheelRenderService` (the named React component / Inversify service replacing the `<dl>` table render). 24.1 builds.
- **WC-M3-CP-2** — `CosmicClockRenderService` (the 360-degree + 24-amino + 1-Axis Mundi clock renderer with θ=degree-node, φ=torus_stage 3D toroidal layout; Braille-canvas-style fallback for non-WebGL contexts). 24.2 builds.
- **WC-M3-CP-3** — `TarotDecanService` (the renderer-side service that calls `bridge.invokeGatewayRpc('s2.codon.scalar_ref.read', …)` for tarot decan addressing). 24.7 builds.
- **WC-M3-CP-4** — `HexagramBodyDynamicsService` (same pattern, for hexagram body dynamics). 24.8 builds.
- **WC-M3-CP-5** — `ThirdSpandaMathemeProofPanel` (the matheme proof inspector). 24.12 builds.

### ALIGNED-ONLY-NOTE

- **WC-M3-AON-1** — The `MathemeHarmonicProfile` boundary, `SharedBridgeAdapter`, `ReadinessBanner`, `Inversify` DI symbols are all landed in `@pratibimba/m-extension-runtime`. Wave-C consumes — no parallel runtime.
- **WC-M3-AON-2** — `M3ScalarOracleRef` + `resolveM3ScalarOracleRef` privacy-class guard is landed. Wave-C consumes; new services route every reach through it.
- **WC-M3-AON-3** — The `surfaceBlockers` / `surfacePendingFields` discipline is landed in `codon-wheel.ts`. Wave-C extends the lists rather than parallel-tracking pending state.

## Proposed Wave-C Closing Tranches (m3-mahamaya frontend deep design — id space `24.x`)

- **24.1** — Wheel-as-default-surface widget (`M3CosmicWheelRenderService`); replace `<dl>` render
- **24.2** — Cosmic-Clock renderer (385-node clock: 360 degree + 24 amino backbone + 1 Axis Mundi; angular/hop edge overlay)
- **24.3** — 16-lens aperture switcher (16 sacred-circle divisions; simultaneous static apertures)
- **24.4** — 9-walk traversal navigator (sequential walk paths distinct from the 16 apertures)
- **24.5** — 64-codon hexagram browser with line-change projection
- **24.6** — Tarot wheel (22 major arcana + 56 minor) keyed off `codonId` and `tarotMinorId`
- **24.7** — Decan-tarot addressing chain breadcrumb panel
- **24.8** — Hexagram body-dynamics viewer (per-hexagram chakra + body_zones)
- **24.9** — TCT / Nine-of-Wands cardinality renderer-side surfacing rule + test fixture
- **24.10** — `M1_LENS` vs `M3_LENS_STACK` namespace discipline + two named widget consumers
- **24.11** — Quintessence / Akasha emergent balance indicator (`pp+mm+mp+pm = 4X` runtime audit visualised)
- **24.12** — Third-Spanda matheme proof panel (DR-M3-6; five canonical forms)
- **24.13** — Codon-rotation projection on K² torus lens-ring (named reads-only export for cosmic-1-2-3 composition per 15.4)
- **24.14** — 3-coin I-Ching cast ribbon (A=6 / T=9 / C=7 / G=8)
- **24.15** — Profile-tick discipline + inline-provenance audit across all 24.x widgets
- **24.16** — Renderer-service architecture (`services/` folder; `SHARED_BRIDGE_ADAPTER` as the only conduit)
- **24.17** — Parametrized renderer (one `M3CosmicWheelRenderService`, three modes: `'badge' | 'mini-view' | 'full'` — Track 08 mini-mode parity)

---

**End of matrix.** The m3-mahamaya extension substrate is **ALIGNED at first-slice presence**: the contract version, the `M3ProjectionSurface` builder, the `ReadinessBanner` surface, the privacy boundary, the `M3ScalarOracleRef` guard, the `TRACK_08_CONTRIBUTION` are all landed. The Wave-C work is the **per-Mn DEPTH pass**: turning the flat `<dl>` first-slice render into the full alive-and-tarot-like K² wheel + the 385-node cosmic clock + the 16 apertures + the 9 walks + the hexagram browser + the tarot wheel + the decan addressing chain + the hexagram body dynamics + the Quintessence indicator + the Third-Spanda matheme proof panel + the 3-coin cast ribbon + the named composition-handle for cosmic-1-2-3 — all over the existing `buildM3ProjectionSurface` contract, all through `SharedBridgeAdapter`, all profile-tick driven, all inline-readiness rendering, all privacy-class enforced. Six Wave-B kernel-bridge handoffs are named (WC-M3-SA-1..6) for the profile-field / method-family extensions Wave-C needs upstream.
