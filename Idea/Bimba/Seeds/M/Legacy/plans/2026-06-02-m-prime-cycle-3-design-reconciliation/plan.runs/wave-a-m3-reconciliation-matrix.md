# Wave-A M3 (Mahāmāyā) — 4-Way Design-Reconciliation Matrix

**Task ID:** wave-a-m3
**Domain:** M3 — Mahāmāyā (symbolic-transcription engine; 64 codons / 472 rotational states / 56+8 Tarot / 360° clock-field)
**Cycle:** M' Cycle 3 — Design Reconciliation
**Author:** Wave A subagent
**Date:** 2026-06-02

---

## Sources Actually Consulted

1. **Corpus 1 — UX intent.** `Idea/Pratibimba/System/Subsystems/Mahamaya/mahamaya-ux-full-m3-branch.md` (full read, 189 LOC).
2. **Corpus 2 — M' Seed spec.** `Idea/Bimba/Seeds/M/M3'/M3'-SPEC.md` (full read, 522 LOC) + selective companions: `m3-prime-symbolic-transcription-research.md` (TCT/Nine-of-Wands open question, role-theft framing), `m3-prime-clifford-consolidation.md` (Cl(4,2) ≅ M(8,ℝ) consolidation, validation status 7/11), `alpha_rasa_bridge_ql.md` (header only), `m3-prime-ql-binary-tabulation.md` (not opened — out of M3'-renderer scope this cycle).
3. **Corpus 3 — Body/S substrate.** `Body/S/S0/portal-core/src/codon_rotation_projection.rs` (full read, 211 LOC, materialised LUT), `Body/S/S0/portal-core/src/codon.rs` (full read, classify_codon 3-tier), `Body/S/S0/epi-lib/include/m3.h` (referenced by spec), `Body/S/S0/epi-lib/src/m3.c` (TCT entry at line 326 with rank=8/Nine, ROTATIONAL anchor — see grep), `Body/S/S0/epi-lib/test/m3/test_m3.c` (TCT state_count = 7 asserted at lines 525-527), plus grep over `Body/S/S0/portal-core/src/` for `resonance72`, `codonRotationProjection`, `mahamaya`, and `Body/S/S0/epi-cli/schemas/src/kernel-bridge.ts` for schema presence.
4. **Corpus 4 — Theia surface.** `Body/M/epi-theia/extensions/m3-mahamaya/` directory: `package.json`, `src/common/index.ts` (EXTENSION_ID, TRACK_08_EXPORTS, DECLARED_BLOCKERS), `src/common/codon-wheel.ts` (full `buildM3ProjectionSurface`, contract version `2026-06-01.07-T6`, M3_EXPECTED_ROTATIONAL_STATES === 472, throws when `codonRotationProjection` missing).
5. **Reference — Cycle 2 tranche.** `Idea/Bimba/Seeds/M/Legacy/plans/2026-06-02-m-prime-cycle-2-canonical/05-m3-mahamaya-extension.md` (T0–T3 ownership: clock-cosmos / 472-state landscape / multi-matrix inspector / Janus + world-clock).

---

## 4-Way Reconciliation Matrix

| # | Claim (UX doc) | Spec authority (M3'-SPEC) | Code / substrate evidence | Theia surface | Status |
|---|---|---|---|---|---|
| 1 | M3 parses any entity into codon/hexagram/tarot/Cl(4,2) signature (§1 axiom, §3) | §0/1, §8.1, §8.4 — codon = quaternion in Cl(4,2); 2-bit nucleotide logic; (X,Y,Z) → (pp,mm,mp,pm) | `portal-core/src/codon.rs::classify_codon` 3-tier (Perfect/Imperfect/NonPalindromicNonDual/Dual); `codon_rotation_projection.rs::codon_charge_quaternion` returns normalized [pp,mm,mp,pm] from 6/9/7/8 nucleotide values | `codon-wheel.ts::activeProjectionFacts` reads `mahamaya.codonId`, `codon`, `codonClass`, `nucleotideBits`; no UX entry-parser yet (parser input = M2-5 DET output) | **ALIGNED** (computation present; renderer surfaces it as facts) |
| 2 | 472 rotational states = 40 × 7 + 24 × 8 (§5 / clock-field cardinality) | §6 readiness: `40 × 7 + 24 × 8 = 472`; §7 modal-inversion landscape | `codon_rotation_projection.rs::CODON_ROTATION_SURFACE_COUNT = 472`, `codon_rotation_surface()` enumerates exactly that; classify_codon yields the 40/24 split | `codon-wheel.ts::M3_EXPECTED_ROTATIONAL_STATES = 472`; `validateM3LibrarySummary` checks `matchesM3Spec` | **ALIGNED** |
| 3 | (lens, mode) ↔ (codon, rotation) bidirectional projection (§5, §7 of UX) | §7 — the load-bearing forward/reverse map; canonical home `portal-core/src/codon_rotation_projection.rs` | `codon_rotation_from_lens_mode(lens, mode)` and `lens_mode_from_codon_rotation(codon, rotation)` both materialised; `dataset_lut_state: "materialized-kernel-lut"`; round-trip via `reverse_lens`/`reverse_mode` | `codon-wheel.ts::activeProjectionFacts` reads `projection.lens / mode / surface_index / codon_class / rotational_state_count`; surface throws if profile-field absent | **ALIGNED** (cycle-2 05.T1 closed substrate work; surface consumes it) |
| 4 | Each tarot card IS one codon; turning the card changes the mode (§1, §3, §5) | §7 narrative; §8.7 56+8 exact-cover; §9 Tarot compression panel | `epi-lib/src/m3.c::M3_TAROT_CODON_MAP[4][16]` (single-codon + dual-codon-court entries; 56 cards × {1 or 2} codons = 64); `_Static_assert(84+96+88+92==360)` per-suit integral invariant | `codon-wheel.ts::M3LibrarySummary` carries `nonDualCodonCount`/`dualCodonCount`/`scalarRefDetails`; library not yet seeded with the tarot detail dict in S2 (`source: 'pending-S2-M3-library-summary'` branch) | **SPEC-AHEAD** (C/Rust law is exact; S2 library handle exists; S2 graph nodes for the Tarot/I-Ching detail dict not yet populated — cycle-2 02 / S2 owns) |
| 5 | M3 default surface is "alive & tarot-like"; full multi-matrix is **summonable** (UX §0, §10; SPEC §7.5) | §7.5 explicit surface-philosophy invariant | n/a (renderer concern) | `package.json::description` states "first-slice priority: 64/472 wheel ... and trace overlay only"; widget present (`m3-mahamaya-widget.tsx`); inspectors (§9 dinucleotide-matrix, charge-quaternion, 24-spoke, Major-Arcana, DNA/RNA toggle, four render-views) NOT yet implemented as summonable inspectors | **DOC-AHEAD** (extension scaffolded; multi-matrix inspectors are cycle-2 05.T2 deliverable, not yet executed) |
| 6 | M3-0 reception law: M2 72-source → DET `floor(idx72 × 8/9)` → 64-address → codon-quaternion (§3, §6 UX; §8.12 SPEC) | §3 profile fields `resonance72`; §8.12 transduction chain rendered as provenance, no local interpretation | `portal-core/src/kernel.rs::resonance72: MathemeResonance72Projection` populated; `events.rs::resonance72_index` carried (0..72); `epi-cli/schemas/src/kernel-bridge.ts::resonance72` and `codonRotationProjection` present | `codon-wheel.ts::buildM30Provenance(payload, mahamaya)` reads provenance strip; observabilityEvent `m3.codon_projection` includes evidenceHandle | **ALIGNED** (modulo open uniqueness/injectivity decision below — see row 12) |
| 7 | 360° clock-field is a live data structure: nodes pinned by clock geometry; cross-clock relations as angular/hop edges; perturbs as tick turns (UX §4) | §8.6 — 384 = 64 × 6 line-change graph, 360 dynamic degree-nodes + 24 backbone-spokes; §3 `mahamayaAddress64`, `hexagramId`, `lineChangeOperator(Address)` | `epi-lib/src/m3_clock_lut.c::CLOCK_DEGREE_LUT[360]` (per spec §0 anchor — 749 LOC) populated; suit-integral compile-time assertions hold | `codon-wheel.ts::buildDepthViews` produces `flatClock`/`lensAnnulus`/`toroidalWorld`/`hopfIdentity` as **stub depth records** keyed off activeFacts; the angular/hop traversal graph rendering not yet present in widget | **DOC-AHEAD** (substrate ready; the clock-field-as-live-graph UX is not yet rendered — needs an angular/aspect-edge layer on top of the codon wheel) |
| 8 | 720° SU(2) double-cover never collapses to 360-only (UX §4, SPEC §1 commitments + §8.9) | §1 commitment 1; §3 `degree720`; §8.9 SU(2) double-covering wheel | `kernel.rs` materialises `degree720`, `su2Layer`, `phase`, `position6` per spec §3 | `codon-wheel.ts::activeProjectionFacts` reads `payload.degree720`; `M3WorldClockPayload` carries `degree720` and uses it to drive readiness divergence check (`authority_payload_missing` if S3 world_clock diverges from profile) | **ALIGNED** |
| 9 | Dual surface: M3' clock-wheel + M0' graph view over **one Neo4j substrate**; neither may fork (UX §2, §6; SPEC §1 commitment + §8.13) | §8.13 co-foliated double-torus + dual-rendering law; §6 readiness test "neither view forks Neo4j" | S2 graph nodes for the M3 library (the Tarot/I-Ching scalar dict) are not yet seeded — `wheelSummary` falls to `pending-S2-M3-library-summary` | `codon-wheel.ts::buildWheelSummary` returns `pending-S2-M3-library-summary` when no library is passed; the contract for "shared M0'+M3' substrate handle" is declared but the M0' graph view is owned by a separate extension (not cross-checked here) | **SPEC-AHEAD** (M0'/M3' shared-substrate contract is named; the S2 m3-library seeding is the missing kernel work — cycle-2 Track 02 owns; surface honest-pending state is correct) |
| 10 | Cl(4,2) at four scales: M1 ring · M3 codon · M4 personal · Kerykeion natal — one algebra | §8.4 explicit ("same Cl(4,2) as M1 SU(2) ring and M4-4-4-4 personal-quaternion"); consolidation companion §1.6 Cl(4,2) ≅ M(8,ℝ) | `portal-core/src/quaternion.rs` (one Quaternion type); `codon_rotation_projection.rs::codon_charge_quaternion` returns [f32;4]; M1 SU(2) ring and M4 personal types share the same `Quaternion` | Not surfaced as a single rendered identity at M3' — the charge-quaternion display (§9 SPEC) is an unbuilt inspector | **DOC-AHEAD** (one-algebra-at-four-scales is the cross-cutting invariant; the Wave-B kernel-bridge agent should verify "one Cl(4,2) type, four call-sites" — not M3-domain greenfield, just an audit) |
| 11 | **TCT / Nine of Wands**: dataset (`nodes-full-detail.json`) marks 8/full-rotational; spec/code/tests say 7/non-dual (UX §10.1; SPEC §10.1 open question; research doc lines 99-101, 171) | §10.1: code/spec/tests are runtime authority → dataset reconciliation, not code change | `epi-lib/test/m3/test_m3.c:525-527`: `TEST("TCT is non-dual initiated"...); TEST("TCT state count = 7", ...);` — runtime authority asserts 7-state. `m3.c::M3_TAROT_CODON_MAP[1][8] = { Nine, TCT, NONE }` (single-codon Nine of Wands). `codon.rs::classify_codon(0x35 i.e. T_C_T = imperfect-palindromic, n1=n3, n1≠n2) → ImperfectPalindromic` (non-dual; 7 rotational states) | n/a renderer | **CONTRADICTION** → decision-register: confirm dataset reconciliation (dataset must move to 7), not a code-value change. Cycle 3 owns the formal decision entry. |
| 12 | 72→64 epogdoon uniqueness/injectivity scope (UX §3 closing; SPEC §10.1) | §10.1 open: "the profile contract must define the uniqueness scope before UI or tests assume global injectivity"; §8.12 acknowledges collisions/folds | `kernel.rs::resonance72: MathemeResonance72Projection` exists with `lens_anchor_index` (0..72); no contract field declaring uniqueness/injectivity scope — the `floor(idx72 * 8 / 9)` reduction is integer-deterministic but the mapping is many-to-one (72 → 64 by definition: 9 of the 72 fold), so injectivity is locally non-trivial | `codon-wheel.ts` renders provenance strip but does not encode a fold/gap UI distinct from `evolutionaryGap` | **CONTRADICTION** → decision-register: define uniqueness scope (per-cell? per-cycle? gap-marked?) before any UI claims "the codon for this 72-index". Cycle 3 owns the decision; M3' rendering is correct to render honest gap, but the spec must close the scope. |
| 13 | "17th lens" / "16+1" wording — Lens Annulus depth-view (UX §4.1; SPEC §8.10) | §8.10 open: "do not silently add a new lens. Treat the current law as 16 lens positions plus a Level-0 meta-position until companion spec normalises" | `codon_rotation_projection.rs::LENS_COUNT = 12` (not 16, not 17 — the **rotational lens** count is 12; this is the M1' chromatic lens, not the M3' "16+1 Mahāmāyā lens-stack" annulus from §8.10). Two different "lens" namespaces are in play. | No "Lens Annulus" widget yet | **CONTRADICTION** → decision-register: name-space the "lens" — (a) the M1' 12 chromatic lens-anchors (LENS_COUNT=12 in code), (b) the 16+1 Mahāmāyā lens-stack from §8.10 / Lens Annulus depth view. The two MUST NOT be conflated in renderer code or tests. Cycle 3 owns the decision. |
| 14 | M3' is the **Matter pole** of the 1-2-3 integrated engine; consumed by integrated-1-2-3 plugin (UX §6 / SPEC §8.14) | §8.14 — m3-mahamaya standalone + integrated 1-2-3 plugin share kernel-bridge | Kernel-bridge is the only conduit per §2/§8.14 | `index.ts::TRACK_08_EXPORTS = ["M3CodonChip","M3WheelMiniView"]` + `TRACK_08_CONTRIBUTION` declared; the integrated-1-2-3 plugin extension exists at `Body/M/epi-theia/extensions/plugin-integrated-1-2-3/` (separate scope, not inspected here) | **ALIGNED** at the contract surface; integrated-plugin consumption verification belongs to Wave-B theia-shell agent. |
| 15 | M3' renders only kernel-trace overlay; reward/training authority outside renderer (UX §8; SPEC §8.14, §10.1) | §8.14 — "reward computation, training, governance, and runtime promotion remain outside the renderer" | n/a renderer | `codon-wheel.ts` event payload sets `rewardTrainingAuthority: 'outside-renderer'` and emits `m3.kernel_trace_view` with `traceMode: 'read-only'`, `promotionAuthority: 'S5/M5-4'` | **ALIGNED** |
| 16 | Privacy boundary: public-current + scalar oracle refs only (UX §8; SPEC §4) | §4 — no raw bioquaternion, journal, or unreconciled Graphiti; resolve scalar refs only | n/a renderer | `index.ts::PRIVACY_CLASS = 'public_current_with_scalar_oracle_refs_only'`; `codon-wheel.ts::resolveM3ScalarOracleRef` rejects calls carrying `protectedArtifactBody` | **ALIGNED** |
| 17 | M3-5 Pauli-Jung World-Clock view (Várlaki/Rudas `1 + 2×32 + 2×36 = 137`) as depth mode, not new substrate (UX §4.1; SPEC §8.9.1, §8.13) | §8.13 four depth modes over same profile/graph state; §8.9.1 source warrant | `degree720`, `su2Layer`, `position6` from `kernel.rs` carry the SU(2) skeleton | `codon-wheel.ts::buildDepthViews` exposes `toroidalWorld` and `hopfIdentity` as **labelled depth-view records** but with no rendering payload beyond the active facts | **DOC-AHEAD** (depth-mode rendering is a cycle-3 surface-build item under cycle-2 05.T0/T2 — substrate ready, view not yet executed) |
| 18 | M3 serves Nara (M4-2 Oracle, M4-0 Identity 64-code bridge, M4-3 Janus transformation) (UX §7; SPEC §4) | §4 oracle-artifact scalar-resolution boundary; §8.11 Janus | `portal-core/src/oracle_lut.rs`, `transcription.rs`, `nara_journal.rs` present | `codon-wheel.ts::resolveM3ScalarOracleRef` is the boundary; full Nara consumption is M4's concern | **ALIGNED** (M3 side correct; Wave-A M4 agent verifies the consuming side) |

---

## Anomalies

### CONTRADICTIONS (route to decision register — cycle-3 tranche 13.x)

- **C-1 (row 11): TCT / Nine of Wands cardinality.** Dataset `nodes-full-detail.json` carries 8/full-rotational; runtime (`test_m3.c:525-527`, `classify_codon` algorithm) carries 7/non-dual. Spec §10.1 already names code/spec/tests as authority. **Decision needed:** confirm dataset reconciliation (move dataset to 7) as the resolution, and add an import-time validator that rejects the 8 value. Cycle 3 should *register* this decision; the reconciliation work itself is a Track-02 (S2 bimba-map population) item.
- **C-2 (row 12): 72 → 64 uniqueness scope.** `floor(idx72 × 8/9)` is many-to-one by construction (9 of 72 indices fold). Spec §10.1 admits the contract has no declared uniqueness scope; UX §3 says "frequency→matter transduction" without naming the fold. **Decision needed:** declare the fold/gap profile-field contract (per-cell vs per-cycle uniqueness; explicit gap-marker semantics) before any UI assumes injectivity. Honest gap-rendering at M3' is correct; the *contract* must close.
- **C-3 (row 13): "17th lens" / "16+1" / namespace collision.** Code's `LENS_COUNT = 12` (`codon_rotation_projection.rs:5`) is the M1' chromatic-lens count, NOT the §8.10 "16+1 Mahāmāyā lens-stack". UX §4.1 names the **Lens Annulus** depth mode without disambiguating. **Decision needed:** name-space split — `M1_LENS` (12 chromatic anchors) vs `M3_LENS_STACK` (16+1 Mahāmāyā lens-stack from §8.10) — and forbid silent introduction of a "17th lens" until the companion specs converge. Cycle 3 registers; renderer naming follows the decision.

### CODE-PENDING (substrate-gap; do NOT rebuild)

- **CP-1 (row 4 & row 9): S2 M3-library seeding pending.** `wheelSummary` renders `source: 'pending-S2-M3-library-summary'` because S2 bimba-map nodes for the Tarot/I-Ching/codon scalar-detail library are not yet populated. Owner: cycle-2 Track 02 (S2 bimba-map population). M3' renders correct honest-pending; nothing for M3' to fix.
- **CP-2 (row 7): clock-field-as-live-data-structure rendering.** The angular/aspect-edge graph layer the UX describes (cross-clock relations as angular / hop / traversal edges, perturbing as the tick turns) is not yet rendered atop the codon wheel — only the wheel cells are. The 384-line-change LUT and 24-spoke lattice substrate is materialised; the *graph-overlay rendering* is a cycle-3 closing-tranche item.

### ORPHANS

- **O-1 (row 5 & row 17): summonable multi-matrix inspectors + four depth-view modes** are named in M3'-SPEC §9 and cycle-2 05.T2 but have no owner declared inside cycle 2 beyond a tranche line — the actual *who-builds-what-when* sequencing for (a) dinucleotide-matrix inspector, (b) charge-quaternion display with 4X-invariant audit, (c) 24-spoke lattice w/ 12-deep ring-buffer, (d) Major-Arcana / chromosome panel, (e) DNA/RNA phase toggle, (f) Flat Clock / Lens Annulus / Toroidal / Hopf-Identity depth modes — is not specified. Not an architecture orphan (the spec owns it) but an *execution-ownership* orphan that cycle-3 tranche 4.2 closes.

---

## Proposed Cycle-3 Tranches

Tranche IDs follow the cycle-3 numbering (cycle-2 used 02–07 for M0–M5; cycle 3 closes tranches over the same axes — 4.x marks M3 closure).

### 4.1 — Codon-rotation projection field contract closure (CODE-PENDING-CLOSURE)

- **Classification:** code-pending-closure
- **Deliverable:** Cycle 2 substrate inheritance: `Body/S/S0/portal-core/src/codon_rotation_projection.rs` is landed and `dataset_lut_state: materialized-kernel-lut`. **No first-build.** Cycle 3 closure: (a) audit the round-trip `forward_surface_index ↔ reverse_lens_mode_index` proportional-mapping (current implementation is a proportional projection, not a semantically-grounded modal-inversion table — spec §7 names the canonical semantic map; an audit should confirm whether the current LUT is the *canonical kernel LUT* or a place-holder proportional one); (b) extend the test in `Body/S/S0/portal-core/tests/track_01_t2_profile_contract.rs` to assert the `(lens,mode) ↔ (codon,rotation)` round-trip on representative cells from §7.
- **Verification:** `cargo check -p portal-core` and `cargo test -p portal-core codon_rotation` from the substrate root; grep `dataset_lut_state` for `materialized-kernel-lut` literal presence.

### 4.2 — M3' summonable inspector + four-depth-view execution sequencing (DOC-AHEAD-LANDING / no-orphan-fill)

- **Classification:** doc-ahead-landing (combined with no-orphan-fill for O-1)
- **Deliverable:** Cycle 2 substrate inheritance: `Body/M/epi-theia/extensions/m3-mahamaya/` scaffold + `M3_CODON_WHEEL_CONTRACT_VERSION = '2026-06-01.07-T6'` extension contract is landed; first-slice "64/472 wheel + provenance + trace overlay" is the package.json-declared current state. **Cycle 3 builds-onto (not greenfield) the extension** by adding the six summonable inspectors named in M3'-SPEC §9 — dinucleotide-matrix inspector, charge-quaternion display (with `pp+mm+mp+pm = 4X` runtime audit), 24-spoke lattice + 12-deep ring-buffer, Major-Arcana / chromosome panel, DNA/RNA phase toggle — and the four depth-view modes (Flat Clock Debug, Lens Annulus, Toroidal/World Clock, Hopf Identity), each as a summonable sub-widget over the existing `buildM3ProjectionSurface` contract. Per-suit integral panel (`84+96+88+92 = 360` rendered from the live profile) is part of the charge-quaternion inspector. No new spec, no new C tables, no new clock — only renderer + agent-mediation glue.
- **Verification:** `test -d Body/M/epi-theia/extensions/m3-mahamaya && grep -r 'dinucleotide\|chargeQuaternion\|twentyFourSpoke\|majorArcana\|dnaRnaPhase' Body/M/epi-theia/extensions/m3-mahamaya/src/`; assert the `MathemeHarmonicProfile.codonRotationProjection` profile-field is the **only** projection input (no renderer-local mapping tables — grep should find zero hardcoded codon/hexagram/tarot maps inside the extension's `src/`).

### 4.3 — Clock-field-as-live-data-structure (angular/hop edge overlay) (DOC-AHEAD-LANDING)

- **Classification:** doc-ahead-landing
- **Deliverable:** Cycle 2 substrate inheritance: `epi-lib/src/m3_clock_lut.c::CLOCK_DEGREE_LUT[360]` + the 384 line-change graph + 24-spoke 15° lattice + 12-deep ring-buffer are landed substrate. Cycle 3 lands the UX §4 **angular/hop/traversal edge overlay**: cross-clock relations rendered as angular relations (aspect / opposition / trine / square) and as hops along the 384 line-change graph; the codon-wheel "perturbs as the tick turns". Owner: the same `m3-mahamaya` extension; **no new C / Rust kernel work** — substrate is sufficient.
- **Verification:** `grep -r 'aspectEdge\|hopGraph\|lineChangeEdge\|spokeLattice' Body/M/epi-theia/extensions/m3-mahamaya/src/`; widget render-test confirms degree-tick advance redraws node positions.

### 4.4 — TCT / Nine-of-Wands dataset reconciliation decision-register entry (CONTRADICTION-DECISION)

- **Classification:** contradiction-decision
- **Deliverable:** Cycle 2 substrate inheritance: runtime law (`classify_codon`, `test_m3.c`) is authoritative — 7-state non-dual. Cycle 3 writes the decision-register entry confirming dataset reconciliation as the resolution (NOT code change), names the import-time validator to add in S2 bimba-map populator that rejects the value `8` for TCT, and routes user final-validation per the ur-process.
- **Verification:** `grep -rn "TCT" Idea/Bimba/Seeds/M/Legacy/plans/2026-06-02-m-prime-cycle-3-design-reconciliation/13-*.md`; substrate-side a sanity-check `cargo test -p portal-core classify` confirms `classify_codon(0x35)` returns `ImperfectPalindromic`.

### 4.5 — 72→64 uniqueness/injectivity contract decision-register entry (CONTRADICTION-DECISION)

- **Classification:** contradiction-decision
- **Deliverable:** Decision-register entry naming the uniqueness scope of `floor(index72 × 8/9)` — per-cell vs per-cycle, the explicit gap-marker semantics, and the profile-field shape that carries the fold/gap state (extension of `evolutionaryGap`, or a new `det72to64Fold` field). User final-validation required; until then, M3' continues to render `pending-dataset-lut`/`provisional-gap` honestly.
- **Verification:** decision-register file presence at tranche 13; absence of `assert injectivity` patterns in `portal-core/tests/`.

### 4.6 — "17th lens" / "16+1" namespace decision-register entry (CONTRADICTION-DECISION)

- **Classification:** contradiction-decision
- **Deliverable:** Decision-register entry distinguishing (a) **M1' 12 chromatic lens-anchors** — the `LENS_COUNT = 12` in `codon_rotation_projection.rs` — from (b) the **16+1 Mahāmāyā lens-stack** in M3'-SPEC §8.10 (Lens Annulus depth view), and forbidding silent merger. The renderer naming for the Lens Annulus inspector under tranche 4.2 must follow the decision.
- **Verification:** decision file + grep `LENS_COUNT` returns the M1' chromatic anchors only; no `M3_LENS_STACK_COUNT = 17` introduction in code.

### 4.7 — M3 × Cl(4,2) cross-cutting one-algebra audit (ALIGNED-ONLY-NOTE, with Wave-B handoff)

- **Classification:** aligned-only-note (handoff item for Wave-B kernel-bridge agent)
- **Deliverable:** Cycle 2 substrate inheritance: `portal-core/src/quaternion.rs` is the one quaternion type. Cycle 3 records as a Wave-B audit item: confirm "one Cl(4,2) algebra in code, four call-sites" (M1 ring, M3 codon, M4 personal, Kerykeion natal) — this is NOT an M3-domain rebuild; it is an audit-handoff to the kernel-bridge / profile-contract agent.
- **Verification:** `grep -rn "Quaternion\|Cl(4,2)\|cl_4_2\|cl42" Body/S/S0/portal-core/src/ | wc -l` returns one canonical type definition.

---

## Cross-cutting notes for the controller

- **Anti-greenfield posture is correct for M3.** Every M3 tranche above is `consume as-is` / `audit / verify` / `extend` over the landed `portal-core::codon_rotation_projection`, `epi-lib::m3.{c,h}`, and `m3-mahamaya` Theia extension. First-build ownership in tranche 4.2 is for a **product surface** (summonable inspectors + depth views) over a landed contract — not greenfield substrate.
- **All three CONTRADICTIONS (TCT, 72→64 uniqueness, 17-lens namespace) belong in the controller's tranche 13 decision-register**, not as M3-domain "fixes". M3' rendering is *already* honest-pending where the contract is unclosed.
- **No M3-domain ORPHANS at the architecture level** — the spec owns everything. The only "orphan" is *execution-ownership for the inspectors and depth modes*, which 4.2 absorbs.
- **One Wave-B handoff: tranche 4.7 audit** ("one Cl(4,2), four scales") belongs to the cross-cutting kernel-bridge agent — flagging it here is sufficient.
