# Wave C — `ide-shell-m0-m5` Chrome Deep UX Matrix

**Task id:** `wave-c-ide-shell-chrome`
**Domain:** Per-widget UX of the eight landed widgets + `bridge-gate` inside `Body/M/epi-theia/extensions/ide-shell-m0-m5/`, plus the left-sidebar activity-bar system (per 15.3), plus the Smart-Connections code-pending gate (per 11.4), plus the Backend Studio LSP contribution (per 15-foundation `ide-deep` row), plus the ide-shell vs OmniPanel ownership split for the evidence / review surfaces (per 15.2).
**Anti-greenfield posture:** the eight widgets + `bridge-gate.tsx` + `frontend-module.ts` are landed substrate. Every tranche either `audits-and-extends` an existing widget, `repurposes` an existing scaffold field, `first-builds against an unowned M' product surface` (activity-bar mode contributions, Backend Studio LSP slot, Smart-Connections code-pending stub), or `cross-links` to OmniPanel / Mn surfaces. NO competing widget shell; NO direct S0/S2/S3/S5 imports; SharedBridgeAdapter is the only network surface; the kernel-bridge readiness gate is the only readiness primitive.

## Source list (corpora actually consulted)

- **Corpus 1 (UI foundation contract)** — `Idea/Bimba/Seeds/M/Legacy/plans/2026-06-02-m-prime-cycle-3-design-reconciliation/15-ui-design-foundations.md` (nine foundation principles; §"`ide-deep`" left-sidebar contract; §"Left-sidebar-system"; tranches 15.2 OmniPanel reframe, 15.3 activity-bar system, 15.6 profile-tick + inline readiness, 15.7 BimbaPratibimbaUiState, 15.10 status-bar discipline, 15.11 dispatch-genealogy primitive).
- **Corpus 2 (Theia shell hosting closure)** — `11-theia-shell-surface-hosting.md` (TS-07 the eight ide-shell widgets are M0/M5 chrome; TS-08 ACR is M5-4 with human-required IOD-17 parity; TS-09 kernel-bridge readiness rendered inline; tranche 11.2 cross-layout intent T5 promotion, 11.3 daily-layer ORPHAN trace, 11.4 smart-connections-sidebar code-pending marker, 11.5 forbidden-direct-import lint, 11.6 acceptance-harness state-identity, 11.9 surface-extension-contract ledger).
- **Corpus 3 (Wave-B shell matrix)** — `plan.runs/wave-b-theia-shell-matrix.md` (TS-07 ide-shell widgets aligned; TS-08 ACR human gate; TS-09 bridge-gate consumes readiness; TS-18 M0' CRUD vs governed-route routes through ide-shell hosting BOTH possibilities; TS-23 smart-connections-sidebar CODE-PENDING; DR-TS-1 0/1/4+2 separation already resolved).
- **Corpus 4 (Stage 1 cross-links — M0 and M5 chrome flow into ide-shell)** —
  - `21-m0-anuttara-frontend-deep.md` SC-2 (canon writes via M5 atelier, `mutatesGraphCanon: false`); SC-8 (cross-layout intent routes into ide-shell widgets, NEVER duplicated inside m0-anuttara); tranches 21.3 / 21.5 / 21.12 (cross-link `coordinate-tree-widget`, `canon-studio-widget`, `logos-atelier-widget` via cross-layout-intent-dispatcher).
  - `26-m5-epii-frontend-deep.md` Role 2 (five chrome widgets in ide-shell — Logos Atelier 26.3 scent-following retrofit, Evidence 26.4 `MediatedRunEvidencePacket`, Review 26.5 IOD-17 parity, Autoresearch 26.6 Möbius-pass ribbon, ACR 26.7 T8 contents + DR-M5-1 roster collapse + Pi-monitor reframe + 26.9 Aletheia subagent trace + 26.14 `PiAxiomTranslationInspector`).
- **Corpus 5 (Extension code — the 8 widgets + bridge-gate + frontend-module)** —
  - `Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/bimba-graph-viewer-widget.tsx` (~150 LOC; `openCoordinate(coordinate)` → `s2.graph.node`; renders `<dl>` of `coordinate / namespace / label / pointerAnchor / sourceAnchor / specAnchor / codeAnchor / testAnchor / gdsReadiness / neighborCount / privacyClass / privacyDropped`; `BimbaSubgraphPayload` shape — `neighbors[]` typed but **not rendered**).
  - `Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/canon-studio-widget.tsx` (~210 LOC; `<textarea>` editor; `decorateCoordinates(this.content)` decoration scanner; save through vault-bridge command `vault-bridge.s1prime.vault.write_file`; rejected with `"no vault-bridge registered"` until T4.5 lands; no autocomplete, no Smart Connections, no inline QL decoration rendering).
  - `Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/agentic-control-room-widget.tsx` (~230 LOC; T4 shell: VAK fields + capability-tree from `capability-matrix.json` + `t8-host` empty placeholder; `applyIntent` consumes CrossLayoutIntent payload; `selectRoute(route, actor)` typed but no run-tree, no tool-stream, no abort/retry/continue, no evidence-deposit form, no review-decision controls).
  - `Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/coordinate-tree-widget.tsx` (~130 LOC; recursive `<ul>` via `renderTree(node, depth)`; reads `s2'.coordinate.resolve`; no family colouring, no active-coordinate highlight, no CRUD-vs-governed-route surface, no expand/collapse-per-family controls).
  - `Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/logos-atelier-widget.tsx` (~155 LOC; six L0-L5 generic stages with `notes` + `provenanceHandles[]`; `attachProvenance(stageId, handle)` typed; no scent-following stages, no Aletheia tools, no etymology namespace, no Möbius write-back to canon).
  - `Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/evidence-pane-widget.tsx` (~205 LOC; `EvidenceRecord` shape — generic id/title/coordinate/privacyClass/artifactUri/sourceAnchor/graphAnchor/testAnchor/reviewId/bridgeReadinessHandle/sessionKey/dayNowContext/profileGeneration; `addRecord`/`setRecords` with privacy gate; no mediator badge, no dispatch-trace mini-graph, no `MediatedRunEvidencePacket`, no tool-stream cross-link).
  - `Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/review-pane-widget.tsx` (~175 LOC; `ReviewItem` shape — id/title/status/humanRequired/privacyClass/proposer/coordinate/summary; `refreshInbox()` reads `s5'.review.inbox`; human-required banner present; no IOD-17 three-way parity readout, no dispatch-genealogy click-through, no evidence-packet click-through).
  - `Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/autoresearch-pane-widget.tsx` (~165 LOC; `AutoresearchCandidate` shape — id/title/score/privacyClass/source/coordinate/anchors[]; `refreshHistory()` reads `s5'.improve.history`; no Möbius-pass ribbon, no autoresearch-as-concept frame, no per-capacity filter, no recompose-pass count, no requires_human/forbidden_authority disclosure).
  - `Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/bridge-gate.tsx` (~85 LOC; `IdeShellBridgeGate` React component; subscribes `onConnectionChange` + `onProfile`; renders pending shell until `connected && profileGeneration !== null`; readiness is a single binary `ready/pending` flag, NOT the nine-id readiness taxonomy from `07-t0` contract — does NOT surface `bridge_unavailable / profile_missing_field / s2_graph_blocked / s3_subscription_blocked / s5_review_blocked / authority_payload_missing / privacy_blocked / degraded_but_readable / ready_public_current`; does NOT render per-binding border-colour / pending badge / blocked overlay per 15.6 inline-provenance principle).
  - `Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/frontend-module.ts` (~410 LOC; eight `AbstractViewContribution` classes; `defaultWidgetOptions.area`: `BimbaGraphViewer=main`, `CanonStudio=main`, `AgenticControlRoom=main`, `EvidencePane=right`, `CoordinateTree=left`, `LogosAtelier=main`, `ReviewPane=right`, `AutoresearchPane=right`; `IdeShellM0M5Config` rebindable for tests; `registerIntentTarget` for `BIMBA_GRAPH / CANON_STUDIO / AGENTIC_CONTROL_ROOM / EVIDENCE_PANEL` — `COORDINATE_TREE / LOGOS_ATELIER / REVIEW_PANE / AUTORESEARCH_PANE` have NO intent target registered).
  - `Body/M/epi-theia/extensions/ide-shell-m0-m5/src/common/contract.ts` (eight widget IDs; eight forbidden-privacy classes + four allowed; `isPrivacySafe()` gate; four intent-target ids — only half of the eight widgets have one).
- **Corpus 6 (Layout substrate)** — `Body/M/epi-theia/extensions/pratibimba-layouts/src/common/layout-types.ts` (`DAILY_0_1_DESCRIPTOR` includes none of the ide-shell widget ids; `IDE_DEEP_DESCRIPTOR` includes 7 of 8 ide-shell widgets — `autoresearch-pane` is NOT in `expectedWidgets`; `pratibimba.smart-connections-sidebar` is declared but extension does not exist).
- **Corpus 7 (Contract preflight)** — `Body/M/epi-theia/extensions/contracts/07-t0-extension-contract-preflight.{md,json}` (KernelBridgeAPI capabilities `readCurrentProfile / readPointerAnchor / readReadiness / subscribeObservability / invokeGatewayRpc / depositKernelObservation / requestReviewEvidence`; subscriptions `profile / connection_status / observability`; `sharedBridgeAdapter.forbiddenDirectImports`; readiness taxonomy nine ids).
- **Corpus 8 (Migration sources & gated forwards)** — `Body/M/epi-theia/extensions/MIGRATION-SOURCES.md` (smart-connections-sidebar / vault-bridge / canon-studio / shared-services are forward-not-yet-scaffolded; smart-connections-sidebar is T4.5 gated on Track 03 T6.5).
- **Cross-link corpora (consumed for boundary law, NOT re-authored)** —
  - 11-theia-shell-surface-hosting.md (TS-07 / 11.2 / 11.4 / 11.6 / 11.9 / 11.11 highlight-mark cross-pattern);
  - 15-ui-design-foundations.md (binding nine principles; 15.2 OmniPanel reframe rules; 15.3 activity-bar; 15.6 inline readiness; 15.11 dispatch-genealogy primitive);
  - 21-m0-anuttara-frontend-deep.md (Tranches 21.3/21.5/21.12 cross-link into ide-shell);
  - 26-m5-epii-frontend-deep.md (Tranches 26.3 → 26.7, 26.9, 26.10 `MediatedRunEvidencePacket`, 26.14 `PiAxiomTranslationInspector`).

---

## Master 4-way matrix

| # | Claim (UX / foundation) | Spec authority | Code/substrate evidence | Theia surface | Status |
|---|---|---|---|---|---|
| WC-IS-01 | The eight ide-shell widgets + `bridge-gate` are M0/M5 chrome — one unified chrome system, NOT eight independent panels (TS-07; 15-foundation §"`ide-deep`" + §"Left-sidebar-system"). | TS-07; 26-m5-epii Role 2 (five widgets are M5-flavoured chrome); 21-m0-anuttara SC-2 (M0 governance flows through this chrome). | Eight widget files + `bridge-gate.tsx` + `frontend-module.ts` all colocated under `Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/`; share `IdeShellBridgeGate` + `isPrivacySafe()` gate. | `frontend-module.ts` registers eight `AbstractViewContribution` classes; each calls `IdeShellBridgeGate` wrapper. **Unified chrome contract is implicit — not declared as a contract anywhere.** | **DOC-AHEAD (contract)** — closing tranche 28.1 lands a `Body/M/epi-theia/extensions/ide-shell-m0-m5/CHROME-CONTRACT.md` enumerating: eight widgets + bridge-gate as one chrome; activity-bar vs main vs right-sidebar slot assignment; SharedBridgeAdapter as only network; the M0/M5 governance flow (read on left, write through M5 atelier → routed via canon-studio). |
| WC-IS-02 | Activity-bar discipline (15-foundation principle 7) — left-sidebar modes are activity-bar-switched, NOT stacked. `daily-0-1` modes: Coordinate Tree · Bimba Graph Viewer · Canon Studio. `ide-deep` modes: same + Backend Studio + Smart Connections (15.3). | 15-foundation principle 7; 15.3 tranche; 15-foundation `ide-deep` row. | Three widgets are candidates for activity-bar slots: `coordinate-tree-widget` (already `defaultWidgetOptions.area: 'left'`), `bimba-graph-viewer-widget` (currently `area: 'main'`), `canon-studio-widget` (currently `area: 'main'`). **No activity-bar mode registration** (no `ViewContainer` / `TabBarToolbar` / activity-bar contribution naming the three modes); Backend Studio + Smart Connections do not exist. | Widgets contribute to Theia areas independently; no activity-bar mode grouping; no per-layout filter. | **SPEC-AHEAD + CODE-PENDING (cross-link 15.3 + 11.4)** — closing tranche 28.2 lands a `LeftSidebarActivityBarContribution` registering the three `daily-0-1` modes (Coordinate Tree · Bimba Graph Viewer · Canon Studio) + the two `ide-deep`-only modes (Backend Studio code-pending, Smart Connections code-pending) with per-layout filtering against `epi-logos.layout.active` preference. |
| WC-IS-03 | Bimba Graph Viewer is the single substrate / three renderings (15-foundation §"Left-sidebar-system" — solar-anchor for `daily-0-1`, full 4+2 lattice for `ide-deep`). | 15-foundation `ide-deep` row + §"Left-sidebar-system"; DR-IG-1 two-relation-families discriminator (cross-link 09-integrated-bimba); 21-m0 21.3 (M0' QL-structure layer deep-links into Bimba Graph). | Widget consumes `s2.graph.node` → `BimbaSubgraphPayload`; renders `<dl>` of node attributes + `neighborCount` only. **No visual graph rendering**, no node click → coordinate context, no edge filter by relation family, no `daily-0-1` solar-anchor vs `ide-deep` full-lattice mode. | Widget renders as flat `<dl>`. The "visual graph viewer" foundation claim is **substrate-aligned but surface-flat** — no canvas, no SVG, no D3/ForceGraph, no rendering distinction by layout. | **DOC-AHEAD + CODE-PENDING (surface)** — closing tranche 28.3 audits + extends `bimba-graph-viewer-widget.tsx` with two named renderings (solar-anchor compact-card for `daily-0-1`; full 4+2 lattice ForceGraph or SVG for `ide-deep`); node click → publishes `selectedCoordinate` via SharedBridgeAdapter (consumed by all per-Mn widgets per 21-m0 SC-5 / 15-foundation principle 1); edge filter by `c_1_relation_family` two families (per DR-IG-1). |
| WC-IS-04 | Canon Studio is markdown editor with **QL/bimba decoration** + **Smart Connections autocomplete via `s1'.semantic.*`** (15-foundation §"Left-sidebar-system" `Canon Studio` row). | 15-foundation §"Left-sidebar-system"; 21-m0 SC-2 (governance via M5 atelier — Canon Studio is the routed-write surface). | Widget has `decorateCoordinates(this.content)` scanner producing list of decoration matches; rendered as `<ul>` BELOW the textarea — **not inline as Monaco decorations**. Autocomplete is not wired (Smart Connections sidebar does not exist; `s1'.semantic.*` not invoked from Canon Studio). | `<textarea>` editor; decorations rendered as a separate list, not as inline span highlights. No Monaco. No `s1'.semantic.*` autocomplete. No PASU.md edit flow. No QL frontmatter linter. | **CODE-PENDING (cross-link 15.3 + 11.4 + 11.10)** — closing tranche 28.4 audits + extends `canon-studio-widget.tsx` to (a) move to Monaco-based editor with inline QL/bimba decoration (or retain textarea but render inline `<mark>` spans), (b) wire `s1'.semantic.*` autocomplete via SharedBridgeAdapter when the smart-connections-sidebar code-pending gate releases (Track 03 T6.5), (c) cross-link M5 atelier routed-write per 21-m0 SC-2 + 26-m5 governance flow, (d) PASU.md edit flow (cross-link 25-m4 PASU.md frontmatter contract). |
| WC-IS-05 | Agentic Control Room substrate becomes OmniPanel content model per 15.2 + DR-M5-1 + 12.14; standalone ACR widget becomes thin Pi-runtime-monitor / capability-roster surface (26.7 reframe). | 15.2 (OmniPanel hosts Pi Chat / Sessions / Dispatch Trace / Tool Stream / Evidence / Review / Gateway / Diagnostics); DR-M5-1 + 12.14 (ACR substrate → OmniPanel runtime); 26.7 (T8 contents land INSIDE ide-shell-m0-m5 ACR widget body + Pi-monitor reframe); 27-omnipanel-shell (downstream tranche owns the OmniPanel deep work). | Widget has T4 shell + empty `t8-host` section + capability-tree + VAK fields. T8 contents (RunTree, ToolStream, AbortRetryContinueControls, EvidenceDepositForm, ReviewDecisionControls) are SPEC'D in 26.7 to land here (`Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/acr/` sub-folder). | Widget is named "Agentic Control Room"; 26.7 reframes label to "Pi Runtime Monitor (ACR)". Capability tree + VAK fields stay; T8 contents land. | **AUDIT-EXTEND (cross-link 26.7 + 15.2 + DR-WC-IS-1 below)** — closing tranche 28.5 audits + extends `agentic-control-room-widget.tsx` per 26.7 deliverable (a) T8 contents in `acr/` sub-folder + (c) Pi Runtime Monitor reframe; the **ide-shell ACR widget vs OmniPanel Dispatch Trace tab split** is the load-bearing split — see DR-WC-IS-1. |
| WC-IS-06 | Coordinate Tree is the coordinate-rooted navigation backbone (15-foundation principle 1). Tree expand/collapse per family (P/S/T/M/L/C). Active-coordinate highlight. CRUD-vs-governed-route surface per DR-M0-1 (TS-18). | 15-foundation principle 1; TS-18 (CRUD vs governed-route both possibilities hosted in ide-shell); 21-m0 21.3 (M0' QL-structure layer deep-links into Coordinate Tree). | Widget reads `s2'.coordinate.resolve` → recursive `CoordinateNode` tree; renders `<ul>` with `data-depth` + `data-test`. **No family-aware expand/collapse**, no active-coordinate highlight, no CRUD affordance, no governed-route deep-link to canon-studio, no privacy-class colouring. | Tree renders as flat recursive `<ul>`. Active-coordinate global state is not consumed (per 21-m0 SC-5 the active coordinate is global — but Coordinate Tree does not highlight it). | **AUDIT-EXTEND (cross-link 15-foundation principle 1 + TS-18 + 21-m0 21.3)** — closing tranche 28.6 extends `coordinate-tree-widget.tsx` with (a) per-family colouring (P/S/T/M/L/C/Empty/Pratibimba — six C-family-canonical colours + two namespace colours), (b) active-coordinate highlight via SharedBridgeAdapter.onCoordinateContext (writes back on click), (c) CRUD-vs-governed-route mode toggle (Reading default; Authoring routes via cross-layout-intent-dispatcher to canon-studio per DR-M0-1, NEVER mutates locally per `mutatesGraphCanon: false`). |
| WC-IS-07 | Logos Atelier carries axiom-translation surface (DR-B-2) + canonical-write governance for M0' (DR-M0-1) + scent-following stages (26.3). | 26.3 (scent-following retrofit replaces L0-L5 generic stages with root / cognate / drift / psychoid / pros-hen / Möbius write-back); 26.14 (axiom-translation lands as sub-pane inside ACR per DR-B-2; Atelier cross-links to it from Möbius write-back); 21-m0 SC-2 (Atelier is the M5-routed-write canonical surface). | Widget has six generic L0-L5 stages with `notes` + `provenanceHandles[]`. Aletheia crystallisation tools (`aletheia_gnosis_query / aletheia_thought_route / aletheia_crystallise`) not wired. Etymology namespace not enforced. | Generic L0-L5 stage tabs; provenance handle list; no scent-following grammar; no Möbius write-back to canon-studio. | **AUDIT-EXTEND (cross-link 26.3 + 21-m0 SC-2)** — closing tranche 28.7 audits + extends `logos-atelier-widget.tsx` per 26.3 deliverable (six scent-following stages, Aletheia tools via `aletheia_*` gateway methods, etymology namespace, Möbius write-back stage routes to canon-studio via cross-layout-intent-dispatcher). |
| WC-IS-08 | Evidence pane is `MediatedRunEvidencePacket` renderer (26.4 + 26.10 schema landing); ide-shell hosts the pane; OmniPanel Evidence tab (15.2) is the same data folded for the agentic-membrane context. | 26.4 (Evidence pane extends to `MediatedRunEvidencePacket` shape, mediator badge, dispatch-trace mini-graph, tool-stream link); 26.10 (`MediatedRunEvidencePacket` schema in `integrated-composition`); 15.2 (OmniPanel Evidence tab is the agentic-membrane landing). | Widget consumes generic `EvidenceRecord` (subset of `MediatedRunEvidencePacket`); `addRecord` / `setRecords` with privacy gate; no mediator badge, no dispatch-trace mini-graph, no tool-stream link, no axiom-translation link, no contemplation-object link. | Generic `<ul>` of evidence records. No `MediatedRunEvidencePacket`. No OmniPanel cross-link. | **AUDIT-EXTEND + DR-WC-IS-2 split (cross-link 26.4 + 26.10 + 15.2)** — closing tranche 28.8 audits + extends `evidence-pane-widget.tsx` per 26.4 (import `MediatedRunEvidencePacket` from `integrated-composition`, mediator badge, dispatch-trace mini-graph, tool-stream link, axiom-translation link, contemplation-object link). **The ide-shell-evidence-pane vs OmniPanel-Evidence-tab split is the load-bearing split — see DR-WC-IS-2.** |
| WC-IS-09 | Review pane is human-required gate landing (15.2 — NOT modal; review pane IS the landing surface); IOD-17 three-way parity (TS-08; 26.5). | TS-08 (ACR is M5-4 + IOD-17 parity); 26.5 (Review pane deepens to IOD-17 three-way parity + dispatch-genealogy click-through + evidence-packet click-through). | Widget consumes `ReviewItem` with `humanRequired` flag; human-required banner present; **no IOD-17 three-way parity readout (no capability-matrix-state, no agent-contract-state, no widget-state; no parity-violated red banner; no dispatch-genealogy click-through; no evidence-packet click-through).** | `<ul>` of review items with status + human-required banner. No three-way parity matrix. | **AUDIT-EXTEND + DR-WC-IS-2 split (cross-link 26.5 + 15.2)** — closing tranche 28.9 audits + extends `review-pane-widget.tsx` per 26.5 (`ReviewItemDeep` with `iod17Parity` + `dispatchGenealogyRef` + `mediatedRunEvidencePacketId`; three-cell parity readout; red parity-violated banner; dispatch-genealogy click-through to ACR; evidence-packet click-through to Evidence pane). **Same split decision as Evidence — DR-WC-IS-2.** |
| WC-IS-10 | Autoresearch pane surfaces per-finding source / confidence / action affordances; autoresearch-as-concept frame (26.6); Möbius-pass display ribbon (26.6); per-capacity filter (26.6 + 26.2). | 26.6 (autoresearch deepening — autoresearch-as-concept, Möbius-pass ribbon, per-capacity filter, dry-run badge, requires_human non-bypassable disclosure). | Widget consumes `AutoresearchCandidate` (id/title/score/coordinate/anchors); `refreshHistory()` reads `s5'.improve.history`. **No autoresearch-as-concept header, no Möbius-pass ribbon, no per-capacity filter, no recompose-pass count, no dry-run badge, no requires_human disclosure.** | `<ul>` of candidates with score + coordinate + anchors. | **AUDIT-EXTEND (cross-link 26.6)** — closing tranche 28.10 audits + extends `autoresearch-pane-widget.tsx` per 26.6 (autoresearch-as-concept frame, `<MobiusPassRibbon />` reading `s5'.improve.status` → `recompose.rs` pass-state, per-capacity filter dropdown from 26.2, dry-run + requires_human + forbidden_authority disclosure). |
| WC-IS-11 | Bridge-gate is kernel-bridge readiness gate (TS-09); inline rendering per 15.6 (per-binding border-colour / pending badge / blocked overlay; NEVER a separate errors panel); subscribes to kernel-bridge readiness ledger (07-t0 nine readiness ids). | TS-09 (kernel-bridge is the single boundary every surface consumes; kernel-bridge-readiness surfaces readiness inline); 15.6 (profile-tick clock + readiness inline rendering — every datum shows readiness state inline). | `bridge-gate.tsx` subscribes `onConnectionChange` + `onProfile`; renders pending shell on `!connected || profileGeneration === null`. **Binary ready/pending only — does NOT consume the nine-id readiness taxonomy from `07-t0` contract; does NOT render per-binding border-colour / pending badge / blocked overlay; does NOT subscribe to `subscribeObservability` readiness stream.** | Wrapping component renders entire pending shell over widget body when not connected. Coarse-grained. | **AUDIT-EXTEND + SPEC-AHEAD (cross-link TS-09 + 15.6 + 07-t0 readiness taxonomy)** — closing tranche 28.11 audits + extends `bridge-gate.tsx` to (a) subscribe to `bridge.subscribe('observability')` for readiness ledger updates, (b) expose a `useBridgeReadiness(bindingKey)` hook per-binding so individual widget data bindings (e.g. `bimba-graph-viewer` `s2.graph.node`, `coordinate-tree` `s2'.coordinate.resolve`, `review-pane` `s5'.review.inbox`) render per-binding border-colour + pending badge + blocked overlay per the nine-id taxonomy. The wrapping pending shell stays only for `bridge_unavailable`. |
| WC-IS-12 | Smart-Connections sidebar is code-pending per 11.4 (Track 03 T6.5 gating); the layout-claim `pratibimba.smart-connections-sidebar` is in `IDE_DEEP_DESCRIPTOR.expectedWidgets` but extension does not exist (TS-23). | 11.4 (code-pending closure for `pratibimba.smart-connections-sidebar`); 15-foundation `ide-deep` row (Smart Connections is `ide-deep`-only); MIGRATION-SOURCES.md (smart-connections-sidebar gated on Track 03 T6.5). | `smart-connections-sidebar/` package does NOT exist under `Body/M/epi-theia/extensions/`. `IDE_DEEP_DESCRIPTOR.expectedWidgets` includes the widget id. | Layout-switcher would resolve a missing widget id; per TS-23 the layout-switcher tolerance is in-doubt. | **CODE-PENDING (cross-link 11.4)** — closing tranche 28.12 lands a `smart-connections-sidebar-stub.tsx` inside ide-shell-m0-m5 that registers the widget id `pratibimba.smart-connections-sidebar` with a Theia activity-bar contribution rendering a "Smart Connections — gated on Track 03 T6.5" placeholder + the `MIGRATION-SOURCES.md` gating reference. When the real extension scaffolds at T4.5, the stub is removed and the real extension takes over the widget id. Layout-switcher tolerance test asserts no throw. |
| WC-IS-13 | Backend Studio (`ide-deep`-only) — LSP contributions (rust-analyzer / clangd / pylsp); navigates `epi-lib`, `portal-core`, S1–S5 cores (15-foundation `ide-deep` row); coordinate-aware (jump to coordinate in source). | 15-foundation `ide-deep` row + §"Left-sidebar-system" `Backend Studio` row; 26.7 (RunTree nodes click-through to `backend-studio.openSource` with source-anchor URI); 26.14 (PiAxiomTranslationInspector cross-link to Backend Studio for source-anchor). | NO Backend Studio surface exists today. Theia ships rust-analyzer / clangd / pylsp via Theia LSP infrastructure; the contribution that names them "Backend Studio" + routes coordinate-aware does not exist. | No surface. | **CODE-PENDING + SPEC-AHEAD (first-build against an unowned M' product surface)** — closing tranche 28.13 lands `Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/backend-studio/backend-studio-contribution.ts` registering an activity-bar mode "Backend Studio" (visible only when layout active = `ide-deep`); slot for rust-analyzer / clangd / pylsp LSP contributions via existing Theia LSP service; `openSource(coordinate, anchorUri)` command that resolves the coordinate's source-anchor via `s2.graph.node` (read-only) and opens the file at the anchored line via Theia editor service. RunTree (26.7) + PiAxiomTranslationInspector (26.14) consume `backend-studio.openSource`. |
| WC-IS-14 | Cross-layout intent routing (11.2) — only four of eight ide-shell widgets have an intent target registered today (BIMBA_GRAPH / CANON_STUDIO / AGENTIC_CONTROL_ROOM / EVIDENCE_PANEL); coordinate-tree / logos-atelier / review-pane / autoresearch-pane have no intent target. | 11.2 (cross-layout intent T5 promotion); 21-m0 21.3 + 21.12 (cross-link deep-links INTO ide-shell widgets via `cross-layout-intent-dispatcher`); 26.4 + 26.5 + 26.7 (dispatch-genealogy click-through between widgets requires intent targets). | `IDE_SHELL_INTENT_TARGETS = { CANON_STUDIO, BIMBA_GRAPH, AGENTIC_CONTROL_ROOM, EVIDENCE_PANEL }` — four of eight. `registerIntentTarget` calls in `frontend-module.ts` exist for these four only. | Four intent targets registered; four widgets cannot be routed-into via cross-layout-intent envelope. | **SPEC-AHEAD (cross-link 11.2)** — closing tranche 28.14 extends `IDE_SHELL_INTENT_TARGETS` to include `COORDINATE_TREE / LOGOS_ATELIER / REVIEW_PANE / AUTORESEARCH_PANE`; registers `registerIntentTarget` for all four in `frontend-module.ts`; each handler consumes `CrossLayoutIntent` payload (coordinate / artifactUri / reviewId / dayNow / sessionKey / profileGeneration / privacyClass / requestedExtensionId / requestedContributionId) per `omnipanel-types.ts`. |
| WC-IS-15 | Bridge-gate consumes kernel-bridge readiness gate inline per 15.6; integrated bridge-gate pattern (from `integrated-composition`) is the sibling — both should share readiness primitive. | 15.6 (profile-tick clock + readiness inline rendering); TS-09 (kernel-bridge readiness primitive); 26.10 (`MediatedRunEvidencePacket.bridgeReadinessHandle` is the shared primitive). | `bridge-gate.tsx` is one component; `integrated-composition` has `IntegratedBridgeGate`. Both consume readiness binary; neither consumes nine-id taxonomy. | Two parallel gate components; no shared readiness primitive. | **AUDIT (folded into 28.11)** — readiness-ledger subscription pattern from 28.11 should be exported from `m-extension-runtime` so both `ide-shell-bridge-gate` and `integrated-bridge-gate` consume one source. Cross-link 11.6 acceptance-harness state-identity test. |
| WC-IS-16 | Autoresearch pane is NOT in `IDE_DEEP_DESCRIPTOR.expectedWidgets` — orphan-against-layout (TS-15 sibling). | TS-15 (daily-layer widgets ORPHAN trace); layout-types.ts inspection. | `IDE_DEEP_DESCRIPTOR.expectedWidgets` includes seven ide-shell widget ids; `pratibimba.ide-shell.autoresearch-pane` is absent. | Layout-switcher will not request the autoresearch pane on materialise; the widget exists but is not bound to a layout. | **ORPHAN (against layout-types)** — closing tranche 28.15 adds `pratibimba.ide-shell.autoresearch-pane` to `IDE_DEEP_DESCRIPTOR.expectedWidgets` + asserts the binding in `validate-extension-contract-preflight.test.mjs` (per 11.5 lint extension). |
| WC-IS-17 | M0/M5 ownership split: `bimba-graph-viewer` / `canon-studio` / `coordinate-tree` are M0' chrome (read + governed-route write); `logos-atelier` / `evidence-pane` / `review-pane` / `autoresearch-pane` / `agentic-control-room` are M5' chrome (governance + agentic dispatch); `bridge-gate` is shared infrastructure (21-m0 SC-2 + 26-m5 Role 2). | 21-m0 SC-2 + Surface Contracts; 26-m5 Role 2 ("Five widgets landed; Wave-C audits + extends each"). | No declared "M0 chrome" vs "M5 chrome" partition in code or contract. | All eight widgets coexist with no semantic grouping. | **DOC-AHEAD (contract)** — folded into 28.1 chrome-contract; per-widget category declared (M0' / M5' / shared); orphan-against-category check in validator. |
| WC-IS-18 | Privacy class flow-through per 07-t0 — every widget gates on `isPrivacySafe(privacyClass)` before commit to render tree (already-aligned). | 07-t0 contract `privacy_blocked` readiness id; SC-7 (21-m0); 26-m5 SC chain. | All eight widgets call `isPrivacySafe()` before commit; bridge-gate inherits the same gate via wrapping. **`privacyDropped` count is rendered per widget but not federated** — no global "privacy-dropped" summary anywhere. | Each widget tracks its own privacy-dropped count. | **ALIGNED (partial)** — closing tranche 28.16 adds a shared `PrivacyDropFeed` (lightweight DI service) that aggregates per-widget privacy-drop events; the OmniPanel Diagnostics tab (per 15.2) renders the aggregate; the ide-shell chrome continues to render per-widget counts. Cross-link 27-omnipanel-shell tranche on Diagnostics. |
| WC-IS-19 | Profile-tick as primary clock (15-foundation principle 2) — every widget re-renders on `MathemeHarmonicProfile` tick advance. | 15.6 (profile-tick clock); 15-foundation principle 2. | Widgets call `this.update()` only on user input or after `await`-ed bridge calls. `bridge-gate.tsx` subscribes to `onProfile` but only triggers re-render at the gate level — child widgets do NOT subscribe per-tick. | Widgets re-render lazily; not driven by profile-tick. | **SPEC-AHEAD (cross-link 15.6)** — closing tranche 28.17 wires each widget body to subscribe to `bridge.onProfile` via React.useEffect-style pattern in the `IdeShellBridgeGate` context; `useProfileTick()` hook propagated to children; widget re-render on each tick. |
| WC-IS-20 | Status bar (15.10) surfaces profile-tick / day-now / session id / gateway readiness / profile generation / active coordinate; ide-shell widgets must NOT own these — they consume from status-bar state thread. | 15.10 (status-bar discipline); 21-m0 SC-5 (active coordinate is global); 27-omnipanel-shell Diagnostics. | Widgets render `profileGeneration` / `sessionKey` / `dayNow` / `coordinate` inline (e.g. ACR VAK fields, Evidence pane DTO fields). The status-bar contribution does NOT exist yet (cross-link 15.10). | Widgets duplicate state that should live in status bar. Acceptable for IDE-mode deep-render; redundant for shared chrome. | **DOC-AHEAD (cross-link 15.10)** — closing tranche 28.18 audits: ide-shell widgets MAY render these fields for deep-render but MUST consume them from the SharedBridgeAdapter projection (not from their own state); widget tests assert the values mirror the status-bar source. 15.10 tranche owns the status-bar build. |
| WC-IS-21 | Acceptance harness — eight ide-shell widgets must each survive the `daily-0-1` ↔ `ide-deep` toggle with active-coordinate + session + profileGeneration preserved (11.6). | 11.6 (acceptance-harness cross-layout state-identity assertion). | `acceptance-harness/tests/topology.test.mjs` exists; 11.6 SPEC's the extension. | No ide-shell-specific state-identity test exists. | **SPEC-AHEAD (cross-link 11.6)** — closing tranche 28.19 extends the 11.6 test pattern with per-widget state-identity assertions for the eight ide-shell widgets + bridge-gate + active activity-bar mode. |
| WC-IS-22 | Surface → extension → contract ledger (11.9) — every widget id in `layout-types.ts` mapped to (extension package, contract entry, status). | 11.9. | `surface-extension-contract-ledger.json` not yet authored. | No ledger. | **SPEC-AHEAD (cross-link 11.9)** — closing tranche 28.20 contributes the ide-shell rows (eight widgets + smart-connections stub + Backend Studio + activity-bar contributions) to the 11.9 ledger; per-widget status: `landed` / `code-pending` / `audit-extend-pending`. |

---

## Anomalies

### CONTRADICTIONS (Wave-C decision-register candidates — surface-scoped)

- **DR-WC-IS-1 — ide-shell ACR widget vs OmniPanel Dispatch Trace / Tool Stream split.** 15.2 reframes the ACR substrate as OmniPanel content model (Pi Chat / Sessions / Dispatch Trace / Tool Stream / Evidence / Review / Gateway / Diagnostics); 26.7 lands T8 contents (RunTree / ToolStream / AbortRetryContinueControls / EvidenceDepositForm / ReviewDecisionControls) **inside ide-shell-m0-m5 ACR widget body** (sub-folder `acr/`). The genuine question: which surface OWNS the dispatch-genealogy view? Proposed resolution: BOTH render it, ONE primary. OmniPanel = agentic-membrane / cross-layout / shared-with-daily-0-1 context (always-on; Pi voice-and-trace). Ide-shell ACR = `ide-deep`-only deep observatory with the IOD-17 governance source-of-truth (capability-matrix); ACR is the GOVERNANCE PRIMARY (audit perimeter), OmniPanel is the AGENTIC PRIMARY (Pi voice). Same `MediatedRunEvidencePacket` data, two foldings: tree in ACR RunTree (governance audit); time-ordered + actor-grouped in OmniPanel Dispatch Trace (Pi voice context). DR landing in 13-decision-register.
- **DR-WC-IS-2 — ide-shell evidence/review panes vs OmniPanel Evidence/Review tabs split.** Same shape as DR-WC-IS-1. Proposed resolution: ide-shell-evidence-pane (`ide-deep` only) = deep render with all `MediatedRunEvidencePacket` fields + dispatch-trace mini-graph + tool-stream link + axiom-translation link + contemplation-object link — the FULL evidence record (governance audit context); OmniPanel-Evidence-tab (cross-layout) = abbreviated record list + click-through to ide-shell-evidence-pane for the full render. Same data; two foldings. Identical pattern for Review: ide-shell-review-pane = IOD-17 three-way parity matrix + governance audit; OmniPanel-Review-tab = inbox + click-through to ide-shell-review-pane for the full parity readout. Cross-link 13-decision-register.
- **DR-WC-IS-3 — Canon Studio editor primitive: Monaco vs textarea + decoration list.** 15-foundation §"Left-sidebar-system" `Canon Studio` row says "markdown editor with QL/bimba decoration + Smart Connections autocomplete via `s1'.semantic.*`". Current widget is `<textarea>` + a separate decoration list rendered BELOW. The genuine question: do we upgrade to Monaco (Theia-native, supports inline decorations, autocomplete) or retain textarea with inline `<mark>` spans (lighter, no LSP)? Proposed: Monaco. Theia ships Monaco; reusing it gives us autocomplete + Smart Connections + inline QL decoration + frontmatter linter + PASU.md edit flow per 25-m4 cross-link. Cross-link 13-decision-register.

### CODE-PENDING / ORPHAN

- **WC-IS-02 + WC-IS-12 + WC-IS-13** — activity-bar mode contributions, smart-connections-sidebar stub, Backend Studio LSP slot — all first-build against an unowned M' product surface (no current owner; 15.3 + 11.4 + 15-foundation `ide-deep` row name them; closing 28.2 / 28.12 / 28.13 land them inside ide-shell-m0-m5).
- **WC-IS-14** — four of eight ide-shell widgets have no cross-layout intent target registered; 28.14 closes.
- **WC-IS-16** — `autoresearch-pane` widget id is orphan against `IDE_DEEP_DESCRIPTOR.expectedWidgets`; 28.15 fills.
- **WC-IS-19** — widgets do not re-render on profile-tick; 28.17 closes.
- **WC-IS-11 + WC-IS-15** — bridge-gate consumes binary readiness, not nine-id taxonomy; 28.11 closes (folded into shared `m-extension-runtime` readiness primitive).
- **WC-IS-08 + WC-IS-09 + DR-WC-IS-1 + DR-WC-IS-2** — Evidence/Review/ACR ownership split must be ratified before 28.5 / 28.8 / 28.9 land.

### ALIGNED (no Wave-C work)

- **WC-IS-18** — privacy class flow-through is already enforced via `isPrivacySafe()` at every widget. Federation (28.16) is a polish closure, not a contract fix.
- Kernel-bridge / SharedBridgeAdapter / forbidden-import discipline — all eight widgets honour the contract; no direct S0/S2/S3/S5 imports anywhere.

## Proposed Cycle-3 Closing Tranches (ide-shell chrome domain — id space `28.x`)

> All tranches obey the anti-greenfield rule: audit + extend existing widgets, OR first-build against an unowned M' product surface (activity-bar contributions, Backend Studio LSP slot, Smart-Connections code-pending stub), OR doc-ahead-landing. SharedBridgeAdapter is the only network surface; kernel-bridge readiness is the only readiness primitive; privacy gate is non-bypassable.

### 28.1 — Chrome contract doc-ahead-landing + M0/M5 ownership partition

- **Classification:** `doc-ahead-landing`
- **Source:** WC-IS-01 + WC-IS-17
- **Deliverable:** `Body/M/epi-theia/extensions/ide-shell-m0-m5/CHROME-CONTRACT.md` declaring: (a) the eight widgets + bridge-gate as one unified chrome system; (b) per-widget category — `bimba-graph-viewer / canon-studio / coordinate-tree` as **M0' chrome** (read + governed-route write); `logos-atelier / evidence-pane / review-pane / autoresearch-pane / agentic-control-room` as **M5' chrome** (governance + agentic dispatch); `bridge-gate` as **shared infrastructure**; (c) per-widget activity-bar vs main vs right-sidebar slot assignment; (d) SharedBridgeAdapter as only network; (e) M0/M5 governance flow (read on Coordinate Tree / Bimba Graph Viewer left; write through M5 atelier → routed via Canon Studio + Logos Atelier per DR-M0-1 + 21-m0 SC-2); (f) cross-link to 27-omnipanel-shell ownership split (DR-WC-IS-1 + DR-WC-IS-2).
- **Verification:** `test -f Body/M/epi-theia/extensions/ide-shell-m0-m5/CHROME-CONTRACT.md`; `grep -n "M0' chrome\|M5' chrome\|shared infrastructure\|activity-bar slot" .../CHROME-CONTRACT.md`; per-widget category cross-referenced in `frontend-module.ts` doc-comment; validator extends `validate-extension-contract-preflight.mjs` to assert every ide-shell widget id has a CHROME-CONTRACT category.

### 28.2 — Left-sidebar activity-bar system contribution

- **Classification:** `spec-ahead-integration` (consumes 15.3, 15.7)
- **Source:** WC-IS-02
- **Deliverable:** New `Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/activity-bar/left-sidebar-activity-bar-contribution.ts` registering Theia activity-bar modes filtered by `epi-logos.layout.active` preference: for `daily-0-1` — `Coordinate Tree` (icon: tree) · `Bimba Graph Viewer` (icon: graph) · `Canon Studio` (icon: scroll); for `ide-deep` — same three + `Backend Studio` (icon: terminal, code-pending per 28.13) + `Smart Connections` (icon: link, code-pending per 28.12). Active mode persists via SharedBridgeAdapter `currentStateSelectors` so it survives layout toggle per 15.7 BimbaPratibimbaUiState. Move `defaultWidgetOptions.area` of `bimba-graph-viewer-widget` + `canon-studio-widget` from `'main'` to `'left'` when in `daily-0-1`; retain `'main'` for `ide-deep` per 15-foundation §"`ide-deep`" row. **Cross-link 26-omnipanel-shell** for the right-sidebar OmniPanel partition.
- **Verification:** `test -f Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/activity-bar/left-sidebar-activity-bar-contribution.ts`; `pnpm --filter @pratibimba/ide-shell-m0-m5 test`; integration test asserts activity-bar registers exactly three modes for `daily-0-1` + five for `ide-deep`; cross-layout state-identity preserves active mode (cross-link 11.6 + 28.19).

### 28.3 — Bimba Graph Viewer two-rendering audit + neighbor graph

- **Classification:** `audit-extend`
- **Source:** WC-IS-03 + 15-foundation §"Left-sidebar-system" + DR-IG-1 (two-relation-families discriminator)
- **Deliverable:** Audit `bimba-graph-viewer-widget.tsx`. (a) Add `RenderingMode = 'solar-anchor' | 'full-lattice'` switched by `epi-logos.layout.active` (solar-anchor for `daily-0-1` = compact card showing active coordinate + immediate Bimba-side neighbors only; full-lattice for `ide-deep` = SVG/ForceGraph rendering of `BimbaSubgraphPayload.neighbors[]` with node click → publishes via SharedBridgeAdapter.publishCoordinateContext). (b) Edge filter dropdown by `c_1_relation_family` discriminator (two families: `structural | correspondential` per DR-IG-1 / 21-m0 21.4 / TS-16). (c) Node click handler emits `CrossLayoutIntent` with `coordinate` payload — consumed by Coordinate Tree (highlight), per-Mn widgets (focus), Canon Studio (open canonical source). (d) Privacy gate + `privacyDropped` counter retained; `bridge-gate` consumes nine-id readiness per 28.11. (e) NO rebuild of subgraph fetch logic — `openCoordinate(coordinate)` stays. New rendering layer is in `Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/bimba-graph-viewer/graph-canvas.tsx` (ForceGraph or SVG; consume Theia's existing `react-force-graph` if available, else SVG).
- **Verification:** `pnpm --filter @pratibimba/ide-shell-m0-m5 build && test`; `grep -n "RenderingMode\|solar-anchor\|full-lattice\|graph-canvas\|c_1_relation_family" Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/`; rendering-mode test asserts solar-anchor in `daily-0-1`, full-lattice in `ide-deep`; edge-filter test partitions neighbors by family; node-click test publishes coordinate context via SharedBridgeAdapter mock.

### 28.4 — Canon Studio Monaco upgrade + QL/bimba decoration + Smart Connections autocomplete + PASU.md edit flow

- **Classification:** `audit-extend` + `spec-ahead-integration` (consumes 11.4 smart-connections gate + 25-m4 PASU.md)
- **Source:** WC-IS-04 + DR-WC-IS-3
- **Deliverable:** Audit `canon-studio-widget.tsx`. (a) Per DR-WC-IS-3 RESOLVED (Monaco): replace `<textarea>` with Theia's Monaco editor mounted in the React widget; reuse existing `decorateCoordinates` for inline Monaco decorations (`editor.IModelDeltaDecoration` per QL coordinate / bimba wikilink / frontmatter key). (b) Wire `s1'.semantic.*` Smart Connections autocomplete via `KERNEL_BRIDGE_API.invokeCapability` with `gatewayMethod: 's1\'.semantic.suggest'` — registered as Monaco `CompletionItemProvider`; falls back to "Smart Connections gated on Track 03 T6.5" placeholder when `smart-connections-sidebar` stub still active (28.12). (c) Frontmatter linter consumes the C-family typology authority (per DR-S1-5 + 21-m0 SC-2): validate `c_0_* / c_1_* / c_2_* / c_3_* / c_4_* / c_5_*` keys + coordinate frontmatter against `m-extension-runtime` C-family schema. (d) PASU.md edit flow per 25-m4 cross-link — when `uri` matches `Idea/Pratibimba/Self/PASU.md`, render special PASU keys (`c_0_birth_date / c_0_birth_location / c_2_jungian / c_3_gene_keys / c_4_human_design / c_5_quintessence_hash / c_5_quintessence_clock`) with their typed editors (date picker, location picker, etc.). (e) Vault-bridge save retained as-is (rejection until T4.5 lands); per 21-m0 SC-2 governance routes through M5 Logos Atelier — Authoring-mode in Canon Studio surfaces "Open in Logos Atelier" deep-link button next to Save.
- **Verification:** `grep -n "MonacoEditor\|CompletionItemProvider\|IModelDeltaDecoration\|PASU\|c_0_birth_date" Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/canon-studio-widget.tsx`; Monaco-mount test; QL-coordinate inline decoration test; `s1'.semantic.suggest` autocomplete test (with mocked SharedBridgeAdapter); PASU.md edit flow opens typed-editors for the seven PASU keys; vault-bridge gate retained.

### 28.5 — Agentic Control Room T8 contents + DR-M5-1 roster collapse + Pi-monitor reframe

- **Classification:** `audit-extend` (consumes 26.7)
- **Source:** WC-IS-05 + DR-WC-IS-1
- **Deliverable:** Mirror of 26.7 — three sub-deliverables on `agentic-control-room-widget.tsx`: (a) **T8 contents** in new `Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/acr/` sub-folder: `run-tree.tsx` (Pi→Anima→subagent tree from `dispatchTrace`; per 26.10 `DispatchTraceNode` schema), `tool-stream.tsx` (time-ordered tool-call list per 15.11), `abort-retry-continue-controls.tsx` (gated on `humanRequired===false`), `evidence-deposit-form.tsx` (wraps `s5'.epii.deposit`), `review-decision-controls.tsx` (wraps `s5'.review.transition`; disabled when `humanRequired===true` with IOD-17 parity check). (b) **DR-M5-1 roster collapse** per 26.7 (b) — Pi single harness + Anima dispatcher + six Aletheia subagents (Anansi/Janus/Moirai/Mercurius/Agora/Zeithoven) as crystallisation-mode sub-items NOT peer agents; legacy `constitutional_agents` renders as psyche-facet badges per 26.8. (c) **Pi-monitor reframe** — `<PiRuntimeMonitorBanner />` at top; widget label reframes to "Pi Runtime Monitor (ACR)" per 26.7 (c); widget id preserved. (d) **Per DR-WC-IS-1 RESOLVED**: ACR is GOVERNANCE PRIMARY (audit perimeter; IOD-17 capability-matrix source-of-truth + dispatch-tree with full governance fields); OmniPanel Dispatch Trace (27-omnipanel-shell) is AGENTIC PRIMARY (Pi voice context; time-ordered + actor-grouped). RunTree nodes click-through to Backend Studio (28.13) `backend-studio.openSource(source-anchor)`; click-through to Evidence pane (28.8) via `mediatedRunEvidencePacketId`. (e) Aletheia subagent trace `aletheia-subagent-trace.tsx` per 26.9 lands in `acr/` sub-folder.
- **Verification:** `test -d Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/acr`; `grep -n "RunTree\|ToolStream\|AbortRetryContinueControls\|EvidenceDepositForm\|ReviewDecisionControls\|PiRuntimeMonitorBanner\|aletheia-subagent-trace" .../ide-shell-m0-m5/src/browser/`; T8 contents render; roster-collapse test (Pi + Anima + 6 Aletheia subagents); human-required disables abort/retry + review decision; IOD-17 parity violation surfaces red; click-through to Backend Studio + Evidence pane works.

### 28.6 — Coordinate Tree extension: family colouring + active-coordinate highlight + CRUD-vs-governed-route toggle

- **Classification:** `audit-extend`
- **Source:** WC-IS-06 + TS-18 + 21-m0 21.3
- **Deliverable:** Audit `coordinate-tree-widget.tsx`. (a) Per-family colouring: P/S/T/M/L/C six C-family-canonical colours per ide-deep MEMORY canonical colour map (each derived from C-family-letter LUT) + two namespace colours for `Empty/` and `Pratibimba/` (per repo-ontology). Apply as inline CSS class `coordinate-family-{P|S|T|M|L|C|Empty|Pratibimba}`. (b) Active-coordinate highlight via `SharedBridgeAdapter.onCoordinateContext` subscription — the active coordinate's `<li>` gets `class="active-coordinate"`; on click, the widget publishes via `SharedBridgeAdapter.publishCoordinateContext` so all other widgets refocus (per 15-foundation principle 1 + 21-m0 SC-5). (c) CRUD-vs-governed-route mode toggle (`'reading' | 'authoring'`): Reading = current read-only tree; Authoring = surfaces "Propose canonical edit" button on each node — emits `CrossLayoutIntent` to Canon Studio (cross-link 28.4) per DR-M0-1 — NEVER mutates locally; `mutatesGraphCanon: false` invariant per 21-m0 SC-2. (d) Tree expand/collapse per family-segment for navigation breadth control. (e) Privacy-class colouring on each node per 07-t0 `privacy_blocked` readiness.
- **Verification:** `grep -n "coordinate-family-\|active-coordinate\|RenderingMode\|reading\|authoring\|publishCoordinateContext\|CrossLayoutIntent" Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/coordinate-tree-widget.tsx`; family-colouring test (six C-family + two namespace); active-coordinate highlight test (subscribes + publishes); CRUD-vs-governed-route mode test (Authoring routes to Canon Studio via `CrossLayoutIntent`); privacy-class colouring test.

### 28.7 — Logos Atelier scent-following retrofit + Aletheia tools + Möbius write-back

- **Classification:** `audit-extend` (consumes 26.3)
- **Source:** WC-IS-07 + 21-m0 SC-2
- **Deliverable:** Mirror of 26.3 inside ide-shell — replace `ATELIER_STAGES` (six generic L0-L5) with `SCENT_FOLLOWING_STAGES` (root / cognate / drift / psychoid / pros-hen / mobius-write-back); wire Aletheia tools (`aletheia_gnosis_query / aletheia_thought_route / aletheia_crystallise`) via `KERNEL_BRIDGE_API.invokeCapability`; `<ReadinessBanner state='pending-gateway' />` until 6.1 lands; etymology namespace enforcement (`etymology://` URI scheme); Aletheia subagent surfacing on Möbius write-back stage per 26.9; Möbius write-back stage routes the canonical articulation to Canon Studio (28.4) via `CrossLayoutIntent` per 21-m0 SC-2 governance flow; subagent veto banner per 12.19. Cross-link 26.14 PiAxiomTranslationInspector (axiom-translation as sub-pane inside ACR; Atelier links to it from Möbius write-back).
- **Verification:** `grep -n "SCENT_FOLLOWING_STAGES\|aletheia_gnosis_query\|aletheia_crystallise\|mobius-write-back\|etymology://" Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/logos-atelier-widget.tsx`; stage-progression test asserts six new stages; Möbius-write-back invokes `aletheia_crystallise` under bridge gate + routes to Canon Studio via `CrossLayoutIntent`; Aletheia subagent veto banner red + non-blocking on human gate.

### 28.8 — Evidence pane deepening — `MediatedRunEvidencePacket` + dispatch-trace mini-graph + DR-WC-IS-2 ide-shell-vs-OmniPanel split

- **Classification:** `audit-extend` (consumes 26.4 + 26.10)
- **Source:** WC-IS-08 + DR-WC-IS-2
- **Deliverable:** Mirror of 26.4 — replace `EvidenceRecord` import with `MediatedRunEvidencePacket` from `@pratibimba/integrated-composition` (lands at 26.10); mediator badge (Pi / Anima / Aletheia · {subagent}); inline dispatch-trace mini-graph (collapsible per 15.11); tool-stream link to OmniPanel Tool Stream tab; axiom-translation link to PiAxiomTranslationInspector inside ACR (28.5 + 26.14); contemplation-object link to ContemplationObjectViewer inside m5-epii (26.12); close-path landing per 19.7. **Per DR-WC-IS-2 RESOLVED**: ide-shell evidence-pane = FULL `MediatedRunEvidencePacket` deep render (`ide-deep` only; governance audit context); OmniPanel-Evidence-tab (27-omnipanel-shell) = abbreviated list + click-through to ide-shell-evidence-pane for full render. Cross-layout intent envelope (28.14) carries `evidenceRecordId` so click-through highlights the record in either surface.
- **Verification:** `grep -n "MediatedRunEvidencePacket\|dispatchTrace\|toolStream\|axiomTranslationSteps\|contemplationObjectRef" Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/evidence-pane-widget.tsx`; mediator-badge + mini-graph + tool-stream cross-link + axiom-translation link + contemplation-object link render-tests; cross-layout dispatch test (OmniPanel click → ide-shell highlights same record).

### 28.9 — Review pane deepening — IOD-17 three-way parity + dispatch-genealogy click-through + DR-WC-IS-2 split

- **Classification:** `audit-extend` (consumes 26.5)
- **Source:** WC-IS-09 + DR-WC-IS-2
- **Deliverable:** Mirror of 26.5 — extend `ReviewItem` to `ReviewItemDeep` with `iod17Parity` three-cell readout + `dispatchGenealogyRef` + `mediatedRunEvidencePacketId`; render parity-violated red banner; click-through to ACR (28.5) on `dispatchGenealogyRef`; click-through to Evidence pane (28.8) on `mediatedRunEvidencePacketId`. **Per DR-WC-IS-2 RESOLVED**: ide-shell review-pane = FULL parity readout + governance audit; OmniPanel-Review-tab (27-omnipanel-shell) = inbox + click-through to ide-shell review-pane for full parity. Reviews are NOT modal per 15.2 — both surfaces are landing surfaces; the OmniPanel is cross-layout-always-on, ide-shell is `ide-deep`-only.
- **Verification:** `grep -n "ReviewItemDeep\|iod17Parity\|dispatchGenealogyRef\|mediatedRunEvidencePacketId" Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/review-pane-widget.tsx`; parity-violation red banner; parity-OK green-check; click-through opens ACR + Evidence with pre-populated refs; human-required banner blocks agent transitions.

### 28.10 — Autoresearch pane deepening — autoresearch-as-concept + Möbius-pass ribbon + per-capacity filter

- **Classification:** `audit-extend` (consumes 26.6)
- **Source:** WC-IS-10
- **Deliverable:** Mirror of 26.6 — `autoresearch-as-concept` header (dry-run only, `requires_human` non-bypassable, `forbidden_authority` disclosure); `<MobiusPassRibbon />` reading `s5'.improve.status` → `recompose.rs` pass-state (Surface → Route → Orchestrate → Integrate; active stage highlighted; recompose-pass count; dry-run badge); per-capacity filter dropdown (six options from 26.2 capacities + "all"); local widget state per 15-foundation principle 7; extend `AutoresearchCandidate` with optional `capacity?` field.
- **Verification:** `grep -n "MobiusPassRibbon\|autoresearch-as-concept\|requires_human\|dry-run\|recompose-pass" Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/autoresearch-pane-widget.tsx`; header-frame render-test; Möbius-pass ribbon active-stage test; per-capacity filter narrows candidate list.

### 28.11 — Bridge-gate nine-id readiness taxonomy + per-binding inline rendering + shared readiness primitive

- **Classification:** `audit-extend` + `spec-ahead-integration` (consumes 15.6 + 07-t0 readiness taxonomy)
- **Source:** WC-IS-11 + WC-IS-15
- **Deliverable:** Audit `bridge-gate.tsx`. (a) Subscribe to `bridge.subscribe('observability')` for readiness ledger updates; consume the nine readiness ids from 07-t0 (`bridge_unavailable / profile_missing_field / s2_graph_blocked / s3_subscription_blocked / s5_review_blocked / authority_payload_missing / privacy_blocked / degraded_but_readable / ready_public_current`). (b) Expose `useBridgeReadiness(bindingKey)` hook + `<BridgeReadinessBadge bindingKey={...} />` component for per-binding rendering: border-colour (green / amber / red), pending badge (e.g. "s2_graph_blocked"), blocked overlay. (c) Wrapping pending shell stays ONLY for `bridge_unavailable`; finer-grained states render inline at the binding site per 15.6. (d) Export the readiness primitive from `@pratibimba/m-extension-runtime` so `IntegratedBridgeGate` (in `integrated-composition`) consumes the same source — folds WC-IS-15. (e) The eight ide-shell widgets adopt the per-binding pattern: `bimba-graph-viewer` binds `s2.graph.node`; `coordinate-tree` binds `s2'.coordinate.resolve`; `evidence-pane` binds `s5'.review.history`; `review-pane` binds `s5'.review.inbox`; `autoresearch-pane` binds `s5'.improve.history`; `logos-atelier` binds `aletheia_gnosis_query`; `canon-studio` binds `vault-bridge.s1prime.vault.write_file`; ACR binds `capability-matrix` + per-route.
- **Verification:** `grep -n "useBridgeReadiness\|BridgeReadinessBadge\|bridge_unavailable\|profile_missing_field\|s2_graph_blocked\|degraded_but_readable\|ready_public_current" Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/`; nine-id readiness rendering test (one per id); per-binding inline badge test for each widget; shared primitive export from `m-extension-runtime` consumed by `integrated-composition` `IntegratedBridgeGate`; wrapping pending shell only for `bridge_unavailable`.

### 28.12 — Smart-Connections sidebar code-pending stub

- **Classification:** `code-pending-closure` (consumes 11.4)
- **Source:** WC-IS-12
- **Deliverable:** New `Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/smart-connections/smart-connections-sidebar-stub.tsx` registering Theia activity-bar contribution with widget id `pratibimba.smart-connections-sidebar` (the layout-claim from `IDE_DEEP_DESCRIPTOR.expectedWidgets`); body renders placeholder "Smart Connections — gated on Track 03 T6.5 (smart-connections-sidebar extension scaffolds at T4.5 per MIGRATION-SOURCES.md)" + readiness banner `state: 'pending-extension'`. Layout-switcher tolerates the stub. When the real extension scaffolds at T4.5, the stub is removed via this same code-pending marker file's deletion-and-replace contract; widget id preserved.
- **Verification:** `test -f Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/smart-connections/smart-connections-sidebar-stub.tsx`; `pnpm --filter @pratibimba/pratibimba-layouts test` (layout-switcher tolerates pending widget); `grep -n "pratibimba.smart-connections-sidebar\|Track 03 T6.5\|pending-extension" .../smart-connections/`; MIGRATION-SOURCES.md row referenced.

### 28.13 — Backend Studio LSP slot + coordinate-aware navigation

- **Classification:** `spec-ahead-integration` (first-build against unowned M' product surface; 15-foundation `ide-deep` row)
- **Source:** WC-IS-13
- **Deliverable:** New `Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/backend-studio/backend-studio-contribution.ts` + `backend-studio-service.ts`. (a) Activity-bar mode "Backend Studio" visible only when `epi-logos.layout.active = 'ide-deep'`. (b) LSP slot — register rust-analyzer / clangd / pylsp via Theia's existing LSP infrastructure (`MonacoLanguages.register`); the contribution names them as Backend Studio's language servers; supports navigating `Body/S/S0/epi-lib/`, `Body/S/S0/portal-core/`, `Body/S/S1/`, `Body/S/S2/`, `Body/S/S3/`, `Body/S/S4/`, `Body/S/S5/` sources. (c) `backend-studio.openSource(coordinate: string, sourceAnchor: string)` command — resolves the coordinate's source anchor via `KERNEL_BRIDGE_API.invokeCapability` with `gatewayMethod: 's2.graph.node'` (read-only), opens the file at the anchored line via Theia editor service. (d) Consumed by RunTree (28.5) node click → `backend-studio.openSource(coord, anchor)`; consumed by PiAxiomTranslationInspector (26.14) cross-link to source skill; consumed by Coordinate Tree (28.6) "View source" affordance per node. (e) Privacy gate retained — source paths are public by default; forbidden privacy classes block file open.
- **Verification:** `test -f Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/backend-studio/backend-studio-contribution.ts`; activity-bar mode "Backend Studio" registered in `ide-deep` only; `backend-studio.openSource` command resolves source-anchor + opens file via Theia editor; RunTree + PiAxiomTranslationInspector + Coordinate Tree click-throughs work; privacy gate blocks forbidden classes.

### 28.14 — Cross-layout intent target registration for the four un-registered widgets

- **Classification:** `spec-ahead-integration` (consumes 11.2)
- **Source:** WC-IS-14
- **Deliverable:** Extend `Body/M/epi-theia/extensions/ide-shell-m0-m5/src/common/contract.ts` `IDE_SHELL_INTENT_TARGETS` to include `COORDINATE_TREE / LOGOS_ATELIER / REVIEW_PANE / AUTORESEARCH_PANE` (resolves as `pratibimba.ide-shell-m0-m5.{name}.open`). In `frontend-module.ts` add `registerIntentTarget` for each in the corresponding `*Contribution` class; each handler consumes `CrossLayoutIntent` payload (`coordinate / artifactUri / reviewId / dayNow / sessionKey / profileGeneration / privacyClass / requestedContributionId`) per `omnipanel-types.ts`. Coordinate Tree handler invokes `loadTree(coordinate)`; Logos Atelier handler invokes `setTerm(term)` if `requestedContributionId === 'term:{value}'`; Review Pane handler invokes `refreshInbox()` + selects `reviewId`; Autoresearch Pane handler invokes `refreshHistory()` + filters per `capacity` from intent.
- **Verification:** `grep -n "COORDINATE_TREE\|LOGOS_ATELIER\|REVIEW_PANE\|AUTORESEARCH_PANE" Body/M/epi-theia/extensions/ide-shell-m0-m5/src/common/contract.ts`; eight intent targets total registered in `frontend-module.ts`; per-target handler test exercises the consumer-side wiring with synthetic `CrossLayoutIntent`.

### 28.15 — Autoresearch pane orphan-against-layout fill

- **Classification:** `no-orphan-fill` (cross-link 11.5 lint extension)
- **Source:** WC-IS-16
- **Deliverable:** Add `pratibimba.ide-shell.autoresearch-pane` to `IDE_DEEP_DESCRIPTOR.expectedWidgets` in `Body/M/epi-theia/extensions/pratibimba-layouts/src/common/layout-types.ts`. Extend `validate-extension-contract-preflight.test.mjs` per 11.5 to assert every `IDE_DEEP_DESCRIPTOR.expectedWidgets` id has either a registered widget factory in `frontend-module.ts` OR a `code-pending` marker (smart-connections-sidebar stub).
- **Verification:** `grep -n "pratibimba.ide-shell.autoresearch-pane" Body/M/epi-theia/extensions/pratibimba-layouts/src/common/layout-types.ts`; orphan-fill test asserts no `expectedWidgets` id is unowned; `node --test Body/M/epi-theia/extensions/test/validate-extension-contract-preflight.test.mjs`.

### 28.16 — Federated PrivacyDropFeed (OmniPanel Diagnostics tab feed)

- **Classification:** `aligned-only-polish` (consumes 15.2 Diagnostics tab)
- **Source:** WC-IS-18
- **Deliverable:** New `Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/services/privacy-drop-feed.ts` — DI singleton `PrivacyDropFeed` with `record(widgetId, privacyClass, droppedAt)` + `subscribe(listener)` API. Per-widget privacy-drop hooks publish to feed (replace per-widget `this.privacyDropped += 1` with `feed.record(this.id, privacyClass, Date.now())`). OmniPanel Diagnostics tab (27-omnipanel-shell) subscribes + renders aggregate; ide-shell widgets continue to render their own count. Cross-link 27-omnipanel-shell tranche.
- **Verification:** `test -f Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/services/privacy-drop-feed.ts`; eight widgets publish via the feed; aggregate test in OmniPanel Diagnostics (when 27-omnipanel-shell tranche lands).

### 28.17 — Profile-tick re-render contract per widget

- **Classification:** `spec-ahead-integration` (consumes 15.6)
- **Source:** WC-IS-19
- **Deliverable:** Each ide-shell widget body subscribes to `bridge.onProfile` via the `IdeShellBridgeGate` context; expose `useProfileTick()` hook in `bridge-gate.tsx`; child widgets call `useProfileTick()` and trigger `this.update()` on each advance per 15-foundation principle 2. Existing `await`-driven re-renders stay; tick-driven re-renders are additional. Cross-link 11.6 acceptance-harness state-identity test.
- **Verification:** `grep -n "useProfileTick\|onProfile\|tick-advance" Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/`; profile-tick replay test asserts each widget re-renders on each tick with stable input; no parallel local timer competes.

### 28.18 — Status-bar consumption audit (no own-state for shared fields)

- **Classification:** `doc-ahead-landing` (consumes 15.10)
- **Source:** WC-IS-20
- **Deliverable:** Audit each ide-shell widget — for the six shared status-bar fields (profile-tick state, day-now anchor, session id, gateway readiness, profile generation, active coordinate) MUST consume from SharedBridgeAdapter projection, NOT from own widget state. Widget tests assert the rendered values mirror the SharedBridgeAdapter source. 15.10 tranche owns the status-bar build itself.
- **Verification:** `grep -n "SharedBridgeAdapter.cachedProfile\|cachedProfile.generation\|cachedCoordinateContext\|profileGeneration\|sessionKey" Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/`; per-widget audit notes added as code comments where field is rendered.

### 28.19 — Acceptance-harness state-identity assertion per ide-shell widget

- **Classification:** `spec-ahead-integration` (consumes 11.6)
- **Source:** WC-IS-21
- **Deliverable:** Extend `Body/M/epi-theia/extensions/acceptance-harness/tests/topology.test.mjs` per 11.6 with per-widget state-identity assertions — toggling `daily-0-1` ↔ `ide-deep` with active coordinate `M3-1-0-13` (sample codon) + session + profile generation preserves: (a) Coordinate Tree active-coordinate highlight; (b) Bimba Graph Viewer selected coordinate; (c) Canon Studio open uri; (d) Logos Atelier current term; (e) Evidence pane record selection; (f) Review pane item selection; (g) ACR VAK fields; (h) Autoresearch pane filter; (i) active activity-bar mode (28.2); (j) bridge-gate readiness state per binding (28.11).
- **Verification:** `pnpm --filter @pratibimba/acceptance-harness test`; per-widget state-identity assertion passes.

### 28.20 — Surface→extension→contract ledger contribution (ide-shell rows)

- **Classification:** `no-orphan-fill` (consumes 11.9)
- **Source:** WC-IS-22
- **Deliverable:** Contribute the ide-shell rows to `Body/M/epi-theia/extensions/contracts/surface-extension-contract-ledger.json` per 11.9: eight widgets (status: `audit-extend-pending` until 28.3 .. 28.10 land), `bridge-gate` (`landed`), activity-bar contribution (`code-pending` → `landed` after 28.2), smart-connections-sidebar (`code-pending`), Backend Studio (`code-pending` → `landed` after 28.13). Each row carries `{widgetId, extensionPackage, contractEntry, status, ownsWidget, ownsActivityBarMode, layoutBindings[]}`.
- **Verification:** `node --test Body/M/epi-theia/extensions/test/surface-extension-contract-ledger.test.mjs`; ide-shell rows present + status correct; cross-link 14-no-orphan-audit.

---

**End of matrix.** The ide-shell chrome substrate is **landed at presence level**: eight widgets + bridge-gate + frontend-module + contract + decorations + capability-matrix-types + graph-types + vault-bridge-gate all coexist under one extension. The genuine Wave-C work concentrates on (a) **per-widget DEPTH** — each widget audits-and-extends to match its 26-m5 / 21-m0 spec (28.3 / 28.4 / 28.5 / 28.6 / 28.7 / 28.8 / 28.9 / 28.10), (b) **activity-bar + Backend Studio + Smart-Connections code-pending** systems that 15.3 + 11.4 + 15-foundation name (28.2 / 28.12 / 28.13), (c) **bridge-gate nine-id readiness** per-binding inline rendering per 15.6 (28.11), (d) **cross-layout intent** completion for four un-registered widgets (28.14), (e) **DR-WC-IS-1 / DR-WC-IS-2 / DR-WC-IS-3** ownership splits (ide-shell-as-governance-primary vs OmniPanel-as-agentic-primary; Monaco vs textarea). Downstream cross-links — 27-omnipanel-shell consumes the ownership split, 28.1 chrome-contract is the single source-of-truth for the partition.
