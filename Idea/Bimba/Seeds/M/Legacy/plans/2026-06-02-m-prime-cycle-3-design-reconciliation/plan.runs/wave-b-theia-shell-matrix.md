# Wave B — Theia Shell / Substrate-Hosting Reconciliation Matrix

**Task id:** `wave-b-theia-shell`
**Domain:** epi-theia shell hosting the six M' surfaces + two integrated plugins + OmniPanel `/` membrane
**Anti-greenfield posture:** `Body/M/epi-theia/extensions/*` is landed substrate. Every tranche below is `consume as-is`, `audit/verify`, `extend`, or a named integration closure. No first-build of an already-scaffolded extension.

## Source list (corpora actually consulted)

- **Corpus 1 (UX intent)** — `Idea/Pratibimba/System/Subsystems/Anuttara/anuttara-ux-full-m0-branch.md` §3 ("one substrate, three renderings, six data-layers")
- **Corpus 2 (M' Seed authority)**
  - `Idea/Bimba/Seeds/M/M'-SYSTEM-SPEC.md` §"Shell layer" L91–149 (`0 = cosmic shell`, `/ = command membrane`, `1 = personal shell`, `M0–M5 = subsystem pages`; the `(0/1)` toggle as `#` enacted at UI scale)
  - `Idea/Bimba/Seeds/M/M'-TAURI-PORT-SPEC.md` §"Canonical 0/1/4+2" L52
- **Corpus 3 (substrate)**
  - `Body/M/epi-theia/extensions/pratibimba-layouts/src/common/layout-types.ts` (the `daily-0-1` + `ide-deep` descriptors with widget manifests)
  - `Body/M/epi-theia/extensions/pratibimba-layouts/src/browser/{cross-layout-intent-dispatcher,layout-switcher,layout-commands,layout-status-bar,session-state-service}.ts`
  - `Body/M/epi-theia/extensions/omnipanel-shell/src/{browser,common,shared}/*` (widget, contribution, runtime-stub, `OmniPanelTabId`, `CrossLayoutIntent`)
  - `Body/M/epi-theia/extensions/ide-shell-m0-m5/src/browser/*-widget.tsx` (bimba-graph-viewer, canon-studio, agentic-control-room, coordinate-tree, logos-atelier, evidence-pane, review-pane, autoresearch-pane, bridge-gate)
  - `Body/M/epi-theia/extensions/{m0-anuttara,m1-paramasiva,m2-parashakti,m3-mahamaya,m4-nara,m5-epii}/package.json` (six per-extension packages present)
  - `Body/M/epi-theia/extensions/{plugin-integrated-1-2-3,plugin-integrated-4-5-0,integrated-composition}/package.json`
  - `Body/M/epi-theia/extensions/{kernel-bridge,kernel-bridge-readiness,m-extension-runtime,agentic-control-room,body-lite-surface,acceptance-harness}/package.json`
  - `Body/M/epi-theia/extensions/contracts/07-t0-extension-contract-preflight.{md,json}` (six extensions, KernelBridgeAPI capability/subscription/forbidden-import set, readiness taxonomy)
  - `Body/M/epi-theia/extensions/contracts/08-t0-composition-contract-preflight.{md,json}` (the two integrated plugins, shared `Body/M/epi-theia/shared` composition shapes)
  - `Body/M/epi-theia/extensions/MIGRATION-SOURCES.md` (declares `Body/M/epi-tauri` retired except as named source files)
- **Corpus 4 (Theia surface)** — same files as corpus 3 (the substrate IS the surface for this domain)
- **Wave-A cross-reference** — `wave-a-m0..m5-reconciliation-matrix.md` (consumed for ORPHAN/CONTRADICTION cross-checks: `m0.anuttara.languageMap` orphan-fill on M0; `m1.paramasiva.kleinTopology` SPEC-AHEAD on M1; M2 `shem-asma` view collapse contradiction; M3 `cosmicWheel` aligned; M4 `dayContainer` vault-path contradiction; M5 `metaConversation` doc-ahead)

## Master 4-way matrix

| # | Claim (UX doc) | Spec authority (M' Seed) | Code/substrate evidence | Theia surface | Status |
|---|---|---|---|---|---|
| TS-01 | Shell `0` is the cosmic-side parent (M1'–M3' clock-cymatic preview); shell `1` is the personal-side parent (M4'/M5'/M0' return); they are the (0/1) matheme toggle, not two screens (Anuttara UX §3; M'-SYSTEM-SPEC L94–148) | M'-SYSTEM-SPEC defines `0 = cosmic`, `1 = personal`, `/` = OmniPanel, `M0–M5` = full-depth subsystem pages; the shell 0/1 toggle is `#` applied to user context | `pratibimba-layouts/src/common/layout-types.ts` exposes `daily-0-1` + `ide-deep` descriptors with widget manifests; switcher + commands + status-bar present | `PRATIBIMBA_LAYOUT_DAILY_0_1` / `PRATIBIMBA_LAYOUT_IDE_DEEP` named in `layout-types.ts`; `LayoutSwitcher` browser service mounted | **ALIGNED** on the two-layout existence; **CONTRADICTION** on naming semantics — see TS-02 |
| TS-02 | The (0/1) shell split IS the matheme's (0/1) parent — two faces of one state (M'-SYSTEM-SPEC §"The Shell 0/1 Split IS the (0/1) Inversionary Parent") | The two layouts are the **same coordinate** seen from cosmic vs personal faces (state continuity required: lens, mode, profile generation, tick) | Code names them `daily-0-1` ("First-mounted lean free-flow workspace") and `ide-deep` ("Summoned heavy IDE workbench"); descriptions frame them as "lean daily" vs "heavy IDE" — NOT cosmic-face vs personal-face | The `daily` label collapses BOTH cosmic-shell-0 and personal-shell-1 into one "daily" layout; the deep IDE conflates with `4+2` subsystem depth | **CONTRADICTION** — spec asserts three distinct layers (`0`, `1`, `4+2`); code currently expresses **two** layouts (`daily-0-1`, `ide-deep`). Either (a) `daily-0-1` legitimately holds both shell-0 and shell-1 simultaneously and the toggle is intra-layout, or (b) we need a third layout / a within-layout cosmic⇄personal toggle. Resolution required — see DR-TS-1 |
| TS-03 | `/` OmniPanel is the S0' command membrane, always cross-cutting, present in every layout (M'-SYSTEM-SPEC L96; Anuttara UX §3) | OmniPanel is canonical `/` membrane; must show current (0/1) state, active subsystem, in-flight agent invocations | `omnipanel-shell` package exists with `OmniPanelWidget`, `OmniPanelContribution`, `OmniPanelTab[]` manifest, `CrossLayoutIntent` envelope (`common/omnipanel-types.ts` L86–97) | `omnipanel.shell` widget id reused across BOTH layouts in `layout-types.ts` `expectedWidgets`; tabs `overview/chat/logs/settings` visible in both, deep-only tabs hidden in `daily-0-1` | **ALIGNED** (extension exists, widget hosted, tabs typed, intent envelope typed) |
| TS-04 | Six M' surfaces (M0' anuttara, M1' paramasiva, M2' parashakti, M3' mahamaya, M4' nara, M5' epii) each as a full-depth page (the `4+2` body) | M'-SYSTEM-SPEC + each Mn'-SPEC: one extension per subsystem, KernelBridgeAPI-mediated only | Six packages present in `Body/M/epi-theia/extensions/`: `m0-anuttara`, `m1-paramasiva`, `m2-parashakti`, `m3-mahamaya`, `m4-nara`, `m5-epii` | Each registers widget IDs declared in `contracts/07-t0-extension-contract-preflight.json` (`m0.anuttara.*`, `m1.paramasiva.*`, etc.) | **ALIGNED** (substrate hosts all six) |
| TS-05 | Two integrated plugins: 1-2-3 Cosmic Engine (carries `137 = 64+72+1` at M1/M2/M3) and 4-5-0 Jiva-is-Siva recognition (privacy-first M4/M5/M0) | `08-t0-composition-contract-preflight.md` §"Integrated Plugins"; both consume the same KernelBridgeAPI; M4 protected-local never crosses public bridge | `plugin-integrated-1-2-3` deps on `m1-paramasiva`+`m2-parashakti`+`m3-mahamaya`; `plugin-integrated-4-5-0` deps on `m4-nara`+`m5-epii`+`m0-anuttara`; both depend on `integrated-composition` | Both Theia extensions present, declare `theiaExtensions.frontend` modules | **ALIGNED** at extension-presence level; downstream readiness depends on M1/M2/M3 kernel-bridge profile fields (see Wave-A M1.2-M2.3, M3.4 contradictions) |
| TS-06 | Extension-contract preflight + composition-contract preflight exist and enforce the no-direct-S0/S2/S3/S5-import discipline | `07-t0-extension-contract-preflight.{md,json}`: nine-entry readiness taxonomy, shared bridge adapter, route convention `epi-logos://ide/<extension>/<surface>`; `08-t0`: shared `Body/M/epi-theia/shared` shapes, same taxonomy, same forbidden imports | Two contract markdowns + two JSONs present; readiness taxonomy of 9 ids (`bridge_unavailable`, `profile_missing_field`, `s2_graph_blocked`, …, `ready_public_current`) | Validators referenced: `Body/M/epi-theia/extensions/scripts/validate-extension-contract-preflight.mjs`; `Body/M/epi-theia/extensions/test/validate-{extension,composition}-contract-preflight.test.mjs` | **ALIGNED** (preflight authoritative + validators named) |
| TS-07 | `ide-shell-m0-m5` hosts M0/M5 chrome: Bimba graph viewer, Canon Studio, Agentic Control Room, coordinate tree, Logos Atelier, evidence + review + autoresearch panes | `M5'-SPEC` + the M5 capacity workflows; Anuttara UX §10 names Logos Atelier + Bimba graph as M0' surfaces | Browser widgets present: `bimba-graph-viewer-widget`, `canon-studio-widget`, `agentic-control-room-widget`, `coordinate-tree-widget`, `logos-atelier-widget`, `evidence-pane-widget`, `review-pane-widget`, `autoresearch-pane-widget`, `bridge-gate.tsx` | All eight widgets land via `ide-shell-m0-m5` frontend-module, activate in `ide-deep` layout (per package.json description) | **ALIGNED** (shell chrome lands the eight named widgets) |
| TS-08 | Agentic Control Room is M5-4 (one of six operational capacities; Pi/Sophia/Anima/Aletheia constitutional agents) and must enforce human-required gate (IOD-17 three-way parity) | `M5'-SPEC` epii-operational-capacities; Wave-A M5 contradiction DR-M5-1 (Pi as constitutional agent) | `@pratibimba/agentic-control-room` extension exists, T8 deliverable; tests `run-flow.test.mjs`, `human-gate.test.mjs`, `evidence-envelope.test.mjs` named | `agentic-control-room` package depends on `ide-shell-m0-m5` (lives INSIDE the shell-hosted ACR slot) | **ALIGNED** at hosting; Wave-A M5 carries the constitutional-agent decision register |
| TS-09 | `kernel-bridge` is the single boundary every surface consumes; `kernel-bridge-readiness` surfaces readiness inline | `07-t0`: `KernelBridgeAPI` capabilities `readCurrentProfile`, `readPointerAnchor`, `readReadiness`, `subscribeObservability`, `invokeGatewayRpc`, `depositKernelObservation`, `requestReviewEvidence`; subscriptions `profile`, `connection_status`, `observability` | Both extensions present (`kernel-bridge` + `kernel-bridge-readiness`); ide-shell + omnipanel-shell + body-lite + ACR all dep on `@pratibimba/kernel-bridge` | `bridge-gate.tsx` present in `ide-shell-m0-m5/src/browser/` (consumes readiness gate) | **ALIGNED** at substrate; Wave-B `kernel-bridge` sibling matrix owns the profile-field readiness ledger |
| TS-10 | The `daily-0-1` lean layout must include: journal entry, agent check-in, lightweight cymatic placeholder, bridge-readiness status, compact OmniPanel | UX §3 + body-lite-surface package: review-alert badge, agent check-in widget, safe-source-handle row, four typed deep-link intents | `daily-0-1` `expectedWidgets`: `pratibimba.daily.journal`, `pratibimba.daily.agent-checkin`, `pratibimba.daily.cymatic-placeholder`, `pratibimba.daily.status-display`, `pratibimba.omnipanel.shell` | `body-lite-surface` extension lands the daily-layer chrome; gated to `daily-0-1` only per package description | **ALIGNED** at expected-widget manifest; the actual `pratibimba.daily.*` widget contributors must trace to `body-lite-surface` browser modules (see TS-15) |
| TS-11 | Cross-layout state identity: same coordinate / lens / profile generation / tick must survive the shell-0 ↔ shell-1 ↔ subsystem-page traversal (M'-SYSTEM-SPEC L145–147) | The (0/1) toggle is `#` over current context; subsystem-page open is `.` nesting; state preserved by kernel-bridge DI singletons (`layout-types.ts` L7-12) | `CrossLayoutIntent` envelope typed: `coordinate`, `artifactUri`, `reviewId`, `dayNow`, `sessionKey`, `profileGeneration`, `privacyClass`, `requestedLayout`, `requestedExtensionId`, `requestedContributionId` (`omnipanel-types.ts` L86-97) | `cross-layout-intent-dispatcher.ts` present in `pratibimba-layouts/src/browser/`; `session-state-service.ts` present | **ALIGNED** at typed contract + dispatcher; **SPEC-AHEAD** on the full T5 cross-layout intent routing impl (T2 scaffold note at top of `omnipanel-types.ts`) — see closing tranche 11.5 |
| TS-12 | Subsystem pages reachable from either shell with full state continuity (M'-SYSTEM-SPEC L146) | The OmniPanel must always be able to show current (0/1) state, active subsystem, in-flight agent invocations | `CrossLayoutIntent.requestedExtensionId` + `requestedContributionId` typed; dispatcher present | Dispatcher exists; per-extension intent handlers (m0 .. m5) need verification | **SPEC-AHEAD** — typed envelope lands; per-extension `requestedContributionId` consumers in each `mN-*` extension need an audit (closing 11.6) |
| TS-13 | `Body/M/epi-tauri` is deprecated migration-source only (standing invariant) | M-PRIME-CYCLE-3 kickoff standing invariant; `MIGRATION-SOURCES.md` enumerates per-file ports + retired set | `MIGRATION-SOURCES.md` lists every epi-tauri source file as ported OR retired with reason | epi-tauri is NOT in the active extensions tree | **ALIGNED** (named deprecation honored) |
| TS-14 | Acceptance harness verifies the full shell composition end-to-end (bridge + OmniPanel + ide-shell + ACR) | `M'-SYSTEM-SPEC` release discipline; ADR-05-011 release ADR | `acceptance-harness` extension present with `topology.test.mjs` + `acceptance-plan.test.mjs`; boots real gateway/SpaceTimeDB/Neo4j/Redis/S5 | Headless Node script + Theia-side acceptance contribution + operator runbook all named in package | **ALIGNED** at scaffold; harness coverage of `daily-0-1` vs `ide-deep` distinction + cross-layout intent traversal is a SPEC-AHEAD closure (11.7) |
| TS-15 | Daily-layer widgets (`pratibimba.daily.journal`, `pratibimba.daily.agent-checkin`, `pratibimba.daily.cymatic-placeholder`, `pratibimba.daily.status-display`) declared in layout `expectedWidgets` (`layout-types.ts` L52-58) | UX §3 + body-lite-surface package description | `body-lite-surface` exists but is described as Track 09 T9b "Pairs with the deep Agentic Control Room"; the four daily-layer widget IDs above are NOT obviously contributed by it from its package metadata | Layout manifest names them; widget contributor mapping not confirmed in this read | **SPEC-AHEAD** (or potentially **ORPHAN** if no extension owns `pratibimba.daily.journal/agent-checkin/cymatic-placeholder/status-display`) — closing tranche 11.8 |
| TS-16 | M2' `m2.parashakti.correspondenceTree` view in contract preflight collapses the six addressing-axes of 72 (per Wave-A M2 contradiction on `shem-asma`) | Cross-reference Wave-A M2 finding (M2 17th-lens-shaped question) | `07-t0-extension-contract-preflight.json` declares `m2.parashakti.correspondenceTree` (not `correspondenceBrowser`) | Theia widget id consumed by m2-parashakti extension | **CONTRADICTION (downstream)** — owned by Wave-A M2 tranche 3.6; shell merely hosts; flagged here for surface→extension→contract closure (no shell-side fix) |
| TS-17 | M4' `m4.nara.dayContainer` vault path discrepancy (Wave-A M4 contradiction: `${VAULT}/Pratibimba/Nara/{day_id}/` spec vs `${vaultRoot}/day/{dayId}/` extension) | Cross-reference Wave-A M4 finding | `07-t0-extension-contract-preflight.json` declares `m4.nara.dayContainer` view; vault path is internal to `m4-nara` extension, not shell | Surface lands; data-path resolution is M4-owned | **CONTRADICTION (downstream)** — owned by Wave-A M4 tranche 5.2; flagged for shell-acceptance harness to assert exactly one path |
| TS-18 | `M0'` six-data-layer engagement system (full CRUD under governance — Anuttara UX §7) | Anuttara UX + Wave-A M0 contradiction (CRUD vs governed-route) | The ide-shell hosts `bimba-graph-viewer-widget` + `canon-studio-widget` + `coordinate-tree-widget`; CRUD scope unclear at shell level | Surface hosting present; CRUD governance is method-routing-owned not shell-owned | **CONTRADICTION (downstream)** — owned by Wave-A M0 tranche 1.2; shell hosts both possibilities so it does not need a separate decision |
| TS-19 | `m-extension-runtime` is the shared runtime package every M' extension consumes (typed gateway clients, DI singletons, shared types) | Migration-sources declares `m-extension-runtime` as T1-done, absorbs shared contracts from epi-tauri + epi-app shared/ | Package present at `Body/M/epi-theia/extensions/m-extension-runtime/`; every M-extension package + integrated plugins + body-lite + ACR dep on it | All six per-Mn extensions consume `@pratibimba/m-extension-runtime` | **ALIGNED** |
| TS-20 | Theia is the canonical M' shell (vs deprecated epi-tauri) — `/pratibimba/system` + `Body/M/epi-theia` is the M' Theia shell authority | Standing invariant (kickoff + memory note `project_theia_shell_moved_to_epi_theia.md`) | `Body/M/epi-theia/extensions/` is the active substrate tree (16 directories: 6 Mn-extensions + integrated-composition + 2 integrated plugins + ide-shell + omnipanel-shell + pratibimba-layouts + kernel-bridge + kernel-bridge-readiness + m-extension-runtime + agentic-control-room + body-lite-surface + acceptance-harness + contracts + scripts + test) | All Theia-side; epi-tauri is migration-source-only | **ALIGNED** (the residual UX prose referencing "Tauri implementation" in M'-SYSTEM-SPEC §"The Shell 0/1 Split" is stale — flagged in TS-21) |
| TS-21 | M'-SYSTEM-SPEC §"The Shell 0/1 Split" L143 says "This is load-bearing for the Tauri implementation" but Tauri is deprecated; also M'-TAURI-PORT-SPEC.md exists as a spec authority though the tauri target is retired | Standing invariant: epi-tauri deprecated → spec wording needs an audit-and-rename pass | M'-TAURI-PORT-SPEC.md `0/1/4+2 discipline` reference is correct in substance; the carrier name is stale | Spec text only — no Theia surface change | **DOC-AHEAD (residual stale wording)** — spec wording must rename "Tauri implementation" → "M' Theia shell implementation" (closing tranche 11.9, parallel to Wave-A M1 tranche 2.1 audit-pattern) |
| TS-22 | Each M-extension contract declares `forbiddenDirectImports` for `Body/S/S0`, `Body/S/S2`, `Body/S/S3`, `Body/S/S5`, plus per-extension-specific (`portal-core`, `neo4j-driver`, `@clockworklabs/spacetimedb-sdk`, `redis`, `epii-review-core`, `epii-agent-core`) | `07-t0` `sharedBridgeAdapter.forbiddenDirectImports` (10 entries) + per-extension overrides | JSON fixture present; validator `validate-extension-contract-preflight.mjs` named | Forbidden-import enforcement runs in validators (not at TypeScript compile yet) | **SPEC-AHEAD** on a real forbidden-import lint that runs in CI against the actual source trees of the six Mn-extensions (closing tranche 11.10) |
| TS-23 | Smart-connections sidebar + Canon Studio + vault-bridge are forward-not-yet-scaffolded extensions named in `MIGRATION-SOURCES.md` | `MIGRATION-SOURCES.md` "Forward extensions not yet scaffolded" section | NOT present as packages: `smart-connections-sidebar/`, `vault-bridge/`, `canon-studio/`, `shared-services/`, optional `epi-claw/` | Layout `ide-deep.expectedWidgets` references `pratibimba.smart-connections-sidebar` — surface declared but contributor absent | **CODE-PENDING (named, gated)** — gating documented in migration-sources; the `pratibimba.smart-connections-sidebar` widget id is currently an unfulfilled layout claim — closing tranche 11.11 |
| TS-24 | OmniPanel must expose tabs that route into both layouts; deep-only tabs hidden in `daily-0-1` | UX §3 + `omnipanel-types.ts` L51-56 comment | `OMNIPANEL_TABS` array declares `availableInLayouts` per tab; deep-only tabs (`channels`, `sessions`, `nodes`, `models`, `skills`, `cron`, `config`, `instances`, `debug`) hidden in `daily-0-1`; cross-layout tabs (`overview`, `chat`, `logs`, `settings`) visible in both | OmniPanel widget consumes `OMNIPANEL_TABS`; filter not yet wired in fully (T2 scaffold note) | **SPEC-AHEAD** — typed manifest present; wiring of `availableInLayouts` filter against the active layout preference (`epi-logos.layout.active`) needs verification (closing tranche 11.12) |
| TS-25 | The `daily-0-1` ⇄ `ide-deep` toggle preserves selected coordinate, session key, DAY/NOW, profile generation, bridge subscription via kernel-bridge DI singletons (NOT layout system) | `layout-types.ts` L7-12 doc-comment makes this explicit | Code design statement matches spec; the singletons live in `kernel-bridge` extension | Cross-layout state continuity test absent from named test scripts | **SPEC-AHEAD** — explicit acceptance-harness test asserting "toggle daily-0-1 ⇄ ide-deep with selected M3 codon preserved" needed in `acceptance-harness` (closing tranche 11.7) |

## Anomalies

### CONTRADICTIONS (decision-register candidates)

- **DR-TS-1** — Two-layout vs three-stratum naming: spec says `0` cosmic / `1` personal / `/` membrane / `4+2` depth (four distinct strata); code expresses two layouts (`daily-0-1`, `ide-deep`) collapsing shell-0 + shell-1 into one "daily" layout. Either (a) the (0/1) toggle is intra-layout within `daily-0-1` (typed somewhere not yet found) or (b) we need an additional layout / surface mode. Cannot be decided by Wave-B alone — routes to user final-validation.
- **DR-TS-2** (downstream of Wave-A M2 3.6) — M2 `m2.parashakti.correspondenceTree` widget id collapses Shem (8×9=72) and Asma (99+1) into one tree view; cycle-3 must decide whether the surface remains one tree with axis filter (six addressing axes) or splits.
- **DR-TS-3** (downstream of Wave-A M0 1.2) — M0' full-CRUD-under-governance vs governed-route: ide-shell hosts both `coordinate-tree-widget` (CRUD-flavored) and `canon-studio-widget` (governed-write via bridge). Shell hosting does not need a decision but downstream method-routing does.
- **DR-TS-4** (downstream of Wave-A M4 5.2) — `m4-nara` vault path: spec says `${VAULT}/Pratibimba/Nara/{day_id}/`, extension writes `${vaultRoot}/day/{dayId}/`. Shell-acceptance-harness must assert exactly one.

### CODE-PENDING / ORPHAN

- **TS-15** — daily-layer widgets named in `daily-0-1.expectedWidgets` (`pratibimba.daily.journal`, `pratibimba.daily.agent-checkin`, `pratibimba.daily.cymatic-placeholder`, `pratibimba.daily.status-display`) — their extension owner from the read so far is unclear (`body-lite-surface` package description names different artifacts: review-alert badge, agent check-in widget, safe-source-handle row, four deep-link intents). Possible **ORPHAN**: a named layout-expected widget without an extension owner. Closing 11.8 traces this.
- **TS-23** — `pratibimba.smart-connections-sidebar` declared in `ide-deep.expectedWidgets` but extension `smart-connections-sidebar/` not yet scaffolded (gated on Track 03 T6.5 per `MIGRATION-SOURCES.md`).
- **TS-22** — forbidden-import lint exists as preflight JSON authority but no source-tree CI lint enforcement confirmed.
- **TS-21** — Residual "Tauri implementation" wording in M'-SYSTEM-SPEC + the entire `M'-TAURI-PORT-SPEC.md` title need a stale-carrier audit (epi-tauri deprecated).
- **TS-25** — Cross-layout state-identity preservation test absent from named acceptance-harness test files.

## Proposed Cycle-3 Closing Tranches (theia-shell domain — id space `11.x`)

> All tranches obey the anti-greenfield rule: every one targets a M' product surface, an extension-contract closure, a decision register, or a named integration blocker. None propose re-building landed `Body/M/epi-theia/extensions/*` packages.

### 11.1 — Shell-0 / Shell-1 / 4+2 / `/` separation invariant audit and decision

- **Classification:** `contradiction-decision`
- **Source:** TS-01, TS-02, DR-TS-1
- **Deliverable:** Decision-register entry DR-TS-1 deciding whether `daily-0-1` layout holds **both** shell-0 (cosmic) and shell-1 (personal) as an intra-layout (0/1) toggle (and if so, the toggle widget contribution that lands it) OR whether a third layout/mode is required. Update `pratibimba-layouts/src/common/layout-types.ts` description text + (if intra-layout) name the toggle widget. Audit M'-SYSTEM-SPEC §"Shell layer" to assert exactly the chosen pattern.
- **Verification:** `pnpm --filter @pratibimba/pratibimba-layouts test` (rebuilds + runs); `node --test Body/M/epi-theia/extensions/test/validate-extension-contract-preflight.test.mjs`; grep-no-residual-ambiguity in spec.

### 11.2 — Cross-layout intent routing T5 promotion (consume `CrossLayoutIntent` envelope as-is, finish wiring)

- **Classification:** `spec-ahead-integration`
- **Source:** TS-11, TS-12, TS-24
- **Deliverable:** Finish T5 promotion of `cross-layout-intent-dispatcher.ts` — wire each of the six M-extensions to consume `CrossLayoutIntent.requestedExtensionId` + `requestedContributionId`; wire `OMNIPANEL_TABS.availableInLayouts` filter against `epi-logos.layout.active` preference. Extend, do NOT re-scaffold.
- **Verification:** `pnpm --filter @pratibimba/omnipanel-shell test`; `pnpm --filter @pratibimba/pratibimba-layouts test`; integration test in `acceptance-harness` exercising "OmniPanel intent → daily-0-1 → ide-deep transition with M3 codon preserved" (added to `tests/topology.test.mjs`).

### 11.3 — Daily-layer widget ownership trace + ORPHAN closure

- **Classification:** `no-orphan-fill`
- **Source:** TS-10, TS-15
- **Deliverable:** Trace the four `pratibimba.daily.journal | agent-checkin | cymatic-placeholder | status-display` widget ids to either (a) `body-lite-surface` contributions to be renamed/aligned, or (b) a named owning extension. If no owner exists, either downgrade the layout claim (remove from `expectedWidgets`) or land a `pratibimba-daily-widgets` extension under `Body/M/epi-theia/extensions/` (first-build allowed only because no extension currently owns these named M' product surfaces).
- **Verification:** Extension presence at `Body/M/epi-theia/extensions/<owner>/`; widget id registry assertion in `validate-extension-contract-preflight.test.mjs` extended with `expectedWidgets` ↔ extension-contributor mapping; `pnpm --filter @pratibimba/body-lite-surface test`.

### 11.4 — Smart-connections-sidebar layout-claim hold (CODE-PENDING acknowledgment)

- **Classification:** `code-pending-closure`
- **Source:** TS-23
- **Deliverable:** Add a `code-pending` marker on `ide-deep.expectedWidgets` `pratibimba.smart-connections-sidebar` entry referencing the gating ("Track 03 T6.5"); ensure layout-switcher does not throw on missing widget; document in `MIGRATION-SOURCES.md` extension table. No rebuild — gated-on dependency.
- **Verification:** Layout-switcher test with stub widget; `pnpm --filter @pratibimba/pratibimba-layouts test`.

### 11.5 — Forbidden-direct-import lint enforcement against the six M-extension source trees

- **Classification:** `spec-ahead-integration`
- **Source:** TS-22
- **Deliverable:** Extend `Body/M/epi-theia/extensions/scripts/validate-extension-contract-preflight.mjs` to grep each of `Body/M/epi-theia/extensions/{m0-anuttara,m1-paramasiva,m2-parashakti,m3-mahamaya,m4-nara,m5-epii}/src/**/*.ts` for `forbiddenDirectImports` strings + fail on hit. Consume the existing contract authority as-is.
- **Verification:** `node Body/M/epi-theia/extensions/scripts/validate-extension-contract-preflight.mjs`; `node --test Body/M/epi-theia/extensions/test/validate-extension-contract-preflight.test.mjs`.

### 11.6 — Acceptance harness assertion: cross-layout state-identity preservation

- **Classification:** `spec-ahead-integration`
- **Source:** TS-25, TS-11
- **Deliverable:** Extend `Body/M/epi-theia/extensions/acceptance-harness/tests/topology.test.mjs` with an end-to-end assertion that toggling `daily-0-1` ⇄ `ide-deep` with a selected M3 codon + active session + profile generation preserves all of (`coordinate`, `lens`, `mode`, `profileGeneration`, `sessionKey`, `dayNow`) — the kernel-bridge DI singleton contract from `layout-types.ts` L7-12 made executable.
- **Verification:** `pnpm --filter @pratibimba/acceptance-harness test`.

### 11.7 — Spec-side stale "Tauri implementation" wording audit

- **Classification:** `doc-ahead-landing`
- **Source:** TS-21
- **Deliverable:** Audit `Idea/Bimba/Seeds/M/M'-SYSTEM-SPEC.md` §"The Shell 0/1 Split IS the (0/1) Inversionary Parent" L143 + `M'-TAURI-PORT-SPEC.md` for residual "Tauri" wording — downgrade where the substance still applies to Theia, retire where it does not. Parallel pattern to Wave-A M1 tranche 2.1 (residual M0-witness wording audit). NOT a Theia-substrate change.
- **Verification:** `grep -rn "Tauri implementation\|tauri-side\|epi-tauri target" Idea/Bimba/Seeds/M/*.md` returns nothing not explicitly marked deprecated.

### 11.8 — Integrated-plugin readiness gate against Wave-A profile-field findings

- **Classification:** `spec-ahead-integration` (cross-references Wave-B kernel-bridge tranche `10.x`)
- **Source:** TS-05, Wave-A M1.2 (klein_flip), M2.3 (klein_flip), M3 (no code-pending in matrix but 17th-lens decision pending), M4.5 (cymatic solver)
- **Deliverable:** `plugin-integrated-1-2-3` + `plugin-integrated-4-5-0` must each declare typed `IntegratedReadiness` blocked states that consume the Wave-A `pending-*` markers from `MathemeHarmonicProfile` (`klein_flip_state`, `resonance72`, `planetaryChakral`, etc.) and render via the shared composition contract — extension of existing `integrated-composition` package, no rebuild.
- **Verification:** `pnpm --filter @pratibimba/integrated-composition test`; `pnpm --filter @pratibimba/plugin-integrated-1-2-3 test`; `pnpm --filter @pratibimba/plugin-integrated-4-5-0 test`; fixture parity with the kernel-bridge readiness ledger.

### 11.9 — Surface→extension→contract closure ledger (the master traceability artefact for the shell domain)

- **Classification:** `aligned-only-note` + `no-orphan-fill`
- **Source:** TS-04, TS-07, TS-10, TS-15, TS-23
- **Deliverable:** Single ledger JSON at `Body/M/epi-theia/extensions/contracts/surface-extension-contract-ledger.json` mapping every widget id appearing in `pratibimba-layouts/src/common/layout-types.ts` to its (extension package, contract entry, status). Closes the no-orphan audit at shell scope.
- **Verification:** `node --test Body/M/epi-theia/extensions/test/surface-extension-contract-ledger.test.mjs` (new, sibling to existing validators); validator asserts every `expectedWidgets` id has an owner or a `code-pending` marker.

---

**End of matrix.** The shell substrate is largely **ALIGNED** at presence level: all six Mn-extensions, both integrated plugins, both contract preflights, kernel-bridge + readiness, OmniPanel, layouts, ide-shell chrome, agentic-control-room, body-lite, acceptance-harness, m-extension-runtime are all packages-on-disk under `Body/M/epi-theia/extensions/`. The genuine cycle-3 work concentrates on (a) the **DR-TS-1 (0/1/4+2//) layout-vs-stratum decision**, (b) **daily-layer widget ORPHAN trace**, and (c) **cross-layout intent + state-identity** completion. Downstream Wave-A M0/M2/M4 contradictions surface as shell-acceptance-harness assertions, not shell substrate rebuilds.
