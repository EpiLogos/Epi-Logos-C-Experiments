# 43.T43.1 Repo-Hygiene Audit

Task: `43.T43.1` - repo-hygiene sweep: immediate fixes plus gated legacy removal.

Last verification pass: 2026-06-17.

VAK route for this run:
- `vak_evaluate`: CPF `C2`, CT `CT4b`, CP `CP4`, CF `(0/1/2)` -> `eros`, CFP `CFP0`, CS `Day`.
- Dispatch posture: execution and verification. No code symbols were edited; GitNexus symbol-impact analysis was not applicable.

## Immediate Hygiene Ledger

| Surface | Intended disposition | Current observation | Status |
|---|---|---:|---|
| `.clangd` include paths | Point at `Body/S/S0/epi-lib/include` and `Body/S/S0/vendor/blake3` | Present: `-IBody/S/S0/epi-lib/include` and `-IBody/S/S0/vendor/blake3`. | Pass |
| `.gitignore` migration patterns | Ignore `Body/**/test/bin/`, `Body/**/*.app/`, and `target/` | Patterns are present. | Present |
| Root `epi_logos.{c,h}` | Quarantine under `Body/_quarantine/root-spec-seed/`; root copies untracked | `git ls-files epi_logos.c epi_logos.h epi_logos` -> `0`; quarantine contains `epi_logos.c` and `epi_logos.h`. | Pass |
| `Body/S/S0/epi-lib/include/m0_calculus.h` | Header readable by non-owner UID | `stat` reports mode `644`. | Pass |
| `.env.graph-dev` paths | Use migrated `Body/S/S3/redis-context/scripts/redisvl_cache_service/...` path | Present for both `EPILOGOS_SEMANTIC_CACHE_PYTHON` and `EPILOGOS_SEMANTIC_CACHE_SCRIPT`. | Pass |
| `target/` tracked artifacts | Remove from Git index, keep ignored on disk | `git ls-files target/` -> `3719`. | Failing in current worktree |
| `Body/S/S0/epi-lib/test/bin/` binaries | Remove from Git index, keep ignored on disk | `git ls-files Body/S/S0/epi-lib/test/bin` -> `21`. | Failing in current worktree |
| `Body/S/S3/epi-app/EpiLogos-Dev.app/` bundle | Remove from Git index, keep ignored on disk | `git ls-files 'Body/S/S3/epi-app/EpiLogos-Dev.app/**'` -> `4`. | Failing in current worktree |

Attempted correction, re-run 2026-06-17:

```bash
git rm -r -q --cached target Body/S/S0/epi-lib/test/bin Body/S/S3/epi-app/EpiLogos-Dev.app
```

Result:

```text
fatal: Unable to create '/Users/admin/Documents/Epi-Logos C Experiments/.git/index.lock': Operation not permitted
```

The tracked artifact cleanup is therefore blocked in this Codex filesystem profile because `.git/index` is not writable. The working tree can be read and normal files can be written, but the required non-destructive index update cannot be applied from this session.

## Gated Legacy Removal Ledger

| Legacy surface | Gate | Disposition |
|---|---|---|
| `Body/S/S3/epi-app/` tracked source | [[11-theia-shell-surface-hosting]] | Migration source only; remove when Track 11 Theia-shell parity closes. Current tracked count: `git ls-files Body/S/S3/epi-app` -> `209` including the app bundle entries above. |
| `extensions/MIGRATION-SOURCES.md` and stale `epi-tauri` references | [[11-theia-shell-surface-hosting]] | Remove with Track 11 close. |
| `m1-paramasiva-played-torus` vs `m1-paramasiva` | [[02-m1-paramasiva-reconciliation]] and DR-M1-2 | Resolve duplicate disposition under Track 02. DR-M1-2 says the played-K2 Bevy/wgpu surface is canonical; confirm the loser before removal. |
| `Body/S/S4/ta-onta/S4-2p-pleroma/staged/` | [[12-agentic-layer-s4-s5]] | Promote or quarantine under Track 12. Current tracked staged count: `git ls-files 'Body/S/S4/ta-onta/S4-2p-pleroma/staged/**'` -> `32`. |

## Keep List

Do not sweep:
- `Idea/Bimba/Seeds/**/Legacy` - canon document archive governed by `LEGACY-DOCS-MIGRATION-MANIFEST.json`.
- `Body/S/S4/pi-agent/` - still live-referenced; retire only when claw-rust lands.
- Declared `.gitmodules` vendors.

## Verification Snapshot

Commands run:

```bash
git ls-files target/ | wc -l
git ls-files Body/S/S0/epi-lib/test/bin | wc -l
git ls-files 'Body/S/S3/epi-app/EpiLogos-Dev.app/**' | wc -l
git ls-files epi_logos.c epi_logos.h epi_logos | wc -l
stat -f '%Lp %N' Body/S/S0/epi-lib/include/m0_calculus.h
git ls-files 'vendor/legacy/epi-tauri/**' 'vendors/**/depwire-output.json' 'Idea/.smart-env/**' 'Body/S/S3/epi-app/dist/**' 'Body/S/S3/epi-app/node_modules/**' 'Body/_quarantine/tmp-taonta-analysis/**' 'Body/_quarantine/epi-cli-dot-epi-runtime/session.json' | wc -l
git ls-files | grep -E 'epi-app/|epi-tauri' | wc -l
git ls-files 'Body/S/S4/ta-onta/S4-2p-pleroma/staged/**' | wc -l
```

Observed counts:
- `target/`: `3719` tracked files.
- `Body/S/S0/epi-lib/test/bin`: `21` tracked files.
- `Body/S/S3/epi-app/EpiLogos-Dev.app`: `4` tracked files.
- Root `epi_logos` seed/binary: `0` tracked files.
- `m0_calculus.h`: mode `644`.
- Filesystem-only clutter list: `0` tracked files.
- `epi-app/|epi-tauri`: `211` tracked paths; this remains expected until the Track 11 gate closes, except for the app bundle entries that should be untracked now.
- `S4-2p-pleroma/staged`: `32` tracked paths; remains gated by Track 12.

## Closeout

This audit artifact is current, but the tranche cannot be honestly marked `done` in this session because the required tracked-artifact verification still fails and the non-destructive Git index cleanup is blocked by `.git/index.lock` write permission. The remaining immediate fix is an index-only cleanup:

```bash
git rm -r --cached target Body/S/S0/epi-lib/test/bin Body/S/S3/epi-app/EpiLogos-Dev.app
```

After that succeeds, re-run:

```bash
git ls-files target/ Body/S/S0/epi-lib/test/bin | wc -l
git ls-files 'Body/S/S3/epi-app/EpiLogos-Dev.app/**' | wc -l
```

Expected counts are `0` and `0`.
