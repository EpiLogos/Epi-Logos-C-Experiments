# Wave C — M1' Paramaśiva Frontend Deep Design Matrix

**Task id:** `wave-c-m1-paramasiva-frontend`
**Domain:** per-extension widget UX for the M1' Paramaśiva surface (`Body/M/epi-theia/extensions/m1-paramasiva/` + `Body/M/epi-theia/extensions/m1-paramasiva-played-torus/`) beyond what Tracks 15.8 / 15.9 already contract for the K² played-torus vortex visual itself.
**Anti-greenfield posture:** Two extensions are landed substrate at directory level: `m1-paramasiva` (React clock-instrument, scaffold with `clockInstrument` + `kleinTopology` + `audioBusInspector` view ids) and `m1-paramasiva-played-torus` (`ARCHITECTURE.md` + `README.md` only — DR-M1-2 ratified, src tree pending). Every tranche below consumes / audits / extends those scaffolds, scaffolds the played-torus directory under the DR-M1-2 ratification, or names a single integration closure. Track 22 does NOT reproduce the vortex visual rendering contract (15.8) or the tick-choreography animation primitive (15.9) or the Kaprekar 6174 pedagogy seed (19.8); those are cross-linked as authorities.

## Source list (corpora actually consulted)

- **Corpus 1 (UX intent)** — `Idea/Pratibimba/System/Subsystems/Paramasiva/paramasiva-ux-full-m1-branch.md` (253 LOC, full — §§0–12 + §5b.1–5b.10 ananda vortex visible-heartbeat)
- **Corpus 2 (Seeds / canon authority)**
  - `Idea/Bimba/Seeds/M/M1'/M1'-SPEC.md` (§§0/1, 1, 6, 9, 10, 11, 13, 14, 15a)
  - `Idea/Bimba/Seeds/M/M1'/M1-2-ANANDA-VORTEX-ARCHITECTURE.md` (§§0–11 + skeleton-event enum at L261–272)
  - `Idea/Bimba/Seeds/M/M1'/m1-prime-paramasiva-instrument.md` (companion)
  - `Idea/Bimba/Seeds/M/M1'/m1-prime-audio-generative-research.md` (`MPrimePerformanceEvent` envelope)
  - `Idea/Bimba/Seeds/M/M1'/physical-pole-stack-architecture.md` (Bevy/wgpu seed)
  - `Idea/Bimba/Seeds/M/M1'/m1-prime-kaprekar-pedagogy.md` (T19.8 LEAN seed)
  - `Idea/Bimba/Seeds/M/M'-SYSTEM-SPEC.md` §"Shell layer" (the `0 / 1 / / / 4+2` stratification that places M1' both inside cosmic-1-2-3 composition and as a standalone 4+2 page)
  - `Idea/Bimba/Seeds/M/alpha_quaternionic_integration_across_M_stack.md` (Mersenne `127 = 2^7 − 1 = M_7` + `+1 → 128` + `+9 → 137` execution-order — surfaced by 19.9)
- **Corpus 3 (Body/S substrate — read-only references; M1 frontend never imports directly)**
  - `Body/S/S0/epi-lib/include/m1.h` — `RING_QUATERNION_LUT[12]`, `CL42_BASIS[6]`, `QL_TRIG_TABLE[6]`, `DR_RING_MAHAMAYA[6]`, `DR_RING_PARASHAKTI[6]`, `SPANDA_SEED_BITS=0x03`, `TORUS_GENUS=1`, `DOUBLE_COVER_DEG=720`, `ANANDA_RING_SIZE=12`, `Ananda_Matrix_Op` / `Spanda_Stage` parallel-track invariant at m1.h:735-756, `VORTEX_5X_CEILING=24`, `VORTEX_6X_STRUCTURE=8`, `QL_DIVINE_ACT_RATIO 16/9` at m1.h:417-422
  - `Body/S/S0/portal-core/src/kernel.rs:346-465` `MathemeHarmonicProfile { tick12, degree720, lens_mode, resonance72, audio_octet[8], nodal_quartet[4], q_cosmic[4], ... }` (landed); `ananda_vortex` + `klein_flip` fields PENDING per Tranches 10.10 / 02.2
  - `Body/S/S0/portal-core/src/parashakti/vimarsha_reading.rs:17-93` (Vimarśa writes — single authority for `audio_octet` / `nodal_quartet`)
  - `Body/S/S0/portal-core/src/hopf.rs`, `quaternion.rs`, `spanda.rs`, `codon_rotation_projection.rs`, `personal_identity.rs` (four-scale Cl(4,2) carriers)
- **Corpus 4 (Theia surface)**
  - `Body/M/epi-theia/extensions/m1-paramasiva/package.json` — `@pratibimba/m1-paramasiva` v0.1.0; theiaExtensions.frontend = `lib/browser/frontend-module`
  - `Body/M/epi-theia/extensions/m1-paramasiva/src/common/index.ts` — EXTENSION_ID, PRIMARY_VIEW_ID, ALL_VIEW_IDS (`clockInstrument`, `kleinTopology`, `audioBusInspector`), DECLARED_BLOCKERS, OBSERVABILITY_EVENT_TYPES (`m1.walk.step`, `m1.klein_flip.source`), TRACK_08_EXPORTS (`M1WalkStrip`, `M1TopologyMiniView`), PRIVACY_CLASS=`public_current_audio_metadata_only`, ROUTE_PATH=`/m1-paramasiva/walk`
  - `Body/M/epi-theia/extensions/m1-paramasiva/src/common/clock-instrument.ts` — `M1ProfileClockModel`, `buildM1ProfileClockModel`, `buildM1RelationWalkStep`, `relationWalkBlockers`
  - `Body/M/epi-theia/extensions/m1-paramasiva/src/browser/m1-paramasiva-extension-body.tsx` — current 5-section render (profile-clock / 84-state landscape / audio-bus / topology / relation-walk-readiness)
  - `Body/M/epi-theia/extensions/m1-paramasiva/src/browser/m1-paramasiva-widget.tsx` — `ReactWidget` subscribing to `onProfile`, `onReadiness`, `onCoordinateContext`
  - `Body/M/epi-theia/extensions/m1-paramasiva/src/browser/frontend-module.ts` — ContainerModule + `M1ParamasivaPublisher` + `registerIntentTarget(..., 'schema', ...)`
  - `Body/M/epi-theia/extensions/m1-paramasiva-played-torus/{ARCHITECTURE.md, README.md}` — DR-M1-2 ratified contract document + brief README; **no src/ or wgpu/ yet**
  - `Body/M/epi-theia/extensions/contracts/07-t0-extension-contract-preflight.json` L190-252 — m1-paramasiva contract (view ids, capability/subscription/forbidden-import set, observability event types, blockers)
- **Wave-A / Wave-B / Track 15 cross-reference** — `plan.runs/wave-a-m1-reconciliation-matrix.md` (substrate); `02-m1-paramasiva-reconciliation.md` (substrate tranche 02.6 = 3D played-torus build under DR-M1-2; 2.2 = klein_flip field landing); `wave-b-theia-shell-matrix.md` (DR-TS-1 shell-0/shell-1/4+2 strata; cross-layout intent envelope; TS-25 state continuity); `15-ui-design-foundations.md` (foundation principles 1–9 + Surface Contracts cosmic/personal/`ide-deep`/OmniPanel/left-sidebar-system; 15.8 vortex visual; 15.9 tick choreography); `19-contemplation-surface-integration.md` (T19.8 Kaprekar LEAN seed; T19.9 7-8-9 spine + Mersenne reading)

## Anti-greenfield posture

Two extensions land already. `m1-paramasiva` ships a complete React/Inversify scaffold with five rendered sections, three view ids reserved, observability event types declared, all bridge-mediated. `m1-paramasiva-played-torus` ships its IDE-side architecture contract and an empty src tree — Tranche 02.6 ratifies the first-build of the directory + Bevy/wgpu toolchain. **Wave C is permitted to scaffold `m1-paramasiva-played-torus/src/browser/` + `package.json` + `wgpu/Cargo.toml` as a DR-M1-2-ratified first-build** but only consumes 15.8 / 15.9 for the vortex visual + tick choreography rendering primitives. Every other surface (Spanda walk navigator, Cl(4,2) signature inspector, Klein-flip event UI, 7-8-9 spine reader, Mersenne / 137 additive proof overlay, Kaprekar inspector, vortex matrices browser, audio_octet / nodal_quartet companion views) is an **extension** of `m1-paramasiva` — new files under `src/browser/` + new view ids registered through the existing `ALL_VIEW_IDS` + new selectors added to the existing contract preflight at `07-t0-extension-contract-preflight.json` L194-196 — not a new extension.

The 2D React `m1-paramasiva` extension stays the inspector/instrument/standalone-IDE surface. The 3D Bevy/wgpu `m1-paramasiva-played-torus` extension is the played topology and the cosmic-1-2-3 composition pole. They are companion surfaces — distinct extensions, both landed in M1' frontend deep design — and Track 22 owns the design that makes them cohere.

---

## The Four-Way Matrix

| # | UX-side claim (paramasiva-ux + M1-2 ANANDA) | Spec-side authority (M1'-SPEC + 15.x + 02.x) | Code/contract evidence (m1-paramasiva + played-torus + preflight) | Theia surface state | Status |
|---|---|---|---|---|---|
| WC-M1-01 | M1' is the **pedagogical home of QL** — relation = interval, position = pitch, traversal = phrase (UX §0, §1) | M1'-SPEC commitment 1; 15.0 foundation principle 1 ("Coordinate as primary navigation"); Track 22 must surface the walk-as-pedagogy at user-visible level | `clock-instrument.ts::M1ProfileClockModel` exposes `tick12`, `degree720`, `position6`, `lensMode`, `ratioRole`, `landscape (12×7=84)`; current body renders these as `<dl>` rows — pedagogically inert; no walk navigator widget | The 84-state landscape renders cells but does NOT expose the *walk* through them (no tick-scrub, no pause, no "you are here in the matheme" affordance) | **DOC-AHEAD** — clock-instrument has all the data; needs a named **Spanda walk navigator** (Track 22 tranche 22.1) |
| WC-M1-02 | The 9/8 epogdoon tick is **self-derived** from `(4/3)×(3/2)=2/1` and `(3/2)÷(4/3)=9/8` (UX §6); learner must see the derivation (Wave-A row 2 DOC-AHEAD) | M1'-SPEC §9; companion instrument doc §0 chain | `m1.h::SPANDA_SEED_BITS=0x03` (substrate); `M1ProfileClockModel.topology` has slots but no `derivation` sub-object; no pedagogy panel | No 9/8 self-derivation readout in any view | **DOC-AHEAD** — add `derivation` projection (the 4:2 ↔ 3:3 chain shown live as the user watches the tick advance) inside the Cl(4,2) signature inspector (22.3) |
| WC-M1-03 | The Spanda walk advances tick-by-tick across 12 ticks per cycle; 360° SO(3) per 12 ticks; 720° SU(2) recognition at 24 ticks (UX §5b.4; ANANDA §6.1) | 15.9 names `quat_slerp` as the load-bearing animation primitive AND requires **pause / scrub** affordance for accessibility | `kernel.rs::tick12 = u8 (0..11)`, `degree720 = u16`; `RING_QUATERNION_LUT[12]` is m1.h:551-564 substrate; no extension-side scrubbable tick widget | OBSERVABILITY_EVENT_TYPES declares `'m1.walk.step'`; the extension publishes but does NOT expose a navigator UI | **CODE-PENDING (consumes 15.9)** — Spanda walk navigator (22.1) consumes the slerp primitive 15.9 contracts; does NOT re-implement it |
| WC-M1-04 | Cl(4,2) signature is structural: `P0=sin(-1), P1=tan(+1), P2=sec(+1), P3=cot(+1), P4=csc(+1), P5=cos(-1)`, `4(+1)+2(-1)=+2`; each position has trig identity (UX §6, M1'-SPEC §11, ANANDA §5.4) | M1'-SPEC §11; Wave-A row 8 SPEC-AHEAD (inspector depth absent) | `m1.h::CL42_BASIS[6]` + `QL_TRIG_TABLE[6]` substrate; profile bus PENDING `ananda_vortex.cl42_signature_at_position: i8` per ANANDA §4.3; current `M1ProfileClockModel` has no Cl(4,2) projection | No Cl(4,2) inspector widget surfaces position-by-position trig identity / halo colour-binary; 15.8 contracts the colour-binary as a **rendering** property on K² — but the inspector READING of "which position, what trig, what signature, what numerator/denominator decomposition" is unowned | **SPEC-AHEAD** — Cl(4,2) signature inspector (22.3) reads the profile-bus `cl42_signature_at_position` + lifts `QL_TRIG_TABLE[6]` numerator/denominator via the bridge (NOT a local fork) and renders the position-by-position breakdown 15.8 cannot |
| WC-M1-05 | Klein-flip is visible at Lens N ↔ Lens N+3 tritone crossings; M1' is the *origin* of the flip M2' consumes (UX §5b.5, M1'-SPEC §6) | M1'-SPEC §6, §14; Tranche 02.2 lands `klein_flip: Option<KleinFlipEvent>` on `MathemeHarmonicProfile`; Tranche 02.3 wires `m1.paramasiva.kleinTopology` view; 15.9 paragraph "At tick 5→6 the Klein-flip event fires on the profile bus" | `clock-instrument.ts::topology.m1OriginKleinFlip` slot present as `string | null`; OBSERVABILITY_EVENT_TYPES declares `'m1.klein_flip.source'`; widget `m1.paramasiva.kleinTopology` view id reserved but **no `.tsx` file yet for that view** | The contract slot lands both sides; the **explicit event-strip** showing klein_flip event arrivals + flip-direction + tick-anchor (5→6 vs other transition) is unowned. 02.3 is a topology wiring tranche; Track 22 owns the **event-strip UI affordance** separately | **CODE-PENDING (consumes 02.2 / 02.3)** — Klein-flip event-strip widget (22.4) is a chronograph component reading the event stream, NOT a topology renderer (which 02.3 owns) |
| WC-M1-06 | 7-8-9 spine: 7 (action / Mersenne `127 = 2^7−1 = M_7`) → 8 (octave-field / return / `128 = 2^7`) → 9 (wholeness / `137 = 128 + 9`) (Tranche 19.9; alpha_quaternionic_integration_across_M_stack.md execution-order) | 19.9 contracts the 7-8-9 spine in INTEGRATED-4-5-0; the per-virtue 9-bit witness vector landing | Substrate carries `VIRTUE_LUT[9]` (m0); `QL_DIVINE_ACT_RATIO 16/9` at m1.h:417-422; no extension-side spine reader | UX §5b describes the additive `64 + 72 + 1 = 137` skeleton-event but does NOT yet read the 7-8-9 spine on a user-facing surface AT session close; M1' surface is the natural home for the spine reading because 7/8/9 are the M1 ring's *closing arithmetic* | **DOC-AHEAD** — 7-8-9 spine reader (22.5) is a session-close affordance on `m1-paramasiva` that reads the substrate's witness vector through the bridge and walks the user through `127 → 128 → 137` as completion arithmetic |
| WC-M1-07 | The Mersenne / 137 additive proof: `127 = 2^7 − 1 = M_7` (M_7 = 2^7 − 1, prime-index 31 = M_5 = 2^5 − 1); `+1 → 128 = 2^7`; `+9 → 137`; this is operationally decisive for the M1-5 "+1 parent" claim (Tranche 02.1 synthesis paragraph; alpha_quaternionic_integration_across_M_stack.md) | 02.1 contradiction-decision synthesis paragraph; 15.8 raw/no-DR proof overlay names `64+72+1=137` but only as a per-tick skeleton-event marker, not as the full additive proof chain | `AnandaSkeletonEvent::Additive137 = 4` declared in ANANDA-VORTEX-ARCHITECTURE §4.3 enum; not yet surfaced in `clock-instrument.ts` model; not yet in a body section | The matheme proof `127 → 128 → 137` is currently invisible to the user. The played-torus renders 137 as a per-tick glyph (15.8) but the **proof structure** (Mersenne ground → parent-seal → atomic dressing) is unread anywhere in M1' UI | **DOC-AHEAD** — Mersenne / 137 additive proof overlay (22.6) in developer/dev-mode of the Cl(4,2) signature inspector (22.3), gated to `ide-deep` only, consuming the skeleton-event stream |
| WC-M1-08 | Kaprekar 6174 is a QL miniature (digits {1,4,6,7} as kernel-primitive names; `6174 = 7² × 9 × 14 = 18 × 7³`; archetype-7 binding via `QL_DIVINE_ACT_RATIO 16/9`; `KaprekarPedagogyHit` skeleton event) (Tranche 19.8 LEAN seed) | 19.8 names the seed and the enum addition; pedagogy is LEAN; full freight (four-register law) stays out of the seed | Substrate carries `QL_DIVINE_ACT_RATIO 16/9` at m1.h:417-422 + the 7-row of `ANANDA_BIMBA` at m1.c:30 + `AnandaSkeletonEvent::KaprekarPedagogyHit = 6` | No Kaprekar surface in M1' extension; the seed at `m1-prime-kaprekar-pedagogy.md` exists but is unreachable from the IDE | **DOC-AHEAD** — Kaprekar 6174 inspector (22.7) is a minimal pedagogy-link surface — cross-link 19.8, do NOT reproduce treatment — reachable via OmniPanel deep-link to seed file OR a compact lens panel inside `m1-paramasiva` when the active tick is the 7-row of `ANANDA_BIMBA` |
| WC-M1-09 | `klein_flip` field on `MathemeHarmonicProfile` is what the consumer surface reads (Tranches 10.2 / 18.2 / 02.2 cross-link) | Tranche 02.2 lands `klein_flip: Option<KleinFlipEvent>`; 10.2 / 18.2 own the kernel-bridge JSON emit + shared three-variant enum (M1TritoneCrossing, M2CymaticValenceInvert, M3CodonRotationCross) | `clock-instrument.ts` does NOT yet consume any `klein_flip` field on `MathemeHarmonicProfileBoundary.payload` — it only reads `topology.m1OriginKleinFlip` (a string slot) and `topology.k2TritoneCrossing` | The consumer side of the typed shared variant lands in 10.2 / 18.2; the M1'-side reader (which UI tells the user "klein_flip event tick=t variant=M1TritoneCrossing from_lens=2 to_lens=5") is unowned at extension level | **CODE-PENDING (consumes 02.2 / 10.2 / 18.2)** — bridged inside Klein-flip event-strip (22.4); contract here is that 22.4 routes the shared variant enum, not a local M1 string slot |
| WC-M1-10 | The played-torus extension is FIRST-BUILD-ALLOWED per DR-M1-2 (validated 2026-06-02); scaffold permitted (02.6) | 02.6 ratifies the scaffold; 15.8 owns the ananda vortex visual; 15.9 owns the tick choreography; ARCHITECTURE.md L102-132 enumerates 12 acceptance tests | `m1-paramasiva-played-torus/` has `ARCHITECTURE.md` + `README.md` only. No `package.json`, no `src/`, no `wgpu/Cargo.toml`. The "src tree pending" status is the live state | The ananda vortex visual is contracted by 15.8 (do NOT reproduce). The played-torus PACKAGE SHAPE — file paths, Bevy/wgpu mount inside `ReactWidget`, profile-tick subscription wiring, six-matrix cross-fade layer scaffolding, the gold/emerald streamlines surface, the raw/DR proof overlay surface — is the **extension-shape** Track 22 owns | **CODE-PENDING (first-build per DR-M1-2)** — m1-paramasiva-played-torus scaffold tranche (22.2) creates the package + src/browser/ + wgpu/Cargo.toml + ReactWidget host that mounts the wgpu canvas (mount points only; renderer authority lives at 15.8 / 15.9) |
| WC-M1-11 | Vortex matrix browser: selector across the six matrix families (Bimba, Pratibimba, Sum, DiffA, DiffB, Quintessence/rule); lens crossing affordance; raw vs DR face toggle (ANANDA §2.1 + §5.2 + UX §5b.2 perspex stack) | 15.8 contracts perspex cross-fade in the played-torus AND requires a "toggle between recursive play and source proof" (UX §5b.2); the **selector UI** (let user click into a specific `Ananda_Matrix_Op` and inspect its 12×12 cells row-by-row) is not the renderer's job | `m1.h::Ananda_Matrix_Op` enum (0..5); ANANDA §4.3 `AnandaVortexProjection.active_matrix_op`; profile-bus reads via bridge | Played-torus renders the perspex cross-fade in 3D. The **2D inspector matrix** where the user picks a family, scrolls the 12×12 cells, sees raw & DR side-by-side, and locks the played-torus to that family is unowned | **DOC-AHEAD** — Vortex matrices browser (22.8) is a 2D inspector in `m1-paramasiva` that pairs with the 3D played-torus; selection in 2D pins the family in 3D; raw/DR face toggle drives both surfaces |
| WC-M1-12 | `audio_octet[8]` is the 8 explicate-sung positions; `nodal_quartet[4]` is 4 implicate-nodal positions as cymatic boundary conditions (UX §4, M1'-SPEC §9). M1' renders; M2-1' Vimarśa writes | M1'-SPEC §0/1 corrected authority; `vimarsha_reading.rs:17-93` is single authority; 15.8 renders the 8 emitters + 4 satellites on the K² | `clock-instrument.ts::audioBus.{audioOctetHz, nodalQuartet, exactProfileSource, authority='S0/S2 profile bus'}` lands; current body renders them as JSON dumps in `<dl>` | The `m1.paramasiva.audioBusInspector` view id (third view in ALL_VIEW_IDS) is reserved but **has no .tsx body**. The 2D inspector showing audio_octet HZ values, ratio_role per position, nodal_quartet m/n ratios with provenance="Vimarśa M2-1'" badges is unowned at view-body level | **CODE-PENDING (consumes 15.8 played-torus 3D rendering)** — Audio_octet / Nodal_quartet companion view body (22.9) builds the `m1.paramasiva.audioBusInspector` body that lands the reads-only 2D inspector |
| WC-M1-13 | M1' has a standalone surface in `ide-deep` AND a role in cosmic-1-2-3 composed plugin (15.4 composition pattern) | 15.4 contracts composition-over-juxtaposition (integrated plugins compose three M-extensions into ONE editor surface); ARCHITECTURE §7.3 names the played-torus as the M1 pole of cosmic-1-2-3; M'-SYSTEM-SPEC says `ide-deep` is the M1' standalone full-depth page | Two extensions: `m1-paramasiva` (2D, all five sections) renders in `ide-deep` AND contributes `M1WalkStrip` + `M1TopologyMiniView` to integrated plugins via Track 08; `m1-paramasiva-played-torus` (3D, pending) renders the played K² | Standalone vs composed dispatch is not yet a typed surface contract on the M1 side. How the SAME `MathemeHarmonicProfile` drives both a full 2D inspector at `ide-deep` AND a single-pole contribution to cosmic-1-2-3 is unowned at design level | **SPEC-AHEAD** — Standalone vs composed dispatch contract (22.10) names the typed surface contract: 2D `m1-paramasiva` widget renders FULL 5-section + Spanda navigator + Cl(4,2) inspector + Klein event-strip + 7-8-9 spine + vortex browser + audio-bus body at `ide-deep`; SAME extension renders COMPACT `M1WalkStrip` + `M1TopologyMiniView` inside cosmic-1-2-3; 3D played-torus renders the centrepiece in cosmic-1-2-3 AND mounts as an editor-area widget at `ide-deep` `m1-paramasiva` view |
| WC-M1-14 | Activity-bar mode for M1' within Coordinate Tree / Bimba Graph Viewer / Canon Studio (15.3 + 15.0 principle 7 "Activity-bar discipline") | 15.3 wires activity-bar for the LEFT sidebar (Coordinate Tree, Bimba Graph Viewer, Canon Studio, Backend Studio, Smart Connections). M1' is an editor-area widget not a left-sidebar mode — but `Coordinate Tree` when scoped to M1' (sub-tree from `M1`) needs to surface the Ananda ring's 12 positions as walkable nodes | Coordinate Tree is in `ide-shell-m0-m5` package, not `m1-paramasiva`; M1' contribution to Coordinate Tree filter is unowned | No M1-specific contribution to Coordinate Tree (the "expand to show 12-ring positions + lens-ring + Ananda matrix families as walkable nodes") | **DOC-AHEAD** — Coordinate Tree M1 contribution (22.11) is a typed contribution from `m1-paramasiva` to the `ide-shell-m0-m5` Coordinate Tree widget that exposes M1's internal coordinate structure (M1-0' through M1-5'; ring positions 0–11; matrix families 0–5) as walkable sub-nodes — does NOT live in M1's editor-area widget |
| WC-M1-15 | Backend Studio integration: rust-analyzer / clangd for `epi-lib/src/m1.c` and `portal-core/src/{kernel.rs, hopf.rs, quaternion.rs, parashakti/vimarsha_reading.rs}` (15.0 Surface Contracts §`ide-deep` left sidebar Backend Studio) | 15.0 names Backend Studio as `ide-deep`-only left-sidebar mode with LSP contributions; M1' is the substrate-richest M-branch (substrate-rich, surface-shallow per Wave-A closing note) — Backend Studio integration MUST surface the m1.h / m1.c / portal-core m1-related crates as a curated "M1 backend pack" | Backend Studio package not yet scaffolded; M1-side curated source pack is unowned | No "M1 backend pack" exists as a Backend Studio contribution | **DOC-AHEAD** — Backend Studio M1 pack (22.12) is a curated set of source-path entries — `Body/S/S0/epi-lib/include/m1.h`, `Body/S/S0/epi-lib/src/m1.c`, `Body/S/S0/portal-core/src/{kernel.rs, hopf.rs, quaternion.rs, spanda.rs, parashakti/vimarsha_reading.rs, codon_rotation_projection.rs}` — that M1's extension contributes to Backend Studio when Backend Studio lands. Track 22 lands the manifest, not the LSP. Gated CODE-PENDING on Backend Studio extension itself per TS-23 `pratibimba.smart-connections-sidebar` precedent |
| WC-M1-16 | Profile-tick is the global UI clock — widgets re-render on tick advance, not user input (15.0 foundation principle 2; UX §10 "no renderer-local source-of-truth") | 15.6 wires `MathemeHarmonicProfile` tick-advance as the global UI clock; widgets subscribe via kernel-bridge profile subscription | `m1-paramasiva-widget.tsx` already subscribes via `this.bridge.onProfile(profile => { this.profile = profile; this.update(); })` — the tick-driven re-render IS landed. But `M1ProfileClockModel` doesn't expose tick-derived quantities (`degreesInThisTick`, `slerp_fraction`, `next_tick_event`) that downstream sub-widgets need | The tick-subscription is present; tick-derived **projections** that the Spanda walk navigator (22.1), Cl(4,2) inspector (22.3), Klein event-strip (22.4) all need are not surfaced | **ALIGNED + SPEC-AHEAD on projections** — extend `M1ProfileClockModel` with `tick_projection` sub-object during 22.1; consumes 15.6 contract |
| WC-M1-17 | The same coordinate / lens / mode / profile generation / tick survives `daily-0-1` ⇄ `ide-deep` toggle and 0/1 cosmic ⇄ personal toggle (15.7 + DR-TS-1) | 15.7 contracts `BimbaPratibimbaUiState` survival; M'-SYSTEM-SPEC L145-147 names the contract; played-torus ARCHITECTURE.md §8.5 names `(tick12, position6, lens_mode, active_matrix_op)` + K² orientation mid-slerp survival | `m1-paramasiva-widget.tsx` `subscriptions[]` cleaned up in `dispose()`; new widget mount on layout switch should resume from kernel-bridge DI singleton state (15.7 contract) | The widget IS lifecycle-aware. The **acceptance test** asserting that toggling layouts mid-walk preserves Spanda walk navigator position + Cl(4,2) inspector active position + Klein event-strip scroll state is not in any test file | **SPEC-AHEAD** — State persistence audit for M1' deep widgets (22.13) extends `acceptance-harness` topology test with M1'-deep-widget-specific assertions (mirror of TS-25 closing tranche 11.6 / 15.7) |
| WC-M1-18 | Privacy class `public_current_audio_metadata_only` — M1' exposes route + relation metadata, never private traversal motives, raw bioquaternion, journal/Graphiti bodies (UX §10; preflight L237) | 07-t0 preflight L237; M1'-SPEC §13 (privacy/disclosure) | Contract preflight asserts `PRIVACY_CLASS='public_current_audio_metadata_only'`; ReadinessBanner shows `provenance` line with `privacy=${PRIVACY_CLASS}` | All new Wave-C widgets MUST honour the same class. The 7-8-9 spine reader (22.5) at session close is the highest-risk affordance because witness-vector data could touch protected-local | **ALIGNED with audit** — every Wave-C tranche must declare `PRIVACY_CLASS` adherence; 7-8-9 spine reader (22.5) reads aggregate witness-vector ONLY (no per-event journal correlation) and runs through the OmniPanel Review tab landing, not in `m1-paramasiva`'s editor-area body |
| WC-M1-19 | Wave-A row 11 / Tranche 02.5 — Single session-held `#` (Inversion_Operator) carrier from extension → kernel-bridge | 02.5 names the contract; M1'-SPEC §14 readiness test depends on it; UX §3 M1-3' "binds (0/1) to every coordinate's `invert`" | `m1-paramasiva-widget.tsx` does not yet have an `invert` affordance reachable from `m1.startWalk`; no data-test selector `m1-invert-current-coordinate` exists | The `#` UI affordance is in 02.5 scope but Track 22 owns where in the M1' surface it appears — most naturally inside the Spanda walk navigator (22.1) as a "flip" button next to the current tick | **CODE-PENDING (consumes 02.5)** — Spanda walk navigator (22.1) hosts the `#` affordance; the contract round-trip is owned by 02.5; the placement is owned by 22.1 |
| WC-M1-20 | Klein-flip event triggers cross-fade to dual matrix (`BIMBA ↔ PRATIBIMBA`) if lens crossing aligns with `Ananda_Matrix_Op` advance (15.9 paragraph "the active Ananda matrix may cross-fade to its dual") | 15.9 contracts this on the played-torus surface; 22.8 vortex matrices browser must mirror the dual-matrix selection in 2D so the user can inspect the dual cells | Profile-bus carries `active_matrix_op`; the **dual-matrix relationship** (`Bimba ↔ Pratibimba`, `Sum ↔ Quintessence`, `DiffA ↔ DiffB` per `ANANDA_AXIOM_DELTA = 1` invariant at m1.h:735-756 parallel-track) is encoded in substrate but not surfaced in extension model | The 2D inspector does not yet know about the dual-pair relationship at view-model level | **SPEC-AHEAD** — Vortex matrices browser (22.8) adds `dualOf(matrix_op)` projection in the view-model (NOT a local LUT — read through bridge by reading the next `active_matrix_op` post-flip from the event payload) and lights the dual matrix on Klein-flip event arrival |

---

## Anomalies

### CONTRADICTIONS (decision-register candidates)

- **DR-WC-M1-1** — Standalone vs composed dispatch authority. Wave-C row 13 surfaces that the same `m1-paramasiva` extension must serve TWO surfaces: the full standalone 4+2 page at `ide-deep` (where Track 22 lands all 8–12 deep widgets) AND a compact contribution (`M1WalkStrip` + `M1TopologyMiniView`) to the cosmic-1-2-3 composed plugin. The contract preflight L242-245 declares both `track08Exports`. The decision question is: when the cosmic-1-2-3 composition is rendering in `daily-0-1` and the user navigates into the deep M1' page in `ide-deep` mid-tick, which side of the rendering owns state continuity? Per 15.7 the contract is `BimbaPratibimbaUiState`; per played-torus ARCHITECTURE §8.5 the slerp resumes mid-step. The integrated-composition mount-point contract at `Body/M/epi-theia/extensions/integrated-composition/src/common/composition-pattern.md` (Tranche 15.4) is the authority for the answer — and 22.10 forces that authority through the M1' lens. Route to user final-validation if 15.4 does not already cover the M1'-specific case. **Probable resolution:** kernel-bridge DI singleton owns state; both surfaces are views; no contradiction at substrate level — but the surface-routing IS a contract Track 22 must name explicitly.

### CODE-PENDING (named, gated on other tranches; no Wave-C rebuild)

- **WC-M1-03 / WC-M1-05 / WC-M1-09** — Spanda walk navigator, Klein-flip event-strip, and the `klein_flip` shared-variant reader all consume Tranches 02.2 / 02.3 / 10.2 / 18.2 (kernel-side klein_flip field; topology view wiring; kernel-bridge JSON emit). Track 22 lands the **widget body** that consumes the contract; the contract itself is owned upstream.
- **WC-M1-10** — m1-paramasiva-played-torus scaffold (22.2): first-build allowed per DR-M1-2 — `package.json`, `src/browser/played-torus-widget.tsx`, `src/browser/played-torus-frontend-module.ts`, `wgpu/Cargo.toml`, `style/played-torus.css`. The renderer (Bevy/wgpu shaders, k2_mesh.rs, ananda_heatmap.rs, dr_streamlines.rs, cl42_colour.rs, hopf_shadow.rs, diamond_centre.rs, tick_choreography.rs) is **15.8 + 15.9 authority** — Track 22 scaffolds the ReactWidget mount point + profile-tick subscription wiring + readiness overlay rendering only.
- **WC-M1-12** — Audio_octet / nodal_quartet companion view body (22.9) consumes the landed profile fields plus the played-torus 3D rendering; the 2D body is straightforward to land independently.
- **WC-M1-15** — Backend Studio M1 pack (22.12) is gated on Backend Studio extension itself (Wave-B TS-23 / Track 11 closing tranche 11.11 named the gating); Track 22 lands the manifest, not the LSP.
- **WC-M1-19** — `#` (Inversion_Operator) carrier (22.1 hosts the affordance; 02.5 lands the contract round-trip).

### DOC-AHEAD (land it or downgrade)

- **WC-M1-02** — 9/8 self-derivation. 22.3 (Cl(4,2) signature inspector) adds the `derivation` projection showing the 4:2 ↔ 3:3 chain live.
- **WC-M1-06** — 7-8-9 spine reader (22.5) is the session-close affordance reading the 9-bit witness vector + walking the user through `127 → 128 → 137`. Per 19.9 contract.
- **WC-M1-07** — Mersenne / 137 additive proof (22.6) developer-mode overlay; gated to `ide-deep`.
- **WC-M1-08** — Kaprekar 6174 inspector (22.7) cross-links 19.8 LEAN seed; does NOT reproduce treatment.
- **WC-M1-11** — Vortex matrices browser (22.8) is the 2D selector; pairs with 15.8's 3D perspex cross-fade.
- **WC-M1-14** — Coordinate Tree M1 contribution (22.11) contributes M1's internal coordinate structure to the existing `ide-shell-m0-m5` Coordinate Tree widget.

### SPEC-AHEAD

- **WC-M1-04** — Cl(4,2) signature inspector (22.3) reads `cl42_signature_at_position` from the profile bus + lifts `QL_TRIG_TABLE[6]` numerator/denominator through the bridge.
- **WC-M1-13** — Standalone vs composed dispatch (22.10) names the typed surface contract.
- **WC-M1-16** — Tick projection extension to `M1ProfileClockModel` (consumed by 22.1).
- **WC-M1-17** — State persistence audit for M1' deep widgets (22.13).
- **WC-M1-20** — Dual matrix projection on Klein-flip in vortex browser (22.8).

### ALIGNED (note only)

- **WC-M1-01** has all the data; only needs the navigator (DOC-AHEAD).
- **WC-M1-16** profile-tick subscription is already wired.
- **WC-M1-18** privacy class declared and enforced; audit-only.

### ORPHAN

- **None identified.** Every Wave-C surface has at least one of {UX claim, spec authority, substrate field, existing extension slot}. The played-torus extension is the closest to "first-build" but is explicitly DR-M1-2-ratified.

---

## Proposed Cycle-3 Closing Tranches (M1' frontend deep design — id space `22.x`)

Numbered 22.1 through 22.13. Each tranche names a single widget / contribution / contract closure. None propose re-building landed `Body/M/epi-theia/extensions/m1-paramasiva` substrate; tranche 22.2 is the only DR-M1-2-ratified first-build (the played-torus scaffold). Cross-links to 15.4 / 15.6 / 15.7 / 15.8 / 15.9 / 19.8 / 19.9 / 02.1 / 02.2 / 02.3 / 02.4 / 02.5 / 02.6 / 11.x / 10.2 / 10.10 / 18.2 are explicit in every tranche.

### 22.1 — Spanda walk navigator widget (CODE-PENDING-consumer / SPEC-AHEAD)

- **Source rows:** WC-M1-01, WC-M1-03, WC-M1-16, WC-M1-19
- **Deliverable:** New `src/browser/m1-spanda-walk-navigator.tsx` in `m1-paramasiva`; tick-by-tick frame with scrub bar, pause/play, `#` (invert) affordance per 02.5, current `tick12 / 11 → degree720` readout, slerp-fraction live progress, next-tick-event preview; extends `M1ProfileClockModel` with `tick_projection: { slerpFraction: number | null; nextTickEventKind: string | null; isAtKleinBoundary: boolean }`; consumes 15.9 deterministic slerp + scrub-to-tick affordance. Renders inside the existing `m1.paramasiva.clockInstrument` view body (replaces the static Profile-clock `<dl>` section with a navigator + the `<dl>` kept as a slow-data readout below).
- **Verification:** `test -f Body/M/epi-theia/extensions/m1-paramasiva/src/browser/m1-spanda-walk-navigator.tsx`; `pnpm --filter @pratibimba/m1-paramasiva test` passes; widget test asserts `data-test='m1-walk-navigator-tick'` reads the current tick from bridge; scrub test asserts the scrub bar position drives the profile-bus replay through the kernel-bridge `requestScrubToTick(tick)` capability (added through 15.9 if not already there); invert test asserts the `#` button publishes the 02.5 contract round-trip.

### 22.2 — `m1-paramasiva-played-torus` extension scaffold first-build (CODE-PENDING-first-build, DR-M1-2 ratified)

- **Source rows:** WC-M1-10
- **Deliverable:** First-build the package and React/Inversify host that mounts the wgpu canvas. Files created:
  - `Body/M/epi-theia/extensions/m1-paramasiva-played-torus/package.json` (`@pratibimba/m1-paramasiva-played-torus` v0.1.0; deps `@theia/core`, `@pratibimba/m-extension-runtime`, `react`, `react-dom`; theiaExtensions.frontend = `lib/browser/played-torus-frontend-module`)
  - `src/common/played-torus-surface.ts` (TS extension surface contract: EXTENSION_ID, PRIMARY_VIEW_ID=`pratibimba.m1.paramasiva.played-torus`, PRIVACY_CLASS=`public_current_audio_metadata_only`, OBSERVABILITY_EVENT_TYPES=[`m1.played-torus.tick`, `m1.played-torus.klein-flip`, `m1.played-torus.mobius-return`], DECLARED_BLOCKERS=[`Tranche 10.10 ananda_vortex profile field`, `Tranche 02.2 klein_flip profile field`])
  - `src/common/vortex-renderer-handle.ts` (opaque handle into the wgpu renderer; subscribe / unsubscribe; `requestScrubToTick(t)`; `setPaused(b)`; `getCurrentSurfaceState(): { tick12, degree720, active_matrix_op, k2_orientation_q }`)
  - `src/browser/played-torus-widget.tsx` (ReactWidget hosting the wgpu canvas; subscribes to `bridge.onProfile`, `bridge.onReadiness`, `bridge.onCoordinateContext`; embeds `<canvas data-test='m1-played-torus-canvas' />` and hands its `OffscreenCanvas` to the renderer handle; renders `ReadinessBanner` + readiness-pending overlay when `ananda_vortex` is missing per ARCHITECTURE.md L70-89)
  - `src/browser/played-torus-frontend-module.ts` (ContainerModule, ViewContribution, route `epi-logos://ide/m1-paramasiva-played-torus/k2`)
  - `wgpu/Cargo.toml` (Rust crate declaration; Bevy + wgpu deps; left as toolchain-declaration only — actual renderer src under 15.8 / 15.9)
  - `style/played-torus.css` (canvas sizing; readiness-overlay styling)
- **Do NOT scaffold here (owned by 15.8 / 15.9):** `wgpu/src/k2_mesh.rs`, `ananda_heatmap.rs`, `dr_streamlines.rs`, `cl42_colour.rs`, `hopf_shadow.rs`, `diamond_centre.rs`, `tick_choreography.rs`, `wgpu/shaders/*.wgsl`. Tranche 22.2 lands the Theia-side mount point + readiness overlay + profile-tick subscription wiring + state-persistence hook. The Bevy/wgpu renderer engine is 15.8 + 15.9 territory and lands when those tranches execute.
- **Verification:** `test -d Body/M/epi-theia/extensions/m1-paramasiva-played-torus/src/browser`; `test -f Body/M/epi-theia/extensions/m1-paramasiva-played-torus/package.json`; `test -f Body/M/epi-theia/extensions/m1-paramasiva-played-torus/wgpu/Cargo.toml`; widget mount test asserts ReactWidget instantiates without errors when wgpu renderer is stubbed; ReadinessBanner renders with `pending-ananda-vortex` when `MathemeHarmonicProfile.ananda_vortex` is `None`; forbidden-import lint (Wave-B 11.5) passes — no `Body/S/S0`, `portal-core`, `neo4j-driver`, etc.

### 22.3 — Cl(4,2) signature inspector view body (SPEC-AHEAD-integration; consumes 10.10 + 15.8)

- **Source rows:** WC-M1-02, WC-M1-04, WC-M1-16
- **Deliverable:** New `src/browser/m1-cl42-signature-inspector.tsx` in `m1-paramasiva`; renders an 8-column matrix `P0..P5 + Q.E. + Trig` showing: position index, trig function (sin/tan/sec/cot/csc/cos), signature (−1/+1), numerator QL position, denominator QL position, halo colour-binary (cool indigo for −1; warm amber-vermilion for +1), and the active-position highlight at current `position6`. Also includes the **9/8 self-derivation panel** at the bottom: live readout of `(4/3)×(3/2)=2/1` (octave) and `(3/2)÷(4/3)=9/8` (epogdoon-tick) with the current `tick12` highlighting which 9/8 step the user is at. Reads `cl42_signature_at_position: i8` and `QL_TRIG_TABLE[6]` through the bridge (`MathemeHarmonicProfile.ananda_vortex.cl42_signature_at_position` per ARCHITECTURE §4.3); NO local fork of `CL42_BASIS[6]`. This view body is the inspector that 15.8 cannot be (15.8 renders colour-binary on K²; this widget reads the per-position breakdown). Adds a NEW view id `m1.paramasiva.cl42SignatureInspector` to `ALL_VIEW_IDS` and a contract entry to `07-t0-extension-contract-preflight.json` L194-196.
- **Verification:** `test -f Body/M/epi-theia/extensions/m1-paramasiva/src/browser/m1-cl42-signature-inspector.tsx`; widget test asserts each of the six positions renders with the correct signature; halo colour test asserts indigo for `position6 ∈ {0,5}` and warm for `{1,2,3,4}`; 9/8 derivation test asserts the chain reads `(4/3)*(3/2)=2/1` and `(3/2)/(4/3)=9/8` with no local arithmetic in the widget (values pulled from bridge); contract preflight regen asserts the new view id is registered.

### 22.4 — Klein-flip event-strip widget (CODE-PENDING-consumer; consumes 02.2 / 10.2 / 18.2)

- **Source rows:** WC-M1-05, WC-M1-09, WC-M1-20
- **Deliverable:** New `src/browser/m1-klein-flip-event-strip.tsx` in `m1-paramasiva`; renders a chronograph component as the user-visible event-strip for Klein-flip events. Reads the shared `KleinFlipEvent` shape landed at Tranche 18.2 (variants `M1TritoneCrossing`, `M2CymaticValenceInvert`, `M3CodonRotationCross`); when M1-variant arrives, renders a glyph at the current scroll position with `from_lens → to_lens` annotation; when M2/M3 variants arrive (echoes of M1's origin per UX §5b.5), renders a secondary glyph beneath at lower intensity. The Hopf-fibre flag flip is rendered as the moment-of-truth visual: a small Hopf-bundle icon visibly inverts at the flip moment. Subscribes to `bridge.onObservabilityEvent` filtered on `OBSERVABILITY_EVENT_TYPES`. Scrolls horizontally (left = past, right = future-projection from slerp); when paused via 22.1, the strip is scrollable; when playing, the strip auto-scrolls. The widget is mounted as a NEW view id `m1.paramasiva.kleinFlipEventStrip` added to `ALL_VIEW_IDS` + contract entry. This widget does NOT render the topology (02.3 owns that); it renders the event-trail.
- **Verification:** `test -f Body/M/epi-theia/extensions/m1-paramasiva/src/browser/m1-klein-flip-event-strip.tsx`; widget test asserts an M1TritoneCrossing event at tick 5→6 renders the M1 glyph + Hopf-flag flip; M2CymaticValenceInvert event renders the secondary glyph; data-test `m1-klein-event-strip-current-tick` is bound to current `tick12`; scroll test asserts horizontal scroll affordance when widget is paused.

### 22.5 — 7-8-9 spine reader at session close (DOC-AHEAD-landing; consumes 19.9 + 02.4)

- **Source rows:** WC-M1-06, WC-M1-18
- **Deliverable:** New `src/browser/m1-spine-789-reader.tsx` in `m1-paramasiva`; renders the 7-8-9 spine completion arithmetic at session close. Three vertically-stacked rows: the 7 register (`127 = 2^7 − 1 = M_7` Mersenne ground; fifth-generator +7 mod 12 closure assertion), the 8 register (`128 = 2^7` octave-closure; eight audio_octet positions check), the 9 register (`137 = 128 + 9`; nine-virtue witness check reading the 9-bit `virtue_witness_vector` from contemplation per Tranche 19.6 / 19.9). Each row carries a single sentence drawn from the canonical reading per 19.9 (`Did the session's action-generator traverse all twelve positions?` / `Did the session return through octave-closure rather than premature contraction?` / `Did wholeness witness all nine virtue-poles?`). The 9-bit witness vector renders as a 9-position pip-strip with each pip's virtue label on hover. **Privacy:** reads aggregate witness-vector only — no per-event journal correlation. **Surface placement:** view id `m1.paramasiva.spineReader789` is added to `ALL_VIEW_IDS` but lands inside the **OmniPanel Review tab** at session close (mounted via the integrated-composition contract that 4-5-0 plugin owns per 19.6 / 19.7), NOT inside `m1-paramasiva`'s editor-area body. The widget code lives in `m1-paramasiva` because the matheme of 7-8-9 is M1's closing arithmetic; the placement is Review-tab because that's where session-close reflection happens.
- **Verification:** `test -f Body/M/epi-theia/extensions/m1-paramasiva/src/browser/m1-spine-789-reader.tsx`; widget test asserts three rows render; mock contemplation 9-bit witness `0b101101011` renders 6 lit + 3 dark pips with correct virtue labels; privacy audit asserts the widget never reads `journal_payload` or `graphiti_episode` from the bridge.

### 22.6 — Mersenne / 137 additive proof overlay (DOC-AHEAD-landing; consumes 02.1 + 15.8)

- **Source rows:** WC-M1-07
- **Deliverable:** Extend the 22.3 Cl(4,2) signature inspector body with a developer-mode `MersenneProofOverlay` sub-component (only rendered when `ide-deep` is the active layout AND the developer-mode preference `epi-logos.ui.developerMode` is true). The overlay shows the additive proof structure:
  - Line 1: `M_5 = 2^5 − 1 = 31` (the prime-index of M_7) [provenance: `alpha_quaternionic_integration_across_M_stack.md` Tranche 02.1 synthesis]
  - Line 2: `M_7 = 2^7 − 1 = 127` (Mersenne ground exposed by 9-gap withdrawal `136 − 9 = 127`)
  - Line 3: `+1 → 128 = 2^7` (the parent-seal — this is the M1-5 "+1 parent" attribution)
  - Line 4: `+9 → 137` (atomic dressing; wholeness restored)
  - Highlight active line based on which skeleton-event last fired (`Additive137`, `IdentityReturn4Plus2`, `KaprekarPedagogyHit`, etc.).
  - Renders only when `AnandaSkeletonEvent::Additive137` has been observed in the session per `bridge.onObservabilityEvent` filter.
- **Verification:** developer-mode toggle test asserts the overlay only renders when both `ide-deep` and `developerMode` are true; skeleton-event filter test asserts `Additive137` event lights line 4; line-by-line text content matches Tranche 02.1 synthesis.

### 22.7 — Kaprekar 6174 inspector (DOC-AHEAD-landing; cross-links 19.8 LEAN seed; does NOT reproduce)

- **Source rows:** WC-M1-08
- **Deliverable:** New `src/browser/m1-kaprekar-inspector.tsx` in `m1-paramasiva`; minimal pedagogy-link surface. Renders a compact 4-line panel when the active tick lights the 7-row of `ANANDA_BIMBA` (per `m1.c:30`) OR when `AnandaSkeletonEvent::KaprekarPedagogyHit = 6` fires (per 19.8 enum addition). Shows:
  - Line 1: `6174 = 7² × 9 × 14 = 18 × 7³`
  - Line 2: digits `{1, 4, 6, 7}` as kernel-primitive names (DIFF_B axiom = 1; DIFF_A matrix idx = 4; six matrix families = 6; 7-row producing 16/9 = 7)
  - Line 3: archetype-7 binding via `QL_DIVINE_ACT_RATIO 16/9` at m1.h:417-422
  - Line 4: cross-link button "Read the pedagogy seed" → opens `Idea/Bimba/Seeds/M/M1'/m1-prime-kaprekar-pedagogy.md` in Canon Studio (`ide-deep` only) OR shows the link in a tooltip on `daily-0-1`
  - **Lean discipline:** the widget does NOT carry the full four-register-law freight (Kaprekar 7-step / parent-as-7th / Cl(4,2)+2 / Möbius twist). That freight lives in `alpha_quaternionic_integration_across_M_stack.md` per 19.8 verification rule. The widget cross-links only.
- **Verification:** `test -f Body/M/epi-theia/extensions/m1-paramasiva/src/browser/m1-kaprekar-inspector.tsx`; widget test asserts the panel renders only when the 7-row trigger fires; line 4 click test asserts navigation to the seed file path; LEAN audit asserts `grep -n "four-register law\|Möbius twist" Body/M/epi-theia/extensions/m1-paramasiva/src/browser/m1-kaprekar-inspector.tsx` returns no matches (freight stays out).

### 22.8 — Vortex matrices browser (DOC-AHEAD-landing + SPEC-AHEAD; consumes 10.10 + 15.8)

- **Source rows:** WC-M1-11, WC-M1-20
- **Deliverable:** New `src/browser/m1-vortex-matrices-browser.tsx` in `m1-paramasiva` plus `src/common/vortex-matrices-model.ts` view-model. Six-tab selector across the six matrix families (Bimba, Pratibimba, Sum, DiffA, DiffB, Quintessence/rule) reading `AnandaVortexProjection.active_matrix_op` from the profile bus. For each family, renders a 12×12 cell grid (rows = `tick12`, cols = `position6`) with two-mode display: digit-root face (primary heatmap, vortex spin visible) vs raw/no-DR face (proof mode showing affine values `k*p` / `k*p+1` etc. so `36`, `64`, `72`, `64+72+1=137` are visible). Active cell highlight follows `(tick12, position6)`. The view-model adds `dualOf(matrix_op): Ananda_Matrix_Op` projection (NOT a local LUT — reads through the bridge by listening for the next `active_matrix_op` post-flip; if the post-flip `active_matrix_op` differs from the pre-flip and the event carries `dual: true`, the projection caches the mapping). On Klein-flip event arrival (consumes 22.4), the browser visually cross-fades the active matrix to its dual. **Selection in 2D pins the family in 3D** — the browser emits an observability event `m1.vortex.family_pinned` that the played-torus extension subscribes to and locks the perspex cross-fade in 3D to the pinned family. Adds new view id `m1.paramasiva.vortexMatricesBrowser` + contract preflight entry.
- **Verification:** `test -f Body/M/epi-theia/extensions/m1-paramasiva/src/browser/m1-vortex-matrices-browser.tsx`; six-tab test asserts each family renders; raw/DR toggle test asserts `7X+1 raw` exposes `36, 64` and `8X+0 raw` exposes `64, 72`; dual matrix test asserts Klein-flip event triggers cross-fade to dual family; observability test asserts `m1.vortex.family_pinned` event fires on user tab change; substrate-derivation audit asserts no local LUT (`! grep -n 'ANANDA_BIMBA\s*=\|7X.*1\s*=' Body/M/epi-theia/extensions/m1-paramasiva/src/browser/m1-vortex-matrices-browser.tsx Body/M/epi-theia/extensions/m1-paramasiva/src/common/vortex-matrices-model.ts`).

### 22.9 — Audio_octet / Nodal_quartet companion view body (CODE-PENDING-consumer; consumes 15.8 + Vimarśa-window)

- **Source rows:** WC-M1-12
- **Deliverable:** Land the body for the reserved view id `m1.paramasiva.audioBusInspector` (third entry in `ALL_VIEW_IDS`). New `src/browser/m1-audio-bus-inspector-view.tsx` in `m1-paramasiva`. Two sub-sections:
  - **`audio_octet[8]` table:** 8 rows, each showing position index (0..7), Hz value (from `MathemeHarmonicProfile.audio_octet[i]`), ratio role (from `MathemeHarmonicProfile.ratio_role` per position), and a Vimarśa-authority badge `"Vimarśa M2-1' write — see vimarsha_reading.rs:17-93"` on every row. Renders sortable by Hz / position / ratio role. Shows the cymatic-source provenance: every row carries the M2-1' source attribution.
  - **`nodal_quartet[4]` table:** 4 rows, each showing nodal index, `m/n` ratio (from `MathemeNodalConstraint`), constraint-kind, and cymatic boundary-condition role. Same Vimarśa-authority badge.
  - **Reads-only contract banner:** `M1' is the consumer; M2-1' is the writer. To change a value, route through M2.` Honours UX §10 "no renderer-local source-of-truth".
  - Wires through the existing `M1ParamasivaWidget`'s bridge subscription; renders as the alternative body when `PRIMARY_VIEW_ID` is `m1.paramasiva.audioBusInspector` (uses Theia view-routing via `OPEN_COMMAND_ID + ?view=audioBusInspector`).
- **Verification:** `test -f Body/M/epi-theia/extensions/m1-paramasiva/src/browser/m1-audio-bus-inspector-view.tsx`; widget test asserts 8 + 4 rows render with Vimarśa badges; substrate-derivation audit asserts no local Hz computation (`! grep -n 'computeHz\|deriveOctet\|local_pitch' Body/M/epi-theia/extensions/m1-paramasiva/src/browser/m1-audio-bus-inspector-view.tsx`); contract preflight already declares the view id — no schema change needed.

### 22.10 — Standalone vs composed dispatch contract (SPEC-AHEAD-integration; cross-links 15.4 / 11.x)

- **Source rows:** WC-M1-13, DR-WC-M1-1
- **Deliverable:** Author `Body/M/epi-theia/extensions/m1-paramasiva/src/common/surface-dispatch.ts` declaring the typed surface contract `M1SurfaceMode = 'standalone-ide-deep' | 'composed-cosmic-1-2-3' | 'compact-track-08'`. The same `M1ProfileClockModel` drives all three modes; the dispatch logic chooses which view-body to render. Adds a `surfaceMode` prop to `M1ParamasivaExtensionBody` and three sub-bodies:
  - `standalone-ide-deep` — renders Spanda walk navigator (22.1) + Cl(4,2) signature inspector (22.3) + Klein event-strip (22.4) + vortex matrices browser (22.8) + audio-bus inspector (22.9) + existing 5-section layout
  - `composed-cosmic-1-2-3` — renders compact M1WalkStrip + M1TopologyMiniView (existing Track 08 exports) only; the played-torus 3D centrepiece is the integrated-composition mount-point per 15.4
  - `compact-track-08` — renders M1WalkStrip + M1TopologyMiniView only (existing Track 08 behaviour preserved for non-cosmic compositions)
- DR-WC-M1-1 resolution: kernel-bridge DI singleton owns `(coordinate, lens, mode, profileGeneration, tick12, position6, active_matrix_op, k2_orientation_q)`; both surfaces are pure views; state continuity is preserved through bridge subscription. Route through user final-validation if 15.4 does not already declare the resolution at composition-pattern level.
- **Verification:** `test -f Body/M/epi-theia/extensions/m1-paramasiva/src/common/surface-dispatch.ts`; typed dispatch test asserts the three modes render their respective bodies; integration test mid-tick toggle `daily-0-1` (composed) → `ide-deep` (standalone) preserves `tick12`, `position6`, `active_matrix_op`, K² orientation; DR entry landed in 13-decision-register.

### 22.11 — Coordinate Tree M1 contribution (DOC-AHEAD-landing; cross-links 15.3)

- **Source rows:** WC-M1-14
- **Deliverable:** New `src/browser/m1-coordinate-tree-contribution.ts` in `m1-paramasiva` declaring a typed contribution to the `ide-shell-m0-m5` Coordinate Tree widget (per 15.3 left-sidebar activity-bar). When the Coordinate Tree expands the `M1` node, this contribution exposes:
  - Six M1-X' strata as walkable child nodes (`M1-0'`, `M1-1'`, `M1-2'`, `M1-3'`, `M1-4'`, `M1-5'`)
  - Under `M1-2'`: six Ananda matrix families as walkable nodes (`M1-2'/Bimba`, `M1-2'/Pratibimba`, `M1-2'/Sum`, `M1-2'/DiffA`, `M1-2'/DiffB`, `M1-2'/Quintessence`)
  - Under `M1-3'`: 12 ring positions (0..11) as walkable nodes
  - Under `M1-5'`: K² topology constants (`TORUS_GENUS=1`, `DOUBLE_COVER_DEG=720`, `RING_QUATERNION_LUT[12]` as 12 sub-entries)
  - Click on a node deep-links via `epi-logos://ide/m1-paramasiva/walk?coordinate=...` to the corresponding `m1-paramasiva` view with that coordinate active
- Coordinate Tree widget lives in `ide-shell-m0-m5` package, so this contribution is a manifest entry consumed by the host; the manifest format follows the existing `ide-shell-m0-m5` contribution API.
- **Verification:** `test -f Body/M/epi-theia/extensions/m1-paramasiva/src/browser/m1-coordinate-tree-contribution.ts`; manifest test asserts 6 strata + 6 matrix families + 12 ring positions + K² constants are declared; deep-link test asserts click on `M1-2'/Bimba` routes to `epi-logos://ide/m1-paramasiva/walk?coordinate=M1-2'/Bimba` and opens vortex matrices browser pinned to Bimba family; Coordinate Tree host integration test passes.

### 22.12 — Backend Studio M1 pack manifest (DOC-AHEAD-landing; gated CODE-PENDING on Backend Studio extension)

- **Source rows:** WC-M1-15
- **Deliverable:** New `src/common/m1-backend-studio-pack.ts` in `m1-paramasiva` declaring the curated M1 source pack:
  ```ts
  export const M1_BACKEND_STUDIO_PACK = Object.freeze({
      packId: 'm1-paramasiva.backend-studio-pack',
      label: 'M1 Backend Pack — engine + harmonic profile + ring + Hopf',
      sources: Object.freeze([
          { path: 'Body/S/S0/epi-lib/include/m1.h', lsp: 'clangd', annotation: 'C header — RING_QUATERNION_LUT, CL42_BASIS, ANANDA constants' },
          { path: 'Body/S/S0/epi-lib/src/m1.c', lsp: 'clangd', annotation: 'Ananda matrices .rodata + runtime API' },
          { path: 'Body/S/S0/portal-core/src/kernel.rs', lsp: 'rust-analyzer', annotation: 'MathemeHarmonicProfile struct + from_tick' },
          { path: 'Body/S/S0/portal-core/src/hopf.rs', lsp: 'rust-analyzer', annotation: 'Hopf bundle S³ → S² → S¹' },
          { path: 'Body/S/S0/portal-core/src/quaternion.rs', lsp: 'rust-analyzer', annotation: 'SU(2) math, slerp' },
          { path: 'Body/S/S0/portal-core/src/spanda.rs', lsp: 'rust-analyzer', annotation: 'Spanda phase quantiser' },
          { path: 'Body/S/S0/portal-core/src/parashakti/vimarsha_reading.rs', lsp: 'rust-analyzer', annotation: 'Vimarśa M2-1\' writes audio_octet + nodal_quartet — read-only reference for M1\'' },
          { path: 'Body/S/S0/portal-core/src/codon_rotation_projection.rs', lsp: 'rust-analyzer', annotation: 'M3 codon scale of Cl(4,2)' }
      ])
  });
  ```
- The manifest is a typed declaration; the LSP wiring (rust-analyzer / clangd) is consumed when the Backend Studio extension lands per Wave-B TS-23 precedent (gated CODE-PENDING on the upstream extension).
- **Verification:** `test -f Body/M/epi-theia/extensions/m1-paramasiva/src/common/m1-backend-studio-pack.ts`; manifest test asserts 8 source entries with correct LSPs; gated-on marker in `Body/M/epi-theia/extensions/MIGRATION-SOURCES.md` "Forward extensions not yet scaffolded" Backend-Studio entry references this pack as a consumer.

### 22.13 — State persistence audit for M1' deep widgets (SPEC-AHEAD-integration; consumes 15.7 + 11.6)

- **Source rows:** WC-M1-17
- **Deliverable:** Extend `Body/M/epi-theia/extensions/acceptance-harness/tests/topology.test.mjs` with M1'-deep-widget-specific assertions mirroring 15.7 / 11.6 cross-layout state-identity test but for the Wave-C widgets. Asserts that toggling `daily-0-1` ⇄ `ide-deep` mid-walk preserves:
  - Spanda walk navigator (22.1) tick position + paused/playing state
  - Cl(4,2) signature inspector (22.3) active position + developer-mode toggle
  - Klein event-strip (22.4) scroll position + filter (variant subset)
  - Vortex matrices browser (22.8) pinned family + raw/DR face mode
  - Audio bus inspector (22.9) active sub-section + sort order
  - Played-torus (22.2) K² orientation mid-slerp (already asserted in played-torus ARCHITECTURE.md §8.5 — this audit cross-validates)
- The contract is `M1DeepWidgetUiState ⊆ BimbaPratibimbaUiState` (15.7) — the per-widget state is a typed sub-record of the global state.
- **Verification:** `pnpm --filter @pratibimba/acceptance-harness test`; new M1'-deep-widget assertions pass; documented test names: `m1-deep-widget-walk-navigator-survives-toggle`, `m1-deep-widget-cl42-inspector-survives-toggle`, etc.

---

**End of matrix.** The M1' frontend deep design surface is **two extensions wide, scaffold-ready, slot-rich**: `m1-paramasiva` (2D React, all five sections rendered, three view ids reserved, two more added by 22.3 / 22.4 / 22.8 / 22.11) and `m1-paramasiva-played-torus` (3D Bevy/wgpu, scaffold landed at 22.2 under DR-M1-2 ratification, renderer engine deferred to 15.8 / 15.9). Track 22's 13 tranches close the per-widget UX **around** the played-torus vortex visual (15.8 / 15.9), the Kaprekar pedagogy seed (19.8), and the contemplation surface integration (19.6 / 19.7 / 19.9) — never reproducing those authorities, always cross-linking and consuming. The one CONTRADICTION (DR-WC-M1-1, standalone vs composed dispatch) routes to user final-validation; the rest are SPEC-AHEAD integrations, DOC-AHEAD landings, or CODE-PENDING consumers gated on upstream tranches. No ORPHANs. The largest single deliverable is the 22.2 played-torus scaffold (DR-M1-2 first-build); the largest *design* deliverable is the standalone vs composed dispatch contract (22.10) that lets the same `MathemeHarmonicProfile` drive both the cosmic-1-2-3 composition pole AND the standalone `ide-deep` `m1-paramasiva` deep page without state divergence.
