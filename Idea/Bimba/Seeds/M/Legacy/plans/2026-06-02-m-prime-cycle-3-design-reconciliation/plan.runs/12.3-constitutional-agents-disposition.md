# 12.3 — `constitutional_agents` Array Audit and Disposition

**Tranche:** 12.3 (task `12.T12.3`) — *contradiction-decision DOWNGRADED to audit; replaces orphan-fill*
**Date:** 2026-06-18
**Governing decisions:** [[DR-M5-1]] / [[DR-B-1]] (Pi-as-harness; ACR repurpose), [[DR-B-3]] / [[DR-S4-TECHNE]] (Aletheia subagent roster), Tranche 12.29 (constitutional CT0–CT5 + CF1–CF5 team-composition gates).
**Evidence base:** `plan.runs/wave-b-agentic-layer-matrix.md` rows **B2, B6, B27, O-B-1** (read with the DR-M5-1 lens); `Body/S/S4/plugins/pleroma/capability-matrix.json`.

---

## 1. The question this audit answers

Per [[DR-M5-1]] the array

```json
"constitutional_agents": ["anima", "eros", "logos", "mythos", "nous", "psyche", "sophia"]
```

is **NOT a peer-agent ontology**. The Wave-B matrix surfaced it as one of *four disjoint ontologies* the UX colloquially overloads as "agents" (B6): ACR-governance-4 (sophia/anima/pi/aletheia) ≠ constitutional-7 ≠ ta-onta-carriers-6 ≠ Aletheia-subagents-6. The executable dispatch roster is **Pi (harness) + Anima (main agent) + the six Aletheia subagent techne-guardians** (Anansi/Janus/Moirai/Mercurius/Agora/Zeithoven). There are therefore **no missing peer-agent `.md` profiles to land** — the original Wave-B orphan-fill framing (O-B-1, matrix tranche row "12.3 — Constitutional-agent profile orphan-fill") is **withdrawn**.

For each of the seven names this audit asks: is it **(1)** psyche-aspect rendering material surfaced through Anima — document as such, OR **(2)** tangent — deprecate.

**Outcome (summary): all seven are disposition (1).** None is tangent. None is deleted (non-deletion clause). No orphan-fill performed.

## 2. The reframe that resolves the apparent contradiction

[[DR-M5-1]] offered a binary — *document as psyche-aspect rendering material* **OR** *deprecate outright*. Tranche 12.29 (landed 2026-06-18, run `20260618T193021Z-12-T12.29`) refined the first horn: the constitutional six are **not discarded and not peer dispatch targets** — they are the **team-composition primitive at the CF-gate scale** that Anima's dispatch policy (`dispatch-policy.ts`, Tranche 12.23) reads. Both readings are simultaneously true and non-contradictory:

- **As authorial register** — when Anima renders recognition / meditation / synthesis work, the six names are the Jungian-functional *voices* (registers) it writes in. Not separate dispatch authorities (per Tranche 12.18/12.08 veto discipline: "psyche-aspect rendering … is Anima's authorial register choice — not separate dispatch authorities").
- **As team-composition member** — when Anima composes a CF1…CF5 team for a task, the six names enumerate *how many constitutional members are activated* (`cf_team_composition_gates`). CF is the shared scale with the Aletheia techne-guardian mapping (`shared_cf_scale_note`).

Anima is the seventh name and is categorically different from the other six (see §3).

## 3. Per-name disposition

| Name | CT | CF join gate | Disposition | Rationale |
|------|----|----|----|----|
| **anima** | — (dispatcher) | composes CF1–CF5 | **(1) RETAIN — dispatcher profile, not an aspect** | Anima is the *main dispatching agent* (Pi-harnessed), CF `(4.0/1-4.4/5)`. It is the lemniscate self-fold by which the six aspects become dispatchable — not a seventh worker beside them. The only one of the seven that is an actual runtime register. |
| **nous** | CT0 | CF1 | **(1) RETAIN — aspect register + CF1 team member** | Epistemic-clearing voice / pre-artifact ground; first member Anima activates at CF1 `(0/1)`. Profile is `(00/00)` Para-Vāk rendering material. |
| **logos** | CT1 | CF1 | **(1) RETAIN — aspect register + CF1 team member** | Nomos / scope-and-definition voice; joins at CF1 alongside Nous. |
| **eros** | CT2 | CF2 | **(1) RETAIN — aspect register + CF2 team member** | Relational-operator voice; joins at the Trika gate CF2 `(0/1/2)` (per Track 12.29 / DR-EROS-1, refined by Tranche 12.27). |
| **mythos** | CT3 | CF3 | **(1) RETAIN — aspect register + CF3 team member** | Pattern / archetypal-analysis voice; joins at CF3 `(0/1/2/3)`. |
| **psyche** | CT4 (4a+4b) | CF4 | **(1) RETAIN — aspect register + CF4 member + structural kernel** | Oikonomia / NOW-holder continuity voice. Most load-bearing: it is the [[vama_shakti_template_authority]] kernel (per DR-VAMA-1) and the CF `(4.0/1-4.4/5)` continuity-holder whose 6-section soul-shape the Vama Shakti factory specializes. Deprecation would be a contradiction with Track 41. |
| **sophia** | CT5 | CF5 | **(1) RETAIN — aspect register + CF5 member** | Review-crystallisation / Möbius-return voice; joins at CF5 `(0/1/2/3/4/5)`. Also the ACR-governance review role (distinct ontology, B11) — but here it is the constitutional CT5 register. |

**No name resolves to disposition (2) tangent.** The genuine tangent identified by DR-M5-1 is the **ACR extension** (`Body/M/epi-theia/extensions/agentic-control-room/`) and its `AgenticActor` union conflation — handled by Tranches 12.1 / 12.14 / DR-B-3, NOT by this audit and NOT by touching these seven profiles.

## 4. Disposition applied

### 4a. Agent profiles (write scope — RETAINED, annotated)

Per the **non-deletion clause**, none of `Body/S/S4/ta-onta/S4-4p-anima/S4'/agents/{nous,logos,eros,mythos,psyche,sophia,anima}.md` is deleted. Each is annotated with a `Disposition` footer recording: CF-bound aspect/profile material, surfaced through Anima, NOT a peer runtime agent, retained per this audit. The 6-section ANIMA.md body shape is preserved; the footer is additive. No orphan-fill — no new profile files created.

### 4b. `capability-matrix.json` (patched per outcome)

`Body/S/S4/plugins/pleroma/capability-matrix.json` already reflects the audit outcome (cleanup 2026-06-15 + Tranche 12.29):

- `constitutional_agents: []` with `_constitutional_agents_status` — the **executable-roster** reading is deprecated (the array is no longer a dispatch source of truth).
- `constitutional_ct_mapping` — the CT0–CT5 register semantics retained (disposition-1 "document as such").
- `cf_team_composition_gates` (CF1–CF5) + `cf_aletheia_techne_mapping` (CF0–CF5) + `shared_cf_scale_note` — the team-composition reading (Tranche 12.29).
- `anima_authorial_registers_deprecated` — the seven names retained as authorial-register metadata.

This audit adds an `_audit_12_3` back-reference field pointing here and recording the final disposition (all seven RETAINED; none tangent; none deleted), closing the DR-M5-1 verification loop (`grep -n "constitutional_agents" … reflects audit outcome`).

## 5. Verification

- [x] Audit doc at `plan.runs/12.3-constitutional-agents-disposition.md` (this file).
- [x] `capability-matrix.json` patched per audit outcome (`_audit_12_3` back-reference + existing deprecation/mapping/gates structure confirmed consistent).
- [x] No orphan-fill of agent profiles — zero new `.md` files; the seven existing profiles retained and annotated.
- [x] Each retained profile carries a `Disposition` footer documenting it as CF-bound aspect/profile material per DR-M5-1.

```
grep -n "_audit_12_3\|_constitutional_agents_status" Body/S/S4/plugins/pleroma/capability-matrix.json
grep -rl "Disposition (DR-M5-1 / Track 12.3" "Body/S/S4/ta-onta/S4-4p-anima/S4'/agents/"   # → 7 files
```
