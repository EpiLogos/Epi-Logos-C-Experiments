# Wave C — M4' Nara Frontend Deep DELTA Matrix

**Task id:** `wave-c-m4-nara-frontend`
**Domain:** M4' Nara per-widget frontend design — everything the UX doc specifies that 11.10/11.11/11.12 + 19.4/19.6/19.11/19.12 do NOT cover.
**Scope discipline:** This matrix is the DELTA. It does not re-author the contract-canvas, highlight-mark, highlight-service, ambient-state-strip, tuning-bar, m4_session_open, contemplation RPC, Khora flow-watcher, chronos_response_orbit, chronos_reentry, khora_write_highlighted_inscription, or mercurius_kairos_now — those have authoritative tranches already. Every claim here is either (a) absent from those tranches or (b) a downstream consumer that needs its own widget body. Cycle 2 substrate inheritance is honoured: `m4-nara` extension scaffold (`src/common/nara-surface.ts` + `src/browser/m4-nara-widget.tsx`, contract `2026-06-01.07-T7`) is the consume-as-is base; `personal_identity.rs`, `nara_journal.rs`, `m4.h`/`m4.c`, `medicine.rs`, `oracle.rs`, kerykeion adapter chain, graphiti-runtime privacy boundary, and the `nara.*` gateway routing through `S4S5DomainAdapter` are all landed.
**Anti-greenfield posture:** Every widget in this matrix targets an EXISTING substrate field, an EXISTING vault-path convention, an EXISTING M4 sub-branch domain, or an EXISTING gateway RPC surface. The m4-nara extension itself is `first-build allowed` only for these named M' product widget surfaces (no surface currently owns them); first-build was already ratified at the extension level by contract `2026-06-01.07-T7`. No widget here proposes a new substrate module.

## Source list (corpora actually consulted)

- **Corpus 1 (UX intent)** — `Idea/Pratibimba/System/Subsystems/Nara/nara-ux-full-m4-branch-update.md` (1321 LOC; §§2.1–2.7, 4.1–4.3, 5.1–5.4, 6.1–6.4, 7.1–7.4, 8.1–8.8, 10.1–10.7, 12.1–12.3, 13.1–13.5)
- **Corpus 2 (M' Seed authority)**
  - `Idea/Bimba/Seeds/M/M4'/M4'-SPEC.md` (315 LOC; §§6.1–6.7, 7.1–7.13)
  - `Idea/Bimba/Seeds/M/M4'/M4-ARCHITECTURE.md` (788 LOC; §§2, 4.3, 5.3, 7, 8.7)
  - `Idea/Bimba/Seeds/M/M4'/2026-06-04-prospective-retrospective-canvas-spec.md` (canvas spec §§2.1–2.5, 3.1, 4.1, 6.4, 7)
  - `Idea/Bimba/Seeds/M/M4'/m4-prime-psychoid-cymatic-field-engine.md` (§§2–8)
  - `Idea/Bimba/Seeds/M/M4'/m4-prime-nara-day-episodes-and-oracle-artifacts.md`
  - `Idea/Bimba/Seeds/M/M4'/nara-m4-0-0-birthdate-encoding-spec.md`
  - `Idea/Bimba/Seeds/M/M'-SYSTEM-SPEC.md` (shell-layer §`Shell 0/1 Split`)
- **Corpus 3 (substrate)**
  - `Body/S/S0/epi-lib/include/m4.h` (772 LOC) + `Body/S/S0/epi-lib/src/m4.c` (673 LOC)
  - `Body/S/S0/portal-core/src/personal_identity.rs` (q_personal, ElementalBalance, PersonalResonance, compose_personal_quaternion, from_kerykeion_json)
  - `Body/S/S0/portal-core/src/nara_journal.rs` (artifact taxonomy + day archive)
  - `Body/S/S0/portal-core/src/kernel.rs` (MathemeHarmonicProfile.resonance, conjugate_form_character, codon_charge_quaternion)
  - `Body/S/S0/portal-core/src/vak_address.rs` (Cl(4,2) carrier types — `cpf, ct, cp, cf, cfp, cs` lattice)
  - `Body/S/S0/epi-cli/src/nara/wind.rs`, `epi-cli/src/nara/medicine.rs` (CHAKRA_BODY_ZONES[8], DECAN_BODY_PARTS[36], DECAN_HERBS[36], PLANET_CHAKRA[8], ELEMENT_CHAKRA, SIGN_ELEMENT), `epi-cli/src/nara/oracle.rs` (PIP_DECAN_MAP, COURT_SIGN_MAP, HEXAGRAM_BODY_DYNAMICS[64]), `epi-cli/src/nara/transform.rs`, `epi-cli/src/nara/lens.rs`, `epi-cli/src/nara/logos.rs`, `epi-cli/src/nara/pratibimba.rs`, `epi-cli/src/nara/identity.rs`, `epi-cli/src/nara/weights.rs`, `epi-cli/src/vault/pasu.rs`, `epi-cli/src/vault/kairos.rs`
  - `Body/S/S3/gateway/src/spacetime.rs` (record_oracle_draw + Tranche 5.17 OracleSpreadPosition target), `Body/S/S3/graphiti-runtime/src/lib.rs` (privacy boundary literal)
  - `Body/S/S4/ta-onta/S4-3p-chronos/S3'/kairos-python-adapter.ts`
- **Corpus 4 (Theia surface)** — `Body/M/epi-theia/extensions/m4-nara/{package.json, src/common/{index.ts, nara-surface.ts}, src/browser/{frontend-module.ts, m4-nara-widget.tsx}, style/index.css}`; contract `2026-06-01.07-T7`. `M4NaraWidget.PRIMARY_VIEW_ID = 'm4.nara.dayContainer'`; `ALL_VIEW_IDS = ['m4.nara.dayContainer','m4.nara.graphitiBrowser','m4.nara.personalField']`; `PRIVACY_CLASS = 'protected_local_handle_only'`; `DECLARED_BLOCKERS` enumerated.
- **Cross-tranche references** — 05-m4-nara-reconciliation (Tranches 5.1, 5.2, 5.4, 5.5, 5.6, 5.7, 5.8, 5.9, 5.11, 5.15, 5.16, 5.17, 5.18, 5.19, 5.20); 11-theia-shell (Tranches 11.3, 11.10, 11.11, 11.12); 15-ui-design-foundations (Tranches 15.1, 15.2, 15.3, 15.4, 15.5, 15.6, 15.7, 15.10, 15.12); 19-contemplation-surface-integration (Tranches 19.2, 19.4, 19.6, 19.7, 19.11, 19.12); 08-integrated-4-5-0-recognition; 06-m5-epii-reconciliation.

## Standing invariants honored

- **Anti-greenfield (M4 surface band):** the canvas/highlights/ambient/tuning stack lives in 11.10–11.12. The contemplation gateway lives in 19.6. The flow-watcher / response-orbit / re-entry trio lives in 19.11. The Mercurius kairos populator lives in 19.12. Wave-C MUST NOT re-author them. Where Wave-C widgets read or write from these, the matrix names the consumed primitive verbatim and links the owning tranche.
- **M4 protected-local privacy class.** Every widget body that touches `q_personal`, `Q_composed`, journal/dream/oracle interpretation body, kerykeion natal raw, PASU.md content, Q_activity raw, Graphiti episode bodies, or `c_5_quintessence_hash` raw bytes obeys `PRIVACY_CLASS = 'protected_local'`. Public-bridge projection (`buildPublicProfilePayload`) and S2 canonical projection (`buildS2CanonicalProjection`) NEVER carry the body. The shell ledger (Tranche 11.9 / 14) asserts the projection invariants.
- **SharedBridgeAdapter only.** `forbiddenDirectImports = ['Body/S/S0','Body/S/S2','Body/S/S3','Body/S/S5','neo4j-driver']` (`m4-nara` contract). Every Wave-C widget consumes profile, readiness, coordinate context, and gateway RPC through `@pratibimba/m-extension-runtime` `SharedBridgeAdapter`. No widget imports `portal-core`, `nara_journal.rs`, `medicine.rs`, `oracle.rs`, kerykeion adapter, or graphiti-runtime types directly.
- **Cl(4,2) is ONE algebra.** `portal-core/src/vak_address.rs` exposes `cpf, ct, cp, cf, cfp, cs` carrier types and `VakAddress`. Per DR-M4-2 clause 2 the axis order is `[w=Earth, x=Fire, y=Water, z=Air]`. Per DR-M4-2 clause 5 the polarity is `0=cosmic, 1=personal` ALL THE WAY DOWN — every Wave-C widget surface defaults to polarity 1 (personal) and inherits the cosmic complement only when composed in `4-5-0` per 15.4. Per 5.16 the L2' canonical element-ID set (`0=AETHER, 1=EARTH, 2=WATER, 3=AIR, 4=FIRE, 5=SALT`) is the single element vocabulary for every element-bearing render.
- **DR-M4-1 vault path.** Canonical day path is `${VAULT_ROOT}/Idea/Empty/Present/{YYYY}/{MM}/W{WW}/{DD}/`. Every Wave-C widget that resolves a day, NOW.md path, or DayContainer artifact obeys it. The matrix consumes the (forthcoming Tranche 5.2) aligned `dayContainerPath(vaultRoot, dayId)` — no widget here hardcodes a divergent path.
- **CT4b NOW + CT0 FLOW templates** consume the existing canonical NOW.md and FLOW.md schemas. Tranche 5.19 lands `c_3_tranche_mode`, `c_3_response_orbit`, `c_3_klein_weighting`, `c_3_briefing_emitted` keys; Wave-C widgets bind them as displayed state. `t_4_kairos_context: "[[Kairos]]"` is the kairos-enrichment marker.
- **Two roles, one substrate (per 15.4).** Every widget that ships standalone in `ide-deep` MUST also be composable in the `4-5-0` personal plugin's editor-area composition (per 15.4 + 08); no widget owns Hopf-linked-torus rendering exclusively (psychoid renderer's first-build ownership is in the m4-nara extension itself per Wave-A M4 ORPHAN closure + Tranche 5.5; Wave-C only wraps it).

## The 4-way matrix

| # | Claim (UX doc) | Spec authority (M' Seed) | Code/substrate evidence | Theia surface | Status |
|---|---|---|---|---|---|
| MN-01 | Day calendar nav surface across `Empty/Present/{YYYY}/{MM}/W{WW}/{DD}/` is a first-class M4 navigation widget (UX §6.1 "DAY/NOW header"; UX §12.3 "Daily surface" panel list; UX §2.6 M4-4 "lived and dream evidence"). | M4'-SPEC §6.6 (DayContainer + canonical day path); DR-M4-1 ratified to `Idea/Empty/Present/{day_id}/`; M4-ARCHITECTURE §2.5 day-path coherence. Canvas spec §3.1 frontmatter schema additions land on the same path. | `Body/S/S0/epi-lib/include/m4.h:268-276` `m4_snapshot_now()`; `epi-cli/src/vault/day.rs` (`epi vault day-init`) writes the path canon; portal-core `nara_journal.rs` artifact ledger reads from the same root. No code-side change required by Wave-C — pure consumer. | `m4-nara/src/common/nara-surface.ts:215` `readNaraDayContainer({vaultRoot,dayId})` exists but only resolves ONE day; **no calendar / week / month navigation widget exists.** Wave-C lands one widget that pivots day selection via SharedBridgeAdapter coordinate context + ISO-week navigation. | **SPEC-AHEAD** — substrate provides single-day reader; multi-day calendar nav surface does not exist. First-build allowed (no surface currently owns it). |
| MN-02 | DayContainer detail view exposes the per-day session list, `c_3_klein_weighting` on each NOW.md, `c_4_artifact_role` of every artifact, and links to canvases (UX §12.1 "Required Artifact Fields"; UX §6.3 "Graphiti episode trajectories"). | M4'-SPEC §6.6 (NaraDayContainer typed shape); canvas spec §3.1 `c_3_*` keys (Tranche 5.19); M' frontmatter law (`coordinate`, C-family keys). | `nara-surface.ts:62-74` `NaraDayContainer` carries `artifactTree`, `graphitiEpisodes`, `nowLineage`, `artifactCounts`, `scalarRefs`. Wave-C widget reads these; widget never loads bodies. | Current `M4NaraWidget` (line 116-135) renders the DayContainer as a 4-row dl (`Day`, `Artifacts`, `Graphiti episodes`, `Privacy`). No per-session breakdown, no Klein-weighting badge per session, no artifact-role chip, no canvas deep-link per artifact. | **SPEC-AHEAD** — `NaraDayContainer` shape is rich; widget render is thin. Wave-C widget surfaces the per-session breakdown without loading any body. |
| MN-03 | Journal entries sidebar — timeline of NOW.md inscriptions across days, daily-0-1 activity-bar mode (UX §6.1 "lean personal-field sidebar"; 15.3 "Modes for `daily-0-1`: … Journal Entries"). | 15-foundation §"Personal-side of `daily-0-1`" line 41: "Left sidebar (activity-bar): Day Calendar … · Journal Entries · Personal Coordinate". | `nara-surface.ts` artifact ledger groups by `kind: 'journal'` already (line 525-526). No sidebar widget consumes it. | The activity-bar mode `Journal Entries` is named in 15.3 but no extension owns it. ORPHAN unless m4-nara contributes. | **SPEC-AHEAD / ORPHAN** — 15.3 activity-bar mode is layout-declared with no owning extension. Wave-C lands the m4-nara contribution. |
| MN-04 | PASU.md identity setup is a guided wizard surface (UX §2.1 M4-0 identity-system evidence; PASU.md birth-date / birth-location / natal-chart-path / jungian / gene-keys / human-design). | M4'-SPEC §7.2 Kerykeion natal → q_personal; M4'-SPEC §7.10 six-layer identity branch; nara-m4-0-0-birthdate-encoding-spec; M4-ARCHITECTURE §7. PASU residency at `Idea/Pratibimba/Self/PASU.md` ratified per MEMORY.md. | `epi-cli/src/vault/pasu.rs::PasuRecord` + `epi vault pasu show/get/set birth-date|birth-location|natal-chart-path` CLI; `epi-cli/src/nara/identity.rs::Identity::set` for jungian/gene-keys/human-design augment; `personal_identity.rs::from_kerykeion_json(...)` consumes natal-chart-path. Substrate is real and CLI-driven. | No PASU edit surface exists in `m4-nara` extension. CLI is the only path today. | **SPEC-AHEAD** — substrate complete; surface absent. Wave-C lands a stepper wizard widget. |
| MN-05 | Quintessence hash + clock display widget reads `c_5_quintessence_hash` + `c_5_quintessence_clock` from PASU as read-only carry (UX §2.2 #4.4.4.4 personal-Pratibimba quintessence). | M4-ARCHITECTURE §5.3.1 quintessence identity hash; MEMORY.md "M4 Quintessence Identity Hash: oracle charges → unit quaternion → clock position → BLAKE3"; cosmic clock spec §"M4 Quintessence Identity Hash". | `epi-cli/src/vault/pasu.rs::PasuRecord.c_5_quintessence_hash + c_5_quintessence_clock`; `spacetime.rs::quintessence_hash_blake3` (read via gateway). `m4_mobius_return` XORs `wisdom_delta` into hash (T19.7). | No quintessence display in m4-nara extension. | **SPEC-AHEAD** — substrate emits both values; widget absent. Wave-C lands read-only display. |
| MN-06 | Personal cymatic field renderer (Hopf-linked tori at personal scale, dipyramid + P0/P0' axis-point per DR-IG-6) is the editor-area composition center of the `4-5-0` plugin AND a standalone widget in `m4-nara` per UX §6.3 deep surface (UX §6.3 "#4.4.4.4 personal field rendering"; 15.4 personal-side editor area). | M4'-SPEC §7.3a Hopf projection; psychoid-cymatic-field-engine §§2–5; M4-ARCHITECTURE §5.3.1 dipyramid geometry (DR-IG-6); Tranche 5.5 minimum-viable solver lands the `psychoid_cymatic` module; Tranche 5.12 dipyramid geometry sweep VALIDATED. Per 15.4 the personal cymatic field is the central composition slot of the `4-5-0` plugin. | Tranche 5.5 lands `portal-core::psychoid_cymatic` opaque renderer handle. No renderer surface today. `nara-surface.ts:83-91` `ProtectedPersonalFieldInput` declares the input handle contract but `renderProtectedPersonalField` (line 322) emits handle-only ledger, not a field render. | The `m4.nara.personalField` view id is declared in `ALL_VIEW_IDS` (`m4-nara/src/common/index.ts:11`) but no widget body exists. | **SPEC-AHEAD / ORPHAN** — view id is declared, renderer is named at 5.5, dipyramid geometry is ratified (DR-IG-6), but no widget body. Wave-C wraps the renderer handle in a `ReactWidget` with subscriber lifecycle. |
| MN-07 | Personal coordinate Q_personal current-state indicator (left-sidebar activity-bar in `daily-0-1` per 15.3) renders resonance + conjugate-form + lean identity sidebar (UX §6.1 "light resonance indicator … lean elemental/personal-field sidebar"; §6.5 "no quaternion-dump"). | M4'-SPEC §7.3 resonance metric; §7.5 MathemeHarmonicProfile.resonance + conjugate_form_character; 15.3 "Personal Coordinate (Q_personal current state)" activity-bar mode. Tranche 5.1 renders the per-artifact / day-summary version; this is its activity-bar-sidebar twin. | `personal_identity.rs::PersonalResonance::from_quaternions(q_personal, q_cosmic)`; `kernel.rs:486` populates `MathemeHarmonicProfile.resonance`; `ConjugateFormCharacter` re-exported via `harmonic_profile.rs`. | Tranche 5.1 lands the per-artifact render; the activity-bar-sidebar Q_personal indicator is its complement. m4-nara extension carries no sidebar widget. | **SPEC-AHEAD** — substrate ships resonance and ConjugateFormCharacter; activity-bar mode `Personal Coordinate` is declared in 15.3 with no owner. Wave-C lands the activity-bar contribution. |
| MN-08 | Oracle cast widgets — I-Ching (3-coin) + Tarot draw with psyche-anchor (UX §2.4 M4-2 oracle/symbolic dialect; UX §6.3 "oracle inspectors"; UX §12.3 deep panel "Oracle inspectors"). | M4'-SPEC §6.6 Quaternal Tarot/I-Ching as first-class M4' artifact kinds; canvas spec §7 daily-briefing live-spreads consumer; M3 codon ↔ Tarot decan-map per MEMORY.md. Tranche 19.4 lands `m4_session_open` → `m4_draw_tarot` as session-context inheritance. Tranche 5.11 lands OracleFrame + symbolic protein refs. | `epi-cli/src/nara/oracle.rs` (CLI: `oracle cast iching|tarot`, `oracle history`, `oracle hygiene`); `Body/S/S0/epi-lib/include/m4.h:298-313` `M4_Tarot_Draw`; `m4_draw_tarot` at `m4.h:765-766`; `oracle.rs::PIP_DECAN_MAP[9][4]`, `COURT_SIGN_MAP[16]`, `ACE_ELEMENT_MAP[4]`, `HEXAGRAM_BODY_DYNAMICS[64]`. | No oracle widget in `m4-nara` extension. CLI is the only path. | **SPEC-AHEAD** — substrate is rich; cast widgets absent. Wave-C lands a single CompositeOracleWidget consuming both modalities. |
| MN-09 | Oracle history viewer — past casts, decay window state, hygiene state (UX §6.3 "oracle inspectors"; M4'-SPEC §7.3 "decay window" on Q_activity). | M4'-SPEC §6.6 oracle artifact kind; Tranche 5.11 OracleFrame extension; Tranche 5.17 OracleSpreadPosition aliveness state (generating / muting / mute); `QActivityUpdatePolicy.decayWindowOpen` already in `nara-surface.ts:76-81`. | `nara-surface.ts::inspectOracleArtifact` (line 351) returns the per-artifact inspector record (handle, scalarRefs, lensApplications, qActivityUpdate). Tranche 5.17 lands `OracleSpreadPosition.live_state` in SpacetimeDB. | No history viewer in `m4-nara` extension. | **SPEC-AHEAD** — substrate inspector + Tranche 5.17 aliveness API; widget absent. Wave-C lands the viewer. |
| MN-10 | Medicine view — chakra-body-zone / decan-body-part / herbalism herbs / elemental balance surface (UX §2.3 M4-1 "body, transit, somatic-medicinal field"; UX §12.3 "M4-1 body correlations"; UX §8.2 M4-1↔M4-3). | M4'-SPEC §7 M4-1 medicine layer; canvas spec §6.4 "briefing reads, it does not reinvent" (consumer law); MEMORY.md "medicine.rs full LUTs"; Tranche 5.16 L2' canonical element-ID harmonisation. | `epi-cli/src/nara/medicine.rs`: `CHAKRA_BODY_ZONES[8]`, `DECAN_BODY_PARTS[36]`, `DECAN_HERBS[36]`, `PLANET_CHAKRA[8]`, `ELEMENT_CHAKRA[5→6 post-5.16]`, `SIGN_ELEMENT[12]`, `balance()`, `chakra()`, `materia()`, `prescribe()`, `body_zones_for_elem_sig()`. | No medicine view in `m4-nara` extension. | **SPEC-AHEAD** — substrate is complete; widget absent. Wave-C lands a read-only Medicine widget. |
| MN-11 | Transform containers — Bohm dialogue / Talking Circle / Diamond container surfaces with cycle stages + alchemical ops (UX §2.5 M4-3 "transformation processor"; canvas spec §6.5 alchemical-tonal register). | M4'-SPEC §7 M4-3 layer; MEMORY.md "transform: containers (Bohm/TalkingCircle/Diamond)"; canvas spec §6.5 alchemical vocabulary (nigredo / solutio / sublimatio / calcinatio / coagulatio / fixatio / separatio / conjunctio). | `epi-cli/src/nara/transform.rs` (CLI: `epi nara transform`); container + cycle + alchemical operator types. | No transform widget in `m4-nara` extension. | **SPEC-AHEAD** — substrate present; widget absent. Wave-C lands transform widget. |
| MN-12 | Lens application widget — jungian / trika / phenomenal apply + synthesize (UX §3.2 M4-3 native MEF basin; UX §12.2 LensPositionRouter service). | M4'-SPEC §7 M4-4 lens layer; MEMORY.md "lens: list/jungian/trika/phenomenal/apply/synthesize"; canvas spec §6.5 lens-square / route language. | `epi-cli/src/nara/lens.rs` (CLI: `epi nara lens list/jungian/trika/phenomenal/apply/synthesize`). | No lens widget in m4-nara extension. | **SPEC-AHEAD** — substrate present; widget absent. Wave-C lands lens widget. |
| MN-13 | Logos cycle stage UI — 6-stage A-Logos → An-a-Logos progression (UX §2.5 M4-3 transformation arcs; UX §2.6 M4-4 evidence). | M4'-SPEC §7 M4-5 logos branch; MEMORY.md "logos: 6-stage cycle (A-Logos→An-a-Logos), stage files". | `epi-cli/src/nara/logos.rs` (CLI: `epi nara logos`); 6 stage progression. | No logos widget in m4-nara extension. | **SPEC-AHEAD** — substrate present; widget absent. Wave-C lands logos widget. |
| MN-14 | Pratibimba personal coordinate widget (Neo4j-backed, consent-gated atlas-sync) at `#4.4.4.4` (UX §2.2 "#4.4.4.4 is the living personal-Pratibimba locus"; UX §10.7 protected-local; UX §2.7 M4.5 review and canon teaching). | M4'-SPEC §7.1 + §7.6 personal-quaternion strict privacy class; §6.7 atlas-sync consent gates; M4-ARCHITECTURE §7. | `epi-cli/src/nara/pratibimba.rs` Neo4j-backed stubs (consent-gated atlas-sync); `nara-surface.ts::ConsentRecord` + `evaluateVoiceCorpusAdmission` already model the consent path. | No pratibimba widget in m4-nara extension. | **SPEC-AHEAD** — substrate stub + consent model present; widget absent. Wave-C lands consent-gated pratibimba widget. |
| MN-15 | Kairos display widget — current natal + transit; 3 temporal layers (natal stable / transit daily / oracle 4h decay) (UX §2.2 "Three temporal modes"; UX §6.1 daily resonance indicator). | M4'-SPEC §7.2 Kerykeion natal; MEMORY.md "Three temporal modes: natal (stable) + transit (daily kairos) + oracle (4h decay)"; cosmic clock §"4 nested backbone rings"; Tranche 19.12 mercurius_kairos_now populator. | `Body/S/S4/ta-onta/S4-3p-chronos/S3'/kairos-python-adapter.ts`; `epi-cli/src/vault/kairos.rs::fetch|status|show` CLI; `M4_Temporal_Now.planet_degrees[10]` (post-19.12). | No kairos widget in `m4-nara` extension. | **SPEC-AHEAD** — substrate adapter chain + CLI present; Tranche 19.12 populates planet_degrees[10]; widget absent. Wave-C lands the kairos widget reading from the populated field. |
| MN-16 | Mercurius signal-relay UI — kairos refresh indicator + planet-degree[10] live read (UX §2.2 transit layer; 19.12 Mercurius CF3 signal-relay). | 19.12 `mercurius_kairos_now(natal_ref) -> M4_Temporal_Now`; MEMORY.md "Mercurius (CF(0/1/2/3)) carries kairos signal: PASU.md → Kerykeion → M4 degrees → agents". | Tranche 19.12 wires `Body/S/S4/ta-onta/S4-5p-aletheia/modules/mercurius-relay.ts`. Mod-10 planet order (Sun=0..Pluto=9; Earth NOT in array) per DR-M2-1 + Track 03.5. | No mercurius UI surface in m4-nara extension. | **SPEC-AHEAD** — Tranche 19.12 closes substrate; Wave-C lands the UI indicator (consumes, does not re-author). |
| MN-17 | Three-mode time-axis switcher (natal / real-time / kairotic) (UX §5.1 Q_identity natal; §5.2 Q_transit real-time; §5.3 Q_activity kairotic). | M4'-SPEC §7.3a Q_composed = Q_identity · Q_transit · Q_activity; canvas spec §3.1 frontmatter `c_3_klein_weighting` carries the prospective/retrospective sense; the time-axis switcher is the user control over WHICH composition reading is foregrounded in the field renderer. | `personal_identity.rs::compose_personal_quaternion(q_identity, q_transit, q_activity)`; `nara-surface.ts::ProtectedPersonalFieldInput.qIdentityHandle/qTransitHandle/qActivityHandle/qComposedHandle` already typed. | Tuning bar (11.12) covers tranche-mode / response-orbit / sense-override; the three-mode time-axis switcher is a DIFFERENT control surface and is not in 11.12. No widget today. | **SPEC-AHEAD** — typed handle inputs exist; switcher absent. Wave-C lands a distinct three-mode time-axis control surface. |
| MN-18 | Privacy class enforcement display — every artifact shows `PRIVACY_CLASS: protected_local` indicator; no public-bridge crossings (UX §10.7 "protected-local"; UX §13.2 "pratibimba namespace is the data-seam"). | M4'-SPEC §7.6 strict privacy class; M4-ARCHITECTURE §4.3 DR-M4-3 strict-invariant ("no raw bodies cross the bus"); contract `2026-06-01.07-T7` `PRIVACY_CLASS = 'protected_local_handle_only'`. | `nara-surface.ts:280` per-event `protectedBodiesRendered: false`; `buildPublicProfilePayload` (line 297) returns empty `bodyFields`; `buildS2CanonicalProjection` (line 287) sets `protectedBodiesIncluded: false`. Substrate enforcement is real. | Current widget (m4-nara-widget.tsx:78) renders a single provenance string. **No persistent privacy-class chrome** indicating the protected-local boundary visually on every artifact and every composed view. | **SPEC-AHEAD** — substrate enforces invariant; surface lacks consistent visual indicator. Wave-C lands a small persistent chrome contribution. |
| MN-19 | Wisdom delta XOR closure display at session-close (UX §13.5 "The One Loop"; T19.7 m4_mobius_return XOR; T19.6 contemplation gateway). | T19.7 `wisdom_delta` XOR into `quintessence_hash`; T19.6 contemplation RPC returns the delta; M4-ARCHITECTURE §"Möbius return"; M5'-SPEC review-core; MEMORY.md "M4 Quintessence Identity Hash". | T19.7 wires `m5_compose_contemplation_object` + gateway RPC + `m4_mobius_return(epii, identity)` inside `m5_execute_mobius_return`. `wisdom_delta` is bioquaternionic resonance-correction encoded as quaternionic-difference bytes. | No close-of-session ceremony display in m4-nara extension. | **SPEC-AHEAD** — T19.7 closes substrate; Wave-C lands a visible session-close pane showing the `wisdom_delta` byte trail folding into `quintessence_hash` (read-only carry from gateway). |
| MN-20 | Tarot psyche-anchor coherence panel — which cards drawn at session-open, which codons appeared in the trajectory, coherence read (T19.4 + T19.6 psyche-anchor coherence question). | T19.4 `m4_session_open` draws `M4_Tarot_Draw` as session-context inheritance; T19.6 reads "Did the tarot psyche-anchor's cards correspond to codons that appeared in the trajectory?". `m3_major_arcana_from_codon` utility lands at T19.5. | `m4.h::M4_Session_Frame.tarot_psyche_anchor` (post-T19.4); `m3.h::m3_major_arcana_from_codon(codon)`. | No psyche-anchor panel in m4-nara extension. | **SPEC-AHEAD** — substrate complete after T19.4-T19.5; widget absent. Wave-C lands the coherence panel. |
| MN-21 | `t_4_kairos_context: "[[Kairos]]"` frontmatter indicator on kairos-enriched artifacts (MEMORY.md kairos frontmatter signal; canvas spec §3.1 — though §3.1 does not call out this key, MEMORY.md does). | MEMORY.md kairos paradigm: "`t_4_kairos_context: "[[Kairos]]"` = T4 frontmatter key marking kairos-enriched artifacts"; 19.12 sets it on any artifact whose write triggered a kairos refresh. | Khora writers (`khora_write` + `khora_write_highlighted_inscription` per 19.11) set the key when kairos delta is non-empty. | No frontmatter-aware artifact chip in m4-nara extension. | **SPEC-AHEAD** — substrate sets the key; widget does not surface it. Wave-C extends the DayContainer detail view (MN-02) with a kairos-context chip per artifact reading `t_4_kairos_context`. |
| MN-22 | Composition with M5 epii + M0 anuttara in `4-5-0` plugin per 15.4 (UX §6.4 "Integrated 4/5/0 Recognition Surface"; UX §13.5 "The One Loop"). | 15.4 composition-pattern; 08-integrated-4-5-0-recognition; M'-SYSTEM-SPEC §"Integrated Plugins"; M4'-SPEC §6 personal-side parent; M5'-SPEC §3 review gate; M0'-SPEC anuttara ground tone. | `plugin-integrated-4-5-0/package.json` depends on `m4-nara` + `m5-epii` + `m0-anuttara` + `integrated-composition`. | The integrated-4-5-0 plugin is scaffolded but does not yet COMPOSE m4-nara widgets into its editor area. Wave-C widgets must declare their composition contracts via `TRACK_08_CONTRIBUTION` so they can be picked up. | **SPEC-AHEAD** — every Wave-C widget MUST declare its `TRACK_08_CONTRIBUTION` mini-mode (badge / compact-card / inspector) per existing `m4-nara/src/common/index.ts:23-36` pattern. Cross-link Tranche 25.N integrated-composition surface (and Wave-C tranche on integrated 4-5-0 — Tranche 28 if it lands). |

## Anomalies

### CONTRADICTIONS (decision-register candidates)

- **DR-WC-M4-1 — Three-mode time-axis switcher vs tuning bar sense-override (MN-17 / 11.12).** Tuning bar (11.12) carries `sense override` (prospective / retrospective Klein face). The three-mode time-axis switcher (MN-17) carries `natal / real-time / kairotic` (which composition reading is foregrounded). These are ORTHOGONAL controls — Klein sense is the "from which face" axis (per Tranche 5.15), time-axis is the "of which composition" axis. **Decision required:** confirm they remain separate widget surfaces with separate keystroke + persistent state, OR collapse them into a single 2D tuning matrix. Recommend separate (different axes, different consumer law).
- **DR-WC-M4-2 — Q_personal renderer (MN-06) ownership: m4-nara extension vs `4-5-0` integrated plugin composition (per 15.4 personal-side editor area).** 15.4 places "personal cymatic field (Hopf-linked tori at personal scale, M4 psychoid renderer)" as the CENTER composition slot of the `4-5-0` plugin. M4'-SPEC §7.7 + Tranche 5.5 + canvas spec §2.4 imply the renderer can also live standalone in `ide-deep` on `m4-nara`. **Decision required:** confirm "one renderer surface, two compose sites" — single React component implementing `m4.nara.personalField` view, registered as TRACK_07 standalone widget AND TRACK_08 composition contribution. Recommend single source.
- **DR-WC-M4-3 — Identity setup wizard write path (MN-04): widget calls SharedBridgeAdapter.invokeGatewayRpc → `nara.pasu.set` (new RPC) vs widget delegates to `epi vault pasu set` CLI shell-out.** Wave-A M4 establishes `nara.*` route through `S4S5DomainAdapter`. PASU writes are protected-local. **Decision required:** define `nara.pasu.set` RPC and route through gateway (the canonical path), avoiding a CLI shell-out from the renderer.
- **DR-WC-M4-4 — Atlas-sync consent persistence boundary (MN-14): consent record local-protected vs reviewable.** UX §10.7 says protected-local; M4'-SPEC §7.6 says consent records may be reviewable when explicit. `nara-surface.ts::ConsentRecord` is typed but its residency law (where it stores) is not declared. **Decision required:** define ConsentRecord residency as `Idea/Pratibimba/Self/PASU.md`'s `c_4_atlas_sync_consents` array (PASU-resident) per the MEMORY.md PASU vocabulary.
- **DR-WC-M4-5 — Privacy-class chrome (MN-18) vs status-bar discipline (15.10).** 15.10 status bar surfaces "profile-tick state, day-now anchor, session id, gateway readiness, profile generation, active coordinate. Nothing else." Privacy-class indicator is NOT in the 15.10 list. **Decision required:** confirm privacy-class indicator lives on each widget's chrome (border-tint per privacy class) and NOT in the status bar, per 15.10 discipline. Recommend per-widget chrome.

### CODE-PENDING (named, classified, NOT proposed for greenfield rebuild)

- The personal-cymatic field renderer minimum-viable solver (MN-06) is Tranche 5.5's deliverable. Wave-C consumes its handle; it does not re-author the solver.
- The OracleSpreadPosition aliveness state (MN-08 live spreads in oracle widget, MN-09 history viewer) is Tranche 5.17's deliverable. Wave-C consumes the SpacetimeDB rows via gateway RPC.
- The `psychoid_cymatic` opaque renderer handle and the `(q_b, q_p)` decomposition (Tranche 5.4) feed MN-06. Both are owned upstream.
- L2' canonical element-ID harmonisation (Tranche 5.16) is a prerequisite for MN-10 medicine and the ambient strip somatic signature (11.12). Wave-C medicine widget consumes post-5.16 IDs.
- `M4_Temporal_Now.planet_degrees[10]` populator (Tranche 19.12) feeds MN-15 kairos display and MN-16 mercurius UI. Wave-C consumes post-19.12.
- The wisdom_delta XOR closure (MN-19) is wired in Tranche 19.7. Wave-C reads the result via gateway and renders.
- Identity-augment proposal lifecycle (Tranche 5.9) `proposed → reviewed → accepted|rejected → applied` is the M4-side adapter; MN-14 pratibimba widget reads pending proposals via that adapter.

### ORPHAN warnings

- **MN-03 Journal Entries activity-bar mode** — declared in 15.3 with no owning extension. Wave-C closes by m4-nara contribution.
- **MN-06 Personal cymatic field widget body** — `m4.nara.personalField` view id declared in `ALL_VIEW_IDS` but no body exists. Wave-C closes by m4-nara contribution.
- **MN-07 Personal Coordinate activity-bar mode** — declared in 15.3 with no owning extension. Wave-C closes by m4-nara contribution.

## Proposed Cycle-3 closing tranches (m4-nara frontend domain — id space `25.x`)

> All tranches obey anti-greenfield in the M' product surface band: m4-nara extension owns these widgets (first-build per `2026-06-01.07-T7` contract); every widget consumes a landed substrate primitive or a tranche-closed upstream; nothing re-authors 11.10/11.11/11.12 or 19.4/19.6/19.11/19.12.

### 25.1 — Day-Calendar navigation widget (MN-01)

- **Classification:** spec-ahead-integration (first-build for `m4.nara.dayCalendar` view)
- **Source:** MN-01; DR-M4-1 canonical day path; 15.3 activity-bar discipline.
- **Deliverable:** new `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/day-calendar.tsx` ReactWidget pivoting day selection across `${VAULT_ROOT}/Idea/Empty/Present/{YYYY}/{MM}/W{WW}/{DD}/` with ISO-week roll-over; dispatches `nara.day.open` via SharedBridgeAdapter when user selects a day; subscribes to `SharedBridgeAdapter.onCoordinateContext` so navigation updates the global coordinate state per 15-foundation principle 1.
- **Verification:** `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; widget test renders 7×N grid bound to fixture day-list; navigation test asserts ISO-week boundaries match `epi vault day-init` path canon; deep-link test asserts `cmd-K` reveals the day under the active session.

### 25.2 — DayContainer detail view with per-session breakdown + canvas deep-link (MN-02, MN-21)

- **Classification:** spec-ahead-integration
- **Source:** MN-02; MN-21 kairos chip; existing `NaraDayContainer.artifactTree`; 5.19 NOW.md frontmatter keys.
- **Deliverable:** extend `M4NaraWidget` (current scaffold) to render per-session blocks listing NOW.md sessions in `dayContainer.nowLineage` order with chips: `c_3_klein_weighting`, `c_4_artifact_role`, `c_3_briefing_emitted` timestamp (if present), `t_4_kairos_context` chip when present; each artifact row carries a deep-link to its canvas (consume 11.10's `canvas-editor.tsx` widget routing).
- **Verification:** `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; render test against synthetic DayContainer with two sessions × four artifacts; deep-link click test routes through SharedBridgeAdapter to `canvas-editor.tsx` (11.10); privacy-class assertion confirms no body field rendered.

### 25.3 — Journal Entries activity-bar widget (MN-03) — closes 15.3 ORPHAN

- **Classification:** no-orphan-fill
- **Source:** MN-03; 15.3 activity-bar discipline (cross-link Tranche 11.3 layout claim).
- **Deliverable:** new `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/journal-entries-sidebar.tsx` contributing the `Journal Entries` activity-bar mode for `daily-0-1`; renders timeline (last 30 days) of NOW.md inscriptions across days; opens each in canvas-editor (11.10) on click. Register via Theia activity-bar contribution per 15.3.
- **Verification:** `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; activity-bar registration test asserts `Journal Entries` mode is contributed and gated to `daily-0-1` only; timeline test renders synthetic 7-day inscription history.

### 25.4 — PASU identity setup wizard (MN-04, DR-WC-M4-3)

- **Classification:** spec-ahead-integration; DR-WC-M4-3 RPC decision
- **Source:** MN-04; M4'-SPEC §7.2 + §7.10; MEMORY.md PASU vocabulary; `vault/pasu.rs`.
- **Deliverable:** new `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/pasu-wizard.tsx` 6-step stepper (birth-date → birth-location → natal-chart-path (upload OR Kerykeion fetch via 19.12) → jungian → gene-keys → human-design). Writes through SharedBridgeAdapter `invokeGatewayRpc('nara.pasu.set', {key, value})` — new gateway method routed through `S4S5DomainAdapter` per DR-WC-M4-3. Read path: `invokeGatewayRpc('nara.pasu.show')`. PASU residency at `Idea/Pratibimba/Self/PASU.md` per MEMORY.md.
- **Verification:** `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; `cargo check -p epi-s3-gateway`; round-trip test write-then-read each PASU key; `grep -nE 'nara.pasu.set|nara.pasu.show' Body/S/S3/gateway/src/` returns new routes; protected-local invariant test confirms PASU body never enters `buildPublicProfilePayload`.

### 25.5 — Quintessence display widget (MN-05)

- **Classification:** spec-ahead-integration
- **Source:** MN-05; M4-ARCHITECTURE §5.3.1; PASU.md `c_5_quintessence_hash` + `c_5_quintessence_clock`.
- **Deliverable:** new `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/quintessence-display.tsx` read-only widget rendering BLAKE3 hash (first 8 bytes formatted), clock position (degree on cosmic clock 0-360), and the most-recent `wisdom_delta` byte trail (consume via T19.7 gateway result, MN-19). Subscribes to profile-tick via SharedBridgeAdapter per 15-foundation principle 2 — re-renders on every tick advance without raw quaternion exposure.
- **Verification:** `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; render test against fixture PASU + fixture wisdom_delta; profile-tick subscriber test asserts re-render on synthetic tick advance; privacy test confirms raw `q_personal` body never enters the rendered surface (handle-only).

### 25.6 — Personal cymatic field widget body (MN-06, DR-WC-M4-2)

- **Classification:** spec-ahead-integration; ORPHAN closure for `m4.nara.personalField` view; first-build under M4-ARCHITECTURE §5.3.1 dipyramid geometry (DR-IG-6 ratified)
- **Source:** MN-06; Tranche 5.5 `psychoid_cymatic` opaque renderer handle; DR-IG-6 dipyramid geometry; 15.4 personal-side editor composition slot.
- **Deliverable:** new `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/personal-cymatic-field.tsx` ReactWidget mounting the renderer handle returned from `SharedBridgeAdapter.invokeGatewayRpc('nara.field.handle')` (which wraps `psychoid_cymatic::renderer_handle()`). Renders the dipyramid (2 apex poles P5/P5' + 4 top/base P1-P4 + 4 inverted-base P1'-P4' interleaved + central axis-point P0/P0') with Hopf-linked tori at personal scale per Tranche 5.5. Honours DR-WC-M4-2: single component, registered AS `PRIMARY_VIEW_ID`-sibling `m4.nara.personalField` AND as TRACK_08 composition export `M4PersonalCymaticField`. Privacy: opaque handle only — never reaches into renderer state.
- **Verification:** `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; widget test mounts the renderer with fixture handle; geometry test asserts dipyramid renders all six P/P' positions per DR-IG-6 (P0, P0', P1..P4, P1'..P4', P5, P5'); composition test asserts the widget composes correctly under `plugin-integrated-4-5-0` editor center slot per 15.4; privacy test asserts no quaternion body crosses the bus.

### 25.7 — Personal Coordinate Q_personal activity-bar widget (MN-07) — closes 15.3 ORPHAN

- **Classification:** no-orphan-fill; spec-ahead-integration
- **Source:** MN-07; 15.3 activity-bar; Tranche 5.1 per-artifact resonance render.
- **Deliverable:** new `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/personal-coordinate-sidebar.tsx` contributing the `Personal Coordinate` activity-bar mode for `daily-0-1`. Renders lean panel (per UX §6.5 "no quaternion-dump"): resonance metric `|q_personal · q_cosmic|` (numeric 0.000–1.000), `ConjugateFormCharacter` (Major / Minor / Shadow), four element-glyphs from `ElementalBalance` via L2' canonical IDs from 5.16, dominant chakra from `PLANET_CHAKRA`/`ELEMENT_CHAKRA`. Subscribes to profile-tick for re-render per 15-foundation principle 2. Consumes via `SharedBridgeAdapter.onProfile` → `MathemeHarmonicProfileBoundary.resonance` + `conjugate_form_character`.
- **Verification:** activity-bar registration test asserts `Personal Coordinate` contributed to `daily-0-1`; render test consumes `ConjugateFormCharacter.Major` fixture; L2' element ordering test asserts (Earth, Water, Air, Fire) post-5.16 IDs; no-quaternion-dump test asserts the rendered DOM contains no `[w=,x=,y=,z=]` value strings.

### 25.8 — Composite Oracle widget (I-Ching + Tarot) (MN-08)

- **Classification:** spec-ahead-integration; consumes 19.4 m4_session_open + 5.11 OracleFrame + 5.17 OracleSpreadPosition
- **Source:** MN-08; M4'-SPEC §6.6 Quaternal Tarot/I-Ching artifact kinds; Tranche 5.11 OracleFrame + deck-context + symbolic protein refs; Tranche 19.4 m4_session_open psyche-anchor; canvas spec §7 daily-briefing live-spreads consumer; m3 tarot-decan addressing.
- **Deliverable:** new `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/oracle-cast.tsx` ReactWidget with two sub-modes (3-coin I-Ching with A=6/T=9/C=7/G=8 nucleotide encoding per MEMORY.md; Thoth-Tarot draw with psyche-anchor consuming `m4_draw_tarot` via gateway). Spread-position UI consumes `OracleSpreadPosition` rows from Tranche 5.17. Cross-link tarot-decan addressing widget from M3 (cross-link Tranche 24.x if M3 wave-C tranche lands; otherwise consume `oracle.rs::PIP_DECAN_MAP[9][4]` via gateway). Writes the cast artifact as `kind: 'oracle'` via `createNaraArtifact`; carries `vak_address`, `deck_context`, scalar M3 refs per 5.11.
- **Verification:** `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; cast-flow test for I-Ching produces hexagram + line-change + codon refs; tarot draw test produces `M4_Tarot_Draw` with psyche-anchor + decan + codon refs; OracleSpreadPosition aliveness state test (generating → muting → mute) reflected in UI; protected-local assertion confirms interpretation body stays local.

### 25.9 — Oracle history viewer (MN-09)

- **Classification:** spec-ahead-integration; depends on 25.8 + 5.17 + 5.11
- **Source:** MN-09; `inspectOracleArtifact` (nara-surface.ts:351); `QActivityUpdatePolicy.decayWindowOpen`; 5.17 OracleSpreadPosition `live_state`.
- **Deliverable:** new `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/oracle-history.tsx` ReactWidget listing past oracle casts in `dayContainer.artifactTree.filter(a => a.kind === 'oracle')` order; per-cast row shows decay window state (open / 4h-decay / closed), hygiene state, live-state from OracleSpreadPosition, and a click-open into Oracle Cast widget (25.8). Consumes via SharedBridgeAdapter `invokeGatewayRpc('nara.oracle.history', {dayRange})`.
- **Verification:** widget test against fixture artifact tree with 3 oracle casts of mixed kind + state; live-state mapping test (Tranche 5.17 enum → UI badge); decay-window expiry test confirms transitioning to closed; privacy invariant confirms no interpretation body rendered.

### 25.10 — Medicine view (MN-10)

- **Classification:** spec-ahead-integration; depends on 5.16 L2' canonical element IDs + 19.12 planet_degrees populator
- **Source:** MN-10; `epi-cli/src/nara/medicine.rs`; canvas spec §6.4 "briefing reads, it does not reinvent" consumer law.
- **Deliverable:** new `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/medicine-view.tsx` ReactWidget with three panels: (a) **Chakra-body-zones**: `CHAKRA_BODY_ZONES[8]` rendered as 8-row chakra ladder; per-row indicates dominant element from `ELEMENT_CHAKRA` post-5.16; click expands body-zone strings. (b) **Active decan**: live decan from `M4_Temporal_Now.planet_degrees[Sun]` per 19.12; renders `DECAN_BODY_PARTS[36]` for the active decan + ruling planet from `PLANET_CHAKRA[8]`. (c) **Herbalism**: `DECAN_HERBS[36]` per active decan. Consumes via `invokeGatewayRpc('nara.medicine.snapshot')`. Read-only — no `prescribe()` write surface (per UX §10.4 "body has veto power" — recommendations are evidence, not authority).
- **Verification:** L2' canonical IDs (post-5.16) verified in widget output; active-decan rotation test consumes synthetic `M4_Temporal_Now`; body-zone string assertion against `CHAKRA_BODY_ZONES[8]` fixture; no-write assertion confirms widget never mutates Q_identity, Q_activity, or M4-0 evidence.

### 25.11 — Transform container widget (MN-11)

- **Classification:** spec-ahead-integration
- **Source:** MN-11; `epi-cli/src/nara/transform.rs`; canvas spec §6.5 alchemical-tonal register.
- **Deliverable:** new `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/transform-containers.tsx` ReactWidget with three container modes (Bohm Dialogue / Talking Circle / Diamond), each carrying its cycle-stage progression. Alchemical operator overlay (nigredo / solutio / sublimatio / calcinatio / coagulatio / fixatio / separatio / conjunctio) tags each stage per canvas spec §6.5. Writes container artifact as `kind: 'contemplative'` via `createNaraArtifact` with payload `{container, stage, alchemical_op}`.
- **Verification:** widget test renders all three containers + all six core ops + two operations; stage-transition test asserts only forward-stage advance unless explicit user back-step; artifact-write test confirms `kind: 'contemplative'` envelope.

### 25.12 — Lens application widget (MN-12)

- **Classification:** spec-ahead-integration
- **Source:** MN-12; `epi-cli/src/nara/lens.rs`; UX §3.2 M4-3 native MEF basin.
- **Deliverable:** new `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/lens-application.tsx` ReactWidget with: (a) lens list panel (jungian / trika / phenomenal) backed by `nara.lens.list` RPC; (b) per-lens apply form (`nara.lens.apply({lens, subject})`); (c) synthesize panel composing multiple lens applications (`nara.lens.synthesize({applications})`). Output renders as a `kind: 'contemplative'` artifact carrying lens position references + active-square (per UX §3.2 MEF basin) + `vak_address` (per 5.11).
- **Verification:** lens-list RPC test; apply-form test against fixture subject; synthesize composition test produces multi-lens artifact with active-square assertion; privacy test confirms only the lens position + active-square fields enter `buildS2CanonicalProjection`, not the subject body.

### 25.13 — Logos cycle stage widget (MN-13)

- **Classification:** spec-ahead-integration
- **Source:** MN-13; `epi-cli/src/nara/logos.rs`; MEMORY.md 6-stage A-Logos → An-a-Logos progression.
- **Deliverable:** new `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/logos-cycle.tsx` ReactWidget showing the user's current logos stage with 6-stage progression visualization (A-Logos → An-a-Logos). Per-stage card shows the stage file content (read-only). Advance / regress controls write via `invokeGatewayRpc('nara.logos.advance|regress', {sessionKey})`. Stage transitions create `kind: 'contemplative'` artifacts.
- **Verification:** 6-stage render test; advance/regress test cycles the state machine; artifact-write test on each transition; privacy invariant.

### 25.14 — Pratibimba personal-coordinate widget with consent gating (MN-14, DR-WC-M4-4)

- **Classification:** spec-ahead-integration; DR-WC-M4-4 ConsentRecord residency decision
- **Source:** MN-14; M4'-SPEC §7.1 + §7.6; `epi-cli/src/nara/pratibimba.rs` Neo4j-backed stubs; `nara-surface.ts::ConsentRecord` + `evaluateVoiceCorpusAdmission`.
- **Deliverable:** new `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/pratibimba-coordinate.tsx` ReactWidget for `#4.4.4.4` PersonalNexus. Panels: (a) **Current state**: rendered handles from `ProtectedPersonalFieldInput` (consume `renderProtectedPersonalField`); (b) **Atlas-sync consent control**: typed `ConsentRecord` editor (action, scope, pressureFree, inspectable) with DR-WC-M4-4 residency at `Idea/Pratibimba/Self/PASU.md`'s `c_4_atlas_sync_consents` array; (c) **Pending proposals**: read-only list of `proposed | reviewed` identity-augment proposals from Tranche 5.9 adapter (`nara.identity.proposals.list`). Only `applied` proposals affect `Q_identity` — widget is consumer, not driver.
- **Verification:** widget test renders handles + consent control + proposals list; consent-record write test routes through `nara.pasu.set` (25.4) and lands in PASU array; proposal-state test asserts read-only on `proposed | reviewed`; privacy test asserts no raw `q_b, q_p` body enters the surface.

### 25.15 — Kairos display widget (MN-15)

- **Classification:** spec-ahead-integration; depends on 19.12 mercurius_kairos_now
- **Source:** MN-15; MEMORY.md three temporal modes (natal / transit / oracle); 19.12 populator; `M4_Temporal_Now.planet_degrees[10]`.
- **Deliverable:** new `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/kairos-display.tsx` ReactWidget rendering 10-planet wheel (Sun=0..Pluto=9 mod-10 per DR-M2-1; Earth as observer-centre NOT in wheel) with three layered rings: (a) natal positions (from PASU.md natal-chart-path); (b) current transit positions (from `M4_Temporal_Now.planet_degrees[10]` via 19.12); (c) oracle moment (the cast-time snapshot, if active oracle cast within 4h decay). Consumes via `invokeGatewayRpc('nara.kairos.snapshot')`. Mercurial refresh chip shows last refresh time (cross-link 25.16).
- **Verification:** mod-10 ordering test asserts Sun=0, Moon=1, ... Pluto=9 with no Earth in wheel; three-ring render test against fixture PASU + fixture `M4_Temporal_Now` + fixture oracle moment; refresh-chip subscriber test re-renders on 19.12 kairos delta event; protected-local assertion (natal data stays local).

### 25.16 — Mercurius signal-relay indicator (MN-16)

- **Classification:** spec-ahead-integration; consumes 19.12; pairs with 25.15
- **Source:** MN-16; Tranche 19.12 `mercurius_kairos_now`; MEMORY.md "Mercurius (CF(0/1/2/3)) carries kairos signal".
- **Deliverable:** new `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/mercurius-relay-indicator.tsx` small chrome contribution (24px tall) showing kairos refresh state (last refresh ISO; delta count since last; KAIROS_ENABLED=true/false from FR-3 stub gate). Subscribes to mercurius delta events via `SharedBridgeAdapter.subscribeObservability({kind: 'mercurius.kairos.delta'})`. Composes with 25.15 Kairos display as upper chrome.
- **Verification:** subscribe test asserts indicator updates on synthetic delta event; FR-3 stub-mode test asserts gracefully-disabled indicator when `KAIROS_ENABLED=false`; mod-10 ordering propagation test asserts planet count = 10 on every delta payload.

### 25.17 — Three-mode time-axis switcher (MN-17, DR-WC-M4-1)

- **Classification:** spec-ahead-integration; DR-WC-M4-1 distinct-from-tuning-bar decision
- **Source:** MN-17; M4'-SPEC §7.3a Q_composed composition; per DR-WC-M4-1 separate from 11.12 tuning bar.
- **Deliverable:** new `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/time-axis-switcher.tsx` ReactWidget — segmented control with three modes: `natal` (foregrounds `qIdentityHandle`), `real-time` (foregrounds `qTransitHandle`), `kairotic` (foregrounds `qActivityHandle`). Switching writes session-scope state via `SharedBridgeAdapter.invokeGatewayRpc('nara.session.set_time_axis', {sessionKey, mode})` and re-foregrounds the corresponding handle in the personal cymatic field renderer (25.6). Keystroke: `cmd-shift-T` to cycle modes. Persisted at session scope; default `real-time`.
- **Verification:** mode-cycle test asserts cmd-shift-T progresses through three modes; renderer-foregrounding test asserts 25.6 receives the correct handle as foreground on mode change; persistence test asserts session-scope mode survives layout switch per 15.7.

### 25.18 — Privacy-class chrome contribution (MN-18, DR-WC-M4-5)

- **Classification:** doc-ahead-landing + spec-ahead-integration; DR-WC-M4-5 per-widget-chrome decision
- **Source:** MN-18; M4-ARCHITECTURE §4.3 DR-M4-3 strict-invariant; contract `PRIVACY_CLASS = 'protected_local_handle_only'`.
- **Deliverable:** new `Body/M/epi-theia/extensions/m4-nara/style/privacy-chrome.css` with three border-tint classes (`mext-privacy-protected-local` = warm-earth `#8a7355`; `mext-privacy-protected-handle-only` = slate `#6b7588`; `mext-privacy-shared-archetype` = gold `#d4a574`). Every Wave-C widget (25.1-25.20) MUST apply the matching tint class on its outer container. NOT a status-bar entry (per DR-WC-M4-5 confirmation of 15.10 status-bar discipline). Title-bar tooltip carries the full `PRIVACY_CLASS` string.
- **Verification:** every widget test asserts the outer container carries one of the three tint classes; CSS contract test asserts each tint has a defined rule; privacy-class assertion test enumerates Wave-C widgets and confirms each declares its tint at mount time.

### 25.19 — Session-close wisdom-delta + quintessence-XOR ceremony pane (MN-19)

- **Classification:** spec-ahead-integration; depends on 19.6, 19.7
- **Source:** MN-19; T19.7 m4_mobius_return XOR wisdom_delta; T19.6 contemplation RPC; UX §13.5 "The One Loop"; M4-ARCHITECTURE §"Möbius return".
- **Deliverable:** new `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/session-close-ceremony.tsx` ReactWidget invoked on session-close trigger (consumed via `SharedBridgeAdapter.subscribeObservability({kind: 'm5.session.contemplation.complete'})`). Renders: (a) wisdom_delta byte trail (formatted as hex); (b) XOR animation folding delta into `quintessence_hash`; (c) the four contemplation seeds (Arch 3/5/7/9 prompts from T19.3 `CONTEMPLATION_PROMPT_LUT[12]`); (d) per-virtue witness vector (9-bit from T19.6). Reads via `invokeGatewayRpc('nara.session.close_summary', {sessionKey})`. Never mutates; ceremony surface only.
- **Verification:** widget mounts on synthetic close event; wisdom_delta-render test asserts byte trail visible; XOR-animation test fires on tick boundary; virtue-witness 9-bit vector test asserts all nine bits surfaced; privacy test asserts only handle-form of `quintessence_hash` enters the rendered surface (not raw 32-byte BLAKE3).

### 25.20 — Tarot psyche-anchor coherence panel (MN-20)

- **Classification:** spec-ahead-integration; depends on T19.4 + T19.5
- **Source:** MN-20; T19.4 m4_session_open draws tarot psyche-anchor; T19.5 m3_major_arcana_from_codon; T19.6 psyche-anchor coherence question.
- **Deliverable:** new `Body/M/epi-theia/extensions/m4-nara/src/browser/widgets/psyche-anchor-coherence.tsx` ReactWidget rendering the session's tarot psyche-anchor cards (from `M4_Session_Frame.tarot_psyche_anchor`) alongside the codons that appeared in the session's M3 trace; coherence row computed via T19.5 `m3_major_arcana_from_codon` reverse-lookup. Shows coherence score (count of codon→arcana matches / total codons traversed). Read-only. Consumes via `invokeGatewayRpc('nara.session.psyche_anchor', {sessionKey})`.
- **Verification:** widget test renders cards + codon trace against fixture session; coherence-score test asserts deterministic count from fixture; reverse-lookup test consumes T19.5 utility output; privacy test asserts session-frame body never crosses the bus (handle-only).

### 25.21 — Integrated `4-5-0` composition mini-mode contributions for every Wave-C widget (MN-22)

- **Classification:** spec-ahead-integration; consolidates Wave-C composition surface
- **Source:** MN-22; 15.4 composition-over-juxtaposition; 08-integrated-4-5-0; existing `TRACK_08_CONTRIBUTION` pattern in `m4-nara/src/common/index.ts:20`.
- **Deliverable:** extend `TRACK_08_EXPORTS` (currently `["M4ArtifactHandleChip","M4RecognitionMiniView"]`) with each Wave-C widget's mini-mode export name: `M4DayCalendarChip` (25.1), `M4SessionBreakdownCard` (25.2), `M4JournalTimelineCard` (25.3), `M4PasuWizardBadge` (25.4), `M4QuintessenceChip` (25.5), `M4PersonalCymaticField` (25.6 — center composition slot per 15.4), `M4PersonalCoordinateBadge` (25.7), `M4OracleCastBadge` (25.8), `M4OracleHistoryCard` (25.9), `M4MedicineCard` (25.10), `M4TransformBadge` (25.11), `M4LensCard` (25.12), `M4LogosChip` (25.13), `M4PratibimbaCoordinateBadge` (25.14), `M4KairosWheel` (25.15), `M4MercuriusRelayChip` (25.16), `M4TimeAxisSwitcherChip` (25.17), `M4SessionCloseCeremonyCard` (25.19), `M4PsycheAnchorCoherenceCard` (25.20). Each export declares `miniModes: ['badge','compact-card','inspector']` per existing pattern; `currentStateSelectors` and `selectionHandlers` extended to cover the new view ids. Cross-link `plugin-integrated-4-5-0` plugin consumer (Tranche 28.x if Wave-C lands a 4-5-0 composition tranche; otherwise 08-integrated-4-5-0 consumes).
- **Verification:** `pnpm -C Body/M/epi-theia/extensions/m4-nara build` clean; contract-preflight validator (`scripts/validate-extension-contract-preflight.mjs`) accepts the extended exports; composition-contract validator (`08-t0`) asserts each export has matching mini-mode contribution; integration test mounts each Wave-C widget under the `4-5-0` plugin editor area without breaking the composition contract per 15.4.

## Status summary

- **ALIGNED:** 0 (every claim has a substrate-or-spec-ahead gap; nothing is purely aligned in the widget-rendering band)
- **SPEC-AHEAD:** 19 (MN-01..02, 04..22; every widget has landed substrate / spec ground that the surface does not yet consume)
- **ORPHAN (closed by Wave-C):** 3 (MN-03 Journal Entries activity-bar, MN-06 personal-field view body, MN-07 Personal Coordinate activity-bar)
- **CODE-PENDING upstream (Wave-C consumes, does not re-author):** 7 (Tranches 5.1, 5.5, 5.9, 5.11, 5.16, 5.17, 19.12 + indirectly 5.4, 19.6, 19.7)
- **CONTRADICTION (decision-register):** 5 (DR-WC-M4-1..5)

## Closing note

M4 is the deepest delta surface in cycle 3. The substrate is exceptional — `personal_identity.rs`, `medicine.rs`, `oracle.rs`, `transform.rs`, `lens.rs`, `logos.rs`, `pratibimba.rs`, `pasu.rs`, `kairos.rs` are all CLI-real with rich LUTs; `vak_address.rs` exposes the Cl(4,2) carrier types; the m4-nara extension contract `2026-06-01.07-T7` declares the privacy law and three view ids. What is missing is the per-widget surface that lets the user see and shape what the substrate has been computing. Wave-C does not propose any new module under `Body/S/S0/`; every tranche above wraps a CLI-real or substrate-landed primitive into a Theia React widget under `Body/M/epi-theia/extensions/m4-nara/src/browser/`. The five contradictions are clean decisions; the three orphans close inside m4-nara; the seven upstream code-pending items are owned by Tranches 5.x and 19.x, not Wave-C. The matrix is the delta over 11.10/11.11/11.12 + 19.4/19.6/19.11/19.12 — the rest of the M4' product surface.
