# Wave-B Agentic Layer (S4↔S5) — Reconciliation Matrix

**Task ID:** `wave-b-agentic-layer`
**Domain:** Cross-cutting — ta-onta carriers (Khora / Hen / Pleroma / Chronos / Anima / Aletheia), the seven constitutional agents (Anima, Eros, Logos, Mythos, Nous, Psyche, Sophia + Pi as fourth constitutional per UX), the six Aletheia subagents (Anansi, Moirai, Janus, Mercurius, Agora, Zeithoven), the M5-4 Agentic Control Room (ACR), the S4↔S5 shared-intelligence seam, the `epi-gnostic` RAG, `epi-kbase` semantic search, and canon-as-context.
**Anti-greenfield posture:** ta-onta carriers, the Pleroma capability-matrix, `epi-gnostic`, `epi-kbase`, and the `agentic-control-room` Theia extension are landed substrate. Tranches phrase "consume as-is", "audit/verify", "extend", or "name owner for a method-routing closure."

---

## Sources Consulted

1. **UX (Corpus 1):** `Idea/Pratibimba/System/Subsystems/epii/epii-ux-full-m5-branch.md` §2.5, §5, §7, §10, §11 (the agentic-control-room and the S4↔S5 seam claims)
2. **Specs (Corpus 2):** `Body/S/S4/ta-onta/S4-5p-aletheia/CONTRACT.md` (canonical Aletheia + subagent contract); `Body/S/S4/plugins/pleroma/capability-matrix.json` (constitutional-agent gates, dispatch tools, m5_4_governance, mediated_run_evidence_bridge); `Body/S/S5/epi-kbase/CONTRACT.md`
3. **Code (Corpus 3):** `Body/S/S4/ta-onta/{khora,hen,pleroma,chronos,anima,aletheia}/{CONTRACT.md, extension.ts, spine-contribution.ts, modules/, S{n}/, tests/}`; `Body/S/S4/pi-agent/{agents,prompts,extensions,lib,tests}`; `Body/S/S5/epi-gnostic/epi_gnostic/{cli.py,graphiti_service.py,wrapper.py,enrichment,storage,config.py}` + `tests/test_enrichment.py`; `Body/S/S5/epi-kbase/src/`; `Body/S/S4/ta-onta/aletheia/S5'/agents/{anansi,moirai,janus,mercurius,agora,zeithoven,aletheia}.md` + `janus-envelope.schema.json`; `Body/S/S3/gateway/src/` (gnostic-method registration audit)
4. **Surface (Corpus 4):** `Body/M/epi-theia/extensions/agentic-control-room/{src/common/{index.ts,run-model.ts,parity.ts},src/browser/{acr-runtime-service.ts,frontend-module.ts,run-flow-widget.tsx},package.json,tests}`
5. **Reference:** Wave-A M5 matrix (`plan.runs/wave-a-m5-reconciliation-matrix.md`); cycle-2 plan 10 (`2026-06-02-m-prime-cycle-2-canonical/10-ta-onta-carriers-constitutional-agents-and-aletheia.md`)

---

## 4-Way Reconciliation Matrix

| # | Claim (UX doc) | Spec authority (M' Seed / ta-onta CONTRACT) | Code / substrate evidence | Theia surface | Status |
|---|---|---|---|---|---|
| B1 | "M5-4 is the Agentic Control Room — Sophia/Anima/Pi/Aletheia, the S4↔S5 shared seam" (epii UX §2.5) | `capability-matrix.json` `m5_4_governance.review_surface_roles` registers exactly `{sophia, anima, pi, aletheia, epii_review}` | `Body/M/epi-theia/extensions/agentic-control-room/src/common/run-model.ts` declares `AgenticActor = anima\|eros\|logos\|mythos\|nous\|psyche\|sophia\|aletheia\|pi`; `acr-runtime-service.ts` dispatches via `s4'.mediation.route` through `KERNEL_BRIDGE_API.invokeCapability` | `ACR_WIDGET_IDS = {RUN_TREE, TOOL_STREAM, DIAGNOSTICS, EVIDENCE_DEPOSITION, REVIEW_DECISION}` in `agentic-control-room/src/common/index.ts` — five widgets present | **ALIGNED** |
| B2 | "Pi is one of the four ACR governance roles" (UX §2.5) | `capability-matrix.json` `m5_4_governance.review_surface_roles.pi` is fully specified with permitted/forbidden actions, AND `constitutional_agents` list is `{anima,eros,logos,mythos,nous,psyche,sophia}` (NOT including pi) | `Body/S/S4/pi-agent/` is a SEPARATE module from `Body/S/S4/ta-onta/`; `agents/anima.md` exists; no `pi-agent/agents/pi.md` | ACR's `AgenticActor` type includes both 'pi' and the seven constitutionals | **CONTRADICTION** — UX wording "Sophia/Anima/Pi/Aletheia" + matrix m5_4 governance treats Pi as a fourth review role, but `constitutional_agents[]` excludes pi. Wave-A M5 already raised this as DR-M5-1. Carry as DR-M5-AGENTIC-1. |
| B3 | "Aletheia owns the Gnosis RAG pipeline (S4-5p)" (UX §2.5, §5.1) | `Body/S/S4/ta-onta/S4-5p-aletheia/CONTRACT.md` registers `aletheia_gnosis_{ingest,query,notebook_create}` + `aletheia_thought_route` + `aletheia_crystallise` + `aletheia_seed_refresh` | `Body/S/S5/epi-gnostic/epi_gnostic/{cli.py,graphiti_service.py,wrapper.py}` is the real RAG pipeline (Graphiti + RAG-Anything/LightRAG); `epi-gnostic/tests/test_enrichment.py` exercises `_create_gnostic_node`/`_delete_gnostic_node` (Gnosis nodes in Neo4j separate namespace). `Body/S/S4/ta-onta/aletheia/extension.ts` + `modules/sophia-ingest.ts`, `moirai-rehear.ts`, `janus-doorway.ts`, etc. are landed | NOT consumed by ACR yet — `agentic-control-room` calls `s4'.mediation.route` and `s5'.review.submit` but no `s5'.gnostic.*` calls visible | **SPEC-AHEAD / CODE-PENDING** — Aletheia ownership is established by spec + Body code; the gateway-side `s5'.gnostic.{ingest,query,notebook,status}` registration is the un-landed seam (Wave-A M5 6.1). |
| B4 | "Six ta-onta carriers: Khora/Hen/Pleroma/Chronos/Anima/Aletheia" (UX §5.1) | Each has a CONTRACT.md at `Body/S/S4/ta-onta/{khora,hen,pleroma,chronos,anima,aletheia}/CONTRACT.md` — first-class spec | All six dirs have `extension.ts` + `spine-contribution.ts` + `S{n}/` or `S{n}'/` payload folder + `modules/` + `tests/`. No carrier is ambient. | ACR widget set targets only `m5_4` review; carriers are not surfaced individually (consumed via the kernel bridge). Acceptable: carriers are infrastructure, not panels. | **ALIGNED** |
| B5 | "Six Aletheia subagents: Anansi, Moirai, Janus, Mercurius, Agora, Zeithoven" (UX §2.5 implicit; CONTRACT explicit) | `aletheia/CONTRACT.md` lines 200-209 names exactly these six (+ "Anansi hosts Darshana") | `Body/S/S4/ta-onta/aletheia/S5'/agents/{anansi,moirai,janus,mercurius,agora,zeithoven,aletheia,README}.md` — all seven .md files present (aletheia.md is the parent; six subagents present) | Not surfaced in ACR as discrete actors — invoked via `s4'.mediation.route` actor parameter. ACR `AgenticActor` type lists `anima/eros/logos/mythos/nous/psyche/sophia/aletheia/pi` but NOT `anansi/moirai/janus/mercurius/agora/zeithoven` | **DOC-AHEAD / SPEC-AHEAD** — Aletheia subagents are spec-and-code-grounded but the ACR run-model `AgenticActor` does not surface them. Either extend the type or route them as Aletheia-internal (probably the latter, per CONTRACT "emergent, invoked by Sophia and Psyche"). Decision needed. |
| B6 | "Seven constitutional agents: Sophia, Anima, Pi, Aletheia, …" (UX wording) vs the spec list `{anima,eros,logos,mythos,nous,psyche,sophia}` (seven; Pi outside) | `capability-matrix.json` `constitutional_agents`: 7 items, no pi, no aletheia | `Body/S/S4/pi-agent/agents/anima.md` only — pi-agent dir treats Pi as the *agent runtime*, not as a constitutional. `pi-agent/agents/teams.yaml` + `agent-chain.yaml` define dispatch chains | UX/spec wording mismatch leaks into the ACR run-model where `AgenticActor` collapses both lists | **CONTRADICTION** — "the four ACR roles" (Sophia/Anima/Pi/Aletheia) vs "the seven constitutional agents" (anima/eros/logos/mythos/nous/psyche/sophia) — these are two different lists conflated by colloquial UX wording. Resolve via decision register: ACR governance roles ≠ constitutional roster ≠ ta-onta carriers ≠ Aletheia subagents. Four disjoint ontologies. |
| B7 | "Canon as context — the 72-fold harmonic is the tuning fork, gnostic namespace is the score, S4 and S5 both tune to it without re-passing" (UX §5.2) | Aletheia CONTRACT §"Gnosis RAG Pipeline" requires 3072-dim embedding space unified across Bimba + Gnosis (`GEMINI_EMBED_DIMS=3072`); cross-link via `RELATES_TO_COORDINATE`; Neo4j Gnosis namespace separate | `epi-gnostic/epi_gnostic/graphiti_service.py` + `enrichment/` + `tests/test_enrichment.py` cross-namespace edge tests landed; 3072-dim embedding choice is a config decision, not yet end-to-end verified for the 72-fold harmonic | No ACR widget tunes to `MathemeHarmonicProfile.resonance72` — Wave-A M5 6.3 already flagged `resonance72`, `planetaryChakral`, `mahamaya`, `pointerAnchor`, `depositionAnchor` as CODE-PENDING on the profile | **CODE-PENDING** — the "shared tuning fork" claim depends on `MathemeHarmonicProfile.resonance72` landing (cross-link to Wave-B kernel-bridge tranche 10.x). Until then the seam is rhetorical. |
| B8 | "Aletheia is emergent, invoked by Psyche and Sophia; all routing through Anima dispatch" (Aletheia CONTRACT inv. 1-2) | CONTRACT key invariants 1 & 2 | `aletheia/extension.ts` + `aletheia/modules/anansi-lineage.ts`, `moirai-rehear.ts`, etc. — but the dispatch wiring "Sophia → Anima → Moirai → Klotho/Lachesis/Atropos" requires concrete handler in `anima/modules/`; ACR calls `dispatch_moirai_night_pass` (declared in capability-matrix.json dispatch_tools) but this method's gateway endpoint must exist | ACR `AgenticRoute` includes `dispatch_moirai_night_pass`; calls through `s4'.mediation.route` | **SPEC-AHEAD** — route exists in UI + matrix, gateway handler audit needed: does `s4'.mediation.route` know about `dispatch_moirai_night_pass` and chain into the Möbius pass? Verification command: `grep -r "dispatch_moirai_night_pass" Body/S/S3/gateway/src/ Body/S/S4/ta-onta/anima/modules/`. |
| B9 | "Pi handles axiom-translation (philosophical-English ↔ formal-notation ↔ OWL ↔ SHACL); routing between M5-1 and M5-2" (UX §2.5) | `capability-matrix.json` `m5_4_governance.review_surface_roles.pi.permitted_actions` lists `prepare_agent_run_evidence`, `dispatch_bounded_agent_run`, `run_runtime_smoke`, `prepare_dry_run_promotion_plan` — no axiom-translation tool registered | `Body/S/S4/pi-agent/` exists (composite-entry.ts, lib/, prompts/, extensions/, agents/{anima.md, agent-chain.yaml}) but no `axiom-translate.ts` or OWL/SHACL adapter; `pi-agent/agents/anima.md` only carrier .md | Not surfaced in ACR | **DOC-AHEAD** — UX gives Pi axiom-translation role; spec gates Pi only to dispatch/evidence/runtime-smoke. Either the UX overstates Pi (downgrade) or extend `Body/S/S4/pi-agent/` with axiom-translation tools. Decision needed for the M5-1↔M5-2 routing seam. |
| B10 | "epi-kbase: bounded resource context, NotebookLM-fork, project-scoped bkmr search" (Aletheia CONTRACT §3072-dim; epi-kbase CONTRACT.md) | `Body/S/S5/epi-kbase/CONTRACT.md` exports `bindKbaseProject, kbaseSearch, skillSearch, hasHighRelevanceMatch`; depends on `bkmr` CLI on PATH | `Body/S/S5/epi-kbase/src/` + `package.json` landed. Aletheia consumes via `aletheia/modules/` — verify `kbase_search` is called from any aletheia module: `grep -r "kbaseSearch\|bindKbaseProject" Body/S/S4/ta-onta/` | Not surfaced in ACR; consumed only via aletheia tooling | **ALIGNED** (carrier-internal; not a UX-surface claim) |
| B11 | "Sophia advises and coordinates across capacities without becoming a panel of her own" (UX §1) | `capability-matrix.json` `agent_capability_gates.sophia.role_restrictions = [leaf, review_disclosure_and_epii_deposit]` (NOT orchestrator); `anima.role_restrictions = [orchestrator, …]` | Anima is the orchestrator (capability-matrix); Sophia is a leaf review agent. Consistent with UX. | ACR has no "Sophia panel" — review_decision widget is shared, evidence_deposition is shared. Sophia's actions surface as review-role, not as a UI pane | **ALIGNED** |
| B12 | "The pratibimba graph namespace is the 4/5/0 seam as data — governed handles only" (UX §5.3) | `capability-matrix.json` `mediated_run_evidence_bridge.privacy_guards` lines 791-797: "Graphiti protected handles only; no protected bodies", "Nara protected-local bodies never enter evidence packets" | `Body/S/S3/graphiti-runtime/` + `Body/S/S5/epi-gnostic/epi_gnostic/graphiti_service.py` — Graphiti runtime is landed. Whether `:HAS_DAY`/`:CONTAINS_DAILY_NOTE` relations for the pratibimba namespace are registered is Wave-A M4 5.3 (CODE-PENDING). | ACR `RunEvidenceEnvelope` carries `coordinate`, `artifactUri`, `sourceAnchor`, `graphAnchor`, etc.; `isPrivacySafe(privacyClass)` enforced in `acr-runtime-service.ts` lines 89, 159, 219 | **SPEC-AHEAD** — privacy guards declared in spec + enforced in ACR, but pratibimba-namespace relation insertion is CODE-PENDING (cross-link to Wave-A M4 5.3). |
| B13 | "Janus envelope: handoff schema between Chronos and Aletheia for Möbius pass" (Aletheia CONTRACT) | `aletheia/S5'/janus-envelope.schema.json` declared required: `day_id, session_ids[], thought_count_by_bucket, archive_path, trigger_type` | Schema file present at `Body/S/S4/ta-onta/aletheia/S5'/janus-envelope.schema.json`. Test coverage check: `grep -r "janus-envelope" Body/S/S4/ta-onta/{chronos,aletheia}/tests/`. | Not a UI claim | **ALIGNED** (carrier-internal contract) |
| B14 | "Sixfold Day/Night' lifecycle — Chronos triggers, Aletheia runs Möbius pass" (Aletheia CONTRACT §Möbius) | CONTRACT specifies `cron_evening` PI hook + Moirai Klotho/Lachesis/Atropos modes | `aletheia/modules/chronos-integration.ts`, `moirai-rehear.ts`, `gate-trigger.ts` — landed. `dispatch_moirai_night_pass` declared in capability-matrix.json | ACR exposes `AgenticRoute = dispatch_moirai_night_pass` | **ALIGNED** (with B8's gateway-handler audit caveat) |
| B15 | "Six operational-capacity governance lanes: anuttara/paramasiva/parashakti/mahamaya/nara/epii_on_epii" (capability-matrix `capacity_governance`) | `m5_4_governance.capacity_governance` — explicit six-lane structure with leads (sophia × 5, anima × 1 for nara) | No corresponding M5 surface views in `agentic-control-room/src/browser/` — Wave-A M5 6.3 flags this as SPEC-AHEAD ("Six operational-capacity views over capacity_workflows.rs") | ACR currently only renders a generic run-flow widget, not six-capacity panes | **SPEC-AHEAD** — six lanes are spec-grounded; ACR needs six capacity views (Wave-A M5 6.3). |
| B16 | "MediatedRunEvidencePacket carries 16 required fields including `bridgeReadinessHandle`, `profileGeneration`, `dayNowContext`, …" (capability-matrix `mediated_run_evidence_bridge.packet_required_fields`) | Spec lists 16 fields, capability_allowlists per actor (sophia/aletheia/pi/anima/human) | `RunEvidenceEnvelope` in `run-model.ts` carries `candidateId, coordinate, artifactUri, sourceAnchor, specAnchor, codeAnchor, testAnchor, graphAnchor, …` — incomplete match to spec packet fields | ACR `setEvidence()` accepts envelope; widgets render it | **DOC-AHEAD / SPEC-AHEAD** — UI envelope is a subset of the spec packet. Field-by-field parity audit needed. |
| B17 | "Recursive self-review (`recursive_self_review_requires_user_final_validation: true`) for sophia/anima/pi/aletheia" (capability-matrix) | Explicit in spec for all four ACR roles | Need to verify ACR `submitReviewDecision()` enforces "recursive self review → human-required gate." The current `enforceHumanGate({decision, humanRequired, actorIsHuman})` checks the candidate's `humanRequired` flag, not a recursive-self-review classifier | ACR enforces `humanRequired` candidate flag (lines 249-260 of acr-runtime-service.ts) | **CODE-PENDING** — recursive-self-review classifier (is this run reviewing prior runs by the same agent?) not visible in ACR `enforceHumanGate`. Closure needed. |
| B18 | "Aletheia subagents are PI-native specialists — identity is their tool domain, NOT phase assignment" (CONTRACT inv. 3) | CONTRACT inv 3-4 | All six subagent .md files at `aletheia/S5'/agents/` are real files | Not surfaced in ACR; consumed via aletheia-internal dispatch | **ALIGNED** |
| B19 | "Aletheia is tool-guardian, not peer agent" (USER MEMORY + CONTRACT) | CONTRACT places Aletheia as S4-5' carrier (membrane), not a peer-agent role; spec lives at `Body/S/S4/ta-onta/S4-5p-aletheia/CONTRACT.md` | Confirmed: Aletheia has no separate `aletheia-agent/agent-contract.json` — the carrier IS the contract | ACR treats `aletheia` as an `AgenticActor` for routing dispatch, consistent with tool-guardian role | **ALIGNED** (memory honored; no missing-contract finding) |
| B20 | "Capability parity assertion — UI capability set must match gateway-enforced set (IOD-17)" (parity.ts comment) | IOD-17 register requirement | `Body/M/epi-theia/extensions/agentic-control-room/src/common/parity.ts` exports `assertCapabilityParity()` — function present | Tests folder at `agentic-control-room/tests/` — verify parity test exists and runs against `capability-matrix.json` + a gateway list | **SPEC-AHEAD** — parity asserter exists; gateway-side `s4'.mediation.capabilities.list` endpoint existence + the live parity test must be audited. |
| B21 | "TillDone execution backbone gates non-TillDone tool use" (capability-matrix `execution_backbone`) | Source: `vendors/pi-vs-claude-code/extensions/tilldone.ts`, body_path: `Body/S/S4/ta-onta/S4-2p-pleroma/S2/tilldone.ts`, gated by `EPI_TILLDONE_MODE` env | Verify `Body/S/S4/ta-onta/pleroma/S2/tilldone.ts` exists | Not a UI claim | **CODE-PENDING audit** — body_path existence check needed; if absent the spec is the vendor source pointer only. Verification command: `test -f "Body/S/S4/ta-onta/pleroma/S2/tilldone.ts"`. |
| B22 | "Khora owns command/tool membrane + session-ground" (cycle-2 plan 10 T1) | `Body/S/S4/ta-onta/khora/CONTRACT.md`; canonical source S4-0'-SPEC.md | `khora/extension.ts` + `khora/S0/` + `khora/S0'/` + `modules/` + `tests/` landed | Not a UI claim; consumed via session-init | **ALIGNED** |
| B23 | "Hen owns S4-side carrier role into S1' diagram/MOC law" (cycle-2 plan 10 T2) | `Body/S/S4/ta-onta/hen/CONTRACT.md` | `hen/extension.ts` + `hen/S1/` + `hen/S1'/` + `hen/M/` + `modules/` + `tests/` landed | Not a UI claim | **ALIGNED** |
| B24 | "Pleroma owns capability substrate / skill-plugin membrane / bounded primitive registry" (cycle-2 plan 10 T3) | `Body/S/S4/ta-onta/pleroma/CONTRACT.md` + the canonical capability-matrix.json | `pleroma/extension.ts` + `pleroma/S2/` + `pleroma/S2'/` + `staged/`; the capability-matrix is the parity source | ACR `parity.ts` reads matrix directly (per comment) | **ALIGNED** |
| B25 | "Chronos owns Day/NOW/Kairos/session runtime" (cycle-2 plan 10 T4) | `Body/S/S4/ta-onta/chronos/CONTRACT.md` | `chronos/extension.ts` + `chronos/S3'/` + `modules/` + `tests/` landed | Not a panel | **ALIGNED** |
| B26 | "Anima owns dispatch/orchestration/Psyche carrier" (cycle-2 plan 10 T5) | `Body/S/S4/ta-onta/anima/CONTRACT.md`; matrix `agent_capability_gates.anima` = orchestrator | `anima/extension.ts` + `anima/S4/` + `anima/S4'/` + `modules/` + `tests/` landed | ACR's gateway dispatch is `s4'.mediation.route` — Anima's seam | **ALIGNED** |
| B27 | "Nous/Logos/Eros/Mythos/Psyche — leaf constitutional agents with explicit role_restrictions" (cycle-2 plan 10 T7-T11; capability-matrix `agent_capability_gates`) | Spec gates each with role restrictions + tools + skills | Each must have a real `.md` agent profile somewhere. `pi-agent/agents/` only has `anima.md`. The other constitutional .md files are NOT visible in pi-agent/agents/. Where do they live? | Listed in ACR `AgenticActor` type | **ORPHAN/CODE-PENDING** — constitutional agent `.md` profiles for nous/logos/eros/mythos/psyche not found in `pi-agent/agents/`. Either they live elsewhere (audit needed: `find Body/S/S4 -name "nous.md" -o -name "logos.md" -o -name "eros.md" -o -name "mythos.md" -o -name "psyche.md" -o -name "sophia.md"`) or they are ambient (anti-greenfield-rule violation: would require a tranche). |

---

## Anomalies Section

### CONTRADICTIONs (decision-register routing)

- **DR-B-1 (consolidates with Wave-A M5 DR-M5-1):** Pi's status — is Pi a "fourth constitutional ACR role" (UX §2.5) or "the agent runtime layer separate from constitutionals" (`capability-matrix.json constitutional_agents = 7, excludes pi`)? Resolution must reconcile: ACR governance four-tuple (sophia/anima/pi/aletheia) ≠ constitutional_agents seven-tuple ≠ ta-onta-carriers six-tuple ≠ Aletheia-subagents six-tuple. These are **four disjoint ontologies** the UX colloquially overloads as "agents."
- **DR-B-2:** Pi's tool surface — UX §2.5 gives Pi axiom-translation (philosophical-English ↔ OWL ↔ SHACL); capability-matrix gates Pi only to dispatch/evidence/runtime-smoke. Either downgrade the UX wording or land axiom-translation tooling in `Body/S/S4/pi-agent/`.
- **DR-B-3:** ACR `AgenticActor` type union — should Aletheia subagents (anansi/moirai/janus/mercurius/agora/zeithoven) be in the `AgenticActor` enum, or strictly Aletheia-internal? CONTRACT inv 2 says "all routing through Anima dispatch; Aletheia subagents emergent." Recommended: keep them Aletheia-internal, NOT surfaced in ACR `AgenticActor`.

### CODE-PENDING (substrate gaps — anti-greenfield: name, do not rebuild)

- **CP-B-1:** `s5'.gnostic.{ingest, query, notebook, status}` gateway endpoints — substrate (epi-gnostic) is landed; the gateway-side registration is the missing seam. (Identical to Wave-A M5 6.1.) Verification: `grep -r \"s5'.gnostic\\." Body/S/S3/gateway/src/`.
- **CP-B-2:** `MathemeHarmonicProfile.resonance72` (+ `planetaryChakral`, `mahamaya`, `pointerAnchor`, `depositionAnchor`) — the canon-as-context "tuning fork" claim cannot be true until the profile carries these fields. Cross-link to kernel-bridge tranche 10.x and Wave-A M2 3.1 (klein_flip_state).
- **CP-B-3:** Recursive-self-review classifier in `acr-runtime-service.ts:enforceHumanGate` — spec requires `recursive_self_review_requires_user_final_validation` for sophia/anima/pi/aletheia, but UI gate only checks `humanRequired` flag.
- **CP-B-4:** Six operational-capacity views over `capacity_workflows.rs` — Wave-A M5 6.3 already named; ACR needs six capacity-lane panes.
- **CP-B-5:** Field-parity audit between `RunEvidenceEnvelope` (UI) and `MediatedRunEvidencePacket.packet_required_fields` (spec, 16 fields).
- **CP-B-6:** Gateway dispatch handler for `dispatch_moirai_night_pass` — declared in capability-matrix `dispatch_tools` + reachable from ACR `AgenticRoute`, but the gateway router for `s4'.mediation.route` must know about it. Audit needed.

### ORPHANs

- **O-B-1:** Constitutional agent `.md` profiles for `nous, logos, eros, mythos, psyche, sophia` — none visible at `Body/S/S4/pi-agent/agents/` (only `anima.md` present). Either they live elsewhere (audit) or they are ambient and need a no-orphan tranche to land profile files. This is the explicit inheritance from cycle-2 plan 10 T7-T12 — verify whether cycle 2 actually landed these.

### DOC-AHEAD (landing tranches or UX downgrades)

- **DA-B-1:** Aletheia subagents in ACR — UX implies they're visible-as-actors; CONTRACT says emergent. Decision-register entry, then either extend ACR surface OR downgrade the implication.
- **DA-B-2:** Pi axiom-translation tooling (DR-B-2's landing form if "land" is chosen).

---

## Proposed Cycle-3 Tranches (closing the Agentic Layer)

All tranches obey anti-greenfield: ta-onta carriers, epi-gnostic, epi-kbase, pleroma capability-matrix, and the ACR extension are CONSUMED. Tranches `audit`, `extend`, or `name owners for method-routing closures`.

| ID | Title | Classification | Deliverable | Verification |
|---|---|---|---|---|
| **12.1** | ACR-actor parity audit — reconcile `AgenticActor` union with four disjoint ontologies | doc-ahead-landing | Audit report in `plan.runs/12.1-acr-actor-parity.md` enumerating ACR-governance-4 vs constitutional-7 vs ta-onta-6 vs aletheia-subagents-6; decision-register entry DR-B-3 resolution applied to `run-model.ts` | `grep -n "AgenticActor" Body/M/epi-theia/extensions/agentic-control-room/src/common/run-model.ts` + `node Body/M/epi-theia/extensions/agentic-control-room/tests/*.test.js` (or `pnpm --filter @pratibimba/agentic-control-room test`) |
| **12.2** | `s5'.gnostic.*` gateway-endpoint registration over production `epi-gnostic` | code-pending-closure | Register `s5'.gnostic.{ingest,query,notebook,status}` in `Body/S/S3/gateway/src/` routing to `epi-gnostic/epi_gnostic/{cli.py, graphiti_service.py, wrapper.py}` | `grep -rn "s5'.gnostic\\." Body/S/S3/gateway/src/` returns ≥4 method registrations; `cargo check -p epi-s3-gateway` clean; `pytest Body/S/S5/epi-gnostic/tests/test_enrichment.py -q` passes |
| **12.3** | Constitutional-agent profile orphan-fill — locate or land `nous/logos/eros/mythos/psyche/sophia` `.md` profiles | no-orphan-fill | If extant: audit doc enumerating their actual paths; if absent: land profiles at `Body/S/S4/pi-agent/agents/{nous,logos,eros,mythos,psyche,sophia}.md` per cycle-2 plan 10 T7-T12 contracts | `find Body/S/S4 \( -name nous.md -o -name logos.md -o -name eros.md -o -name mythos.md -o -name psyche.md -o -name sophia.md \) -type f` returns ≥6 entries (or audit doc explains the residency) |
| **12.4** | Recursive-self-review gate extension in ACR `enforceHumanGate` | code-pending-closure | Extend `Body/M/epi-theia/extensions/agentic-control-room/src/common/run-model.ts` `enforceHumanGate` to accept a `recursiveSelfReview: boolean` parameter and gate per `capability-matrix.json` `m5_4_governance.review_surface_roles.{sophia,anima,pi,aletheia}.recursive_self_review_requires_user_final_validation` | Contract test in `agentic-control-room/tests/` asserts: `enforceHumanGate({decision:'approve', humanRequired:false, actorIsHuman:false, recursiveSelfReview:true, actor:'sophia'}) === {ok:false, …}` |
| **12.5** | Six operational-capacity panes over `capacity_workflows.rs` (binds Wave-A M5 6.3) | spec-ahead-integration | Six capacity-lane widgets in `agentic-control-room/src/browser/capacity-{anuttara,paramasiva,parashakti,mahamaya,nara,epii-on-epii}-pane.tsx` reading `m5_4_governance.capacity_governance` lanes | Widget IDs registered in `ACR_WIDGET_IDS`; `cargo check -p capacity-workflows` (or grep for the corresponding capacity_workflows.rs in Body/S/S4) clean; UI smoke test renders six lanes |
| **12.6** | MediatedRunEvidencePacket field-parity closure between UI envelope and spec | spec-ahead-integration | Extend `RunEvidenceEnvelope` in `agentic-control-room/src/common/run-model.ts` to include all 16 spec fields (`profileGeneration, bridgeReadinessHandle, sessionKey, dayNowContext, currentProfile, graphContext, sessionRuntime, semanticCandidates, s5Refs, reviewId`) | Parity test compares `RunEvidenceEnvelope` keys vs `capability-matrix.json.mediated_run_evidence_bridge.packet_required_fields` — set-equality assertion |
| **12.7** | Pi tool-surface decision: axiom-translation lands OR UX downgrades (DR-B-2) | contradiction-decision | Decision-register entry in `plan.runs/13-decisions.md` DR-B-2; if "land": tooling at `Body/S/S4/pi-agent/lib/axiom-translate.ts` consuming `Body/S/S5/epi-gnostic` OWL/SHACL; if "downgrade": diff against `Idea/Pratibimba/System/Subsystems/epii/epii-ux-full-m5-branch.md` §2.5 | DR entry exists + applied; if land: `test -f Body/S/S4/pi-agent/lib/axiom-translate.ts`; if downgrade: doc diff applied |
| **12.8** | Pi-as-fourth-ACR-role decision (DR-B-1; consolidates Wave-A DR-M5-1) | contradiction-decision | Resolution in decision register: either extend `constitutional_agents[]` in capability-matrix to include `pi` (and add `agent_capability_gates.pi`), OR explicitly document that ACR governance roles are a distinct ontology from constitutional roster | DR entry exists + `capability-matrix.json` updated (or contradicting wording downgraded in UX doc); `node Body/S/S4/plugins/pleroma/tests/parity*.test.js` passes |
| **12.9** | Gateway handler audit for `dispatch_moirai_night_pass` + Aletheia Möbius-pass routing | spec-ahead-integration | Audit doc verifying `s4'.mediation.route` knows about `dispatch_moirai_night_pass` and chains into `aletheia/modules/moirai-rehear.ts` per Aletheia CONTRACT §Möbius and Janus envelope schema | `grep -rn "dispatch_moirai_night_pass" Body/S/S3/gateway/src/ Body/S/S4/ta-onta/anima/modules/` returns hits in BOTH; `pnpm --filter @pratibimba/agentic-control-room test` includes night-pass route smoke |
| **12.10** | Capability-parity live-assertion wiring (`s4'.mediation.capabilities.list`) | spec-ahead-integration | Register gateway endpoint exposing the capability list; wire `assertCapabilityParity()` against it in ACR startup | `grep -rn "capabilities.list" Body/S/S3/gateway/src/`; ACR test asserts parity at runtime |
| **12.11** | TillDone substrate residency audit | code-pending-closure | Verify `Body/S/S4/ta-onta/pleroma/S2/tilldone.ts` exists and matches capability-matrix `execution_backbone.body_path`. If missing, copy from vendor source or downgrade `body_path` claim. | `test -f "Body/S/S4/ta-onta/pleroma/S2/tilldone.ts"` succeeds OR audit doc explains divergence |
| **12.12** | Aletheia-subagents-in-ACR decision (DR-B-3) | contradiction-decision | DR entry: keep subagents Aletheia-internal (recommended per CONTRACT inv 1-2), OR extend ACR `AgenticActor` to surface them. Apply to `run-model.ts` per chosen path. | DR entry + run-model.ts diff applied |
| **12.13** | S4↔S5 shared-intelligence seam runtime audit — canon-as-context (UX §5.2 made measurable) | code-pending-closure (depends on CP-B-2 via kernel-bridge) | Audit report verifying: (a) Bimba + Gnosis embedding space is 3072-dim end-to-end (`GEMINI_EMBED_DIMS=3072`); (b) `RELATES_TO_COORDINATE` cross-namespace edges land per `epi-gnostic/tests/test_enrichment.py::test_cross_namespace_edge_created`; (c) `MathemeHarmonicProfile.resonance72` deps (defer to kernel-bridge tranche 10.x) | `pytest Body/S/S5/epi-gnostic/tests/test_enrichment.py::test_cross_namespace_edge_created -q` passes; embedding-dim grep returns single value (`grep -rn "EMBED_DIMS\|embedding_dim" Body/S/S5/epi-gnostic/`) |

### Tranche-id-domain flag

Per kickoff instructions: agentic-layer = `12.x`. Tranche **12.13** straddles kernel-bridge profile (cross-link to `10.x` for `resonance72`) and integrated-bimba (cross-link to Wave-A M0 for cross-namespace edges) — flagged here as `12.13` (S4↔S5 seam owns the integration) with explicit dependencies on `10.x` and `1.x`.

---

## End of Matrix
