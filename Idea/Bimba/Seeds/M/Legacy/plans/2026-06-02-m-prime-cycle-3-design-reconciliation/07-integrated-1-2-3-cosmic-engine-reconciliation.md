# Track 07 — Integrated 1-2-3 Cosmic Engine Reconciliation

Reconciles the integrated `1-2-3` cosmic engine plugin across the four corpora. This plugin composes [[M1']] + [[M2']] + [[M3']] through the kernel-bridge profile-tick — the cosmic stratum of the `137 = 64 + 72 + 1` matheme (M1 +1 → M2 72 → M3 64). The plugin package is landed; cycle-3 work is composition contract + readiness gating against Wave-A pending profile-field markers. Anti-greenfield throughout.

## Total-Shape Architecture (Phase A)

Canonical composition-architecture document: [`Idea/Bimba/Seeds/M/INTEGRATED-1-2-3-COSMIC-ENGINE-ARCHITECTURE.md`](Idea/Bimba/Seeds/M/INTEGRATED-1-2-3-COSMIC-ENGINE-ARCHITECTURE.md) (899 lines). Profile-bus projection `CosmicCompositionState` per Tranche 10.IG. DR-IG-2 unifies the three-variant Klein-flip event, DR-IG-4 pins `torus_knot_phase` to `MathemeHarmonicProfile.m1_topology`, and DR-IG-5 pins the cymatic surface to the torus inside composition. Composition-over-juxtaposition PASSES; M2 cymatic + M3 codon-rotation render ON K² surface (Tranche 15.4 composition pattern). New `K2SurfaceHandle` ownership at Tranche 02.6 → composition mount-point at 07.X.

## Source Specs and Matrix

- Canonical: `Idea/Bimba/Seeds/M/M'-SYSTEM-SPEC.md` (shell-0 cosmic 1-2-3 invariant), `Idea/Bimba/Seeds/M/alpha_quaternionic_integration_across_M_stack.md` (137 architecture), `Idea/Bimba/Seeds/M/M3'/full_theoretical_alignments_ql_physics.md` (register-disciplined 137 / coupling-flow alignment)
- Companions: M1'/M2'/M3' specs (the three poles being composed), `Idea/Bimba/Seeds/M/Legacy/plans/2026-06-02-m-prime-cycle-2-canonical/08-integrated-1-2-3-cosmic-engine-plugin.md` (cycle-2 owner)
- Cross-references: Wave-B kernel-bridge matrix; Wave-A M1 (Tranches 02.2, 02.3, 02.4), M2 (Tranches 03.2, 03.3, 03.4), M3 (Tranche 04.2)

## Cycle 2 Substrate Inheritance

Consume as-is — `Body/M/epi-theia/extensions/plugin-integrated-1-2-3` package (deps wire to the three M-extensions); `@pratibimba/integrated-composition` for shared `LayoutClaim` arbitration; `08-t0-composition-contract-preflight` inherits `07-t0`'s bridge/forbidden-import discipline. Cycle 2 Track 08 named the plugin; cycle 3 closes its readiness contract and composition seam.

## Tranches

1. **7.1 — Integrated readiness gate against Wave-A pending profile fields** *(spec-ahead-integration; cross-link to Wave-B Tranche 11.8)*

   `plugin-integrated-1-2-3` declares typed `IntegratedReadiness` blocked-states consuming Wave-A pending markers (`klein_flip_state` from M1 02.2 / kernel-bridge 10.2; `resonance72` from kernel-bridge; six-axes-of-72 decoding from M2 03.3; `audio_octet[8]` / `nodal_quartet[4]` aligned but conditional on M1' performance event 02.4) via the shared `integrated-composition` contract. Extend, no rebuild.

   Verification: `pnpm --filter @pratibimba/integrated-composition test`; `pnpm --filter @pratibimba/plugin-integrated-1-2-3 test`; fixture parity with kernel-bridge readiness ledger (`plan.runs/wave-b-kernel-bridge-matrix.md`).

2. **7.2 — 137 = 64 + 72 + 1 composition assertion (Third Spanda Equation, five canonical forms)** *(spec-ahead-integration)*

   Add composition-contract assertion that the plugin's stratification preserves the matheme spine: M3 codon (64) + M2 invariant (72) + M1-5 (+1) = 137. The `+1` parent attribution rendered in topology slot `parentAttribution` must read `M1-5`, **not** `M0-Anuttara-witness`. Cross-link to DR-M1-1 / DR-M5-2.

   Extend the assertion with the `full_theoretical_alignments_ql_physics.md` register discipline: the integrated surface may display the **Third Spanda Equation's five canonical forms** (per `ql_m0_m3_third_spanda_integral_quilting_v2.md`):
   - `137 = 64 + 72 + 1` (M-stack composition)
   - `137 = 64 + 2(36) + 1` (Spanda-bridge form: doubled recognition-square)
   - `137 = 128 + 8 + 1` (binary/QCD decomposition: 2^7 + gluon-octet + color-singlet)
   - `137 = 2^7 + 9` (Mersenne-binary view)
   - `137 = (2^7 − 1) + 1 + 9` (Mersenne-prime-substrate view: M_7 + parent + wholeness)

   plus the **translation rule** `9_{M_2} = 8_{M_3} + 1_{M_1}` rendered as a labelled bridge between the 72-side and 64-side composition slots — the M1 parent unit literally sits between them. Any physics lane (`alpha_EM(0)^-1 ~= 137.036`, `alpha_EM(M_Z)^-1 ~= 128`, QCD `8+1`, `b_3 = -7` asymptotic freedom) is a labelled measurement/reference face, not a fourth rendered pole and not a QL-derived calculation.

   The **7-8-9 spine** (per `N_5 = 8n ± n` Spanda crown bifurcation) is rendered as a triadic labelled overlay on the composition: 7 = actional contraction (8n − n, harmonic-seventh 7/4 emerging from `(72−9)/36`), 8 = octave-field/return, 9 = wholeness/epogdoon-extension (9/8). The 7 is doubly grounded at the Mersenne layer (`127 = 2^7 − 1`, prime-index `31 = M_5`); display as `archetype-7 generator` annotation.

   Verification: widget render-test asserts `parentAttribution === 'M1-5'`; `grep -n parentAttribution Body/M/epi-theia/extensions/plugin-integrated-1-2-3/src/` shows correct binding; `grep -rn "127 = 2\\^7\\|2\\^7 - 1\\|9_M2 = 8_M3 + 1_M1\\|Third Spanda Equation\\|Mersenne" Body/M/epi-theia/extensions/plugin-integrated-1-2-3/src/` returns the canonical-form symbolic-skeleton wiring (overlay strings, not local computation); render/string test rejects `QL derives alpha` and accepts only `symbolic_skeleton` / `measurement_face` / `physics_reference` labels.

3. **7.3 — Solar-anchor design principle integration** *(doc-ahead-landing; cross-link to Tranche 09.5)*

   Plugin owns the **B-8 / B-9 / B-12** composition seam from Wave-B integrated-bimba: solar anchor + planetary placement + cross-surface edit propagation listener via kernel-bridge profile-tick events. Authored as the composition contract in `plugin-integrated-1-2-3/src/common/composition-contract.ts`.

   Verification: `ls Body/M/epi-theia/extensions/plugin-integrated-1-2-3/`; `cd Body/M/epi-theia/extensions/plugin-integrated-1-2-3 && yarn build`; `test -f package.json && grep -n 'solar\|planetary\|propagation' src/common/composition-contract.ts`.

4. **7.4 — Cl(4,2) one-algebra cross-pole assertion** *(aligned-only-note; cross-link to Tranches 02.7, 04.7, 10.7)*

   Plugin contract notes that M1 ring quaternion, M3 codon quaternion, and M4 personal quaternion share **one Cl(4,2) algebra** at `Body/S/S0/portal-core/src/quaternion.rs`. Cross-rendering integrity depends on this. Audit owned by Tranches 02.7 / 10.7; this note encodes the dependency.

   Verification: `grep -rn 'Quaternion\|cl_4_2\|cl42' Body/S/S0/portal-core/src/` returns one canonical type; plugin contract references the audit memo.

5. **7.5 — Plugin layout claim against shell-0 (cosmic) decision** *(spec-ahead-integration; cross-link to Tranche 11.1 DR-TS-1)*

   Plugin's `LayoutClaim` for shell-0 must match DR-TS-1 (VALIDATED 2026-06-02): no third layout; cosmic/personal is the intra-`daily-0-1` 0/1 toggle and `ide-deep` is the 4+2 depth surface.

   Verification: DR-TS-1 row in `13-decision-register.md` marked validated; `pnpm --filter @pratibimba/plugin-integrated-1-2-3 test` asserts layout-claim against ratified shell shape.

6. **7.6 — Coupling-flow overlay on the 137 visual signature** *(doc-ahead-integration; cross-link to Tranche 4.10 / 10.M3)*

   The integrated 1-2-3 plugin may expose the M3 `CouplingFlowAlignment` as a compact overlay on the existing 137 visual signature: symbolic skeleton badges on the K²/cymatic/codon composition, physics-descent labels in a summonable tooltip, and measurement-face values only as source-warranted references. It must not add a fourth geometry layer, because the composition remains exactly M1(+1) + M2(72) + M3(64).

   Verification: composition contract has no fourth pole; overlay consumes `coupling_flow_alignment` when present and renders `pending-coupling-flow-alignment` otherwise; no renderer-local RG/electroweak/QCD formulas in plugin source.

7. **7.7 — M3 codon-ring rotation-axis flip on Klein-flip event** *(spec-ahead-integration; DR-IG-3 VALIDATED)*

   On `KleinFlipEvent` at tick 5→6, the integrated surface performs the ratified M3 codon-ring 200ms rotation-axis flip, concurrent with the M1 K² fold and M2 cymatic valence invert. Patch INTEGRATED-1-2-3 §6.5 to replace "minimal flicker" with this axis-flip choreography.

   Verification: visual replay fixture asserts M1 fold + M2 valence invert + M3 axis flip occur from one profile-tick event; no local timer simulates the flip.

8. **7.8 — Consume `MathemeHarmonicProfile.m1_topology.torus_knot_phase` SSOT** *(spec-ahead-integration; DR-IG-4 VALIDATED)*

   Integrated 1-2-3 consumes `m1_topology.torus_knot_phase` from the profile bus via Tranche 10.M1 / 18.4. No parallel plugin-local `torus_knot_phase` definition is allowed.

   Verification: `grep -rn "torus_knot_phase" Body/M/epi-theia/extensions/plugin-integrated-1-2-3/src` shows reads only; definition lives in profile-bus types.

9. **7.9 — Consume ratified three-variant `KleinFlipEvent` SSOT** *(spec-ahead-integration; DR-IG-2 VALIDATED)*

   INTEGRATED-1-2-3 §4.2.2's proposed two-variant `CosmicKleinFlip` struct is replaced by the kernel `KleinFlipEvent` enum from Tranche 18.2. Subscribers match all three variants exhaustively.

   Verification: architecture doc no longer defines `CosmicKleinFlip`; plugin tests cover M1 tritone crossing, M2 valence invert, and M3 codon rotation cross variants.
