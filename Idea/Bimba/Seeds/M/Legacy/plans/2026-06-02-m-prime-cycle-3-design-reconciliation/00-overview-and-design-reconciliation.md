# Cycle 3 — First Full System Build: Overview

Cycle 3 is **the first full system build** — the cycle that wires the canonical AI architecture (the bioquaternionic JEPA-EBM operator; the 4'-5'-0' mental pole) and the full M' surface into one working system over the landed S-stack. **The deliverable is the built system the tranches plan for, not the plan set.** Design reconciliation is the preparatory discipline that precedes execution — contradiction-resolved, gap-explicit, code-grounded tranches that `/m-dev` then builds; plan updates (this pass included) are the final step before execution, in service of it. Cycle 1 built the S-stack. Cycle 2 owned the M' surfaces over landed substrate. Cycle 3 drives the pedagogical/UX design to the bottom against the real code and Theia substrate, resolves every load-bearing decision that needs user final-validation, and **builds the first full system shape on that resolved ground.**

## Method — the Four Corpora

For every load-bearing design claim, follow it across all four corpora:

```
(1) UX DOC          (2) M' SEED SPEC        (3) CODE / SUBSTRATE        (4) THEIA SURFACE
the pedagogical      the canonical domain    what is actually landed     the extension/shell
intent we wrote      authority + companions  in Body/S and Body/M        that renders it
```

Status taxonomy per claim:

- **ALIGNED** — doc ↔ spec ↔ code ↔ Theia all agree; note only.
- **DOC-AHEAD** — UX doc asserts intent the spec/code doesn't yet carry → landing tranche.
- **SPEC-AHEAD** — spec defines a contract the Theia surface doesn't consume yet → integration tranche.
- **CODE-PENDING** — a kernel/profile/graph contract is a known `pending-*` / blocker → named closure (no rebuild).
- **CONTRADICTION** — two sources disagree → decision-register entry routed to user final-validation.
- **ORPHAN** — a canonical surface/carrier/agent has no owner → no-orphan finding.

## Anti-Greenfield Rule (binding)

`Body/S/S0`–`S5`, `Idea/Pratibimba/System`, and `Body/M/epi-tauri` are landed/consumed substrate. Any cycle-3 tranche touching them must be phrased `consume as-is`, `audit/verify`, or `extend` — first-build ownership is **only** for an M' product surface, a named carrier/agent/subagent owner, a method-routing closure, or a concrete named integration blocker.

## Standing Invariants (already true — honor, do not re-derive)

- S0 is the membrane (CLI/process/adapter). S1–S5 own service law.
- `Body/M/epi-theia` is the M' Theia shell authority. `Body/M/epi-tauri` is deprecated migration-source only.
- The matheme spine is `M1 (+1) → M2 (72) → M3 (64)`; the `+1` parent is **M1-5, not M0** — residual `M0-witness` wording is a CONTRADICTION to be downgraded.
- The 72 is **one invariant, six addressing-axes** (MEF×QL · tattva · decan · Shem · maqam · DET-projection); mantra (100) and Asma'ul-Husna (99+1) are sonic overlays; planet LUT (10) is keying via decan-link, not a seventh 72-axis.
- Shell separation is load-bearing: shell `0` (cosmic 1-2-3) · shell `1` (personal 4-5-0 + flow-writing) · `4+2` depth · `/` OmniPanel — never collapse.
- Cl(4,2) runs at four scales (M1 ring · M3 codon · M4 personal · Kerykeion natal) — verify it is one algebra in code (kernel-bridge audit owns the proof).
- Hen owns Pi/vault entity-candidate compilation: dangling wikilinks and Obsidian root-created notes enter Empty candidate residency, promote through coordinate-native World/Types incubation, and graduate into flat World only after review. Smart Env suggests; Hen writes; S2 syncs.
- The C layer is the semantic typology authority inside World/Types: templates route through C1/CT, entities/properties/tags through C2, canvases/diagrams through C3, type/MOC authorities through C4, and World graduation through C5.
- **Third Spanda Equation is the canonical matheme spine** (ratified 2026-06-04 per DR-M3-6): `137 = [8(8+9) − 9] + 1 + 9` in five equivalent forms (Mersenne `(2^7−1)+1+9` · Binary `2^7+9` · Octave-field `8(8+9)+1` · Spanda-bridge `64+2(36)+1` · M-stack `M_3(64)+M_2(72)+M_1(1)`). Execution-order trace: `64+72=136 → −9 → 127=M_7 → +1 → 128=2^7 → +9 → 137 → +δ → 137.035999…`. The 127 Mersenne substrate (with prime-index `31 = M_5`) is structurally `M_5 → M_7` at the actional-Archetype-7 layer — it is the canonical evidence that the +1 attribution is M1-5's K² topological-necessity. Canonical source: [`Idea/Bimba/Seeds/M/ql_m0_m3_third_spanda_integral_quilting_v2.md`](../../ql_m0_m3_third_spanda_integral_quilting_v2.md).
- **Translation rule `9_M2 = 8_M3 + 1_M1` is canonical matheme law** (ratified per DR-IG-7): the M2 9-fold wholeness-gap projects to M3's 8-fold transcriptional gap plus the M1 parent unit. Recurs as QCD `3 ⊗ 3̄ = 8 ⊕ 1`. Whenever an 8-gap appears at M3, the reading must locate the M1 +1 that restores M2 wholeness.
- **Anuttara pentadic runtime trace is a Cycle-3 development surface** (Track 36): the 0/1 tick substrate projects through the five-degree Shem quantum into `72*5=360`, through the paired Mahamaya fifteens into `24*15=360`, and through the 384 line-change graph into M4/M5 recognition. Whole-number addressing reaches 5 from 0; natural-number addressing reaches 6 from 1. This is the canonical 4/5/6 hinge and complement-family layer, not a generic "+2" adjustment and not an after-the-fact physics adornment.
- **VAK is the operational language** (ratified per DR-VAK-3): the six VAK Context Frame families (CPF/CT/CP/CF/CFP/CS) wired in [`vak.h`](../../../../../Body/S/S0/epi-lib/include/vak.h) · [`vak_address.rs`](../../../../../Body/S/S0/portal-core/src/vak_address.rs) · [Anima `extension.ts`](../../../../../Body/S/S4/ta-onta/S4-4p-anima/extension.ts) ARE the system's language at the constitutional layer. The diatonic-scale-as-CF-progression is canonical operational truth carried by `MathemeDiatonicContext` at [`kernel.rs`](../../../../../Body/S/S0/portal-core/src/kernel.rs); music theory derivations live across M1-M4; M0-5 Śakti runtime is the foundational musical-linguistic capability within shakti-as-experiencer.
- **The kernel IS a bioquaternionic JEPA-EBM operator** (ratified per DR-MP-1, DR-MP-2, DR-MP-3): per [`epi-logos-kernel-spec.md`](../../epi-logos-kernel-spec.md) frontmatter, the kernel predicts in latent representation space (the bimba map as the manifold itself), evaluates by energy not by autoregressive likelihood, and learns through context-target prediction whose objective is energy-minimization. **The 4'-5'-0' mental-pole triplet IS the canonical AI architecture**: 4' (#-layer / Nara) = **LLM** (traversal-voice), 5' ({lens}-layer / Epii) = **EBM** (energy-evaluation across 12 MEF lenses / 72 fine-grained positions, gradient $\nabla_{q_p} E_{\text{total}}$ driving Möbius descent step $q_p^{(n+1)} = q_p^{(n)} - \log(9/8) \cdot \nabla E$), 0' (R-layer / Anuttara) = **Verifier** (R-virtue constraint-checker reading `VIRTUE_LUT[9]` Parameśvara virtues; raises questions in symbolic-coordinate strings rather than passing-or-failing). The 1-2-3 physical pole (engine) and 4-5-0 mental pole (intelligence) are two complementary aspects of one operation; the matheme operates at developmental scale exactly as it operates at tick scale. **Cycle 3 is where the canonical AI architecture is wired into the build**: per-track tranches operationalize each position (M5 6.8-10 EBM-resonance-predictor; M4 5.20-21 LLM-traversal-voice + Anuttara-symbolic-parse skill; M0 1.10-11 Verifier-R-virtue-constraint-checker + symbolic-coordinate-string emission). Canonical sources: [`epi-logos-kernel-spec.md`](../../epi-logos-kernel-spec.md), [`mental-pole-mechanics.md`](../../M4'/mental-pole-mechanics.md).
- **VAK is a four-expression typed transition calculus** (ratified per DR-VAK-7 Phase-I 2026-06-15): VAK lives as (1) **alphabet** at M0 (the 109-node Anuttara syntax — count provisional, the alphabet by which the system can say any statement of itself); (2) **field** = M, L, C, P, T, S + their inverses across which anything becomes apparent; (3) **C' = the S4 dispatch addressing** (CPF/CT/CP/CF/CFP/CS) — the coordinate calculus that walks the field; (4) **within L5' lens + T/T' coords** — the in-use perceptual/accumulative expression where wikilinks accumulate preformatively. Earlier framing collapsing VAK to C' only is incomplete. **"Compress to VAK"** means compress to a coordinate address from which `q_{n}_{semantic}` (already general per DR-Q-1) becomes discoverable; q_* is NOT embedded in the compression code. Canonical landing: Track 01 Tranche 1.18; cross-cutting infrastructure: CCT-17 + Tranche 12.33 (`compress_through_VAK()` orchestrator).
- **S5' is ONE substrate layer** (ratified per DR-S5-ONE-1 Phase-I 2026-06-15): gateway routes + Khora session-workspace + S0 tmux integration + Redis hierarchical `{day}/{session}/{turn}/{coordinate}/*` keys + CLI `epi gnostic *` parity must work as ONE substrate (temporal + contextual + informational-conditional + logos-definitional). No gnostic operation may bypass the gateway; no gateway route may exist without a CLI command. Three independent wave-2 scouts identified `s5'.gnostic.*` registration as the single biggest cycle 3 unblock; this DR mandates the full ONE-substrate scope, not gateway-only. Canonical plan: Track 39; canonical execution tranche: 12.2 EXPANDED.
- **`/World` is a 1st-class S2 namespace** (ratified per DR-WORLD-1 Phase-I 2026-06-15): `:World` (+ `:Archetypal` alias) graph label with `WORLD_FORM_OF` / `WORLD_ONTOLOGY_OF` typed relations linking entity nodes to base C-coordinates as the **psychoid root**. `/World` IS NOT parallel structure; it IS the C-axis psychoid expression of the canonical M/S/L/P/T/C field. The four-namespace map (post-cycle-3): `:Bimba` + `:World` (+ `:Archetypal`) + `:Gnostic` (with sub-labels `:Gnostic:Corpus`, `:Gnostic:Notebook`, `:Gnostic:Etymology`, `:Gnostic:Skills`) + `:Pratibimba` (S0-protected). Canonical landing: Tranche 9.14.
- **Library + Atelier are Theia IDE projections, NOT standalone extensions** (ratified per DR-LIB-ATELIER-1 Phase-I 2026-06-15): the M5-0' Library = vault filesystem organised by M-coordinate map (file-tree overlay + OmniPanel tab + breadcrumb header); the M5-5' Logos Atelier = etymological-cluster lens on the inbuilt m0-anuttara graph viewer + OmniPanel scent-following commands operating on currently-open files. No `library-surface` or `logos-atelier` standalone Theia extensions; the projections use existing extension contribution points. Canonical landing: Tranches 6.1 EXPANDED + 6.2 REFRAMED + 6.2b (Library projection) + 11.3 AMENDED + CCT-19.
- **Eros is a relational operator** (ratified per DR-EROS-1 Phase-I 2026-06-15): connections, resonances, oppositions, dissonances. CT2 in the constitutional CT0-CT5 mapping. Eros is a subagent that DOES different things including dev-work roles (TDD/verification IS one). The late TDD-verifier-only over-specification in `eros.md` is rectified. The constitutional CT0-CT5 mapping (Nous=CT0 / Logos=CT1 / Eros=CT2 / Mythos=CT3 / Psyche=CT4a+CT4b / Sophia=CT5) + CF1-CF5 team-composition gates (CF1={Nous,Logos}, CF2=+Eros, CF3=+Mythos, CF4=+Psyche, CF5=+Sophia) joins the 6 Aletheia techne-guardians (Anansi CF0 ... Zeithoven CF5) at the **same CF gate scale**: constitutional team-size and Aletheia techne-mode invocation are the same gate. Canonical landing: Tranches 12.27 (Eros) + 12.29 (CT/CF canon).
- **The Phase-I corrections honor original VAK + Pleroma planning** while integrating the DiscoverAI research scout findings: every Phase-I addition is wiring + exposure + clarification of substrate already in code or canonical in spec — except the M1' CPT trainer (Tranche 2.10), which IS the one greenfield build piece. Original research (MUSE-AutoSkill / SkillOpt / YES harness self-optimization / HiP-If hierarchical planning / HORMA filesystem memory / MemoryGraphRAG 3-layer / EKSFT distillation) is applied through native Epi-Logos primitives — Hermes-style skill_lookup on the VAK-indexed skill notebook (12.28), VAK-uniform entitlement (12.32), constitutional CT/CF team composition (12.29), MemoryGraphRAG-3-layer via `:World` + `:Bimba` + `:Gnostic` namespace stack (9.14 + 6.1 EXPANDED + CCT-17b), HORMA-via-VAK coordinate addressing (CCT-17), HiP-If `(C, H, g_k, τ)` via `ConversationSliceHandle` + Chronos as bifurcation router (12.31), EKSFT for M1' CPT (2.10). None foreign; all native to the matheme.
- **The three arenas are the ontological substrate axis** (ratified per DR-ARENA-1, Phase-M 2026-06-16): Siva (`.rodata` / immutable archetypes #0–#5 / canonical bimba `q_b`) projects to `:Bimba` nodes; Shakti (mutable heap instances / proposals / parametric experts / pratibimba `q_p`) projects to `:World`/runtime nodes; Prana (`Tensor_Arena` / 3072-dim embeddings) projects to the vector + EBM-head manifold where `E = ‖q_b − q_p‖²` is scored. The ONE Indras Net substrate (DR-S5-ONE-1) IS the persisted projection of these three S0 arenas, and the JEPA-EBM operator (DR-MP-1/2/3) IS the triad in motion. **HOT/WARM/COLD is the orthogonal cache-temperature axis layered over Prana — never the same axis as Siva/Shakti/Prana.** The **Prana arena** is the session-bound live VAK-compression surface — a per-NOW, Psyche-held working-set whose entries are `(coordinate ↔ embedding ↔ q_*)` triples — where the dense-vector and coordinate-language registers (held apart everywhere else) are made one per act, via recall (read-in, pre-addressed) and genesis (write-out, coordinate-earned-by-imprint → birth-codon). Ratified DR-ARENA-1 + DR-PRANA-1; canonical authority [[M'-PRANA-ARENA-SPEC]]. Canonical ground: [CLAUDE.md §VI](../../../../../CLAUDE.md); landing: Track 39, Tranche 6.8, CCT-14b, CCT-16.
- **Parametric knowledge injection is a sanctioned Nara-slot carriage mode** (ratified per DR-PARAM-1, Phase-M 2026-06-16): DMOA-style coordinate/birth-codon-routed, EBM-energy-gated (`E = ‖q_b − q_p‖²` is the gate, not Shannon entropy) micro-experts carry stable canonical facts in the local Nara (4') model's weights — canon-in-weights, content-in-context, KV-cache-preserving. It is a fast leaf-cache **companion** to GraphRAG, never a replacement; relational/multi-hop/provenance knowledge stays in the Indras Net substrate. Bounded to the stable-canonical-leaf class. Build rides Tranches **12.24** (manufacturing via `epii-distillation` + `mlx-lora`), **12.2 EXPANDED** (retrieval seam in `query_with_layers`), **CCT-14b** (terminus).

## Master Alignment Matrix (digest)

| Subsystem | ALIGNED | DOC-AHEAD | SPEC-AHEAD | CODE-PENDING | CONTRADICTION | ORPHAN | Matrix file |
|---|---|---|---|---|---|---|---|
| M0 Anuttara | 6 | 6 | 3 | 4 | 3 | 2 | `plan.runs/wave-a-m0-reconciliation-matrix.md` |
| M1 Paramaśiva | 6 | 4 | 4 | 3 | 1 | 0 | `plan.runs/wave-a-m1-reconciliation-matrix.md` |
| M2 Paraśakti | 6 | 3 | 4 | 2 | 2 | 2 | `plan.runs/wave-a-m2-reconciliation-matrix.md` |
| M3 Mahāmāyā | 8 | 4 | 2 | 0 | 3 | 1 | `plan.runs/wave-a-m3-reconciliation-matrix.md` |
| M4 Nara | 4 | 4 | 6 | 4 | 5 | 2 | `plan.runs/wave-a-m4-reconciliation-matrix.md` |
| M5 Epii | 4 | 3 | 4 | 3 | 2 | 2 | `plan.runs/wave-a-m5-reconciliation-matrix.md` |
| Kernel-bridge | 9 | – | 1 | 4 | 1 | 1 | `plan.runs/wave-b-kernel-bridge-matrix.md` |
| Theia shell | 7 | 1 | 4 | 1 | 2 | 1 | `plan.runs/wave-b-theia-shell-matrix.md` |
| Agentic S4↔S5 | 12 | 3 | 5 | 5 | 3 | 1 | `plan.runs/wave-b-agentic-layer-matrix.md` |
| Integrated bimba | 4 | 3 | 5 | 3 | 3 | 1 | `plan.runs/wave-b-integrated-bimba-matrix.md` |

The matrices in `plan.runs/` are the **reconciliation evidence base** — the per-row evidence with file:line citations that grounds the build. The tranche docs in `01-14` synthesize the matrices into m-dev-executable build units.

## Execution Sequence

Cycle 3 closes in this dependency order:

1. **Decisions first (Tranche 13)** — every CONTRADICTION resolved before its downstream tranches begin work. Three contradictions are load-bearing across the whole stack: the +1-parent (M1-5 vs residual M0-witness wording), the 17th-lens / 16+1 namespace question, and the M0' CRUD-vs-governed-route shape.
2. **Profile-spine closure (Tranche 10 + Track 36.1-36.2)** — `klein_flip` field landing on `MathemeHarmonicProfile`, planetary-LUT cardinality decision (DR-KB-1), `depositionAnchor` typed-vs-DTO decision (DR-KB-2), `s2_anchor`/`s3_anchor` audit, and `AnuttaraPentadicRuntimeTrace` landing as the typed 0/1->5 complement-family trace. Most downstream M-extension widgets read against this ledger.
3. **Per-domain reconciliation (Tranches 01-06 + Track 36.3 / 36.5 / 36.6 + Track 38 §8 M5 tunability surface)** — M0..M5, each in parallel where possible, blocked only by decisions and profile fields they depend on. M3 owns the Maxwell/Mahamaya 15 inspector, M4 consumes protected Q handles, and M5 consumes the trace as a real EBM feature family. Track 38 lands the system-wide tunability surface at M5-2'/M5-3'/M5-4' (cross-coordinate: schema authority / Tuning UI / runtime lifecycle) as the foundational-and-manipulable principle's spec home — first-class consumers are Tranches 5.26 / 5.27 / CCT-14b, each of which references the surface explicitly.
4. **Integrated plugin composition (Tranches 07-09 + Track 36.4-36.5)** — 1-2-3 cosmic engine + 4-5-0 recognition + the cross-rendering bimba-graph substrate close once the per-domain tranches stabilize. The pentadic runtime trace overlays both compositions as profile-bus data.
5. **Shell + agentic closure (Tranches 11-12 + Track 41)** — shell separation invariant decision (DR-TS-1), cross-layout intent routing T5 promotion, ACR governance gates, gnostic gateway registration, **Track 41 Vama Shakti Factory + Nara M4' Dia-logical Arena closure** (Psyche-templated `techne_vama_summon` + SpacetimeDB Arena tables + Theia M4' widget; four canonical classifiers egregore/sprite/daemon/mantra per DR-VAMA-6; DR-VAMA-1..6 VALIDATED Phase-K 2026-06-16).
6. **No-orphan audit + release gates (Tranche 14 + Track 36.7)** — fail if any canonical surface, carrier, agent, plugin, profile-field, or doc-claim lacks an owner or a decision, including `0/1->5`, paired Mahamaya 15s, `24*15=360`, `AnuttaraPentadicRuntimeTrace`, the four SpacetimeDB Arena tables, the `m4.nara.dialogicalArena` view id, and the `techne_ensoul` registration.

## Decisions Routed to User Final-Validation (preview — full register in Tranche 13)

| ID | Subject | Source domain |
|---|---|---|
| DR-M0-1 | M0' CRUD vs governed-route via M5 atelier | M0 |
| DR-M0-2 | Anuttara source naming canon (`c_1_*` vs `symbol`/`formulation_type`) | M0 |
| DR-M0-3 | Retire residual "M0 has no internal sixfold" wording (M0' has six M0-X' data layers) | M0 |
| DR-M1-1 | Audit and downgrade residual `M0-Anuttara-witness` wording (+1 parent is M1-5) | M1 |
| DR-M1-2 | K² played-torus 3D surface owner (React 2D vs Bevy/wgpu extension) | M1 |
| DR-M2-1 (DCC-03) | Planet-count + Earth-observer semantics for the 9:8 M2→M3 bridge | M2 |
| DR-M2-2 | Six axes of 72 + overlays canonicalisation (the M2 17th-lens-shaped question) | M2 |
| DR-M3-1 | TCT / Nine-of-Wands cardinality (dataset 8 vs runtime 7) | M3 |
| DR-M3-2 | 72→64 epogdoon identity with M2 planetary 9:8; no profile-field decision | M3 |
| DR-M3-3 | 17th-lens / 16+1 namespace split (M1_LENS 12 vs M3_LENS_STACK 16+1) | M3 |
| DR-M4-1 | DayContainer vault path (`Pratibimba/Nara/{day_id}/` vs `day/{dayId}/`) | M4 |
| DR-M4-2 | q_personal baseline + Cl(4,2) axis order + Vāma classifier policy + 0/1 cymatic polarity | M4 |
| DR-M5-1 (consolidated DR-B-1) | Pi as named ACR governance role vs constitutional-roster ontology | M5 / Agentic |
| DR-B-2 | Pi tool-surface: axiom-translation land or UX downgrade | Agentic |
| DR-B-3 | Aletheia subagents in ACR `AgenticActor` union | Agentic |
| DR-KB-1 | Planetary projection LUT cardinality (7-degree+Pluto vs 10+Earth-observer) | Kernel-bridge |
| DR-KB-2 | `depositionAnchor` typed profile field vs bridge-synthesized DTO | Kernel-bridge |
| DR-TS-1 | Shell-0/Shell-1/4+2/`/` separation: intra-layout toggle vs third layout | Theia shell |
| DR-IG-1 | Two-relation-families schema discriminator (`c_1_relation_family`) | Integrated bimba |
| DR-S1-4 | Hen entity-candidate lifecycle from Empty to Types to World | S1' / Hen |
| DR-S1-5 | C-layer semantic typology as World/Types authority | S1' / Hen |
| DR-TUNE-1 | Foundational-and-manipulable principle as cycle-3 standing invariant | M5' / cross-cutting |
| DR-TUNE-2 | M5-2'/M5-3'/M5-4' cross-coordinate tunability residency | M5' / cross-cutting |
| DR-TUNE-3 | Tuning risk-class taxonomy (A user-gated / B auto-with-rollback / C Aletheia-pattern) | M5' / cross-cutting |
| DR-TUNE-4 | Privacy-class enforcement via Anuttara verifier (slot-rule pattern extended) | M5' / cross-cutting |
| **DR-VAMA-1** | Psyche as canonical template kernel for `techne_vama_summon` (CF (4.0/1-4.4/5) continuity-holder = Spanda) | Track 41 / Agentic |
| **DR-VAMA-2** | Vama Shakti essential-identity determinism via BLAKE3(coord ‖ form_digest ‖ sattva ‖ class_byte) | Track 41 / Identity |
| **DR-VAMA-3** | Summoning is addressable-by-coordinate-only (no draft/sidecar; bioquaternion-determinism IS canon) | Track 41 / Hen-boundary |
| **DR-VAMA-4** | Arena substrate canon: SpacetimeDB + Theia M4' widget under m4-nara scaffold (CLI is admin-only) | Track 41 / M4' |
| **DR-VAMA-5** | Dialogue-only capability profile for Vama Shaktis (structural, not configurable) | Track 41 / Agentic |
| **DR-VAMA-6** | Four canonical Vama Shakti classifiers (egregore/sprite/daemon/mantra) with real behavioral semantics | Track 41 / M4' |

Each row is fully developed in `13-decision-register.md` with affected files, recommended resolution, and the verification command that closes the row.

## Subagent Returns

The reconciliation matrices are not summaries — they are the load-bearing rows with file:line citations. Every Mn tranche depends on its matrix; every cross-cutting tranche depends on its Wave-B matrix. Reading a tranche without reading its matrix is reading half the work.

- Wave A — `plan.runs/wave-a-m{0..5}-reconciliation-matrix.md`
- Wave B — `plan.runs/wave-b-{kernel-bridge,theia-shell,agentic-layer,integrated-bimba}-matrix.md`
- Track 36 — `36-anuttara-pentadic-runtime-trace.md` (late-cycle integration track tying Anuttara M0-3 archetypal number language, M3 Mahamaya computation, integrated composition, and M5 EBM learning into a typed 0/1->5 runtime trace)
- Track 38 — `38-tunability-surface-architecture.md` (late-cycle M5 deliverable: system-wide tunability surface at M5-2'/M5-3'/M5-4'; Tranches 06.7-06.12 land schema crate + Tuning UI + Tier 2 self-awareness review lane + Tier 3 ML-training hook + audit loop + migration of existing config surfaces; DR-TUNE-1..4 PROPOSED)
- Track 39 — `39-s5-prime-one-substrate-layer.md` (S5' ONE substrate: gateway + Khora + S0 tmux + Redis hierarchical + CLI parity as one Indras Net access layer; DR-S5-ONE-1 VALIDATED 2026-06-15)
- Track 40 — `40-bimba-canon-update-ledger.md` (Phase-J additive canon-update aggregator; CU-IDENTITY/FORM/ENTITY/REL/VOCAB/XREF rows with lifecycle + provenance; intake from Sophia envelope + Hen lifecycle + manual + CLI + sub-agent surfacing; downstream landing into Bimba Seeds + World/Types + DR register + S2 graph-schema)
- Track 41 — `41-vama-shakti-factory-and-dialogical-arena.md` (Phase-K dia-logical synthesis: Vama Shakti Factory psyche-templated `techne_vama_summon` in Pleroma + Nara M4' Dia-logical Arena SpacetimeDB tables + Theia widget; four canonical classifiers egregore/sprite/daemon/mantra; PASU IS itself a Vama Shakti per load-bearing symmetry; ontologies-in-conversation primary use case; 12 tranches; DR-VAMA-1..6 VALIDATED Phase-K 2026-06-16; canonical M4' seed at `Idea/Bimba/Seeds/M/M4'/m4-prime-vama-shakti-factory-and-dialogical-arena.md`; cross-link consumers in Tracks 12 / 25 / 39 / 40 / 16 / 14)
- Track 42 — `42-harness-dynamics-surface.md` (Phase-K multi-harness runtime substrate: normalized harness dispatch envelope + turn-event stream in `gateway-contract`, harness registry with NativeCli/Acp backing kinds, unified S0 launcher, per-session harness binding in Khora `session-workspace.json`, harness-neutral transcript-of-record, two-axis delegation router (model-family × harness/subscription), symlink skill projector across harnesses, harness-blind Sophia/Aletheia → Graphiti memory pipeline; Pi canonical parent, any harness as sub-session; 9 tranches; designed for direct build, no DR gate; cross-link consumers in Tracks 12 / 39 / 06)

## What "the bottom of the design" means here

The cycle is complete enough to constitute m-dev cycle 3 when:

1. Every load-bearing claim across the six UX docs has a row in a four-way matrix with a status.
2. Every CONTRADICTION is a decision-register entry routed to user final-validation.
3. Every CODE-PENDING blocker is named with its owning spec and the contract that unblocks it — none silently assumed landed, none proposed as greenfield rebuild.
4. Every UX doc claim is either ALIGNED, has a landing tranche (DOC-AHEAD), or is explicitly downgraded with a reason.
5. The no-orphan audit (Tranche 14) passes: no canonical surface, carrier, agent, plugin, profile-field, or doc-claim is ownerless.
6. The plan set is route-able by `m-dev-plan-assess.mjs` and sequenced as above.
