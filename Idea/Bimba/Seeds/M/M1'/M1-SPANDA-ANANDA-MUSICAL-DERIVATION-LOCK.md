# M1 Spanda–Ananda Musical Derivation Lock

Status: focused correction/lock companion to the M1 deep capability matrix  
Coordinate field: `M1-2 Ananda` ↔ `M1-3 Spanda` ↔ `M1-4 QL flowering` ↔ `M1-5 topology`  
Executable music authority: `EpiLogos/QL-MEF` #31 / PR #81  
Wayfinder: `EpiLogos/Epi-Logos-C-Experiments` #32

## 0. Purpose

This companion fixes the exact M1 derivational relation now established across the theorem source, the current QL-MEF musical implementation, the Ananda source architecture and the M1 deep-instrument programme.

It supersedes two older shorthand readings wherever they still occur in the larger M1 matrix:

1. First Spanda is not adequately represented as two sequential three-step halves. Its direct/pratibimba faces are co-arising aspects of one self-differentiation and remain synchronically available while the same doubled field is articulated diachronically as a 12-walk.
2. `D1/D2/D3` are not three additional pairing families alongside `A/B/C`. `A/B/C` are the three primary positional relation families; `D1→D2→D3` names increasing conjugate completion from two to three to four coordinates.

The larger matrix remains the subsystem survey. This file is the focused derivational lock for musical/kernel work.

---

## 1. First Spanda — generated bi-phase

Canonical structural equation:

```text
0 = (0/0)
  → ((0/1)/(1/0))
  → T0 || T1
  → (1/0 + 0/1)
  → 1/1 = 1 = 100%
```

The two directional faces are present from genesis:

```text
T0  emanative / proportional / bimba
T1  reversionary / geometrical / pratibimba
```

Their superposition earns the standing whole `1/1`; the whole is not posited before the movement.

The engine consequence is therefore:

```text
synchronic
  six positions × direct/prime conjugate phase

and

diachronic
  one 12-state traversal of that doubled sixfold
```

The temporal walk serialises a synchronically doubled field. A runtime state must preserve active phase and conjugate co-state together with `tick12`, `position6`, Hopf/SU(2) state and the corresponding Ananda relation.

### 3:3 and 3:1

The horizontal `3:3` is the processual articulation of three moments and their complementary recognitional moments. Ontologically its recognitional pole is one act with a three-moment rhythm:

```text
1,2,3 ≡ 0, /, 1

4 : 3 → 0     enfold/contextualise
5 : 0 → 1     recognise as one
0 : 0 AND/OR 1 → 0/1     superpose/regenerate
```

Hence:

```text
3:3 = three moments of articulation : three moments of their recognition
3:1 = articulated three : the one recognitive act which holds them
```

Position `#4` is the aperture where sequential differentiation becomes simultaneously held Context.

---

## 2. Second Spanda — self-accounting of the generated whole

```text
100% = 2^6 + 6^2
     = 64 + 36
```

The two terms are already the numerical germ of the later M3/M2 distinction:

```text
64 = 2^6   determinate binary field / Mahāmāyā germ
36 = 6^2   sixfold self-relation / Paraśakti germ
```

Paraśakti's later `72` is the doubled articulation of the `36` field:

```text
36 × 2 = 72
72 / 64 = 9/8
```

The reduced internal proportion is:

```text
64/36 = 16/9 = 4^2/3^2 = (4/3)^2
```

This immediately generates the central directional ratio field:

```text
4/3   manifestation / going-forth
3/4   recognitional inverse
3/2   complementary / aspirational fifth
2/3   grounding inverse
```

and the epogdoon/non-closure appears by two independent internal relations:

```text
2 / (16/9) = 9/8
(3/2) / (4/3) = 9/8
```

The same `9/8` later articulates the Paraśakti/Mahāmāyā seam `72/64`.

---

## 3. Pythagorean closure and the 6/12 topology

The Second Spanda exposes:

```text
16 = 4^2
9  = 3^2
```

whose primitive completion is:

```text
3^2 + 4^2 = 5^2
```

The triangle returns the QL cardinalities directly:

```text
area      = (3×4)/2 = 6
perimeter = 3+4+5   = 12
```

The six therefore appears both arithmetically:

```text
1+2+3 = 1×2×3 = 6
```

and geometrically as the area of the generated `3-4-5` relation. Twelve is the corresponding doubled circulation.

The `5` is the diagonal completion disclosed by the generated `3/4` relation. It is relationally significant without being collapsed into either leg.

---

## 4. Two native sixfold axes

The same sixfold carries two distinct partitions:

```text
4:2  vertical/topological
     four explicate positions held with two implicate poles

3:3  horizontal/processual-harmonic
     three articulations and three recognitional/complementary moments
```

They are concomitant reads of one generated field. Their cross-reading yields the canonical musical ratios already implemented by QL-MEF #31.

---

## 5. Jankó instrument Figure

The historical Jankó keyboard gives a concrete playable surface on which both sixfold partitions coexist.

Historical/instrumental facts retained separately from QL interpretation:

```text
six-row isomorphic keyboard
= two interleaved whole-tone row families
× three repeated touch-points per sounding note

traditional alternating colour distributions:
4 white : 2 black
3 white : 3 black
```

Project-level instrumental reading:

```text
QL 4:2  ↔ Jankó 4:white / 2:black row family
QL 3:3  ↔ Jankó 3:white / 3:black row family

QL two whole-tone helices
↔ Jankó two interleaved whole-tone row families

QL chromatic cross-face semitone
↔ Jankó diagonal/cross-row semitone movement
```

Jankó is a controller/playable Figure over the accepted derivation, never its authority. The detailed controller contract lives in QL-MEF PR #81 at `docs/music/JANKO-QL-INSTRUMENT-FIGURE.md`.

---

## 6. Relation grammar — A/B/C and D completion

The accepted kernel/music distinction is:

```text
A / B / C
three primary pre-pitch positional relation families

then

D1 → D2 → D3
conjugate completion degree
2  →  3  →  4 coordinates

ending in the complete square
{X, Y, X′, Y′}
```

Operator names such as transform/require/complete describe operations available within the appropriate completion state; they do not make D a fourth relation family.

The executable QL-MEF musical object consumes these canonical IDs and gives basis-specific pitch/interval consequences over the same direct/prime `Z12` field.

---

## 7. Spanda ↔ Ananda ↔ executable music

Spanda and Ananda are two registers of one M1 movement:

```text
Spanda
phase / oscillation / generation / return
        ↕
Ananda
arithmetic relation / recurrence / harmonic residue
```

Ananda must retain three typed readings where applicable:

```text
raw 12×12 source arithmetic
true DR 12×12 recursive residue
explicit 10×10 / mod-10 decimal aperture
```

The current executable frontier is not the musical grammar itself; QL-MEF #31 already implements that grammar. The open M1-2 bridge is:

```text
Given
  Ananda family
  raw relation
  DR residue
  decimal aperture where relevant
  Spanda phase
  direct/prime conjugate state

derive/test
  A/B/C relation family
  D1/D2/D3 completion degree
  exact ratio
  basis-specific interval
  L/L′ tonic-relative realization
```

The complete source state must therefore survive the runtime handoff so this mapping can be derived and falsified rather than guessed.

---

## 8. Product/instrument composition

The M-level and deep-instrument relation remains:

```text
M1 / M1′
bi-phasal formal-harmonic generation and playable matheme
        ↓
M2 / M2′
36→72 differentiated Power and situated vibrational realization
        ↓
M3 / M3′
64-fold determinate transcription, transformation and clock/score
```

The integrated `1-2-3` application composes these as one cosmic event. M1′, M2′ and M3′ remain individually inspectable/playable instruments at their own depths.

The reciprocal return is equally native:

```text
M4 entity/event/narrative
→ M3 score/re-inscription
→ M2 vibrational relation plan
→ M1 harmonic realization/performance
```

The cross-system trace contract and M2-5 planetary↔chakral situated relation are recorded in `../M1-M4-HARMONIC-TRANSDUCTION-RETURN-MATRIX.md` and Wayfinder #32.

---

## 9. Acceptance invariants

Any M1 musical/kernel implementation should preserve all of the following together:

- First Spanda direct and prime phases are co-arising and concurrently addressable;
- the 12-walk is a temporal articulation of the doubled sixfold rather than a second independent topology;
- `64+36` precedes `36→72`; `72/64=9/8` is retained as the later M2/M3 proportional seam;
- `16/9=(4/3)^2`, `2/(16/9)=9/8` and `(3/2)/(4/3)=9/8` remain exact rational invariants;
- the `3-4-5` geometry returns area `6` and perimeter `12`;
- `4:2`, `3:3` and the deeper recognitional `3:1` remain distinct but composed readings;
- A/B/C are the three relation families;
- D1→D3 is conjugate completion, not a parallel family taxonomy;
- Ananda raw, digit-root and decimal-aperture readings remain typed and provenance-bearing;
- Jankó remains an instrument projection over canonical QL state;
- the wider M1→M2→M3 and M4→M3→M2→M1 traces preserve position, face, ratio, relation, completion and source provenance.
