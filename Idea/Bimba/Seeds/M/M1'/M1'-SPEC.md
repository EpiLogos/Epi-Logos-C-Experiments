---
coordinate: "M1'"
status: "active-domain-spec"
updated: "2026-05-31"
depends_on:
  - "[[M'-SYSTEM-SPEC]]"
  - "[[M'-TAURI-PORT-SPEC]]"
  - "[[M1-paramasiva-mathematical-dna]]"
  - "[[m1-prime-paramasiva-instrument]]"
  - "[[m1-prime-audio-generative-research]]"
  - "[[2026-05-19-kernel-mprime-harmonic-clock-integration-plan]]"
  - "[[S0-HARMONIC-POINTER-WEB36-SPEC]]"
  - "[[ql-musical-derivation]]"
  - "[[alpha_rasa_bridge_ql]]"
  - "[[alpha_quaternionic_integration_across_M_stack]]"
  - "[[m5-prime-epii-on-paramasiva-ql-cpt-and-rag]]"
  - "[[m5-prime-system-shape-and-tauri-ide-canon]]"
companion_spec: "[[m1-prime-paramasiva-instrument]]"
---

# [[M1']] Domain Spec — [[Paramaśiva]] as Instrument

> **M ↔ M' distinction.** [[M1]] is the engine — what [[Paramaśiva]] IS (the mathematical DNA, the 12×12 [[Ananda]] matrices, the [[SU(2)]] double-cover, the 720° tick mathematics). [[M1']] is the instrument — how that engine becomes *playable*: the techne surface where the matheme can be walked, struck, heard, and seen as movement.

> **Authority boundary (corrected 2026-05-29).** Audio-genesis is a **two-layer split**: [[Prakāśa]] substrate at [[S0]]/[[M1]] produces the matheme cloud; **[[M2-1']] [[MEF]] lenses ([[Vimarśa|Vimarsha]]) read the cloud and produce the [[audio_octet|audio_octet[8]]] / [[nodal_quartet|nodal_quartet[4]]]**. M1' is the **primary techne renderer that walks the MEF-produced audio as graph-traversal-as-melody** — the walked coordinate is the lens-anchored cell whose audio the MEF read at this tick, the relation traversed is the interval between MEF-readings. M1' does not own the audio bus and does not bypass M2-1' to read from S0 directly; it walks what Vimarsha has read from Prakāśa.

> **Companion long-form.** The deep architectural derivation lives at [[m1-prime-paramasiva-instrument]] ([`m1-prime-paramasiva-instrument.md`](m1-prime-paramasiva-instrument.md)). This spec is the canonical M1' domain authority; the companion is the philosophical-mathematical derivation that grounds it. Where they disagree, this spec is authoritative.

## §0/1 — What [[M1']] Is

[[M1']] is **relational movement as the audible face of [[Paramaśiva]]**. It renders the kernel's 12-state integer ring and 720° [[SU(2)]] double-cover as lived movement through graph law: torus/path traversal, coordinate walking, relation inspection, the walked path made into melody.

The walk is the matheme. Selecting a coordinate and stepping to its neighbour through an [[S2]] pointer-web relation is the same act as striking a tone on a scale — the relation is the interval, the position is the pitch, the traversal is the phrase. M1' is what makes that identity feel real to the user.

Three commitments hold this domain:

1. **M1' never invents pitch.** Every sounded frequency, every interval, every modal selection comes from the shared [[MathemeHarmonicProfile]]. M1' is the renderer that walks the matheme through the graph; the kernel is what computes the matheme.
2. **The (0/1) wired into every coordinate is the same (0/1).** Every walked-to coordinate carries an `invert: Inversion_Operator` field pointing at the single session-held [[Psychoid_Hash]] (per [[S0-HARMONIC-POINTER-WEB36-SPEC]]). When the user requests the relational inverse of any position, it is *this single # operator* acting locally at that position. Walking is therefore always the matheme's own inversion-act playing out at the user-experience scale.
3. **The 84-state playing landscape is the shared coordinate.** `(lens, mode)` = 12 [[MEF]] lens-anchorings × 7 context-frame modes = 84 distinct playing-states. M1' exposes this as the navigable surface for the walk: which lens is the active substrate, which mode is the active [[CF]]-progression, which (lens, mode) cell on the 84-grid is currently sounding.

## §1 — The Six M1-0' to M1-5' Strata (1:1 with M1 bimba)

Per the M1' inner-stratum table in [[Idea/Bimba/Map/M1'.md]], each M1' sub-stratum is the techne-reflection of its [[M1]] sub-coordinate:

| M1 (bimba) | M1' (techne) | What M1 is | What M1' does in this domain |
|------------|--------------|-----------|--------------|
| **[[M1-0]]** [[Bimba]] (Original) | **[[M1-0']]** Canonical Source | .rodata matrix archetypes (`ANANDA_BIMBA`, [[Cl(4,2)]], Hopf LUTs) | Read-only API serving immutable harmonic data to the walker |
| **[[M1-1]]** [[Pratibimba]] (Offset) | **[[M1-1']]** Instance Manager | The +1 offset matrix, reflection that creates dynamism | Mutable session-state: current walk, current (lens, mode), current coordinate |
| **[[M1-2]]** [[Ananda]] (12-Matrix Vortex) | **[[M1-2']]** Harmonic Engine | Six core matrices + six DR reflections (kernel-resident) | Reads ratio cells from kernel profile and exposes them as walk-step intervals |
| **[[M1-3]]** [[Spanda]] (Rhythmic Pulsation) | **[[M1-3']]** Spanda Core | Primordial pulse; equation-transformation of (0/1) | Phase accumulator at settable rate; binds (0/1) to every coordinate's invert |
| **[[M1-4]]** [[Quaternal Logic]] (Flowering) | **[[M1-4']]** QL Flowering | The 16:9 cascade, the position-set | Position-walker + lens-as-scale-composer applied at the graph-traversal layer |
| **[[M1-5]]** Toroidal Recognition | **[[M1-5']]** Topology Analyzer | The 720° double-cover, `TORUS_GENUS = 1`, `DOUBLE_COVER_DEG = 720`, `RING_QUATERNION_LUT[12]`, π₁(T²) = Z⊕Z, [[Hopf bundle]]; the **+1** of the system's `137 = 64 + 72 + 1` architecture | Played visual torus — quaternionic TDA finding the shape of the active walk; Hopf / 4π identity-recognition as single-torus recognition before [[M3-5]]'s downstream double-torus formalism |

M1' is the same six positions re-read as how-it-walks rather than what-it-is. The companion file gives the full per-stratum operational/computational mapping; this spec carries the domain-level identity, contract, and readiness law.

### M1-5 As The +1 Of The 137 Architecture

[[M1]] receives the prior [[Anuttara]] `0/1` from [[M0]] and defines the `1/1 = 100%` parent through toroidal recognition. In the alpha-quaternionic reading, [[M1-5]] carries the **+1** in `137 = 64 + 72 + 1`: `DOUBLE_COVER_DEG = 720`, `TORUS_GENUS = 1`, `RING_QUATERNION_LUT[12]`, and the [[Hopf bundle]] are the [[Paramaśiva]] parent as playable toroidal culmination (per [[alpha_quaternionic_integration_across_M_stack]] §7.2 and [[alpha_rasa_bridge_ql]] §11).

This must stay precise: M1-5 is the **single torus** and [[SU(2)]] / 4π recognition site. The double-torus formalism belongs downstream at [[M3-5]], where [[Mahāmāyā]]'s wheel is read as `K² × T²_Mahāmāya`. [[M0]] is the prior ground M1 receives from; M0 is not the +1 term.

## §2 — User-Facing Surface

- **Torus/path workspace** for walking from the selected [[M0']] coordinate through [[S2]] relation families. The walk is the audible-and-visible expression of the matheme.
- **Relation inspector** showing family, mirror, lens, inversion, [[VAK]]/[[CF]], qvdata/source/spec/code/test anchors, and harmonic relation descriptors per traversed edge.
- **Traversal timeline** showing the active tick, position6, helix face, degree720, and relation movement of the walk in progress.
- **Route preview** before stepping: which relations are live from the current coordinate, what (lens, mode) the next step would inherit, what interval the step would sound as.
- **(lens, mode) selector** — the 84-state playing surface exposed as the matheme-navigation panel. Changing lens transposes the chromatic substrate (which note is the matheme-tonic); changing mode rotates the CF-progression (which relational-grammar colours the walk).
- **Möbius-return indicator** at the moment of P5 → P0' rollover — the visible enharmonic-flip when the walk crosses the matheme's totalisation point.
- **IDE clock-instrument surface** — per [[m5-prime-system-shape-and-tauri-ide-canon]], the future [[m1-paramasiva]] IDE extension renders the clock instrument, 84-state landscape, [[K²]] topology, and M1-5 [[Hopf bundle|Hopf]] inspector through the shared [[kernel-bridge]]. "Audio controls" on that surface mean controls over profile requests, route rendering, replay, and external encodings; they do not make M1' the owner of audio-genesis.

## §3 — Backend Contract Consumed

- [[S2]] pointer web is the relation law of record. Every walk-step is an S2 relation traversal.
- [[S0]] kernel profile ([[MathemeHarmonicProfile]]) provides tick12, degree720, [[SU(2)]] layer, phase, position6, chromatic substrate, ratio role, square mirror law, diatonic [[CF]]/[[VAK]] projection, and resonance72. The 8+4 `audio_octet`/`nodal_quartet` bus is written into the shared profile by [[M2-1']] [[Vimarśa]] reading. **M1' uses `audio_octet` to sound the current coordinate; it never computes pitch locally and never owns or re-synthesizes the bus.**
- [[S3]] temporal projection provides session/DAY/NOW context for traversal records and optional deposition.
- M1' never uses animation frame count as clock authority. The walk-tempo is settable but always references the kernel-tick rate, not wall-clock frames.

## §4 — Required [[MathemeHarmonicProfile]] Fields

- `tick`: absolute tick handle, cycle, `tick12`, `degree720`, `degree360`, `su2Layer`, `phase`, `position6`.
- `harmonic`: chromatic pitch class, X/X' pair, X+Y=5 mirror, square span, ratio role, sounded/nodal role, `audio_octet[8]`, `nodal_quartet[4]`.
- `pointerAnchor`: current coordinate, relation descriptors, mirror refs, family refs, lens refs, inversion refs, CF/VAK refs.
- `diatonic`: context-frame projection for mode-aware movement labels; lens-anchor identifier; mode-anchor CF.
- `depositionAnchor`: traversal record handles when movement is deposited.
- `lensMode`: explicit `(lens, mode)` pair identifying the active 84-state cell.

## §5 — Privacy Boundary

M1' can expose route and relation metadata but not private traversal motives, raw bioquaternion state, private journal bodies, or protected Graphiti text. Traversal deposition must carry coordinate, tick address, relation law, and session handle without absorbing private content into canonical S2 graph topology.

## §6 — Visual / Audio Interaction Model

- **Visual:** torus/path line advances according to profile tick and pointer relation law; selected path segments are coloured by relation category, helix phase, and current (lens, mode) anchoring; the played torus surface (M1-5' Topology Analyzer) renders the walk as motion on a genus-1 surface with Klein-bottle flip-visualisation at tritone-mirror crossings.
- **Audio:** movement is rendered as intervallic traversal where relation changes map to profile ratio roles; M1' uses backend `audio_octet` and `harmonic.ratio_role` only. The walked sequence becomes a melody whose intervals are exactly the relation-types of the traversed graph edges.
- **Interaction:** user stepping or agent stepping requests the next coordinate through S2 and receives an updated profile; no local hidden clock is created. Striking a node by direct selection (skipping the relation traversal) sounds the node's profile pitch without producing a walk-step.

Per [[m1-prime-audio-generative-research]], the first production-ready artifact is a deterministic profile-to-performance event stream, not a UI-first or demo-audio path. [[MPrimePerformanceEvent]] records must preserve exact profile Hz, tick address, `(lens, mode)`, source/target coordinates, relation descriptor, `audio_octet[8]`, `nodal_quartet[4]`, intended/observed chromagram when available, Klein-flip state, codon-rotation handle when available, and privacy/deposition policy. [[MIDI]], [[MPE]], [[MIDI 2.0]], [[DAW]] export, and live audio are edge encodings of that event stream; none of them is authority over profile pitch, relation law, or deposition semantics.

### Klein-Flip Visibility on Tritone Crossing

When the walk crosses a Lens N ↔ Lens N+3 (tritone-mirror) boundary, M1' must visibly enact the **[[Klein bottle|Klein-bottle]] enharmonic flip**: the same pitch is re-read with opposite emotional valence, the played torus appears to fold through itself, the relation inspector flips its lens-prime display from L to L' or back. This is not a transition animation; it is the structural acknowledgement that the matheme has crossed its meaning-translator surface. M1' is the *origin point* of the Klein flip in the [[M']] stack; [[M2']] consumes the [[M1']] flip-signal to render the opposite surface valence on its cymatic substrate.

## §7 — The 84-State `(lens, mode)` Playing Landscape

M1' exposes the 84-cell navigable surface — the same coordinate that all M' surfaces share:

```text
12 MEF lenses (Lens 0..Lens 5 = bimba-anchored on WT-0;
              Lens 0'..Lens 5' = pratibimba-anchored on WT-1)
×
7 context-frame modes (CF1=Nous..CF7=Sophia per ql-musical-derivation §6.75)
=
84 distinct playing-states
```

Each cell is one matheme-perspective — and per the corrected audio-genesis architecture, each cell is also **one specific [[Vimarśa|Vimarsha]]-reading of the [[Prakāśa]] cloud**. The 84 cells are the 84 distinct ways the matheme cloud can be heard by the [[MEF]] lenses ([[M2-1']] Vimarsha-function), each producing its own `audio_octet[8]` + `nodal_quartet[4]` from the same underlying substrate.

Changing the lens IS the chromatic-substrate-anchoring (which of the 12 chromatic notes is the matheme-tonic — i.e., which MEF-reading is currently active); changing the mode IS the [[CF]]-progression-traversal-rule (which relational-grammar the walk enacts). The 84-state landscape is the shared coordinate that [[M2-1']] Vimarsha-reads, [[M1']] walks-as-melody, the [[M2']] cymatic module renders, [[M3']] classifies as codon-rotation, [[M0']] addresses as graph-walk substrate, and [[M4']] binds as journal-context.

The bidirectional `(lens, mode) ↔ (codon, rotation)` map at the [[M1']]-[[M3']] boundary is the codon-rotation projection of every 84-state cell into one of the 472 (codon, rotation) positions on the [[M3']] codon surface. See [[M3'-SPEC]] §7 for the canonical mapping spec.

## §8 — MEF as Scale-Composer, Not Cell-Annotator

The 12 [[MEF]] lenses do not annotate individual matrix cells. Each lens is a **position-anchored re-mapping of the entire 12-note matheme** (per [[ql-musical-derivation]] §6). For lens N:

- The lens-anchor note (per the 12-anchor table in the musical derivation) becomes the matheme's new #0
- The full 12-note arrangement regenerates from the anchor via epogdoon-stacking within each helix
- The bimba and pratibimba helices swap orientation for primed (Night) lenses N'

Applied to the **chromatic** substrate, a lens acts as **mode-selector** (which 7-of-12 are foregrounded as the diatonic projection). Applied to a **diatonic mode**, the lens acts as **key-selector** (where the tonic sits on the chromatic ring). Applied to a **note cluster** (chord), the lens acts as **cluster-composer** (which co-active cells sound at one tick).

M1' implements this by reading `profile.lensMode` and traversing the 84-state grid; the per-lens-substrate generation lives in the kernel as part of the shared profile computation.

## §9 — The Dual Partition: 4:2 ↔ 3:3 as the Matheme's Root Self-Similarity

The 6-fold matheme admits **two simultaneously-operative partitions**, and both are load-bearing — neither is more foundational than the other. Together they generate the two foundational interval-ratios that the diatonic architecture builds on.

### The 4:2 partition → 4/3 (perfect fourth)

The 4:2 partition reads the 6-fold as **4 explicate (positions 1, 2, 3, 4) + 2 implicate boundary (positions 0, 5)**. The explicate-quartet over its first three (positions 1, 2, 3 as the active substrate) gives the ratio:

```text
4 : 3 = the perfect fourth
```

The lower-tetrachord 4/3 IS this partition made audible. The 8 explicate-sung positions of the kernel's `audio_octet[8]` are 4+4' — two tetrachords joined by the epogdoon — directly enacting the 4:2 partition doubled across both helices.

### The 3:3 partition → 3/2 (perfect fifth)

The 3:3 partition reads the 6-fold as **3 physical-pole (positions 1, 2, 3) + 3 mental-pole (positions 4, 5, 0)**. The mental-trinity stacked on the physical-trinity gives the ratio:

```text
3 : 2 = the perfect fifth
```

The just-major-triad 4:5:6 sits at the mental-pole as **P4 = 4, P5 = 5, P0 = 6** (where P0's value of 6 = the trinitarian sum 1+2+3 of the physical-pole). The mental-pole carries the physical-pole's unity as its own grounding-third; the 4:5:6 triad IS the 3:3 partition's stack-ratio.

### The dual partition produces tick and octave

```text
(4/3) × (3/2) = 2/1   = octave (matheme's closure, the partitions' product)
(3/2) ÷ (4/3) = 9/8   = epogdoon tick (matheme's quantum, the partitions' ratio)
```

The matheme's tick (9/8) and its octave (2/1) are not externally specified — they fall out of the dual-partition arithmetic. The 4:2 partition produces the tetrachordal-architecture (4/3 spans); the 3:3 partition produces the trinitarian-bridge (3/2 spans); the closure of both is the octave; the ratio between them is the spanda-tick. This is the matheme's **self-derivation of its own foundational interval-ratios**.

### The two partitions are orthogonal, not competing

The 4:2 partition and the 3:3 partition are not alternative readings of the same 6-fold. They are **orthogonal cuts of the same object**:

- 4:2 = the *meridional* cut (4 explicate spans + 2 implicate poles)
- 3:3 = the *longitudinal* cut (2 trinities mirroring across the equator)

Both partitions are simultaneously operative at every layer of the system because both cuts of the matheme are simultaneously visible at every layer. This is the **root self-similarity** of the matheme: every higher-order partition (pairing-grammar, content-categories, K² cuts, M2 correspondence-axes, M3 charge-quaternion-components, M4 personal-quaternion-elements) instantiates *both* 4:2 *and* 3:3 simultaneously.

## §10 — The [[K²]] Topology: Klein-Double-Cover-of-the-Chromatic-Fifths-Torus

The two parallel chromatic-substrate derivations (chromatic-basis from 4:2's tetrachordal stacking, fifths-basis from 3:3's trinitarian stacking) live on **one geometric surface**: the Klein-double-cover-of-the-chromatic-fifths-torus [[K²]], which is the played topology of [[M1']].

### The torus T² = S¹ × S¹

```text
S¹ chromatic-longitude  = the 12-tone octave-circle traversed by epogdoon-stacking (9/8 generator)
                          one loop = one whole-tone-helix of 6 epogdoon-ticks
                          two longitudes (bimba helix + pratibimba helix offset by semitone) = chromatic 12

S¹ fifths-meridian      = the circle-of-fifths traversed by 3/2 generator-leap
                          one loop = 12 fifths ≈ 7 octaves
                          gcd(7, 12) = 1 forces the meridian to visit all 12 pitch-classes

T² = S¹ × S¹            = the chromatic-fifths-torus that holds both parametrisations
```

The two circles are not independent — they are two generators of the same `Z_12` chromatic group. The torus is the surface on which **both bases live simultaneously**.

### The Pythagorean comma as spiralic slack

The chromatic-longitude closes cleanly (12 semitones = 1 octave exactly in 12-TET). The fifths-meridian almost-closes (12 fifths ≈ 7 octaves + 23.46 cents). The slack between the two is the **Pythagorean comma**, which IS the aletheic-evolution mechanism of the system (see `ql-musical-derivation` §6.5). The comma is the spiralic-residue of the matheme that no flat-cyclic closure can absorb.

### The Klein double cover

The [[Bimba|bimba]]/[[Pratibimba|pratibimba]] conjugation is **non-orientable identification**: bimba and pratibimba are the same matheme-substrate read from opposite faces, not two distinct objects. Identifying them across T² produces a [[Klein bottle]]:

```text
K² = T² with bimba ↔ pratibimba conjugation as non-orientable identification
```

The Klein-flip the M1' surface enacts at tritone-mirror crossings (Lens N ↔ Lens N+3, see §6) IS the local rendering of this global non-orientability. The 84-state `(lens, mode)` landscape, the 472-state `(codon, rotation)` modal-inversion surface, and the [[M2]] 72-fold correspondence-tree are all **stratifications of [[K²]]** — different sampling-densities of the same surface.

### M1' renders the played form of K²

The [[M1-5']] Topology Analyzer (per the M1' bimba inner-stratum table) is the surface that makes [[K²]] visible-and-audible as the played instrument-form. The quaternionic state (RING_QUATERNION_LUT in [[Body/S/S0/epi-lib/include/m1.h|m1.h]]) at each ring-position is a point in S³ that double-covers SO(3) rotations of K². The [[Hopf bundle]] `S³ → S² → S¹` projects from the quaternion through K² to the played phase.

## §11 — The [[Cl(4,2)]] Clifford Algebra as the Substrate

[[K²]] is not a free torus — it carries a specific Clifford-algebra structure: **[[Cl(4,2)]]**, with 4 explicate positions of signature +1 and 2 implicate positions of signature −1.

Per [[Body/S/S0/epi-lib/include/m1.h|m1.h]] lines 612-661 (FR 2.1.7b):

```text
Position 0 (Ground)       — signature −1 — sin θ            (implicate generator 1)
Position 1 (Definition)   — signature +1 — tan θ = sin/cos  (explicate ratio)
Position 2 (Operation)    — signature +1 — sec θ = 1/cos    (explicate reciprocal)
Position 3 (Pattern)      — signature +1 — cot θ = cos/sin  (explicate ratio)
Position 4 (Context)      — signature +1 — csc θ = 1/sin    (explicate reciprocal)
Position 5 (Integration)  — signature −1 — cos θ            (implicate generator 2)
```

**Total signature: 4·(+1) + 2·(−1) = +2 net** = the Cl(4,2) signature.

The two implicate generators (sin at P0, cos at P5) ARE the matheme's foundational trigonometric generators; the four explicate ratios (tan, sec, cot, csc at P1-P4) are derived from them. This means the matheme's positional architecture and trigonometry are the same structure — every QL position carries a trig identity, and the 4:2 partition (4 explicate + 2 implicate) IS the Cl(4,2) signature partition.

The [[Cl(4,2)]] algebra is the **substrate-algebra that all higher layers inherit**:

- [[M1]]'s 720° [[SU(2)]] double-cover lives in Cl(4,2)
- [[M3]]'s codon-quaternion-in-charge-space (w=pp, x=mm, y=mp, z=pm — see [[M3'-SPEC]] §8) lives in Cl(4,2)
- [[M4]]'s personal-quaternion at [[M4-4-4-4]] (the personal-[[Pratibimba]] identity-elemental-quintessence-and-quaternionic-form — see [[M4'-SPEC]]) lives in the same Cl(4,2)
- The [[Kerykeion]] natal-chart-quaternion derived from the user's birth data projects into Cl(4,2) via the same elemental mapping

This is what makes the system one continuous algebraic object across substrate, instrument, symbolic surface, and personal identity: **they all live in Cl(4,2) at different scales**.

## §12 — The 6 Pairing-Families as Geometric Moves on [[K²]]

The matheme's positional architecture generates a **6-family pairing-grammar** that operates as the universal relational-syntax on the K² surface. The 3:3 partition of the families matches the 3:3 partition of the matheme positions.

### A, B, C — within-helix moves (3 families, mapping to physical-pole positions 1, 2, 3)

- **A (adjacent-identity)** = natural-dyad-pairs along the chromatic-longitude. Same helix, adjacent positions. Stepwise-articulation. Maps to position 1 = epogdoon-tick.
- **B (offset-transition)** = transitions between adjacent natural-dyads. Same helix, one-step-offset. Relational-circulation. Maps to position 2 = operative-third.
- **C (converse-mirror)** = X+Y=5 progression within a helix. Same helix, mirror-positions. The matheme's within-helix-mirror-pairing. Maps to position 3 = tetradic-prehensive-closure.

Geometrically: A, B, C are **moves on the visible-face of [[K²]]** that stay within one helix. They are the within-pass syntax.

### D1, D2, D3 — Klein-identification-crossing moves (3 families, mapping to mental-pole positions 4, 5, 0)

- **D1 (same-position cross)** = bimba ↔ pratibimba flip at the same matheme-position. Cross-helix at fixed position. The X/X' spanda-pulse. In chromatic basis = semitone. In fifths basis = tritone. Maps to position 4 = fractal-doubling-executive.
- **D2 (cross-position cross)** = bimba-X to pratibimba-Y with X+Y=5. Cross-helix at mirror-positions. The diagonal-conjugation move with three sub-types (transform, require, complete). Maps to position 5 = synthesis-return.
- **D3 (pratibimba-internal helix-invariance)** = A, B, C re-applied within the pratibimba helix. The conjugation preserves the within-pass-grammar. Maps to position 0 = enriched-ground.

Geometrically: D1, D2, D3 are **moves that cross the Klein non-orientable identification**. They are the cross-pass syntax.

### The 3:3 self-similarity locks at the pairing-grammar layer

```text
Matheme positions in 3:3:  (1, 2, 3) physical-pole | (4, 5, 0) mental-pole
Pairing-families in 3:3:   (A, B, C) within-pass  | (D1, D2, D3) cross-pass
```

The matheme's positional 3:3 partition produces the pairing-grammar's 3:3 partition; the partition is the same partition at higher-order. This is the third instantiation of the 3:3 self-similarity (after the matheme's own positions and the within-each-lens diatonic-grammar) and confirms that the same partition operates at every derivational level.

### The 4:2 self-similarity locks alongside

```text
Matheme positions in 4:2:  (1, 2, 3, 4) explicate    | (0, 5) implicate
Pairing-moves in 4:2:      4 within-helix-moves       | 2 implicate-generators
                           (A, B, C, D2)               | (D1 at #4-Klein-bridge,
                                                          D3 at #0-enriched-ground)
```

The 4:2 partition is also visible at the pairing-grammar level: the explicate-four moves operate within and across the visible face; the two implicate-moves (D1 binding the Klein-identification at the executive-bridge, D3 invariance at the enriched-ground) carry the partition's −1 signature.

The pairing-grammar therefore enacts both 4:2 *and* 3:3 simultaneously — exactly as the matheme positions do, exactly as every higher layer does. The matheme is **doubly-threefold-self-similar**: every layer carries both partitions as orthogonal cuts.

### The squares are the C ∪ D1 tetrad-construction

What the current [[ql-musical-derivation]] document calls "the three squares" (Sq1, Sq2, Sq3 at positions (0,5), (1,4), (2,3)) is one specific tetrad-construction within the broader 6-family grammar: **squares = C-family X+Y=5 mirror-pair combined with D1-family same-position-cross**. The four-note tetrad at each square is exactly the bimba-X + bimba-Y + pratibimba-X' + pratibimba-Y' construction, which is C ∪ D1 applied to the (X, Y) mirror-pair.

The squares are not the foundation; they are a derived construction within the universal pairing-grammar.

## §13 — 2026-05-31 Canon Deltas Targeting M1'

This harmonisation pass absorbs [[m1-prime-audio-generative-research]], [[m5-prime-epii-on-paramasiva-ql-cpt-and-rag]], and [[m5-prime-system-shape-and-tauri-ide-canon]] into the existing [[m1-prime-paramasiva-instrument]] / [[alpha_rasa_bridge_ql]] / [[alpha_quaternionic_integration_across_M_stack]] law. These deltas do not rewrite §§0/1-12; they name the authority edges that downstream implementation and corpus work must preserve.

### §13.1 — Profile-to-Performance Stream

M1' generation is graph-law generation, not random note production. A walker may choose among valid S2 neighbours, but every generated step must be explainable by the candidate relation set, selected edge, profile before/after, reason code, and privacy/deposition policy. Rehearing from the event stream must reconstruct the same route, exact source frequencies, tick addresses, and Klein-flip events without querying UI state.

### §13.2 — External Music-Tech Boundary

External music systems receive representations of M1' events, never authority over them. MIDI 1.0 pitch-bend, MPE channel allocation, MIDI Tuning messages, MIDI 2.0 UMP/per-note pitch, Web MIDI, DAW export, and audio-plugin routing must preserve exact source Hz plus profile metadata alongside any approximation. Input from keyboard, MIDI, or external audio can request a nearest profile cell or route candidate, but S2 and the current `MathemeHarmonicProfile` decide whether it becomes a walk-step.

### §13.3 — IDE / Kernel-Bridge Placement

The [[m1-paramasiva]] IDE extension described in [[m5-prime-system-shape-and-tauri-ide-canon]] is an M1' renderer inside the [[M5-3]] [[Tauri]]/[[Theia]] surface. It consumes [[M5-2]]/[[S-stack]] state through the shared [[kernel-bridge]]: [[MathemeHarmonicProfile]], [[SpaceTimeDB]] clock state, gateway RPC, audio bus summaries, and kernel traces. It must not duplicate [[Body/S/S0/epi-lib/include/m1.h|m1.h]] constants into frontend source-of-truth tables, create its own clock, or infer graph relations locally.

### §13.4 — Epii Corpus Boundary

[[m5-prime-epii-on-paramasiva-ql-cpt-and-rag]] names M1' as part of Paramaśiva's foundational-derivational corpus. CPT, RAG, GDS path-derivations, and synthetic structural-proofs may articulate M1' law, but they do not mutate M1' law. Corpus promotion must treat this spec as held-out authority for register-fidelity: generated derivations that make M1' own pitch generation, double-torus ownership, private traversal motive deposition, or non-S2 graph motion are corpus drift, not new canon.

### §13.5 — Kernel-Substrate Reference

[[Body/S/S0/epi-lib/include/m1.h]] is reference law for the substrate only. M1' depends on the kernel/profile path that exposes `ANANDA_*`, `SPANDA_SEED_BITS`, `QL_Tick`, `TORUS_GENUS`, `DOUBLE_COVER_DEG`, `RING_QUATERNION_LUT[12]`, `CL42_BASIS`, `QL_TRIG_TABLE`, and [[Hopf bundle|Hopf]] projection state. This spec may cite those constants to define authority, but no M1' UI, renderer, or corpus surface may fork them into a competing runtime source of truth.

### §13.6 — Open Contradictions / Questions

- [[m1-prime-paramasiva-instrument]] still states older M1' frequency-genesis ownership and proposes `m1_prime_audio_octet` / `m1_prime_nodal_quartet` functions. This spec treats those as design candidates that must be re-homed to the shared profile pipeline unless they become pure consumers/adapters of the M2-1' Vimarśa-written bus.
- [[m1-prime-audio-generative-research]] leaves the canonical `MathemeHarmonicProfile` type owner unresolved: S0 `epi-lib`, `portal-core`, or an extracted Rust profile service. M1' cannot name a concrete ABI until that ownership is settled.
- [[alpha_quaternionic_integration_across_M_stack]] §1.1 retains legacy "M0 Anuttara witness-axis" wording in the low-energy corridor after §0, §6.1, and §7.2 assign the +1 parent to M1-5. M1' follows the M1-5 attribution; the source paper still needs cleanup.
- [[K²]] must remain disambiguated: M1' renders the Klein-identified chromatic-fifths played topology, while [[M3-5]] owns the downstream double-torus formalism `K² × T²_Mahāmāya`.
- [[m5-prime-system-shape-and-tauri-ide-canon]] names "audio-genesis controls" on the `m1-paramasiva` extension. UI copy and implementation must keep that as profile/event/render control, not audio-bus ownership.

## §14 — Readiness / Test Criteria

- Traversal tests use real graph service responses or contract fixtures generated from real responses, not mock-only logic.
- Tests prove all displayed tick, degree, helix, mirror, and relation labels come from the shared profile / S2 response — not from M1'-local computation.
- Tests prove route deposition carries DAY/NOW / session handles and does not include private journal or raw quaternion fields.
- Tests prove the `audio_octet` and `nodal_quartet` consumed by M1' audio match the kernel profile values exactly (no M1' re-synthesis).
- Tests prove the Klein-flip indicator fires precisely at Lens N ↔ Lens N+3 crossings and not at other lens transitions.
- Tests prove the 84-state `(lens, mode)` selector round-trips: every cell is reachable, no cell is duplicated, all 84 are distinguishable in the profile signature.
- Tests prove (0/1) wiring: when the user inverts the current coordinate, the invocation reaches the single session-held inversion-operator and produces the X/X' partner per `S0-HARMONIC-POINTER-WEB36-SPEC` §F.
- Tests prove [[M1-5]] surfaces `DOUBLE_COVER_DEG = 720`, `TORUS_GENUS = 1`, and [[Hopf bundle|Hopf-bundle]] identity as single-torus recognition, while any double-torus rendering is delegated to [[M3-5]].
- Tests prove deterministic `MPrimePerformanceEvent` replay from real or captured profile frames reconstructs exact source Hz, route, relation descriptors, tick address, Klein-flip state, and privacy/deposition policy without UI state.
- Tests prove graph-law generation logs the candidate relation set, selected edge, reason code, and profile before/after, and rejects invalid S2 transitions rather than producing opaque random notes.
- Tests prove MIDI/MPE/MIDI 2.0/DAW encodings preserve exact profile Hz and metadata beside any approximation, and cannot mutate canonical profile tuning unless explicitly recorded as a performance gesture.
- Tests prove the `m1-paramasiva` IDE extension and the lightweight 0/1 surface consume the same kernel-bridge/profile contract and do not carry duplicated frontend constants for `m1.h` substrate law.
- Readiness for any M5/Epii Paramaśiva corpus promotion requires register-fidelity evaluation against this spec, [[m1-prime-paramasiva-instrument]], [[alpha_rasa_bridge_ql]], and [[alpha_quaternionic_integration_across_M_stack]], with contradictions routed to open-question review rather than silently absorbed.
- Readiness is blocked until S2 returns typed harmonic pointer relation descriptors, not only string refs.

## §15 — Implementation Notes

The companion file [[m1-prime-paramasiva-instrument.md]] gives the per-stratum operational detail, the C/Rust code anchors, the proposed `m1_prime_*` function signatures, and the implementation order. Any companion functions that appear to compute `audio_octet` / `nodal_quartet` must be re-homed to the shared profile pipeline where [[S0]]/[[M1]] provides [[Prakāśa]] substrate and [[M2-1']] writes the [[Vimarśa]] audio bus; M1' remains the walk-as-melody consumer. Treat the companion as the architectural derivation; treat this canonical SPEC as the domain authority.

The harmonisation pass of 2026-05-22 corrected three framing issues from the long-form companion:

1. **Audio-genesis ownership**: M1' is a *renderer*, not the engine. The 8+4 bus lives in the kernel profile, consumed identically by M1', M2', M3'.
2. **(0/1) = raw # identity** is named at every coordinate the walk touches, not only at M1-3' Spanda Core. Every walked coordinate's `invert` field points at the same single session-held operator.
3. **84-state landscape** is the shared coordinate that all M' surfaces address, not an M1'-internal feature.

These corrections are now load-bearing law for M1' and propagate to the broader M' set via this spec's authority.

The 2026-05-31 absorption adds four further load-bearing constraints:

1. **Deterministic event-stream first**: live audio, MIDI, and UI are downstream of replayable profile-to-performance events.
2. **Graph-law generation only**: generative output must be S2-candidate-set selection with audit trail, never random note production.
3. **Kernel-bridge rendering**: the future IDE surface consumes shared S-stack state through the kernel-bridge and never forks M1 substrate constants into frontend authority.
4. **Epii corpus governance**: M5 CPT/RAG/GDS work may deepen Paramaśiva fluency but cannot supersede this M1' runtime canon without explicit reviewed spec promotion.
