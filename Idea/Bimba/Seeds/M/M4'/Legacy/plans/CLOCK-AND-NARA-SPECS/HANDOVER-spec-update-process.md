# Handover Prompt — Spec Update Process Across Harmonised Plan Files

**Purpose**: Drive the surgical-spec-update pass that applies all the architectural work from the recent session-thread (clock + Nara + α-quaternionic + cymatic field + temporal integration + M0/Anuttara + neosemantics + GDS + SpaceTimeDB shared cosmos) back into the canonical M' / S' specs at `Idea/Bimba/Seeds/`.

**Audience**: a fresh-context Claude session (or another agent) charged with executing the spec-update pass.

**Mode**: surgical updates per the §7 / §16 / §19 spec-delta tables in each plan file. No structural rewrites; small additions, annotations, new subsections per the deltas. The plan files are authoritative; the canonical M' specs are the targets.

---

## Context

A recent session-thread harmonised a substantial body of architectural work across the Epi-Logos M-stack. The work spans:

1. **The α-rasa physics-symbolic bridge** (`alpha_rasa_bridge_ql.md` at M3') — fine-structure constant α(E) as parent-coupling of the QL slash, 137 = 64 + 72 + 1 as integer-psychoid-skeleton, Pauli quaternionic substrate, three śaktis in alpha register.

2. **The α-quaternionic integration across the M-stack** (`alpha_quaternionic_integration_across_M_stack.md` at M root) — 137 = M1(+1) + M2(72) + M3(64) along the manifestational sequence, three M3 matrices with i/j/k quaternion axes literally in code, 4π at M1-5 and M2-0, α(E) reframed as epigenetic bioquaternion-to-live-state coupling, full M4 branch (M4-1 to M4-5), Vāma Śaktis as contemplative recognition-vocabulary (not judgment), Diamond-QL Vitruvian via JSX research, **SpaceTimeDB shared cosmos at S3'** with quaternionic identity signatures (BLAKE3 reframed as column-fit only), WebSocket-shared gateway+SpaceTimeDB surface, opt-in synchronicity layer.

3. **The Vāma Śaktis / Vāmeśvarī QL unit** (`ql-unit-vama-shaktis-vameshvari.md` at M4') — the Shaktopāya operative wheel with parent 0/1 = Jñāna/Kriyā Śakti, six positions Vāma-śakti → Vāmeśvarī, descending/ascending passes, inverse pairings, Muṇḍa/Daṇḍa cross-reading. **Operationally: mantric-divinitive recognition-vocabulary**, not sovereignty-position judgment.

4. **The M4'-level psychoid-cymatic field engine + temporal integration spec** (`m4-prime-psychoid-cymatic-field-engine.md` at M4') — Nara as nesting of M1/M2/M3 in psychoid form; rendered as live cymatic-particle-field engine (not iconographic assets); 0/1/4+2 layout discipline; body-topological-without-body-figurative; colour quaternion (6-channel elemental-cap projection); M2' cymatic engine at personal-Pratibimba register; Mahāmāya 16+1 lens-stack as holographic backdrop; Hopf-linked tori + diamond geometry; Vāma Śaktis as contemplative offerings (not judgments); temporal-processual integration via Chronos/Kairos/Graphiti/history-directory; Janus + L3/L3' + L2' alchemical as transformational reading; full simulation register with caching/memoisation; audio-always-running drives cymatics; 9 implementation tranches; **§18–§20 covering M0/Anuttara/neosemantics/GDS**: all # coords = M coords; Anuttara as pre-math language at node level (symbol + formulation_type properties); M3-Mahāmāya IS the graph view (already wired in Neo4j); neosemantics OWL bridge; GDS (Option 1 baseline + Option 3 enrichment); Möbius/Klein topology handling; C kernel ↔ Neo4j sync; embedded graph view in Tauri-v2; Jiva-is-Śiva computationally concrete at 4+2 depth.

5. **The holding-context temp file** (`Idea/Bimba/Seeds/M/M4'/Legacy/plans/CLOCK-AND-NARA-SPECS/TEMP-mahamaya-clock-aural-field-integration.md`) — preserves the persistence-across-compaction chain.

---

## What to do

### Step 1 — Read the plan files in order

Read these in the listed order; each builds on the prior. Read fully; do not skim:

1. `Idea/Bimba/Seeds/M/M3'/alpha_rasa_bridge_ql.md` (the physics bridge — orientation)
2. `Idea/Bimba/Seeds/M/alpha_quaternionic_integration_across_M_stack.md` (the M-stack integration — main spec-delta source)
3. `Idea/Bimba/Seeds/M/M4'/ql-unit-vama-shaktis-vameshvari.md` (Vāma Śaktis QL unit — Trika lens content)
4. `Idea/Bimba/Seeds/M/M4'/m4-prime-psychoid-cymatic-field-engine.md` (M4'-level depth spec + M0/Anuttara/neosemantics/GDS — main spec-delta source for M4'/M0'/M3'/S2')
5. `Idea/Bimba/Seeds/M/M4'/Legacy/plans/CLOCK-AND-NARA-SPECS/TEMP-mahamaya-clock-aural-field-integration.md` (holding context for the temp persistence chain)

The spec-delta tables are in:
- `alpha_quaternionic_integration_across_M_stack.md` §7 (deltas to M0'/M1'/M2'/M3'/M4'/M5')
- `m4-prime-psychoid-cymatic-field-engine.md` §16 (deltas to M4'/M2'/M3'/S3-Gateway) + §19 (deltas to M0'/M3'/S2'/S3-Gateway/M'-SYSTEM-SPEC/M'-TAURI-PORT-SPEC)

### Step 2 — Read the canonical target specs

Read the canonical M' specs that will receive updates:

1. `Idea/Bimba/Seeds/M/M'-SYSTEM-SPEC.md`
2. `Idea/Bimba/Seeds/M/M'-TAURI-PORT-SPEC.md`
3. `Idea/Bimba/Seeds/M/M'-PORTAL-SPEC.md`
4. `Idea/Bimba/Seeds/M/M0'/M0'-SPEC.md`
5. `Idea/Bimba/Seeds/M/M1'/M1'-SPEC.md`
6. `Idea/Bimba/Seeds/M/M2'/M2'-SPEC.md`
7. `Idea/Bimba/Seeds/M/M3'/M3'-SPEC.md`
8. `Idea/Bimba/Seeds/M/M4'/M4'-SPEC.md`
9. `Idea/Bimba/Seeds/M/M5'/M5'-SPEC.md`

And the S' targets where applicable:
- `Idea/Bimba/Seeds/S/S3/S3'/Legacy/specs/S/S3-S3i-GATEWAY.md` (SpaceTimeDB shared cosmos integration)
- Any `Idea/Bimba/Seeds/S/S2'` spec if it exists; otherwise the S2 graph-services materials at `Body/S/S2/`

### Step 3 — Apply spec deltas surgically

For each canonical spec, apply the deltas enumerated in the plan files' delta tables. Discipline:

- **Surgical, not rewritten**: add annotations, new subsections, sharpening notes. Do NOT rewrite existing sections wholesale unless explicitly required.
- **Preserve frontmatter and structure**: respect existing `coordinate`, `status`, `updated`, `depends_on` frontmatter. Update `updated` to today's date. Add new `depends_on` entries pointing to plan files where appropriate.
- **Maintain canonical voice**: the M' specs are domain authorities; speak as such. The plan files are working papers and can speak more exploratively; the canonical specs absorb the conclusions.
- **Cross-reference plan files**: where a delta is substantial, include a one-line pointer to the plan file as `(per [[alpha_quaternionic_integration_across_M_stack]] §6.5)` style.
- **Test for cross-spec coherence**: after each update, scan adjacent specs for newly-introduced contradictions. If any, raise as open question; don't silently resolve.

### Step 4 — Update the TEMP file with progress

After each canonical-spec update is complete, append a progress entry to `Idea/Bimba/Seeds/M/M4'/Legacy/plans/CLOCK-AND-NARA-SPECS/TEMP-mahamaya-clock-aural-field-integration.md` so the persistence chain stays current. Brief entries — coord name + what changed + date.

### Step 5 — Verify readiness criteria

Each M' spec has §N "Readiness / Test Criteria" subsections. Verify the new deltas don't break existing criteria; add new criteria where the deltas introduce new structural law that needs test coverage.

---

## Concrete delta map (the work to actually do)

Below: each canonical spec, the plan-file deltas to apply.

### M0'-SPEC

From `m4-prime-psychoid-cymatic-field-engine.md` §19.1:
- All # coords are M-family coords annotation
- New §: Anuttara as pre-math language at node level (`symbol` + `formulation_type` properties)
- New §: M0' graph view as Mahāmāya's structural rendering on the 0 side; pulse animation tied to world_clock; GDS tangent overlay; Anuttara-syntax inspector; filtered-subgraph rendering
- §10 readiness additions per the spec

From `alpha_quaternionic_integration_across_M_stack.md` §7.1:
- No structural change; M0 is the prior 0/1 ground M1 receives from (NOT the +1 of 137; that's M1)

### M1'-SPEC

From `alpha_quaternionic_integration_across_M_stack.md` §7.2:
- Annotation that M1-5 carries the **+1 of the system's 137 architecture** (`DOUBLE_COVER_DEG = 720`, `TORUS_GENUS = 1`, `RING_QUATERNION_LUT[12]`, Hopf bundle)
- M1-5 is single torus; double-torus formalism lives downstream at M3-5

### M2'-SPEC

From `alpha_quaternionic_integration_across_M_stack.md` §7.3:
- §0/1 annotation: 72-invariant explicitly named as the "72" of 137 architecture
- §8.6 or new §: Fibonacci ground at M2-0 as φ-rooted dynamic generator of 72-fold static vibrational invariant
- §9 or new §: 9:8 epogdoon at M2-5 as M2-M3 conjugation enacted at planetary-chakral bridge
- §9 annotation: Earth-as-observer-ground as bodily-cosmic witness-anchor

From `m4-prime-psychoid-cymatic-field-engine.md` §16.2:
- §0/1 annotation: M2' cymatic engine has dual application registers — cosmic-facing (existing) and personal-Pratibimba at #4.4.4.4
- §6 addition: M2-1' audio bus drives BOTH cymatic standing-wave rendering AND personal-Pratibimba cymatic field; audio is primary state driving visual derivation

### M3'-SPEC

From `alpha_quaternionic_integration_across_M_stack.md` §7.4:
- §8.2 annotation: three matrices' i, j, k quaternion-axis assignment from `M3_MATRIX_QUATERNION_AXIS` (m3.h:166-170) named explicitly
- §8.3 reframe: 64-element transformation tables as hexagram-scale action of same three matrices
- New §: 64 × 3 × 2 = 384 factoring as gauge-axis × eigenvalue-polarity decomposition
- New §: 137 = 64 + 72 + 1 architecture on M1 → M2 → M3 spine
- New §: Pauli physics-register reading as interpretive overlay (no separate bimba coord)
- §8.9 annotation: 720° SU(2) wheel = same 4π as M1-5; M3-5 carries double-torus
- §8.6 sharpening: 384 = 24 × 16 partition
- New §: four representational views per temp-file §1.G

From `m4-prime-psychoid-cymatic-field-engine.md` §16.3, §19.2:
- §8.6 annotation: 16+1 lens-stack has dual rendering registers — cosmic-wheel (existing) and frequency-filter backdrop for personal field
- New §: Janus aspect at M3 — every codon-rotation as temporal-doorway
- §1 annotation: M3 has two rendering surfaces — clock-wheel AND graph view; both on the 0 side; both consume same canonical Neo4j substrate

### M4'-SPEC

From `alpha_quaternionic_integration_across_M_stack.md` §7.5:
- §7 upgrade: three-quaternion composition `Q_identity (#4.0) · Q_transit (#4.1) · Q_activity (#4.2)`
- §7 addition: bioquaternion `(q_b, q_p)` as T²_Mahāmāya decomposition
- §7 addition: (p, q) torus-knot classification carried as M4-3 phase-history telemetry
- §7 addition: Hopf identity-trajectory on S²
- New §: Diamond-QL Vitruvian rendering as user's lived self-image at #4.4.4.4 (link to `m4-prime-psychoid-cymatic-field-engine.md`)
- No personal-137 mapping at M4-0 (clarification, not addition)

From `m4-prime-psychoid-cymatic-field-engine.md` §16.1:
- §7 addition: personal-quaternion at M4-4-4-4 rendered as **psychoid cymatic field** (not avatar/figure/dashboard); local-protected; never cosmic-facing
- New §: temporal-processual reading layer via Graphiti + Chronos/Kairos + history-directory; daily/weekly/period readings as trajectory through psychoid field
- New §: pointer to the full M4'-depth spec at `m4-prime-psychoid-cymatic-field-engine.md`

### M5'-SPEC

From `alpha_quaternionic_integration_across_M_stack.md` §7.6:
- Brief annotation: system's recognition of 137 = 64 + 72 + 1 as integer-psychoid-skeleton is pedagogical-philosophical fact reviewable in the canon; the Pauli-Jung psychoid speculation finds operational form in M-stack architecture
- Add: Jiva-is-Śiva recognition at M4.5 as the contemplative-architectural punchline of the 4+2 depth surface

### M'-SYSTEM-SPEC

From `m4-prime-psychoid-cymatic-field-engine.md` §19.5:
- §0/1 layout discipline: 0 side = Mahāmāya graph view + clock-wheel; 1 side = psychoid cymatic field + flow surface; 4+2 depth = both together with canonical bimba map as architectural-knowledge backdrop enabling Jiva-is-Śiva recognition

### M'-TAURI-PORT-SPEC

From `m4-prime-psychoid-cymatic-field-engine.md` §19.5:
- Annotation that the Tauri-v2 main app structure mirrors the 0/1 layout discipline
- The existing `BimbaMap2D.tsx` / `BimbaMap3D.tsx` stubs at `Body/M/epi-tauri/src/domains/M0_Anuttara/` are the 0-side graph affordance to be extended per §18.10

### S2' (or S2 graph-services)

From `m4-prime-psychoid-cymatic-field-engine.md` §19.3:
- New §: neosemantics integration plan (`epi:` namespace OWL ontology, relation-type characteristics, class hierarchies, OWL2 RL reasoning, optional CIDOC-CRM/FOAF/SKOS/PROV-O alignment)
- New §: Graph Data Science (GDS) plan (FastRP + Personalised PageRank + Node similarity + Louvain + K-NN over `c_5_embedding`)
- New §: two-phase GDS projection contract (Option 1 baseline + Option 3 enrichment; both spec'd for build choice)
- New §: Möbius/Klein topology handling (Approach A baseline, Approach B/C deferred)
- New §: C kernel ↔ Neo4j synchronisation contract (single source of truth = C kernel for 65 core relations; build-time sync; runtime audit; versioning)

If `Idea/Bimba/Seeds/S/S2'` doesn't exist as a spec, create it; otherwise extend.

### S3-S3i-GATEWAY

From `alpha_quaternionic_integration_across_M_stack.md` §11 and `m4-prime-psychoid-cymatic-field-engine.md` §16.4, §19.4:
- Complete native-WebSocket subscription path (first SpaceTimeDB milestone)
- Add new tables to `epi-spacetime-module/src/lib.rs`: `world_clock`, `pratibimba_presence` (RLS-filtered), `shared_archetype_event` (opt-in), `coincidence` (RLS-filtered to participants)
- Add reducers: `advance_world_clock` (scheduled 1 Hz), `bind_pratibimba_presence`, `detect_coincidences` (scheduled 1/min)
- Quaternionic identity signature reframe: `quintessence_hash = BLAKE3(canonical_bytes(q_Nara) ‖ caps)`; daily rotation = quaternion multiplication; transit_signature = Hopf projection to S² grid quantization
- Gateway shares WebSocket surface with SpaceTimeDB Universal NOW plane (per `alpha_quaternionic_integration_across_M_stack.md` §11.3)

---

## Quality criteria

When you're done, verify:

1. **Every delta in the plan files' delta tables has been applied** to the canonical spec. Cross-check by reading the delta tables and grep'ing the target specs for the delta-content.
2. **No structural rewrites** — diff each updated spec against its prior version; the diffs should be additions, annotations, and subsection insertions, not bulk replacements.
3. **Frontmatter dates updated** to today; `depends_on` lists include new plan-file references where relevant.
4. **Cross-spec coherence preserved** — read each updated spec end-to-end after updating; no contradictions with adjacent specs.
5. **Readiness criteria coverage** — new structural laws introduced by deltas have new readiness criteria added.
6. **Progress entries in the TEMP file** for each coord updated.

---

## What NOT to do

- Do NOT rewrite the canonical specs wholesale; surgical edits only
- Do NOT touch implementation code in `Body/` — this pass is spec-only
- Do NOT invent new architecture not present in the plan files; absorb what's there
- Do NOT collapse the plan files' working-paper register into the canonical specs' authority register without proper voice-shift; the plan files explore, the canonical specs decide
- Do NOT skip the M0/Anuttara/S2'/SpaceTimeDB deltas — these are load-bearing for the full integration even though they're at coordinates further from the originating M3/M4 work
- Do NOT silently resolve cross-spec contradictions; flag them as open questions for review

---

## Open questions to flag (not to resolve)

The plan files leave some architectural choices open for the user to decide:

1. **GDS projection model**: Option 1 (two-phase with local centroid) vs Option 3 (episode-as-property aggregation) — both spec'd, choice deferred. Flag as decision-needed in the S2' spec.
2. **Möbius/Klein GDS handling**: Approach A (coord-duplication preprocessing) vs Approach B (custom walks) vs Approach C (two embeddings per node) — Approach A as baseline; flag the production-level choice as decision-needed.
3. **Simulation register at the cymatic engine**: Option F (full physics simulation) is the target; Option S (stylised cymatic-inspired) is the baseline fallback. Both built; runtime switching; flag the device-capability-thresholds as decision-needed.
4. **Vāma Śaktis branch extension scope**: the QL unit is one entry-point; the full branch (Yoginīs, Mātṛkās, Kuleśvarīs, bīja-mantras, divinity-correspondences) is future work. Flag in M4' spec as future-extension placeholder.
5. **City-scape rendering register at 4+2 depth**: literal architectural rendering vs stylised geometric abstraction? Plan files lean toward stylised; flag as decision-needed for the Tauri implementation pass.
6. **The 0-side affordance unification**: graph view (M0' affordance) + clock-wheel (M3' affordance) co-resident on 0 side, or alternating with user-toggle, or composed as graph-overlaying-clock? Plan files imply co-resident; flag for UX design pass.

---

## Companion specs flagged for future production (per §19.6)

These are NEW specs to be produced after the spec-update pass completes. Do NOT produce them in this pass; flag as the next work:

- `m4-prime-psychoid-cymatic-engine-implementation.md` — detailed shader / particle / audio implementation
- `m4-prime-vama-shaktis-branch-extension.md` — full mantric-divinitive recognition-vocabulary
- `m4-prime-temporal-reading-protocols.md` — daily/weekly/lunar/solar-arc/period reading protocols
- `s2-prime-graph-data-science-and-neosemantics.md` — full GDS + n10s build spec
- `m0-prime-graph-view-and-anuttara-inferential-surface.md` — full M0' graph view spec

---

## Deliverables

When the spec-update pass is complete, produce:

1. The 9 updated canonical M' / S' specs (per delta map above)
2. A brief progress summary at the bottom of `TEMP-mahamaya-clock-aural-field-integration.md` listing each coord updated with one-line summary
3. A list of open questions encountered during the pass (additional to the six flagged above)
4. A list of cross-spec contradictions flagged (if any)

That completes the spec-update tranche. The next work (after user review) is the companion-spec production per §19.6 above, then the implementation tranches in `m4-prime-psychoid-cymatic-field-engine.md` §15.

---

## Final discipline

This is canon-update work. The plan files are exploratory; the canonical M' specs are the system's structural authority. Approach this pass as a *crystalliser* — taking the working-paper material and depositing it into canonical form, with the discipline that what you write into the M' specs becomes the system's stated architecture going forward.

Surgical. Faithful. Sufficient. Done.
