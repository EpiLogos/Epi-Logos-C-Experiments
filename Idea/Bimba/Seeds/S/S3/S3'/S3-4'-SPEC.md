---
coordinate: "S3.4'"
c_4_artifact_role: "shard-spec"
c_1_ct_type: "CT1"
c_0_source_coordinates:
  - "[[S3'-SPEC]]"
  - "[[S3-SPEC]]"
  - "[[S3-SHARD-INDEX]]"
  - "[[S3'-TRACEABILITY-INDEX]]"
  - "[[S-SHARD-HARMONIZATION-PROTOCOL]]"
  - "[[ARCHITECTURE-DIAGRAM-PACK]]"
---

# S3.4' Shard: Chronos Temporal Law

## Canonical Role

[[S3.4']] is the [[P4']] / [[CT4]] temporal law of [[S3']]: [[Chronos]] as runtime context, [[Day]] / [[NOW]] / [[Kairos]] orientation, safe kernel temporal projection, Redis temporal key law, session arc timing, and presence/current-state publication. It is temporal fact, not agent orchestration.

## Source And Diagram Anchors

Read [[S3'-SPEC]], [[S3-SPEC]], [[S3'-TRACEABILITY-INDEX]], [[S3-S3i-GATEWAY]], [[S3']], [[Chronos]], and the S3' MOC canvas. Diagram anchors are [[ARCHITECTURE-DIAGRAM-PACK#Diagram 2 S S Deep Structure]], [[ARCHITECTURE-DIAGRAM-PACK#Diagram 4 Cross-System Coupling]], and [[ARCHITECTURE-DIAGRAM-PACK#Diagram 5 Implementation Reality vs Canon]]. Legacy anchors include [[2026-04-04-graphiti-unified-temporal-context-service]], [[2026-03-23-nara-clock-canonical-runtime-implementation-plan]], [[2026-03-10-nara-runtime-full-plan]], and historical [[S3-3']] Chronos/Kairos API material.

## Current Body Reality

`Body/S/S3/redis-context/src/lib.rs` fixes S3 temporal Redis ownership and namespaces. `Body/S/S3/epi-spacetime-module/src/lib.rs` carries `KairosSurface`, `GlobalTemporalSurface`, `WorldClock`, `WorldClockTick`, reducer ABI versions, and safe projection tables. `Body/S/S3/gateway-contract/src/lib.rs` exposes `SPACETIME_CLOCK_PROTOCOL_VERSION`, `SPACETIME_PROJECTION_SCHEMA_VERSION`, `s3'.kernel.envelope.publish`, and temporal subscription methods. The live CLI/RPC temporal context bridge remains in S0 gateway code, but it delegates toward S3/S3' contracts.

## Build Contract

Temporal context must publish safe facts: day wikilinks, NOW path/wikilink, history archive path, Redis context keys, Graphiti namespace/arc references, Kairos snapshot id, and safe kernel projection. It must not publish raw [[PASU]], birth data, journal text, raw Graphiti body, private profile masks, or protected personal graph facts. Clock state is public-current only when derived from shared kernel time or opt-in shared surfaces.

## API / Envelope / Implementation Hooks

APIs include `s3'.temporal.context`, `s3'.kernel.envelope.publish`, `s3'.temporal.subscribe`, `s3'.spacetime.subscribe`, `bind_kairos_surface`, `bind_global_temporal_surface`, `advance_world_clock`, and Redis keys under `s3:gateway:temporal`.

## Test Obligations

Redis namespace tests, SpaceTimeDB schema/reducer tests, and temporal gateway tests must prove safe projection, version agreement, Redis separation, and no private identity leakage. Kernel envelope tests must verify only safe-public-current fields are projected.

## Open Gaps

Chronos is also named as an [[S4.3']] ta-onta carrier. This shard must keep the distinction clear: [[S3.4']] owns temporal runtime facts; [[S4.3']] owns carrier/tool grammar through [[ta-onta]].

## Boundaries

[[S3.4']] owns temporal projection law. [[S1']] owns vault materialisation, [[S2]] owns graph/cache semantics, [[S4.3']] names the Chronos carrier, [[S5']] owns reflective temporal meaning, and [[Nara]] consumes only governed personal surfaces.
