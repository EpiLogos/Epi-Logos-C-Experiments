# AGENTS.md — Map / M0 Anuttara

## Purpose
Generated pithy index of the M0 Anuttara graph branch — a Neo4j→repo *reflection* (Track 45), not crystallised canon. Subsystem 0 of the Epi-Logos six-fold architecture, functioning as the absolute foundational ground and transcendent source from which all other subsystems emerge. Anuttara represents the absolute ground of the Epi-Logos meta-structure, fu…
Canon: [[M0]] (World Form) -> [[M-SYSTEM-INDEX]]; design: [[45-bimba-map-indexing-and-dox-okf-unification]].

## Ownership
- Pithy `map-index` nodes for M0 descendants, tree-depth ≤ 3 (107 nodes). **The folder tree mirrors the children relation** (containment edges, e.g. HAS_INTERNAL_COMPONENT) — a coord with children is a folder + `{coord}.md` folder-note. Each node = content detail + `Contains` + a full `Relations` index + a Neo4j pointer.
- Context-frame coords (e.g. `M0-(0/1)`) render `/` as `∕` in file/link names; `coordinate:` + the graph pointer keep the true `/`. Nest under their containment parent (e.g. `M0-(4.0/1)` lives under `M0-4`).
- Does NOT own canon meaning — that is the [[M0]] Form + its [[Seeds]] specs. Full detail lives in Neo4j + `datasets/anuttara-deep/` (legacy `#`-tagged source).

## Local Contracts
- Projector: `Idea/Bimba/Map/datasets/scripts/project-map-index.mjs`. Source: `datasets/anuttara-deep/` (nodes + relations). Graph: `neo4j://Bimba/M0`.

## Work Guidance
- Generated — do NOT hand-edit nodes; change the graph/source and re-run the projector.
- `[[wikilink]]` is the navigation contract; every coordinate node is a resolvable page.
- Canonical enrichment (never overwrite): [[anuttara-language-map]].

## Verification
- `bimba-vault-validate` over this directory (map-index role, Map/** residency).

## Child DOX Index (= OKF concept index)
- Entry coordinates: [[M0-0]] · [[M0-1]] · [[M0-2]] · [[M0-3]] · [[M0-4]] · [[M0-5]]
