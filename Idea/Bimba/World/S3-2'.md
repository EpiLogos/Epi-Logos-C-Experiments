---
coordinate: "#S3.2'"
name: "Pleroma (Skills) API"
parent: "#S3'"
ql_position: 2
type: "api_sublayer"
constitutional: "SPARSHA - Capabilities touch ground"
---
# S3.2': Pleroma API (Skills & Commands)

> **QL Position**: #2 - Operation/Process
> **Parent**: S3' (Infrastructure API)
> **Greek Source**: Gnostic fullness - Complete collection

## Description

Skill library, command invocation, template invocation - Pleroma provides the fullness of all capabilities.

## Core Operations

```python
class PleromaAPI:
    def invoke_skill(skill_name: str, args: dict) -> SkillResult
    def invoke_command(command_name: str) -> CommandResult
    def invoke_template(template_name: str, context: dict) -> TemplateResult
    def list_skills() -> List[SkillInfo]
    def list_commands() -> List[CommandInfo]
    def list_templates() -> List[TemplateInfo]
    def get_capability(name: str) -> Optional[Capability]
```

## Components

- **Skills**: Atomic composables
- **Commands**: Orchestrated workflows
- **Templates**: Frozen processes (CT')
- **Capability Registry**: All available operations

## Related

- Parent: [[S3']]
- Coordinate: [[C0-P2-M0-S3-L2]]
