---
coordinate: "#S0.2'"
name: "Stream API"
parent: "#S0'"
ql_position: 2
type: "api_sublayer"
---
# S0.2': Stream API

> **QL Position**: #2 - Operation/Process
> **Parent**: S0' (Type API)

## Description

API for stream transformation - pipes, filters, and data flow operations.

## Operations

```python
class StreamAPI:
    def pipe(input_stream: Stream, *filters) -> Stream
    def grep(pattern: str, input_stream: Stream) -> Stream
    def sed(script: str, input_stream: Stream) -> Stream
    def awk(program: str, input_stream: Stream) -> Stream
    def jq(query: str, input_stream: Stream) -> Stream
    def sort(input_stream: Stream) -> Stream
    def uniq(input_stream: Stream) -> Stream
    def wc(input_stream: Stream) -> int
```

## Related

- Parent: [[S0']]
- Coordinate: [[C0-P2-M0-S0-L2]]
