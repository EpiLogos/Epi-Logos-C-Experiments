---
coordinate: "#S0.1'"
name: "Command API"
parent: "#S0'"
ql_position: 1
type: "api_sublayer"
---
# S0.1': Command API

> **QL Position**: #1 - Definition/Material
> **Parent**: S0' (Type API)

## Description

API for atomic command execution - the material atoms of terminal interaction.

## Operations

```python
class CommandAPI:
    def execute_command(cmd: str, args: List[str]) -> CommandResult
    def cat(path: str) -> str
    def echo(text: str) -> None
    def ls(path: str = ".") -> List[str]
    def read_line() -> str
    def cd(path: str) -> bool
    def pwd() -> str
    def mkdir(path: str, parents: bool = False) -> bool
    def rm(path: str, recursive: bool = False) -> bool
```

## Related

- Parent: [[S0']]
- Coordinate: [[C0-P1-M0-S0-L1]]
