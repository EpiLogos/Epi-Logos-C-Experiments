---
coordinate: "#S0.5'"
name: "Package API"
parent: "#S0'"
ql_position: 5
type: "api_sublayer"
---
# S0.5': Package API

> **QL Position**: #5 - Integration/Synthesis
> **Parent**: S0' (Type API)

## Description

API for ecosystem package integration - npm, pip, apt, brew, cargo, gem.

## Operations

```python
class PackageAPI:
    def npm_install(package: str, global: bool = False) -> InstallResult
    def pip_install(package: str, user: bool = False) -> InstallResult
    def apt_install(package: str) -> InstallResult
    def brew_install(package: str, cask: bool = False) -> InstallResult
    def cargo_install(package: str) -> InstallResult
    def gem_install(package: str) -> InstallResult
    def get_path() -> List[str]
    def which(command: str) -> Optional[str]
    def list_installed(manager: PackageManager) -> List[Package]
    def update_packages(manager: PackageManager) -> UpdateResult
```

## Related

- Parent: [[S0']]
- Coordinate: [[C0-P5-M0-S0-L5]]
