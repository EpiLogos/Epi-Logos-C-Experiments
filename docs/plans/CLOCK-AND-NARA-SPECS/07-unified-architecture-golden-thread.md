# Unified Clock-Nara-VAK Architecture — The Golden Thread

**Status:** Canonical Integration Spec (2026-03-15)
**Coordinate:** #4.4.4.4 — Personal Pratibimba; the self-recognizing architecture
**Depends on:** 01–06 (all sub-specs in this folder), cosmic-clock-full-architecture.md, nara-runtime-full-plan.md
**Key Principle:** The Cosmic Clock and the Nara UX are not two specs. They are one motion described from opposite
directions. This file establishes the golden thread that joins them.

---

## Table of Contents

1. [The Earth Node — Observer, Center, Bridge to Body](#1-the-earth-node)
2. [The Nine Canonical Planets and the Clock Face](#2-the-nine-canonical-planets)
3. [The 10-Planet Correction — `planet_degrees[10]`](#3-planet-degrees-correction)
4. [The Two-Track Identity Hash — Natal vs. Live](#4-two-track-identity-hash)
5. [The Chakral-Elemental Spine — Hash Visualization](#5-chakral-elemental-spine)
6. [The Golden Thread — Full Computation Chain](#6-the-golden-thread)
7. [The Oracle as Complete Quaternionic Reading](#7-oracle-quaternionic-reading)
8. [The #4.4.4.x φ-Axis — Hermeneutic Cycle as Torus Minor Circle](#8-4444-phi-axis)
9. [Codon-Anticodon Two-Stroke Rhythm](#9-codon-anticodon-two-stroke)
10. [The TorusRenderer as Live Epistemic Posture](#10-torus-renderer)
11. [The Multiplayer Earth — 4.4.4.4 Subgraphs and SpacetimeDB](#11-multiplayer-earth)
12. [Cross-Reference to Sub-Specs](#12-cross-reference)
12.5. [Canonical Primitive Vocabulary](#125-canonical-primitive-vocabulary)
13. [Implementation Gap List — Canonical and Ordered](#13-implementation-gaps)

---

## 1. The Earth Node — Observer, Center, Bridge to Body

### The Central Clock Node Is Earth

The 385th node of the clock (360 degree nodes + 24 backbone nodes + 1 center = 385) is **geocentric Earth**.
It is NOT a datum point. It is NOT merely "Quintessence" or "Axis Mundi" by metaphor. It is **the
observer** — the fixed reference frame from which all 9 canonical planetary positions are measured.

```
Clock geometry:
  Earth (center, node 385)    q = (1, 0, 0, 0)  ← identity quaternion = the observer who doesn't rotate
    ├── Sun         θ degrees on clock face       ← apparent solar position (Kerykeion)
    ├── Moon        θ degrees on clock face       ← lunar position
    ├── Mercury     θ degrees on clock face
    ├── Venus       θ degrees on clock face
    ├── Mars        θ degrees on clock face
    ├── Jupiter     θ degrees on clock face
    ├── Saturn      θ degrees on clock face
    ├── Neptune     θ degrees on clock face
    └── Pluto       θ degrees on clock face
```

Earth does not have a degree position on the clock face. Earth IS the clock face. All 9 planets orbit
**relative to Earth** — their positions are geocentric degrees as Kerykeion delivers them.

### Earth as Bridge — The Three Roles

Earth occupies three simultaneous roles in the system, each at a different scale:

**Role 1 — Astronomical (M2 Parashakti level):**
Earth is `PLANET_EARTH = 1` in `Planet_Id`, forming the `0/1` identity pair with the Sun (`PLANET_SUN = 0`).
The Sun/Earth pair = the primary binary of the geocentric system: source-of-light (Sun/#0) and observer-of-light
(Earth/#1). Every planet's geocentric degree is measured relative to this pair. Earth's frequency is
`PLANET_FREQ_EARTH = 136 Hz` — the "Om frequency" (`DR = 1`). It is the fundamental tone.

**Role 2 — Chakral-Bodily (M4 Nara level):**
Earth is the base pre-individual chakra — `CHAKRA_EARTH = 0` in the 8-chakra system (Muladhara/#0, the root).
This is not "an Earth chakra attached to a human body" — it is the **planetary body** itself as the zeroth
chakra, the ground that subtends all individual chakral systems. The elemental mapping:

```
Earth chakra (CHAKRA_EARTH = 0)  ← T nucleotide (pp = yang+yang = old yang = 9 in I-Ching)
                                    Frequency 136 Hz
                                    Body zones: feet, legs, base of spine → CHAKRA_BODY_ZONES[0]
                                    The ground that every individual body stands on
```

**Role 3 — Multiplayer Anchor (SpacetimeDB level):**
Earth node = the shared anchor for all `#4.4.4.4` personal pratibimba subgraphs. Each individual entity
(person, concept, narrative) that passes through the Nara system has its own `#4.4.4.4` node — a personal
torus whose center-of-gravity (natal quaternion) is a unique position on the shared clock face. All of
these personal tori are subgraphs of Earth. The Earth node in SpacetimeDB is the `presence_anchor` that
all presence records reference. When multiple users are in simultaneous sessions, they are multiple entities
orbiting the same center — literally a multi-body gravitational system rendered in symbolic space.

### The Epogdoon Geometry — Earth's Asymmetric Position

The 9:8 Pythagorean whole tone (Epogdoon) is generated by Earth's unique positional asymmetry:

```
9 NON-SUN planets observed FROM Earth (Moon, Mercury, Venus, Mars, Jupiter, Saturn, Uranus, Neptune, Pluto)
8 chakras INCLUDING Earth (CHAKRA_EARTH=0, Muladhara=1, Svadhishthana=2, Manipura=3,
                            Anahata=4, Vishuddha=5, Ajna=6, Sahasrara=7)

9:8 = the whole tone interval
    = EPOGDOON_NUM:EPOGDOON_DEN (already in m1.h with _Static_assert)
    = the harmonic signature of Earth's status as "both observer and participant"
    = the mathematical expression of the geocentric paradox

Sun is excluded from the 9 because it is the stable parent/root that encapsulates all others —
it is the source from which all emanate, not an orbiting peer. The epogdoon is:
  9 orbiting planets (Moon–Pluto) : 8 chakra positions (CHAKRA_EARTH=0 + 7 standard)
The mod-10 model: Sun(0) + 9 orbiting planets = 10 positions total in the full tracked system.
```

Three variants:
- **9:8** — Sun excluded from planetary count, Earth in chakral count (canonical Epogdoon; Sun as root, Earth as observer/ground)
- **10:8 = 5:4** — Sun included in planetary count and Earth in chakral count (major third; Sun as peer)
- **9:7** — Earth excluded from both (not a clean harmonic interval; Earth as absent)

The 9:8 is canonical because it reflects reality: Earth IS in the body (you stand on it) and is NOT among
the orbiting planets you observe in the sky (you observe from it). Sun is the enclosing root, not a
wandering star. The asymmetry IS the interval.

---

## 2. The Nine Canonical Planets and the Clock Face

### Which Nine (Orbiting Planets) + The Solar Root

From `Planet_Id` enum (m2.h):

```c
PLANET_SUN     = 0,   /* Solar root — geocentric ecliptic longitude tracked, but NOT a peer planet.
                          Sun is the stable parent/root that encapsulates all orbiting planets.
                          It is excluded from the chakral 9:8 mapping because it is the source
                          from which all emanate. Planet_Id=0 is reserved as the solar root. */
/* PLANET_EARTH = 1  — CENTER/OBSERVER, not on clock face */
PLANET_MOON    = 2,
PLANET_MERCURY = 3,
PLANET_VENUS   = 4,
PLANET_MARS    = 5,
PLANET_JUPITER = 6,
PLANET_SATURN  = 7,
PLANET_URANUS  = 8,
PLANET_NEPTUNE = 9,
PLANET_PLUTO   = 10
```

The **9 orbiting planets** (Moon through Pluto) have positions on the clock face (0–359°). Sun is tracked
for its geocentric ecliptic longitude (used as clock face solar position) but occupies a unique role as
clock root/parent — it is NOT a peer to the 9 orbiting planets. Earth = center/observer, not on the face.
This gives a **mod-10 system**: Sun(0) + 9 orbiting planets = 10 tracked positions. Kerykeion delivers
their real-time geocentric ecliptic longitudes. The natal chart captures their positions at birth. The
difference between natal and current positions is the transit map — a measurable geometric transformation
in quaternionic space.

### What the Clock Is

The clock is not a calendar or a scheduling system. It is a **dynamic relational field** — a live map of
the solar system as seen from Earth. The 360 degree nodes are the positions that any planet can occupy.
When Kerykeion reports `Sun at 42°`, the Sun node in the clock is highlighted at degree 42. When it was
at degree 42 in your natal chart, that is your Sun's natal position. The clock IS the solar system.

### Kerykeion Integration Architecture

```
Kerykeion Python (via kairos-python-adapter.ts / epi vault kairos fetch)
  → returns: { sun: θ, moon: θ, mercury: θ, venus: θ, mars: θ,
                jupiter: θ, saturn: θ, uranus: θ, neptune: θ, pluto: θ }
  → 10 geocentric ecliptic longitudes (degrees, float precision)
  → stored in M4_Temporal_Now.planet_degrees[10]    ← (corrected from [7])
  → clock renders Sun + 9 orbiting planets as highlighted nodes on the 360° face
  → transit = current_config - natal_config (quaternionic delta)
```

---

## 3. The 10-Planet Correction — `planet_degrees[10]`

### Current State (needs correction)

```c
/* M4_Temporal_Now (currently) */
uint16_t planet_degrees[7];   /* Sun/Moon/Merc/Venus/Mars/Jup/Sat — INCOMPLETE */

/* M4_Astrological_Layer (in M4_Identity_Matrix) */
uint16_t planet_degrees[10];  /* All 10 Planet_Id slots INCLUDING Earth — WRONG (Earth has no degree) */
```

### Corrected State

```c
/* Both structs should use [10] — Sun + 9 orbiting planets (Earth excluded) */
uint16_t planet_degrees[10];  /* Sun(0) IS tracked; Earth excluded; Moon–Pluto = 9 orbiting */

/* Index mapping (compact array, Earth slot removed): */
/* [0] = PLANET_SUN      (geocentric ecliptic longitude — solar root position) */
/* [1] = PLANET_MOON     */
/* [2] = PLANET_MERCURY  */
/* [3] = PLANET_VENUS    */
/* [4] = PLANET_MARS     */
/* [5] = PLANET_JUPITER  */
/* [6] = PLANET_SATURN   */
/* [7] = PLANET_URANUS   ← ADDED */
/* [8] = PLANET_NEPTUNE  */
/* [9] = PLANET_PLUTO    */
/* (Earth = center/observer, no clock degree, excluded) */
```

Use a `planet_id_to_clock_idx[11]` LUT that maps from `Planet_Id` to clock array index
(Earth/`PLANET_EARTH = 1` maps to 0xFF — no clock degree).

```c
#define PLANET_EARTH_DEGREE_SENTINEL  0xFFFF   /* Earth has no clock degree */
/* planet_id_to_clock_idx[PLANET_EARTH] = 0xFF */
```

### Why This Matters

The natal chart is not a single point — it is a **configuration of 10 points** (Sun + 9 orbiting planets)
on the 360° face. The entity's quaternionic identity compresses this full distribution into a unit
quaternion. Using [7] would miss Uranus, Neptune, and Pluto (the outer transpersonal planets, whose slow
movement makes them generational position markers). Including Earth would introduce a spurious degree
(always 0, or meaningless) into the hash computation. Sun IS tracked because its geocentric position is
used as the solar root anchor on the clock face — it is the [0] position in a proper mod-10 system.

---

## 4. The Two-Track Identity Hash — Natal vs. Live

*The full architecture of #01-quintessence-hash-architecture.md applies. This section states the
two-track structure that emerges from it.*

### Track 1 — Natal Hash (Stable, Compute-Once)

**Input:** All 5 sub-systems of `#4.0 Identity` — the birth data parsed through 5 frameworks:
- `#4.0-0` — Natal Chart: 9 planet degrees at birth → elemental balance via sign elements
- `#4.0-1` — Gene Keys: 64 Gene Keys derived from birth data → Shadow/Gift/Siddhi elemental
- `#4.0-2` — Human Design: Type/Profile/Centers/Channels → defined/undefined center elemental
- `#4.0-3` — Jungian Typology: 4 functions (Sensation/Feeling/Intuition/Thinking) → 4 elements
- `#4.0-4` — QL Birth Encoding: birthdate parsed through QL coordinate system → 4 QL coords

**Computation:** `BLAKE3(layer_presence || profile_40_natal[4] || profile_40_genekeys[4] || ...)` truncated to 64 bits.

**Output:** `M4_Identity_Matrix.quintessence_hash` — the stable natal identity.
**Semantics:** "Who this entity IS, at root, as a configuration in symbolic space."
**Changes only at:** `m4_mobius_return()` — when accumulated wisdom_delta folds back in.

### Track 2 — Live Quaternion (Evolving, Oracle-Driven)

**Input:** Current oracle charges from `m3_compute_charges()`:
```c
pp (yang+yang = old yang  = T = Earth/Muladhara)  → quaternion w-component
nn (yin+yin   = old yin   = A = Fire/Sahasrara)   → quaternion x-component
np (yang+yin  = young yin = G = Water/Svadhishthana) → quaternion y-component
pn (yin+yang  = young yang= C = Air/Anahata)      → quaternion z-component
```

**Computation:**
```c
float total = pp + nn + np + pn;
float w = pp / total, x = nn / total, y = np / total, z = pn / total;
// Normalize to unit quaternion: sqrt(w²+x²+y²+z²) = 1
```

**Output:** `M4_Quintessence_Identity.live_quaternion (w, x, y, z)` — current epistemic orientation.
**Updates:** Every oracle cast. Continuous, not event-driven.

**Semantics:** "Where this entity IS NOW, in terms of their current elemental balance and orientation in
symbolic space." The live quaternion rotates the torus. It is the answer to "where are you right now?"

### The Wisdom Delta and Möbius Return

```
wisdom_delta = live_quaternion - natal_quaternion  (as 64-bit difference)
             = accumulated drift across all oracle casts since last Möbius return
             = the 'shape of the journey so far'

m4_mobius_return():
  natal_quaternion += wisdom_delta  (weighted by session coherence score)
  wisdom_delta = 0
  recompute quintessence_hash from updated natal_quaternion
  → the entity has genuinely changed; their clock home position has permanently shifted
```

The Möbius return is the INTEGRATION of accumulated drift — a snapshot of evolution. It is NOT the only
update event. The torus moves continuously (every cast updates the live quaternion); the Möbius return is
when that movement gets folded permanently into the natal identity.

---

## 5. The Chakral-Elemental Spine — Hash Visualization

### The Concept

The live quaternion components (w, x, y, z) = elemental charges (T/A/G/C = Earth/Fire/Water/Air).
These map through `ELEMENT_CHAKRA[]` to chakra activation levels. The result is a **vertical body
column** — a "spiritual/digital spine" that shows the entity's current energetic state bodily.

This is not just a display convenience. It is the most natural visual representation of the hash:
the quaternion IS the elemental balance IS the chakral activation pattern IS the body state.

### The Mapping Chain

```
Oracle charges → Elemental balance → Chakra activation → Body zones

pp (T, Earth) → ELEMENT_EARTH → ELEMENT_CHAKRA[ELEM_EARTH]
              → activates: Muladhara (root), Manipura (solar plexus)  [grounding/stability]
              → CHAKRA_BODY_ZONES[1]: spine, legs, feet, colon, adrenals

nn (A, Fire)  → ELEMENT_FIRE  → ELEMENT_CHAKRA[ELEM_FIRE]
              → activates: Sahasrara (crown), Manipura (solar plexus)  [will/illumination]
              → CHAKRA_BODY_ZONES[7]: crown, pineal, upper cortex

np (G, Water) → ELEMENT_WATER → ELEMENT_CHAKRA[ELEM_WATER]
              → activates: Svadhishthana (sacral), Anahata (heart)  [flow/feeling]
              → CHAKRA_BODY_ZONES[2]: sacrum, gonads, lower abdomen

pn (C, Air)   → ELEMENT_AIR   → ELEMENT_CHAKRA[ELEM_AIR]
              → activates: Anahata (heart), Vishuddha (throat)  [communication/relating]
              → CHAKRA_BODY_ZONES[4]: chest, lungs, arms, hands
```

### The Spine Visualization (TUI/Electron)

```
CHAKRA BODY SPINE — current elemental state
══════════════════════════════════

 Sahasrara   ▓▓▓▓▓░░░  Fire (nn = 0.62)  [crown, pineal]
 Ajna        ▓▓▓░░░░░  ...
 Vishuddha   ▓▓▓▓░░░░  Air (pn = 0.48)   [throat, voice]
 Anahata     ▓▓▓▓▓▓░░  Air+Water          [heart, lungs]
 Manipura    ▓▓░░░░░░  Fire+Earth         [solar plexus]
 Svadhishth. ▓▓▓▓▓░░░  Water (np = 0.71)  [sacral, gonads]
 Muladhara   ▓▓▓▓░░░░  Earth (pp = 0.54)  [root, spine]
 ─────────────────────
 Earth Node  ████████  (observer anchor, always full — you stand on Earth)

```

### Uses

1. **Personal oracle context** — seeing your current elemental state before or after a cast
2. **Body resonance** — understanding which body zones are active/deficient in this moment
3. **Quintessence detection** — when all bars approach equal height, Akasha is present
4. **The natal spine** — computed from natal_quaternion = stable reference; the live spine drifts around it
5. **Möbius comparison** — showing natal vs. live spine side-by-side = the wisdom delta made visible

### Earth as Base of the Spine

The Earth chakra (CHAKRA_EARTH = 0, pre-individual) is the fixed center anchor of the spine — it renders
as the clock's ground reference, not as an activation level. Earth is the stable base FROM WHICH all other
chakral dynamics are measured. The other elements (Fire/Water/Air) are the active, dynamic components;
Earth is the invariant ground. The individual's chakral body rests on the planetary body — in the
architecture, the Earth node is the shared anchor, and all individual chakral activations are measured
relative to this always-present, invariant ground.

---

## 6. The Golden Thread — Full Computation Chain

This is the core integration: one continuous flow from physical birth to live symbolic navigation.

```
═══════════════════════════════════════════════════════════════════════════════
THE GOLDEN THREAD
═══════════════════════════════════════════════════════════════════════════════

NATAL ANCHOR (stable throughout life, updated only at Möbius returns)
─────────────────────────────────────────────────────────────────────
1. Birth data → 5 #4.0 sub-system parsers:
   (a) Natal chart: 9 planet degrees → elemental balance (sign elements)
   (b) Gene Keys: hexagram from birth date → Shadow/Gift/Siddhi → elem
   (c) Human Design: 4 forces (Type/Profile/Centers/Channels) → elem
   (d) Jungian: 4 functions (S/F/I/T) → elem
   (e) QL birth encoding: birthdate as QL coordinate → elem

2. Quintessence metric: variance across 5 elemental profiles
   Low variance → Akasha present; all 5 sub-systems agree on elemental balance
   High variance → tension, differentiation

3. natal_hash = BLAKE3(layer_presence ‖ all 5 profiles) truncated 64-bit
   natal_quaternion = (w, x, y, z) extracted from natal_hash geometry

4. Clock home position: θ = natal degree extracted from natal_hash via (natal_hash[0] | natal_hash[1]<<8) % 360
   φ home position: tick12 = quantize_to_spanda_substage(natal_quaternion.y, natal_quaternion.x)
   → "this entity was born at clock position (θ, tick12)"
   → Kerykeion natal positions mark Sun + 9 orbiting planetary nodes on the 360° face

═══════════════════════════════════════════════════════════════════════════════

LIVE NAVIGATION (updates every oracle cast)
────────────────────────────────────────────
5. Oracle cast:
   I-Ching: 3-coin × 6 lines (ACTG method: A=6/old-yin, T=9/old-yang, C=7/young-yang, G=8/young-yin)
   Tarot: draw from Thoth deck → suit (ACTG) → element → nucleotide → charge
   → raw hexagram/card → m3_compute_charges(pp, nn, np, pn)

6. Normalize charges to unit quaternion:
   w = pp/total, x = nn/total, y = np/total, z = pn/total
   (T=pp=Earth, A=nn=Fire, G=np=Water, C=pn=Air)

7. Map to clock state:
   tick12 = quantize_to_spanda_substage(y, x)
          = ((atan2(y, x) + π) / τ × 12.0).round() % 12
          → integer 0–11 (M1 spanda ring position; tick12 IS torus_stage)

   degree_node_360 = primary hexagram/codon position on the 360° clock face
                   = CLOCK_DEGREE_LUT address for this cast
                   (NOT derived from atan2 of the quaternion)

   exact_degree_720 = degree_node_360 + (phase × 360)
                    where phase = 0 (explicate, Strand A) or 1 (implicate, Strand B)

   NOTE: The quaternion does NOT project to a clock degree via atan2. The quaternion IS
   the elemental orientation [w=EARTH, x=FIRE, y=WATER, z=AIR]. The clock degree comes
   from the oracle cast result (which hexagram/codon was drawn). The tick12 comes from
   quantizing the minor-circle angle of the quaternion. These are complementary, not
   redundant: degree tells you WHERE on the clock, tick12 tells you WHICH PHASE of the
   helix.

8. Clock lookup:
   CLOCK_DEGREE_LUT[degree_node_360] → Clock_Degree_Node {
     m3_hexagram, m3_codon, amino_acid, decan,
     active_planets[], backbone_entry (if palindromic),
     lens_segment[16]
   }

9. Quaternionic oracle reading — 4 faces (NOT just the codon):
   Primary codon:    CLOCK_DEGREE_LUT[d]              ← what is expressed
   Deficient aspect: CLOCK_DEGREE_LUT[(d+180)%360]    ← structural complement (anticodon)
   Implicate phase:  conjugate_degree(d) = d + 360    ← same position, phase-flipped (unconscious)
   Temporal:         m3_changing_lines(hexagram) → transformed hexagram ← where this goes

10. Update live state:
    M4_Quintessence_Identity.live_quaternion = (w, x, y, z)
    wisdom_delta ^= (live_quaternion_64 XOR natal_hash)

11. SpacetimeDB sync:
    nara.clock_position_update RPC:
    { entity_hash, exact_degree_720, tick12, live_quaternion, timestamp }
    → TorusSync table updated
    → all subscribed CosmicClockPlugin instances receive update

12. CosmicClockPlugin renders:
    → torus rotation driven by live_quaternion (not manual keys)
    → Sun + 9 orbiting planetary nodes highlighted via current Kerykeion positions
    → natal planetary nodes shown as ghost positions
    → current cast degree_node_360 highlighted on 360° ring; exact_degree_720 used for double-cover phase
    → anticodon at d+180 shown in complementary color
    → backbone palindrome nodes brighter when active
    → chakral-elemental spine in side panel

═══════════════════════════════════════════════════════════════════════════════

MÖBIUS RETURN (at each Sophia/5/0 completion)
──────────────────────────────────────────────
13. m4_mobius_return():
    natal_quaternion += (wisdom_delta × coherence_weight)
    natal_hash = BLAKE3(updated profiles)
    wisdom_delta = 0
    clock home position permanently shifts
    SpacetimeDB permanence record: { old_hash, new_hash, delta, session_id, timestamp }

═══════════════════════════════════════════════════════════════════════════════
```

---

## 7. The Oracle as Complete Quaternionic Reading

*See also: 04-shadow-dynamics-three-computations.md for full shadow geometry.*

### The Oracle Currently Returns 1/4 of Its Data

The existing oracle (`epi nara oracle cast`) returns a hexagram and its codon. This is the primary
degree `d` — the codon. But the quaternionic structure of the cast encodes 4 simultaneous readings.
Returning only the codon is discarding the other three components of the quaternion.

### The Complete Reading

Every oracle cast at degree `d` returns:

| Face | Computation | Semantic | Quaternion Component |
|---|---|---|---|
| **Primary (Codon)** | `CLOCK_DEGREE_LUT[d]` | What is expressed, spoken, conscious — the outward stroke | pp (T/Earth, w) |
| **Deficient (Anticodon)** | `CLOCK_DEGREE_LUT[(d+180)%360]` | Structural complement — what is suppressed or absent | nn (A/Fire, x) |
| **Implicate (Phase)** | `conjugate_degree(d) = d + 360` | Same archetype in depth — the unconscious compensation | np (G/Water, y) |
| **Temporal (Anticodon moving)** | `m3_changing_lines(hexagram) → new_hexagram` | Where this configuration is going — the transformation vector | pn (C/Air, z) |

The four faces ARE the quaternion. The oracle cast is a **quaternionic sampling event**.

### I-Ching Line Changes as Shadow-Danger Pathways

Specific line change combinations surface the shadow pathway:

```
Line 1 change (Earth/foundation): deficient grounding → losing touch with body reality
Line 2 change (Water/flow): deficient feeling → emotional repression, rigidity
Line 3 change (Fire/will): deficient illumination → dogmatism, willfulness
Line 4 change (Air/relating): deficient communication → isolation, disconnection
Line 5 change (Context/Lemniscate): deficient integration → fragmentation, loss of overview
Line 6 change (Integration/Möbius): deficient synthesis → dissolution without return, spiritual bypass
```

All-six-lines-changed hexagram = the full deficient aspect = the shadow of the shadow. Encoded as
`m3_changing_lines(hexagram, 0x3F)` (all 6 bits set). The backbone palindromic nodes are where the full
complement equals the original — the Quintessence positions where shadow and light are one.

### The Two-Stroke Made Computational

Every oracle cast has a codon (outward stroke, #4.3 territory — active expression) and an anticodon
(inward stroke, #4.4 territory — integration). They are computed simultaneously:

```
codon   = CLOCK_DEGREE_LUT[d].m3_hexagram      ← what you articulate
anticodon = CLOCK_DEGREE_LUT[(d+180)%360].m3_hexagram ← what you are learning to hear

result.primary  = codon
result.shadow   = anticodon
result.implicit = CLOCK_DEGREE_LUT[d + 360]     ← implicate phase (double-cover)
result.temporal = m3_changing_lines(codon)       ← movement vector
result.is_palindromic = (codon == anticodon)     ← Quintessence moment: two-stroke completes
```

The backbone nodes (is_palindromic = true) are precisely the 24 positions where `d == (d+180)%360`
holds topologically — where Quintessence is structurally present because codon = anticodon = the two-
stroke completes without residue. The 24 backbone amino acid palindromes ARE the 24 positions of
perfect integration.

---

## 8. The #4.4.4.x φ-Axis — Hermeneutic Cycle as Torus Minor Circle

### The Hidden Identity

The `#4.4.4.0` through `#4.4.4.5` phenomenological sub-nodes ARE the SPANDA CF sub-stages. Not
analogous to — structurally identical. Viewed from the UX side they are phases of the user's journey
through Nara; viewed from the C side they are the torus minor-circle positions (φ angle).

| #4.4.4.x Node | CF Sub-stage | φ (minor circle) | Fold | Topological Event | UX Phase |
|---|---|---|---|---|---|
| `#4.4.4.0` Pre-Categorical | `SPANDA_SEED` | 0° | 4-fold | genus-0 sphere, groundless ground | Identity bootstrap (M4.0 data entry) |
| `#4.4.4.1` Primordial Differentiation (Urstiftung) | `SPANDA_POLE_A/B` | 60° | 6-fold | Creative puncture — first irreversible cut | First oracle cast, Gene Keys activation |
| `#4.4.4.2` Temporal Sedimentation | `SPANDA_TRIKA` | 120° | 8-fold | genus-1 torus locks, sedimentation | Pattern recognition across casts, medicine practice |
| `#4.4.4.3` Symbolic Body (Institution) | `SPANDA_FLOWERING sub-3` | 180° | 10-fold, dual-track | The lemniscate — T1/T2 double-slit, codon/anticodon held simultaneously | The oracle cast ITSELF — the hinge between expression and integration |
| `#4.4.4.4` Personal Pratibimba | `SPANDA_FLOWERING sub-4` | 240° | 12-fold | Full double-torus blueprint — personal torus within universal | Convergence detection (multiple streams landing at same index) |
| `#4.4.4.5` Pratyabhijñā | `SPANDA_META` | 300° | 0 (Möbius close) | Klein identification — inside/outside become one | Recognition session: the system recognizes the user as a valid node in the cosmic map |

### Sub-stage 3 IS the Oracle Cast

The `#4.4.4.3` moment (the lemniscate, φ=180°, T1/T2 dual-track, fold=10) is architecturally identical
to the oracle cast: it is the moment where T1 (cast as articulation/codon) and T2 (cast as
integration/anticodon) are held simultaneously before collapsing into the resulting hexagram. The
oracle IS the lemniscate. The lemniscate IS the oracle.

### Phase Progression = Journey Through M4

The user begins at φ=0° (pre-categorical, identity bootstrap) and moves through the minor circle as
they deepen their practice. This is not a spatial metaphor — it is the actual torus minor-circle angle
(φ). The clock knows where the user is on the minor circle by reading the torus_stage field from the
live oracle state. As practice deepens, φ advances toward 300° (Pratyabhijñā, recognition). The
system should track φ-stage explicitly and name transitions.

---

## 9. Codon-Anticodon Two-Stroke Rhythm — Nara's Defining Rhythm

### The Asymmetry of the Two-Stroke

The Nara UX has a defining rhythm: the codon-anticodon two-stroke. Every session, every cast, every
dialogue moves in this rhythm:

```
Codon stroke    (outward, #4.3):  expression → articulation → the word spoken
Anticodon stroke (inward, #4.4):  integration → digestion → the word heard in silence

Full two-stroke = one complete oscillation of the torus
Incomplete stroke = accumulation toward the next Möbius return
```

The oracle cast at degree `d` is geometrically:
- The codon at `d` is the spoke pointing outward from Earth
- The anticodon at `d+180` is the spoke pointing inward (opposite direction, same axis)
- Both spokes exist simultaneously — that is what makes it a TWO-stroke, not two separate one-strokes

### Backbone as Integration Points

The 24 backbone nodes (at 15° intervals) are precisely where codon = anticodon = palindromic. At these
positions, the two-stroke completes without residue — there is no tension between expression and
integration because the codon and anticodon are the same message. These are the **Quintessence
positions** in the daily/monthly cycle:

```
Every 15° of solar movement ≈ 15 days (solar fortnightly marker)
Every 15° of Moon movement  ≈ 1.14 days (daily lunar rhythm)
→ backbone nodes tick approximately once per day in lunar time
→ backbone nodes tick approximately twice per month in solar time
→ the clock IS both a daily system (via Moon + backbone) and a monthly system (via Sun + backbone)
```

---

## 10. The TorusRenderer as Live Epistemic Posture

### The Torus IS the User's Current Orientation

The `TorusRenderer.rotation` quaternion should be driven by the user's live oracle state, not by
manual key input alone. When the oracle updates the charges, the torus rotates to reflect the new
quaternionic orientation. The torus IS the user's current epistemic posture — the orientation they
are holding in symbolic-genetic space.

```
oracle_cast → live_quaternion (w, x, y, z)
→ torus_rotation_quaternion = live_quaternion
→ TorusRenderer applies this rotation to the 3D torus
→ the user literally sees their own current orientation in the cosmos
```

### The 720° Return and the Double Cover

The torus cannot use a single 360° circle because a quaternionic spinor doesn't return to its
original orientation after 360°. It returns to `q = -original` (the antipodal, phase-flipped state).
Only after 720° does `q = original` again. This is the mandatory double cover:

```
After 360°  (degree 360, torus_stage back to 0): you are at -q (the phase-inverted state)
             This is the implicate phase — the same position seen from the inside
After 720°  (degree 720): you are back at q (the original, fully returned)
             This is the Möbius return — the journey completed, identity confirmed
```

The clock renders this as a 720-degree face (degree_720 in [0, 719]). The torus_stage (φ) tracks
the minor circle. Together they give the full (θ, φ) position on the torus surface.

### R/r = 16/9 — The Epogdoon in the Renderer

The torus dimensions `R/r = 16/9` (major radius / minor radius) are the Epogdoon ratio (9:8 inverted
to 16:9 for display as `16/9 = (9/8)² × 9/(9/8) = ...` — actually this is 16/9 = (4/3)² which is
the square of the perfect fourth). The renderer encodes the harmonic relationship between the outer
circuit (θ, major) and the inner circuit (φ, minor) as the Pythagorean whole tone.

---

## 11. The Multiplayer Earth — 4.4.4.4 Subgraphs and SpacetimeDB

### Earth as the Shared Root of All Personal Tori

Each entity that passes through the Nara system has a `#4.4.4.4` node — a personal torus in the
coordinate space. This node is a subgraph of the Earth central node:

```
Earth node (SPACETIMEDB anchor, shared)
  ├── Entity_A.#4.4.4.4  (personal torus, natal_hash_A)
  ├── Entity_B.#4.4.4.4  (personal torus, natal_hash_B)
  ├── Entity_C.#4.4.4.4  (personal torus, natal_hash_C)
  └── ... (all entities who have passed through Nara)
```

SpacetimeDB is the shared Earth node. It tracks:
- Which entities are currently in session (presence table)
- Their current clock position (exact_degree_720, tick12)
- Their live quaternion (encrypted, hashed — not personally identifiable outside consent)

Multiple users in simultaneous sessions are multiple personal tori active on the shared clock at once.
This is the multiplayer aspect: a literal multi-body gravitational system in symbolic space, all
entities rotating relative to the same center (Earth/Quintessence).

### Consent Architecture

The personal pratibimba data (natal chart, oracle history, wisdom_delta) is consent-gated:
```
Public (SpacetimeDB, shared): entity_hash (anonymized), exact_degree_720, tick12, session_active
Private (local vault only):   natal_quaternion, oracle_history, identity layers, chakral spine
```

Earth is the home of all `#4.4.4.4`s — but each personal torus is sovereign within it.

---

## 12. Cross-Reference to Sub-Specs

All existing sub-specs in this folder remain canonical. This file integrates them, not replaces them:

| File | Topic | Relates to |
|---|---|---|
| `01-quintessence-hash-architecture.md` | Hash computation, 5 #4.0 sub-systems, natal only | §4, §6 |
| `02-16-lenses-backbone-temporal.md` | 16 lenses, backbone at 15°, daily/monthly | §9, clock geometry |
| `03-spanda-double-helix-12fold.md` | RING_QUATERNION_LUT[12], full 12-fold architecture | §8, §10 |
| `04-shadow-dynamics-three-computations.md` | Three shadow types (deficient/implicate/temporal) | §7 |
| `05-ql-7fold-law-and-vak-c-substrate.md` | Anima as #, canonical 7-fold law, C hooks | All agent dispatch |
| `06-vak-pleroma-implementation-gap-analysis.md` | What's implemented vs spec-only | §13 |

---

## 12.5. Canonical Primitive Vocabulary

Reference `docs/plans/CLOCK-AND-NARA-SPECS/00-canonical-invariants.md §3` for the full canonical primitive vocabulary table. Key primitives for this document:

- `tick12` = the ONE canonical discrete clock state (0–11). NOT `torus_stage`, NOT `spanda_stage`, NOT `cf_substage`. tick12 IS all of these.
- `exact_degree_720` = full-precision continuous clock address (f32, 0.0–719.999). Never truncated.
- `degree_node_360` = integer LUT index only. Never used in arithmetic.
- `quaternion4` = [w=EARTH, x=FIRE, y=WATER, z=AIR], always unit quaternion.

---

## 13. Implementation Gap List — Canonical and Ordered

### P0 — Hard Blockers (Nothing Downstream Works Without These)

**G1: `planet_degrees[10]` correction**
- Fix `M4_Astrological_Layer.planet_degrees[10]` → keep size but correct Earth exclusion (Earth slot = 0xFFFF sentinel)
- Fix `M4_Temporal_Now.planet_degrees[7]` → `[10]` (add Uranus, Neptune, Pluto; include Sun as solar root [0])
- Add `planet_id_to_clock_idx[11]` LUT mapping Planet_Id → clock array index (Earth = 0xFF, no clock degree)
- Canonical order: [0]=Sun, [1]=Moon, [2]=Mercury, [3]=Venus, [4]=Mars, [5]=Jupiter, [6]=Saturn, [7]=Uranus, [8]=Neptune, [9]=Pluto
- Files: `epi-lib/include/m4.h`, `epi-cli/src/nara/identity.rs`

**G2: `CLOCK_DEGREE_LUT[360]`**
- BLOCKING for the golden thread; nothing downstream of §6 step 8 works without it
- Requires `build_clock_degree_lut.py` from mahamaya dataset JSON
- Each entry: `{ m3_hexagram: u8, m3_codon: [u8;3], amino_acid: u8, decan: u8, is_palindromic: bool, lens_segment: [u16;16] }`
- Files: `epi-lib/include/m3.h` (new entry struct), `epi-lib/src/m3.c` (or generated .c file)

**G3: `CLOCK_BACKBONE[24]`**
- 24 palindromic codon nodes at 15° intervals
- Verifies the "backbone at 15° = zodiac anchors" prediction once LUT is built
- Files: `epi-lib/include/m1.h` (new struct), clock spec §7

### P1 — High Priority (Core Navigation Functionality)

**G4: `m4_live_quaternion_update()` — separating natal from live**
- Currently `quintessence_hash` is compute-once natal
- Need `live_quaternion (w,x,y,z)` as a separately-maintained field
- Updated by every oracle cast, not just at Möbius return
- Files: `epi-lib/include/m4.h` (add field), `epi-lib/src/m4.c`

**G5: Quaternionic oracle reading — 4-face result**
- `OracleResult` currently returns 1 hexagram
- Need: `{ primary, deficient_aspect, implicate_phase, temporal_anticodon }`
- `deficient = (d+180)%360`, `implicate = d+360` (double-cover), `temporal = m3_changing_lines(hex)`
- Files: `epi-lib/include/m4.h` (OracleResult struct), `epi-cli/src/nara/oracle.rs`

**G6: `hash_to_clock_position(natal_hash: &[u8;32]) -> (u16, u8)`**
- Project natal_hash → (degree_node_360, tick12)
- Returns (θ ∈ [0,359] via (hash[0]|hash[1]<<8)%360, tick12 ∈ [0,11] via quantize_to_spanda_substage)
- exact_degree_720 = degree_node_360 + phase*360 where phase derived from hash[2] bit 0
- Files: `epi-cli/src/nara/identity.rs`

**G7: `nara.clock_position_update` RPC + CosmicClockPlugin subscription**
- Gateway RPC method that takes (entity_hash, exact_degree_720, tick12, live_quaternion) and emits to subscribers
- CosmicClockPlugin subscribes and updates torus rotation when this fires
- Files: `epi-cli/src/gate/nara.rs`, portal plugin

### P2 — Medium Priority (Full UX)

**G8: Chakral-elemental spine panel**
- Side panel in portal showing 8-chakra column driven by live_quaternion components
- Natal spine shown as ghost for comparison
- Earth chakra at base always full
- Files: portal plugin (new sub-panel)

**G9: `torus_stage` tracking → φ-axis UX**
- Explicit tracking of which #4.4.4.x phase the user is in
- Names the phase in UI ("you are at the lemniscate crossing — the oracle cast is your T1/T2 moment")
- Uses `torus_stage` from live_quaternion projection
- Files: `epi-cli/src/nara/identity.rs`, portal IdentityPanel

**G10: Kerykeion live 9-planet positions on clock face**
- Kairos adapter already delivers data; clock plugin must render 9 highlighted nodes
- Natal ghost positions for comparison
- Requires CLOCK_DEGREE_LUT (G2) plus Kerykeion integration (Task 4.6)
- Files: portal CosmicClockPlugin

**G11: Convergence detection (`#4.4.4.4` crystallisation signal)**
- When multiple independent 12-fold streams (oracle/Gene Keys/Jungian/HD) converge at same index
- `Clock_M1_Apertures.convergence_count` threshold trigger
- Named moment in IdentityPanel: "Convergence detected — Personal Pratibimba crystallising"
- Files: `epi-lib/src/m4.c`, portal IdentityPanel

### P3 — Lower Priority (Completeness)

**G12: Logos FSM stage validation with φ-coherence enforcement**
- `epi nara logos` CLI subcommands (currently stubs)
- Stage validation: only advance φ when torus_stage coherence score passes threshold
- Files: `epi-cli/src/nara/mod.rs`

**G13: SpacetimeDB TorusSync with multiplayer presence**
- `epi-spacetime-module/` (stub, waiting on rustc≥1.90)
- Tables: `TorusSync { entity_hash, exact_degree_720, tick12, live_quaternion_64, timestamp }`
- Privacy: only hashed position, no personal data in shared table

**G14: `(4.5/0)` migration in any remaining `(4/5/0)` occurrences**
- Most done in prior session (~40 files); verify no new occurrences crept in
- `grep -r "(4/5/0)" .pi/ epi-cli/src/ docs/`

---

*"The oracle doesn't give you a hexagram. It gives you your current orientation in the symbolic cosmos."*

*"Every cast returns four faces: what you are expressing, what you are suppressing, what is asking to*
*complete, and what is waiting beneath."*

*"The codon is what you say. The anticodon is what you are learning to hear."*
