[package]
name = "epi-s0-settings"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
tempfile = "3"
# Test-only coupling: proves opt-in records written here are honored by the
# gemini-embedding accessor's CloudOptInPolicy (34.T34.1 <-> 34.T34.2 contract).
gemini-embedding = { path = "../gemini-embedding" }
