# AGENTS.md — epi-cli

## Purpose
Rust crate `epi-logos` (lib `epi_logos`, bin `epi`): "The Master CLI for the Epi-Logos coordinate system — ontology-is-code" — the master command surface + TUI that drives every S-layer over FFI and the gateway.
Canon: [[ARCHITECTURE-DIAGRAM-PACK]] -> [[S-SYSTEM-INDEX]] -> [[S0-SPEC]] / [[S0-ARCHITECTURE]]

## Ownership
- `Cargo.toml` / `src/lib.rs` — crate root: re-exports command modules and bridges sibling crates (`hen` <- `epi-s1-hen-compiler-core`, `epii_*` <- `epi-s5-*-core`)
- `src/main.rs` — `clap` entrypoint for the `epi` binary
- `src/agent/tmux.rs` — gateway-governed terminal lease authority, including `epi agent tmux topology`: isolated-socket tmux day-session/window/pane allocation from Anima decisions. [[cmux]] is an optional visible attachment to the same tmux session, never a second process lifecycle. Canon update flag: [[S0-SPEC]] / [[S4-2-SPEC]].
- `build.rs` — compiles `../epi-lib` C sources via `cc` (the C FFI bridge); links BLAKE3 from `../vendor/blake3`
- `src/` command families — `gate/` (gateway RPC/control, including [[M4]] Nara session protein bridge commands and `s4'.psyche.update` bounded Psyche patches with renderer state), `canon/` (`epi canon coord/search/diff` depth-ladder distribution surface over [[S5']] gnostic canon and [[Bimba]] coordinate payloads), `nara/` ([[M4]] personal, including [[Vama Shakti]] arena admin commands and the `epi nara train-lora` local-only membrane into the S4-x Nara skill family), `portal/` + `tui/` (ratatui-hypertile), `vault/`, `agent/` + `techne/` (agent/gateway lifecycle), `graph/`, `know.rs` (`epi know` unified coordinate-knowing packet), `slot.rs` (Pi-Agent model-slot + harness-slot config), `skill.rs` ([[Agora]] skill registry / [[Aletheia]] retrain-review CLI), `sync/`, `ffi/`, `core/`, `notebook/`, `profile/`, `book/`, `code/`, `sesh/`, `up.rs`, `vimarsa/`; `src/main.rs` also hosts the thin `pi train-ebm` / `pi export-ebm-state` S0 membrane over S5 resonance-corpus law.
- `src/gate/s1_hen.rs` — thin gateway adapter for governed [[S1]] vault reads/writes/moves, `s1'.base.ensure`, and `s1'.q_articulation.accept`; base ensure discovers sorted canonical [[CT]] forms under `Idea/Bimba/World`, delegates contract validation/schema emission to [[Hen]], and rejects absolute or parent-traversing reflection residency before joining the vault root. Q acceptance first requires the S5 `ReviewStore`'s human-gated approval, then sequences S2 canonical-target/[[Anuttara]] preflight -> S1 pure Hen amendment -> S2 vault sync, with the next graph revision required to match the paired review epoch. Rename integrity and literal rewrite semantics remain delegated to `hen-compiler-core::wikilinks::reconcile_rename`, never reimplemented in [[S0]].
- `src/gate/improve.rs` — thin gateway membrane over [[S5]] autoresearch, including `s5'.improve.q_review.run/latest/night_pass`; `night_pass` carries temporal context only and opens the [[S2]] graph client for the S5-owned live-corpus read, while detector policy and durable queue law remain [[S5]]-owned.
- `src/gate/anima.rs` — thin `s4'.mediation.route` gateway adapter over the [[S4]] Pleroma capability matrix. At the live boundary it enforces the matrix's `aletheia-mode-internal` class: an effective tool grant, `anima.dispatcher` role, and active `aletheia.mode.active` session state are all required. The capability universe and entitlement law remain [[S4]]-owned; this adapter does not define a second policy store.
- `src/gate/tuning.rs` — thin `s5'.tune.*` gateway adapter over [[portal-core]]'s [[M5']] tunable registry and [[S5]] `tuning_review`: live list/get, validated Tier-1 set, audit read, lock toggles, Tier-2 proposal submit/list/resolve, and persistence only after S5 governance admits the action. Class A remains unchanged until human approval; Class B needs a complete constitutional triplet before bounded auto-application. It never defines schema metadata, type/range validation, review policy, or Tier-3 [[Aletheia]] lifecycle.
- `src/gate/verifier.rs` — live `s0'.verifier.*` adapter. It runs `check_state`, `emit_query`, and 128-registry membership through the compiled [[M0']] verifier; `respond_question` is loopback-only, requires an exact question from one of the [[S3]] runtime's recent emitted profile generations, validates through the parser mirror, and atomically persists beneath the gateway state root with `0700` store / `0600` file permissions on Unix. Caller source/session values are recorded only as `loopback-client-claim` provenance, never authenticated identity; the opaque receipt remains `responded` without fabricating re-verification. `owl_query` fails explicitly until a configured [[S2]] ontology adapter owns execution. Canon update flag: [[S0-SPEC]], [[S3-SPEC]], and [[M0'-SPEC]].
- `src/gate/day_start.rs` + `src/vault/day.rs` — Track 32.T32.11's thin first-day gateway membrane: `vault.day.ensure` delegates the idempotent canonical nested day scaffold to the existing [[S1]]-owned adapter law; `khora.session_start` creates or reuses exactly one protected-local `now.md` beneath that day and returns an opaque path/session receipt. Kairos and the Nara psyche-anchor remain separate explicit calls by the [[M4']] carrier. `tests/gate_day_start.rs` drives both methods through a real WebSocket and real temporary filesystem. Contract-surface flag: [[S0-SPEC]], [[S1-SPEC]], [[S3-SPEC]], [[S4-SPEC]], and [[M4'-SPEC]].
- `src/gate/temporal.rs` + `src/gate/server/mod.rs` — the [[S3]] projection membrane stamps the live [[S2]] `GraphMeta.graph_revision` into hydrated `kernel.graphRevision` context and the shared `profile.update` heartbeat. The heartbeat samples through a bounded background reader with reconnect and task-abort ownership; graph latency never enters the 1 Hz emission loop, and unavailable metadata remains honestly absent. Tranche 23.T23.9 also attaches `harmonicProfile.cymaticSpheres` only after a fresh complete [[Kerykeion]] sky is routed through canonical [[F_routing]]; it transports the existing profile field and adds no gateway method. Contract-surface flag: [[M2-ARCHITECTURE]], [[M2'-SPEC]], [[S0-SPEC]], and [[S3-SPEC]].
- `src/gate/server/{dispatch,mod}.rs` + `schemas/src/kernel-bridge.ts` — 36.T36.8's live [[VAK]] profile-bus membrane. `sessions.patch` strict-parses the Rust `VakAddress`, asks the real [[S2]] `HybridRetriever::vak_bias_weights` policy whether bias is empty, and installs that correlate in the shared [[S3]] runtime; the 1 Hz heartbeat attaches the portal-owned `vakLanguificationTrace`. The schema enforces lowercase Rust profile-VAK shape plus CF/address, Para/Vaikhari, octave-return, and 72-to-half-decan coherence. `tests/gate_vak_languification_profile.rs` drives dialogical aperture and recognized closure over a real WebSocket. Contract-surface flag: [[S0-SPEC]], [[S3-SPEC]], [[M0'-SPEC]], and [[M'-SYSTEM-SPEC]].
- `src/gate/graph.rs` + `src/gate/server/dispatch.rs` — thin live [[S2]] graph adapters: `s2.graph.list` parses the six [[M0]] branch prefixes and fixed paging inputs, opens the real Neo4j client, then delegates count/exclusion/page law to `epi-s2-graph-services`; `s2.parashaktiCorrespondences` keeps its kernel decan chain available offline and, only when `includeShadowDecans: true`, emits the typed 36-primary + 36-light + conditional-36-shadow aggregate. Its additive `sixSonicCards` receipt projects decan/tattvic throughline, Shem kernel indices with live-name enrichment, maqam intervals, mantra, Asma routing, and planetary Cousto/velocity from compiled M0/M2 tables; `bridge72` projects the C-backed 9:8 compression alongside the S0/Nara decan -> planet -> chakra -> body-zone chain, and `correspondenceTree` exposes the compiled 100-entry Mantra and Asma overlays plus all ten M2 planet operators so the carrier does not recreate a LUT. A missing graph remains explicit rather than a dataset fallback. `tests/kernel_bridge_runtime_contract.rs` and `tests/gate_m2_sonic_cards.rs` drive the public and real-gateway paths. Contract-surface flag: [[S0-SPEC]], [[M2'-SPEC]], and [[M3'-SPEC]] should record the additive aggregate and the still-unregistered `kernelBridge.m3.tarotReversedMeaning` seam during canon harmonisation.
- `schemas/src/kernel-bridge.ts` — strict public-profile mirror for rerun 21.T21.15's [[M0]] `m0_void_structure_ring`: exactly 16 sequential `lensIndex` rows, exact `#0-4-{n}` coordinates, non-empty kernel labels, and the closed `canonical | canonical_absent | blocked` state vocabulary. It also admits rerun 24.T24.19's additive [[Fibonacci Ground]] transport: optional legacy-compatible `digitLut`/`backboneDegrees` plus backend-projected live/natal marker positions; current producers are required to emit them and the carrier treats absence as pending. `Body/M/pratibimba-app/scripts/live-wire.mjs` validates this schema against a real spawned gateway and pins the `CLOCK_LENSES_16` labels; no gateway adapter or renderer-local lens body exists. Contract-surface flag: [[S0-SPEC]] / [[M3'-SPEC]].
- `src/nara/kairos.rs` + `src/nara/identity.rs` + `src/gate/nara.rs` + `src/gate/nara_close_bundle.rs` — local [[Kerykeion]]/Kairos process adapter and Nara gateway membrane. `EPI_NARA_HOME` is the explicit process-isolation root used by real gateway harnesses; absent it, the established user Nara root remains unchanged. `nara.kairos.probe_kerykeion` is dependency-only: it reports Python/import/version availability and never reads [[PASU]] or natal fields; Track 32.T32.10 uses it before persisting opt-in. `nara.contemplate_session_close` strictly deserializes the caller-provided [[ContemplationObject]], delegates 4'-5'-0' composition to [[Body/S/S3/gateway]], and serializes the returned wisdom-delta envelope without inventing evidence or agent output. The loopback-only `nara.session_close` adapter derives the twelve-position +7 and octave-return aggregates from traversal evidence, binds the canonical protein handle to the exact session, derives the full active-[[PASU]] scope server-side, and atomically persists one `0700`/`0600` protected-local record: the unchanged M1 aggregate plus a strict `nara.session_close.contemplation.read` opaque-reference projection. Both reads require the active-PASU/session/ref boundary. Neither read persists or returns contemplation trajectories, raw protein bodies, [[Graphiti]] relation bodies, raw verifier constraints, wisdom deltas, or caller-supplied identity claims. Contract-surface flag: [[S0-SPEC]], [[S3-SPEC]], and [[M5'-SPEC]] should record the additive M5 projection route during canon harmonisation.
- `src/nara/oracle_cast.rs` + `src/gate/server/dispatch.rs` — the governed [[M3']] `s5.oracle.iching.cast` adapter performs one OS-random three-coin cast, persists that exact cast to [[Nara]] oracle history, and returns a strict line/hexagram/change-index/cast-id/provenance receipt. The browser never supplies or derives oracle lines. `tests/gate_s5_oracle_iching_cast.rs` drives two calls through a real WebSocket gateway and proves receipt semantics plus persisted cast-id progression.
- `src/nara/medicine_frame.rs` + `src/nara/medicine_route.rs` + `src/gate/nara.rs` — canonical Medicine LUT projection through `nara.medicine.snapshot` and the explicit `nara.medicine.pin` adapter. Snapshot is read-only evidence; pin accepts only a canonical decan herb and atomically replaces the active `EPI_NOW_PATH` after S1 Hen mutates `c_4_pinned_materia`. Canon update flag: [[S0-SPEC]] / [[M4'-SPEC]].
- `src/nara/transform.rs` + `src/nara/transform/lifecycle.rs` + `src/gate/nara.rs` — Track 25.T25.11's governed `nara.transform.start` / `nara.transform.advance` lifecycle. The adapter owns the three canonical container stage tables, optimistic `expectedStage` guard, confirmed-only regressions, atomic active state, one `kind: contemplative` transition artifact per move, and the S1-Hen mutation of NOW `c_4_active_alchemical_op`; the carrier supplies no stage law. Contract-surface flag: [[S0-SPEC]], [[S1-SPEC]], [[S3-SPEC]], and [[M4'-SPEC]].
- `src/nara/logos.rs` + `src/gate/nara.rs` — Track 25.T25.13's six-stage A-Logos→An-a-Logos cycle. Beside the existing `nara.logos.run/status/stage`, `nara.logos.advance` writes the next stage under `EPI_NARA_HOME/logos/{date}-stage-{n}.md` as a contemplative transition artifact carrying `c_3_stage_from`/`c_3_stage_to` (from `-1` = the pre-cycle ground), and `nara.logos.regress` writes a separate `{date}-regress-{n}.md` artifact with the explicit `c_4_regression: true` flag before stepping the cursor back by removing the highest completed stage. Both cursor logic and artifact rendering are dependency-injected over an explicit dir for hermetic tests; the M' carrier (`m4.nara.logosCycle`) is a strict consumer that supplies no cycle law. Contract-surface flag: [[S0-SPEC]], [[S3-SPEC]], and [[M4'-SPEC]].
- `src/gate/epii_axiom.rs` + `src/gate/server/dispatch.rs` — Track 26.T26.14's DR-B-2 Pi axiom-translation producer (physical S0, conceptual [[S5']] epii). `s5'.epii.axiom_translate` runs a candidate articulation through the chain Philosophical English → Formal Notation → OWL → SHACL, ONE model invocation per step over the PI harness (`pi_harness_invoke` resolves `epi slot show --json` then `pi -p --model <provider>/<model>` — the provider-agnostic selector; never a bespoke client, raises loudly on no-usable-model, per blindfolded_teacher / Track 12.22), and persists the session to `<state_root>/epii/axiom-translations/<id>.json` with the carrier's camelCase + kebab-form contract; each step is verified `pi`, the session stays `pending` until human final-validation. `s5'.epii.axiom_translation_history` reads the persisted sessions. The per-step invocation is dependency-injected (`StepInvoke`) for hermetic tests; the M' carrier (`m5.epii.axiomTranslation`) is a strict read consumer. Contract-surface flag: [[S0-SPEC]], [[S3-SPEC]], and [[M5'-SPEC]].
- `src/gate/codon.rs` + `src/gate/server/dispatch.rs` — Track 30.T30.13's `s2.codon.aa_lookup` membrane. It validates and encodes a three-base DNA/RNA codon, then returns amino-acid and START/STOP identity directly from the C-backed `portal-core::transcription` authority; it owns no amino-acid table or UI presentation. Contract-surface flag: [[S0-SPEC]], [[S3-SPEC]], and [[M3'-SPEC]].
- `src/entity.rs` + `src/world.rs` — CCT-14 CLI parity (`epi entity capture/classify/promote_to_type/list`, `epi world graduate/list_entities`) per [[DR-S5-ONE-1]]: both call the SAME `src/gate/s1_hen.rs` handlers the gateway `s1'.entity.*`/`s1'.world.*` dispatch arms use (lifecycle LAW lives in `hen-compiler-core::entity_lifecycle`; birth-codon per CCT-14b).
- `tests/` — extensive contract/integration suites (gate_*, nara_*, vault_*, agent_*, portal_*, kernel_*; `entity_capture_round_trip` + `world_graduate_via_cli` cover CCT-14 CLI↔gateway parity)
- `schemas/` (TS dataset validator), `contract-inventory/` (`s0-membrane-inventory.json`), `scripts/`, `assets/`, `vendor/`
- Does NOT own coordinate semantics it merely invokes — graph law lives in [[S2]], gateway/session law in [[S3]], agent runtime in [[S4]]; domain law stays in its owning module, not pulled into [[S0-SPEC]] by convenience.

## Local Contracts
- (no local CONTRACT.md; no `//!` header in `src/lib.rs`) — binding surface is the `clap` command tree in `src/main.rs`, the C FFI declarations in `src/ffi/`, and `contract-inventory/s0-membrane-inventory.json`
- Owning spec: [[S0-SPEC]] / [[S0-ARCHITECTURE]]

## Work Guidance
- Run `gitnexus_impact({target, direction:"upstream"})` before editing any symbol; warn the user on HIGH/CRITICAL risk.
- C/FFI invariant: `GET_PTR(ptr)` before every dereference; the HC struct stays 128 bytes.
- Reference all coordinates/specs/crates/agents as `[[wikilink]]`; vault writes use coordinate-prefixed `c_n_*` frontmatter.
- `src/gate/kernel_bridge_runtime.rs` must enforce [[DR-M4-4]] q-partition checks before caching or serialising safe profile payloads: private `q_personal` / `q_identity` / `q_activity` / `q_composed` snake-case keys and derivatives are refused; public `q_*` / `qm_*` carrier keys must match the 0-5 allowlist.
- Bell kernel ([[m123-modal-resonator-bell-kernel-spec]] §§5–6): `src/gate/kernel_bridge_runtime.rs` owns `M123ChimeFrameJsonShape` + `m123_chime_frame_from_profile` (contract `S0.kernel-bridge.m123-chime-frame`); the heartbeat in `src/gate/server/mod.rs` publishes it as a 1 Hz `m123.chime` SIBLING of `profile.update` — additive, never a replacement. World-clock tick/degree mismatch → `worldClockBinding.state: "stale"` + incoherent frame; consumers block readiness. `kernelBridge.m2.epogdoonProjection(address72)`, `kernelBridge.m2.planetaryElementalWeights()`, `kernelBridge.m3.lensCodonBinary(lensId)`, and `kernelBridge.m3.lensField(lensId)` (the generic lens-field dynamic — `typed_json_m3_lens_field` composes portal-core `lens_field` structure + live activation with the `pleroma_lens` symbolic decoration at lens 6 only; `layout` param is pleroma-only; unpositioned kairos yields honest-null Akasha) are guarded on-demand gateway capabilities. `src/gate/server/dispatch.rs` validates integer request boundaries, adapts the fresh complete [[Kerykeion]]/Kairos sky into the kernel `PortalClockState` for the elemental feed, and returns only typed kernel projections, never renderer arithmetic or a zero-state substitute. The lens route addresses the primary [[Fibonacci Ground]] as functional lens 16 and the 16 derived divisions as ids 0..15, makes every derived packet carry `groundingLensId: 16`, and for 24.T24.20 transports the portal-owned 3x2-bit codon, canonical class, charge/quaternion/X witness, six line hops, and compiled RNA capability without recomputation. `s5'.gnostic.musical_transcript` and `s2.graph.ananda_position` are likewise on-demand gateway adapters over the existing [[M3]] transcription and [[S2]] position projections: they parse the full `VakAddress` envelope at the boundary and do not duplicate projection law. Rust, S3 method registry, and active [[Pratibimba]] TypeScript preflight remain exact peers. The Zod mirrors live in `schemas/src/kernel-bridge.ts` (lensMode law: lens 0..11, mode 0..6 — never swap back; public-current payloads reject raw field/personal-cymatic/[[M4]] body keys). Contract-surface flag: [[M3'-SPEC]], [[S0-ARCHITECTURE]], and [[S3-SPEC]].
- Zod surface growth 2026-07-06 (Track 00.T12/T13): `schemas/src/kernel-bridge.ts` + new `schemas/src/gateway-bus.ts` now type the previously-`z.unknown()` profile projections (anandaVortex, including its complete six-family 12×12 `matrixCells` transport, harmonicGrammar, bedrock, graph/deposition/future anchors, readinessLedger — snake_case on the wire, context-frame web, pointer anchors), the exact 12-string [[M0]] `contemplationPromptLut`, and 4 gateway bus event channels; these TYPE what the kernel already emits (conforming, not inventing). Quintessence is handle-only per [[DR-M4-3]]: 9-key allow-list including the public-safe backend `natalFibonacciPosition`, no ≥16-hex digests in string values, unit-quaternion norm, `quintessenceWeight` the one resonance scalar. Live-wire (`Body/M/pratibimba-app/scripts/live-wire.mjs`) enforces field-coverage: every wire field is manifest-covered, justified-EXEMPT, or DECLARED_NOT_EMITTED — an uncovered field FAILS. Still-`z.record` gaps (EXEMPT-tracked): chromatic, elements, planetaryChakral, binary, codonRotationProjection. Owning canon flag: [[S0-SPEC]]/kernel-bridge contract docs should absorb the typed-surface inventory on the next harmonisation pass.
- B-12 graph propagation (09.T9.5): `schemas/src/kernel-bridge.ts` mirrors additive `KernelBridgeCachedProfile.graphRevision`; the gateway reads the canonical [[S2]] revision, never derives it in [[S0]], and the active carrier consumes it only through the shared profile-generation seam. Owning canon: [[S3-SPEC]] and [[M'-SYSTEM-SPEC]].
- `epi app dev` / `epi app launch` must preflight the local gateway before launching the Pratibimba Electron surface; `epi app dev` passes `EPI_GATEWAY_URL` into Theia so `kernel-bridge` and OmniPanel use the managed gateway instead of a stale or absent process.
- Gateway liveness records must describe a real bound listener: write `status.json` only after bind/registration succeeds, and clear stale status/PID files when preflight cannot probe a gateway.
- For `s4'.mediation.route`, read `Body/S/S4/plugins/pleroma/capability-matrix.json` through the existing adapter helper; never duplicate the [[Aletheia]] internal-tool list in [[S0]].

## Verification
- `cargo test -p epi-logos` (Rust). FFI bridge to the C layer: `make rust-test` at the repo root.
- `cargo test --manifest-path Body/S/S0/epi-cli/Cargo.toml nara_session_dispatch_cli_bridge_returns_protected_handle --lib` for the [[M4]] Nara session dispatch CLI bridge.
- `cargo test --manifest-path Body/S/S0/epi-cli/Cargo.toml slot_list_round_trip` for `epi slot` model/harness config round-tripping.
- `cargo test --manifest-path Body/S/S0/epi-cli/Cargo.toml --test pi_ebm_commands` for the `pi train-ebm` / `pi export-ebm-state` S0 mirror.
- `cargo test --offline --manifest-path Body/S/S0/epi-cli/Cargo.toml --test nara_train_lora_cli` for the real binary-to-local-skill corpus/checkpoint path.
- `cargo test --offline --manifest-path Body/S/S0/epi-cli/Cargo.toml --test cli_canon_coord_depth_ladder`; `--test cli_canon_search_semantic`; `--test cli_canon_diff_structural` for `epi canon` surfaces.
- `cargo test --manifest-path Body/S/S0/epi-cli/Cargo.toml --test kernel_bridge_m123_chime_frame` for the bell-kernel chime frame; `cd schemas && npx vitest run` for Zod contract parity (parses the real generated baseline profile incl. `modalResonator`).
- Governed [[S1]] rename: `cargo test --manifest-path Body/S/S0/epi-cli/Cargo.toml --test gate_s1_vault_surface --test vault_hen_boundary_audit` (real temp filesystem plus spawned WebSocket gateway).
- Kairos dependency probe: `cargo test --offline --manifest-path Body/S/S0/epi-cli/Cargo.toml --test gate_nara_kairos_probe` (real Python process plus WebSocket gateway; no identity fixture).
- First-day lifecycle: `cargo test --offline --manifest-path Body/S/S0/epi-cli/Cargo.toml --test gate_day_start` (real WebSocket plus canonical day/NOW filesystem writes and idempotent retry).
- Medicine snapshot + governed pin: `cargo test --offline --manifest-path Body/S/S0/epi-cli/Cargo.toml --test gate_nara_medicine_snapshot` (real WebSocket gateway plus real filesystem NOW mutation).
- Transform container lifecycle: `cargo test --manifest-path Body/S/S0/epi-cli/Cargo.toml --test gate_nara_transform_lifecycle` (real WebSocket gateway, guarded progression/refusal, real NOW mutation, state, and contemplative artifacts).
- Codon amino-acid adapter: `cargo test --manifest-path Body/S/S0/epi-cli/Cargo.toml --test gate_codon_aa_lookup` (real portal-core/C LUT identity plus malformed-input refusal).
- Mediation entitlement boundary: `cargo test --offline --manifest-path Body/S/S0/epi-cli/Cargo.toml --test gate_mediation_route` (real gateway route invocation against the S4 capability matrix).
- Live Anuttara verifier edge: `cargo test --offline --manifest-path Body/S/S0/epi-cli/Cargo.toml --test gate_anuttara_verifier` (real WebSocket gateway executing compiled M0 check/query/membership routes plus generation-bound protected-local symbolic-question response persistence; stale/malformed/blank responses and OWL requests without an S2 adapter fail closed, and Unix permission modes are asserted).
- Live [[VAK]] languification profile edge: `cargo test --offline --manifest-path Body/S/S0/epi-cli/Cargo.toml --test gate_vak_languification_profile` (real WebSocket `sessions.patch` through the shared heartbeat, including S2 bias policy, Para aperture, Vaikhari recognition closure, and 72/half-decan evidence).
- M3 transcription and [[Ananda]] projection edge: `cargo test --offline --manifest-path Body/S/S0/epi-cli/Cargo.toml --test gate_m3_transcription_projection` (real spawned WebSocket gateway, full VAK envelope, and missing-envelope rejection).
- M3 functional-lens transcription packet: `cargo test --offline --manifest-path Body/S/S0/epi-cli/Cargo.toml --test gate_m3_lens_codon_binary` (real spawned WebSocket gateway, complete 24.T24.20 degree rows, Ground/derived cardinalities, and id-17 refusal).
- M2 planetary-elemental edge: `cargo test --manifest-path Body/S/S0/epi-cli/Cargo.toml --test gate_m2_planetary_elemental_weights` (real spawned WebSocket gateway over a fresh complete Kairos cache; proves nine-orbiter projection, normalization, and aspect handles).
- Tunability adapter: `cargo test --offline --manifest-path Body/S/S0/epi-cli/Cargo.toml --test gate_tuning_surface` (real spawned gateway, Tier-1 mutation/audit, Class A human-review application, and Class B triplet-gated auto-application).
- Contemplation adapter: `cargo test --offline --manifest-path Body/S/S0/epi-cli/Cargo.toml --test gate_contemplation_live` (real spawned gateway, full [[ContemplationObject]] request, and 4'-5'-0' response envelope).
- [[M1']] session-close aggregate adapter: `cargo test --offline --manifest-path Body/S/S0/epi-cli/Cargo.toml --test gate_nara_session_close_live` (real WebSocket gateway proving canonical handle/session binding, backend-derived +7 and octave-return evidence, active-[[PASU]] confinement, opaque readback, and privacy-safe persistence).
- [[M0]] residual browser adapter: `cargo test --offline --manifest-path Body/S/S0/epi-cli/Cargo.toml --test gate_m0_residual_list_live` (requires real local Neo4j; no seed-file substitute).

## Child DOX Index
- (leaf)
