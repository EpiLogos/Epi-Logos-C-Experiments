---
name: fine-tuning-with-trl
description: SFT, DPO, PPO, GRPO, and reward-model training through TRL.
version: 0.1.0
tags: [hermes, ml, vendored]
platforms: [darwin, linux]
source: hermes
---

# fine-tuning-with-trl

Use this vendored Hermes skill when the Epi-Logos ML skill surface needs: SFT, DPO, PPO, GRPO, and reward-model training through TRL.

## Contract

- Keep runtime credentials in the owning model-slot or deployment config.
- Record artifacts in the Agora skill index before Anima dispatch.
- Preserve downstream skill residency; this vendored skill supplies method capability, not subsystem law.

## Verification

Run `epi skill show fine-tuning-with-trl` and `epi skill register fine-tuning-with-trl` after vendoring.
