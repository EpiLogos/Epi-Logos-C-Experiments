---
name: unsloth
description: Memory-efficient local fine-tuning accelerator for supported transformer models.
version: 0.1.0
tags: [hermes, ml, vendored]
platforms: [darwin, linux]
source: hermes
---

# unsloth

Use this vendored Hermes skill when the Epi-Logos ML skill surface needs: Memory-efficient local fine-tuning accelerator for supported transformer models.

## Contract

- Keep runtime credentials in the owning model-slot or deployment config.
- Record artifacts in the Agora skill index before Anima dispatch.
- Preserve downstream skill residency; this vendored skill supplies method capability, not subsystem law.

## Verification

Run `epi skill show unsloth` and `epi skill register unsloth` after vendoring.
