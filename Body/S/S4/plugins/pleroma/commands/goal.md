---
description: "Create a dialogical VAK GoalPrelude in the active NOW, then return to the user for intent confirmation before any run or schedule."
disable-model-invocation: true
---

Invoke `goal-prelude` and follow it exactly.

The first pass must write a `goal-prelude` markdown file into the active NOW and return a concise, open summary to the user. Do not create a cron job, `GoalRun`, Epii review resolution, or permanent crystallisation unless the user explicitly continues into that next mode.
