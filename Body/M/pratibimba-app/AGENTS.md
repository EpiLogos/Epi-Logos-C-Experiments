# AGENTS.md — pratibimba-app

## Purpose
`Body/M/pratibimba-app` is the **active M' carrier** (per the Carrier Decision 2026-07-02 in [[M'-SYSTEM-SPEC]] and the ratified [[M'-SURFACE-REENVISIONING-2026-07-01]]): one Tauri v2 binary in which kernel, gateway, and face are one supervised organism. Boot = supervise — the Rust side spawns/adopts the `epi` gateway (S3, port 18794); the webview face holds exactly four stores driven by the gateway's `profile.update` heartbeat.

## Ownership
- `src/panes/M2CorrespondencePane.tsx` + `src/components/CymaticField.tsx` — Tranche 23.T23.20's M2 cymatic MonoPoly reader: invokes only `kernelBridge.m2.cymaticMonoPolyState(address72)`, renders the returned behaviour state, and never derives tone count or resonance class locally. Canon update flag: [[M2'-SPEC]], [[S0-SPEC]], [[S3-SPEC]].
- `src/components/EpogdoonProofOverlay.tsx` + `src/panes/M2CorrespondencePane.tsx` — Tranche 23.T23.8's profile-tick-only 9:8 proof reader: query-gated or persisted-M2-preference proof identity/decomposition and the 9-generation bloom mounted on the planetary evidence card. It cites, but never implements, `m2_epogdoon_compress`; no local timer or address reconstruction is permitted. Canon update flag: [[M2'-SPEC]], [[M2-ARCHITECTURE]].
- `src/components/CymaticSpheresSurface.tsx` + `src/panes/M2CorrespondencePane.tsx` — Tranche 23.T23.9's third Layer-C carrier: persisted `'spheres'` selection renders eight concentric Three.js chakra spheres with profile-resolved spherical-harmonic modes around the Earth/Sun centre and the active [[F_routing]] planetary-hour ruler. It strict-consumes `harmonicProfile.cymaticSpheres`, rebuilds scene state only on profile generation, and renders explicit projection/WebGL blockers; no browser chakra/planet LUT, timer, or plate fallback exists. `tests/e2e/m2-correspondence.spec.ts` proves the spawned-real-gateway path, nonblank pixels, active-ruler routing, generation response, and desktop/mobile framing. Canon update flag: [[M2-ARCHITECTURE]], [[M2'-SPEC]], [[S0-SPEC]], [[S3-SPEC]].
- `src/components/CymaticField.tsx` — Tranche 23.T23.4's plate renderer: consumes the hash-pinned [[M2']] seed raster from the kernel-provided octet/quartet and conserved 72-address, then draws its 72-cell overlay and colour-binary from the correspondence receipt's planetary element. It has no local clock, audio output, or element classifier. Canon update flag: [[M2'-SPEC]], [[S0-SPEC]].
- `src-tauri/` — Rust host: `supervisor.rs` (gateway supervision, `gateway://status` events, `gateway_status` command), `identity.rs` (E6 personal-pole LOCAL reads — the natal sky never crosses the gateway bus; the bus carries only the kernel's `quintessence` handle summary per DR-M4-3), `composition_state.rs` (strict atomic [[M5']] integrated-composition state under `~/.epi-logos/composition/`; opaque `q-composed://` handles only), `lib.rs` (shell assembly).
- `src/` — the face: `bridge/` (ported kernel-bridge types + plain-TS `GatewayClient`), `state/stores.ts` (tick · coordinate · session · provenance) plus the `/` membrane's typed session record in `panes/omni/omnipanelSessionState.ts`, `components/` + `App.tsx` (0/1 shell, command-routed title-bar coin / `cmd-period` inversion, consumed lemniscate mask transition, six-entry status strip).
- `src/panes/{semanticConnections,SemanticConnectionsPane}.ts{x}` + `App.tsx` — rerun 28.T28.12's production Smart Connections sidebar. The left-border receiver invokes only the real `s1'.semantic.suggest_links` route for an explicit vault-relative note, strict-consumes typed candidates/evidence/privacy/staleness, and opens selected neighbours through `vault.open`. Missing `.smart-env/multi` remains an honest `no-index` state; no placeholder, renderer similarity score, index builder, or canon write exists. `tests/e2e/semantic-connections.spec.ts` drives the mounted pane against a spawned gateway and real temporary vault. Canon update flag: [[M'-PORTAL-SPEC]] should record the admitted `pratibimba.smart-connections-sidebar` carrier.
- `src/panes/{canonStudio,canonEditorExtensions,MarkdownEditorPane}.ts{x}` — rerun 28.T28.4's Canon Studio read half (CHROME-CONTRACT §2 `editor`). The frozen shell's Monaco pair lands as the carrier's own editor stack: `Decoration.mark` over a whole-document scan gives inline `ql-coordinate` / `bimba-wikilink` marks (coordinate validity stays `ui/tokens.ts`'s `coordinateFamilyGrade`; wikilinks win over nested coordinates so the range builder never sees an overlap), and a `CompletionSource` gated to an unclosed `[[` asks the live `s1'.semantic.suggest_links` for the note's own neighbours under `isPrivacySafe`. The frontmatter fold discloses S1's DR-S1-5 typology receipt (`s1'.type.classify_c_layer`) and nothing it derived itself; frontmatter **key-shape** law remains S1's (`hen-compiler-core::validate_frontmatter`, reachable only as `epi vault frontmatter-validate` — no gateway seam exists, and the fold states that rather than re-implementing it). The identity note routes to the live `identity.openWizard` (25.T25.4) instead of growing a second PASU editor, and the governed write leaves as a `logos-atelier` `CrossLayoutIntent` carrying `artifactUri` — this surface never mutates canon (§4). `tests/e2e/canon-studio.spec.ts` drives both halves against a spawned gateway. Canon update flag: [[M'-PORTAL-SPEC]] should record the admitted `pratibimba.canon-studio` carrier view id; DR-VAK-unrelated.
- `src/composition/compositionState.tsx` — Tranche 29.T29.10's strict [[M5']] composition-state extension and React context above both faces; persists the shared state spine plus cosmic/personal/cross fields through the Tauri host without adding a fifth runtime store, and refuses raw quaternion material at the persistence boundary.
- `src/composition/cosmicComposition.ts` — **29.T29.2 / DR-WC-IP-2 (ROUTED)**: the cosmic 1-2-3 composition's declared slot ownership, and the production caller `geometricSlotEnforcement.ts` never had. That module carried the whole law (cosmic slots, the reads-only rule on `texture`/`cell-state`, the protected-local boundary, `compositionLoad()`) and the ONLY file in the repo referencing it was its own test — which even constructed a cosmic composition that nothing in the app declared. The surface rendered its K² torus, cymatic skin and codon annulus with none of them claiming a slot. Now `surface`=m1-paramasiva-played-torus, `texture`=m2-parashakti (reads-only), `cell-state`=m3-mahamaya (reads-only), run through the real loader at the `CosmicEngine` root and reported on the surface ROOT as data attributes — the file's own `data-m3-lens-ring-contract` idiom, chosen so a screenshot-baselined face gains no rendered text. A parity test binds each declared slot to the modulation carrier that renders it (`k2-torus`/`cymatic-skin`/`codon-annulus`), so the declaration cannot describe a composition that is not on screen. `compositionLoad()` additionally gained `contested-geometric-slot`: two owners on one slot is the three-stack overlay this composition replaces — whichever drew last would win silently — and it was granted silently before.
- `src/composition/personalComposition.ts` — **29.T29.3 / DR-WC-IP-3 (ROUTED)**: the personal 4-5-0 composition's declared slot ownership, and the call that finally makes the M4 protected-local boundary reachable. The five forbidden handle classes (`raw-quaternion`, `raw-audio-octet`, `plaintext-journal`, `graphiti-episode-body`, `raw-natal-chart`) are all PERSONAL material, so 29.T29.2's cosmic caller could never exercise them — no cosmic contributor declares a journal body or a natal chart — and `PERSONAL_GEOMETRIC_SLOTS` was referenced by exactly one file in the repo: its own test. The guard was unreachable on the only path it was written for. Now `left-composition`=m4-nara (`opaque-handle` — the composition carries the vault PATH; `NaraCanvasEditor` reads the day's body from S1 itself, so the journal text never crosses), `right-composition`=m5-epii (`recognition-surface`), `grounding`=m0-anuttara (`r-virtue-witness`), `composition-ambient`/`composition-status`=m4-nara (`visual-state`), run through the real loader at the `PersonalRecognitionEngine` root and reported as data attributes (the surface rides `composition-4-5-0-personal.png`, and `visibility: hidden` preserves its layout box, so rendered text would still move the capture). `center-composition`=m4-nara (`psychoid-renderer-handle`) is declared OWNED-and-BLOCKED, not omitted: 25.T25.6 is pending and `engine/dipyramidGeometry.ts` — the DR-IG-6 fixture "every M4-5' psychoid renderer must consume" — has zero consumers; a test asserts each slot is either carried or blocked, never quietly absent. The parity map is file+symbol rather than carrier-id because the personal chrome legitimately spans files (`TimeAxisSwitcher` mounts in `App.tsx`'s `personalHome` around the engine; the privacy tint is worn by the leaf surfaces). `ownerOfSlot` moved to `geometricSlotEnforcement.ts`, which owns the result type it reads. Canon update flag: [[M'-SYSTEM-SPEC]] should record the personal slot-owner register.
- `src/composition/{profileTickSubscription.ts,compositionProfileContext.tsx}` + `scripts/lint-single-clock.mjs` — **29.T29.4 / DR-WC-IP-4 (ROUTED)**: the single-clock invariant as the tranche specifies it. `openCompositionProfileSubscription()` opens ONE subscription and fans it out; `<CompositionProfileProvider>` opens exactly one at each composition mount (`App.tsx`'s `cosmic` and `personalHome` cases) and disposes it on unmount; `useCompositionProfile()` reads that context and THROWS outside a provider — an unwrapped contributor is a composition that never declared its subscription. `publishProfileTick()` is the one way a frame enters the clock: the gateway `onProfile` callback calls it, and so does every test, because a test driving the clock IS the wire for that test. That removed the reason for a carve-out — 57 sites across 42 test files were migrated off `useTickStore.setState`/`setProfile`, verified by AST scan to seed no generation the wire could not produce (0 divergent). The lint is therefore UNIFORM: `new GatewayClient` only in `App.tsx` (plus the client's own tests), clock writes only in `state/stores.ts`, its own test, and the seam. It judges `setProfile` by RECEIVER not by name, since several suites define a local helper of that name that already delegates. Migrating to the real seam also put those tests through the store's stale-generation gate for the first time, which caught five that had been seeding wire-impossible state (a repeated generation, a bare `generation` with a null profile). `tests/e2e/single-clock.spec.ts` proves one clock drives both faces in a real browser. Canon update flag: [[M'-SYSTEM-SPEC]] should record the profile-tick seam.
- `src/ui/motionTokens.ts` (owns `TRANSITIONS`) + `scripts/lint-e2e-import-graph.mjs` — **shared UF-gate unblock, cross-lane.** The whole Playwright suite could not enumerate (`playwright test --list` -> "Total: 0 tests in 0 files"), so NO UF-class tranche in ANY lane could close. Cause: `motionTokens.ts`, a pure token module, re-exported `TRANSITIONS` from `primitives.tsx` for convenience, so every consumer of a motion token — including pure-law modules that specs legitimately import — pulled in a React component and, through it, `iconography.ts`'s eager `import.meta.glob`, a Vite build-time API Playwright cannot transform. Chain: `design-primitives.spec.ts` -> `stateGrammar.ts` -> `motionTokens.ts` -> `primitives.tsx` -> `iconography.ts`. FIX: the edge is inverted rather than the glob guarded or the icon map moved — `TRANSITIONS` IS a motion token and now lives in the motion-token source, with `primitives.tsx` re-exporting it for its components. DR-UI-4's values are untouched (400 cubic-out / 240 linear / 320 smoothstep) and its "no divergent second copy" law still holds by object identity (`motionTokens.test.ts` asserts `toBe`). Track 30's build-time icon-resolution guarantee is left fully intact — `iconography.ts` is not modified. `lint-e2e-import-graph.mjs` prevents recurrence and prints the FULL chain, because the thrown frame names only the leaf and finding this edge took a per-spec bisect; it is chained ahead of vitest in `pnpm test`. Flagged for Track 30 (30.T30.9 `e48b0fc4` landed the glob; 30.T30.3 owns motionTokens).
- `src/composition/integratedReadinessEnvelope.ts` — **29.T29.5**: the typed `IntegratedReadiness` envelope (`compositionId` + `perGeometricSlot` + `compositionBlockers`) and the LEDGER-PARITY law that keeps its blocker vocabulary honest. A blocker id is a promise that something real is missing and that a named track will land it; an id naming nothing renders as an honest pending state while pointing at no work. So every id declares what BACKS it — `wave-a-marker` (the field-backed ledger port `engine/integratedReadiness.ts`, which carries the carrier-real profile paths) or `pending-contributor` (a registered geometric slot whose owner has not landed). The parity test refuses a marker-backed id whose marker is absent from `WAVE_A_PENDING_MARKERS`, a contributor-backed id naming an unregistered slot, and any id a composition declaration uses that the registry does not know. THAT LAST CHECK CAUGHT A REAL DEFECT: 29.T29.3 shipped `pending-psychoid-cymatic-renderer`, invented at the call site, where the vocabulary spells `pending-psychoid-cymatic-solver` — corrected here and across its tests and e2e spec. `buildIntegratedReadiness()` folds per-slot ownership and live field absence into one reading, so the two compositions cannot disagree about what 'ready' means; toggling `klein_flip` absence surfaces `pending-klein-flip` and reddens the slot, which is the tranche's own acceptance. Canon update flag: [[M'-SYSTEM-SPEC]] should record the composition blocker register.

- `src/composition/waveCContributions.ts` — **rerun 25.T25.21**: the M4' Wave-C contribution register, carrier-native. The brief asks for the frozen `TRACK_08_CONTRIBUTION` table to be extended and says the `plugin-integrated-4-5-0` consumer READS it to lay out the personal editor area; neither half survives contact with the tree. `TRACK_08_CONTRIBUTION` has ZERO runtime consumers in any of the six barrels — `setContributors` (`plugin-integrated-4-5-0-widget.tsx:87`) is defined and never called, and `contributorRecords` initialises `[]` and is never assigned — and the carrier's live composition path (`personalComposition.ts` → `compositionLoad.ts`) keys on `CompositionContributor`, which shares NO field with the TRACK_08 row shape. So this is a CHECKED MANIFEST, not wiring: 22 rows, each carrying view id, frozen export lineage, real carrier file+symbol, the `data-testid` a receipt drives, the mount mechanism as a discriminated union (`flexlayout-tab` with its label, `nested-section`, `direct-jsx`, `overlay-command`, `composition-slot`, `cross-coordinate`, `model-only`, `absent`), layout law, privacy class, mini-modes and a quoted spec line. What the frozen table was protecting is name-truth, and it failed at that too — five of its 23 names have no definition anywhere in `m4-nara/src`, because a string literal in an `as const` array is never type-checked. `waveCContributions.test.ts` reads the real sources in BOTH directions (symbol present, testid emitted, model key AND factory arm present, label adjacent to key, command catalogued, slot real; plus a reverse sweep that fails when a Wave-C surface renders with no row) and was proven RED on four injected lies. **VIEW IDS RESOLVE TO THE BRIEF, NOT THE BARREL**: `m4.nara.transformContainers` over `…transform`, `m4.nara.kairosDisplay` over `…kairosWheel`, and `m4.nara.journalTimeline` / `…sessionBreakdown` / `…graphitiBrowser` are NOT minted — no assigning spec line, and the refusal is recorded in `UNENROLLED_FROZEN_EXPORTS` rather than left silent. **GAPS ARE DECLARED, NEVER COLLAPSED**: 25.7 keeps its own `no-face` row even though `crossLayoutIntent.ts:86` routes its INTENT onto the 25.14 pane — the two content-laws are disjoint (live resonance/element/chakra read vs handle panel + consent editor), 25.T25.7 is pending, and folding the row would close a pending tranche by adjacency. 25.23's fretboard is enrolled although 25.21's own export list omits it, since the tranche's acceptance is that EVERY Wave-C view id maps to an owner. Three live surfaces whose own tranche is still open carry `landedBy` naming what actually built them, so the register cannot launder them as delivered. `disposition` is declared and NULL on every row: Track 52 T5 fills it when the 4+2 subsystem pages land, so that tranche edits a field instead of rewriting a table. **THE THREE PER-EXPORT SEAMS (SPEC:288)** — `currentStateSelector` / `selectionHandler` / `evidenceSerializer` — are carried per row and are non-null EXACTLY where the surface renders (15 of 22 rows). The frozen barrel satisfied that requirement by `.map()`ing three templates over 23 names: every selector read the same three fields off a `shared-bridge` this carrier does not have, every handler routed to the same `ROUTE_PATH` constant, and every serializer carried the extension-wide `PRIVACY_CLASS` — so the one thing SPEC:288 states in words, that the class match the widget's chrome, was violated on all nine handle-only surfaces. Here the selector names the store hook or RPC the carrier file really calls (checked by reading it), the handler names a REGISTERED `CROSS_LAYOUT_INTENT_TARGETS` entry whose component must equal the row's own mount (or says in a sentence why nothing routes to it), and the serializer carries the row's own class with the handle-bearing attribute — a handle-only row with nothing carrying a handle FAILS. Two real findings fell out: `m4-nara/transform` (`crossLayoutIntent.ts:118`) and `m4-nara/logos` (`:120`) are registered but resolve to `personalHome`, so a cross-layout selection for either lands on the Now composition instead of the `transformContainers` / `logosCycle` tab that owns it; recorded, not repaired, because that table is Track 31's routing spine. **THE SPEC:290 CONSUMER LAYOUT** is `INTEGRATED_450_CONSUMER_LAYOUT`: all six personal geometric slots, each reconciled against the LIVE `loadPersonalComposition()` via `ownerOfMountedSlot` rather than against a copy of the 29.3 table. Two of the three slots SPEC:290 assigns are occupied by something else, and that is 29.T29.3's landed decision (DR-WC-IP-3, with a screenshot baseline on it), not drift: left went to the day canvas and right to m5-epii's recognition layer, so each specced occupant is a DR-FACE-7 fate-A `carried-elsewhere` naming WHERE it does reach the user — a carry that names no destination reads as a loss. Centre is `blocked` on the registered `pending-psychoid-cymatic-solver` (25.T25.6 pending). A Track-25 register may state that divergence; repointing the slots is Track 29's call. `tests/e2e/m4-contribution-registry.spec.ts` is the UF half — it derives its work-list FROM the register, so a row cannot claim reachability nobody clicks, and it caught a region lie the source gate passed 27/27 on; it also reads every `data-<slot>-owner` off the mounted composition in a real browser, DRIVES each carried-elsewhere mount, and dispatches every routed selection handler through the live intent spine to watch its surface paint. Both gates were proven RED on injected lies (four on the source gate; four more on the seams and the slot table). **CITATION LAW, added after independent verification proved the first cut's central promise unenforced.** A warrant is this register's anti-fabrication device — a reader trusts the quotation instead of opening the spec — and the first gate only regex-checked its FORMAT, so `25-…-deep.md:99999 — "the medicine wheel SHALL be rendered in interpretive dance"` passed green. The gate now OPENS the cited document and asserts the quoted text really sits at that line (`CITABLE_FILES` + `citedSourceLine`, compared on content so backticks and curly quotes are immaterial), plus a separate check that rejects a paraphrase wearing quotation marks. That caught a real defect immediately: 25.1's warrant had elided ` in \`src/common/index.ts:11\`` and closed the paren early, so it was not the quotation it claimed to be. **SPEC IS CITED BY LINE; SOURCE IS CITED BY ANCHOR.** The register's most-cited target moved three times before anyone first checked it (`crossLayoutIntent.ts` :86 → :115 → :143) and every stale number still read as a fact, so prose fields may no longer carry a `.ts`/`.tsx` line number — the gate rejects them — and `SourceCitation {file, anchor, mustBeAbsent?}` is the checkable channel instead. `mustBeAbsent` is what lets a GAP cite anything real: an absence cannot be evidenced by a file that CONTAINS the thing, so a gap names where the surface WOULD be registered (shell factory, intent table) and proves it is not there. Three of six gap rows had carried pure prose while the field's own doc promised `file:line`, and the gate measured only its LENGTH. Six further lie classes are now closed: a symbol must be DECLARED, rendered, or emitted as a handle (a bare `includes` accepted `'e'`); `face` is checked against the body of the model builder that claims it; `landedBy` must name a tranche the carrier's own header mentions; every citation resolves; the cross-coordinate ruling is read back out of the file that decided it; and — because rootness is a DOM fact, not a source fact — the browser receipt asserts each declared testid has no handled ancestor inside its tab panel, which is the one lie that had survived BOTH gates green. The layout-law receipt also stopped hardcoding one view id: 52.T4 made twelve rows `daily-only`, so it now derives the set from the register and drives 52.T3's real `layout.switch.ide-deep` instead of the side channel that tranche removed. Canon update flag: [[M4'-SPEC]] should record the Wave-C view-id register, the brief-over-barrel resolution rule, the SPEC:290-vs-29.T29.3 slot divergence, and the spec-by-line/source-by-anchor citation law.
- `scripts/__fixtures__/live-wire-capture.json` — **cross-lane refresh.** Sibling commit `a52ec4d6` (24.T24.7) added `s2.codon.scalar_ref.read` to the REQUIRED projection manifest and to the live gateway but never refreshed the replay fixture, so the live bus carried it and the committed capture did not — reddening `app-test` repo-wide. Refreshed from a green live capture taken under the gate lane; all 11 live-wire tests pass, including the corruption-rejection cases that manipulate specific frame shapes. Flagged for Track 24: a new required projection has to land WITH its fixture.
- `src/composition/compositionLoad.ts` — **29.T29.6 / DR-WC-IP-5**, closing 15.4's verification: the ONE composition loader, and the two laws kept apart by kind. A CONTRACT breach (side-by-side compact view, raw personal body on a slot, two owners on one slot, a contributor with nowhere to live) hard-fails the mount and now names EVERY offender with a sentence its author can act on — the previous loader stopped at the first, which made an author fix one, re-run, and find the next, for no gain since the mount is refused either way. A RUNTIME absence is different in kind: the contract holds, the owners are real, and a field the kernel has not transported leaves one slot unable to paint — so the mount SUCCEEDS, carries its 29.5 readiness envelope, and reports the degraded slots rather than taking the whole surface away because one pole is waiting. `geometricSlotEnforcement.rejectionFor()` is the single per-contributor verdict both loaders call, so they cannot drift on what the law is, only on how much of it they tell you. `loadCosmicComposition`/`loadPersonalComposition` are now partial application of this loader, so there is one result shape and one implementation; the engines read owners through `ownerOfMountedSlot`. The raw-body refusal is kept universal rather than scoped to the personal layout as the spec words it — the forbidden classes are personal material and a cosmic contributor declaring a journal body is just as wrong. Canon update flag: [[M'-SYSTEM-SPEC]].
- `src/composition/kleinFlipChoreography.ts` — **29.T29.7 / Track 07 §7.7 (DR-IG-3)**: the composition finally SAYS the fold happened. The three-pole choreography was already atomic and already timer-free — the modulation graph parses the Klein flip off the profile, flips valence and rotation axis together, stamps `flipAtMs`, dispatches ONE frame to every carrier, and `deriveKlein` turns that stamp into `foldProgress` (0..1 folding, >1 settled) on the rAF loop; the spec's director-with-three-handles is carried by that one graph. What did NOT exist: 29.11 declared `composition.kleinflip.choreography.start`/`.end` and NOTHING in the repository emitted either, so the Dispatch Trace showed no fold and no observer could tell whether the three poles moved together or at all — the same declared-and-never-emitted shape as `portal.vak_eval` and the slot law before 29.2. Now an observability carrier registered on the cosmic graph emits at the two boundaries only, naming the poles the fold moved. It is a CARRIER, not a timer: `.end` fires on the frame where `foldProgress` first crosses 1, because 15.9's "no parallel animation timer competes" would be absurd to honour in the fold and break in the reporting — a fake-timer test asserts that constructing it and waiting emits nothing. The phase rule is pure, so the emission is testable with no clock at all. Canon update flag: [[M'-SYSTEM-SPEC]].
- `src/engine/matheme137Overlay.ts` — **29.T29.8 / Track 07 §7.2**: the Third Spanda spine as an ANNOTATION DIRECTIVE on the cosmic composition — 64-side on the M3 lens-ring, 72-side on the M2 cymatic surface, the `+1` bridge at the K² equatorial seam labelled with the translation rule, the 7-8-9 crown orbits, the Mersenne proof-mode label and the Kaprekar chip. DIRECTIVE, NOT RENDER, per the tranche's own division of labour ("composition issues the directive; played-torus renders"): it states WHAT the annotation says and WHERE it belongs and draws no mesh — a test asserts the directive carries no coordinates or geometry, because a second renderer here would fork the surface (22.2 owns the mesh). COMPOSED FROM THE LAW, NEVER RESTATED: every string and number comes from `compositionMatheme.ts` (Track 07.T7.2) — the five canonical forms, the QCD octet/singlet form, the translation-rule symbol, the 7-8-9 roles, and `assertParentAttribution`, which throws on the forbidden `M0-Anuttara-witness` rather than rendering it; a second copy of `137 = 128 + 8 + 1` here would be a place for the two to drift. The skeleton-event read reuses `m1KaprekarInspector`'s existing path rather than forking a second parser, and `Additive137` is accepted by declared NAME only — 19.8 states a wire ordinal for `KaprekarPedagogyHit` alone, and guessing one for the other would invent a wire contract. Rendered in the cosmic strip with the canonical forms as its hover; the strip is hidden at screenshot time, and the VR baselines were re-run to confirm no pixel shift. Canon update flag: [[M'-SYSTEM-SPEC]].
- `src/engine/contemplationFlowDirector.ts` — **29.T29.9**, closing the 19.6/19.7 composition path: the `contemplate_session_close` triplet read as a directive across the personal 4-5-0 slots — the LLM-Nara reading and its tarot psyche-anchor ribbon on 4'/LEFT, the EBM evaluation on 5'/RIGHT, the nine R-virtue lamps and the symbolic-coordinate questions on the 0'/UNDER-LAYER. NOT EVENT-MOUNTED, because the event does not exist: the brief subscribes to `m5.session.contemplation.complete` "fired by gateway per Track 19.7", but `EVENT_NAMES` (S3 `protocol.rs`) is `agent/chat/tick/health/heartbeat`, `PORTAL_EVENT_NAMES` carries no contemplation channel, the C `m5_execute_mobius_return` cited as its trigger emits nothing and is reachable only from two C unit tests, and every occurrence of that literal in the repo sits inside FROZEN epi-theia. 25.T25.19 already met this gap on the session-close ceremony and ruled *read the close, do not subscribe to a ceremony that never fires* — this follows that ruling, so `composition.contemplation.complete` (declared in `COMPOSITION_EVENT_TYPES` since 29.11 and until now emitted by NOTHING) is what this composition EMITS once its three slots land, not what it waits on. TWO SOURCES, ONE DIRECTIVE: the live `nara.contemplate_session_close` response carries the whole triplet, but the app never originates a `ContemplationObject` (the loopback close path does), so a mounted composition reads the PERSISTED projection and names the five readings that projection drops (`liveOnlyPending`) rather than showing them as absent. READ VERBATIM, NEVER RE-SCORED: the three Klein-V4 tritone squares, the gauge-trio verdict, the gradient magnitude and `arch9_wholeness` are all copied through — 26.1 fixed that law for the EBM surface, and re-deriving arch-9 here would repeat the frozen epi-theia director's error of reading the Parameśvara witness off `virtueBits[0]` (Love/Peace) while the gateway reads bit 8 (Reality — Completion, VIRTUE_LUT index 8). NO SECOND RPC for the symbolic chips: `anuttara-symbolic-parse` is a PI agent skill with no gateway registration, and S3 has already made the round trip server-side and returns `llm_response` with each parsed question. NAMED, NOT FABRICATED: six spec-ahead asks have no source on any wire — the 16-byte wisdom-delta XOR tape (the wire's `wisdom_delta` is composed PROSE; the kernel's `uint64` reaches no gateway surface), the three gauge-coverage bars (the wire carries one BOOLEAN), the 4-charge balance (`M5_ArchNineChargeState` is C-only), the 72-dim predicted/target overlay (that is 26.1's surface off the profile bus), the Möbius quaternion arrow (a scalar magnitude, not a quaternion), and the canvas inscription WRITE (`khora_write_highlighted_inscription` is a PI extension tool, not a gateway method) — each is listed with its reason and a test asserts the directive fabricates none of them. WHICH HALF PRODUCTION CALLS, stated because the file does not make it obvious: production imports `useContemplationFlowDirective` and `formatContemplationReading` only — the LIVE-response half has no production caller, because the carrier has no producer of a `ContemplationObject` and inventing a trajectory app-side would fabricate the session it claims to contemplate. It is the parser for the response 19.6 really returns, proven against a real gateway, and what a carrier-side close path would consume the day one exists. Proof is split by the layer that can give it: the pure directive in `contemplationFlowDirector.test.ts`, the wiring in `contemplationFlowDirectorHook.test.tsx`, the live-response SHAPE against a REAL spawned gateway in `bridge/gatewayClient.live.test.ts`, and the real browser in `tests/e2e/personal-contemplation-flow.spec.ts` — which OPENS AND CLOSES A REAL SESSION through live `nara.session_close` before asserting, because the e2e harness makes a fresh state root per run and `awaiting-close` is exactly what a broken persisted read also produces; an empty-reading assertion could not tell working from broken. It then asserts this closure's own 7-of-9 witness vector, all three slot positions, both named lists, and `composition.contemplation.complete` arriving in the OmniPanel dispatch trace. Mounted on `PersonalRecognitionEngine` as data attributes only — that surface rides the `composition-4-5-0-personal.png` baseline, and NOT as `title`, which would give the `<section>` an accessible name (turning the engine into a landmark named by a runtime gateway-state sentence over its own `<h2>Recognition</h2>`) and hang a native tooltip over the whole surface. Canon update flag: [[M'-SYSTEM-SPEC]] should record the contemplation directive and its live-vs-persisted source law.
- `src/panes/contemplationObjectViewer.tsx` — **29.T29.9 drive-by fix**: two strict readers of `nara.session_close.contemplation.read` disagreed about the payload it really returns. 25.20 widened the substrate projection with per-card `anchor_cards` and the sibling reader (`panes/m4SessionCloseCeremony.ts`) took the key; this 26.12 reader did not, so its `rejectsUnknownKeys` blocked every close persisted after that widening with `unexpected triplet.llm field anchor_cards`. Accepting the key does NOT relax the aggregate-only law — the viewer still reports counts, and the raw `matched_anchor_codons` body stays forbidden. Regression-tested red-green.
- `src/composition/integratedDeepLinks.ts` + `src/commands/crossLayoutIntent.ts` — Tranches 28.T28.14/29.T29.14's active-carrier intent completion: the eight frozen ide-shell capabilities resolve onto mounted carrier hosts, `term:{value}` and `capacity:{id}` are strict handler aliases, and the two `epi-logos://ide/integrated-*` composition routes persist validated hints before dispatching the unchanged nine-field envelope into `daily-0-1`. No tenth envelope field or parallel router exists. Canon update flag: [[M'-SYSTEM-SPEC]] / [[M5'-SPEC]].
- `src/onboarding/firstSessionOrchestration.ts` + `contracts/onboarding-completion-ledger.json` — Tranches 32.T32.11/13's first-day boundary: the explicit empty-journal gesture sequences live `vault.day.ensure` -> `khora.session_start` -> optional `nara.kairos.sync` -> `nara.session_open`, then records `first-session.start`; the executable ledger validator enforces the exact 20-step set, completion/skip law, Track-32 ownership, and no collision with AST-discovered carrier view ids. Canon update flag: [[M4'-SPEC]], [[S1-SPEC]], [[S3-SPEC]], and [[S4-SPEC]].
- `src/composition/M3CodonRotationProjectionForLensRing.ts` — Tranche 24.T24.13's immutable [[M3']] read boundary for the integrated 1-2-3 engine: projects the one authority-bussed active codon cell, canonical charge-quaternion aliases, line-change address, tick, and profile generation onto the [[K2]] lens ring without owning or mutating geometry.
- `src/ui/waveCDesignSystem.ts` — Track 20.T20.2's typed Track-D source: the ten Wave-C widget-layer invariants, their consuming enforcement routes, and canonical source-line citations. Later Track 27–32 carrier work imports this manifest instead of restating the cross-cutting contract.
- `src/ui/primitives.tsx` — the carrier's Track 30 primitive shelf: single-source provenance borders, bracket-aware family-tinted coordinate strings, real-gateway amino-acid codon strings, accessible Unicode hexagram/change witnesses, verifier-authored symbolic-coordinate decomposition, pending/readiness states, blocked recovery overlay, Cl(4,2) palette, motion configurations, and bedrock provenance tooltip. `M3ThirdSpandaPanel` and `M3IChingCastRibbon` consume the governed primitives; no browser codon or hexagram identity LUT exists. Canon update flag: [[M'-PORTAL-SPEC]], [[M3'-SPEC]], [[S0-SPEC]], and [[S3-SPEC]] should record the admitted `s2.codon.aa_lookup` dependency.
- `src/ui/highlightCategoryRegistry.ts` — rerun 30.T30.7's canonical `HighlightCategoryRegistry`: the promotion of 11.11's ten-category register (4 user-authored + 6 agent-authored) to ONE source, replacing the three private copies that `panes/m4NaraHighlightMark.ts`, `S4-0p-khora/modules/highlighted-inscription.ts` and the Chronos orbit constant had each been holding. Each entry carries side, `colourToken` + the `--nara-highlight-*` custom property, a real semantic meaning, `allowedSourceExtensions` (user-side is `m4-nara` ONLY; agent-side is the S4 triple), `floatingMenuEligible` (user-side only — an agent category can never reach the FloatingMenu), the 11.11 `visualRegister`, and the **19.11 `responseOrbit`**, because that alignment is structural rather than incidental: the orbit IS the inscription (`immediate`→`recognition`, `next-morning`→`retrospective-surfacing`, `saturnine`→`prospective-surfacing`), and a category with no fixed orbit says `undefined` rather than defaulting to one. The user/agent partition is a COMPILE-TIME union split (`UserHighlightCategoryId` / `AgentHighlightCategoryId`), not just a runtime `side` field, so a user-only surface can refuse an agent category in its types; a test asserts the two never disagree. `m4NaraHighlightMark` now DERIVES `USER_/AGENT_HIGHLIGHT_CATEGORIES` and `HIGHLIGHT_VISUAL_REGISTERS` from the register — nine of the ten ids are no longer spelled there at all, and `daily-note` survives only as a parse fallback (asserted line-by-line). **Cross-stack shape is mirror-and-check, never import**: the 30.7 brief asks the S4 consumers to import the register, but M→S is a forbidden edge (the same law behind `ui/bridgeReadiness`), so `highlightCategoryRegistry.test.ts` READS the Khora and Chronos sources and fails when their vocabulary diverges — injection-proven (renaming `somatic-mark` in Khora fails the parity test by name). Canon update flag: [[M4'-SPEC]] should record the register as the category authority and the orbit↔category alignment as law.
- `src/ui/stateGrammar.ts` + the five state-grammar primitives in `src/ui/primitives.tsx` — rerun 30.T30.6's **empty / loading / pending / blocked** grammar. Four states a binding can be in with no datum (`EmptyState` — no data and no blocker; `LoadingPulse` — awaiting; `PendingBadge` — a named field absent; `BlockedOverlay` — refused) plus `ReadinessIndicator`, the 8x8-dot / 16x16-icon chip that annotates one that has data. All five are typed on the nine-id [[bridgeReadiness]] taxonomy, all five surface `ownerTrack` in tooltip and `aria-label` (so a reader can tell WHICH axis is down and WHO owns it), and all five colour from `epilogos.colour.readiness.id.<id>` — per id, never one generic amber. `ownerTrack`/`meaning` are MIRRORED into `src/ui/bridgeReadiness.ts` from the frozen `contracts/07-t0-…-preflight.json#readinessTaxonomy` (M→epi-theia is a forbidden edge); `severity`/`tier` stay COMPUTED so there is one severity law. `BlockedOverlay`'s call-to-action is derived per id and routes through the ONE command registry (`s5_review_blocked` → `omnipanel.openReview`); an id with no honest recovery renders NO button — privacy is a governed act, not one-click consent, and the automatic gateway reconnect means `bridge_unavailable` opens Diagnostics rather than faking a Reconnect. `LoadingPulse` obeys Foundation principle 2: opacity is a pure function of `tick12` with NO local clock, and only `bridge_unavailable` falls back to a 200ms cycle that is a CSS animation, never a JS timer (so 30.11's no-`setInterval`/rAF law needs no exception). It is CONTINUOUS motion per [[30.T30.5]], so reduced motion stops it dead. `src/ui/primitives.test.tsx` holds the 9x5 acceptance matrix, the ownerTrack reason-routing, the deep-link proof, an injection-proven consume-not-fork lint, and the styles.css↔tokens.ts parity + WCAG-AA assertions; `tests/e2e/design-primitives.spec.ts` is the real-shell UF proof. Canon update flag: [[M'-PORTAL-SPEC]] should record the state grammar as the carrier's binding-level rendering law.
- `src/ui/readinessGrammar.ts` + `contracts/readiness-state-grammar.{md,json}` — rerun 32.T32.5's **per-readiness-state UX grammar**, closing DR-WC-OB-1 (retarget of the frozen epi-theia `extensions/contracts/readiness-state-grammar.*` per CHARTER rule 2). The resolution is structural, not asserted: contract authority stays exactly the nine [[bridgeReadiness]] ids, and the five UX flavours (`pending_first_tick`, `s3_gateway_unreachable`, `s5_atelier_blocked`, `pending_dataset`, `ready_protected_local`) each declare a `parentState` among those nine, so a flavour CANNOT be free-standing — `readinessGrammar.test.tsx` refuses one whose parent is outside the taxonomy. The grammar does **not** re-declare what 28.11 already decided: `shell`/`tier`/`severity`/`recovery` are COMPUTED from `needsWrappingShell`/`readinessTier`/`readinessSeverity`/`readinessRecovery`, so the JSON is a machine-checked SNAPSHOT rather than a second source of truth (the test fails if file and code disagree); the grammar's own contribution is the per-state copy plus the flavour layer. `flavourOf(state, snapshot, context)` takes a context because the snapshot genuinely cannot answer two of the five — it cannot tell "bridge down" from "bridge up, no tick yet" (both read as an absent binding at tick −1) and carries no privacy class — so `useReadinessFlavour` reads those from the stores that own them and a flavour with no context to stand on returns **null**, which is a real answer rather than a guessed variant. `BridgeReadinessBadgeView` carries `bridge-readiness-state-{id}` on every render and layers `bridge-readiness-flavour-{flavour}` only when one fires; the two ALIAS flavours deliberately carry no CSS of their own, since an alias renames a state for the reader and must not make one condition render two ways. **Two recorded divergences from the 2026-06-02 spec prose, both because later landed law wins:** that table calls `s2_graph_blocked`/`s3_subscription_blocked` "overlay", but 28.11(a) keeps the wrapping shell for `bridge_unavailable` alone and renders finer states inline at the datum (15.6); and `bridge_unavailable` opens Diagnostics rather than a "Reconnect" button, which `readinessRecovery` already decided and reasoned, so the grammar points at it instead of forking it. `tests/e2e/readiness-state-grammar.spec.ts` is the UF proof — it rides badges the app really renders (the M3 navigator's `cosmicClock.walks` binding resolves `bridge_unavailable` for real), sweeps every readiness node the live face paints, and carries the negative that matters: ticks are flowing, so `pending_first_tick` must NOT fire. Canon update flag: [[M'-PORTAL-SPEC]] should record the flavour layer as the render-time variant law over the nine-id taxonomy.
- `src/ui/{emptyStateRegistry.ts,emptyStateGrammar.ts,mExtensionEmptyStates.tsx}` + `scripts/lint-empty-state-registry.mjs` — rerun 32.T32.6's **empty-state grammar per extension** (closes O-WC-OB-2; cross-link 21-26 stage 1). Two recorded retargets off the frozen Theia shape: (1) "per extension" becomes per M-family **contribution** — `viewId` is a contribution id declared in `src/commands/crossLayoutIntent.ts#CROSS_LAYOUT_INTENT_TARGETS` (`m0-anuttara/language`, `m1-paramasiva/playedTorus`, `m2-parashakti/cymatic`, `m3-mahamaya/wheel`, `m4-nara/journal`, `m5-epii/review`), never the dead `m3-mahamaya.primary` Theia view id; (2) `EmptyStateRegistryImpl (@injectable() @singleton())` bound in `frontend-module.ts` becomes a module-level singleton, since Theia plumbing is dead under the carrier contract — the CONTRACT (`register`/`resolve`/`all` + a disposable) is kept exactly, including the law that a STALE disposable must not delete the registration that replaced it. The six copy blocks are the spec's own strings (`:180-185`) verbatim, split into header/summary/hint; the shape is header + summary + missing-contributors + reasons table, replacing single-`<p>` empty messages at all six surfaces. **It consumes the landed 30.6 `<EmptyState>` primitive rather than forking one** — before this tranche that primitive had ZERO production call sites; the one addition to it is an optional `actionTestId` so `JournalTimelinePane` keeps its 32.11 `start-first-session` button instead of growing a second beside it. Nothing in a row is invented: `emptyStateReasonRows` derives readiness id, reason and `ownerTrack` from `classifyReadiness` + `readinessGrammar`, the reason falls back to the id's canonical copy only when the bridge gave none, an UNREPORTED binding is honestly `bridge_unavailable`, and `data-activated` separates a real block (table drawn) from a surface that is *merely* empty (no table — an empty table would assert a block nobody reported). Per-Mn differences are only where the spec asks: M1's cold-start step is read from the real onboarding completion ledger, M4's PASU-incomplete warning fires only when the wizard was not skipped (32.7 "if applicable"), M2's pending-dataset chip fires only on a reported `authority_payload_missing`. `scripts/lint-empty-state-registry.mjs` (`pnpm lint:empty-states`, chained into `pnpm test` before Vitest) is the registry-completeness gate and is **bidirectional** — it DERIVES the M-family extension set and the legal `viewId`s from the target ledger's AST rather than restating them, then flags `unregistered-extension`, `unknown-target`, `duplicate-registration`, `unmounted-registration` (a registration whose declared `mount` surface renders nothing — registered, never fired), `unregistered-mount`, and `unverifiable-mount` (production props it cannot statically read). Test files are excluded from the MOUNT scan only, because a mount inside a spec proves nothing about a production surface. Proven RED-then-GREEN against the REAL tree in both directions, not just on fixtures. `tests/e2e/empty-state-grammar.spec.ts` is the UF proof: it rides a condition the app reaches on its own (no coordinate is selected on boot, so the M0 language reader is genuinely idle) and carries the negative that matters — after the Walk publishes a live coordinate the empty state must be GONE. Canon update flag: [[M'-PORTAL-SPEC]] should record the empty-state registry as the per-contribution law over the nine-id taxonomy.
- `src/ui/{errorUxGrammar.ts,InlineErrorSurface.tsx}` — rerun 32.T32.7's **error UX grammar + Diagnostics deep-link** (cross-link 15.2). Where 32.6 frames "nothing has arrived yet", this frames "a call FAILED". The four spec paths (`:226-230`) are ONE declared table, each row carrying `status: 'live' | 'unwired'`; `errorUxGrammar.test.ts` refuses a live row whose `deepLinkCommandId` is not in `COMMAND_CATALOG`, and refuses an unwired row that does not say why — so a declared path can never be a route to nothing. **The deep-link is not minted here.** The frozen spec fires `commands.executeCommand('omnipanel.openTab', 'diagnostics')`, a command this carrier does not have; the carrier route is the CCT-4 chord command `omnipanel.tab.activate.7` (⌘8), which `readinessRecovery('bridge_unavailable')` ALREADY names, so `DIAGNOSTICS_DEEP_LINK` reads that recovery instead of restating an id. `<InlineErrorSurface>` is declarative rather than the spec's imperative `showInlineError({...})` mount — a recorded retarget: Theia widgets have no owning render tree, this carrier does, and every failing site already holds its failure in local state, so a module-level error bus would be a second source of truth for state the panes own. Three LIVE path-1 sites: `TryItAffordance` (a user-chosen gateway method — the most direct kernel-bridge call the carrier has; a client-side JSON refusal is deliberately kept OFF the Diagnostics route because it never reached the bridge), `ContextPackSection` (retry is its own `refresh`), and `MarkdownEditorPane` (the read effect is keyed on a retry attempt, so retry RE-ISSUES the real `vault_read` rather than redrawing a cached refusal). Path 3 adds the route out of a blocked integrated readiness at `CosmicEngine`/`PersonalRecognitionEngine` — that blocked id is the nine-id `profile_missing_field`, whose taxonomy recovery is the same Diagnostics route (asserted, so a divergence fails loudly). Path 4 lands inside 32.6's M4 empty state: kairos ENABLED with no PASU identity at all warns in the grammar's exact words and routes to `identity.openWizard`, replacing the softer "incomplete" notice rather than stacking on it, and staying silent while kairos is off (FR-3's default is not a misconfiguration). `ReadinessBanner` gains `onRetry` plus the taxonomy's own recovery button — a state with no honest recovery (`degraded_but_readable`, `privacy_blocked`) and a named-producer state outside the nine both render none. **Path 2 (extension contract preflight) is declared `unwired`, not silently dropped:** the enforced contract JSON is real and read by `scripts/lint-import-boundaries.mjs`, but the carrier's preflight is BUILD-time (five lints + `src/chromeContract.test.ts`) and writes no artefact; the spec's `validation-results.json` is produced by nothing in this repository, so the banner would link to nothing. `tests/e2e/error-ux-grammar.spec.ts` is the UF proof and it proves the two things jsdom cannot: that a REAL substrate refusal reaches the surface (the app's own `vault.open` on a path the real e2e sidecar's real filesystem does not hold — nothing stubbed), and that the deep-link LANDS on the Diagnostics fold rather than no-opping the way `activateOmnipanelTabByIndex` silently does on a wrong face / unmounted node / absent tab. It also measures non-modality with the error up (no dialog role, and ⌘. still toggles the face). Canon update flag: [[M'-PORTAL-SPEC]] should record the four-path error grammar beside the empty-state and readiness grammars.
- `contracts/ui-family-palette-derivation.md` — rerun 30.T30.8's derivation prose for the 36-token family palette (retarget of the frozen epi-theia `extensions/contracts/ui-family-palette-derivation.md` per CHARTER rule 2). It records WHY each family letter carries its tier hue and each raw archetype its grade — the two axes (`P S T M L C` → TIER, `#0–#5` → GRADE), the six per-tier rationales (P foundation-neutral · S substrate-slate · T thought-parchment · M the canonical subsystem colour-worlds · L epistemic-mist · C ontological-violet with C0/C5 at the cool Cl(4,2)−1 endpoints per Möbius return), and the DR-WC-DL-1 default-vs-rejected-alternative record. **The M-tier is an identity axis, not a ramp** — each M-archetype is its own world, which is why the monotonic-grade law is asserted over P/S/T/L/C only. DR-WC-DL-1 was **VALIDATED 2026-07-23** (the Architect), and its ratified M-worlds (M0 achromatic · M1 deep blue · M2 red/pink · M3 gold/orange · M4 green/teal · M5 purple) **supersede the 2026-06-02 spec prose** (which proposed M0 indigo · M1 amber · M2 violet-tattva · M3 gold · M4 earth-slate-gold · M5 amber-EBM) — follow the DR, never blend the two. Values and resolvers are NOT owned here: they are `src/ui/tokens.ts:92` (`FAMILY_PALETTE`), `:243` (`familyGrade`), `:252` (`coordinateFamilyGrade`), landed by 30.T30.2 and pinned by `src/ui/familyPalette.test.ts`. **Named open seam:** the older flat `FAMILY_HUES` (`tokens.ts:60`, one hue per family, no grade) still tints the `ln` coordinate-string and mark primitives (`src/ui/primitives.tsx:93/:282/:327`), so a rendered coordinate shows its tier but not its archetype grade; migrating those renderers is 30.10/30.13 primitive work (it moves visual-regression baselines), not a token edit.
- `src/ui/iconography.ts` + `src/assets/icons/*.svg` (19) + `contracts/ui-iconography.md` — rerun 30.T30.9's iconography system (retarget of the frozen epi-theia `extensions/contracts/ui-iconography.{ts,md}` + `contracts/icons/` per CHARTER rule 2). ONE icon authority: five left-sidebar-mode glyphs, the 0/1 `coin-flip` chrome toggle, the `lemniscate` transition glyph, six Mn subsystem glyphs, six family-letter glyphs. **The sidebar bindings are DERIVED from `LEFT_SIDEBAR_MODES`, not written beside it** — `SIDEBAR_MODE_ICON_NAMES` is a `Record<LeftSidebarModeId, …>`, so a mode added or renamed there fails typecheck until it names its glyph; drift is structurally impossible rather than policed. The one real name disagreement (carrier mode `bimba-graph` ↔ frozen glyph `bimba-graph-viewer`) is reconciled in that map and recorded in §2 of the contract, not silently smoothed. **Codicons is retired genealogy (DR-FACE-7, same ruling as "activity bar"):** the frozen spec's icon→Codicon fallback table cannot survive the retarget because this carrier ships no icon font at all (zero `codicon` references in the tree) — declaring one would fabricate a dead dependency, so the fallback is the carrier's real convention, the TEXT LABEL, and each `SidebarModeIconBinding.fallbackLabel` **is** its mode's own `label` (asserted equal, not a second copy). Scope is the cosmic pole only: per DR-FACE-7's two-poles-over-one-spine reading the personal `4-5-0` reachables (`commands/crossLayoutIntent`) are deliberately unglyphed, and no rail is rendered here — 52.T6 wires the registry. Authoring law (all five mechanically checked): `viewBox="0 0 32 32"` so one asset serves 16/24/32; `currentColor` only, the sole indirection being `lemniscate`'s gradient whose stops are themselves `currentColor`; **globally unique DOM ids** — the frozen set used `id="title"` in all nineteen files, so inlining any two would have broken `aria-labelledby` for both, fixed on the port; consumed as a mask over `currentColor` (the convention `.face-toggle-icon` already used), so a glyph inherits the colour its consumer resolved instead of forking a palette; and `iconAssetUrl` THROWS on a missing asset rather than returning a broken path, because the register resolves through an eager `import.meta.glob` and an unshipped glyph must be a build defect, not an empty box on a user's screen. `CoordinateString` now renders the family glyph before the coordinate text as a decorative (`aria-hidden`, zero-text) mask — the coordinate's copyable text and accessible name are unchanged. It tints from the same flat `FAMILY_HUES` the string already used, so it **rides the named 30.8 seam above and does not deepen it**: the glyph shows tier, not archetype grade, and moves to `FAMILY_PALETTE` when 30.10/30.13 migrate the renderers. The duplicated `src/assets/coin-flip.svg` was collapsed into the set and its single `styles.css` reference repointed. `src/ui/iconography.test.ts` reads the REAL SVGs through a `?raw` glob (register↔directory bijection both directions, the authoring law, and each resolved URL decoded to prove it serves that glyph's own drawing); `tests/e2e/iconography.spec.ts` is the UF proof that a glyph actually PAINTS — real gateway session, real Chromium, resolved mask, real layout box, `background-color` equal to the family hue — plus the live guard on the coin-flip asset move. Canon update flag: [[M'-PORTAL-SPEC]] should record the icon register as the carrier's glyph authority and the text-label fallback as replacing the Codicon convention.
- `src/composition/compositionEvents.ts` + `src/panes/omni/CompositionDispatchTracePane.tsx` — rerun 29.T29.11's fourteen-type composition event boundary and root-level `Dispatch` timeline. Both faces emit lifecycle/integration events into the existing bounded observability ring; the pane strict-filters malformed records and creates no fifth store or renderer clock.
- `src/ui/dailySurfaceOwnership.ts` + `src/panes/{FileTreePane,GraphExplorerPane}.tsx` — rerun 11.T11.3's exhaustive daily-surface ownership ledger. Journal resolves to `JournalTimelinePane`; `cymatic-placeholder` is superseded by the real `CosmicEngine`; status resolves to the six-thread `StatusStrip`; Library is a YAML-frontmatter coordinate-shelf overlay on the existing lazy file tree; Atelier is the real etymological/cognate cluster colouring inside the existing M0' graph. The frozen active-run `agent-checkin` promise has no equivalent receiver in the active carrier and is explicitly retired; Pi Chat dispatch chips and Logs are named observation successors, not mislabelled active-run control. No standalone daily/library/atelier extension exists.
- `src/ui/layoutClaims.ts` — rerun 11.T11.4's active layout-claim boundary. `pratibimba.smart-connections-sidebar` is explicitly `code-pending` in `ide-deep`, gated by 03.T6.5 with frozen provenance at `Body/M/epi-theia/extensions/MIGRATION-SOURCES.md` and delivery owner 28.T28.12. `App.tsx` resolves the claim on every active-layout render, exposes its id as shell diagnostics, and deliberately mounts no receiver; a future `landed` claim fails closed unless its component is present in a live FlexLayout model.
- `src/ui/preferences.ts` + `src/ui/preferences.test.ts` — rerun 31.T31.9's ONE preference-key authority (retarget of the frozen epi-theia `m-extension-runtime/.../epi-logos-preferences.ts`: Theia's `PreferenceContribution` + Inversify binding is dead plumbing, the LAW — one declaration point, one namespace, a default and a description per key — is live). `PREFERENCE_KEYS` holds every `epi-logos.{area}.{setting}` literal the carrier uses and the six declaration sites now ALIAS it (`themeStore`, `omnipanelRuntime`, `kairosEnablement` ×2, `pasuOnboarding`, `walkthrough`). This was not tidiness: `epi-logos.onboarding.pasu-skipped` was a named constant in `onboarding/pasuOnboarding.ts` AND an inline literal in `onboarding/firstSessionOrchestration.ts`, so renaming one would have left the other reading a key nothing writes — the user's PASU skip would stop being remembered with no error and no crash. **The register describes the app as it IS**: six live keys, each carrying its real default, its owning tranche and the module that reads it; the 31.9 spec's other three are recorded in `SPECIFIED_PREFERENCES` as `pending`/`superseded`/`declined` rather than declared as live keys nothing reads (a registered-but-unfired seam). Two of those carry corrections worth keeping: `epi-logos.kairos.enabled` SUPERSEDED by the landed `epi-logos.privacy.kairos-enabled` (the opt-in is a privacy decision), and `epi-logos.motion.reduced` DECLINED because reduced motion is read from the OS `prefers-reduced-motion` query — a carrier preference could contradict the platform accessibility setting. Enforcement: the sibling test walks the real `src` tree for preference-shaped literals and fails on any the authority does not declare, subtracting command ids against the real `COMMAND_CATALOG` (commands share the `epi-logos.` prefix — `epi-logos.help.openWalkthrough` is a command, not a preference) so neither register can hide behind the other; proven RED-on-injection and GREEN-on-removal, and it asserts it scanned >100 files so a green scan cannot mean it looked nowhere. The layout and theme enums are CONSUMED from `layoutId.ts`/`themeMapping.ts` by identity, never restated, so a preference can never offer a layout the app cannot mount. Owns no STORAGE — each consumer keeps its own read/write path. Canon update flag: [[CHROME-CONTRACT]] should record the preference register alongside the command catalog, and 31.12's contributions catalog consumes both lists.
- `src/ui/settingsSections.ts` + `src/panes/omni/SettingsPane.tsx` + CHROME-CONTRACT §2 `omniSettings` — rerun 32.T32.4's Settings UX surface (retarget of the frozen epi-theia `m-extension-runtime/.../epi-logos-settings-page.tsx`: Theia's `PreferenceContribution` + `PreferenceProxy` + Inversify DI is dead plumbing and "Preferences → Open Settings (UI)" is a dead menu path; the LAW — six named sections, a declared scope each, every preference read in one place — is live). **Settings is a FOLD, not a dialog** (15.2 "the tab IS the landing surface" + CCT-8): a settings modal is precisely the blocking layer `lint:no-modal` exists to refuse, so it joins the OmniPanel manifest, which goes 9 → 10 (`omniSettings`, both layouts — a preference is not a property of the layout you happen to be in). Adding the fold is a contract-surface move and it is recorded as one: §2 gains its row, and the per-fold state record `SettingsTabState` joins `OmniPanelPerTabState` — the type system REFUSED the fold until it did, which is that map working as designed. **THE HONEST SHAPE is inherited from 31.T31.9, not overturned.** The 32.4 spec lists ~20 preferences; six exist. A section renders its LIVE keys as working controls and every other key the spec names as a `pending`/`superseded`/`declined` disclosure row carrying the register's OWN note — never a switch that moves nothing, because a settings page whose controls do nothing is worse than no settings page: it lies about what the app does. All three verdicts are really in use, asserted. **No preference key is spelled in this module** — the first cut hand-listed the eight disclosures and 31.T31.9's guard (`ui/preferences.ts` is the only place in `src` that spells a key) correctly rejected it; the fix made the documented rule EXECUTABLE instead, a `DISCLOSURE_AREA_SECTION` map over the key's `{area}` segment (`privacy.*`→Privacy, `motion.*`→Motion; the four with no matching section go where their subject lives — `kairos` is a privacy opt-in, `profile` is motion, `keymap` is chrome, developer/per-surface modes are diagnostics), so disclosures are derived from `SPECIFIED_PREFERENCES` and cannot drift from it. **Every write rides the seam that already owned it**: theme → `useThemeStore.setSelection`; kairos → the probe-gated `runKairosEnable`/`runKairosSkip`, NEVER a raw write of the enabled key — the load-bearing case, pinned by a test, is that enabling while kerykeion is absent leaves the preference `false` and says why, because a direct write would report the temporal ingress as live with no dependency behind it. Two live keys are deliberately `read-only` here (the layout is 52.T3's switch; the onboarding arrays are the ledger's), and each read-only row NAMES its real writer so an uneditable value does not read as merely broken. Reduced motion is reported from the OS `prefers-reduced-motion` query rather than offered as an override (the register declined it; a carrier preference could contradict the platform accessibility setting). `settingsSections.test.ts` asserts the live↔section and specified↔section bijections in BOTH directions against the register and that every action id exists in `commands/catalog.ts`; `SettingsPane.test.tsx` holds the render/scope/default matrix and both kairos probe outcomes; `tests/e2e/settings.spec.ts` is the UF proof — the fold really materialises in the FlexLayout border, the theme control really moves `document.documentElement[data-theme]`, the narrowed section really survives the 0/1 fold (fold state, not component state), and the Diagnostics action really puts the walkthrough on screen. Canon update flag: [[M'-PORTAL-SPEC]] should record `omniSettings` as the tenth fold and the disclosure grammar (live vs pending/superseded/declined) as the carrier's settings-rendering law.
- `src/ui/layoutId.ts` + `src/ui/layoutId.test.ts` — Track 52.T1's ONE layout-id authority. Canon fixes exactly two layouts inside one process ([[M5'-SPEC]] :91, :159); the union was declared four times independently (`OmniPanelLayoutId`, `ActiveLayoutId`, `LeftSidebarLayoutId`, `CrossLayoutId`) and inlined at four more sites (`m1KaprekarInspector`, `m1SurfaceDispatch`, `DiagnosticsPanel`, `ActiveLayoutDisplay` — the fourth was NOT in the track brief's list of three; the track states its audit is not exhaustive and it was found by AST scan, not inference). All eight now resolve to `LayoutId`; `LAYOUT_IDS` is the tuple, `parseLayoutId` the total parse whose daily fallback `parseOmniPanelLayoutPreference` now delegates to — behaviour is unchanged for every input, and this tranche changes nothing that renders. The sibling test is the enforcement: an AST walk fails when a layout id appears in TYPE position anywhere outside the authority, while ordinary VALUE uses (`activeLayout === 'daily-0-1'`, `availableInLayouts: [...]`) stay legal — the distinction is why it is an AST walk and not a regex (house pattern: `src/chromeContract.test.ts`). A mutual-assignability assertion pins the four aliases to the one type, catching drift the AST walk cannot see (an alias widened to `string`). T4/T5 add a real deep layout on top of this domain; the guard is what keeps a seven-site edit from shipping a layout half the app does not believe in. This module names the domain only — it owns no selection, persistence, or per-layout availability law.
- `src/commands/layout.ts` + `src/ui/layoutPreference.ts` + `src/components/OmniPanelLayoutSwitch.tsx` (+ `layout.test.ts`, `OmniPanelLayoutSwitch.test.tsx`, `tests/e2e/layout-switch.spec.ts`) — Track 52.T3's **layout switch**, the mechanism canon names and the carrier did not have. [[M5'-SPEC]] :159 — "the omni panel switches layouts inside one process; there is no second app" — yet `setActiveLayout` had exactly two callers (persistence restore, and a cross-layout intent's `preferredLayout`), so `ide-deep` was reachable only as a SIDE EFFECT of asking for something else. Three commands (`layout.switch.daily-0-1` / `layout.switch.ide-deep` / `layout.toggle`, all catalogued per [[CHROME-CONTRACT]] §11), one addressed control injected into the OmniPanel's OWN border tab strip through FlexLayout `onRenderTabSet` (so the `right` slot stays owned by the omnipanel per `shellSlotPolicy.ts`, and the control STATES the active layout rather than flipping something invisible), and one transition seam in `App.tsx` (`applyLayout` / `switchLayout`) that every layout change — switch AND intent — now goes through. **Every real transition mints the seven-field `createCrossLayoutIdentityReceipt`**; a same-layout switch is not a transition and mints nothing. **Persistence moved to localStorage** (`ui/layoutPreference.ts`) and out of the Tauri `ui_state` blob, which fixed a live defect: the Settings fold's Layout row (32.T32.4) reads the preference register through `browserKairosPreferences(localStorage)` and therefore always reported the shipped default no matter which layout the shell was in — the one row about the layout lied about the layout. Every other live key in `EPI_LOGOS_PREFERENCES` already lives there; the `ui_state` value is now read ONCE at boot as a legacy migration and never written again. **The 52.T2 side channel is closed in the same pass**: `crossLayoutIntent.ts`'s `target()` helper defaulted `preferredLayout` to `'ide-deep'` and 32 of the 55 rows inherited it, so an intent could carry a person across the DCC-07 boundary with no row having decided it. The default is DELETED — every row states its answer — and a row may promote to depth only into a component whose render actually differs by layout (`DEPTH_DIFFERENTIATED_COMPONENTS`: `bimbaGraph`, `m1SurfaceDeep`, `m3Inspectors`, `m3PentadicInspector`); the audit is a sibling test, not a comment. 18 promote, 16 pull back to daily, 21 preserve the user's choice. Three e2e specs that had been entering deep through the accidental Kairos carriage (`m1-surface-face-layout`, `pending-layout-claim`, `bimba-graph-rendering-mode`) now drive the real control, and `medicine-view` asserts the route no longer moves the layout. **Scope**: this is the switch, not the deep layout — `ideDeepDefault()` is 52.T4's, and today `ide-deep` still only WITHDRAWS the three face-0 daily widgets, which is exactly what the e2e asserts. `LAYOUT_VERSION` is NOT bumped: no default model changed. Canon update flag: [[CHROME-CONTRACT]] §2 gained the `layout-switch` row and §11 the register site.
- `src/ui/deepPaneSet.ts` + `src/ui/deepPaneSet.test.ts` + `App.tsx::ideDeepDefault` (+ `tests/e2e/deep-pane-set.spec.ts`) — Track 52.T4's **deep pane set**: `ide-deep` stops being a subtraction and becomes a layout. [[M5'-SPEC]] :91 specifies it as "the full 4+2 surface with M0/M5 IDE chrome, six M-extensions, two integrated plugins, agentic control room"; before this tranche selecting it had exactly one runtime effect — three face-0 daily widgets were withdrawn — so 52.T3's switch had nothing to switch to. **PER-FACE, per [[DR-DEEP-LAYOUT-1]]**: the shell now holds FOUR flexlayout models, one per (face, layout) cell, and `modelOf(models, face, layout)` is a two-axis lookup rather than a derivation from either axis alone — face-independence would have made `face.toggle` a no-op in depth, which is the DCC-07 collapse 52.T2 had just repaired running the other way. The composition is a DECLARATION, not layout JSON in the shell: `DEEP_PANE_SET` (22 mounts — the IDE explorer rail on BOTH faces, M0'-M3' structural depth on `cosmic-deep-main`, M4'/M5' lived-and-governance depth on `personal-deep-main`), `DEEP_PANE_WITHDRAWALS` (8, each with its reason — [[M'-TAURI-PORT-SPEC]] :65 "Shell surfaces preview; subsystem pages deliver depth. **Do not merge them.**"), and `DEEP_PANE_RESERVATIONS` (3). **The partition is total and the negative half is load-bearing**: `deepPaneSet.test.ts` holds all three against [[CHROME-CONTRACT]] §2 in both directions, against an AST walk of the real `App.tsx` registry, and against `CROSS_LAYOUT_INTENT_TARGETS` (a receiver an intent can carry into depth must be mounted there) — and the three designated `pending` deep surfaces (`agenticControlRoom` 28.T28.5, `coordinateTree` 28.T28.6, `backendStudio` 28.T28.13) are RESERVED with their decided slot and model and deliberately NOT mounted, so this tranche lands the layout without pre-empting the surfaces that go in it. §2's Mount column gained a `· **deep:** withdrawn` / `· **deep:** reserved` marker on exactly those rows, machine-held in both directions. **THE OPENING-TAB LAW, found the hard way**: both faces' models render on layout entry and FlexLayout's `tabEnableRenderOnDemand` mounts the SELECTED tab of each tabset, so each model's first declared mount comes up with no gesture asking for it, on the hidden face too. Opening the personal tabset on `m1SurfaceDeep` therefore mounted its embedded `WalkPane`, whose mount effect auto-arrives at seed `'M1'` and publishes it into the ONE shared coordinate store — entering depth silently re-pointed every M0'/M1' subscriber and broke `error-ux-grammar.spec.ts:160` (32.T32.7). `canonUpdateLedger` opens instead, and `DeepPaneMount.mountPublishes` makes the hazard a source-anchored declaration that fails closed if such a mount is ever ordered first OR if the anchor stops matching. The underlying defect is WalkPane's, not the layout's, and is NOT repaired here — flagged for the M1'-walk owner (22.T22.1 / T2.5). `shellSlotPolicy.ts` gained `layoutVaried` and was reconciled: `left` and `main` are what really differ per layout, while `bottom` — previously the ONLY slot declared per-layout — is composed by neither carrier layout. `LAYOUT_VERSION` 23 → 24 and `PersistedUiState` gained `personalDeep`/`cosmicDeep`, so a layout switch no longer discards the tab left open in the other layout. **Scope**: this is the layout, not the 4+2 body — the six M0'-M5' subsystem pages are 52.T5's and nest INSIDE these two deep models; the deep left rail is still hardcoded children, 52.T6 makes the slot genuinely activity-bar-switched. Canon update flag: [[M5'-SPEC]] should record the deep layout as per-face and [[CHROME-CONTRACT]] §2's deep-exception markers as the carrier's per-layout mount law.
- `src/styles.css` base-voice reset + `src/ui/typeVoiceBaseReset.test.ts` — **cross-tranche repair, found by the 28.T28.5 gate and fixed at the layer that owned it.** `tests/e2e/type-voice.spec.ts` (30.T30.1) refused the tranche with `button.diagnostics-deep-link @ 13.33px` ×2 — intermittently, on roughly every other run. The cause was not the failing tranche's pane (which is DEEP-ONLY and never mounts in the daily layout that spec sweeps): the base-voice `font: inherit` reset in `styles.css` listed `input, select, textarea` and **omitted `button`**, even though its own rationale comment names the UA 13.33px default as the reason the reset exists. A form control does not inherit type from the cascade, so the carrier had compensated by hand TWENTY-NINE times — one `font: inherit` per button class — and any class that never received one was a single render away from red. `32.T32.7`'s standalone `DiagnosticsDeepLink` (default class `diagnostics-deep-link`, mounted twice in `CosmicEngine` and once in `PersonalRecognitionEngine`) was the first to miss it, and because those mounts are gated on `integratedReadiness.state === 'profile_missing_field'` they render ONLY inside the boot window before the profile fills — hence the coin-flip. `button` joins the element reset, which closes the class of defect rather than styling the one button that exposed it. **The e2e could not have been the guard**: it catches an off-scale element only when that element happens to be on screen during its sweep, which is why a deterministic sibling now reads the reset out of the real stylesheet and asserts it covers every form-control tag the carrier actually renders in JSX (proven RED on removing `button`, GREEN on restoring it). Comments are stripped before matching — the first cut of that test matched its own rationale. Canon update flag: none; this is 30.T30.1's own invariant, repaired.
- `src/panes/acr/{acrGovernance,acrReviewInbox}.ts` + `src/panes/acr/{AgenticControlRoomPane,PiRuntimeMonitorBanner,ReviewDecisionControls,AbortRetryContinueControls,Iod17ParityMatrix}.tsx` (+ `tests/e2e/agentic-control-room-deep.spec.ts`) — rerun **28.T28.5**'s **Agentic Control Room**, landed into the position 52.T4 reserved for it: [[CHROME-CONTRACT]] §2 `agenticControlRoom` flips `pending` → `live`, `App.tsx::factory` gains its case, and the entry moves from `DEEP_PANE_RESERVATIONS` into `DEEP_PANE_SET` — **DEEP-ONLY**, the carrier's first surface with no daily residency at all, because [[DR-WC-IS-2]] gives the always-on abbreviated render to the `/` membrane and DR-WC-IS-1 gives the governance perimeter to this pane. Not first in `personal-deep-main`, per the opening-tab law: it reads three gateway seams on mount. It declares no `mountPublishes` — those effects are READS, and the only shared-store write it can cause is the OmniPanel routing a user's evidence click. **The T8 contents are COMPOSED, not rebuilt**: 27.T27.10 already migrated RunTree → `DispatchGenealogyTree`, ToolStream → `ToolStreamPanel`, and the deposit form → `panes/omni/evidence/EvidenceDepositForm.tsx` over the live `s5'.epii.deposit`; 28.5 (e)'s Aletheia subagent trace + non-blocking veto (12.19) is already inside the genealogy tree (`AletheiaCrystallisationGroup` + `VetoBanner`); and 28.5 (b)'s DR-M5-1 roster collapse was already landed in `omnipanelCapabilities.ts`. `acrRoster()` PROJECTS those two lists rather than re-enumerating them, so a guardian added there appears here and Sophia cannot re-acquire an actor row by being retyped in a second place. A second form over the same method would be two writers of one contract, and a second `ToolStreamPanel` would be two readers of one dataset with two copies of the same tab state — so the TEMPORAL fold is reached by the canonical 27.9 route (`agentic-control-room.select-run`) carrying the selected node into the `/` membrane, rather than duplicated here: structural fold in the governance pane, time-ordered fold on the membrane, one node identity across both (15.11, [[DR-WC-IS-2]]). **Two methods the 28.5 spec named do not exist, and the surface says so** ([[DR-ACR-1]]): `s5'.review.transition` is registered nowhere — the S5 surface is `submit`/`inbox`/`resolve`/`history` (`epii-review-core::S5_REVIEW_METHODS`) and the transition on an open item is `resolve`, so the decision control rides `inbox` → `resolve` under `m5ReviewGate`; and `s5'.epii.runtime_control` does not exist either, so `<AbortRetryContinueControls>` is an **honest pending-wire surface** — three disabled affordances beside the method name and the reason, the 32.T32.7 precedent, never a button that cannot act. Both are held in `ACR_METHOD_BINDINGS` with the `ui/errorUxGrammar.ts` law (an `unwired` seam states a reason, a `live` one does not excuse itself), rendered as the pane's own "Substrate seams" section, and — the part that matters — the CLAIM is tested against the substrate: `acrGovernance.test.ts` runs `git grep` over `Body/S/**/*.rs` and goes red if either absent method ever appears or if `inbox`/`resolve`/`submit`/`deposit` ever stops appearing. **IOD-17 is three INDEPENDENT faces**, not one answer asserted three times: the live `s4'.mediation.capabilities.list` projection (`agent-permitted` only if it exposes a review-commit capability from `capability-matrix.json`'s forbidden-action set at `standard` entitlement — the drift detector), `enforceHumanGate` asked about an AGENT actor, and this surface's own standing declaration that it never enables an agent committal; disagreement renders 28.9's verbatim `IOD-17 parity violated — gateway will reject any transition`, and an unloaded snapshot is `unknown` rather than an optimistic green. The human gate is an AFFORDANCE, not a dead button: a human-required item refuses until the operator declares themself the human of record, which matches both 28.5 (a) and S5's `requires_human_resolution` (it refuses a non-human `resolved_by`, not a human one). A `backendStudio` RunTree deep-link is HELD with its reason — that surface is §2 `pending` (28.T28.13) — and the `agentic-control-room` cross-layout target is deliberately NOT retargeted off `omniDispatchTrace`. Canon update flag: [[M5'-SPEC]] should record the ACR as the deep governance surface and the deep-only mount class; [[CHROME-CONTRACT]] §2 gained the fourth partition case (carried in depth, absent from every daily model).
- `src/engine/cymaticField.ts` — the M2' cymatic dynamics reference (E4): the seed equation `a·sin·sin + b·cos·cos` actually solved (octet pairs → drivers, nodalQuartet → boundary conditions), byte-hash pinned; the CYMATIC_FRAGMENT GLSL in `CosmicEngine.tsx` is a marked transcription that changes in lockstep. Klein valence swaps the sand mapping only, never the field.
- `src/engine/integratedReadiness.ts` + the [[CosmicEngine]] / [[PersonalRecognitionEngine]] status hooks — rerun 11.T11.8's live Wave-A readiness carrier: adapts the one cached gateway profile into the frozen five-marker gate and renders its typed blocker IDs on both 1-2-3 and 4-5-0 faces. `audio_octet[8]` and `nodal_quartet[4]` remain declared conditional until the M1' performance-event stream is retained by the carrier; no second profile/event store or renderer-derived readiness exists.
- `src/engine/solarSystem.ts` — E5 presentation law (glyphs, sign/decan naming, resonance meaning, provisional `planet:{Name}` selection address — canon address OPEN); the scene renders as a celestial atlas (CSS2D labels, zodiac/decan bezel outside the orbits, Earth-as-centre label, kernel identity per body).
- `src/engine/fibonacciGround.ts` + `src/components/M3FibonacciGroundRing.tsx` — Tranche 24.T24.19's Level-0 [[Fibonacci Ground]] carrier: geometry-only placement over backend-authored 0..59 positions, the compiled 60-digit ground payload, 24 [[CLOCK_BACKBONE]] degrees, cardinal/zodiac anchors, and natal/live Sun markers. The carrier performs no Sun quantization or Pisano derivation; absent additive fields remain explicit pending states. Canon update flag: [[M3'-SPEC]], [[S0-ARCHITECTURE]].
- `src/engine/modulation/` — **the one modulation graph** (Sprint-8 E3, [[INTEGRATED-1-2-3-COSMIC-ENGINE-ARCHITECTURE]] §7.3 carrier foothold): torus = oscillator · lens system = modulator · clock = temporal carrier · profile = patch. Every rendered/sounding surface registers as a `ModulationCarrier` (declared `requiredInputs`, typed `ModulationFrame`s, readiness-gated, atomic klein flip) — no renderer computes or hardcodes its own state. At the one subscription point, `ModulationEngine` strictly consumes the kernel [[KleinFlipEvent]] union (`m1TritoneCrossing` / `m2CymaticValenceInvert` / `m3CodonRotationCross`); all valid variants fan one frame to the composition, while malformed or unknown records are non-events. The engine owns the rAF loop (choreography over the kernel tick, never a clock), 5-tuple determinism, and pause/scrub (720-tick ring; space key + `engine.*` commands). Two lens namespaces never merge: `division` (primary Ground lens 16 plus 16 derived clock apertures — also gears rhythmic subdivision, kernel canon [24,12,4]) vs `tonality` ((lens,mode) 84-state, kernel-owned). The profile carries the derived rows in `phaseSpace.lensCarrier[16]` and the operative primary identity in `phaseSpace.fibonacciGround { lensId:16, role:'primary-ground' }`; local arithmetic is the honest fallback.
- `src/components/CymaticTransport.tsx` + `CymaticField.tsx` + `M2CorrespondencePane.tsx` + `tests/e2e/visual-regression.spec.ts` — Tracks 23.T23.16/17's M2-local cymatic inspection transport and real visual determinism proof: Pause deep-captures the exact currently received profile frame by value while the global profile stream continues, so nested live-profile mutation cannot change a held 72-address or raster; the transport supplies the explicit held state to `CymaticField` because consecutive generations may share one address and address equality is not a pause signal. The bridge does not currently expose a retained profile-snapshot window, so the scrubber is disabled with `pending-tick-snapshot-cache`; its typed future cache seam can render real historical ticks and Klein-flip markers, but it never repurposes the global modulation engine's choreography ring or emits audio. Chromium drives a real gateway, observes the status generation advance, and verifies the held canvas pixels remain static.
- `tests/e2e/fixtures/visual-regression/CATALOG.md` + `src/ui/visualRegressionCatalog.test.ts` — Tranche 30.T30.14's visual-regression baseline catalog (retarget of the frozen epi-theia `ui-visual-regression-catalog.md`; DR-WC-DL-5 is PROPOSED, not validated). The catalog documents the carrier's real fixture set — the 4 committed chrome baselines (`composition-1-2-3-cosmic`, `composition-4-5-0-personal`, `face-toggle-mid-crossing`, `block-host-review-fold`) plus the in-run frozen-tick determinism proofs — and records the honest divergence from DR-WC-DL-5's 30-raw-baseline design (committing live kernel-tick canvas pixels would be a fraudulent baseline; most proposed flagship components are not yet landed). The correspondence gate (`app-test`) is the fixture-presence + catalog-completeness test: it asserts exact bijection between the catalog's declared baselines and the committed PNGs, that every snapshot name is produced by a real `toHaveScreenshot` call in `visual-regression.spec.ts`, and that in-run proofs commit no baseline. `visual-regression.spec.ts` (15.T15.12) remains the capture/UF authority above it.
- `src/bases/` — Track 48's production [[MOC]]/Obsidian Base read carrier, mounted as the cosmic `Bases` tab. `mocBaseRuntime.ts` retains structured YAML parsing, recursive real-vault frontmatter evaluation, embedded/linked `.base` filters, and canvas file-node resolution through `vault_read`/`vault_list`. `basesViewModel.ts` is the pure table/cards/list/image scope/filter/sort/group service; `basesDataSource.ts` owns gateway-only static `s1'.vault.read_file` snapshot and dynamic `s2.graph.query`/`s2'.retrieve` adapters; `BasesViewPane.tsx` refreshes live reads on shared coordinate or graph/vault observability changes and row selection updates the existing coordinate store so its sibling context panel cross-filters. The carrier owns no membership law, graph law, snapshot generation, or vault writes. Vitest evaluates real repository snapshots; Playwright proves both gateway sources and cross-filtering against the real spawned gateway and shared temp vault.
- `src/panes/m0Layers.ts` + `src/panes/m0LayerReadiness.ts` + `src/panes/M0LayerRail.tsx` + `src/panes/m0SurfaceState.ts` + `src/panes/M0SurfaceContext.tsx` — the M0' six-layer surface contract, per-layer provenance, and routing model (rerun 01.T1.1 + 09.T9.1/9.4/9.8 + 21.T21.13/18/20, [[M0'-SPEC]] §The Six M0-X' Data Layers): `M0LayerView` discriminates on `placement`; M0-0'..M0-3' classify their own S2 evidence as canonical/canonical-absent/derived/blocked, while M0-4'/M0-5' remain explicit `bridged_local`/`bridged_public` routes into their owning surfaces. `M0InspectorLayer` short keys and `M0_LAYER_ROUTES` provide tab routes; `m0.layer.*` commands switch local views; one `s2.graph.node` read feeds the layer selector, never a canon write. The App-owned persisted M0 record holds the active local layer, implicate/explicate phase, and reading/authoring mode through daily/deep remounts without a fifth gateway store. The M0-0' language list has seven canonical fields: `c_1_symbol`, `c_1_formulation_type`, `c_1_complete_formulation`, `c_1_form`, `c_1_formulation_breakdown`, `c_1_primary_designation`, and `c_1_name`; the same S2 payload prioritises `c_1_form` for implicate and `c_1_complete_formulation` for explicate, while unprefixed names remain response aliases whose S2 provenance names the canonical property. Its archetype-routing sub-section strictly reads the selected node's `c_1_archetype_index` plus the bussed `m0_routing_lut_snapshot`; its lazy Arch 3/5/7/9 syntax readers consume only those routed rows, the already-bussed contemplation prompt, and (for Arch 9) the existing witness projection, then scroll to the persistent footer without creating LUT, prompt, witness, or navigation state. Graph changes leave the rail only through its M5 governed-review proposal deep-link.
- `src/panes/M0CoordinateSummaryCard.tsx` + `App.tsx` — rerun 21.T21.17/20's compact daily-0-1 cosmic-face entrypoint and M0 continuity owner. The card reads only the shared coordinate/tick/readiness stores, renders coordinate + default M0-0' language aperture + provenance, and dispatches the registered `m0-anuttara/graph` cross-layout intent to the existing `ide-deep` Bimba graph receiver. `App.tsx` persists and supplies the M0 active-local-layer, implicate/explicate, and reading/authoring record through both layouts without another Zustand store; it owns neither graph/canon law.
- `src/panes/m0CommunityClockOverlay.ts` + `src/panes/M0CommunityClockPanel.tsx` — the [[M0-3']] synchronic/diachronic overlay (09.T9.6, [[M0'-SPEC]]): the time/community rail invokes the real `s2.graph.gds.tangent_overlay` method for the shared selected coordinate, strict-consumes its `derivedNodes`/status/privacy boundary without graph writes, and places that reading beside the kernel profile's `tick12`/`degree360` world clock. Optional [[Graphiti]] values are handle-shaped protected-local references only; whitespace/prose bodies are rejected. No renderer clock, fifth store, fabricated GDS result, or episode-body fetch exists; GDS unavailable/projection-gated and absent-handle states render explicitly. `tests/e2e/m0-community-clock.spec.ts` compares the mounted Chromium pane with a separate RPC to the same spawned gateway.
- `src/panes/m0M5LibrarySeam.ts` + `src/panes/M0M5LibrarySeamPanel.tsx` — the [[M0']] graph-chrome ↔ [[M5-0']] Library Klein seam (09.T9.9): `GraphExplorerPane` invokes the production `s5'.gnostic.etymology` read for the one shared traversed coordinate and renders direct `bimba_coordinate` anchors beside relationship-backed `bimba_resonances`. The strict projection carries entity id/name, assignment method, home coordinate, and confidence only; content/body fields never enter the view. A real empty cluster is `canonical_absent`, failures block, and no standalone `bimba-graph-viewer`, fifth store, generic view-mode ontology, or graph write exists. `tests/e2e/m0-m5-library-seam.spec.ts` drives M1 map traversal in Chromium against the real gateway, repository `epi-gnostic` executable, and Neo4j.
- `src/panes/m0VirtueWitness.ts` + `src/panes/M0VirtueWitnessPanel.tsx` — the M0' Virtue Witness in-widget panel (rerun 21.T21.10, [[M0'-SPEC]]): strict-reads the optional bussed `anuttaraWitness` projection, unpacks its 9-bit vector LSB-first in canonical virtue order, renders the labelled 3x3 witness grid and green/amber/red coherence bands, and routes open-question selection into the [[21.T21.11]] console. Absence remains an explicit declared-not-emitted state; malformed wire data blocks. The panel owns no witness calculation or local clock and is mounted in `GraphExplorerPane` beside the six-layer rail.
- `src/panes/m0SymbolicQuestions.ts` + `src/panes/M0SymbolicQuestionConsole.tsx` — the [[M0']] Symbolic-Coordinate Question Console (rerun 21.T21.11, [[M0'-SPEC]]): lists raw live `anuttaraWitness.openQuestions` without browser parsing, submits non-empty protected-local responses with the exact current profile generation through the real loopback-only `s0'.verifier.respond_question` method, strict-reads the gateway parser summary, and displays the opaque persisted-response id. A submission remains `responded` until a genuine later verifier pass reports re-verification. `tests/e2e/m0-symbolic-question-console.spec.ts` drives the route against a real spawned gateway.
- `src/panes/m0Contemplation.ts` + `src/panes/M0ContemplationPromptFooter.tsx` — the persistent [[M0']] contemplation footer (rerun 21.T21.9, [[M0'-SPEC]]): strict-reads the live profile's 12-slot `contemplationPromptLut`, selects only the kernel-clocked `tick12` archetype, and submits a non-empty human response through the existing real `s5'.review.submit` governed-review route. The response preserves prompt/archetype/profile-generation provenance and renders the returned review-item id; absent or malformed LUT data blocks, and the carrier owns no prompt table, clock, or canon write. `tests/e2e/m0-contemplation-footer.spec.ts` proves the mounted footer against a real spawned gateway and persisted review receipt.
- `src/panes/M0LazyNodeBrowser.tsx` + `GraphExplorerPane.tsx` — rerun 21.T21.14's [[M0']] lazy 96-node browser: six branch selectors invoke production `s2.graph.list` in fixed 20-row pages, strict-read its live 108-minus-12 invariant, pin the explicit `M0` root, and write node selection into the one shared coordinate store. It owns no graph query law or archetype table. `tests/e2e/m0-lazy-node-browser.spec.ts` proves the mounted browser against real Neo4j through a spawned gateway and Chromium.
- `src/panes/bimbaGraph/{GraphCanvas.tsx,renderingMode.ts,relationFamilyFilter.ts}` + `src/panes/GraphExplorerPane.tsx` + `src/ui/privacyGate.ts` — rerun 28.T28.3's Bimba Graph Viewer deepening. (a) The shell's `activeLayout` selects the rendering mode: the daily 0/1 surface previews the anchored coordinate as a **solar anchor** (centre + immediate neighbours, cap 6, with the honest `N of M` total and a distinct off-window state — the read is `LIMIT 900`, so "not in the loaded window" must never read as "no relations"), the deep layout renders the **full lattice**. (b) Both renderings live in ONE `GraphCanvas`; the pane owns the read, the state and the status line, never the drawing, and force-graph is handed COPIES because it mutates link endpoints in place. (c) The DR-IG-1 three-family partition is one shared predicate both modes obey; family is READ from `c_1_relation_family`, never inferred. (d) A node click publishes into the one shared coordinate store — the Coordinate Tree host itself stays 28.6-pending, as the §2 contract row already records. (e) Gateway receipts cross the CHROME-CONTRACT §7 privacy gate BEFORE render; a refused class renders a refusal, is counted in the federated `PrivacyDropFeed` (28.16), and shows that count as chrome; the `s2.graph.node` per-binding readiness badge stays at the datum. `tests/e2e/bimba-graph-rendering-mode.spec.ts` proves both modes against a spawned gateway and live S2 (deep renders real nodes AND edges — that edge count was structurally 0 until `graph-services/src/row_projection.rs` made `s2.graph.query` return the caller's own RETURN).
- `src/panes/M0VoidStructureRing.tsx` + `M0LazyNodeBrowser.tsx` — rerun 21.T21.15's [[M0']] 16-fold Void-Structure renderer, mounted only while the active browser branch is `#0-4` Holographic Matrix. It strict-reads the exact `m0_void_structure_ring` public-profile field, renders sixteen accessible 22.5-degree SVG arcs in a fixed 240×240 ring, marks canonical absence/blocked provenance without synthesizing data, and publishes lens clicks through the one shared coordinate store. Conjugate pairs `0/8` through `7/15` share an axis class. `tests/e2e/m0-void-structure-ring.spec.ts` proves the live gateway profile, branch mount, arc count, and shared selection in Chromium.
- `src/panes/KleinTopologyPane.tsx` + `src/panes/m1KleinTopology.ts` — the `m1.paramasiva.kleinTopology` widget (rerun 02.T2.3 / 07.T7.8, [[M1'-SPEC]]): renders the M1-5 single-torus invariants (DOUBLE_COVER_DEG=720 / TORUS_GENUS=1 / Hopf / K² tritone) plus the kernel-owned `m1Topology.torusKnotPhase (p,q)` SSOT from the bridge profile, and fires the `m1.klein_flip.source` observability event when the profile carries `kleinFlip = Some(..)`. `CosmicEngine` exposes that same received pair on its composed root; malformed or absent pairs remain `pending-m1-topology`, never locally derived. Logic ported from the frozen epi-theia `m1-paramasiva` clock-instrument contract (data-test hooks + event type are LAW; carrier convention is `data-testid`) and re-verified against the live portal-core `M1TopologyProjection` producer; wired as the `Klein` tab in the cosmic model. M1 renders only — topology genesis stays in [[Body/S/S0/portal-core]].
- `src/panes/{m1DeepFaceData,M1Cl42SignatureInspector,m1KleinFlipEventStrip,m1KaprekarInspector,m1VortexMatricesBrowser,m1AudioBusInspector}.tsx` + `src/panes/m1SurfaceDispatch.tsx` — Track 22.T22.3/4/7/8/9's [[M1']] analytical faces. The standalone `ide-deep` M1 surface projects the strict bussed vortex `cl42SignatureAtPosition`, `position6`, `tick12`, and full six-family 12×12 `matrixCells` transport through the shared deep-face reader; the vortex browser pins a family and switches raw/digit-root faces only over those kernel-authored cells, while legacy active-cell-only frames stay explicitly pending. It displays the canonical six-position trig/formula/QL-pair pedagogy, the token-owned implicate/explicate halo binary, and the profile-driven 9/8 clock. Its Klein chronograph strictly parses the profile's three-variant `kleinFlip` union, retains a bounded generation-anchored display trail, renders variant/lens-transition/tick provenance, and filters the displayed trail without another store or local clock. The audio inspector read-renders `audioOctet[8]` with profile-supplied ratio roles and sortable position/Hz/role columns, plus `nodalQuartet[4]` m/n, writer metadata, and its explicit M1-reader/M2-1'-writer boundary; it never synthesizes pitch or constraints. The Kaprekar inspector opens the canonical [[m1-prime-kaprekar-pedagogy]] file through the carrier's `vault.open` command only when the bussed `position6` reaches the 7-row trigger or the bussed skeleton event is `KaprekarPedagogyHit`; it owns no local event retention or vortex law. The faces own no profile parsing, palette, clock, or runtime LUT; absent vortex data remains `pending-ananda-vortex`. **Track 52.T2 un-conflated the dispatch's two axes** (`DR-M1-FACE-LAYOUT-1`): `resolveM1SurfaceContext` took only `face` and MANUFACTURED a layout from it (face 1 ⇒ `ide-deep`), so the boot state — face 1 in `daily-0-1` — compressed the eight-slot workbench into the daily shell, exactly the collapse DCC-07 ([[M5'-SPEC]] :107, :161) forbids. It now takes the shell's real `activeLayout` (the `layoutId.ts` type, imported not restated) and gates `standalone-ide-deep` on BOTH axes: face 1 **and** `ide-deep`; face 1 in daily gets the `compact-track-08` preview, face 0 stays the 1-2-3 composition in either layout, and `layoutId` is reported on every branch instead of derived on two. `App.tsx`'s two mount sites pass `parseLayoutId(activeLayout)`; a context-less mount falls back to `DEFAULT_LAYOUT_ID`, never to depth. The three bodies expose `data-layout-id` so the axis is observable in the running shell (`tests/e2e/m1-surface-face-layout.spec.ts`). The face-1 residency of the deep mount is a carrier fact, not canon — 52.T4/T5 revisit it when the deep layout gets its own pane set.
- `src/panes/m1CoordinateTreeContribution.ts` — the Track 22.T22.11 M1 contribution for the pending 28.6 Coordinate Tree host: an immutable 6-strata manifest with the six [[M1-2]] Ananda families, twelve [[M1-3]] Spanda tick positions, and three [[M1-5]] topology constants. Activation writes the one shared coordinate store and returns an honest `walk` or `m1PlayedTorus` carrier target; the contribution owns neither the generic tree pane nor flexlayout switching.
- `src/panes/m1BackendStudioPack.ts` — the Track 22.T22.12 typed [[M1']] source-navigation pack: eight read-only C/Rust anchors for the M1 kernel, Ananda tables, Hopf/quaternion/Spanda operations, Vimarsha, and codon rotation. It names `28.T28.13` as the pending Backend Studio owner and does not create LSP processes, an editor opener, or a substitute pane.
- `src/panes/m4DialogicalArena.ts` + `src/panes/M4DialogicalArenaPane.tsx` — the M4' dia-logical arena carrier pane (rerun 41.T41.7, ported from the frozen m4-nara `dialogical-arena.tsx` and corrected to the REAL eight-route `m4.arena.*` family — the frozen widget's `admit`/`release`/`utter` RPCs never existed; utterances ride `turn_advance`, warm admission stages into the CPF (00/00) wizard and rides `summon`, release rides `vama_release_warm`). View id `m4.nara.dialogicalArena`, "Arena" tab on the personal face. Privacy law: `protected_local_handle_only`, `protectedBodiesProjected: false`, non-user dialogue bodies render as opaque-handle placeholders (41.1 contract). The live gateway answers `unimplemented` for the family (ratified CLI-side substrate shape, epi-cli gate/parity.rs) — the pane declares the real RPC seam and renders the honest `pending-wire` banner until the ws seam lands.
- `src/panes/transformContainers.ts` + `src/panes/TransformContainersPane.tsx` + `App.tsx` — Track 25.T25.11's protected-local `m4.nara.transformContainers` carrier and Personal-face "Transform" tab. The strict parser accepts only coherent four/five-stage receipts over the eight canonical alchemical operations and exact contemplative payload; the pane starts/moves only through `nara.transform.start` / `nara.transform.advance`, requires explicit confirmation before sending a back-step, and exports `M4TransformBadge`. Stage law, active state, NOW mutation, and artifact persistence stay backend-owned. Contract-surface flag: [[M4'-SPEC]], [[S3-SPEC]], [[S1-SPEC]], and [[S0-SPEC]].
- `src/panes/logosCycle.ts` + `src/panes/M4LogosCyclePane.tsx` + `App.tsx` — Track 25.T25.13's protected-local `m4.nara.logosCycle` carrier and Personal-face "Logos" tab. The strict parser accepts only the canonical six-stage cursor (`completed_stages`/`next_stage`, total 6) and, when present, a transition where an advance never carries and a regress must carry `regression: true`. The pane reads `nara.logos.status` on mount, dispatches `nara.logos.advance` / `nara.logos.regress` on the explicit forward/back gestures (regress requires confirmation), renders the A-Logos→An-a-Logos ring with completed/active/pending states, exports the compact `M4LogosChip`, and surfaces an explicit `c_4_regression` marker after a backward move. The cycle law + artifact writes are the S0 authority (`epi-cli nara::logos::advance/regress`, which write the transition frontmatter and, on regress, a separate `{date}-regress-{n}.md` artifact). `tests/e2e/m4-logos-cycle.spec.ts` proves advance→regress over a spawned gateway with an isolated `EPI_NARA_HOME`. Contract-surface flag: [[M4'-SPEC]], [[S3-SPEC]], [[S0-SPEC]].
- `src/panes/M4SessionCloseCeremonyPane.tsx` + `src/panes/m4SessionCloseCeremony.ts` + `App.tsx` — rerun 25.T25.19's M4' session-close ceremony, the personal-face left-border "Session close" surface (a ceremony READS, so it sits with the reading surfaces; an eleventh `personal-main` tab was measured to make the whole strip un-clickable at 1280x800). Read-only by law: it reads `nara.session_close.read` (parsed by the EXISTING `readM1SessionCloseBundle`, not a second parser), `nara.session_close.contemplation.read` (its own deny-unknown parser) and handle-only `nara.pasu.show` for the active session, and never closes, advances or writes anything. Four sections: the delta's HANDLE plus the real 4'-5'-0' triplet, the Möbius section (M1 orbit + octave return + an 8-character quintessence handle — the full hash is truncated in the projection, so it cannot reach a DOM node), the four Arch 3/5/7/9 seeds read from the live 12-slot `contemplationPromptLut`, and nine virtue lamps over the canonical `M0_VIRTUE_LABELS` order. TWO RETARGETS from the brief, both stated in the pane header: it is a TAB not a modal (the carrier's lint-enforced no-modal discipline; `LAYOUT_VERSION` bumped 22 → 23 for the new default tab), and it is READ-ON-DEMAND not event-mounted, because `EVENT_NAMES` carries no `m5.session.contemplation.complete` and subscribing to an event no gateway emits would render a ceremony that never fires. TWO HONEST GAPS, named on the surface itself rather than faked: the composed wisdom-delta TEXT is deliberately dropped by the persisted projection (which reduces bodies to counts), and the kernel's 8-byte `M4_Epii_Integration.wisdom_delta` (m4.h:709) is projected onto no gateway surface — so the brief's "byte trail as hex strip" and its XOR animation have no source, and neither is fabricated. `tests/e2e/m4-session-close-ceremony.spec.ts` drives a REAL close over the live wire (import → `nara.session_open` → `nara.session_close`) and asserts the ceremony renders that closure, lamps included. Privacy chrome `mext-privacy-protected-local-handle-only` via the 25.18 register.
- `src/panes/m4DayContainer.ts` + `src/panes/DayCalendarPane.tsx` — rerun 25.T25.2's DayContainer detail: selecting a lived day in the calendar reads that day's REAL Present tree (`Empty/Present/{day}/{session}/now.md` + artifacts, DR-M4-1) and lays it out as one block per session. The brief extends a frozen `M4NaraWidget` over `naraSurface.daySummary.nowLineage`; the carrier has no such summary object, so the lineage is read off the tree itself. CHIP LAW: every chip renders ONLY when its own key is declared in that session's NOW — `c_3_klein_weighting` is delegated to the existing Track-05 reader (one law, never a second parser), while `c_3_briefing_emitted` / `c_3_tranche_mode` / `c_3_response_orbit` are read here; an undeclared chip is absent, never a default or a zero. Artifact rows carry `c_4_artifact_role` and the `t_4_kairos_context` chip read from each artifact's own frontmatter and NEVER guessed from a filename (an undeclared file is `unclassified`), and clicking one opens that artifact through the same `vault.open` the grid uses. PRIVACY: names, roles and paths only — no body, and per UX §6.5 no quaternion field; the brief's `bodySha256` handle came from a frozen artifact-meta RPC the carrier has no equivalent of, so the artifact's vault path IS its handle and `provenanceHandle` says which it is. Frontmatter reads are capped (`DAY_CONTAINER_FRONTMATTER_READ_CAP`) so a heavy day degrades by dropping CHIPS with a visible note, never rows. `tests/e2e/day-container-detail.spec.ts` seeds a real session through the vault sidecar and proves the rendered detail against it, body-absence included.
- `src/blocks/blockContract.ts` + `src/blocks/block-contract.schema.json` — the binding Track-44 surface standard (rerun 44.T44.1, ported from the frozen m-extension-runtime block-contract with provenance): CTX-framed `Block` (mandatory `ctx {cf, ct, cp}` per DR-PSS-1), the closed 19-type core vocabulary, privacy classes, affordances, `BlockSpec`, and the catalog law — `assertBlockAcceptedByCatalog` rejects types absent from `blocks.catalog` or whose IOD-17 faces disagree (DR-PSS-3). The JSON schema is the Rust-side emission mirror (lockstep pinned by test). `CORE_BLOCKS_CATALOG` is the static registration union until the Techne-operated `blocks.catalog` gateway method lands; 44.2+ (registry/host/projections/verdict-loop) build on this contract.
- `src/blocks/blockRegistry.ts` + `src/blocks/BlockHost.tsx` — the 44.T44.2 native registry + host: `BlockRegistry` (`type → BlockSpec`; the union of registrations IS the live catalog; `noOrphanErrors()` is the Track-14 release-gate feed — all 19 core types owned + spec'd), the designed ownership map + BK-GW gateway-method contract table (ported design law; sourceAnchors re-anchor to carrier surfaces as 44.3+ land), and `BlockHost` — renders each block through its spec's `Read` with catalog acceptance, per-block failure isolation, and the privacyGate enforced BEFORE render (refused data never reaches the DOM; the frozen host under-implemented both laws — corrected here).
- `src/panes/canonUpdateLedger.ts` + `src/panes/CanonUpdateLedgerPane.tsx` — the Track-40 CU-ledger review surface (rerun 40.T40.5, "CU Ledger" tab on the personal face): normalized `CanonUpdateRow` over the real `s5'.canon_update.*` contract (7 categories, 8 forward-only lifecycle states), status filter + per-row inspect (derivation/landing-site/provenance), Track-48 query-view posture. The family is deliberately CLI-side (`epi bimba` + gateway canon_update runtime, outside METHOD_NAMES per gate/parity.rs) — the pane declares the real RPC seam and renders the honest `pending-wire` banner; `tests/e2e/canon-ledger.spec.ts` proves that truth against the real spawned gateway.
- `src/components/{AsmaMirrorOverlay,EpogdoonBridgeEngine,MefGrid72Component,SeventyTwoFoldBreadcrumb,ShadowDecanSurface,SixAxisTree}.tsx` + `src/panes/{M2CorrespondencePane,M2SurfaceContext,m2SurfaceState}.tsx` — the M2' correspondence read surfaces and App-owned interaction record (Tranches 03.T3.10, 23.T23.2-5, 23.T23.7, 23.T23.11, 23.T23.14-18): the MEF grid is a 12-lens x 7-mode scaffold with its 12x6 active 72-address overlay, strict third-Spanda MEF axis view, bussed octet halo source, and profile-driven Klein warm/cool swap; it owns no glyph meaning, clock, or lookup table. The Asma 99+1 strip keeps mirror pair · Jalal/Kamal/Jamal group · active phase on the same conserved `address72`; the sonic face renders six provenance-bound kernel/graph receipt sections (Decan+tattva, Shem pair, Maqam, Mantra, Asma, Planetary-Chakral) from `s2.parashaktiCorrespondences`, never browser LUTs. `SeventyTwoFoldBreadcrumb` strictly renders that same receipt's additive `bridge72` projection (epogdoon hexagram, half-decan, decan, planet, chakra, and Nara body-zone chain), with the bussed position-six colour state; it never recreates a LUT or uses a local body-zone map. `SixAxisTree` preserves a non-empty multi-axis reader intersection across remounts, swaps the 72 invariant for the selected compiled 100-entry Mantra/Asma overlay, and displays the ten compiled planet-key rows; it owns no LUT. The Decan card requests `includeShadowDecans` only when its 108-decans reveal is opened: [[S0]] returns the 36 kernel primary + 36 kernel light cells, live [[S2]] descriptors when present, and the third 36-cell shadow column only when complete; missing [[S2]] shadow data and the unregistered [[M3']] reversed-meaning provider render the explicit pending badges. `EpogdoonBridgeEngine` reads all 72 C-backed `kernelBridge.m2.epogdoonProjection(address72)` receipts once, validates their bounded shape, and renders the reported 72-to-64 descent with the cited 8 M3 resonance sentinels and a 56-card floor; it does not compress an address locally. Correspondence face and the actual six-axis/sonic-overlay reader choices persist across face/layout remounts. The broader serializable record reserves its specified later-M2 controls, while kernel-owned coordinates remain in the shared tick store rather than a browser copy. Double-cover discipline pinned by tests: on `m2CymaticValenceInvert` the overlay shows the conjugate reading of the SAME address; `mirrorIdx 0xFF` renders explicit absence; the corpus stays an overlay, never a seventh 72-axis. No card invents a local provenance/readiness state. Contract-surface flag: [[M2'-SPEC]], [[S0-SPEC]], and [[M3'-SPEC]] need the additive receipt and the future M3 provider recorded during canon harmonisation.
- `src/panes/M2CorrespondencePane.tsx` — 23.T23.6 persists the vibrational/psychoid reader choice and consumes the seven-entry compiled [[M0']] psychoid correspondence through the typed M2 receipt; a planet outside the table has explicit canonical absence.
- `src/panes/omni/omnipanelRuntime.ts` + `src/panes/omni/OmniPendingPane.tsx` + `src/panes/TuningPane.tsx` + `src/state/useProfileTick.ts` — the OmniPanel `/` membrane foundation (Tranche 27.T27.0 and 38.T06.8, retargeted from the frozen omnipanel-shell): `OMNIPANEL_TABS` canonical 9-fold manifest (DR-WC-OP-1 collapse: `/ chat` → pi-chat, `logs` → tool-stream; all nine explicitly inhabit BOTH layouts; unlanded folds mount the honest pending pane naming their owning tranche 27.3/.5/.7/.8). `TuningPane` reads and writes only through the live `s5'.tune.*` registry/audit/lock methods; schema, validation, audit persistence, and Tier-2 proposal law remain outside the carrier. The module also owns the `epi-logos.layout.active` parser/filter used when `App.tsx` materialises the border, the shared run-model type-graph (`ActorIdentity`/`DispatchRoute`/`RunStatus`/`RunTreeNode`/`ToolStreamEvent`/`ReviewDecision`/`ReviewTransition`), `isMediationCapabilityAllowed` (exact + `prefix.*` permits, no hidden registry), `toToolStreamEvents` (pure temporal fold over the events ring), and `useProfileTick()` — the ONE 15.6 re-render seam (generation-gated zustand, no timers).
- `src/panes/omni/omnipanelSessionState.ts` — Track 27.T27.11's single typed snapshot for the eight `/` folds: active tab, per-tab state, and hidden/minimal/fullscreen state. `App.tsx` persists and hydrates it beside the two FlexLayout models, reflects actual right-border selection into it, and selects the same fold whenever the user crosses faces. Live Chat, Sessions, and Review folds write their owned state; pending fold records persist without fabricating a body.
- `src/panes/omni/omnipanelCapabilities.ts` + `ACR-MIGRATION-MAP.md` — the completed Track 27.T27.10 ACR-substrate migration boundary: the active carrier strict-consumes the live `s4'.mediation.capabilities.list` projection (membership + entitlement class), derives dispatch/stream capacity bindings, and keeps the executable Pi + Anima + six Aletheia-guardian roster distinct from the six non-dispatch Psyche aspect registers per DR-M5-1. Retired ACR run/evidence/review symbols map to `omnipanelRuntime.ts`, `evidenceShapes.ts`, `m5ReviewGate.ts`, and the carrier singleton gateway/event seams; `evidenceShapes.ts` mirrors the sixteen `m5_4_governance.mediated_run_evidence_bridge.packet_required_fields` and rejects absent profile, graph, session, semantic, or S5 evidence context. No ACR package or second runtime store exists here. Owning canon flag: [[S4-SPEC]] / [[M5'-SPEC]].
- `src/panes/autoresearchModel.ts` + `AutoresearchPane.tsx` + `qPairComposition.ts` — the Track 28.T28.10 M5' Autoresearch disclosure and [[Sophia]] pair-composition surface: strict real-wire consumers for `s5'.improve.status/history` and active-CF-filtered `s5'.improve.q_review.latest`, profile-tick refresh through the one shared hook, the six operational-capacity filter, persisted VAK queue disclosure, and the user-driven sequence `composeAphoristic` -> human-gated S5 review -> `s1'.q_articulation.accept`. The candidate stays editable; S5 approval and [[Hen]] remain the only canonical-write authority. The current S5 status contract does not project a recompose-pass ordinal or per-run human flag, so absence renders honestly; no mock fallback or local clock exists.
- `src/panes/kairosEnablement.ts` + `KairosEnablementPane.tsx` — the Track 32.T32.10 post-[[PASU]] optional [[Kairos]] ingress: FR-3 default-off local preferences, three-card informed opt-in, exact `nara.kairos.probe_kerykeion` dependency probe before persistence, real `nara.kairos.sync` refresh, durable `kairos.enable`/`kairos.skip` ledger, and inline graceful-unavailable state. The probe and pane never read natal data; only the resolved live-sky feed may enter `M4_Temporal_Now`.
- `src/panes/medicineView.ts` + `MedicineViewPane.tsx` — the Track 25.T25.10 `m4.nara.medicine` protected-local surface: strict `nara.medicine.snapshot` projection on the shared profile generation, eight-row chakra ladder with canonical element IDs and expandable body zones, active Sun-decan/body/ruler reading, vernacular + botanical evidence, `pratibimba.intent.dispatch` cross-link to [[Kairos]], and non-optimistic `nara.medicine.pin` persistence to NOW `c_4_pinned_materia`. It renders correspondence evidence only, never prescription or local balance law.
- `src/panes/omni/dispatchGenealogy.ts` + `DispatchGenealogyTree.tsx` + `DispatchGenealogyStream.tsx` — the dispatch-genealogy first-class primitive (rerun 15.T15.11, face-gap per DR-FACE-7): ONE flat dispatch-record dataset folds pure into the tree (Trace) and the time-ordered stream, same node ids in both foldings; deep-link descriptors target `omniEvidence`/`backendStudio`. Synthetic fixture is the acceptance (`dispatchGenealogy.fixture.ts`, Pi → Anima → Moirai); the wire→record producer is track-12's seam and the Dispatch TAB body stays 27.3's (`OmniPendingPane` wiring untouched until then).
- Composition contract law 4 (Tranche 16.T16.2 / CCT-2, DR-IG-5 VALIDATED 2026-06-03): a carrier consuming the `cymatic` mount MUST declare `surface: 'torus'` — the M2 cymatic contribution is pinned to the K² torus inside the cosmic composition; plate/sphere stay standalone-only and are refused at composition load (`src/engine/modulation/engine.ts::register`, `ModulationCarrier.surface`). The engine's `cymatic-skin` carrier declares the pin.
- `src/engine/couplingFlowOverlay.ts` + the Cosmic Engine strip / `M3ThirdSpandaPanel` — Track 07.T7.6's compact 137 coupling-flow disclosure: strict one-snapshot reader for the kernel's complete `couplingFlowAlignment` boundary (symbolic skeletons, physics-reference labels, measurement faces, recognition warrant, caveats). Incomplete or absent alignment stays `pending-coupling-flow-alignment`; the renderer computes no physics value and adds no fourth geometry pole. The real Chromium cosmic-face spec opens the disclosure against the spawned gateway and asserts the bussed source-warranted lanes.
- `src/commands/atelier.ts` + `src/panes/libraryProjection.ts` — CCT-19 activation (Tranche 16.T16.19; Library + Atelier are LENSES over what exists, never standalone surfaces): three Atelier commands operate on the currently-open note (`atelier.scentFollow` stages a Hen-promotion CANDIDATE via `s1'.entity.capture` — Atelier proposes, Hen reviews, canon-write follows the CCT-14 lifecycle; `atelier.cognateSearch` rides `s1'.semantic.suggest_links`; `atelier.psychoidTrace` rides `s0'.anuttara.trace`), deps injected for purity; the Library projection is a YAML-frontmatter coordinate-ancestry shelving lens (`buildLibraryProjection` — `uncoordinated` shelf honest and last) mounted by `FileTreePane`, while the Atelier cluster lens remains inside `GraphExplorerPane` over real S2 relations.
- `src/commands/crossLayoutIntent.ts` + `src/panes/omni/omnipanelRuntime.ts` + `src/state/crossLayoutIdentity.ts` — the Track 31.T31.10 / rerun 11.T11.2, 11.T11.6, 15.T15.7, 28.T28.14, and 29.T29.14 active-carrier cross-layout spine: one immutable 55-target ledger and strict `pratibimba.intent.dispatch` command preserve the nine-field [[CHROME-CONTRACT]] envelope, select the real FlexLayout host on its 0/1 face, promote daily/deep targets through the persisted `epi-logos.layout.active` UI preference, and apply context only through the existing coordinate/session stores. Every declared target reaches a mounted receiver with both requested ids intact; cross-layout OmniPanel targets preserve the current layout, and its nine folds filter by `availableInLayouts`. Each route atomically reads `(coordinate, lens, mode, profileGeneration, sessionKey, dayNow, omniPanelTab, activityBarMode)` before and after the layout mutation, fails closed on drift, and exposes the immutable receipt on the shell for runtime observation; the receipt owns no state. `tests/e2e/cross-layout-state-identity.spec.ts` proves both directions against a real gateway session/profile and filesystem-backed DAY/NOW.
- `src/commands/actionSurface.ts` + `src/commands/registry.ts` + `src/panes/CommandPalette.tsx` — Track 31.T31.11's executable action-contribution policy: `toolbar` contributions bind only to the active widget, `context-menu` contributions only to a selection, and `inline` contributions only to their artifact. The registry rejects mismatched scope declarations at registration; the global command palette lists only unbound commands because it cannot honestly supply a selected target or artifact. The policy owns no selection store, artifact identity, button rendering, or command semantics; [[CHROME-CONTRACT]] §10 is its live contract.
- `src/panes/m4NaraResonance.ts` + `src/panes/M4NaraResonanceSurface.tsx` — the M4' resonance + conjugate-form indicator surface (rerun 05.T5.1, [[M4'-SPEC]] §6.5): pure indicator/day-summary law (numeric + Major/Minor/Shadow; kernel wire spelling `ShadowInversion` normalises to `Shadow`; envelope `resonance` stamp is the truth, at-now profile `personalPole.resonance` fallback, `pending-resonance` when both silent — never fabricated) + the lean chip/strip renderers. Wired: `OraclePane` chip per deposited artifact, `NowPane` day strip (at-now indicator + aggregation over the day's envelopes; deposition does not stamp `resonance` yet, so artifacts aggregate pending — spec-ahead). No quaternion-dump: only label/character/counts reach the DOM; sourceHandle stays a handle reference (DR-M4-3). Computation law stays in [[Body/S/S0/portal-core]] `personal_identity.rs`.
- `src/panes/NaraCanvasEditor.tsx` + `src/panes/m4NaraHighlightMark.ts` + `src/panes/m4NaraHighlightService.ts` + `src/panes/NaraFloatingMenu.tsx` — the [[M4']] canvas-as-input and agent-inscription register (rerun 11.T11.10–11): an anchored day mounts the official Tiptap 3 Markdown editor in `NowPane`, byte-preserves C-family frontmatter, and writes Markdown through the real scoped vault IPC/debounced saver. The port preserves `setHighlight` / `unsetHighlight` / `toggleHighlight` / `extractHighlights`, exposes the four user categories only through the selection menu, and gives six agent categories distinct typed visual registers/CSS accents. `HighlightService.inscribeAgentMark` wraps an existing range without replacing user content, carries `protected_local` + `agent-chat` + `sourceFacet`, and publishes to the mounted editor through a component-local subscription rather than a fifth carrier store. Agent selection intents still need runtime dispatch/deposition. `tests/e2e/journal.spec.ts` proves typed content reaches real filesystem bytes and survives a reload; `m4NaraCanvas.test.tsx` proves all ten visual mappings and non-destructive agent inscription.
- `src/panes/NaraAmbientTuning.tsx` — the [[M4']] ambient strip + session tuning bar (rerun 11.T11.12): strict-reads optional kernel `personalPole.elementalBalance` in canonical Earth/Water/Air/Fire order and optional oracle spread-state counts; unavailable producers render `somatic/chakra/spreads pending`, never local substitutes. The bar locates the latest session `now.md`, structured-parses YAML, and persists only `c_3_tranche_mode`, `c_3_response_orbit`, and complementary `c_3_klein_weighting` through scoped `vault_write`; absent session NOW blocks controls. Alt+Shift+1/2/3 selects explicit/quiet/rhythm. `tests/e2e/nara-tuning.spec.ts` proves Chromium gesture → real filesystem bytes with body preservation.
- `src/engine/PersonalRecognitionEngine.tsx` — the Track 36.T36.5 default personal carrier in the `Now` tab: one [[M0]]/[[M4]]/[[M5]] composition consumes public `anuttaraPentadicTrace` scalars, the protected `qComposedHandle`, and bussed witness/checkpoint context; its protected rendering hook derives the [[Bimba]] `#q_b` and [[Pratibimba]] `#q_p` handles from that one composed handle and never accepts an independent raw bioquaternion body. Generic profiles carry the neutral [[M0]] verifier report (zero claimed virtues plus symbolic questions), while absent M4/M5 inputs render explicit pending or blocked states. `NowPane` remains the lived-day editor nested beneath this recognition surface.
- `src/panes/M5RecognitionLayer.tsx` — Track 26.T26.11's named `m5.epii.recognitionLayer`, mounted inside the personal composition's M5 leg (not alongside it): it reads only the protected-handle presence, bussed `personalPole.resonance`, latest `canonRecognitionStream` anchor, and M5 tritone coherence to render recognition strength, Tat-tvam relation, strongest V4 square, and canonical-close return readiness. Missing inputs stay individually pending; raw `Q_composed` never reaches its DOM.
- `src/panes/m4NaraOracleEnvelope.ts` — the M4' §5.11 oracle-envelope read-law (rerun 05.T5.11): normalizes both the snake_case Rust envelope wire (portal-core `src/nara/`) and the camelCase frozen-TS vocabulary; preserves oracle-frame/protein refs, deck context, sequence mode, packet refs, graph provenance, review state, scalar-ref kinds, interpretation HANDLE only (body never surfaces); `readingCardinality` = DR-VAK-1 positions authority (labels decorative); `projectableSystems` = tarot↔i-ching gated on `m3-codon` provenance; `pending-envelope` when unstamped — never fabricated. Wired: `OraclePane` envelope strip per deposited artifact (deposition does not stamp `envelope` yet — spec-ahead, owner `src-tauri/src/oracle.rs`).
- `src/panes/m5Ebm.ts` + `src/panes/M5EbmObservatoryPane.tsx` — the M5'-as-EBM observatory (Tranche 26.T26.1, retargeted from the frozen m5-epii widget body; 'M5' does not talk. It scores.'): pure view-model over the bussed `mathemeResonance72Projection` EBM fields (checkpoint ref, predicted/target 72-vectors, 4-dim q_p gradient verbatim; local arithmetic ONLY where the spec states the formula — E = ||target-predicted||^2, Moebius step -log(9/8)*grad, the three Klein-V4 square partitions A:(0,5)/B:(1,4)/C:(2,3)); 12x6 SVG grid + three tritone overlays (tokens tritoneSquareA/B/C) + energy/gradient/Moebius readouts; no checkpoint on the bus (10.M5 Wave-B) -> narrative + pending banner, grid suppressed per spec. 'M5 EBM' tab on the cosmic face.
- `src/panes/axiomTranslation.ts` + `src/panes/PiAxiomTranslationInspector.tsx` + `App.tsx` — Track 26.T26.14's DR-B-2 Pi axiom-translation read surface (`m5.epii.axiomTranslation`, cosmic-face "Axiom" tab). The strict parser reuses the landed `AxiomTranslationStep`/`AxiomForm` contract (evidenceShapes, 26.10), accepts only a contiguous Philosophical English → Formal Notation → OWL → SHACL chain, and a `pi|human|pending` session verification. The inspector reads `s5'.epii.axiom_translation_history` on connect and renders the four-column chain (each form cell + arrows), the verification badge (green human / amber pi / red pending), and per-step expandable reasoning; empty history is an honest empty state — it never fabricates a translation. The PRODUCER is the S0 gateway `gate::epii_axiom` (`s5'.epii.axiom_translate`) which runs the chain over the PI harness and persists sessions to `<state_root>/epii/axiom-translations/`; this surface is a pure read consumer. `tests/e2e/m5-axiom-translation.spec.ts` proves the read + four-column render against a real session seeded into the spawned gateway's store. Contract-surface flag: [[M5'-SPEC]], [[S3-SPEC]], [[S0-SPEC]].
- `src/components/M3CosmicWheelRenderService.tsx` + `src/components/QuintessenceIndicator.tsx` — the M3' alive-and-tarot-like default wheel (Tranches 24.T24.1/24.T24.11, retargeted from the frozen m3-mahamaya widget): single source of wheel rendering for all three modes (`badge`|`mini-view`|`full`); `buildM3WheelSurface` keeps geometry and values pure over codonRotationProjection + mahamaya + clock windows, while the shared M3 contexts add only generation/readiness stamps. 64-cell ring coloured by bussed codonClass via CL42_PALETTE/tokens, active cell luminous with rotation arrow, and 22 arcana SLOTS honest-pending (`pending-major-arcana-map`, WC-M3-SA-2). The centre strict-reads optional `mahamaya.chargeQuaternion`, renders four elemental petals proportional to the authority values plus a low-variance Akasha core, and surfaces the authority-provided 4X invariant failure without recomputing it; the current live profile remains `pending-charge-quaternion` until WC-M3-SA-3 emits. Mounted as the dominant zone of `M3InspectorsPane`.
- `src/panes/m3SurfaceContext.tsx` + the M3 wheel, I-Ching ribbon, hexagram browser, Third-Spanda panel, functional lens, and inspectors pane — Tranche 24.T24.15's Wave-C render spine: `M3ProfileTickContext` fans the shared generation-gated profile subscription into every M3 widget, while `M3ReadinessContext` supplies each named datum's ready/pending/blocked reason to an inline bordered chip/overlay. The pane owns the live binding map; isolated widget renderings use an honest state derived from their supplied data. No widget-local clock or separate error panel exists.
- `src/services/m3/` + `src/panes/M3InspectorsPane.tsx` — Tranche 24.T24.16's active-carrier renderer-service boundary: seven explicit symbols resolve fresh constructor-injected services over one narrow `M3GatewayPort`; no service is global and none constructs transport. `CosmicClockRenderService` and the 24.T24.18 `M3PentadicTraceService` are pure profile projectors, while Tarot/decan, hexagram/body, lens-aperture, walk-navigation, and oracle-cast adapters issue only their declared gateway methods. The mounted I-Ching ribbon exercises the landed `s5.oracle.iching.cast` route; the Tarot, aperture, walk, and scalar-reference adapters remain unmounted until their owning upstream capabilities/tranches land, with no LUT or local-behaviour substitute. This is the Tauri carrier's plain-TypeScript replacement for the frozen Theia/Inversify bindings, not a revival of them.
- `src/components/M3CompactViews.tsx` + `src/components/M3CosmicWheelRenderService.tsx` + `src/{App.tsx,components/StatusStrip.tsx}` — Tranche 24.T24.17's three-mode [[M3']] renderer projection: named `M3CodonChip` and `M3WheelMiniView` wrappers derive the same immutable authority surface as the full inspector from the existing tick store, retain the last received surface while the global choreography is paused, and rejoin the live edge on resume. The six-entry context strip nests the one-line badge in its coordinate entry, the daily cosmic face mounts the 64-cell mini-view, and `ide-deep` retains the full wheel; no fifth store or local codon/rotation computation exists. `tests/e2e/m3-renderer-modes.spec.ts` proves live badge/mini/full identity across both layouts and desktop/mobile containment against the spawned gateway.
- `src/components/M3PentadicRelationInspector.tsx` + `src/panes/{PentadicInspectorPane.tsx,m3PentadicInspector.ts}` — Tranche 24.T24.18's named read-only pentadic relation surface. Full mode renders the kernel-authored 0/1→5 hinge, Maxwell/Mahamaya fifteens, backbone/line-change identities, and active 72/64/codon/Q fields; mini mode renders one readiness-aware hinge badge from the same pause-held authority generation as the wheel. Missing strict trace or coupling-flow payload remains visibly pending through the shared provenance primitive. The component formats only; the parser/service owns no 72→64, codon, pairing, line-change, or Q derivation.
- `src/components/M3FibonacciGroundRing.tsx` + `src/components/M3CosmicWheelRenderService.tsx` + `src/engine/fibonacciGround.ts` — Tranche 24.T24.19's [[Fibonacci Ground]] outer envelope: 60 backend-authored digit wedges, four cardinal and eight zodiacal anchors, 24 C-backbone ticks, and independently pending natal/live Sun markers from backend-projected positions. The 2D wheel and 3D engine perform placement geometry only; no client Pisano table or Sun-degree quantisation remains. Contract-surface flag: [[M3'-SPEC]] / [[S0-ARCHITECTURE]].
- `src/components/CosmicClockRenderService.tsx` + `src/components/M3CosmicWheelRenderService.tsx` + `src/panes/M3InspectorsPane.tsx` — Tranche 24.T24.2's 385-node [[M3']] depth overlay: the full wheel composes 360 degree nodes, 24 backend-authored [[CLOCK_BACKBONE]] nodes, and one Axis Mundi from the shared profile tick, while the existing four-mode inspector switch changes only the declared projection mode. The component owns placement geometry only; it neither schedules a clock nor derives aspect/hop authority. Until dedicated profile fields land, `cosmicClock.aspectEdges` and `cosmicClock.hopEdges` remain visibly provenance-pending. `tests/e2e/m3-renderer-modes.spec.ts` proves the live spawned-gateway node counts, pending lanes, and mode switching. Canon update flag: [[M3'-SPEC]].
- `src/components/M3TranscriptionEngine.tsx` + `src/panes/M3InspectorsPane.tsx` + `src/bridge/types.ts` — Tranche 24.T24.20's profile-driven [[M3']] transcription surface. The strict bridge parser accepts only one coherent authority packet per functional lens id 0..16: three 2-bit nucleotide pairs and literal bits, canonical class, `pp/nn/np/pn` charge quaternion plus X/4X witness, six 64x6 line-change hops, compiled RNA capability, and the primary [[Fibonacci Ground]] address. The component formats one row per backend boundary and activates rows only from the shared profile `tick12`; it owns no biology, classification, clock, or lookup table. RNA-family and chromosome graph edges remain visibly pending, and the superseded id 17 aperture is refused. `tests/e2e/visual-panes.spec.ts` proves Ground 60 → Hourly 24 switching and live tick movement against the real spawned gateway. Contract-surface flag: [[M3'-SPEC]], [[S0-ARCHITECTURE]], and [[S3-SPEC]].
- `tests/e2e/m3-lens-switcher.spec.ts` — Tranche 24.T24.3's dedicated 16+1 functional-lens switcher UI-flow proof. The switcher itself (`m3-functional-lens` in `M3InspectorsPane`) and its Ground-default + Hourly-derived-through-Ground-16 resolution are already proven by `visual-panes.spec.ts:271-309` (as part of 24.T24.20); this spec adds the explicit guards the 24.3 brief demands — exactly 16 derived apertures + one primary Ground (`option` count 17, no 18-fold / Frame / Operator lens) and bidirectional switching back to Ground — over the real spawned gateway. Parser reject-17 lives in `src/bridge/types.test.ts`; the substrate in `portal-core::lens_codon_binary`; the spawned-gateway ids-7/16/17 edge in `epi-cli/tests/gate_m3_lens_codon_binary.rs`.
- `src/panes/M3ThirdSpandaPanel.tsx` + `src/panes/m3PentadicInspector.ts` + `src/engine/cosmicPentadicOverlay.ts` — corrected 22.T22.6 integrated [[M1]]→[[M2]]→[[M3]] runtime. The strict reader consumes `anuttaraPentadicTrace.thirdSpanda`; the engine exposes one generation's C M1 ring/advancement, six-axis M2 address, typed epogdoon block/collision/round-trip evidence, and M3 DET-reception/world-clock/codon-rotation state. The panel renders this process before static Mersenne/128/137 and physics-reference forms. No renderer math, M0 ownership, or generic gap/fold conflation is allowed.
- `src/panes/m1SessionCloseReader.tsx` + `src/panes/contemplationObjectViewer.tsx` + `src/panes/omni/ReviewBlocksPane.tsx` — active [[M1']]/[[M5']] Review-fold close readers. The M1 reader invokes registered `nara.session_close.read` only for the exact selected session and strict-parses the protected-local active-[[PASU]] aggregate. The M5 viewer independently invokes `nara.session_close.contemplation.read`, rejects any raw body field or unscoped payload, and renders only the opaque reference plus 4'-5'-0' aggregate counts/flags. Neither calculates closure locally, consumes raw contemplation/protein/[[Graphiti]] bodies, raw verifier constraints, or wisdom deltas, nor stands in for the cumulative [[M1]]→[[M2]]→[[M3]] Third-Spanda runtime. `tests/e2e/m1-session-close-reader.spec.ts` proves open→close→persist→read→bound Review rendering against the real gateway. Contract-surface flag: [[M5'-SPEC]] should record the additive protected contemplation reader during canon harmonisation.
- `src/engine/axisViews.ts` — the six-axes address-view decoder (Tranche 03.T3.3, replacing the frozen warehouse's six-identical-stubs): six DISTINCT per-axis source fields + arithmetic decodes verbatim from portal-core `RoutingAxisViews` (mef/tattva/decan/shem/maqam/det), per-axis [0,71] round-trip pinned; LUT-owned fields (decan rulers, maqam families, det64) declared `kernelSourced` — no renderer-local tables.
- `scripts/boot-smoke.mjs` — the behavioral launch gate (`pnpm smoke`); smoke harnesses resolve the repo-shared `target/debug/epi` pool unless `EPI_BIN` overrides it.
- `scripts/live-wire.mjs` — Track-00.T2 projection harness: real spawned gateway, strict Zod parse of every `profile.update`/`m123.chime` frame, declared `PROJECTION_MANIFEST` assertions (register yours when landing a profile field or gateway method), including B-12's live non-negative [[S2]] `graphRevision`, 21.T21.9's 12-slot `contemplationPromptLut`, 21.T21.15's exact 16-row `m0_void_structure_ring`, 22.T22.6's one-generation Third-Spanda cross-field/cardinality laws, and 36.T36.8's real `sessions.import` -> `sessions.patch` -> typed [[VAK]] languification heartbeat. The capture cannot complete without a trace-bearing profile; replay corruption is pinned by `scripts/live-wire-vak-languification.test.mjs`. Fixture dump to the rerun plan's `plan.runs/wire-captures/`. Canon update flag: [[M'-SYSTEM-SPEC]], [[S0-SPEC]], [[S3-SPEC]], and [[M0'-SPEC]].
- `scripts/gateway-method-audit.mjs` — Track-00.T5 recon probe: sweeps the cycle-3 corpus for named gateway methods, normalizes Markdown quote punctuation before token classification, probes each against a live spawned gateway, and writes `plan.runs/gateway-method-audit.json` (method · advertised · exists · responds · error-class).
- `scripts/lint-import-boundaries.mjs` — rerun 11.T11.5's active-carrier import gate. It reads the frozen `Body/M/epi-theia/extensions/contracts/07-t0-extension-contract-preflight.json` authority directly, validates all six M-extension rows, unions their `forbiddenDirectImports` with the shared-bridge list, then parses every real `src` module edge (static import, re-export, import-equals, dynamic import, `require`, and relative paths resolved against repo root). The unified carrier must reach S0/S2/S3/S5, SpaceTimeDB, Neo4j, Redis, portal-core, and Epii cores only through its bridge/gateway seam; no fictional extension directories are created. `pnpm test` runs this gate before Vitest.
- `scripts/lint-no-modal-discipline.mjs` — rerun 31.T31.8's no-modal gate (CC-08 / CCT-8, `pnpm lint:no-modal`, run by `pnpm test` before Vitest). 15.2's law is that the TAB IS THE LANDING SURFACE: a decision is taken in the surface that owns it, never in a floating layer that seizes the shell. Blocking native calls (`alert`/`confirm`/`prompt`/`showModal`, matched **receiver-agnostically** — `window.confirm`, `globalThis.confirm` and a bare `confirm` are one rule) plus the Theia modal APIs named by CCT-8 (`MessageBox.show`, `OpenDialog.show`, `ConfirmDialog.show`, `Dialog.show`) are findings **carrier-wide** — a deliberate widening of the frozen validator, which scoped by `review|evidence|gate-landing` PATH and matches only 5 of this tree's 503 files (all of `panes/omni/evidence/`), none of them the two real violations this tranche found. It reads the **TypeScript AST, never a regex over text** (the `catalog.test.ts` discipline): the first cut hand-rolled a comment stripper that desynced on a regex literal or a JSX apostrophe and then mis-read the rest of the file in both directions — blanking real code and un-blanking real comments. Those exact shapes are pinned as regressions. The structural warrant is `src/ui/shellSlotPolicy.ts` (31.T31.7): the shell has six slots and none of them is a modal slot. Non-blocking overlays (command palette, PASU wizard card, floating toolbars) are NOT findings — the law is about blocking, not z-index. The sanctioned replacement ships with the rule: `src/ui/InlineConfirm.tsx`. Fixture + real-tree proof in `scripts/no-modal-discipline.test.mjs`; behavioural proof in `tests/e2e/no-modal-discipline.spec.ts` (records Chromium's `dialog` event and asserts zero fired). Canon update flag: [[CHROME-CONTRACT]] §2 (the widened scope).
- `tests/e2e/port-sweep.mjs` + `tests/e2e/app-server.mjs` + `scripts/e2e-port-sweep.test.mjs` — the e2e orphan-port sweep (Track-00 harness hardening). A leftover listener on the dedicated app port 14621 was aborting whole `app-ui-flow` runs in under a second and reading as a shared-gate RED that no spec caused; it cost real gate runs across several tranches. **The sweep must run at playwright.config.ts LOAD, and nowhere later** — Playwright PROBES the web-server port and, with `reuseExistingServer: false`, aborts before it ever runs the web-server command, so a sweep inside the launcher cannot help; and `globalSetup` is also too late, because the web server is a plugin whose setup runs ahead of it. That ordering was established by observation (the failure carried no `[e2e-setup]` line), then confirmed by a launcher-based fix that still aborted. The config-load sweep is guarded on `TEST_WORKER_INDEX` being unset: worker processes re-evaluate the config file, and a sweep running there would kill the very server the run is using. `port-sweep.mjs` is the ONE implementation — `global-setup.ts` consumes it for the gateway/sidecar ports (its previous in-line copy is gone), the config consumes it for the app port, and `global-teardown.ts` sweeps all three on the way out. It is plain `.mjs` because `app-server.mjs` is executed by `node` outside Playwright's TypeScript transform. Two behaviours matter and both are pinned: it SIGTERMs, then **waits and escalates to SIGKILL** (the original in-line version returned while the old listener was still shutting down, handing the next bind an unexplained `EADDRINUSE`), and it **refuses to kill anything outside its allowlist** — a developer's own server on our port is a named failure, never a kill. `app-server.mjs` replaces `pnpm exec vite` as the web-server command so vite is a DIRECT child that Playwright's signal actually reaches, rather than a grandchild behind a package-manager wrapper — that addresses the leak at its source, while the config sweep is the recovery path. `scripts/e2e-port-sweep.test.mjs` is the regression gate: it spawns REAL listeners on REAL ephemeral ports and proves both directions — a recognised orphan is killed and its port freed, an unrecognised holder is left alive and running. Liveness is asserted through the child's own exit event, not `process.kill(pid, 0)`, which reports a not-yet-reaped zombie as alive.
- `tests/e2e/` + `playwright.config.ts` — the real-UI drivable-loop gate (`pnpm test:e2e`, Track-00 hardening; repo-gate suite `app-ui-flow`). **macOS constraint:** `tauri-driver` does not support macOS and Playwright cannot drive a Tauri shell, so the architecture is: Playwright drives the Vite face in real Chromium (dedicated e2e port 14621) against a REAL spawned `epi gate start` on e2e port 18933 plus a real-filesystem temp-vault sidecar (`scripts/e2e-vault-sidecar.mjs`, port 18934 — mirrors `src-tauri/src/vault.rs` semantics over one shared mkdtemp root; change vault.rs first, then the mirror). The gateway receives that same root as `EPILOGOS_VAULT`, so governed RPC reads and sidecar IPC reads observe identical real bytes. The Tauri binary itself stays covered by `pnpm smoke`. Global setup sweeps orphans on the e2e ports, ensures chromium, seeds real vault artifacts, and spawns gateway + sidecar; global teardown kills both and removes the temp roots. Specs include `boot.spec.ts` (live advancing tick), `journal.spec.ts` (real file bytes), `sessions.spec.ts` / `session-continuity.spec.ts` (session wire + 0/1 continuity), `cross-layout-state-identity.spec.ts` (11.T11.6 exact six-field daily/deep route receipts in both directions), `omnipanel-tab-traversal.spec.ts` (27.T27.13's all-fold real click traversal in both layouts and route-state carry), `chrome-command-routing.spec.ts` (31.T31.13's browser-proven palette and shortcut command routing), `autoresearch.spec.ts` (28.T28.10 real S5 status/history pane + governance disclosure), and `visual-regression.spec.ts` (deterministic 0/1 and composition baselines). Artifacts live in `test-results/` (gitignored).
- **Browser-mode seams (e2e only, never production):** `src/bridge/e2eShim.ts` installs `@tauri-apps/api/mocks` mockIPC forwarding every `invoke()` to the vault sidecar — dynamically imported by `main.tsx` ONLY when `VITE_E2E_TAURI_SHIM=1`; `defaultGatewayUrl()` in `gatewayClient.ts` honours `VITE_EPI_GATEWAY_URL` when set (injected by the Playwright webServer env), else the canonical 18794.
- `src/bridge/types.ts` + `src/bridge/gatewayClient.ts` — active-carrier kernel-bridge boundary and capability preflight list; cached profiles lift a validated optional `graphRevision` from the shared gateway envelope for B-12 cross-surface propagation. The boundary mirrors the Rust/Zod capability set, including the live C-backed `kernelBridge.m2.epogdoonProjection(address72)`, on-demand `kernelBridge.m3.lensCodonBinary(lensId)`, and `kernelBridge.m3.lensField(lensId)` projections. `parseLensCodonBinaryProjection` fail-closes on malformed 3x2-bit/class/hop/X witnesses and id 17; `parseLensFieldProjection` enforces parity topology, honest-null Akasha, and the pleroma-only symbolic-system law. Both accept only canonical `pp/nn/np/pn` charges and canonical-B elements; `ModalDigestStrip` remains visual-only and never creates browser audio APIs.
- Does NOT own: kernel computation ([[Body/S/S0/portal-core]]), gateway protocol ([[Body/S/S3/gateway-contract]] + epi-cli `gate/`), vault law (S1/Hen), M-domain law (`Idea/Bimba/Seeds/M/`).

## Local Contracts
- Owning specs: [[M'-SYSTEM-SPEC]] (umbrella + carrier decision), [[M'-PORTAL-SPEC]] (0/1/`/` grammar), per-domain [[M4'-SPEC]] etc. Active plan: [[2026-07-02-pratibimba-app-phase-1]] — its **Binding protocol** section (verifier ≠ closer; behavioral proof; drivable-loop gates) governs all work here.
- One clock: no `setInterval`/rAF-as-clock anywhere; the profile generation gates re-render.
- Privacy: no private journal/identity bodies in stores or events — handles only.
- Privacy-class chrome (25.T25.18, `src/ui/privacyChrome.ts`): every Wave-C M4' surface declares at mount WHICH KIND of material it renders — `protected_local` / `protected_local_handle_only` / `shared_archetype_opt_in` — and wears the matching `mext-privacy-*` border tint plus a title carrying the full PRIVACY_CLASS string. **Classifications are READ OFF THE SPEC, never inferred:** the Track-25 brief assigns a privacy chrome on each widget's own line ("Privacy chrome: …"), and `SPEC_ASSIGNED_SURFACES` quotes that line with its line number per row. A first cut inferred them from pane headers and got three wrong and three missing — a privacy class is a claim about what happens to someone's data, so it is cited, not judged. `CARRIER_EXTENSION_SURFACES` is kept SEPARATE for M4' surfaces the brief never classified (other tracks); those carry carrier judgment quoted from their own header and are flagged for the Architect. `SPEC_EXEMPT_SURFACES` pins the brief's one explicit exemption (25.16, "Privacy chrome: none — indicator only, no body") so it stays untinted. `privacyChrome.test.ts` holds all three groups in lockstep with the real sources both directions, scanning **`src/panes` AND `src/components`** (25.17 lives in components — a panes-only scan was a real hole), and pins the CSS↔JS token duplication. The indicator lives on surface chrome and NEVER in the status bar — 15.10 admits exactly six state threads — proven by that test and by `tests/e2e/privacy-chrome.spec.ts`. **`DR-WC-M4-5` is ROUTED, not RATIFIED** (border colour vs glyph vs both; `13-decision-register.md:2084`, user final-validation pending): this builds the border-colour option the brief specifies and may NOT be cited as validating that DR — a prior close of 25.T25.18 was reset as fraudulent for exactly that claim. `shared_archetype_opt_in` has a defined rule but no surface: the brief assigns it to none, and force-fitting a personal surface into the shareable class would be the misclassification with real consequences. **AMENDED BY 25.T25.21** (found while building the Wave-C register, fixed in the same pass rather than filed): `panes/JournalTimelinePane.tsx` (25.3, SPEC:83) and `panes/OraclePane.tsx` (25.8, SPEC:133) are spec-assigned surfaces that EXIST and were wearing no tint at all — an unclassified surface is not a neutral one, it tells the user nothing about what it is showing them. Both now carry `protected_local` with their quoted line. Separately, a NESTED SECTION may be STRICTER than its host pane and wear its own tint: the day container (`DayCalendarPane.tsx`) renders per-artifact handles and roles, which SPEC:75 assigns handle-only, inside a pane SPEC:69 assigns protected-local — inheriting the weaker tint was a live misstatement about what is on screen. The register stays keyed by FILE; the per-row class lives in `waveCContributions.ts`, whose gate asserts the two agree except across that nested-section boundary. Canon update flag: [[M4'-SPEC]].
- Onboarding walkthrough (32.T32.3, `src/onboarding/walkthrough.ts` + `WalkthroughOverlay.tsx`): the six-step post-identity walk (WC-OB-19). Its six step ids and skip-preference path are NOT invented — they are the `walkthrough.*` rows `contracts/onboarding-completion-ledger.json` (32.T32.13) already declares. **The step copy is GENERATED from the live inventories** (`OMNIPANEL_TABS`, `LEFT_SIDEBAR_MODES`, the six status threads), so adding a tab cannot leave the walkthrough describing an app that no longer exists; `walkthrough.test.tsx` proves each step names the real inventory. NON-MODAL by construction (CCT-8): the backdrop is `pointer-events: none`, the card carries `role="group"` not `dialog`, and `tests/e2e/walkthrough.spec.ts` proves it operationally by flipping the 0/1 face and clicking the status bar WHILE the walkthrough stands. Anchors are ordered CSS candidates resolved against the real DOM; four point at chrome that exists (`face-toggle`, `status-strip`, `status-daynow`, `status-coordinate`), step 2 at the flexlayout border that IS the OmniPanel here, and **step 3's Theia activity-bar has no carrier equivalent** (`registerLeftSidebarModeCommands` is proven dead by `catalog.test.ts`) so it falls back to a centred card — an honest gap for whichever tranche lands the activity bar, not a silent one. Two layout invariants are e2e-guarded because both were real defects caught there: the card is CLAMPED into the viewport (anchoring below a full-height target put the Next button off-screen and trapped the user on step 2), and the spotlight's declared step travels with the box it measured (it briefly painted the previous step's target while claiming the new one). Escape dismisses and writes NEITHER array — dismissing is not a decision about any step. Re-entry is `epi-logos.help.openWalkthrough` in the palette (catalogued), which the 32.4 Settings "Replay" action will reuse. Canon update flag: [[CHROME-CONTRACT]].
- Carrier token discipline (Track 30): raw hex colours, raw font-size px, and raw ms durations live ONLY in the two token sources — `src/styles.css` (CSS custom properties) and `src/ui/tokens.ts` (JS-side named tokens for canvas/Three.js/force-graph consumers; tokens NAME values, they don't redesign them — DR-WC-DL-1 is no longer open, it was VALIDATED 2026-07-23 and its derivation is written up in `contracts/ui-family-palette-derivation.md`). Every other src file imports a named token. Enforced by `node .codex/scripts/lint-carrier-tokens.mjs` (repo-gate suite `carrier-tokens`); `*.test.*`/`*.spec.*` files are exempt — tests assert produced values, they don't render.
- Bell law ([[m123-modal-resonator-bell-kernel-spec]]): all pitch from the kernel bus; bell-partial role labels from the kernel (`modalResonator.bellPartials` on profile/chime) — `BELL_ROLE_CHOREOGRAPHY` in `cosmicMath.ts` is envelope choreography per role, and the local role ORDER is fallback only (`bellSource` reports which). The `m123.chime` event is the strike authority when live (`createStrikeRouter` in `audio/instrument.ts`: one strike per generation, incoherent chime → blocked strike, dead chime stream → profile route recovers); it is liveness-class and never enters the log ring (`eventsStore.ts`).

## Work Guidance
- Widget/UI parts are PORTED from the frozen warehouse `Body/M/epi-theia` (React internals, `src/common` models) — shed Theia wrappers; keep provenance lines pointing at the source file.
- Test IDs use `data-testid`. Tests are behavioral (jsdom mount / fake-socket wire / spawned real gateway); manifest-string assertions close nothing.

## Verification
- **Repo gate (Track 00 law):** `node .codex/scripts/verify-all.mjs` from the repo root — includes this app's typecheck/test/build plus live-wire, gateway-methods (this app's `scripts/gateway-method-audit.mjs` probe run under `.codex/scripts/gateway-method-gate.mjs` with the expected-present ratchet, T11), honesty-lint, and kernel-truth stages, and the full offline backend (T8) + S4 ta-onta (T9) suites. No rerun tranche closes while it is red; `node .codex/scripts/verify-tranche.mjs <TASK_ID>` records the independent verification.
- `pnpm typecheck && pnpm test && pnpm build`
- `cargo test` + `cargo check` in `src-tauri/`
- `pnpm smoke` — spawns a real `epi` gateway and requires an advancing profile tick (repo-gate suite `app-smoke`)
- `pnpm test:e2e` — Playwright real-UI drivable-loop specs in Chromium against a real spawned gateway + real-fs vault sidecar (repo-gate suite `app-ui-flow`)
- `node scripts/live-wire.mjs` — full projection-manifest pass over a real spawned gateway
- Sprint gates: real launch (`pnpm tauri dev`), observed/recorded.

## Child DOX Index
- (none yet)
