# 12.13 Shared-Intelligence Seam Runtime Audit

Task: 12.T12.13 - [[S4]] <-> [[S5]] shared-intelligence seam runtime audit, cross-linked to Tranche 10.x resonance72 and Tranche 09.x cross-namespace edges.

## Summary

- `GEMINI_EMBED_DIMS=3072` is true for [[Bimba]] via [[S2]] graph schema/services and dimensionally aligned for [[Gnosis]]/[[Graphiti]] via `GNOSTIC_EMBEDDING_DIM=3072`; it is not a single env var end-to-end.
- `RELATES_TO_COORDINATE` is not the live [[Gnosis]] enrichment edge name in `test_enrichment`; code and test use `MAPS_TO_COORDINATE`, while [[Aletheia]] canon prose still names `RELATES_TO_COORDINATE`.
- `MathemeHarmonicProfile.resonance72` is consumed by [[S5]] autoresearch resonance EBM, and [[Aletheia]] Gnosis-RAG is Pi-routed, but no direct code path proves `resonance72` is consumed by Aletheia Gnosis-RAG via [[Pi]].
- The local ACR package now preserves the canonical actor split: `AgenticActor = pi | anima | aletheia`, with the six [[Aletheia]] guardians carried as `AletheiaTechneClass` / `techneClass`, not as first-class actors.

## A. Embedding Dimension Seam

Evidence:

- [[Bimba]] graph schema pins `SEMANTIC_EMBEDDING_DIMENSIONS = 3072` and the `coord_embedding` vector index at 3072 dimensions in `Body/S/S2/graph-schema/src/lib.rs:105` and `Body/S/S2/graph-schema/src/lib.rs:2811`.
- [[S2]] graph-services reads `GEMINI_EMBED_DIMS` / `GEMINI_EMBEDDING_DIMS`, defaulting to full resolution in `Body/S/S2/graph-services/src/embeddings.rs:27`.
- [[Gnosis]] config defaults `GNOSTIC_EMBEDDING_DIM` to 3072 in `Body/S/S5/epi-gnostic/epi_gnostic/config.py:32`, and `GraphitiConfig` mirrors that default in `Body/S/S5/epi-gnostic/epi_gnostic/graphiti_config.py:39`.
- [[Gnosis]] passes `self.config.embedding_dim` into LightRAG `EmbeddingFunc` and Neo4j vector-storage kwargs in `Body/S/S5/epi-gnostic/epi_gnostic/wrapper.py:67` and `Body/S/S5/epi-gnostic/epi_gnostic/wrapper.py:91`.
- Neo4j vector storage derives its dimension from the embedding function, defaulting to 3072 when unset, then creates the vector index from that dimension in `Body/S/S5/epi-gnostic/epi_gnostic/storage/neo4j_vector.py:59` and `Body/S/S5/epi-gnostic/epi_gnostic/storage/neo4j_vector.py:91`.
- [[Aletheia]] contract states the intended unified value as `GEMINI_EMBED_DIMS=3072` in `Body/S/S4/ta-onta/S4-5p-aletheia/CONTRACT.md:153`.

Audit result: dimensionally aligned at 3072; env-var naming is split (`GEMINI_EMBED_DIMS` for [[Bimba]]/[[S2]], `GNOSTIC_EMBEDDING_DIM` for [[Gnosis]]).

## B. Cross-Namespace Edge Seam

Evidence:

- `CoordinateEnricher.assign_direct` documents and creates `MAPS_TO_COORDINATE`, not `RELATES_TO_COORDINATE`, in `Body/S/S5/epi-gnostic/epi_gnostic/enrichment/coordinator.py:64` and `Body/S/S5/epi-gnostic/epi_gnostic/enrichment/coordinator.py:90`.
- `test_cross_namespace_edge_created` asserts `MAPS_TO_COORDINATE` in `Body/S/S5/epi-gnostic/tests/test_enrichment.py:186` and `Body/S/S5/epi-gnostic/tests/test_enrichment.py:202`.
- [[Aletheia]] prose says [[Gnosis]] chunks are cross-linked to [[Bimba]] via `RELATES_TO_COORDINATE` in `Body/S/S4/ta-onta/S4-5p-aletheia/CONTRACT.md:136`.

Audit result: code and test currently land `MAPS_TO_COORDINATE`; the task text and Aletheia prose expecting `RELATES_TO_COORDINATE` are not matched by live enrichment code.

## C. resonance72 Consumption Seam

Evidence:

- [[S5]] autoresearch EBM defines `CHANNEL_LENS_RESONANCE_72` and consumes `profile.resonance72` in `Body/S/S5/epii-autoresearch-core/src/resonance_ebm/channels.rs:6`, `Body/S/S5/epii-autoresearch-core/src/resonance_ebm/channels.rs:31`, and `Body/S/S5/epii-autoresearch-core/src/resonance_ebm/channels.rs:58`.
- [[S5]] Q-review harvests cross-namespace resonance edges into `gnosis://resonates_with/...` evidence refs in `Body/S/S5/epii-autoresearch-core/src/q_review.rs:387`.
- [[Aletheia]] registers Gnosis tools as PI-native tool surfaces (`aletheia_gnosis_ingest`, `aletheia_gnosis_query`, notebook, crystallise, Moirai distill) in `Body/S/S4/ta-onta/S4-5p-aletheia/CONTRACT.md:15` and `Body/S/S4/ta-onta/S4-5p-aletheia/CONTRACT.md:59`.
- [[Aletheia]] spine query dispatches through `epi techne gnosis query-gnostic ... --mode hybrid` in `Body/S/S4/ta-onta/S4-5p-aletheia/spine-contribution.ts:65`.
- [[Pi]] owns capability parity at startup, not the ACR package, in `Body/S/S4/pi-agent/composite-entry.ts:30`.

Audit result: `resonance72` is consumed by [[S5]] autoresearch, and Aletheia Gnosis-RAG is Pi-routed, but there is no direct handoff showing Aletheia Gnosis-RAG itself consumes `MathemeHarmonicProfile.resonance72`.

## Verification

- `pnpm --dir Body/M/epi-theia/extensions/agentic-control-room test` passed: 22 tests, 22 pass.
- Requested `pytest Body/S/S5/epi-gnostic/tests/test_enrichment.py::test_cross_namespace_edge_created -q` could not run because `pytest` is not on PATH.
- `python3 -m pytest Body/S/S5/epi-gnostic/tests/test_enrichment.py::test_cross_namespace_edge_created -q` found pytest but failed collection until `PYTHONPATH=Body/S/S5/epi-gnostic` was supplied.
- `PYTHONPATH=Body/S/S5/epi-gnostic python3 -m pytest Body/S/S5/epi-gnostic/tests/test_enrichment.py::test_cross_namespace_edge_created -q` executed the test but failed at real Neo4j Bolt connection with `PermissionError: [Errno 1] Operation not permitted` for `localhost:7687` in this sandbox.
- Requested `grep -rn 'EMBED_DIMS\|embedding_dim' Body/S/S5/epi-gnostic/` returns many matches because it traverses `.venv`, `__pycache__`, and tests; filtered source scan (`rg -n "EMBED_DIMS|embedding_dim" Body/S/S5/epi-gnostic -g '!uv.lock' -g '!*.pyc' -g '!__pycache__/**'`) shows the intended source paths above.

## Closure Notes

- Contract-surface change: `AgenticActor` remains collapsed to the canonical first-class actors, while `AletheiaTechneClass` carries guardian specificity. Owning spec to update/confirm: [[M5'-SPEC]] / [[M5-ARCHITECTURE]] around M5-4' Pi runtime monitor framing.
- Follow-up needed outside this write scope: decide whether live cross-namespace edge law should be `MAPS_TO_COORDINATE` or `RELATES_TO_COORDINATE`, then update [[Aletheia]] contract prose or [[Gnosis]] enrichment code/tests accordingly.
- Follow-up needed outside this write scope: decide whether [[Gnosis]] should also accept `GEMINI_EMBED_DIMS` as an alias for `GNOSTIC_EMBEDDING_DIM` to make the embedding-dimension seam literally end-to-end under one env var.
