# Prompt E — Six Deep M′ Product Readiness Receipt

**Status:** first E application-contract tranche; not full deep-instrument completion  
**Programme:** Epi #20 · QL-MEF #73 · O:I #115  
**Eightfold source authority:** Epi PR #19 / `design/epi-eightfold-product-architecture`  
**Implementation:** Epi PR #21 / `agent/epi-pratibimba-e-deep-contracts`

## 1. Parent state consumed

The corrected parent owner tickets are still open at this cut:

```text
C — epi.personal.450
    Epi #18 + O:I #112

D — Current Situated Matheme + epi.cosmic.123
    QL-MEF #69 + Epi #17 + O:I #101
```

Prompt E therefore consumes their **identity law**, not a fabricated completion:

```text
existing parentProductId
+ existing eventRef and/or subjectRef
+ exact MCoordinateRef
+ existing provenance
```

Historical Personal/Cosmic implementations remain evidence only. PRE-D coordinate/relation parity remains source/conformance evidence, not parent application readiness.

## 2. Common deep-open protocol

Canonical Epi Actions:

```text
epi.action.deep.open
epi.action.deep.focus
epi.action.deep.return
```

Invariant:

```text
existing parent event/subject + MCoordinateRef
        ↓ open
source-derived deep descriptor
        ↓
declared heterogeneous native body
        ↓ O:I presentation-only SurfaceRef
same semantic anchor/readiness/provenance
        ↓ return
exact parent anchor
```

The resolver fails closed on missing parent identity, parent-product drift, M′ drift, missing provenance, undeclared bodies, and deep M4 without protected-local authority. It never mints event, subject or provenance identity.

## 3. Per-M boundary expression

| Product | Parent | `.0` parent ground | `.5` return/exposure | `.1-.4` parent-summonable | Deep-only current composition | Native body/provider | Status |
|---|---|---|---|---|---|---|---|
| `epi.deep.m0` Anuttara | Personal | M0-0′ Bimba language/source | M0-5′ pedagogy/cartography/Möbius return | QL structure; relations/provenance; community/time; Personal bridge | full six-layer graph/cartography, governed mutation, GDS/community exploration | graph/declarative: M0 inspector + S2 graph services/schema | **PARTIAL** |
| `epi.deep.m1` Paramaśiva | Cosmic | M1-0′ immutable harmonic/QL source | M1-5′ played topology, Hopf/SU(2), 4π | current walk; selected Ananda/harmonic; Spanda tick; QL flowering | Ananda vortex field, K² played torus, quaternionic/Hopf/720° world | declarative + GPU/audio: M1 clock + played-torus + portal-core Hopf | **PARTIAL** |
| `epi.deep.m2` Paraśakti | Cosmic | M2-0′ 72 vibrational source | M2-5′ solar-chakral + 72→64 DET | resonance/readiness; element; decan; selected sacred-sonic relation | full 72/six-axis field, material cymatics, tuning-aware sacred-sonic lab | audio/cymatic/GPU: M2 + Vimarśā + 72 LUT | **PARTIAL** |
| `epi.deep.m3` Mahāmāyā | Cosmic | M3-0′ 72→64 reception | M3-5′ K²×T² world-clock / total inscription | current rotation; lens-mode projection; clock; selected Tarot/I-Ching/transcription | full 472 field, 360/720 + 384 line-change graph, double-torus/coupling depth | clock/symbolic/GPU: M3 + codon projection + clock LUT + Mahāmāyā | **PARTIAL** |
| `epi.deep.m4` Nara | Personal | M4-0′ protected identity/source evidence | M4-5′ Personal integration + Epii/Sophia review | safe somatic readout; oracle; flow/trajectory; protected journal/Graphiti handles | Q_identity/Q_transit/Q_activity/Q_composed, #4.4.4.4, sushumna/chakra/elemental/Hopf psychoid-cymatic body, raw protected traversal | protected-local/GPU: M4 + personal identity + Nara journal + Graphiti | **PARTIAL** |
| `epi.deep.m5` Epii | Personal | M5-0′ gnostic/canon/library | M5-5′ Logos/recognition/Möbius return | canon/source; bounded backend explain; bounded Surface explain; canonical Epii dialogue/review | full backend/frontend studios, agentic/autoresearch workbench, Logos Atelier | agentic/editor/graph: M5 + S5 gnostic/review/Epii providers; generic Agency/session stays Actuation/AIKit-owned | **PARTIAL** |

`PARTIAL` is deliberate: substantial native domain bodies/providers exist, but the corrected parent→O:I→deep→parent application relation is not yet accepted. Historical Theia composition does not upgrade status.

## 4. Evidence

### Source

Every descriptor records the common eightfold authority plus that M′ domain's own SPEC/architecture. Boundary expressions are separately derived rather than copied from one generic answer.

### Coordinate / relation

Existing PRE-D M↔M′ parity remains the coordinate/relation source ground. Prompt E uses exact existing M′ identity and does not mint a new coordinate namespace or reinterpret implementation routing as Bimba relation canon.

### Operational

Current Epi source already contains real bodies/providers across all six domains: M0 graph/inspector; M1 clock/played torus/Hopf; M2 Vimarśā/72-space; M3 codon/clock; M4 protected identity/journal/Graphiti; M5 gnostic/review/agent-oriented S5 providers.

Prompt E adds the application relation above those bodies, not a replacement renderer stack.

### CI

Focused workflow: `.github/workflows/epi-deep-products.yml`.

Returned sequence:

1. run `32297298873` failed at TypeScript build because the three Action constants were widened to the `DeepActionRef` union;
2. constants were corrected to literal types without compiler suppression or weakening the request/resolution contract;
3. exact-head run `32297653007` completed **SUCCESS**.

The green lane installs the pinned workspace, builds `@pratibimba/m-extension-runtime`, then runs `extensions/test/deep-product-contract.test.mjs`.

## 5. Parent→deep→parent identity / Agent-native tests

Acceptance proves:

```text
parent eventRef / subjectRef
    == deep resolution anchor
    == bound deep Surface semantic anchor
    == return anchor
```

It also proves:

- a subject-only parent remains subject-only; depth does not mint an event;
- provenance is preserved;
- parent/coordinate/provenance/body drift fails closed;
- deep M4 rejects non-protected authority;
- all M0′–M5′ state their `.0`, `.5`, `.1-.4` parent-summonable and deep-only cuts;
- human native Surface and structured Agent projection share the same Actions, refs, readiness and provenance;
- DOM scraping is not part of Agent access;
- selection never confers disclosure.

Full cross-repository parent→O:I host→native deep body→parent acceptance still depends on corrected C/D and the current O:I host line.

## 6. Surface Ledger cut

| Product | Human disposition | Agent disposition | Current state |
|---|---|---|---|
| M0′ | NAVIGATOR / CANVAS / ALTERNATE-NATIVE graph | structured deep Actions + refs | PARTIAL |
| M1′ | CANVAS / ALTERNATE-NATIVE GPU/audio | same deep Actions + refs | PARTIAL |
| M2′ | CANVAS / ALTERNATE-NATIVE audio/cymatic/GPU | same deep Actions + refs | PARTIAL, provider degradation explicit |
| M3′ | CANVAS / ALTERNATE-NATIVE clock/world | same deep Actions + refs | PARTIAL |
| M4′ | protected CANVAS / ALTERNATE-NATIVE local | protected Actions + opaque handles only | PARTIAL; selection != disclosure |
| M5′ | SIDECAR + CANVAS / ALTERNATE-NATIVE editor | canonical Epi Agent/Agency + same deep Actions | PARTIAL; consume Actuation/AIKit session/runtime |

O:I #105 owns the missing professional host mechanics. Epi must not rebuild tabs/splits/SessionSpace/AgentSession/Search/Navigator to make Prompt E appear complete.

## 7. Returned reality / architecture revision

No returned fact in this tranche requires changing the Eightfold Vision or Application Architecture.

It sharpens three readings:

1. the historical six M-extension packages are substantial native-body evidence, but the historical Theia shell is not generic host authority;
2. the six products are **not equally ready** even though their common application contract is now proven—M3 has rich substrate, M0 retains a six-layer composition gap, M2 material audio/cymatics remains degradable, and M4/M5 governance boundaries are product substance rather than polish;
3. corrected C/D runtime acceptance has not returned, so Prompt E can advance each deep body independently but cannot truthfully claim `OPERATIVE` or `EXPERIENTIALLY_ACCEPTED` parent/deep loops yet.

If corrected C/D later return a different event/subject/authority shape, that is an explicit architecture/conformance revision signal and this contract must change visibly rather than adapting silently.
