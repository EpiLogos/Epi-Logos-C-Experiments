// Shared MonoPoly state vocabulary for 37.T37.5.
// M0's archetypal MonoPoly render and M2's cymatic projection import this
// exact string enum so both surfaces speak one behaviour-state dialect.
export type MonoPolyState = 'mono' | 'actually-many' | 'actualising-one' | 'monopoly';
